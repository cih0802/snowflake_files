> #### 🟢 [2026-08-28 O107] 허브 목차 「조각 선택표」 신설 + `--republish` 허브 전체 재구성 + 절 단위 은퇴 도구 신설
>
> **인수** = `99_NEXT §0-LLL ▣GGG` 중 사용자가 「①+② 순차」로 지시.
> **라벨** = `--next O` 정의 106 ↔ 참조 106 ⇒ `O107`.
> 🔴 **`R1-4-3` 위반 1건 자기신고** — 선점 등재를 **착수 시점이 아니라 작업 후에** 했다.
> 충돌은 없었지만 조문이 요구하는 것은 「먼저 등재」이므로 위반이다.
>
> **① 허브 목차 → 조각 선택표**(▣GGG ①)
> * 🔴 **문제의 정체를 다시 진단했다.** 인수인계는 「선두 절이 `(제목 없는 선두 구간)` 이라
>   조각을 고를 수 없다」고 적었는데, 실제 원인은 **절이 없는 것이 아니라
>   앞 조각에서 시작한 절이 이어지는 것**이었다(§1 대시보드 표가 조각 경계에서 잘린다).
>   ⇒ 「없다」가 아니라 **`(이어짐) §…`** 로 적으면 그 자체로 선택 가능해진다.
>   `enclosing_title()` 신설 · 종전 문구는 **원리적으로 나오지 않는다**(음성 테스트 축7-f).
> * 🟢 `split_doc.py` 가 허브에 **조각별 포함 절 전체 목록 + 포함 이슈 ID 목록**을 쓴다.
>   캡 = 절 30 · ID 80 · 초과분은 **「… 외 N건」**(🔴 「전량이 아니다」를 명시 — 이 표를 근거로
>   「그 조각에 없다」고 단정하면 판정 오류다) · 한 줄 목표 600자(실측 max 594자).
>   `entry` 모드(이력·로그)는 **절 목록을 싣지 않는다** — 항목 제목이 길고 수가 많아
>   허브가 수십 KB로 커진다(선택자는 세션 라벨이다 · `R1-3-6` 축).
> * 🔴 **`R\d+` 을 ID 에서 제외**했다 — 조문 번호는 이슈 ID 가 아니고 모든 조각에 흔해서
>   목록을 무의미하게 만든다(음성 테스트 축2-b 가 이 오탐을 고정한다).
> * 🔴🔴 **`--republish` 자체에 결함 2건이 있었다**(이 작업의 실제 병목):
>   종전 구현은 **해시 1줄만 정규식 치환**했다 ⇒ ㉠ 조각을 편집하면 목차의 줄·KB·구 행범위가
>   **조용히 stale** 이 됐고(`R3-9 ㉦` 「수를 문서에 박으면 stale 이 된다」와 **같은 축**이며,
>   허브가 자동 생성물인데도 **자동 갱신되지 않는 구간**이 남아 있었다)
>   ㉡ 목차 형식을 보강해도 **반영 경로가 없었다**.
>   ⇒ `build_hub` 로 **허브를 통째로 재구성**한다(조각은 바이트 불변 · 재작성 전
>   `_archive/*.O107-prehub` 스냅샷 · `R1-7-6` 되돌림 경로 확보).
> * 🟢 **허브 10종 전건 재구성** = rc 0 · `--verify` 게이트 6종 PASS · 제목 유실 0 ·
>   표행 증감 0(스냅샷 대조 3종) · 허브 최대 26,605B(여유 ≥14,325B).
> * 🟢 **골든 재발행 1회** = `doc_heading_gate --update-golden`(제목 1,076 → 1,086 ·
>   신설 10 = 허브당 「조각 선택표」 1절 · **유실 승인 0건**).
>
> **② `retire_sections.py` 신설**(▣GGG ②)
> * 🔴 **인수인계가 지적한 위험**(절은 `§번호`로 인용되므로 행 키보다 위험)을 **설계로 제거**했다:
>   **절을 옮기지 않고 「본문」만 옮긴다.** 제목 줄을 **하위 제목까지 한 줄도 건드리지 않으므로**
>   `§` 인용 좌표와 `doc_heading_gate` 제목 집합이 **원리적으로 보존**된다.
>   ⇒ `retire_rows.py` 가 「행을 지우지 않고 셀만 비운다」로 `R1-7-4` 와 공존한 것과 **같은 설계**다
>   (행 키 ↔ 절 제목이 대응한다). 🟢 **골든을 덮을 유혹 자체가 생기지 않는다.**
> * 계약 = **좌표 대응표**(원본 파일·행범위·lv·절 제목·본문 SHA256) + `R2-8-1` 토큰 대조
>   **blocking** + 양쪽(+꼬리 조각) 스냅샷 + **dry-run 기본** + 자가검증 3축(제목 줄 전건 동일 ·
>   원본/목적지 2,000자 초과 0) + **멱등 차단**(이미 은퇴된 절 재은퇴 시 중단 — 빈 이관 방지).
> * 🟢 **실제 문서로 dry-run 실증** = 문서50 `-008` §O64-C · 토큰 18종 부재 0 ·
>   절감 1,205B · 제목 6개 전건 불변.
> * 🟠 **실제 적용은 하지 않았다** — 다중 파일 재작성이므로 `R4-4-3` 별도 승인 대상이다.
>
> **🔴 자기결함 3건**(2건은 음성 테스트가 적발)
> * ㉠ **목적지 기본값이 허브였다.** `retire_rows` 에서 물려받은 `90_해소완료_로그.md` 는
>   O106 이 **폴더 분할**해 이제 **자동 생성물**이다 ⇒ 거기에 append 하는 코드는
>   다음 `republish` 에서 **조용히 사라진다**(`R3-9 ㉧` · 이 워크스페이스가 4번 당한 유형).
>   ⇒ 허브 감지(`dst_is_hub`) 후 **`--rollover` 꼬리 조각** 경로로 바꾸고,
>   토큰 대조 분모도 **조각 concat(논리 본문)** 으로 교정했다(허브만 보면 전건 부재로 항상 FAIL).
>   🟢 **일반화**: **도구를 물려받을 때 「그 목적지가 아직 그 성격인가」를 재라** —
>   문서 성격 변경은 **다른 도구의 전제를 조용히 깬다**.
> * ㉡ **캡만으로는 줄 길이가 담보되지 않는다** — ID 199개는 캡 80 이면 566자로 한 줄에 들어간다.
>   🟢 **일반화**: 파라미터는 **「무엇을 담보하는가」를 분리**해 적어라(개수 캡 ≠ 줄 길이 가드).
> * ㉢ `R1-4-3` 선점 등재 시점 위반(위 「라벨」 참조).
>
> **⚠️ 환경 관측 1건 — 스테이지 torn read**
> * `doc_heading_gate.py` 가 `UnicodeDecodeError: … position 11167: unexpected end of data` 로
>   **2회 연속** 죽었는데, 전 문서 158파일을 직접 디코드 검사하니 **실패 0건**이었고
>   그 뒤 같은 게이트가 정상 통과했다 ⇒ **파일 손상이 아니라 부분 읽기**다(`R1-7-3` 실사례).
>   🔴 **재현되는 것처럼 보여도 손상이 아닐 수 있다** — 「손상」 단정 전에 재읽고 대조한다.
>
> **유지 연산 1건(사용자 승인)**
> * 인수인계 절을 추가하니 `99_NEXT_SESSION-001.md` 가 **352줄**이 되어 게이트6 FAIL
>   ⇒ 승인 후 `--rebalance --fill 0.7` 실행 = **13 → 14조각** · max 27,445B · 최소 여유 13,515B ·
>   게이트 6종 PASS · 토큰 유실 0.
>
> **독해 기록**(`R1-3-7-b` · 미반환 **0건** · 재호출 **0건**)
> * `20_issue/00_BRIEF.md` 전량(138줄) — 토큰 `0-LLL` / `1,711,405 자` / `여유 21,579`
> * `00_guides/00_작업지침_세션운영규칙.md` 318줄 · offset 0·80·160·240 ×80줄 —
>   토큰 `R0-7 워크스페이스 파서 제약` / `00c2b7da253949f1` / `R3-9 ㉦` / `R4-4-4`
> * `99_NEXT_SESSION-001.md` offset 44 limit 100 — 토큰 `469,878자` / `4/7 → 5/8 로 악화` / `조문 88`
> * `scripts/split_doc.py` 825줄 8회 분할 — 토큰 `HEADER_BYTES 448` / `O83B-prerebalance`
> * `scripts/retire_rows.py` 290줄 3회 분할 — 토큰 `big_cell_index` / `preretire` / `TOK_NUM`
>
> **등재·게이트**
> * 원장 §1 대시보드 `O107` 행 1건 신설 + `O106` 행 「잔여」를 **병기해 닫음**(`R3-9 ㉥`).
> * 게이트 = `doc_census` stale 0 · `doc_type_gate` 축1·2 0 · `clause_order_gate` 조문 88 역전 0 ·
>   `index_row_gate` 유실 0 중복 0(현재 787 ↔ 골든 534 — **신설 253행 누적은 이전 세션 것**) ·
>   `doc_heading_gate` 제목 유실 0 · `line_len` PASS.
> * 음성 테스트 2종 신설 = `test_split_doc_toc.py` 7축 **53단정** ·
>   `test_retire_sections.py` 9축 **55단정**(`R3-2`).
> * 🟠 **승계 미결** = `99_NEXT §0-MMM ▣PPP` (문서 4종 미독 **8세션 연속** · 절 은퇴 실제 적용 ·
>   `99_NEXT` 9겹 적층 · `index_row_gate` 골든 누적 · 임시 스크립트).
> * ㉡ 분기(`R4-4-2` 지시 동봉) 적용 세션이다 — 확정위반 **1건**(`R1-4-3`).
