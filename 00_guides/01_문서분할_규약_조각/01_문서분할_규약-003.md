<!-- SPLIT-CHUNK 01_문서분할_규약.md | 003/003 | 허브 = 01_문서분할_규약.md | 원문 268~320행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
  * 🆕 **R1-6-22 유지 연산은 인용 좌표를 죽인다 — 끝나면 `doc_coord_gate` 로 확인한다
    (2026-08-28 O110 신설 · 도구는 O109 신설)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C20**(`R2-8` 무변경 이관 · `O111-B`)
    * 판정 = `python3 scripts/doc_coord_gate.py` (전건은 `--list`).
    * **축1a = blocking** — 경로를 명시했는데 그 경로가 없고 **다른 곳에 실재**하는 것
      (= 우리 연산이 옮긴 것). 고치는 법 = 그 **조각**을 `edit` → 허브 `--republish`.
    * 축1b(참조 대상이 어디에도 없다) · 축2(이력 = 과거 기록 · 소급 수정 대상 아님 · `R1-3-6`) ·
      축3(행 번호가 파일 끝 초과) · 축4(약칭 좌표)는 **경고**다.
      🔴 **경고를 blocking 으로 올리지 마라** — 사람 판단이 필요한 것을 막으면
      아무도 못 고치는 상태로 게이트가 영구 FAIL 이 된다(`P130`).
    * 🟢 **파일명만 쓴 인용은 정상 관례다**(`08_AGENT_spec.md`) — 이름이 실재하면 통과한다.
      O109 초판·2판이 이것을 죽은 좌표로 잡아 **오탐 103건·99건**을 냈다.
    * 음성 테스트 = `scripts/test_doc_coord_gate.py`(9축) · 브리핑 게이트 목록에 편입돼 있다.
      🆕 **[2026-08-28 O111] 축5(모호한 파일명) · 축6(은퇴본만 존재)이 추가돼 11축이다.**
      🔴 **축6 신설 근거 = 게이트의 오분류였다** — `basename_index()` 가 `_archive` 를
      가지치기해 **은퇴본만 남은 문서 8건**을 「어디에도 없다」(축1b)로 찍었고, 그 오분류가
      인수인계에 **「삭제인지 개칭인지 사람 판단 필요」 8건**으로 실려 다음 세션에 넘어갔다.
      ⇒ 🟢 **제외 규칙은 그 근거가 성립하는 축에만 적용한다**(`_archive` 는 **스캔 원본**에서는
      빼고 **실재 판정 분모**에는 넣는다). 같은 원리를 `index_row_gate` 도 적용했다(O111).

  * 🆕 **R1-6-23 🔴🔴 유지 연산에는 `--expect` 를 준다 — 신선도·영속성 가드
    (2026-08-28 O111-B 신설 · O107·O111 실사고 2건의 집행 장치)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C21**(`R2-8` 무변경 이관 · `O111-B`)
    · **O107** = `edit` 로 넣은 절(67줄)이 직후 실측으로 확인됐는데, 승인받아 돌린
      `--rebalance` 가 **낡은 내용을 읽어** 전 조각을 그것으로 재기록해 편집이 사라졌다.
    · **O111** = 원장 §1 `O111` 행을 넣고 **같은 명령에서 `grep -c` = 1** 을 확인한 뒤
      `--republish` 를 돌렸는데, 나중에 세니 **행이 없었다**(조각 스냅샷이 없어 재작성 복원).
    🔴 **두 번 다 게이트 6종이 전부 🟢 였다** — `--verify` 는 **자기일관성**만 보고
    (낡아도 일관되면 🟢), `R1-7-2`(해시 안정성)는 **같은 시점 2회 읽기**만 본다.
    ⇒ **시점 간 불일치**는 두 축 모두 **원리적으로** 못 잡는다.
    * 판정 = `--expect TOKEN`(반복 가능)을 주면 도구가 **두 번** 단정한다:
      ㉠ **쓰기 전** = 방금 수집한 내용에 그 토큰이 있는가(**낡은 읽기 차단** · O107 축)
      ㉡ **쓴 뒤** = 디스크에서 되읽어도 있는가(**조용한 소실 차단** · O111 축)
      🔴 없으면 **쓰지 않고 exit 1**(경고가 아니다 — 되돌릴 수 없는 연산이다).
    * 적용 대상 = `--republish` · `--rebalance`. 🔴 **토큰은 「그 세션이 방금 넣은 것」**을 준다
      (예: 세션 라벨 · 새 절 제목 · 새 표 행의 고유 문구). 문서에 원래 있던 흔한 낱말은 무의미하다.
    * ⚠️ **선언 기반이다** — `--expect` 를 생략하면 검사할 것이 없다. 그래서 이 조문이 **의무**다.
    * 음성 테스트 = `scripts/test_split_doc_expect.py`(5축 · 두 사고를 **주입해** 차단을 단정).

  * 🆕 **R1-6-24 🔴 「닫힘」은 제목 판정일 뿐이다 — 절 은퇴 전에 본문 열림 신호를 센다
    (2026-08-28 O111-B 신설 · `R1-6-19` 의 안전 장치)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C22**(`R2-8` 무변경 이관 · `O111-B`)
    ⇒ 제목 이모지만으로 대량 은퇴하면 **4/5 가 오이관**된다.
    * 판정 = `retire_sections.py` 가 본문에서 **열림 신호 5종**(상태 이모지 🔴/🟠/🟡 ·
      복귀·전환 트리거 · 미결·대기 표기 · `BLOCKING-N` 참조 · 존재 이유 자기선언)을 세어
      `--list` 에 숫자로 표시하고, 적용 경로에서는 **기본 차단**한다.
    * 🔴 넘기려면 **그 절을 읽고 `--force`** 를 준다 — 그리고 **그 사실을 이력·원장에 적는다**
      (판단의 근거가 사람에게 있음을 남기는 것이 `--force` 의 대가다).
    * 🟢 **부분 은퇴가 정답인 경우가 많다** — 닫힌 완료 보고와 열린 잔여 조건이 **한 절에 섞여 있는 것**이
      이 워크스페이스의 관례이므로, 절 전체가 아니라 그 안의 닫힌 하위 절만 옮기는 편이 안전하다.
    * ⚠️ **절감 기대치를 낮게 잡아라** — 실측 = 418 B 본문을 옮겨 원본 절감 **27 B**였다
      (포인터 줄이 본문만큼 길다). 은퇴는 **용량 회복 수단이 아니라 가독성 수단**이다.
  * 🆕 **R1-6-25 🔴🔴 분할은 「분모 4곳 편입」으로 끝나지 않는다 — 기계로 대조하라 (2026-08-29 O112-B 신설)**
    🔴 **왜**: O112 가 이 문서를 분할한 직후, `R1-6-18 ④` 의 체크리스트를 **이행했는데도** 두 곳이 깨져 있었다.
    * ㉠ `clause_order_gate` 가 **`조문 0개`를 찍고도 ✅** 를 냈다(허브는 목차만 담는다 · 총계 **95 → 70**)
      ⇒ **「위반 0」은 분모가 비었을 때도 참이다.** 처방 = `logical_text()` + **「0개는 FAIL」**.
    * ㉡ `fix_stale_counts` 가 이 문서를 **`FLAT_SOURCES`(미분할)** 로 계속 취급해 **본문을 건너뛰었다**
      (조용한 0건 처리 · 같은 파일의 주석이 이미 그 위험을 경고하고 있었는데도 놓쳤다).
    * 🟢 **처방 = 체크리스트를 기계 판정으로 바꿨다** = `doc_census.split_denominator_check()`
      (`doc_census.py` 실행에 포함 · 축 = 조각의 줄길이 분모 편입 · FLAT 오분류 · 조문 문서 0개).
      🔴 **축이 늘면 그 함수에 추가한다** — 이 조문이 아니라 **그 함수가 「분할 후 점검」의 정본**이다.
      음성 테스트 = `test_doc_census.py` 축6(분모를 일부러 오염시켜 검출·복구를 양축으로 단정).
    * ⚠️ **`--boundary section` 은 `##` 절이 있어야 균등해진다** — 이 문서는 `#` 1개뿐인 **평면 조문 목록**이라
      원자 1개가 **29 KB(252줄)**로 남았고 `--rebalance --fill` 로도 더 쪼개지지 않는다(원자는 불가분이다).
      🔴 **그 조각이 상한에 닿으면 `--fill` 을 낮추지 말고 「절 제목을 먼저 넣어라」** — 원자를 만드는 것이 선행이다.
  * 🆕 **R1-6-26 🔴🔴 분할 문서의 4층 구조와 용량 책임 (2026-09-17 O170 신설 · 사용자 결정)**
    🔴 **왜**: 사용자 질의 = *"그럼 99문서는 어떤 구성인거지? 허브 원장, 포인터문서, 상세문서 이렇게 구성된건가?
    기왕 재구성하는거 문서 로직을 빠삭하게 세워서 계속 늘어날 문서 관리 잘 할 수 있도록 하자."*
    ⇒ 종전에는 층이 **3층으로 적혀 있었고**(허브 · 조각 · 은퇴처) 허브 안에 자동 색인이 섞여 있어
    **「허브가 왜 커지는가」를 아무 조문도 설명하지 않았다.** 그래서 같은 벽에 **세 번** 부딪혔다
    (O124-B `TITLE_MAX` 인하 → O154-B 캡 인하 → O170 캡 재인하). 이 조문이 그 구조를 확정한다.

    **① 4층 구조 — 층마다 소유자와 수명이 다르다**

    | 층 | 실물 | 누가 쓰는가 | 손으로 고쳐도 되는가 | 인용 정본인가 |
    |---|---|---|---|---|
    | ㉠ **허브** | `99_NEXT_SESSION.md` | `split_doc.py` **전량 재작성** | 🔴 **금지**(조용히 사라진다) | ✗ 목차다 |
    | ㉡ **사이드카**(색인) | `99_NEXT_SESSION_조각/00_선택표.md` | `split_doc.py` **전량 재작성** | 🔴 **금지** | ✗ 색인이다 |
    | ㉢ **조각**(상세 = 정본) | `99_NEXT_SESSION_조각/-001…-035.md` | **사람이 `edit`** | 🟢 **여기만 고친다** | 🟢 **정본** |
    | ㉣ **은퇴처** | `20_issue/90_해소완료_로그(_조각)/` | `retire_sections`/`retire_rows` | 🔴 도구 경유 | 🟢 이관 본문의 정본 |

    🟢 **읽는 순서** = 허브(㉠)로 범위를 잡고 → 절·ID 로 좁히려면 사이드카(㉡) → **그 조각(㉢)을 `read`** 한다.
    🔴 **인용은 항상 ㉢ 또는 ㉣ 좌표로 한다** — ㉠㉡ 의 수치·절 이름을 근거로 쓰지 마라(색인은 절단·캡이 있다).
    🔴 **㉠㉡ 을 손으로 고치는 것이 이 워크스페이스가 4회 당한 유형이다**(경고도 나오지 않는다).

    **② 용량 책임 분리 — 어느 층이 왜 커지는가**

    | 층 | 커지는 원인 | 줄이는 수단 | 🔴 하지 말 것 |
    |---|---|---|---|
    | ㉠ 허브 | **조각 수**(조각 목차 + 대응표가 조각당 1행) | 조각 수를 줄인다(㉣ 은퇴) | 재균형(조각을 **늘린다**) |
    | ㉡ 사이드카 | **조각 수 × 항목 캡** | `TITLES_PER_CHUNK`·`IDS_PER_CHUNK` 인하 | 🔴 **분할하지 마라**(색인을 쪼개면 못 쓴다) |
    | ㉢ 조각 | 본문 증가 | `--rebalance`(조각 수 ↑) · ㉣ 은퇴(총량 ↓) | — |

    🔴🔴 **핵심 판정식 = 「재균형은 조각 여유를 사고 허브·사이드카 여유를 판다.」**
    두 축은 **반대로 움직인다** ⇒ 조각 여유가 없어 재균형을 돌린 **직후에는 허브 상한을 반드시 다시 재라.**
    · 실측(O170) = 재균형 24 → **35조각** ⇒ 허브 38,881 → **46,071 / 40,960 B**(여유 **−5,111**).
    · 🟢 그래서 **선택표를 허브에서 사이드카로 뺐다** ⇒ 허브 **11,425 B**(여유 29,535) · 내용 이동 **0 B**.
    · 🟢 부수 효과 = `TITLE_MAX` 가 **허브 여유와 경쟁하지 않게 됐다** ⇒ 변별력 기준으로 되돌렸다(50 → 64).
    🟢 **판정식 = 표시 상한을 정하는 것은 가독성이지 용량이 아니다** — 용량 제약을 다른 층으로 옮기면
    상한은 가독성 기준으로 되돌려라.

    **③ 신설 문서를 늘릴 때의 순서 (계속 늘어나는 문서 관리)**

    🔴 **문서를 새로 만들기 전에 「기존 문서에 절을 추가하는 편이 낫지 않은가」를 먼저 묻는다**(`R1-6-18` 🟢).
    만들기로 정했으면 **아래 순서를 지킨다** — 순서를 바꾸면 게이트가 **0 을 내며 침묵**한다:
    1. **원장 §0 유형 등재표에 한 줄 먼저**(유형 = 갱신형 / append형 / 정적 / **자동생성**) — 빠뜨리면 `doc_type_gate` 즉시 FAIL.
    2. `write` 툴로 생성 + **LLM-METADATA 헤더**(`doc_id`·`doc_role`·`project`·`created`·`index`).
    3. **상한 이내면 분할하지 않는다**(불필요한 간접층 금지).
    4. **폴더 밖 문서면 분모 4곳 편입** + 🔴 **기준선(골든) 발행**(분모 편입과 골든 발행은 **별개의 두 동작**이다).
    5. `doc_type_gate` · `line_len` · `doc_census` · `cortex ws ls` 로 확인.

    🆕 **유형 「자동생성」의 계약**(O170 신설) = ㉠ 손으로 고치지 않는다 ㉡ **분할하지 않는다**
    ㉢ 상한에 닿으면 **밀도(캡)를 내린다** ㉣ 인용 정본이 아니다.
    🔴 대상 = 허브 전종 · `00_선택표.md` 전종 · `00_BRIEF.md` · `92_실측필요_후속작업.md`.

    **④ 은퇴(㉣)의 한계 — 용량 수단으로 오해하지 마라**

    🔴 `retire_sections` 는 **본문만** 옮기고 **원본 절 제목은 남긴다**(좌표 생존). 그러므로
    ㉠ **허브·사이드카는 줄지 않는다**(색인은 제목을 세므로) ㉡ 줄어드는 것은 **조각 본문**이다.
    🔴 **「제목을 남긴다」는 계약은 「어느 수준의 제목인가」를 말해야 한다** — `##` 절의 span 은
    다음 **동급 이상**까지이므로 **하위 `###` 제목은 본문과 함께 이관된다**
    ⇒ `doc_heading_gate` 가 **제목 유실**로 잡는다(실측 O170 = 유실 8건 · **실유실 0** · 전건 목적지 실재).
    🟢 처방 = 은퇴 직후 ㉠ 유실 제목이 목적지에 실재하는지 **전수 대조**하고 ㉡ 골든을 **`--reason` 과 함께** 발행한다.
    🔴 `retire_sections` 의 「열림 신호」 축은 **인수인계 절에서 전건 오탐한다**(본문이 상태 이모지로 서술된다)
    ⇒ `--force` 가 필요하다 ⇒ 🔴 **그 순간 안전장치가 꺼지므로 대조를 사람이 대신해야 한다.**

    **⑤ 「행선지」 분모는 은퇴처의 분할 여부에 따라 깊어진다**

    🔴 `split_doc --verify` 게이트3 은 유실 토큰의 **행선지**를 `family_text()` 분모에서 찾는다.
    그 분모가 **1단계 하위까지**였고 은퇴처가 분할되면 본문은 **2단계**(`90_해소완료_로그_조각/`)에 들어간다
    ⇒ 실측(O170) = **「행선지 없음 15종」 FAIL**(전 종이 목적지에 실재 · 오탐). 🟢 `_조각` 폴더를 2단계까지 허용해 해소.
    🟢 **판정식 = 은퇴 목적지가 분할되면 분모도 한 단계 깊어져야 한다.** 고정 깊이 분모는
    목적지가 쪼개지는 순간 **조용히 오탐**한다.

  * 🆕 **R1-6-27 🔴🔴 append 성격의 절은 조각이 아니라 「라벨 파일」로 쓴다 (2026-09-17 O172 신설 · 사용자 결정)**

    🔴 **왜**: 사용자 지시 = *"다음 세션용 문서를 남기는게 너무 오래 걸린다. 이건 문서 용량제한 규정
    때문이야. … 세션 번호로 쓰는 숫자(O171 등)를 응용해서 O 단위로 이력 파일을 쪼개는건 어떤가."*
    ⇒ 진단 = `99_NEXT_SESSION` 은 **갱신형인데 append 성격의 인수인계 절이 적층**된다
    (실측 = 조각 35개 중 **25개**에 `## 0-XXXX` 절이 산재 · 총 50절). 이것이 지침 `R1-3-6` 이 말한
    **「성격 혼합이 원인이고 크기는 증상」**의 미집행분이다.

    **① 규격 — 파일명이 판정식이다**

    | 종류 | 파일명 | 누가 세는가 | 조각인가 |
    |---|---|---|---|
    | 조각 | `99_NEXT_SESSION-035.md` | `doc_census.chunk_rx` | 🟢 예 |
    | **라벨 파일** | `99_NEXT_SESSION-O0172-A.md` | `doc_census.label_rx`·`label_paths` | 🔴 **아니다** |
    | 사이드카 | `00_선택표.md` · `00_인수인계_색인.md` | (색인) | 🔴 아니다 |

    🔴 **접미는 항상 붙인다** — 최초 단위도 `-A` 다(무접미 금지 · 사용자 결정 ⓑ).
    🔎 왜 = 무접미와 `-B` 가 섞이면 ㉠ 파일명 폭이 흔들려 **정렬이 발생 순서와 어긋나고**
    ㉡ 「무접미가 1번인가」를 매번 판정해야 한다. 규격화하면 **정렬 = 발생 순서**다.
    🔎 A~Z 로 충분한가 = 실측 최대 **21단위**(`O59` = 무접미 + A~T) ⇒ 담기지만 **여유 5뿐**이므로
    소진 시 **2자 확장**(`-AA`)을 허용하고 정렬은 **길이 우선**으로 둔다.
    🔴 O번호는 **4자리 zero-pad**(`O0172`)다 — 3자리로 쓰면 `O99` > `O100` 으로 정렬된다.

    **② 무엇이 없어지는가 (이 조문의 값)**

    | 종전 단계 | 라벨 파일에서 |
    |---|---|
    | 조각 말미에 python 전량 쓰기(`cat >>` 는 **조용히 유실** · O167) | 🟢 **신규 파일 `write`** — `R1-7-1` 이 신규 파일에 허용하는 경로 |
    | 이전 절 「여기서 시작한다」 **취소선 승계** | 🟢 **불필요** — 현행 = **라벨 최대값** |
    | `doc_heading_gate --update-golden`(취소선이 제목 유실로 잡힌다) | 🟢 **불필요**(제목을 건드리지 않는다) |
    | 조각 상한 근접 시 **재균형**(`R4-4-3` 승인 + 좌표 전멸) | 🟢 **원리적으로 발생하지 않는다**(1세션 ≈ 2~5KB ≪ 40KB) |
    | 허브 `--republish` | 🟠 **라벨 파일이 처음 생길 때만**(색인 포인터 1줄) |

    🟢🟢 **핵심 판정식 = 「현행성을 기재에 두지 말고 파일 시스템의 사실로 만들어라.」**
    🔎 근거 = 종전 판정식(적층 + 취소선)은 **3회 오발행**했다 — O143(날짜 동점에서 `max` 가 가장
    오래된 절 선택) · O165 `D1`(겹친 `~~` 로 문구가 구간 밖) · O163(승계 예고만 하고 절 미작성).
    🔴 세 사고의 공통 원인은 파서가 아니라 **「현행이 문서에 어떻게 적혔는가」에 의존한 설계**다(`J3`).

    **③ 도구와 분모 (🔴 손으로 하지 마라)**

    · 작성 = `python3 scripts/handoff_write.py --label O172-A --entry-file <파일>`
      (머리말·서명 자동 · 2,000자 가드 · 상한 검사 · **도달 재검사** · 색인 재생성까지 한 번에)
    · 다음 접미 = `--next` · 색인만 = `--index` · 색인 = `99_NEXT_SESSION_조각/00_인수인계_색인.md`
    · 🔴 **규격 정본은 `doc_census`** 다(`chunk_rx`·`label_rx`·`label_paths`) — 다른 파일에서
      정규식을 다시 쓰지 마라. 🔎 실측 = 이 워크스페이스는 그 결함으로 같은 폴더의 조각 수를
      **35(`split_doc`) ↔ 36(`doc_census`)** 으로 다르게 셌다(차이 = 사이드카 · `R3-9 ㉡`·`J8`).
    · 🔴 **줄길이 분모에 편입해야 한다** = `doc_line_length_gate.CANON_GLOB` 의
      `99_NEXT_SESSION_조각/*-O[0-9][0-9][0-9][0-9]-*.md` — 라벨 파일은 `-NNN.md` 패턴에 걸리지 않으므로
      편입하지 않으면 **매 세션 새로 생기는 정본이 검사 밖**이 된다.

    🔴🔴 **분모 편입은 「전부 넣기」가 아니다 — 게이트마다 축이 다르다**(O172 결정 · 근거는 각 코드 주석):

    | 게이트 | 라벨 파일 | 왜 |
    |---|---|---|
    | `doc_line_length_gate` | 🟢 **편입** | 절단 손실은 파일 종류와 무관하다 |
    | `doc_type_gate` | 🟢 **편입**(폴더 전건) | 재는 것이 **상한 준수**다 |
    | `doc_coord_gate` | 🟢 편입(폴더 walk) | 좌표 실재는 신규 파일에도 필요하다 |
    | `doc_census`(조각 수) | 🔴 **미편입** | 라벨 파일은 **조각이 아니다** |
    | `doc_heading_gate` | 🔴 **미편입** | 축 = 「편집으로 제목을 삼켰는가」 · 편입하면 **매 세션 골든 재발행**(없애려던 비용) |
    | `index_row_gate` | 🔴 **미편입** | 축 = 「골든 대비 행 증가」 · 신설 잡음이 유실 신호를 묻는다 |

    🟢 **판정식 = 「분모에서 뺀다」는 결정은 뺀 이유와 대체 축을 함께 적어야 결정이 된다.**
    🟢 **판정식 = 「같은 것을 다르게 재지 마라」는 「다른 것을 같게 재라」가 아니다** — 세는 대상이
    다르면 분모도 달라야 하고, **적히지 않은 차이**만이 결함이다(실측 = 조각 수 35 ↔ 36).

    **④ 전환 규칙 — 기존 50절은 옮기지 않는다**

    🔴 **소급 개명·소급 이전을 하지 않는다**(`R2-8-3` 되돌릴 수 없음 · 기존 라벨 개명 금지 계약).
    🟢 조각에 남은 `## 0-XXXX` 절은 **전부 승계된 것**으로 읽는다 — 라벨 파일이 1개라도 있으면
    `session_brief` 는 **라벨 파일만** 현행으로 본다(라벨 0개면 종전 경로로 되돌아간다).
    🔴 그러므로 **전환 후 첫 세션은 조각 절에 인수인계를 쓰면 안 된다** — 쓰면 브리핑이 그것을
    보지 않는다(🟢 판정식 = **두 등록부를 동시에 열지 마라**).


