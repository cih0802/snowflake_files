<!-- SPLIT-CHUNK 01_문서분할_규약.md | 003/003 | 허브 = 01_문서분할_규약.md | 원문 268~320행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
  * 🆕 **R1-6-22 유지 연산은 인용 좌표를 죽인다 — 끝나면 `doc_coord_gate` 로 확인한다
    (2026-08-28 O110 신설 · 도구는 O109 신설)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C20**(`R2-8` 무변경 이관 · `O111-B`)
    * 판정 = `python3 scripts/doc_coord_gate.py` (전건은 `--list`).
    * **축1a = blocking** — 경로를 명시했는데 그 경로가 없고 **다른 곳에 실재**하는 것
      (= 우리 연산이 옮긴 것). 고치는 법 = 그 **조각**을 `edit` → 허브 `--republish`.
    * 축1b(참조 대상이 어디에도 없다) · 축2(이력 = 과거 기록 · 소급 수정 대상 아님 · `R1-3-6`) ·
      축3(행 번호가 파일 끝 초과) · 축4(약칭 좌표)는 **경고**다.
      🔴 **경고를 blocking 으로 올리지 마라** — 사람 판단이 필요한 것을 막으면
      아무도 못 고치는 상태로 게이트가 영구 FAIL 이 된다(`P130`).
    * 🟢 **파일명만 쓴 인용은 정상 관례다**(`08_AGENT_spec.md`) — 이름이 실재하면 통과한다.
      O109 초판·2판이 이것을 죽은 좌표로 잡아 **오탐 103건·99건**을 냈다.
    * 음성 테스트 = `scripts/test_doc_coord_gate.py`(9축) · 브리핑 게이트 목록에 편입돼 있다.
      🆕 **[2026-08-28 O111] 축5(모호한 파일명) · 축6(은퇴본만 존재)이 추가돼 11축이다.**
      🔴 **축6 신설 근거 = 게이트의 오분류였다** — `basename_index()` 가 `_archive` 를
      가지치기해 **은퇴본만 남은 문서 8건**을 「어디에도 없다」(축1b)로 찍었고, 그 오분류가
      인수인계에 **「삭제인지 개칭인지 사람 판단 필요」 8건**으로 실려 다음 세션에 넘어갔다.
      ⇒ 🟢 **제외 규칙은 그 근거가 성립하는 축에만 적용한다**(`_archive` 는 **스캔 원본**에서는
      빼고 **실재 판정 분모**에는 넣는다). 같은 원리를 `index_row_gate` 도 적용했다(O111).

  * 🆕 **R1-6-23 🔴🔴 유지 연산에는 `--expect` 를 준다 — 신선도·영속성 가드
    (2026-08-28 O111-B 신설 · O107·O111 실사고 2건의 집행 장치)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C21**(`R2-8` 무변경 이관 · `O111-B`)
    · **O107** = `edit` 로 넣은 절(67줄)이 직후 실측으로 확인됐는데, 승인받아 돌린
      `--rebalance` 가 **낡은 내용을 읽어** 전 조각을 그것으로 재기록해 편집이 사라졌다.
    · **O111** = 원장 §1 `O111` 행을 넣고 **같은 명령에서 `grep -c` = 1** 을 확인한 뒤
      `--republish` 를 돌렸는데, 나중에 세니 **행이 없었다**(조각 스냅샷이 없어 재작성 복원).
    🔴 **두 번 다 게이트 6종이 전부 🟢 였다** — `--verify` 는 **자기일관성**만 보고
    (낡아도 일관되면 🟢), `R1-7-2`(해시 안정성)는 **같은 시점 2회 읽기**만 본다.
    ⇒ **시점 간 불일치**는 두 축 모두 **원리적으로** 못 잡는다.
    * 판정 = `--expect TOKEN`(반복 가능)을 주면 도구가 **두 번** 단정한다:
      ㉠ **쓰기 전** = 방금 수집한 내용에 그 토큰이 있는가(**낡은 읽기 차단** · O107 축)
      ㉡ **쓴 뒤** = 디스크에서 되읽어도 있는가(**조용한 소실 차단** · O111 축)
      🔴 없으면 **쓰지 않고 exit 1**(경고가 아니다 — 되돌릴 수 없는 연산이다).
    * 적용 대상 = `--republish` · `--rebalance`. 🔴 **토큰은 「그 세션이 방금 넣은 것」**을 준다
      (예: 세션 라벨 · 새 절 제목 · 새 표 행의 고유 문구). 문서에 원래 있던 흔한 낱말은 무의미하다.
    * ⚠️ **선언 기반이다** — `--expect` 를 생략하면 검사할 것이 없다. 그래서 이 조문이 **의무**다.
    * 음성 테스트 = `scripts/test_split_doc_expect.py`(5축 · 두 사고를 **주입해** 차단을 단정).

  * 🆕 **R1-6-24 🔴 「닫힘」은 제목 판정일 뿐이다 — 절 은퇴 전에 본문 열림 신호를 센다
    (2026-08-28 O111-B 신설 · `R1-6-19` 의 안전 장치)**
    · 🔎 **경위·실사고 근거 = `20_issue/91_사고사례집.md` §C22**(`R2-8` 무변경 이관 · `O111-B`)
    ⇒ 제목 이모지만으로 대량 은퇴하면 **4/5 가 오이관**된다.
    * 판정 = `retire_sections.py` 가 본문에서 **열림 신호 5종**(상태 이모지 🔴/🟠/🟡 ·
      복귀·전환 트리거 · 미결·대기 표기 · `BLOCKING-N` 참조 · 존재 이유 자기선언)을 세어
      `--list` 에 숫자로 표시하고, 적용 경로에서는 **기본 차단**한다.
    * 🔴 넘기려면 **그 절을 읽고 `--force`** 를 준다 — 그리고 **그 사실을 이력·원장에 적는다**
      (판단의 근거가 사람에게 있음을 남기는 것이 `--force` 의 대가다).
    * 🟢 **부분 은퇴가 정답인 경우가 많다** — 닫힌 완료 보고와 열린 잔여 조건이 **한 절에 섞여 있는 것**이
      이 워크스페이스의 관례이므로, 절 전체가 아니라 그 안의 닫힌 하위 절만 옮기는 편이 안전하다.
    * ⚠️ **절감 기대치를 낮게 잡아라** — 실측 = 418 B 본문을 옮겨 원본 절감 **27 B**였다
      (포인터 줄이 본문만큼 길다). 은퇴는 **용량 회복 수단이 아니라 가독성 수단**이다.
  * 🆕 **R1-6-25 🔴🔴 분할은 「분모 4곳 편입」으로 끝나지 않는다 — 기계로 대조하라 (2026-08-29 O112-B 신설)**
    🔴 **왜**: O112 가 이 문서를 분할한 직후, `R1-6-18 ④` 의 체크리스트를 **이행했는데도** 두 곳이 깨져 있었다.
    * ㉠ `clause_order_gate` 가 **`조문 0개`를 찍고도 ✅** 를 냈다(허브는 목차만 담는다 · 총계 **95 → 70**)
      ⇒ **「위반 0」은 분모가 비었을 때도 참이다.** 처방 = `logical_text()` + **「0개는 FAIL」**.
    * ㉡ `fix_stale_counts` 가 이 문서를 **`FLAT_SOURCES`(미분할)** 로 계속 취급해 **본문을 건너뛰었다**
      (조용한 0건 처리 · 같은 파일의 주석이 이미 그 위험을 경고하고 있었는데도 놓쳤다).
    * 🟢 **처방 = 체크리스트를 기계 판정으로 바꿨다** = `doc_census.split_denominator_check()`
      (`doc_census.py` 실행에 포함 · 축 = 조각의 줄길이 분모 편입 · FLAT 오분류 · 조문 문서 0개).
      🔴 **축이 늘면 그 함수에 추가한다** — 이 조문이 아니라 **그 함수가 「분할 후 점검」의 정본**이다.
      음성 테스트 = `test_doc_census.py` 축6(분모를 일부러 오염시켜 검출·복구를 양축으로 단정).
    * ⚠️ **`--boundary section` 은 `##` 절이 있어야 균등해진다** — 이 문서는 `#` 1개뿐인 **평면 조문 목록**이라
      원자 1개가 **29 KB(252줄)**로 남았고 `--rebalance --fill` 로도 더 쪼개지지 않는다(원자는 불가분이다).
      🔴 **그 조각이 상한에 닿으면 `--fill` 을 낮추지 말고 「절 제목을 먼저 넣어라」** — 원자를 만드는 것이 선행이다.

