# 데이터 마이그레이션 작업 문서 (A → B → 로컬 → C)

> 본 문서는 **절차·설계 근거·주의사항·윈도우(로컬) 명령어**만 담는다.
> **모든 SQL은 아래 스크립트 파일에 있다.** 문서는 "왜/언제/무엇을 확인하는지", 스크립트는 "어떻게"를 담당한다.
>
> 🆕 **[2026-08-14] 이관 대상에 `GN_DW.ML` 예측결과 16종이 추가됐다.** 브론즈 53 + ML 16 = **69 테이블**.
> 추가 배경 = ML 스키마는 DDL(`99_provided_definition/20_ML_ddl.sql`)이 있어 **구조는 만들 수 있지만
> 데이터가 없어** SERVING 뷰·Semantic View·Agent 도구 등 후속 작업이 막혀 있었다.
> ML 관련 신설·변경 지점은 본문에서 **🆕 ML** 로 표시했고, 상세는 **§5.6** 과 **§11** 에 모았다.

## 0. 문서 구성 (파일 맵)

| 파일 | 실행 계정 | 내용 | 관련 장 |
|------|-----------|------|---------|
| `01_데이터마이그레이션 20260730.md` (본 문서) | — | 절차/설계/주의사항/로컬 명령어 | 전체 |
| `02_데이터마이그 A_PRODUCER.sql` | **A** (Provider) | 공유 생성, DB·스키마 권한 부여, **ML 16종 테이블 단위 부여(2-B)**, 대상 계정 추가, 확인, 대상 목록/규모 스냅샷, `GET_DDL` 추출, teardown | 3.1, 3.1-B, 3.2, 7 |
| `02_1_A DB정보.sql` | **A** (Provider) | 02번 5·6단계 실행 결과 스냅샷 (A1 테이블목록/A2 스키마요약/A3~A6 GET_DDL) — **대조 정본** · 🆕 ML 실측은 A7 로 추가 예정(미확보) | 3.2, 6.1 |
| `03_데이터마이그 B_BROKER.sql` | **B** (Consumer) | 수신 공유 확인, 스테이지 생성, 공유 DB 마운트, **대조 기준값 집계(3.1 · ML 16 확인 3.1-B)**, `BRONZE_*` + `ML` 전 테이블 CSV(GZIP) 언로드, 결과 확인, 정리 | 3.3, 4.1, 4.2, 6.1, 7 |
| `04_데이터마이그 GN_DW_BRONZE_DDL_20260730.sql` | **C** (Target) | 브론즈 4개 스키마 DDL 스냅샷 (테이블 53개 + 시퀀스 2 + 파일포맷 3) | 5.1 |
| `06_데이터마이그 GN_DW_ML_DDL_20260814.sql` | **C** (Target) | 🆕 **ML 스키마 DDL 스냅샷 (예측결과 16종)** — `20_ML_ddl.sql` 무변경 발췌 | 5.1-B |
| `05_데이터마이그 C_CONSUMER.sql` | **C** (Target) | 적재 전 스테이지 검증, 적재 파일 포맷, 일괄 적재 프로시저, 스키마별 적재 실행, GA4 JSON 복원, **ML 적재·PREDICTION 복원(A.5-B)**, 검증(행수·`VALIDATE`/`COPY_HISTORY`), 정리 | 5.2, 5.3.1, 5.4, 5.6, 6, 7 |

스테이지 경로 URI: `snow://workspace/USER$.PUBLIC."snowflake_files"/versions/head/50_handoff/<파일명>`

---

## 1. 개요

```
[A 계정]  --(Direct Share, 동일 리전)-->  [B 계정]
[B 계정]  --(로컬 물리적 다운로드, CSV+GZIP)-->  [작업 서버/로컬(Windows)]
[로컬]    --(업로드)-->                     [C 계정]
```

- **A 계정**: 원본 데이터 보유(Provider)
- **B 계정**: 공유 수신 및 파일 추출/다운로드 수행(Consumer)
- **C 계정**: 최종 적재 대상(Target)

> 전제: A와 B는 **동일 클라우드/동일 리전**이므로 Direct Share가 가능하다.
> C는 리전/클라우드가 다를 수 있으므로 파일 기반(물리적 이관)으로 처리한다.

### 1.1 설계 원칙 (Snowflake 기술문서 기준)
- **파일 포맷: CSV + `COMPRESSION = GZIP`**
  - `GZIP`은 **CSV/JSON** 언로드에서 지원. **PARQUET 언로드는 `SNAPPY/LZO/NONE`만 지원(GZIP 불가)**.
- **전체 스키마 DDL을 먼저 추출**해 C에 동일 구조를 만든 뒤 적재한다.
  - CSV 적재는 **위치(순서) 기반**이며 `MATCH_BY_COLUMN_NAME`을 **지원하지 않는다**(Parquet/ORC/Avro/JSON 전용).
  - 따라서 **원본과 동일한 컬럼 순서·데이터 타입**이 보장되어야 정확히 적재된다.
- **언로드/적재 옵션은 반드시 대칭**: `HEADER=TRUE` ↔ `SKIP_HEADER=1`, `NULL_IF`, `FIELD_OPTIONALLY_ENCLOSED_BY`, `EMPTY_FIELD_AS_NULL`, `TIMESTAMP_FORMAT`.

---

## 2. 사전 준비 체크리스트

| 항목 | 내용 | 담당 | 확인 |
|------|------|------|------|
| 계정 식별자 | A = `CMRQTUT.XC97295`, B(locator) = `GB72026`, C = `zx03676` ⚠️ **C 는 stale** — 2026-08-14 실측 `CURRENT_ACCOUNT()` = **DV07626**(재구축으로 변경) | 인프라 | ☑ |
| 권한 | A: `CREATE SHARE`(ACCOUNTADMIN), B/C: `CREATE DATABASE`, 적재 스키마 권한 | 보안 | ☐ |
| 대상 객체 목록 | 이관할 DB/스키마/테이블 목록 및 행수 | 데이터팀 | ☐ |
| 스키마 DDL | `04_데이터마이그 GN_DW_BRONZE_DDL_20260730.sql` 준비 | 데이터팀 | ☑ |
| 🆕 ML DDL | `06_데이터마이그 GN_DW_ML_DDL_20260814.sql` 준비 (예측결과 16종) | 데이터팀 | ☑ |
| 🆕 ML 원천 실재 | A 계정에 `GN_DW.ML` 결과 16종이 실재하고 행수가 0이 아닌지 — **미확인**(02번 5단계로 확보) | 데이터팀 | ☐ |
| 스토리지 | 로컬 다운로드 공간(용량 산정), 또는 외부 스테이지(S3/GCS/Azure) | 인프라 | ☐ |
| 파일 포맷 | **CSV + GZIP** (GA4·ML 포함 전 스키마 동일) | 데이터팀 | ☑ |
| 도구 | Snowflake CLI(`snow`) 설치 및 A/B/C 연결(connection) 구성 | 작업자 | ☐ |
| 검증 기준 | 행수/체크섬/샘플 비교 방식 합의 | 데이터팀 | ☐ |

### 2.1 실제 사용 식별자 요약
| 구분 | 값 |
|------|-----|
| A(Provider) | `CMRQTUT.XC97295` |
| B(Consumer) locator | `GB72026` |
| 공유명 | `MIG_SHARE` |
| 원본 DB / 스키마 | `GN_DW` / `BRONZE_CRM`(45) · `BRONZE_AGENCY`(4) · `BRONZE_ERP`(1) · `BRONZE_BIGQUERY`(3) = **53 테이블** |
| 🆕 원본 DB / ML | `GN_DW.ML` — 예측결과 **16 테이블만**(`ML_RST_DATA_*`). 학습 20 · 스냅샷 12 · 로그 1 · 뷰 4 · 프로시저 14 · 모델 14 는 **제외** |
| 🆕 이관 총계 | **69 테이블** (브론즈 53 + ML 16) |
| B 마운트 DB | `GN_DW_SHARED` (읽기 전용) |
| B 언로드 스테이지 | `@SANDBOX.TOOLS.my_export_stage/<SCHEMA>/<TABLE>/` |
| C 적재 스테이지 | `@SANDBOX.TOOLS.MIG_LOAD_STAGE/<SCHEMA>/<TABLE>/` |
| C 계정 | ~~`zx03676`~~ → **`DV07626`** (2026-08-14 실측 · ACCOUNTADMIN, WH=`COMPUTE_WH`) |
| 파일 형식 | 전 스키마 CSV + GZIP (GA4·ML 도 CSV) |

---

## 3. 1단계: A → B (Direct Share, 동일 리전)

### 3.1 A 계정 — Share 생성 및 부여
▶ 실행: **`02_데이터마이그 A_PRODUCER.sql`**

- 순서: 공유 생성 → DB `USAGE` → 스키마 4개 `USAGE` → 스키마별 `SELECT ON ALL TABLES` → **ML 부여(3.1-B)** → `ALTER SHARE ADD ACCOUNTS = GB72026` → 확인.
- **스키마 단위 부여가 필수**: DB `USAGE`만 주면 B에서 테이블이 보이지 않는다.
- **`ALL TABLES`는 '현재 존재하는' 테이블만 대상**이다. 이후 테이블이 추가되면 재부여해야 한다.
- 확인 포인트: `SHOW GRANTS TO SHARE` 결과에 DB 1 + 스키마 **5** + 테이블 **69** 권한, `SHOW SHARES LIKE 'MIG_SHARE'` 의 `to` 컬럼에 `GB72026`.
- 공유 직후 스크립트 5단계에서 **이관 대상 목록/규모 스냅샷**(테이블별 `row_count`, `bytes`)을 기록한다. 6장 검증의 원본 기준값이다.

### 3.1-B 🆕 ML A 계정 — ML 예측결과 16종 부여
▶ 실행: **`02_데이터마이그 A_PRODUCER.sql`** 의 **2-B 구간**

- 🔴 **`GRANT SELECT ON ALL TABLES IN SCHEMA GN_DW.ML` 을 쓰지 않는다.** 테이블을 하나씩 부여한다.
  - 이유 = ML 스키마에는 학습용 `ML_TRAIN_DATA_*` **20종** · 원천 스냅샷 **12종** · 로그 **1종** 이 함께 있고,
    사용자 지시로 **학습용은 Agent 노출 금지**다. 통짜로 주면 33종이 함께 열린다.
  - 🟢 **부수 효과(의도한 것)**: 공유받은 DB의 `INFORMATION_SCHEMA` 에는 **부여된 16종만 보인다**
    ⇒ B 의 언로드 루프(4.2)가 자동으로 16종만 대상으로 삼고, 화이트리스트를 두 번 관리하지 않아도 된다.
- 🔴 **재부여가 필요한 조건이 있다.** 정본 DDL(`20_ML_ddl.sql`)이 결과 테이블을 `create or replace TABLE` 로 만들기 때문에,
  원천에서 모델을 다시 돌리면 객체가 새로 생겨 **부여한 `SELECT` 가 소실된다.**
  ⇒ 예측 재실행 후에는 2-B 구간을 다시 실행한다. 확인 방법은 3.3 의 「ML 16 확인」이다.
- 부여 대상 16종(요건 = *260814 기준 머신러닝 개발 내용*):

| 요건 | 테이블 | 컬럼 | VARIANT |
|------|--------|-----:|---------|
| 회원실 1 캠페인별 이탈 예측 | `ML_RST_DATA_SPNSR_CHURN_12M` | 18 | `$18` |
| 회원실 2 회원별 중단 예측 | `ML_RST_DATA_MBER_CHURN_12M` | 18 | `$18` |
| 회원실 3 캠페인카테고리별 회비 예측 | `ML_RST_DATA_CMPGN_CTGR_AMT` | 6 | — |
| 회원실 4 회원 단위 증액 가능성 예측 | `ML_RST_DATA_MBER_INC_12M` | 21 | `$21` |
| 회원실 5 충성회원 가능성 예측 | `ML_RST_DATA_LOYAL_MBER` | 22 | `$22` |
| 기획실 1 부서별 연도말 개발 예측치 | `ML_RST_DATA_MONTHLY_DEPT_DVLP_AMT` | 6 | — |
| 기획실 2 후원사업별 연도말 개발 예측치 | `ML_RST_DATA_MONTHLY_SPNSR_BSNS_ID_DVLP_AMT` | 6 | — |
| 기획실 3 신규/기존별 개발 건수 예측 | `ML_RST_DATA_MONTHLY_NEW_OLD_DVLP_AMT` | 6 | — |
| 나마본 1 월별 신규 후원개발 금액 예측 | `ML_RST_DATA_MONTHLY_DVLP_AMT` | 5 | — |
| 나마본 2 채널 회원평균 LTV 예측 | `ML_RST_DATA_UCMPGN_LTV` | 6 | — |
| 나마본 2 〃 스코어 | `ML_RST_DATA_UCMPGN_LTV_SCORE` | 8 | — |
| 나마본 3 캠페인 후원 LTV 예측 | `ML_RST_DATA_CMPGN_LTV` | 6 | — |
| 나마본 3 〃 스코어 | `ML_RST_DATA_CMPGN_LTV_SCORE` | 8 | — |
| 나마본 4 신규 후원 유치 요인 분석 | `ML_RST_DATA_CHANNEL_NEW_SPNSR_DVLP_CONTRIBUTION` | 5 | — |
| 나마본 5 캠페인별 월별 개발액 예측 | `ML_RST_DATA_MONTHLY_CMPGN_DVLP_AMT` | 6 | — |
| 나마본 6 증액 개발 요인 분석 | `ML_RST_DATA_DVLP_INC_CONTRIBUTION` | 5 | — |

- 🔴 **ML 규모는 A 실측 미확보다.** 02번 5단계 쿼리(ML 필터 포함)를 실행해 결과를 `02_1_A DB정보.sql` 의 **A7** 로 추가한다.
  참고값(A 실측 아님 · 원천 계정 라이브 2026-08-14 O74) = 약 **1,045,732 행**,
  정본은 `05_SV-Agent_ai/20_ML_SV_설계.md §0-A`(테이블별 행수 16종 전량 · 기준월 `202606` 단일).
  ⚠️ 프로시저가 월별 `DELETE`+`INSERT` 누적이므로 **기준월이 늘면 행수가 증가한다** — 이관 시점 실측만 대조 기준으로 쓴다.

### 3.2 A 계정 — 전체 스키마 DDL 추출
▶ 실행: **`02_데이터마이그 A_PRODUCER.sql`** 의 하단 `GET_DDL` 구간(주석 처리되어 있어 필요 시 해제)
▶ 산출물: **`04_데이터마이그 GN_DW_BRONZE_DDL_20260730.sql`**

- CSV 적재는 위치 기반이라 **C에 원본과 동일한 테이블 구조**를 먼저 만들어야 한다.
- 스키마 4개(`BRONZE_CRM/GA4/ERP/AGENCY`)를 각각 추출 → 테이블 53개 + 시퀀스 2 + 파일포맷 3 포함.
- 공유받은(imported) DB에서는 `GET_DDL`이 제한될 수 있으므로 **DDL 추출은 원본 A에서 수행**한다.
- C에서 실행하기 전, DB/스키마명을 C 환경에 맞게 치환한다(파일 상단 USAGE 참조).
- 🆕 **ML 은 재추출이 불필요하다.** `GET_DDL('SCHEMA','GN_DW.ML',TRUE)` 결과가 이미
  `99_provided_definition/20_ML_ddl.sql`(3,217줄)로 확보되어 있고, 이관용 구조 발췌본이
  `06_데이터마이그 GN_DW_ML_DDL_20260814.sql`(예측결과 16종)이다.
  ⚠️ 단 **모델·프로시저가 교체되어 결과 테이블 컬럼이 바뀌면 재추출 후 06번을 갱신**해야 한다.
  그 사고는 C 적재 단계에서 `COUNT_MISMATCH` 로 잡힌다(5.3.1 (4)).

### 3.3 B 계정 — Share로부터 DB 생성
▶ 실행: **`03_데이터마이그 B_BROKER.sql`** (1~3번 구간)

- `SHOW SHARES` 의 **INBOUND** 항목에 `CMRQTUT.XC97295 / MIG_SHARE` 가 보여야 한다.
- 마운트 DB명은 `GN_DW_SHARED`. ACCOUNTADMIN으로 직접 작업하므로 `GRANT IMPORTED PRIVILEGES` 는 생략했다(별도 작업 역할을 쓸 때만 필요).
- **공유받은 DB는 읽기 전용**이다. 파일로 빼려면 스테이지 언로드(4장)가 필수.
- 🆕 **ML 16 확인 (3.1 집계 + 3.1-B 쿼리)** — 언로드 전에 반드시 통과시킨다.
  - 스키마별 집계 기대 = **69 테이블**(CRM 45 + AGENCY 4 + ERP 1 + GA4 3 + **ML 16**).
  - ML 테이블명이 전부 `ML_RST_DATA_` 접두인지 확인한다(03번 3.1-B).
  - 🔴 **16 초과** = A 가 `ALL TABLES` 로 부여해 학습·스냅샷이 열렸다 ⇒ A 에서 회수 후 2-B 재실행.
  - 🔴 **16 미만** = 결과 테이블이 재생성되어 `GRANT` 가 소실됐다 ⇒ A 에서 2-B 재실행.
  - ⛔ 어느 쪽이든 **언로드로 넘어가지 않는다.** 잘못된 목록으로 언로드하면 C 적재까지 오염이 전파된다.

---

## 4. 2단계: B → 로컬 물리적 다운로드 (CSV + GZIP)

### 4.1 스테이지 준비
▶ 실행: **`03_데이터마이그 B_BROKER.sql`** (2번 구간)

- 스테이지: `SANDBOX.TOOLS.my_export_stage`. **스테이지가 먼저 존재해야** `COPY INTO @stage` 가 가능하다.
- 파일 포맷은 별도 객체 없이 **스테이지에 인라인**(`TYPE=CSV`, `FIELD_OPTIONALLY_ENCLOSED_BY='"'`, `COMPRESSION=GZIP`)으로 지정했다.
- ⚠️ **언로드 전 스테이지를 반드시 비운다**(`CREATE OR REPLACE STAGE` 또는 `REMOVE @stage`). `COPY INTO ... OVERWRITE=TRUE` 는 **같은 이름 파일만** 덮어쓰고 폴더는 비우지 않는다. 병렬 워커/분할 개수가 배치마다 달라지면 이름이 겹치지 않은 **이전 배치 파일이 살아남아** 로컬·C까지 함께 옮겨진다(8장-6에서 실제로 겪음).
- ⚠️ 인라인 최소 옵션으로 언로드한 결과 **NULL이 `\\N`(3글자)** 로 기록되었고, C 적재 시 `NULL_IF` 보정이 필요했다(8장-1, `05_..C_CONSUMER.sql` 의 `FF_CSV_LOAD`).
  재이관 시에는 **언로드 단계에서** `NULL_IF`·`EMPTY_FIELD_AS_NULL`·`TIMESTAMP_FORMAT` 을 명시하는 편이 안전하다.

### 4.2 전 테이블 일괄 언로드
▶ 실행: **`03_데이터마이그 B_BROKER.sql`** (5~6번 구간)

- `GN_DW_SHARED.INFORMATION_SCHEMA.TABLES` 를 순회하며 `BRONZE_*` **와 `ML`** 의 모든 BASE TABLE을 동적 `COPY INTO` 한다.
- **경로 규칙**: `@stage/<스키마>/<테이블>/` — 이 구조가 4.3 다운로드 → 5.3 업로드 → 5.4·5.6 적재까지 그대로 이어진다.
- 완료 후 `LIST` 로 **파일 수를 기록**한다. 5.3 업로드 후 C 스테이지 파일 수와 대조할 기준값이다.
- `HEADER = TRUE` → C 적재의 `SKIP_HEADER = 1` 과 짝.
- `MAX_FILE_SIZE` 미지정 → 기본값 분할. 대용량 튜닝 시 `MAX_FILE_SIZE = 268435456`(약 256MB) 등을 지정한다(최대 5GB).
- 소량 검증만 할 때는 스크립트의 `SAMPLE (10000 ROWS)` 주석을 해제한다.
- 익명 블록은 **`EXECUTE IMMEDIATE $$ ... $$;` 로 감싸 놓았다**(8장-3). Workspace의 파일 전체 실행은 `;` 단위로 문장을 자르므로 감싸지 않으면 블록이 조각나 문법 오류가 난다.
- **웨어하우스 크기**: 1.17억 행 / 약 2.8GB 를 언로드한다. XS로는 오래 걸리고 문장 타임아웃 위험이 있어 언로드 구간만 M~L로 올린 뒤 원복한다. 2026-08-12 실행은 LARGE 사용.
- **2026-08-12 실행 결과**: 53개 테이블 → 435 파일 / 52 폴더 / 3,427,853,840 B. `BRONZE_BIGQUERY.SYNC_ERR_INFO`(0행)는 파일이 생성되지 않아 폴더가 52개인 것이 정상이다.
- 🆕 **ML 언로드에서 달라지는 것은 없다.** 같은 루프·같은 포맷·같은 경로 규칙을 쓴다.
  - 🔴 단 `PREDICTION VARIANT` 4종은 CSV 로 나가면서 **JSON 이 문자열이 된다.**
    이것은 언로드 쪽에서 고칠 수 없고 **C 적재에서 `TRY_PARSE_JSON` 으로 복원**한다(5.6). GA4 와 같은 함정이다.
  - ML 규모가 브론즈의 약 0.9% 라 **웨어하우스 상향 판단에 영향을 주지 않는다**(참고값 기준).
  - **ML 실측 기준값은 미확보**다. 6단계 `LIST` 집계 결과를 03번 파일 주석과 05번 A.1 (1) 주석에 **둘 다** 적는다.
    기대 = ML **16 폴더**. 16 미만이면 0행 테이블이 있다는 뜻이므로 3.1 집계로 어느 테이블인지 특정한다.

### 4.3 로컬로 다운로드 (Windows)
`GET`은 클라이언트에서 실행해야 한다. **Snowsight 워크시트에서는 불가.**

```powershell
# 0) 로컬 폴더 완전 초기화 — 이전 배치 파일이 남아 있으면 그대로 C까지 올라간다 (8장-6)
Remove-Item -Recurse -Force downloaded_data

# 전체 일괄 다운로드 (B 계정 연결)
snow stage copy "@SANDBOX.TOOLS.my_export_stage/" "downloaded_data/" `
    -c <B_CONNECTION> --recursive

# 스키마 단위로 나눠 받기
foreach ($SCHEMA in @('BRONZE_CRM', 'BRONZE_AGENCY', 'BRONZE_ERP', 'BRONZE_BIGQUERY', 'ML')) {
    Write-Host "=== 다운로드: $SCHEMA ==="
    snow stage copy "@SANDBOX.TOOLS.my_export_stage/$SCHEMA/" "downloaded_data/$SCHEMA/" `
        -c <B_CONNECTION> --recursive
}

# 다운로드 파일 수 / 용량 확인
Get-ChildItem downloaded_data -Recurse -File | Measure-Object -Property Length -Sum
```

- 파일은 `.csv.gz` 로 **압축 상태 그대로** 저장된다.
- 다운로드 구조: `downloaded_data\<SCHEMA>\<TABLE>\*.csv.gz` (5.3 업로드 구조와 동일)
- 다운로드 후 파일 개수/총 용량을 B의 `LIST` 결과와 대조한다.
- DDL 스크립트(`04_...DDL_20260730.sql` · 🆕 `06_...ML_DDL_20260814.sql`)도 함께 C 작업 폴더에 준비한다.

> 대용량이면 로컬 대신 **외부 스테이지(S3/GCS/Azure)** 경유가 유리하다(GET/PUT 없이 계정 간 직접 이관, 6.2).

### 4.4 참고: 반정형/바이너리 컬럼의 대안 경로 (미사용)
`VARIANT/OBJECT/ARRAY/GEOGRAPHY/GEOMETRY/BINARY` 컬럼은 CSV 왕복 시 손실·오변환 위험이 있어, 일반적으로는 **해당 테이블만 Parquet(SNAPPY)** 로 분리 이관하는 것이 권장된다.

> ⚠️ **이번 이관은 이 경로를 쓰지 않았다.** GA4를 포함한 `BRONZE_*` 전 테이블을 CSV+GZIP으로 일괄 언로드하고,
> GA4 VARIANT는 C 적재 시 `TRY_PARSE_JSON` 으로 복원했다(5.5).
> 이번 대상 중 반정형 컬럼 보유 테이블은 `BRONZE_BIGQUERY.events_20260501` (VARIANT 11개) 하나뿐이었다.
> Parquet 경로를 택할 경우: 언로드는 `COMPRESSION=SNAPPY`, 적재는 `MATCH_BY_COLUMN_NAME = CASE_INSENSITIVE` 사용 가능(CSV와 달리 지원), 업로드 시 재압축 금지.
>
> 🆕 **[2026-08-14] ML 추가로 반정형 보유 테이블이 늘었다** — `PREDICTION VARIANT` 를 가진 결과 **4종**
> (`SPNSR_CHURN_12M` · `MBER_CHURN_12M` · `MBER_INC_12M` · `LOYAL_MBER`) ⇒ GA4 2종 + ML 4종 = **6종**.
> 🟢 그럼에도 **ML 도 CSV 경로를 유지한다.** 근거:
> · VARIANT 가 **테이블당 1개이고 전부 마지막 컬럼**이라 `$n` 위치 매핑이 단순하다(GA4는 11개·중간 산재).
> · GA4 에서 같은 방식이 이미 검증됐고, 포맷을 둘로 나누면 스테이지·파일포맷·검증 경로가 이중화된다.
> · ⚠️ 대가 = **적재에서 반드시 변환**해야 하고, 안 하면 오류 없이 문자열로 들어간다 ⇒ 5.6 의 검증이 그 방어선이다.

---

## 5. 3단계: 로컬 → C 계정 적재

### 5.1 DDL로 스키마/테이블 재생성 — 필수 선행
▶ 실행: **`04_데이터마이그 GN_DW_BRONZE_DDL_20260730.sql`**

CSV는 위치 기반 적재이므로 테이블이 **먼저** 있어야 한다. DDL 미실행 상태로 적재하면 `loaded tables: 0` 이 된다.

```powershell
# DDL 일괄 실행 (C 계정 연결)
snow sql -c <C_CONNECTION> -f "50_handoff\04_데이터마이그 GN_DW_BRONZE_DDL_20260730.sql"

# 생성 확인 (53개 기대: CRM 45 / AGENCY 4 / ERP 1 / GA4 3)
snow sql -c <C_CONNECTION> -q "SELECT table_schema, COUNT(*) FROM GN_DW.INFORMATION_SCHEMA.TABLES WHERE table_schema LIKE 'BRONZE_%' AND table_type='BASE TABLE' GROUP BY 1 ORDER BY 1"
```

### 5.1-B 🆕 ML ML 스키마/테이블 생성 — ML 적재의 필수 선행
▶ 실행: **`06_데이터마이그 GN_DW_ML_DDL_20260814.sql`**

```powershell
# ML DDL 실행 (C 계정 연결) — 04번과 선후 관계 없음
snow sql -c <C_CONNECTION> -f "50_handoff\06_데이터마이그 GN_DW_ML_DDL_20260814.sql"

# 생성 확인 (16개 기대)
snow sql -c <C_CONNECTION> -q "SELECT COUNT(*) FROM GN_DW.INFORMATION_SCHEMA.TABLES WHERE table_schema='ML' AND table_type='BASE TABLE'"

# VARIANT 위치 확인 (4행 기대 · 각 테이블의 마지막 컬럼이어야 한다)
snow sql -c <C_CONNECTION> -q "SELECT table_name, column_name, ordinal_position FROM GN_DW.INFORMATION_SCHEMA.COLUMNS WHERE table_schema='ML' AND data_type='VARIANT' ORDER BY 1"
```

- 06번은 `20_ML_ddl.sql` 에서 **결과 16종의 `CREATE TABLE` 문만 무변경 발췌**한 것이다(컬럼 순서·타입·COMMENT 포함).
- 스키마는 `with managed access` + 소유 `GN_DW_ADMIN` 으로 만든다 — **원천 A 의 실측 DDL과 다른 유일한 지점**이며,
  C 환경의 브론즈 4개 스키마 관례에 맞춘 것이다(테이블 구조는 무변경).
- 🔴 **06번은 프로시저·모델을 만들지 않는다.** 이유:
  - `SNOWFLAKE.ML.CLASSIFICATION` / `FORECAST` 모델 **14종은 프로시저 본문 안에서 `CREATE` 되고**,
    학습 데이터(`ML_TRAIN_DATA_*` · 스냅샷)가 없으면 **생성 자체가 실패**한다.
  - ⇒ **C 계정에서 재학습·재예측은 불가하다.** 이번 이관의 목표는 「예측 결과를 데이터로 받아
    SERVING/SV/Agent 로 소비」까지다. 새 기준월이 필요하면 원천 계정에서 돌린 뒤 결과를 재이관한다.
- ⚠️ `20_ML_ddl.sql`(3,217줄)을 그대로 실행하면 안 된다 — 모델·프로시저 구간에서 실패한다. 06번을 쓴다.

### 5.2 적재용 파일 포맷/스테이지 준비
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (상단 ~ A.2 구간)

- 스테이지: `SANDBOX.TOOLS.MIG_LOAD_STAGE`, 파일 포맷: `SANDBOX.TOOLS.FF_CSV_LOAD`.
- ⚠️ **NULL 토큰 주의**: 언로드 데이터의 NULL은 `\\N`(백슬래시 2개 + N, **3글자**)로 기록돼 있었다(언로드 시 백슬래시 이스케이프로 이중화).
  숫자 컬럼에서 `Numeric value '\N' is not recognized` 오류의 직접 원인이므로 `NULL_IF` 에 3글자 토큰을 반드시 포함한다(스크립트에 반영됨).
- 확인법: 스테이지 파일을 `SELECT $n, LENGTH($n) ... WHERE TRY_TO_DOUBLE($n) IS NULL` 로 조회해 NULL 토큰의 실제 길이(3)를 확인.
- `SKIP_HEADER = 1` (언로드 `HEADER=TRUE` 와 대칭), `EMPTY_FIELD_AS_NULL = TRUE`, `TRIM_SPACE = FALSE`, `ESCAPE_UNENCLOSED_FIELD = NONE`.

### 5.3 로컬 파일 → C 스테이지 업로드 (Windows)
다운로드 구조를 그대로 스테이지에 재현한다: `<SCHEMA>/<TABLE>/*.csv.gz`
PowerShell에서는 줄 연속 문자로 `` ` ``(백틱), 배열은 `@( )` 를 사용한다.

```powershell
# 0) C 스테이지 초기화 — 이전 배치 잔존 파일 제거 (8장-6)
snow sql -c <C_CONNECTION> -q "REMOVE @SANDBOX.TOOLS.MIG_LOAD_STAGE/"

# 이미 GZIP 압축 상태이므로 재압축 금지(--no-auto-compress)
foreach ($SCHEMA in @('BRONZE_ERP', 'BRONZE_AGENCY', 'BRONZE_BIGQUERY', 'ML', 'BRONZE_CRM')) {
    Write-Host "=== 업로드: $SCHEMA ==="
    snow stage copy "downloaded_data/$SCHEMA/" `
        "@SANDBOX.TOOLS.MIG_LOAD_STAGE/$SCHEMA/" `
        -c <C_CONNECTION> --recursive --no-auto-compress --overwrite
}

# 업로드 확인
snow stage list-files @SANDBOX.TOOLS.MIG_LOAD_STAGE/ -c <C_CONNECTION>
```

- 업로드 순서는 **작은 스키마 → 큰 스키마**(ERP → AGENCY → GA4 → 🆕 ML → CRM)로 하면 조기 실패를 빨리 발견할 수 있다.
- 업로드 후 **반드시 5.3.1 검증을 수행**한 뒤 5.4로 넘어간다.

### 5.3.1 적재 전 스테이지 검증 — 필수
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (A.1 구간)

네 가지를 확인한다. 하나라도 걸리면 해당 파일을 `REMOVE` 하고 처음부터 다시 확인한다.
(🆕 4번은 2026-08-13 에 신설된 「테이블 쪽 원인」 방어선이며, 2026-08-14 에 대상이 ML 까지 확장됐다.)

| # | 검사 | 통과 기준 |
|---|------|-----------|
| 1 | C 스테이지 파일 수 vs B `unloaded_files` | 동일 |
| 2 | 폴더별 헤더 문자열 종류(`distinct_headers`) | 1 |
| 3 | 폴더별 중복 파일(`dup_files`) | 0 |
| 4 | CSV 헤더 ↔ 대상 테이블 컬럼 대조 | `FILE_MISSING` 1건(GA4 `SYNC_ERR_INFO`) 외 0건 |

- 2번이 1을 넘으면 **구/신 스키마 파일 혼재** → 컬럼 수 불일치로 적재가 실패한다.
- 3번이 0을 넘으면 **동일 내용 파일 중복** → 오류 없이 조용히 2배로 적재된다. 이게 더 위험하다.
- 검사 쿼리는 진단용 포맷 `FF_CSV_PEEK`(`FIELD_DELIMITER = NONE`, `SKIP_HEADER = 0`)으로 줄 전체를 읽어 헤더 비교와 내용 해시를 한 번에 수행한다. 스테이지 전량 스캔이므로 업로드 직후 1회만 실행한다.
- 🆕 **4번 검사의 대상에 ML 이 포함됐다**(A.1 (4) · `table_schema='ML' AND table_name LIKE 'ML_RST_DATA_%'`).
  - ML 이 **전량 `TABLE_MISSING`** 이면 **06번 DDL 미실행**이다(브론즈만 전량이면 04번 미실행).
  - ML 에 **`COUNT_MISMATCH`** 가 나오면 **원천 ML 이 교체된 것**이다 ⇒ A 에서 `20_ML_ddl.sql` 을 재추출하고
    06번을 갱신한 뒤 해당 테이블만 재생성한다. 모델이 바뀌면 실제로 일어나는 사고다.
  - 🔴 ML 에 **`ORDER_OR_NAME_MISMATCH`** 는 특히 위험하다 — `PREDICTION` 이 마지막 컬럼이라는 전제가 깨지면
    5.6 의 `$n` 매핑이 어긋나 **JSON 이 다른 컬럼에 들어간다.** 반드시 해소한 뒤 적재한다.

### 5.4 스테이지 → 테이블 적재 (스키마 일괄)
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (A.3 프로시저 생성 → A.4 `CALL`)

- 프로시저 `SANDBOX.TOOLS.LOAD_BRONZE_SCHEMA(SCH)` 가 스키마 내 전 테이블을 순회하며 `@stage/<SCHEMA>/<TABLE>/` 에서 `COPY INTO` 한다.
- **커서 + `FETCH INTO`** 방식으로 작성했다. `FOR rec IN (쿼리) DO ... rec.col`(레코드 점 표기)은 이 환경에서 `invalid identifier` 로 실패했다(8장-2).
- 적재 옵션: `ON_ERROR = ABORT_STATEMENT`(정확도 우선, 오류 시 해당 테이블 COPY 전체 롤백), `PURGE = FALSE`(검증 후 수동 정리).
- 실행 순서: `BRONZE_ERP`(1) → `BRONZE_AGENCY`(4) → `BRONZE_CRM`(43, 대용량·수 분 소요).
- **`BRONZE_BIGQUERY` 는 프로시저로 적재하지 않는다.** `CALL ...('BRONZE_BIGQUERY')` 는 주석 처리되어 있다. 일반 COPY로 적재하면 VARIANT가 **문자열**로 들어가 `col:key` / `col[0]` 탐색이 불가해진다. GA4 3개 테이블은 전부 5.5에서 처리한다.
- 🆕 **`ML` 도 이 프로시저로 적재하지 않는다.** 같은 이유(VARIANT 4종)이며 `CALL ...('ML')` 은 주석 처리되어 있다. ML 은 전부 5.6에서 처리한다.
- `MATCH_BY_COLUMN_NAME`은 CSV에서 사용 불가. 반드시 5.1 DDL로 **동일 컬럼 순서**를 맞춘 뒤 적재한다.

### 5.5 GA4 적재 — CSV + JSON 복원
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (A.5.1 ~ A.5.3 구간)

| 테이블 | 컬럼 | 처리 |
|--------|-----:|------|
| `"events_20260501"` | 30 | `TRY_PARSE_JSON` 변환 COPY (A.5.1) |
| `"events_20260719"` | 31 | `TRY_PARSE_JSON` 변환 COPY, 끝에 `$31` 스칼라 추가 (A.5.2) |
| `SYNC_ERR_INFO` | 4 | VARIANT 없음 → 일반 COPY (A.5.3) |

- **VARIANT 컬럼 위치(1-based)**: 4, 11, 12, 14, 15, 16, 18, 22, 23, 24, 29 — 두 `events` 테이블 공통.
- `events_20260719` 의 31번 컬럼 `event_original_occurrence_timestamp` 는 스칼라이므로 변환하지 않는다.
- 컬럼 순서/개수가 DDL과 어긋나면 전량 실패한다. DDL 변경 시 이 목록과 A.5 의 `$n` 위치를 함께 갱신해야 한다.
- 검증: A.6 (3)번 쿼리로 두 테이블의 `TYPEOF("event_params")` 가 **ARRAY**, `parsed = n_rows` 인지 확인한다.

### 5.6 🆕 ML ML 예측결과 적재 — 16종 (VARIANT 4종은 JSON 복원)
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (A.5-B 구간)
▶ 선행 필수: **5.1-B**(06번 DDL 실행) + **5.3.1 (4)**(ML COUNT/ORDER 불일치 0건)

| 구간 | 대상 | 처리 |
|------|------|------|
| A.5-B.1 | VARIANT 없는 **12종** | 전용 프로시저 `SANDBOX.TOOLS.LOAD_ML_RESULT_TABLES()` 로 일괄 COPY. 기대 반환값 `loaded tables: 12` |
| A.5-B.2 | VARIANT **4종** | 테이블별 개별 COPY + `TRY_PARSE_JSON($n)` |
| A.5-B.3 | 학습 33종 · 프로시저 · 모델 · 뷰 | ⛔ 하지 않는다 |
| A.5-B.4 | 검증 | 행수 · **JSON 파싱** · 확률 평탄화 · 기준월 |
| A.5-B.5 | 후속 작업 | SERVING 뷰 → SV → Agent (포인터만) |

- 🔴 **왜 브론즈 프로시저를 재사용하지 않는가**: `LOAD_BRONZE_SCHEMA('ML')` 는 전 테이블에 일반 COPY 를 걸어
  VARIANT 4종의 `PREDICTION` 을 **JSON 문자열**로 넣는다. 오류가 나지 않고, `PREDICTION:probability:"1"` 탐색이
  조용히 실패한다 ⇒ **SERVING 뷰에서 NULL** 이 되고 SV·Agent 는 그대로 답한다(무증상 오답).
  전용 프로시저는 4종을 **테이블명으로** 제외한다(위치 기준이 아니라 이름 기준이라 컬럼 순서 변경에 영향받지 않는다).
- VARIANT `$n` 위치 — 전부 **마지막 컬럼**이므로 앞 컬럼은 스칼라로 그대로 넘긴다:

| 테이블 | 컬럼 수 | `PREDICTION` |
|--------|--------:|--------------|
| `ML_RST_DATA_SPNSR_CHURN_12M` | 18 | `$18` |
| `ML_RST_DATA_MBER_CHURN_12M` | 18 | `$18` |
| `ML_RST_DATA_MBER_INC_12M` | 21 | `$21` |
| `ML_RST_DATA_LOYAL_MBER` | 22 | `$22` |

- **`PREDICTION` 의 실제 구조**(원천 계정 실측 표본 · `20_ML_SV_설계.md §4`):
  `{"class":"0", "probability":{"0":0.92…,"1":0.07…}, "logs":{...}}`
  ⇒ SERVING 뷰는 `PREDICTION:probability:"1"::FLOAT`(확률) · `PREDICTION:class::VARCHAR`(분류)로 평탄화한다.
- 🔴 **검증에서 반드시 볼 것 (A.5-B.4)**:
  - (3) `TYPEOF(PREDICTION)` = **`OBJECT`** · `unparsed = 0`. `VARCHAR` 면 문자열로 들어간 것 ⇒ `TRUNCATE` 후 재적재.
  - (4) 확률이 0~1 이고 NULL 이 아님 — (3) 이 통과해도 **키 경로가 다르면** 여기서 NULL 로 드러난다.
  - (5) `STDR_MT` 목록 — 원천이 월별 `DELETE`+`INSERT` **누적**이라 **여러 기준월이 섞여 있을 수 있다.**
    여러 개면 SV·Agent 에서 기준월을 고정해야 한다(합산하면 같은 미래월이 중복 계상된다).
- ⚠️ **ML 적재는 「끝」이 아니다.** 데이터가 들어와도 SERVING 뷰 7종 · SV 7종 · Agent 도구 7개가 없으면
  Agent 는 ML 을 답하지 못한다. 순서 = `21_ML_SERVING_뷰_DDL.sql` → `22_ML_SV_DDL.sql` → Agent spec.
  🟢 `GN_DW.ML` 에 소비 역할 `GRANT` 는 **불필요**하다(뷰가 소유자 권한으로 읽는다 · `20_ML_SV_설계.md §7-A`)
  ⇒ 학습·중간 테이블이 구조적으로 차폐된다. 애초에 이관하지 않았으므로 이중으로 닫힌다.
- ⚠️ **[2026-08-14 실측] 위 후속 3층은 현재 계정(DV07626)에 배포되어 있지 않다** —
  `SHOW VIEWS IN SCHEMA GN_DW.SERVING` **0건** · `SHOW SEMANTIC VIEWS` 9종에 `SV_ML_*` **없음**.
  `20_ML_SV_설계.md §7` 의 배포 기재는 **다른 계정 기준**이므로, ML 적재 후 **재배포가 필요**하다.
  🔴 그 불일치의 원인은 이 문서가 판정하지 않는다(§11-4 확인 항목).

---

## 6. 검증

### 6.1 대조 항목
▶ 실행: **`05_데이터마이그 C_CONSUMER.sql`** (A.6 구간, C에서 실행) + **`03_데이터마이그 B_BROKER.sql`** (3.1 구간, B에서 실행)

- 스키마별 **테이블 수 / 총 행수** (B `GN_DW_SHARED` 3.1 ↔ C `GN_DW` A.6-(1))
- **테이블별 행수** (B 3.1 ↔ C A.6-(4)) — 합계만 맞고 테이블별로 어긋나는 경우를 잡는다
- **빈 테이블(row_count = 0) 0건**
- GA4 `event_params` 의 `TYPEOF` 가 **ARRAY**, 파싱 행수 = 전체 행수
- 🆕 ML `PREDICTION` 의 `TYPEOF` 가 **OBJECT**, 파싱 행수 = 전체 행수 (A.5-B.4 (3))
- `TABLE(VALIDATE(<table>, JOB_ID => '_last'))` 거부 행 0건 — 세션 종료 후에는 `COPY_HISTORY`(A.6-(5))

체크리스트:
- [ ] 스키마별 테이블 수 일치 (**69** = 브론즈 53 + 🆕 ML 16)
- [ ] 스키마별 행수 일치
- [ ] 테이블별 행수 일치 (A.6-(4) ↔ B 3.1)
- [ ] 빈 테이블 0건 (`SYNC_ERR_INFO` 2개는 원본이 비어 있을 수 있어 예외)
- [ ] 주요 컬럼 합계/최소/최대 값 비교
- [ ] 샘플 레코드(PK 기준 100건) 육안 대조
- [ ] NULL vs 빈문자열 구분 정상 (CSV 특성상 중요)
- [ ] 날짜/타임스탬프 포맷 정상 변환
- [ ] `VALIDATE` 거부 행 0건
- [ ] 🆕 ML 16종 전량 실재 · `PREDICTION` 4종 `TYPEOF=OBJECT` · 확률 0~1 · `STDR_MT` 목록 확인 (A.5-B.4)

### 6.2 적재 결과 이력
초판 이관(2026-07-13, 48 테이블 기준):

| 스키마 | 테이블 | 적재 행수 |
|--------|-------:|----------:|
| BRONZE_CRM | 43 | 112,512,161 |
| BRONZE_BIGQUERY | 1 | 287,025 |
| BRONZE_AGENCY | 3 | 235,572 |
| BRONZE_ERP | 1 | 2,041 |
| **합계** | **48** | **112,536,799** |

- 빈 테이블 0건, GA4 `event_params` = ARRAY(파싱 정상, 287,025행 전부).
- **2026-07-30 재이관(51 테이블 기준)**: 스테이지 잔존 파일 문제(8장-6)로 초기 적재 실패 → 스테이지·로컬 초기화 후 재실행.

**2026-08-12 A 실측 재확인 → 51이 아니라 53 테이블 (정정)**

| 스키마 | 테이블 | 행수 | 용량(B) |
|--------|-------:|----------:|----------:|
| BRONZE_CRM | **45** | 115,875,113 | 2,834,600,960 |
| BRONZE_BIGQUERY | 3 | 576,441 | 129,906,688 |
| BRONZE_AGENCY | 4 | 243,550 | 5,719,040 |
| BRONZE_ERP | 1 | 4,301 | 328,704 |
| **합계** | **53** | **116,699,405** | **2,970,555,392** |

- 04번 DDL 2판에 **2개 테이블이 누락**되어 있었다 → 3판(2026-08-12)에 추가.
  `BRONZE_CRM.TM_MM_FDRM_MBER_RELATNSP_DVLP_AMT`(1,134,848행), `BRONZE_CRM.TM_MM_FDRM_MBER_SPNSR`(2,228,064행)
- A 실측(02_1 A1/A2)과 B의 `GN_DW_SHARED` 집계가 테이블 단위까지 완전 일치함을 확인 → 공유 누락 없음.
- **B 언로드 완료(2026-08-12)**: 53개 테이블 → **435 파일 / 52 폴더 / 3,427,853,840 B** (GZIP CSV).
  폴더가 52개인 것은 `BRONZE_BIGQUERY.SYNC_ERR_INFO`(0행)가 파일을 생성하지 않기 때문이며 정상이다.
- 남은 단계: 로컬 다운로드(4.3) → C DDL 3판 실행(5.1) → C 적재·검증(5.4~6장).
- **테이블별 대조 쿼리 준비 완료**: A는 `02_..A_PRODUCER.sql` 5단계, B는 `03_..B_BROKER.sql` 3.1, C는 `05_..C_CONSUMER.sql` A.6-(4) 에서 동일 형식(`table_schema, table_name, row_count, bytes`)으로 뽑아 비교한다.

**🆕 [2026-08-14] ML 16종 추가 — 실측 미확보**

| 스키마 | 테이블 | 행수 | 용량(B) | 출처 |
|--------|-------:|-----:|--------:|------|
| ML | 16 | **미측정** (참고 1,045,732) | **미측정** | 참고값은 원천 계정 라이브 O74 · 정본 `20_ML_SV_설계.md §0-A` |

- 🔴 **위 참고값은 A 계정 실측이 아니다.** A 에서 02번 5단계를 실행해 `02_1_A DB정보.sql` **A7** 로 등재한 뒤
  그 값을 이 표와 03번 6단계 주석, 05번 A.1 (1)·A.6 (1) 주석에 **함께** 채운다(한 곳만 채우면 검증이 무력화된다).
- ⚠️ ML 은 프로시저가 기준월 단위로 `DELETE`+`INSERT` 하는 **누적 테이블**이다.
  ⇒ 참고값(기준월 `202606` 단일)과 이관 시점 행수가 다를 수 있고, 그것은 오류가 아니다.
- ⚠️ ML 은 브론즈와 달리 **이관 이력이 없다**(초회 이관). 즉 6.2 의 「초판/2판/3판」 계보와 무관하다.

### 6.3 재적재/멱등성
- `COPY INTO` 는 로드 메타데이터로 이미 적재된 파일을 건너뛴다 → 프로시저 재실행 시 신규 파일만 적재(안전). 강제 재적재는 `FORCE = TRUE`.
- `ON_ERROR = ABORT_STATEMENT` 이므로 한 테이블 오류 시 해당 COPY 전체 롤백 → 부분 적재 없음.
- 🆕 **ML 재이관(새 기준월 반영) 시**: 원천이 기준월 단위 `DELETE`+`INSERT` 이므로 파일 내용이 **새 파일명**으로 나온다
  ⇒ `COPY` 가 신규 파일만 적재해 **기준월이 누적**된다(원천과 같은 동작). 이것이 정상이다.
  🔴 단 **같은 기준월을 다시 적재하면 그 월이 두 번 들어간다** — 원천은 `DELETE` 로 막지만 C 는 `COPY` 뿐이다.
  ⇒ 같은 기준월을 다시 넣을 때는 대상 테이블에서 `DELETE WHERE STDR_MT = '<월>'` 를 먼저 실행하거나
  테이블을 `TRUNCATE` 한 뒤 전량 재적재한다. A.5-B.4 (5) 의 `STDR_MT` 목록으로 확인한다.

### 6.4 참고: 외부 스테이지 경유(대안)
로컬 다운로드가 부담되면 B에서 외부 스테이지(S3/GCS/Azure)로 `COPY INTO` 후, C에서 동일 외부 스테이지를 참조해 바로 `COPY INTO` 할 수 있다. 물리적 파일 이동/네트워크 전송을 줄일 수 있다.

---

## 7. 정리(Cleanup)

작업 완료 및 검증 후 수행한다. 관련 SQL은 각 스크립트 하단(주석 처리)에 있다.

| 대상 | 작업 | 위치 |
|------|------|------|
| B 언로드 스테이지 | `REMOVE @SANDBOX.TOOLS.my_export_stage/` | `03_..B_BROKER.sql` 7.2(주석) |
| B 공유 마운트 DB | `DROP DATABASE GN_DW_SHARED` | `03_..B_BROKER.sql` 7.1(주석) |
| A 공유 | `REMOVE ACCOUNTS` → `REVOKE` 전체(🆕 ML 스키마·테이블 포함) → `DROP SHARE mig_share` | `02_..A_PRODUCER.sql` 7장(주석) |
| C 적재 스테이지 | `REMOVE @SANDBOX.TOOLS.MIG_LOAD_STAGE/` | `05_..C_CONSUMER.sql` A.7(주석) |
| C 진단 객체 | `FF_CSV_PEEK`, `LOAD_BRONZE_SCHEMA`, 🆕 `LOAD_ML_RESULT_TABLES` DROP | `05_..C_CONSUMER.sql` A.7(주석) |

```powershell
# 로컬 다운로드 파일 정리 (민감정보 포함 시 안전 삭제 도구 사용)
Remove-Item -Recurse -Force downloaded_data
```

- [ ] B 언로드 스테이지 정리
- [ ] `GN_DW_SHARED` DROP (export/검증 완료 확인 후)
- [ ] A 공유 해제 (🆕 ML `USAGE`·테이블 `SELECT` 회수 포함)
- [ ] C 적재 스테이지 정리
- [ ] 로컬 `.csv.gz` / DDL 파일 삭제

- 🔴 **ML 정리 시점 주의**: A 공유를 끊으면 **ML 재이관이 불가능해진다.**
  ML 은 SERVING 뷰·SV·Agent 재배포(5.6 마지막 항목)까지 끝나야 「검증 완료」다
  ⇒ 뷰·SV 가 실제로 값을 반환하는 것을 확인한 뒤에 A 공유를 해제한다.

---

## 8. 이번에 겪은 함정 (핸드오버 핵심)

1. **NULL 토큰 `\\N`(3글자)**: 기본 `NULL_IF` 로는 숫자 컬럼 로드 실패(`Numeric value '\N' is not recognized`). → `05_..C_CONSUMER.sql` 의 `FF_CSV_LOAD` 3종 토큰 필수(5.2).
2. **스크립팅 레코드 점 표기 실패**: `FOR rec IN (쿼리) DO rec.col` → `invalid identifier`. → 커서 + `FETCH INTO`(5.4).
   - 단, `03_..B_BROKER.sql` 처럼 **커서를 먼저 DECLARE 한 뒤** `FOR rec IN c1 DO rec.col` 은 정상 동작했다.
3. **익명 블록 세미콜론 분할**: 툴/CLI가 `;` 에서 문을 잘라 파싱 오류 → `EXECUTE IMMEDIATE $$ ... $$;` 로 감싸거나 저장 프로시저로 실행.
4. **GA4 VARIANT**: CSV 경유 시 JSON이 문자열로 저장 → `TRY_PARSE_JSON` 변환 필수(5.5).
   - 🆕 **[2026-08-14] 같은 함정이 ML 에서 반복된다** — `PREDICTION VARIANT` 4종(5.6).
     🔴 GA4 보다 더 위험한 이유 = **SERVING 뷰가 그 JSON 경로를 직접 쓴다.** 문자열로 들어가면
     오류가 아니라 **뷰에서 NULL** 이 되고, SV·Agent 는 「값이 없다」로 답한다(무증상 오답).
     ⇒ 「VARIANT 를 가진 테이블은 스키마 일괄 프로시저에서 제외한다」를 **규칙으로** 둔다(A.4 주석 · A.5-B).
5. **대상 테이블 미생성**: DDL 미실행 상태로 로드하면 `loaded tables: 0`. → 5.1 선행 확인.
6. **스테이지 잔존 파일(2026-07-30)**: B 언로드 스테이지를 비우지 않아 7/13 배치 파일이 살아남았고, 로컬·C 스테이지까지 함께 옮겨졌다.
   `COPY INTO ... OVERWRITE=TRUE` 는 **같은 이름 파일만** 덮어쓴다(폴더를 비우지 않음). 워커 분할 개수가 배치마다 달라 이름이 겹치지 않으면 구 파일이 남는다.
   - 증상 A: `Number of columns in file (62) does not match that of the corresponding table (64)` — `BRONZE_ERP.BDGT_ACMSLT_LEDGER`. 신규 컬럼이 **중간(11·12번)** 삽입이라 `ERROR_ON_COLUMN_COUNT_MISMATCH = FALSE` 로 넘기면 이후 전체가 한 칸씩 밀려 **오적재**된다. 절대 쓰지 말 것.
   - 증상 B: 스키마가 같은 테이블은 오류 없이 **중복 적재**. 예) `TD_MS_PSTMTR_LQY_SNDNG` 의 두 파일이 행수 2,022 / min·max 동일.
   - 대책: 4.1 언로드 전 `REMOVE`, 4.3 로컬 폴더 초기화, 5.3 업로드 전 C 스테이지 `REMOVE`, **5.3.1 적재 전 검증**(스크립트 A.1)을 4중으로 배치했다.
7. 🆕 **[2026-08-14] 스키마 통짜 GRANT 가 「노출 금지」를 조용히 깬다**: `GRANT SELECT ON ALL TABLES IN SCHEMA GN_DW.ML` 을 쓰면
   학습·스냅샷 **33종이 함께 열리고**, B 의 언로드 루프가 그것을 그대로 가져가 C 에 적재한다.
   오류가 나지 않으므로 **적재가 끝난 뒤에야 발견**된다. → 3.1-B 의 테이블 단위 부여 + 3.3 의 「ML 16 확인」이 그 방어선이다.
8. 🆕 **[2026-08-14] `create or replace TABLE` 은 GRANT 를 파괴한다**: 정본 `20_ML_ddl.sql` 이 결과 테이블을 그 구문으로 만들기 때문에,
   원천이 모델을 다시 돌리면 **A 가 부여한 `SELECT` 가 사라진다**. 증상은 「B 에서 ML 이 16개가 아니라 더 적게 보인다」다.
   → 3.3 확인에서 잡고, A 에서 02번 2-B 를 재실행한다. **재실행이 정상 운영 절차의 일부**임을 전제로 한다.

---

## 9. 주의사항 / 리스크

- **읽기 전용 공유**: B는 공유 DB를 수정할 수 없으므로 스테이지 언로드가 필수.
- **CSV 적재 정확도**: 위치 기반이므로 컬럼 순서/타입을 원본과 동일하게 재현해야 한다. 언로드/적재 옵션 대칭 필수(1.1).
- **반정형/BINARY 타입**: CSV 왕복 시 손실·오변환 위험. 이번에는 `TRY_PARSE_JSON` 으로 복원했으나, 신규 이관은 Parquet(SNAPPY) 분리 권장(4.4).
- **압축 재적용 금지**: 이미 `.csv.gz` 이므로 업로드 시 `--no-auto-compress`.
- **파일 전송은 클라이언트 실행**: Snowsight 워크시트 불가, Snowflake CLI(`snow stage copy`) 필요.
- **파일 크기 제한**: 언로드 파일 최대 5GB, `MAX_FILE_SIZE` 기본 16MB. 대용량은 분할 언로드.
- **용량 산정**: 로컬 다운로드 시 디스크 여유 공간 사전 확인.
- **보안/규정**: 물리적 다운로드 데이터의 PII 포함 여부 확인, 전송/저장 구간 암호화 및 접근 통제.
- **리전 전송 비용**: 계정/리전 간 이관 시 데이터 전송 비용 발생 가능.
- **일관성**: 이관 중 A 원본이 변경되면 스냅샷 시점 기준으로 고정 권장.
- 🆕 **ML 은 「테스트 단계 산출물」이다**(사용자 확정 2026-08-14 · `20_ML_SV_설계.md` 전제).
  모델·피처·테이블 구조가 향후 대규모로 교체될 수 있다 ⇒ **이관은 일회성 작업이 아니라 반복 작업**으로 설계됐다
  (2-B 재부여 · 06번 갱신 · A.5-B 재실행). 그 반복 비용을 전제로 두고, 구조가 굳기 전에는 자동화하지 않는다.
- 🔴 🆕 **ML 예측결과 자체에 미해소 결함 4건이 있다** — 이관과 별개이며 **이관으로 해소되지 않는다.**
  정본 = `20_ML_SV_설계.md §1` / 이슈 원장 §O74. 요약:
  · ① `MBER_CHURN_12M`·`MBER_INC_12M` 이 **회원당 중복 16,474행(18.02%)** — 테이블 COMMENT 는 「회원 단위」로 선언한다
    ⇒ `COUNT(*)` 로 위험 회원수를 세면 **18% 과대**. SV 층에서 완화했으나 **원천 미해소**.
  · ② 테이블명 `12M` ↔ 구현 **6M** 불일치 ⇒ 예측 지평 **미발행** 상태.
  · ③ 예측 모집단이 전체 회원의 **10.10%** ⇒ 「전 회원 대비 비율」 산출 불가.
  · ④ `'UNKNOWN'` 창작 라벨이 데이터에 섞여 있다(규모는 작다 · `R2-7` 위반).
  ⇒ **이관 담당자는 이 결함을 「적재 오류」로 오진하지 않도록** 위 문서를 먼저 읽는다.

---

## 10. 작업 순서 요약

| # | 계정 | 작업 | 스크립트 |
|---|------|------|----------|
| 1 | A | 공유 생성 → 브론즈 권한 부여 → 🆕 **ML 16종 테이블 단위 부여(2-B)** → `ADD ACCOUNTS = GB72026` → 확인(스키마 5·테이블 69) | `02_..A_PRODUCER.sql` |
| 2 | A | 4개 스키마 `GET_DDL` 추출 · 🆕 ML 은 추출 불필요(이미 확보) | `02_..A_PRODUCER.sql` → `04_..DDL_20260730.sql` |
| 3 | B | INBOUND 확인 → 스테이지 생성/**초기화** → `GN_DW_SHARED` 마운트 → 🆕 **ML 16 확인(3.1-B)** | `03_..B_BROKER.sql` |
| 4 | B | `BRONZE_*` + 🆕 `ML` 전 테이블 CSV(GZIP) 언로드 → `LIST` 로 파일 수 기록 | `03_..B_BROKER.sql` |
| 5 | 로컬 | **로컬 폴더 초기화** → `snow stage copy` 다운로드 (4.3 · 🆕 `ML` 폴더 포함) | PowerShell |
| 6 | C | DDL 실행(테이블 53개 생성) | `04_..DDL_20260730.sql` |
| 6-B | C | 🆕 **ML DDL 실행(테이블 16개 생성)** | `06_..ML_DDL_20260814.sql` |
| 7 | 로컬 | **C 스테이지 `REMOVE`** → 업로드 (5.3, `--no-auto-compress` · 🆕 `ML` 포함) | PowerShell |
| 8 | C | **적재 전 스테이지 검증** (파일 수/헤더 혼재/중복/🆕 **헤더↔테이블 대조**) | `05_..C_CONSUMER.sql` A.1 |
| 9 | C | 파일 포맷·프로시저 생성 → 스키마별 적재 → GA4 3개 테이블 개별 적재 | `05_..C_CONSUMER.sql` |
| 9-B | C | 🆕 **ML 적재** — 12종 일괄 + VARIANT 4종 개별(`TRY_PARSE_JSON`) → ML 검증 | `05_..C_CONSUMER.sql` A.5-B |
| 10 | B/C | 행수·무결성·거부행 대조 (69 테이블) | `05_..C_CONSUMER.sql` + B 집계 |
| 10-B | C | 🆕 **ML 후속 배포** — SERVING 뷰 7 → SV 7 → Agent 도구 7 | `05_SV-Agent_ai/21`·`22`·agent spec |
| 11 | A/B/C | 스테이지·공유·로컬 파일 정리 (🆕 **10-B 확인 후**) | 7장 |

---

## 11. 🆕 ML 이관 — 결정·전제·미해소 항목 (2026-08-14 신설)

### 11-1. 이관 범위 결정 (사용자 확정)
`GN_DW.ML` 의 BASE TABLE 은 **49개**지만 이관 대상은 **예측결과 16종**이다.

| 구분 | 개수 | 이관 | 사유 |
|------|-----:|:----:|------|
| `ML_RST_DATA_*` (예측결과) | 16 | ✅ | Agent 노출 대상. SERVING/SV 의 원천 |
| `ML_TRAIN_DATA_*` (학습) | 20 | ❌ | 사용자 지시 「학습용은 Agent 에서 사용하지 않도록」 |
| 원천 스냅샷 | 12 | ❌ | 학습·예측 입력. 원천 계정 소관 |
| `ML_PROCEDURE_LOG` | 1 | ❌ | 원천 계정의 실행 로그 |
| VIEW (`ML_TRAIN_DATA_*_V`) | 4 | ❌ | 학습 입력 뷰 |
| PROCEDURE (`SP_*`) | 14 | ❌ | 학습 데이터 의존 |
| `SNOWFLAKE.ML` 모델 | 14 | ❌ | 프로시저 본문에서 생성되며 학습 데이터 없이 생성 불가 |

🔴 **귀결 = C 계정은 「예측 결과의 소비 환경」이고 「예측 실행 환경」이 아니다.**
새 기준월 예측이 필요하면 원천 계정(성현 프로 소관)에서 프로시저를 돌린 뒤 결과를 재이관한다.

### 11-2. 이 설계가 이중으로 막는 것
학습용 노출 금지 지시가 **두 층에서** 집행된다.
1. **이관 층** — A 가 16종만 부여하므로 학습 33종은 파일 자체가 만들어지지 않는다(3.1-B).
2. **소비 층** — 소비 역할은 `GN_DW.ML` 에 권한이 없고 SV 로만 조회한다(`20_ML_SV_설계.md §7-A`).

🟢 1층이 없으면 2층만으로도 차폐되지만, 1층이 있으면 **애초에 데이터가 C 에 존재하지 않는다** — 더 강한 보장이다.

### 11-3. 이관으로 해소되지 않는 것 (원천 소관)
`20_ML_SV_설계.md §1`·§8 의 미해소 항목은 **데이터를 옮겨도 그대로 따라온다.**
· 회원 중복 18.02% · `12M`↔6M 불일치 · 모집단 10.10% · `'UNKNOWN'` 창작 라벨
· 개발금액 유형별 합계 불일치(후원사업 125%)
⇒ 이관 검증에서 이 값들이 「이상하게」 보이더라도 **적재 오류가 아니다.** 판정 정본은 그 문서다.

### 11-4. 이번 문서 작업에서 확인된 불일치 (판정 보류)
🔴 **`20_ML_SV_설계.md §7` 은 SERVING 뷰 7종 + SV 7종 + Agent 도구 7개를 배포 완료로 기재하지만,
현재 계정(`DV07626`)에는 그 객체가 없다.** 2026-08-14 실측:
· `SHOW VIEWS IN SCHEMA GN_DW.SERVING` → **0건**
· `SHOW SEMANTIC VIEWS IN DATABASE GN_DW` → 9종(`SV_AD`·`SV_BUDGET` 등)이며 `SV_ML_*` **없음**
· `GN_DW.INFORMATION_SCHEMA.SCHEMATA` → `ML` 스키마 **부재**(BRONZE 4 · SILVER · GOLD · OPS · SERVING · SECURITY · PUBLIC)

⚠️ **원인은 판정하지 않는다** — 다른 계정에서 배포한 것인지, 이후 재구축으로 사라진 것인지 근거가 없다.
🟢 이관 절차에 미치는 영향만 확정한다: **ML 적재 후 21·22번과 Agent spec 을 재배포해야 한다**(10-B 단계).

### 11-5. 미실측으로 남긴 것 (다음 실행자가 채울 것)
| # | 항목 | 채울 위치 |
|--:|------|-----------|
| 1 | A 계정 ML 16종 테이블별 행수·용량 | `02_1_A DB정보.sql` **A7** 신설 + 01번 §6.2 ML 표 |
| 2 | B 언로드 ML 폴더·파일 수·바이트 | `03_..B_BROKER.sql` 6단계 주석 + `05_..C_CONSUMER.sql` A.1 (1) 주석 |
| 3 | C 적재 후 ML 스키마 합계 행수 | `05_..C_CONSUMER.sql` A.6 (1) 주석 |

🔴 세 곳을 **함께** 채운다. 한 곳만 채우면 대조 기준이 갈려 검증이 무력화된다(브론즈에서 실제로 겪은 유형).
