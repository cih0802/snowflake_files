> #### 🟢 [2026-08-31 O125-E] 원장 §1 용량 회복 — 닫힌 O124 계열 2행 장문 셀 은퇴(승인 ㉠)
>
> **지시** = 「㉠ `retire_rows.py` 로 닫힌 O124 이하 행을 `02_상태상세` 로 은퇴 · ㉡ `--rebalance` 는 후순위」.
> 🟢 **㉠ 만으로 목표를 달성했으므로 ㉡ 는 실행하지 않았다**(조각을 늘리면 허브 색인이 커진다 · O124-B `D5`).
>
> ### 1. 결과 — 여유 부족 경고 1 → 0
>
> | 축 | 값 |
> |---|---|
> | `00_INDEX_이슈원장` max 여유 | **7,706 B → 12,139 B**(임계 8,192) |
> | 원장 조각 `-001` 크기 | 33,254 B → **28,821 B**(절감 4,433 B) |
> | 목적지 `02_상태상세-006` | 8,660 B → 14,480 B(이동 **5,083 B**) |
> | `doc_type_gate` 축3·4 | 여유 부족 **1건 → 0건** ⇒ 판정 ✅ |
>
> ### 2. 무엇을 옮겼나 — 행이 아니라 **셀**이다
>
> 대상 = 원장 §1 의 **닫힌 2행**(`O124-B`·`O124`)의 장문 셀. 목적지 절 =
> `02_상태상세_대시보드_갱신형` **§O124 · §O124-B — 원장 §1 장문 셀 은퇴 이관 (2026-08-31 O125-E)**.
> 🟢 **행 키와 열 수는 그대로다** ⇒ `index_row_gate` 유실 **0**(`R1-7-4` 준수) · 인용 좌표 그대로 유효.
> 🟢 `R2-8-1` 토큰 대조 = **대상 65종 · 목적지 부재 0종**(부재 1건이라도 있으면 도구가 원본을 건드리지 않는다).
> 🟢 스냅샷 2건 = `_archive/*.O125-preretire`(`snapshot_util` 경유 · `R1-7-10`) ⇒ **되돌릴 수 있다.**
>
> 🔴 **목적지를 허브가 아니라 조각으로 지정했다** — `retire_rows.py` 는 목적지가 허브여도 그냥 append 하고
> 그 내용은 **다음 `--republish` 에서 사라진다**(도구의 알려진 미시정 · `init_ihcho` 경고). ⇒ 조각
> `02_상태상세_대시보드_갱신형-006.md` 를 목적지로 줬다(선택 근거 = **줄 수 87/300** 으로 여유가 가장 크다 ·
> 최신 조각 `-008` 은 **299/300** 으로 한 줄도 못 받는다). 🔴 그래서 이관 절이 문서 중간에 놓인다 —
> 시간순이 아니라 **용량순** 배치이며 이 사실을 여기 적어 둔다(다음 세션이 「왜 중간인가」를 묻지 않도록).
>
> ### 3. 판정식 (승계)
>
> ㉫ 🔴 **용량 회복은 `--rebalance` 가 아니라 「닫힌 셀 은퇴」로 한다** — 재균형은 총량을 줄이지 못하고
>   조각 수를 늘려 허브 색인을 키운다. 실측 = 은퇴 1회로 **4,433 B** 를 회복했고 경고가 0 이 됐다.
> ㉬ 🔴 **은퇴 목적지는 「허브인지」를 먼저 확인한다** — 허브에 append 하면 조용히 사라진다.
>   그리고 조각을 고를 때 **바이트가 아니라 줄 수 상한**이 먼저 걸린다(`-008` 이 그 사례다).
