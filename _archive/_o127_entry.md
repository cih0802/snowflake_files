> #### 🟠 [2026-08-31 O127 · O127-B] 미해결 이슈 순차 종결 3건(`DEC-44`·`DEC-46 ㉛`·`DEC-47 ⑫`) + 자기검토 3결함 시정
>
> **지시** = 「미해결 이슈를 기존 가이드·해결방침(dec 등)을 고려해, 권장사항과 이전 유사결정을 보여주며
> 사용자 응답으로 하나하나 해결하자」 → 후속 지시 = 「비판적 검토 · 승인 전건 · 문서와 작업을 개선」.
> 🔴 **`R4-4-2 ㉡` 적용**(지시 동봉 ⇒ 브리핑 후 즉시 수행) · 확정위반 자기신고는 아래 ⑦.
>
> **① 🔴 라벨 경합이 실현됐다 — 게이트의 「다음 발급」을 믿을 수 없었다.**
> `id_collision_gate --next O` = 「정의 125 · 참조 125 ⇒ O126」이었으나 **원장 §1 에 `O126` 행이 실재**했고
> 그 행 자신이 **「라벨 선점 누락」을 자기신고**하고 있었다(`R1-4-3` 이 경고한 경합의 실물).
> ⇒ `O127` 전수 grep **0건**을 확인하고 **원장 §1 에 선점 등재 후** 착수했다.
>
> **② 🟢 `DEC-44` 종결(사용자 확정 3건) — 문서30 §30-H(처방) · §30-I(집행 8단계) 신설.**
> · 회계 의미 = **「재작성」**(추경이 그 해 최종액 ⇒ 차수 합산은 이중계상). 근거는 독립 두 축 =
>   §30-F ②(금액 다중집합 상이 0) + §30-G ㉠(항목×연도 24키 중 집행 상이 0).
>   🔴 **현업 문서로 확인된 것이 아니라 실측으로 확정**했음을 §30-H ① 에 명시했다.
> · 배선 = **㉡ 차수를 팩트 컬럼**(`BUDGET_ITEM_DK` MD5 10컬럼 산식 **불변** ⇒ SK 안정) ·
>   ㉠ 기각(SK 전면 변경) · ㉣ 기각(수치가 틀린 채 잔존) · ㉢ 는 미래 확장 여지로만 남겼다.
> · 행 선택 = **「연도별 최신 차수만」이 기본**. 🟢 선례 = `DEC-24`(대표 미납사유 = **최종 차수의 사유** ·
>   「마지막 시도가 최종 상태를 설명한다」) · 🟢 반대 선례 = `DEC-41`(실측이 지지하지 않으면 대표를
>   만들지 않는다) — DEC-44 는 **차수 간 집행 완전 동일**이라는 직접 실측이 있어 그 축에 걸리지 않는다.
> · 🔴 **배선·`severity` 승격·SV 재배포는 미실행** ⇒ `SV_BUDGET` 과대 발행은 **존속**한다(§30-G ㉡).
> · 🟢 §30-E 종전 종결 조건(「30-B 회신 확보」)은 **승계 표기**로 닫았다 — 두 절이 서로 다른 종결
>   조건을 말하는 **이중정의**(`R3-9 ㉡`)를 자기검토에서 적발해 시정한 것이다.
>
> **③ 🟢 `㉛`(`DEC-46`) 종결 — 문서30 §32-G 신설 + 개명표 `0-A ㉤㉥` 등재.**
> · ㉠ 판정식 정밀화 **채택** = L 조건을 「`union all` 이 있다」 → **「그 `union all` 로 원천명이
>   유일하지 않게 된다」**로 좁혔다. 🟢 근거 = **코드베이스 자신의 관례**(`CRM_MEMBER` 4컬럼이 이미
>   그 처리를 받고 원천명 유지) ⇒ `0-A ㉡` 와 같은 형태. 🔴 **개명 확정 12건은 불변**이다.
> · ㉡ `CRM_MEMBER.JOIN_DT` = 🟠 **보류 · 현업 질의 승격**(문서20 **§N-10** 신설) — 규약으로는 P 지만
>   「최초등록일 = 가입일」이 업무상 같다는 확인이 없다.
> · ㉢ `EFFECTIVE_FROM` = ✅ **L 유지** + **쌍 규약 예외 조문 신설**(짝의 한쪽이 L 이면 쌍 전체를 L).
>   🔴 이 예외는 분류 X(변별 토큰)와 **다른 축**임을 명시했다(합치면 판정이 흐려진다).
>
> **④ 🟢 `⑫` 부분 확정 = `DEC-47` 신설(문서30 §33) · 라벨은 게이트 실측(정의 36 · 참조 46 ⇒ 47) + grep 0건.**
> · ㉠ **「활동(건) = 미중단 약정금액 ÷ 10,000」 확정** — 🟢 근거 = `CONF-2` 규약의 재사용
>   (정본 공#38·#151 이 이미 금액÷10,000) · 🔴 「행수로 세지 마라」를 처방에 못박았다.
> · ㉡ `CONF-3` 판정 방향(중단 우세 ↔ 재후원 우세) = 🟠 **문서20 §N-11 로 승격** ·
>   🔴 **「후원 단위 플래그 우회」를 에이전트가 창작하지 않았다**(`DEC-41` 축).
> · ㉢ 센티넬·이상치 = **`R2-7` 그대로**(시작일 93·중단일 2 → NULL + **컬럼별** COMMENT · 음수 금액은
>   `severity: warn` 관측 축으로 분리). 🔴 O72-C 의 **차 2 를 필터로 0 으로 만들지 마라**를 명문화했다.
> · 🔴 **배선 금지는 유지**된다(㉡ 회신 전) ⇒ 착수표 `⑫` 는 열린 채 부분 확정만 병기했다.
>
> **⑤ 🔴🔴 [O127-B 자기검토 최대 결함] 채택된 규칙과 다른 축의 값을 회귀 목표로 옮겨 적었다.**
> §30-I 4 초판에 `PLAN` 기대값을 **62,100,245,921** 로 적었는데 그것은 §30-F ③ 의 **「연사업만」** 열이고,
> 사용자가 채택한 규칙은 **연도별 최신 차수**다. §30-F ① 표로 검산 =
> `PLAN` **65,202,608,326**(2024 추경 20,494,617,158 + 2025 추경 23,490,801,247 + 2026 연사업 21,217,189,921) ·
> `EXEC` **55,094,546,653**(두 규칙에서 **동일** — 집행액이 차수 간 완전 중복이므로).
> ⇒ 🔴🔴 **판정식 신설 = 두 규칙을 가르는 축이 아닌 지표로 회귀 검증하지 마라.** `EXEC` 만 맞은 것은
> 「규칙이 옳게 배선됐다」의 증거가 아니고, 이 결정에서 **가르는 축은 `PLAN` 하나뿐**이다.
> 🟢 시정 = §30-I 4(취소선 + 산식 병기) · §30-H ③ 병기 · §30-E 포인터 · 착수표 `㊴` 에 기대값 명시.
> ⚠️ 함께 = **`EXEC_RATE` 분모도 바뀐다**(현행 집행율과 직접 비교 금지).
>
> **⑥ 🔴🔴 [O127-B] 허위 게이트 보고 1건 정정 — 착수 브리핑의 `gate_census` rc=1 은 실재하지 않았다.**
> 착수 시 「미분류 1건 = `gold_erd_coverage_gate`」로 보고했으나, 그 등재는 **파일에 이미 있었다**
> (`gate_census.py` 66~67행 · 두 줄 묵시 연결). 재실측 = **rc=0 · 미분류 0 · 음성 테스트 25종 전건 rc=0**.
> 원인은 **낡은 마운트 뷰**(`OPS-3`)이고, 같은 세션에서 **원장 조각 바이트 판독이 34,412 ↔ 34,247 로 갈리고**
> **`O126` 행 본문 자체가 판독 시점마다 달랐다**(항목 ⑧⑨⑩ 이 뒤늦게 나타났다).
> ⇒ 🔴 **판정식 = 착수 게이트 결과도 낡을 수 있다 — FAIL 을 사용자에게 발행하기 전에 1회 재실행하라**
> (`R1-7-3` 「손상 판정 전 재읽기」의 게이트 판정판).
>
> **⑦ 🔴 이 세션의 자기결함 · 확정위반**
> · 🔴 **원장 조각 꼬리 절단 1건** — 마지막 표 행이 UTF-8 중간에서 끊겼다(`line_len` 이 적발).
>   `R1-7-3` 대로 해시를 4회 대조해 안정화를 먼저 확인하고, **스냅샷의 4열 원문**으로 꼬리만 복원했다(중복 0).
> · 🔴 **표 행 파괴 1건**(`R1-7-8` 재발) — `㊳` 행의 **접두만 앵커로 써서** 행이 두 줄로 쪼개졌다.
>   즉시 자력 적발·복구(행 1건 · 열 수 불변) ⇒ 🔴 **긴 표 행은 접두 앵커를 쓰지 마라**를 다시 확인했다.
> · 🔴 **`99_NEXT-020` 조각이 40KB 상한을 3회 초과**했다(내 추가분 때문). 매번 **포인터화로 회피**하고
>   재균형·은퇴는 **실행하지 않았다**. 🔴🔴 **은퇴 dry-run 실측 = 이관 557 B / 절감 −655 B**(포인터가 더 크다)
>   ⇒ **승인이 있어도 실측이 반대면 멈춘다**(`R1-6-24` 경고의 실물).
> · 🟠 **잔여 위험 = 그 조각 여유 34 B** · `doc_type_gate` 는 `99_NEXT` 여유 경고(🟠)를 유지한다.
> · 🟢 **[적발] `O126` 행이 2,025자로 `R1-5-1` 상한 초과** 상태였다 ⇒ 이력 포인터로 축약(실측치는 §O126 ⑤).
>
> **⑧ 🟢 구조 결함 시정 = 「결정은 닫고 착수 경로는 안 남긴다」**
> 착수표에 후속 항목이 없으면 다음 세션은 문서30 을 열지 않는다 ⇒ **㊴**(`DEC-44` 배선 · 기대값·정지점 포함) ·
> **㊵**(현업 회신 2건 반영처와 반영 규칙) 신설. 🔴 `㊶`(게이트 FAIL) 는 **허위였으므로 신설 후 즉시 삭제**했다.
>
> **⑨ 🟢 검증 실측**(파이프 없이 종료코드 수신 · O120 판정식)
> `index_row_gate` · `doc_heading_gate` · `clause_order_gate` · `doc_census --strict` · `doc_coord_gate` ·
> `doc_type_gate` · `id_collision_gate` · `gate_census` · `gate_census --run-tests` = **전건 rc=0** ·
> `line_len` **PASS**(상한 초과 0) · `split_doc --republish --expect` **전건 PASS**(유실 0) ·
> 골든 발행 2회(`--reason` 기재 · 실유실 0 확인 후) · `cortex ws ls` 로 스테이지 반영 확인.
> `read` 미반환 **0건** · 재호출 **0건**.
>
> **⑩ 🔴 미실행(의도적)** = `DEC-44` 배선·`severity` 승격·SV 재배포 · 개명 12건 · as-of 배선 ·
> dbt 명령 전건(`R4-1` 정지점 — 명령문만 §30-I 7 에 제시) · 라이브 DDL/DML **0**.
>
> **⑪ 🟠 남은 미해결 이슈**(사용자 요청 목록 기준) = `⑭`(FME 귀속 규칙) · `DEC-45`(다중값 모델링) ·
> `②`(NL 스모크) · `④⑤⑦⑪⑰⑱` · `㉙` · `㉚` · `㉝` · **20_issue 일자 최신화** · `99_NEXT` 여유.
