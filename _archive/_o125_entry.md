> #### 🟢 [2026-08-31 O125] SILVER·GOLD·SERVING 실재 검토 — 「구성됐다」와 「작동한다」는 다른 축이다
>
> **지시** = 「현재 수정된 테이블 설계에 맞게 silver·gold·serving 스키마의 테이블·뷰가 생성되어
> 있는지 검토」(브리핑 후 즉시 수행 · `R4-4-2 ㉡`).
> 🟢 착수 게이트는 리다이렉트로 rc 를 받았다(`| tail` 금지 · O120 판정식):
> `gate_census.py` **rc=0** · `session_brief.py --write` **rc=0** · `id_collision_gate --next O` = **O125**.
> 🟢 라벨은 착수 전에 원장 §1 에 **선점 등재**했다(`R1-4-3`).
>
> ### 1. 판정 결과 — 객체 실재는 전건 일치(미생성 0건)
>
> 판정 근거는 **조회 시점 라이브**이고 문서 기재가 아니다(`R2-4`). 재현 = `python3 scripts/o125_layer_census.py`.
>
> | 층 | 설계 분모 | 판정 |
> |---|---|---|
> | SILVER 테이블 | dbt `models/silver` = `08_SILVER_테이블DDL_20260714.sql` 선언 | 🟢 미생성 0 |
> | GOLD 테이블 | dbt `models/gold/dim`+`fact` = `03_top-down_gold/06_DDL.sql` 선언 | 🟢 미생성 0 |
> | GOLD WIDE 뷰 | dbt `models/gold/wide` | 🟢 미생성 0 |
> | SERVING ML 뷰 | `21_ML_SERVING_뷰_DDL.sql` + 배포 러너 | 🟢 미생성 0 |
> | SERVING 시맨틱뷰 | `05_*_SV_DDL*.sql` + `22_ML_SV_DDL.sql` | 🟢 미생성 0 |
>
> 🔴 **개수를 이 항목에 박지 않는다**(`R3-9 ㉦`) — 값은 위 스크립트가 매 실행마다 센다.
>
> ### 2. 🔴 초판이 SERVING 분모를 0 으로 냈다 — 「0건」은 부재가 아니라 판정식 결함이었다
>
> 정본 SV DDL 은 `CREATE OR ALTER SEMANTIC VIEW` 인데 추출기가 `OR REPLACE` 만 봤다.
> 출력은 「설계 0 · 라이브 17 · 미생성 0」으로 **✅ 를 냈다** — 🔴 **분모가 비면 「누락 0」은 자동으로 참**이다.
> 🟢 `O111 ㉠`(0건을 받으면 분모를 먼저 의심하라)을 자기적용해 문법을 넓혀 **17** 을 회복했다.
> 🟢 파서 음성 확인 = 빈 집합 0 · 표본 컬럼수(37·38)가 `table_ddl_column_gate` 실측과 일치.
>
> ### 3. 컬럼 축 확장 — 기존 게이트 분모는 7개뿐이었다
>
> `table_ddl_column_gate` 는 **7개 테이블**만 본다(그 도구의 스코프). 이 세션은 DDL 정본 ↔ 라이브
> **컬럼 이름 집합**을 GOLD 37개·SILVER 42개 전건으로 대조해 **불일치 0** 을 얻었다.
> 🔴 **순서는 판정하지 않았다** — 순서 드리프트 2건(`DIM_MEMBER_ACQUISITION`·`FACT_EVENT_PARTICIPATION`)은
> 그 게이트가 유지 중이고 **적재 위험이 아니다**(dbt insert 는 양측 이름 지정).
>
> ### 4. 🟠 잔여 — 전부 문서 축
>
> - dbt schema yml 미등재 **7모델**(`CRM_MARKETING_CAMPAIGN`·`CRM_MEMBER_SPONSOR_SPAN`·
>   `CRM_RELATION_ACTIVITY`·`DIM_MARKETING_CAMPAIGN`·`FACT_MEMBER_FEE`·`FACT_MEMBER_SPONSOR_BIZ`·
>   `FACT_TARGET_BIZ`) ⇒ 착수표에 신규 등재(`R3-5`).
> - 라이브에만 1건 = `SILVER.BIGQUERY_REFINED_DATA` — 외부 Python 적재이고 `_sources.yml` 에 source 로
>   등재돼 있으며 SILVER DDL 은 **의도적으로 주석 처리**돼 있다 ⇒ 🟢 **결함이 아니다**.
> - 객체는 있으나 **0행 2건** = `GOLD.FACT_TARGET_BIZ` · `SILVER.CRM_BIZ_TARGET`(원천 미입고 `E-6`).
>
> ### 5. 🟢 산출물
>
> - `scripts/o125_layer_census.py` 신설 — `gate_census` **JUDGE** 에 등재(미분류 FAIL 을 즉시 받아 시정).
> - 게이트 rc = `table_ddl_column_gate` 0 · `dbt_schema_lint` 0 · `wide_select_yml_gate` 0 ·
>   `sv_identifier_gate` 0 · `doc_census --strict` 0 · 행키·제목 0 · 음성 테스트 전건 0.
> - `read` 미반환 **0** · 재호출 **0** · dbt 실행 **0** · 파괴 연산 **0**.
>
> 🔴 **이 항목의 한계는 §O125-B 가 지적한다** — 존재 판정을 「설계대로 작동한다」로 읽으면 틀린다.

> #### 🟢 [2026-08-31 O125-B] O125 자기검토 — 존재 판정을 「정상」으로 읽히게 발행한 것이 최대 결함이다
>
> **지시** = 「이슈문서들을 읽고 이번 세션 작업을 비판적으로 검토 · 승인이 필요한 작업은 전부 승인 ·
> 작업과 문서를 개선」. ⇒ `--rollover` 등 파괴 가능 연산은 **이 승인으로** 집행했다(`R4-4-3`).
>
> ### D1 🔴🔴 확정위반 — 「설계에 맞게 생성됐다」는 값 축을 포함하는 말로 읽힌다
>
> O125 는 **객체·컬럼 이름 집합**만 판정하고 보고에는 「전건 일치 ✅」를 앞세웠다.
> 그런데 정본은 그 반대를 말한다 — 문서50 **`BLOCKING-5`**(열림)는 *「행수는 있으나 대부분의 카운트
> measure·차원 FK 가 전건 0/NULL」* 이다. 🔴 **구조가 있다는 판정이 「지표가 나온다」로 읽히면
> 조용한 오답의 입구**가 된다(Cortex Analyst 가 그 위에 서면 0 을 자신 있게 답한다).
> 🟢 **처방 = 도구가 값 축을 함께 재고 「판정하지 않는 축」을 스스로 선언한다**
> (`o125_layer_census.py` 머리말 ㉠~㉥ + 말미 `값 적재 축 [관측]` 절).
> 🟢 재실측(2026-08-31 · 조회 시점):
> `FMM` CAMPAIGN_SK **0**/40,054,883 · PAYMENT_SK **0** · `FSE` CAMPAIGN_SK **0**/38,467,142 ·
> `FEP` CAMPAIGN_SK·SPONSORSHIP_SK **0**/1,134,126 · `FBD` CAMPAIGN_SK·ORG_SK **0**/2,964.
> ⇒ 🔴 **캠페인 축은 5개 팩트에서 지금도 전건 0 이다.**
>
> ### D2 🔴🔴 판정약점 — 배포 상태 정본을 우회했다
>
> SERVING 을 검토하면서 **배포 상태 정본**인 `05_SV-Agent_ai/04_SV_설계.md` **§0.9** 를 읽지 않고
> 파일 파싱으로 자체 분모를 만들었다. 결과는 우연히 일치했지만(17) **경로가 정본을 우회**했고,
> 그 정본이 담은 **라이브 결손**을 보고에서 빠뜨렸다: `SV_BUDGET.TOTAL_EXEC_BUDGET` 이
> **+79.7% 과대값을 발행 중**(§0.9-D · `DEC-44` · 사람 결정 대기)이다.
> ⇒ 🔴 **「SERVING 17/17 ✅」와 「SERVING 이 틀린 값을 발행 중」이 동시에 참**이다.
> 🟢 처방 = 스크립트 헤더가 §0.9 와 `BLOCKING-5` 좌표를 **출력에 싣는다**(다음 세션이 우회하지 못한다).
>
> ### D3 🔴 정본 stale 2셀 적발 — 재실측이 기재를 반박했다(`BBBB4 ㉠` 적용)
>
> `BLOCKING-5` 표의 「전건 0/NULL」 열에 있던 두 셀이 실측과 달랐다:
> - `FMM.REASON_SK` = **3,164,724 / 40,054,883**(7.90%) — 🔴 **이번에 처음 적발**
> - `FME.ORG_SK` = **3,594,835 / 4,633,105**(DEV 한정 · O38 배선분) — 한정 표기 누락
>
> 🔴 그 절 말미의 교훈이 *「부분 적재 축은 반드시 한정 표기를 붙인다」* 인데 **그 절이 자기 교훈을
> 두 셀에서 어겼다.** 🟢 두 셀을 취소선 + 실측 병기로 정정했다(삭제가 아니라 병기 · `§C24` 처방).
>
> ### D4 🟠 확정위반 — 원장 §1 행에 라이브 수치를 박았다
>
> 초판 O125 행은 `42/42`·`37/37`·`17/17`·`743`·`830` 을 본문에 박았다 — `R3-9 ㉦` 위반이고
> **O124-B `D4` 와 같은 결함의 재발**이다(라이브 수치는 다음 적재에서 stale 이 된다).
> 🟢 수치를 지우고 **판정(미생성 0) + 재는 방법(스크립트 경로) + 판정 축 선언**으로 대체했다.
>
> ### D5 🟠 구조결함 — 게이트를 신설하면서 음성 테스트를 같이 만들지 않았다
>
> `gate_census` **JUDGE** 에 등재까지 하고 음성 테스트가 **0** 이었다(`R3-2`·세션 규칙 ⑥ 위반).
> 🟢 `scripts/test_o125_layer_census.py` 신설 — **오염 기반 축**(정본 문법을 변형해 분모가 0 이 되는 것을
> 재현) + 스키마 한정 오탐 축 + 빈 집합 금지 축 + `column_report` 양방향 축 + 재현율 축.
> 🔴 **단정 수를 문서에 적지 않는다** — 테스트가 스스로 세어 출력한다.
>
> ### D6 🟢 자기검토가 확인한 것(반박 아님)
>
> - `SILVER.BIGQUERY_REFINED_DATA` 를 「설계 밖」으로 적지 않고 **source 등재 + DDL 주석**을 근거로
>   결함 아님으로 판정한 것은 유효하다(`_sources.yml` 실재 확인).
> - 「누락/과잉 GOLD 6개」(문서30 §6 · 문서50 🟡)는 **객체 축에서는 닫혀 있다** — 4종 작성완료 ·
>   `FACT_TARGET_BIZ` 0행 스켈레톤 실재 · `DIM_MEMBER_IDENTITY` 활성. 🔴 열려 있는 것은 **measure·FK 배선**이다
>   ⇒ 그 절 제목의 🟡 는 값 축 때문이며 「테이블이 없다」는 뜻이 아니다.
>
> ### 판정식 신설 (승계)
>
> ㉨ 🔴🔴 **실재 판정과 값 판정을 한 문장에 담지 마라** — 「생성됐다」는 「채워졌다」를 함의하지 않는다.
>   도구는 **자기가 판정하지 않는 축을 스스로 선언**해야 하고, 선언이 없으면 사용자는 ✅ 를 전체 판정으로 읽는다.
> ㉩ 🔴 **분모를 파일 파싱으로 만들기 전에 「그 분모의 정본이 이미 있는가」를 물어라** —
>   있으면 그 정본을 인용하고, 만들었으면 **정본 좌표를 출력에 실어라**.
> ㉪ 🔴 **표의 「전건 0」 열은 부분 적재를 표현할 수 없다** — 한정 표기(`(DEV 한정)`)가 없는 셀은
>   재실측 대상이다. 실제로 두 셀이 그렇게 낡아 있었다.
