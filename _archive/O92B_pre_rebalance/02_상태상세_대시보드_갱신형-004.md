<!-- SPLIT-CHUNK 02_상태상세_대시보드_갱신형.md | 004/007 | 허브 = 02_상태상세_대시보드_갱신형.md | 원문 498~614행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## §O59-Q · 🟢 **DEC-35 3단계(내부)**

| 필드 | 값 |
|---|---|
| 현황 | 🟢 **DEC-35 3단계(내부)** |
| 건수 | **1 해소 · 미규명 1** |
| 관할 문서 | 01 §O59-Q · 30 §23 |
| 인덱스 행 | `00_INDEX_이슈원장.md` §1 |

### 대표 항목 (무변경 이관)

**[2026-08-11 O59-Q]** 🔴 초안의 「개명/분리」는 집행 불요(§23-F 결정 1 = **(c) 확장판**이 대체). 🔴🔴 **실측이 공백을 드러냈다 — WIDE 가 라벨을 하나도 노출하지 않고 있었다**: O59-N 이 SILVER·GOLD 에 26컬럼을 붙였는데 소비뷰는 **코드축만** 갖고 있었다
⇒ DEC-25 15-D 가 WIDE 층에서 미충족이었고 **1·2단계의 목적(현업이 라벨로 이해)이 달성되지 않은 상태**였다. ✅ WIDE **2뷰 +14컬럼**(`WIDE_SERVICE_EVENT` +5 · `WIDE_EVENT_PARTICIPATION` +9 ·
`EVENT_KIND_NAME` 포함 — 이것이 없던 건 O58-C 와 같은 유형) · build 후 물리 **각 62컬럼 · 신규 14/14 실재** · 뷰 COMMENT **560/560 결손 0** · yml↔라이브 문안 **62/62 불일치 0** · 채움 GOLD 팩트와 전건 일치(`EVENT_CATEGORY_NAME` **870,515 = 1,134,126 − 고아 263,611 정확 일치** ⇒ 라벨 결손 0)
· 게이트 4표면 전부 0. 🔴 **거짓 서술 3건 회수**(SV `SEND_STATUS`「산출 불가」 · SV `EVENT_CATEGORY`「라벨 미배선」+ **두 계열을 거꾸로 서술한 오류** · yml `EVENT_KIND`「온라인/오프라인」) · SV 2종 재배포 ·
값 불변(38,470,780 / 1,134,126). 🔴 **회수 중 내가 만든 새 거짓 1건 자기적발** — 없는 라벨축을 쓰라고 지시했다(게이트 정보 3건이 지목 · 교정). 🆕 **P204·P205·P206·P207** / 🟢 **WARN 32→33 규명 완료**(사용자 build 로그 전문 제공 후 즉시)
= **`warn_gold_view_comment_coverage`** WARN 1. 🔴 내 규명이 막혔던 이유 2건 모두 내 결함 — ㉠ artifact 를 실행과 대조하지 않았다(스테이지 07:38 판 vs 실행 07:56 ⇒ 32건 기준선 자체가 stale · **P206**)
㉡ 🔴🔴 **내 라이브 재평가가 이 테스트를 분모에서 빠뜨렸다** — 커버리지를 **컬럼레벨만** 쳤고 이 게이트는 **뷰레벨 COMMENT(`INFORMATION_SCHEMA.VIEWS`)도** 검사한다 ⇒ **P202 를 세운 세션에서 내가 P202 를 어겼다**.
🟠 원인은 미특정(게이트 정본 SQL 라이브 재실행 = **0행 PASS** · 정황 = 마지막 뷰 `WIDE_MEMBER_MONTHLY` 생성과 테스트 START 가 **같은 초** ⇒ 메타데이터 가시성 경합 유력하나 1회뿐이라 단정 안 함). ✅ **처방을 게이트로 고정** — `store_failures=true` 신설(다음 발생 시 실패 행에서 대상 뷰를 바로 읽는다)
· 🆕 **P207**(warn 게이트는 「몇 건」이 아니라 「무엇」을 남겨야 한다)

---

## §O59-N · 🟢 **DEC-35 1·2단계(내부)**

| 필드 | 값 |
|---|---|
| 현황 | 🟢 **DEC-35 1·2단계(내부)** |
| 건수 | **1 해소** |
| 관할 문서 | 01 §O59-P · 50 |
| 인덱스 행 | `00_INDEX_이슈원장.md` §1 |

### 대표 항목 (무변경 이관)

**[2026-08-11 O59-N 실행 · O59-P build 판정]** 코드→라벨 **26컬럼 신설**(SILVER 13 + GOLD 13) — 사용자 `dbt build` = **`PASS=386 WARN=32 ERROR=0 SKIP=0 TOTAL=418`**.
✅ 채움 전건 재현(분모 명시 · `CRM_SEND_MEMBER` 38,471,525 → 축A 20,285,072 · 축B 코드 26,256,109/라벨 24,854,690 · `FEP` 1,134,126 **팬아웃 0** · `FSE` 38,470,780 = SILVER − 회원번호 NULL 745)
· 26컬럼 **전건 감사컬럼 뒤** 배치(규약 준수). ✅ **build 가 라이브 COMMENT 를 덮지 않았다** — 파일↔라이브 **632/632 문안 완전 일치**(P199 유지) · `audit_ddl_rule7` GOLD 632/632 위반 0 · SILVER 신규 유입 0 ·
`table_ddl_column_gate` 7/7. ✅ WARN 32 = 기존 28 + **신규 4**(라벨 not_null 관측 · 의도) · **회귀 0**. 🟢 **§0-Z 재검 쿼리 = 기대 6행 정확 일치**(전부 O28 오염 행) ⇒ 라벨 NULL 관측축 유효.
🔴 **인수인계 stale 3건 회수** — 「`SV_DEV_ACHIEVEMENT` 깨져 있다」·「SV 가 `SERVING` 참조」·「SV 2종 미배포」가 **전부 거짓**(라이브 = SV 9종 · 논리테이블 **32/32 전건 GOLD** · 스모크 4,622,103/2,291,878 = O54 기대치 일치).
🟢 부수 오타 **4건** 교정(`등급 C→B))` · 「1건」이라 적었으나 전수 검색하니 4건 = **P187 재현** · 파일+라이브 동시). 🆕 **P201·P202** / 🟠 잔여 = **O59-P-1** `FSE.SEND_STATUS2` 전건 NULL — 🔴 **내 「유령 컬럼·제거 권고」는 오판이었고 철회했다**(정본 `05_회원_보고서필드 인벤토리` §3-1 이 「발송상태1·2」를 **둘 다
요구**) ⇒ 문서20 **§M-6** 현업 질의(후보는 축B 하나로 좁혔으나 사전 라벨이 「실패 사유」여서 단정 금지) · 🆕 **P203** · **O59-P-2** 테이블 COMMENT 규칙7 16건

---

### 🆕 GA4 SILVER 평탄화 통합 기반 테이블 신설 → `GA4-PK-1`·`GA4-LEN-1` 해소(코드)

**[2026-08-19 O87]** 🔒 라벨 선점(게이트 「다음 O88」 ↔ **정의 최대 86** ⇒ **O87** · `P222` 참조축 과대 15연속).
착수 지시 = 사용자 「합쳐둔 `EVENTS` 를 `BIGQUERY_REFINED_DATA` 로 silver 에 적재하고 그걸로 GA4 테이블을
만들고 싶다 · **이 요건이 GN_DW 설계상 맞는지 데이터 아키텍처 관점에서 검토**하고 그에 맞게 dbt 모델을 수정」.

🟢 **검토 결론 = 요건의 실질은 옳고, 명명·계층 규칙 2건이 설계와 충돌** ⇒ 둘 다 사용자 결정으로 확정.

🔴🔴 **최대 발견 = 「SILVER→SILVER 파생 금지」는 사문(死文)이었다.** 문서 **4곳**이 명문 금지로 선언하는데
라이브 dbt 코드는 **이미 31곳에서 하고 있었다**(`AGENCY_AD_ROW_*` → 성과·방송·사례·디지털 **7** ·
`CRM_CODE` → CRM 11모델 **20** · `CRM_MEMBER_SPONSOR_BIZ` → `CRM_SPONSOR_RELATION` **1** ·
`GA4_IDENTITY` → `IDENTITY_MEMBER_XREF` **1**). 그리고 `AGENCY_AD_ROW_*` 3종은 `02` §3.4 인벤토리에
**등재조차 안 돼 있었다**.

🟢 **`AGENCY_COST` 삭제의 실제 사유는 「월 롤업」이었다** — 같은 2026-07-14 리뷰가 `CRM_SEND_RESULT` 의
SILVER 집계는 *「검토된 집계」*로 **유지**했다(`03_SILVER_작업계획_CRM전용` 136행) ⇒ 판정 기준은
「계층 내 참조인가」가 아니라 **「롤업인가 · 중복 소유권인가」**였다. 보강 = **GOLD 는 계층 내 참조를
15파일에서 한다**(WIDE 14 + `FACT_GA_BEHAVIOR` → `DIM_GA_EVENT`·`DIM_GA_SOURCE`).
⇒ **DEC-37 신설**(허용 경계 = 롤업·중복 소유권 금지 / 정규화 기반→파생 투영 허용).

🔴 **역설 = 금지가 오히려 중복을 만들고 있었다.** `09` 722행의 「`GA4_EVENT` 참조 금지 → CTE 재계산」
강제로 `event_params` 승격 `GROUP BY` 절이 `GA4_EVENT`(11컬럼) ↔ `GA4_EVENT_DIM`(4) ↔ `GA4_IDENTITY`(5)
**세 가지로 갈라졌고** `DEVICE_TYPE` CASE 는 2곳 복제였다(2026-07-27 교정 시 두 곳을 따로 고쳤다).
**어떤 게이트에도 걸리지 않았다.**

🟢 **DEC-38 신설 = 명명 이원화**(BRONZE 스키마 = 원천 시스템 / SILVER·GOLD 객체 = 데이터 도메인 /
**원천 평탄화 기반 테이블만 원천 접두**). 사용자 질의 「GA4 를 전부 BIGQUERY 로 바꾸는건 어떤가」에 대해
**전면 리네임 반대**를 근거 4개로 제시 — ㉠ 2026-08-14 개명 목적이 **BRONZE 를 넓혀 SILVER 를 GA4 로
좁게 유지**하는 것이었다(`_sources.yml` 7~12행 원문) ㉡ BigQuery 는 **전달 경로**라 경로가 바뀌면
`BIGQUERY_EVENT` 는 거짓이 되지만 `GA4_EVENT` 는 참이다 ㉢ 갈림이 BRONZE↔SILVER 에서 SILVER↔GOLD 로
**이동만** 한다 ㉣ 리네임 범위 대비 이득 0. ⇒ 사용자 결정 = **기반 테이블만** `BIGQUERY_REFINED_DATA`.

🟢 **`GA4-PK-1` 해소(사용자 결정 ①)** = PK 4번째 키 `BATCH_ORDERING_ID` → **`EVENT_SEQ`** surrogate
(계보 `SRC_FILE_NAME` 순 결정적 정렬) ⇒ **48,862,926행(17.10%) 복구 + 3.679% 중복 흡수 · 손실 0**.

🟢 **`GA4-LEN-1` 해소** = `VARCHAR(10)`→`VARCHAR(64)` **+ `ID_SCHEME` 6분류** + `XREF.MATCH_METHOD` 에
**`NOT_A_MEMBER_ID`** 신설(「회원번호인데 못 찾음」 ↔ 「회원번호가 아님」 분리 ⇒ 채움률 분모 왜곡 차단).
종전 `S%`/`else` 2분기가 `app-`·이메일·`'null'` 을 `FDRM` 으로 밀어넣던 오염원이었다.

🔴🔴 **자기발견 = dbt 는 hook 을 「누적」한다** — 프로젝트 `+pre-hook: TRUNCATE` 에 모델 `pre_hook` 범위
DELETE 를 더하면 **둘 다 실행**되어 전체가 비워진 뒤 범위만 append 된다. **에러가 나지 않고 행수만
줄어든다** ⇒ `macros/silver_purge.sql` 단일 진입점으로 구조 차단.

🟢 **산출** = 신설 3(`BIGQUERY_REFINED_DATA.sql` · `silver_purge.sql` · `ga4_range_purge.sql`) ·
전환 6(GA4 5 + XREF) · 개정 5(`_sources.yml` **EVENTS source 신설** · `_ga4_schema.yml` ·
`dbt_project.yml` vars/pre-hook · `08` DDL **39테이블** · 설계문서 3종) · `ga4_union_shards` **폐기 표기**.

🔴 **검증 미완** — `dbt parse` 는 **89 models · 345 tests · 41 sources** 로 통과했으나
**`dbt compile` 은 미실행**이다(프로젝트 `profiles.yml` 이 Snowflake-native 규약상 account/user 를
빈 문자열로 두어 샌드박스 CLI 가 연결되지 않고, `~/.dbt/profiles.yml` 보강은 매 호출마다 재생성돼 지워진다).
⚠️ **SQL 문법 오류가 남아 있을 수 있다 — `dbt run` 전에 `dbt compile` 을 먼저 돌릴 것.**

🔴 **자기위반 3** = ㉠ `R1-4` ID 게이트 미이행으로 **DEC-36 오선점**(이미 확정된 FMM↔FMF 수렴) ⇒ 19곳
재번호 · **`O` 계열에는 게이트를 돌렸는데 `DEC` 계열에는 돌리지 않았다** ㉡ `R1-7-8` 앵커 사고 **2회**
(`GA4_EVENT_DIM` 헤더 중복 · `90` `DDL-ORDER-1` 행 중복) — 둘 다 즉시 시정 · **O85 에서도 났던 연속 재발**
㉢ `R4-4-1` 브리핑 순서 역전(Step 2 직후가 아니라 조사 후에 냈다).

🔴 **환경 사고 2** = ㉠ **마운트 교체**(`/workspace` → `user__public_default_`)로 세션 중 편집 경로 상실 ⇒
직접 스테이지 경로로 우회(**파일 손실 0** · GA4 7종 `cortex ws ls` 실재 확인) ㉡ **`C5` 부분 반환 실측** —
`90` 이 스테이지 **33,792B** 인데 마운트가 **28,816B** 만 반환하며 UTF-8 마지막 바이트 절단.
🔴 **해시 4회 동일**이라 torn read 가 아니라 **안정적으로 잘린 뷰**였다 ⇒ `R1-7-2` 해시 안정성 검사만으로는
못 잡는다(안정적이어서 통과한다). **스테이지 실체 크기와 대조해야 잡힌다.** 20초 후 정상화 뒤 편집했다.

🔴 **신규 미결 3**(정본 = `90` §1-B) = `GA4-SEQ-1`(순번 재실행 안정성 미실증) · `GA4-DUP-1`(저장 중복
~50GB 환산) · `GA4-TV-1`(`smart tv` 499행 라벨).

🔴 **미갱신** = 「SILVER→SILVER 금지」 문구가 남은 문서 4종(`09`·`14`·`15`·`02_SILVER_작업계획`)은
역사 기록 성격이라 우선순위를 낮췄다 ⇒ 인용 시 **DEC-37 을 함께 볼 것**.

🔴 **라이브 미적용 = 사용자 지시 「코드·문서 수정까지 · build 전 정지」**(`R4-1`). DDL 실행·`dbt run` 대기.

---
