> #### 🟢 [2026-08-31 O126] GOLD ERD 생성기 신설 + 자기검토 — ERD 가 fan-out 차단 차원을 고립 섬으로 그렸다
>
> **요청** = 「dbt 기반으로 GOLD 스키마 테이블별 ERD 문서를 만들고, 파이프라인 수정 시 재사용 가능하게」.
> 이어서 **「이슈문서를 읽고 이번 세션 작업을 비판적으로 검토하고 개선하라 · 승인 필요 작업 전부 승인」**.
>
> ---
>
> **① 🔴 착수 절차 위반 — 브리핑 없이 시작했다 (`A5` 확정위반 · `R4-4-1`)**
>
> · 초판 작업은 `init_ihcho` 미실행 · `00_BRIEF.md` 미독 · 라벨 선점 없음으로 진행됐다.
> · 검토 지시를 받은 뒤에야 지침을 로드하고 `O126` 을 선점했다(`id_collision_gate --next O` = 126).
> · 🔴 **이 위반이 ②의 원인과 무관하지 않다** — 인수인계 `§0-CCCC` `CCCC1` 이
>   「**「객체가 다 있다」와 「설계대로 작동한다」는 다른 판정이다**」를 말하고 있었고,
>   초판이 정확히 그 함정에 빠졌다. **브리핑을 읽었으면 피할 수 있었다.**
> · 🟠 `R4-4-4` 롤백 근거 기록: 이 세션은 `R4-4-2 ㉡`(지시 동봉 즉시 수행) 분기로 진행했고
>   **확정위반 4건**(브리핑 미출력 · 라벨 선점 누락 · 사후 검증 2종 · `edit` 앵커 사고)이 났다.
>
> ---
>
> **② 🔴🔴 초판 최악 결함 — DIM_MONTH 를 관계선 0개의 고립 섬으로 그렸다**
>
> · **초판 설계** = dbt `_gold_ready_schema.yml` 의 `relationships` 테스트(45) +
>   물리 FK `SHOW IMPORTED KEYS`(56) 를 **합집합**(63)해 ERD 를 그렸다.
>   두 소스가 서로를 포함하지 않는다는 것까지는 실측으로 맞게 잡았다
>   (YAML만 7 = `MEMBER_DK → DIM_MEMBER` SCD2 비유일 / 물리만 18 = yml 테스트 미작성).
> · 🔴🔴 **그러나 합집합도 전량이 아니었다.** 실측 결과 **월 conform 축 7건이
>   dbt relationships 에도, 물리 FK 에도 없다**:
>   `FACT_BUDGET·FACT_MEMBER_FEE·FACT_MEMBER_MONTHLY·FACT_TARGET_BIZ·FACT_TARGET_DEV.MONTH_KEY`
>   + `FACT_MEMBER_SPONSOR_BIZ.START_MONTH_KEY·DSCNTC_MONTH_KEY`.
>   원인 = `06_DDL.sql` [관계 제약] 이 월 conform 키를 **비유일 참조로 보아 선언 보류**했고
>   yml 에도 테스트가 없다 ⇒ **두 소스 어디에도 흔적이 없다.**
> · ⚠️ **왜 이것이 이 스키마에서 가능한 최악의 오도인가** — `DIM_MONTH` 는 **fan-out 차단 차원**이다.
>   `DIM_MONTH.MONTH_KEY` COMMENT 가 🔴🔴 로 「월 팩트는 DIM_DATE 를 직접 조인하지 말고 이 차원을 쓴다 —
>   DIM_DATE 는 일 grain 이라 월팩트와 조인하면 월당 일수만큼 행이 증폭되고 금액·건수가 그 배수로
>   과대해진다」고 경고한다. 그런데 **초판 ERD 는 DIM_MONTH 를 아무것과도 연결하지 않은 채
>   `FACT_MEMBER_FEE → DIM_DATE`(`LAST_PAY_DATE_SK` 등)만 그렸다** ⇒ 독자가 ERD 를 보고
>   **설계가 막으려던 바로 그 조인을 하도록 유도**했다.
> · 🔴 **실측 근거**(초판 산출물 파싱): `DIM_MONTH` 섹션 역참조 팩트 **0** · 섹션 관계선 **0** ·
>   전체 ERD 의 `DIM_MONTH` 관계선 **0**.
> · 🔴 **판정식 = 「Mermaid 문법 38/38 유효」와 「관계가 설계를 옳게 전달한다」는 다른 판정이다.**
>   초판은 문법 검사를 통과했고 FK 병합 산술도 맞았다. **틀린 것은 「무엇을 세는가」였다.**
>   이것이 `R3-9`(게이트가 보지 않는 축)의 실물이고 `CCCC1` 의 재현이다.
>
> ---
>
> **③ 🟢 시정 — 3차 소스(논리 관계) 도입**
>
> · `scripts/gold_erd_coverage_gate.py` **신설**. 판정식 = FACT 의 키 형태 컬럼(`_SK`/`_KEY`/`_DK`)
>   중 병합 FK·물리 PK 어디에도 없는 것 = **「고립」**이고, **고립 1건마다 분류·사유를 등재해야 통과**한다.
>   실측 = FACT 키 컬럼 **71** · 병합 FK 커버 **64** · 고립 **7** · 미분류 **0**.
> · 🔴 **「고립 0」을 요구하지 않는다** — degenerate key 는 관계가 없는 것이 설계다.
>   요구하는 것은 **「모르는 고립 0」**이다. 분류 = `DEGEN`(관계 없음이 설계) /
>   `CONFORM`(FK 선언 불가 · **ERD 에 논리 관계로 표기해야 한다**) / `SELFREF`.
> · `gen_gold_erd.py` 가 게이트를 `import` 해 `LOGICAL_FK` 를 읽어 **3차 소스로 병합**한다.
>   결과 = 선언 FK **63** + 논리 **7** = **총 관계 70**.
> · 표기 = Mermaid **점선**(`||..o{`) + `logical` 배지. 🔴 문서에 명시했다 —
>   **「점선은 약한 관계가 아니라 FK 로 선언되어 있지 않다는 뜻이고, 조인 경로로는 실선과 동등하게
>   유효하다 — 월 팩트는 반드시 DIM_MONTH 를 쓴다」.**
> · 🔴 **미분류 고립이 있으면 생성 자체를 `return 1` 로 차단**한다(경고로 흘리지 않는다).
>   경고였다면 초판과 같은 산출물이 계속 나온다.
> · 🔴 **규칙은 게이트 1곳에만 둔다**(`R3-9 ㉡` 이중정의 금지) — 생성기에 `LOGICAL_FK`·`KNOWN_ORPHANS`
>   정의가 없음을 음성 테스트 축5 가 단정한다.
>
> ---
>
> **④ 🔴 초판 `--yaml-only` 가 불완전 산출물을 전량판 경로에 덮었다**
>
> · 초판은 두 모드가 **같은 `OUT_HTML`** 에 썼다 ⇒ 비용 절감 모드를 한 번 돌리면
>   전량판이 **경고 없이 사라진다**. 이 워크스페이스가 4번 당한 **「조용한 소실」** 유형이다.
> · 🟢 시정 = `OUT_HTML_YAML`(`GOLD_ERD_테이블별_YAML전용.html`) 로 **경로 분리** ·
>   그 판본 상단에 **빠진 것 3종을 열거한 🔴🔴 경고 블록**(특히 「DIM_MONTH 가 고립 섬으로 그려진다」).
> · 실측 = 전량판 점선 **21**(전체+테이블별 합산) vs YAML전용판 점선 **0** ⇒ 경고가 사실과 일치한다.
>
> ---
>
> **⑤ 🔴 정본 stale 3곳 적발·시정 — `03_top-down_gold/06_DDL.sql` 헤더**
>
> | 위치 | 종전 기재 | 실측 |
> |---|---|---|
> | 표제(5행) | 「35개: DIM 20 + FACT 15」 | **파일 선언 37 = DIM 20 + FACT 17** = 라이브 BASE TABLE 37 |
> | 실측대조(13행) | 「선언 FOREIGN KEY 50 = 라이브 50」 | **파일 실행 FK 56 = 라이브 56** |
> | 실행규칙 1(48행) | 「DIM 20개를 … FACT 15개를 생성한다 … O53 로 20/15」 | **FACT 17** |
> | 1행 요약 | 「전체 DDL(35개 테이블)」 | 동일 사유 |
>
> · 🟢 **파일 본문은 정상이었다 — 틀린 것은 헤더 산문뿐이다.** 그러나 헤더는 인수인계에서 먼저 읽히므로
>   본문 신뢰도와 무관하게 오도한다.
> · 🟢 **시정 방식 = 수를 지우고 「재는 방법」으로 대체**(`R3-9 ㉦` 「내가 만든 수를 문서에 박아 넣지 마라」).
>   `doc_census.py` 의 분모는 `20_issue/` 문서군이라 **`03_top-down_gold/` 는 감시 밖**이었다
>   ⇒ 워크스페이스가 이미 확립한 원칙을 **게이트가 보지 않는 파일에 일반화**했다.
> · 🔴 **함정 1건 함께 명문화**: `grep -c 'FOREIGN KEY'` 는 헤더 산문·주석·집계 SQL 을 세어 **60** 을 낸다.
>   실행 FK 는 `grep -cE '^\s+FOREIGN KEY'` = **56**. 차이 4건이 전부 비실행 줄이었다
>   ⇒ **판정식이 세는 것과 사람이 읽는 뜻이 다른 지점**(`O111 ㉢`).
>
> ---
>
> **⑥ 🟢 음성 테스트 2종 신설 (`R3-2` · `R3-9 ⑥`)**
>
> · `scripts/test_gold_erd.py` — **오염 기반**. 논리 관계를 뺀 픽스처로 **초판 결함(고립)을 재현**하고,
>   병합 후 점선이 생기는지를 **양축 단정**한다. 축 = rel_op 출처 분기 · 오염 재현 · 복구 ·
>   blocking 여부 · 규칙 단일 정본 · 출력 경로 분리 · CONFORM↔LOGICAL_FK 대응 · Mermaid 타입 정규화 ·
>   **산출물 회귀 가드**. 🔴 축·단정 수는 테스트가 스스로 세어 출력한다(문서에 적지 않는다).
> · `scripts/test_gold_erd_mermaid.py` — 문법 축 **분리**. 🔴 **일부러 나눴다** —
>   문법 유효는 의미 정합을 보증하지 않는다는 것이 이 세션의 교훈이다(`R3-9 ㉡` 「같은 것을 다르게 재라」).
> · 종료코드는 **리다이렉트로 받았다**(`>/tmp/…out; echo rc=$?`) — 파이프 뒤 `$?` 금지(`O120`).
>
> ---
>
> **⑦ 🔴 `edit` 앵커 사고 1건 (`R1-7-8`) — 즉시 복구**
>
> · `rel_op` 를 추가하며 앵커를 `def eff_pk(info):` 한 줄로 잡아 **그 정의줄을 덮었다**
>   (docstring·return 2줄이 고아가 됐다). 다음 편집에서 즉시 복구하고 `py_compile` 로 확인했다.
> · 🔴 **조문이 말한 그대로다** — 「앵커는 직전 줄까지 포함하라」. 짧은 앵커가 실제로 정의줄을 지운다.
>
> ---
>
> **⑧ 비용 판정 (사용자 원 질문에 대한 답)**
>
> | 소스 | 컴퓨트 | FK 커버리지 |
> |---|---|---|
> | dbt YAML 파싱 | **$0** | 45 / 70 — 🔴 불완전 |
> | + 물리 FK·컬럼 메타 | XS ~2초 | 63 / 70 — 🔴 여전히 불완전 |
> | + 논리 관계(사람 판정) | 동일 | **70 / 70** |
>
> · 🟢 **「dbt 기반이 가장 저렴하다」는 맞지만, 저렴한 소스만으로는 ERD 가 틀린다.**
>   ⇒ 판정식 = **「비용이 얼마인가」가 아니라 「어느 축이 빠지는가」**다.
> · 🟢 GOLD 기반 선택은 타당하다(스타스키마라 FK 가 명시적 · SILVER 는 SQL 파싱이 추가로 필요).
> · 재사용 절차(산출물 상단에 명시) = ① 커버리지 게이트 → ② 생성기 → ③ 음성 테스트.
>   🔴 **① 을 먼저 통과시켜야 한다** — 새 키 컬럼이 생기면 FAIL 로 사람 판정을 부른다.
>
> ---
>
> **⑨ 검증 실행 결과 (전부 종료코드 기준)**
>
> · `gold_erd_coverage_gate.py` rc=0 — 고립 7 · 미분류 **0**
> · `gen_gold_erd.py` rc=0 — 관계 **70**(선언 63 + 논리 7) · BASE TABLE 행 146,457,668
> · `gen_gold_erd.py --yaml-only` rc=0 — 별도 경로 · 점선 0(경고와 일치)
> · `test_gold_erd.py` rc=0 — 단정 **25 / 통과 25 / 실패 0**
> · `test_gold_erd_mermaid.py` rc=0 — 전량판 블록 38 · 실선 204 · 점선 21 · 위반 **0**
> · `line_len.py` rc=0(신규·수정 6파일) · `py_compile` OK · `index_row_gate` rc=0 ·
>   `doc_heading_gate` rc=0 · `doc_type_gate` rc=0
> · `cortex ws ls` — 신규 4스크립트 + 산출물 2건 스테이지 실재 확인(`R2-5`).
>   ⚠️ `ws ls` 는 **FQN 필수**다(`USER$.PUBLIC."snowflake_files":/scripts/`) — 상대경로는 rc=1.
> · `read` 미반환 **0건** · 재호출 **0건**(`R1-3-7-a`).
>
> **독해 좌표·고유 토큰**(`R1-3-7-b`)
> · `00_guides/00_작업지침_세션운영규칙.md` 1~316 전량 · 도착 O ·
>   토큰 = 「R0-7 워크스페이스 파서 제약」·「R2-8-4-d 역방향도 성립한다」·「R4-4-4 🟠 롤백 조건」
> · `20_issue/00_BRIEF.md` 1~143 전량 · 도착 O ·
>   토큰 = 「0-CCCC」·「열린 착수표 13」·「전 문서군 = 2,311,242 자」
> · `03_top-down_gold/06_DDL.sql` 1~100 + 헤더 구간 재독 · 도착 O ·
>   토큰 = 「선언 FOREIGN KEY 50 = 라이브 50」·「SK=0 '(미매핑)'」·「부분 실행 규칙(O53)」
> · `10_dbt_pipeline/models/gold/_gold_ready_schema.yml` 917줄 · 🔴 **1회 미반환**(Output too large)
>   → 캐시 경로 분할 재독 2회로 전량 확보 · 토큰 = 「FACT_TARGET_BIZ」·「EVENT_BK || '|' || MEMBER_DK」
>   ⇒ 판정식 충족(재호출 2 ≥ 미반환 1).
>
> ---
>
> **⑩ 🟠 잔여 · 다음 세션이 판정할 것**
>
> · **㉞ `_gold_ready_schema.yml` 에 `relationships` 테스트 18건 미작성** — 물리 FK 에만 있는 축이다.
>   추가하면 dbt 가 참조 정합을 실제로 검증한다(현재는 ERD 표기만 되고 **검증은 안 된다**).
>   🔴 추가 전에 **위반 행 수를 먼저 실측**하라 — `FACT_TARGET_BIZ` 는 0행이라 공허하게 통과하고,
>   이름 기반 조인 축(`ORG`)은 미매칭이 예상된다(문서50 §O116 WARN 계열과 같은 성격).
> · **㉟ FACT 11개가 물리 PK 미선언** — DIM 20/20 은 선언됐고 FACT 는 6/17 뿐이다.
>   `06_DDL.sql` 실행규칙 8 「FACT PK/UNIQUE 는 grain 미확정으로 보류」와 정합하므로 **결함이 아니다**.
>   🔴 다만 ERD 는 물리 PK 부재 시 **dbt 추론 PK(unique+not_null)로 대체 표기**한다 ⇒
>   그 표기가 「grain 확정」으로 읽힐 위험을 다음 세션이 판정하라(현재는 `pk_note` 로 불일치만 경고한다).
> · **㊱ 산출물이 Mermaid CDN 에 의존한다** — 오프라인에서는 렌더되지 않는다.
>   🟢 폴백을 넣어 **다이어그램 소스가 텍스트로 읽히게** 했고 실패 알림도 표시한다.
>   🔴 그러나 `gen_arch_map.py` 가 확립한 「인라인 · 외부 fetch 없음」 규약에는 **미달**이다 ⇒
>   현업 배포 전에 mermaid 를 vendoring 할지 판정하라.
> · **㊲ 커버리지 게이트가 FACT 만 본다** — DIM → DIM 참조(`DIM_CAMPAIGN.ORG_SK`·`MKTG_CAMPAIGN_SK`)는
>   물리 FK 로 잡혀 ERD 에 있으나, **고립 판정 분모에는 없다** ⇒ DIM 측 고립은 검사되지 않는다.
>   🔴 **「0건」이 「없다」가 아니라 「내 판정식이 못 본다」일 수 있다**(`O111 ㉠`)의 실물 후보다.
