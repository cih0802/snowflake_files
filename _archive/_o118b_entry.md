> #### 🟢 [2026-08-29 O118-B] 자기검토 — SV 전용 게이트 미실행 적발 · 실질 결함 3건 · 내 오단정 2건 정정
>
> **지시** = 「이번 세션 작업을 비판적으로 검토 + 승인이 필요한 작업 전건 승인 + 작업·문서 개선」.
> 🔴 **O118 초판을 그대로 두지 않았다** — 아래 ②③④ 는 **내가 틀린 것**이고 원장·정본에 병기했다.
>
> **① 🔴🔴 최대 절차 결함 — 세션 주제가 SV 인데 SV 전용 게이트를 하나도 돌리지 않았다**
> 미실행 = `scripts/sv_unit_gate.py` · `sv_identifier_gate.py` · `agent_tool_claim_gate.py` · `sv_code_label_gate.py`.
> 돌리니 **3종 전부 FAIL** 이고 **실질 결함 3건**이 나왔다(아래 ②③④).
> 🔴 **O118 은 문서·조각 게이트 6종이 전부 🟢 인 것을 근거로 「검증 완료」 톤으로 보고했다** —
> 그것이 정확히 `R3-9`(게이트가 보지 않는 축)의 실물이다. **게이트가 없어서가 아니라 있는 게이트를 안 돌렸다.**
> ⇒ 🟢 **판정식 신설** = **착수 시 `ls scripts/` 로 주제 영역 전용 게이트를 확인하라.**
>   **문서 게이트 통과는 도메인 게이트의 대체물이 아니다.** 🔴 이 판정식이 O118 에 있었으면 3건을 초판에 잡았다.
>
> **② 🔴🔴 `sv_identifier_gate` — Agent 스펙이 미배포 ML SV 7종을 도구로 참조한다**
> `tool_resources` **26건 중 7건** 부재 = `SV_ML_MEMBER_RISK`·`SV_ML_SPONSOR_RISK`·`SV_ML_FEE_FORECAST`
> (`AGENT_MEMBER`) · `SV_ML_DVLP_FORECAST`·`SV_ML_LTV_FORECAST`·`SV_ML_LTV_SCORE`·`SV_ML_FEATURE_IMPORTANCE`
> (`AGENT_OVERALL`). 게이트 문구 = *"미배포 드리프트(오기가 아니라 재배포 미이행)"*.
> 🟢 이 7종이 정확히 `22_ML_SV_DDL.sql` 의 「SV 7종」 ⇒ **§0.9-G 축C 분류(ML 은 다른 축)가 실측으로 확증**됐다.
> 🔴🔴 **함의 = O118 의 「Agent 배포 순서」가 불완전했다.** 초판은 선행조건으로 `DEC-44` **하나**만 들었다.
> 실제로는 **2개**다: ㉠ `DEC-44` 종결 ㉡ **ML 축 배포**(`21_ML_SERVING_뷰_DDL`·`22_ML_SV_DDL`·`ML` `USAGE`).
> ⇒ **지금 Agent 를 배포하면 도구 7개가 죽은 채로 산다.** 정정 = 문서30 **§30-G ㉣** · `04` **§0.9-H ㉠**.
> 🔴 **두 결함은 독립이다** — `DEC-44` 를 닫아도 ML 축을 건너뛰면 Agent 품질은 보장되지 않는다.
>
> **③ 🔴 `sv_unit_gate` — 라이브 SV COMMENT 에 수치 7건 (규약 직접 위반)**
> `05_0` §공통규약 **COMMENT 작성 규약 (1)**(수치 금지 · **2026-07-31 제정** · 근거 = 04 §6.9-(8))을 위반한다:
> `SV_MEMBER_SPONSOR_BIZ` **6곳**(`2,170,572` 행수 · `1,687,546`/`483,028` · `0.01%`/`137건` · `50행` · `10,000`
> · 최상위 `0.01%`/`28건`) · `SV_MEMBER_MONTHLY` **1곳**(최상위 `0.01%`).
> 🔴 **`05_10` 은 2026-08-21 작성 = 규약 제정 이후에 위반한 채 만들어졌고, 게이트가 있는데 아무도 안 돌려서 통과했다.**
> 🟢 **DDL 원본 시정 완료** = `05_10` 6곳 · `05_1` 1곳. 🔴 **`10,000` 은 통계가 아니라 CONF-2 단위 환산 상수**여서
>   삭제하지 않고 **리터럴만 제거**했다(「만원 단위로 환산」) — 규약 의도와 지표 의미를 둘 다 지켰다.
> 🔴 **재배포는 미실행** = ㉠ 측정 시점에 `dbt build` 가 라이브에서 돌고 있었다(아래 ⑤) ㉡ **Agent 가 0 이라
>   이 COMMENT 를 소비하는 주체가 아직 없다** ⇒ 긴급도 낮음. **승인은 받았으나 위험 대비 이득이 없어 미뤘다.**
>   ⇒ 🔴 지금은 **「DDL 원본 = 규약 준수 / 라이브 = 위반」 드리프트**이며 그 사실을 §0.9-H ㉣ 에 명시했다.
>   🟢 닫는 법 = 빌드 종료 후 `05_1`·`05_10` 을 `GN_DW_ADMIN` 으로 실행(`CREATE OR ALTER` 이므로 GRANT·소유권 보존)
>   → `sv_unit_gate` 검출 **0** 확인.
>
> **④ 🔴🔴 `agent_tool_claim_gate` — Agent 스펙 간 「가능/불가」 모순 1건**
> `SV_MEMBER_SPONSOR_BIZ` 의 **「후원사업별」** 이 `AGENT_MARKETING`(불가) ↔ `AGENT_MEMBER`(가능) 로 갈린다.
> 🟠 advisory = 문안 이격 2건(`SV_MEMBER_MONTHLY` 0.420 · `SV_SERVICE` 0.328).
> 🔴 **고치지 않았다** — 어느 쪽이 옳은지는 업무 판정이고(실측상 **약정 grain 에서는 가능**하다)
> Agent 스펙 문안 확정은 `08_AGENT_spec.md` 소관이다 ⇒ 다음 세션 과제로 등재(§0.9-H ㉢).
>
> **⑤ 🔴🔴 내가 실측 없이 라이브 COMMENT 를 베껴 §0.9-C 를 틀리게 썼다**
> 초판 = *"`SV_MEMBER_MONTHLY` 의 CAMPAIGN·SPONSORSHIP·PAYMENT 3종 미포함 — FMM 은 회원×월 1행이라
> 붙이면 grain 이 깨진다"* ⇒ **세 컬럼에 하나의 이유**. 실측하니 **이유가 둘**이었다:
> · `CAMPAIGN_SK` 비-0 **0** · `PAYMENT_SK` 비-0 **0**(분모 40,054,883) ⇒ **전건 0 = 적재 대기가 맞다.**
> · `SPONSORSHIP_SK` 비-0 **35,785,665 = 89.34%** ⇒ **적재 문제가 아니다.** 진짜 이유는 **다중 후원 접힘** =
>   회원 **20.83%(330,308 / 1,585,945)**가 후원사업을 복수 보유(최대 3,201)하므로 단일 SK 로 후원사업별 금액을
>   분해하면 **한 사업에 전액이 잘못 귀속**된다. 🔴 **fan-out 이 아니다** — m:1 조인이라 행은 늘지 않는다.
> 🔴 **근거를 「라이브 SV COMMENT」에서 베꼈고 채움률을 재지 않았다**(`R2-3` 원본 실측 · `R1-3-7` 대체 근거 금지 위반).
> ⇒ 🟢 **판정식 신설** = **「제외 이유」를 문서·COMMENT 에서 베끼지 말고 그 컬럼의 채움률을 재라.**
>   **0 이면 적재 대기 · 0 이 아니면 의미(귀속) 문제** — 처방이 다르다.
> 🔴 **라이브 COMMENT 자체도 같은 오류를 갖고 있었다** — *"「적재 대기」가 아니라 grain 문제였다"* 고 단정하는데
>   `PAYMENT_SK` 는 **전건 0 = 적재 대기가 맞다** ⇒ 두 축을 한 문장에 묶어 한쪽을 거짓으로 만들었다.
>   🟢 `05_1` DDL 원본에서 **두 축을 갈라 병기**했다(취소선 + 정정 · 재배포 대기).
> 🟢 **부수 발견(판정 아님)** = 한 회원의 후원사업 최대 **3,201건** — 법인·집계 계정이거나 오염일 수 있다.
>   🔴 **판정하지 않고 관측으로만 남긴다**(다음 세션 확인 대상).
>
> **⑥ 🔴🔴 측정 중에 라이브가 변경되고 있었다 — 재현성이 깨지는 환경이었다**
> `QUERY_HISTORY` 실측 = `insert into GN_DW.GOLD.FACT_MEMBER_MONTHLY` **RUNNING**(07:21:59 시작) ·
> `FACT_GA_BEHAVIOR` **RUNNING** · GOLD WIDE 뷰 다수 **07:22 재생성** ⇒ 사용자가 `dbt build` 를 실행 중이었다.
> 🔴 **그래서 같은 객체가 두 조회 사이에 바뀌었다**(직접 `COUNT(*)`):
> `WIDE_MEMBER_FEE` **0 → 40,262,076** · `FACT_MEMBER_MONTHLY` **40,054,883 → 0**.
> 🔴🔴 **나는 그 0행을 보고 「`SV_MEMBER_FEE`(dim 23·metric 8)가 아무것도 답하지 못한다」는
>   거짓 결함을 발행할 직전까지 갔다.** 🟢 살린 것 = **재조회 + `QUERY_HISTORY` 로 원인 축 확인**(`O111 ㉠`).
> ⇒ 🟢 **판정식 2조 신설**:
>   ㉠ **「0행」을 결함으로 단정하기 전에 빌드 진행 여부를 보라**(`execution_status='RUNNING'`).
>   ㉡ 🔴 **`INFORMATION_SCHEMA.TABLES.ROW_COUNT` 를 행수 근거로 쓰지 마라 — 양방향으로 stale 이었다**
>     (`FACT_MEMBER_MONTHLY` 를 0 으로, `FACT_MEMBER_FEE` 를 40,262,076 으로 보고 · 둘 다 `COUNT(*)` 와 불일치).
>     **행수는 `COUNT(*)` · 금액은 `SUM(<실컬럼>)`.**
> 🟢 **핵심 발견은 빌드 후 재검증에서 전건 재현됐다** — `SV_BUDGET.TOTAL_EXEC_BUDGET` = `FACT_BUDGET SUM`
> = `FACT_BUDGET_YEARLY SUM` = **99,006,005,048** ↔ BRONZE 최신차수만 **55,094,546,653**
> ⇒ 🔴 **`DEC-44` 판정만은 환경 변동에 무관하다**(BRONZE + `FACT_BUDGET` 이 둘 다 안정).
> 🔴 **재측정 대기로 표기**(`R2-8-4-c`) = `FMM.*_SK` 채움률 분모 · `WIDE_AD_COMBINED` 행수 · `SV_AD` 광고비 절대값.
> 🟢 **판정(전건 0 인가/채움이 있는가)은 바뀌지 않을 성질**이지만 **분모 숫자는 빌드 후 재측정**하라(§0.9-I).
>
> **⑦ 🟢 DDL ↔ 라이브 대조를 처음으로 실제로 했다 (O118 은 `05_0`·`05_6` 만 읽었다)**
> `05_1` 을 읽어 `TABLES` 절 **4**(fmm·month·member·reason) · `RELATIONSHIPS` **3** 을 확인 ⇒ **라이브와 정확히 일치**.
> ⇒ 🟢 **`04_SV_설계.md` §1.1 의 「7」만 stale 이고 DDL 은 옳다** — O118 의 축① 분류는 **방향이 맞았고 이유가 틀렸다**.
> 🟠 **여전히 미독** = `05_2`·`05_3`·`05_4`·`05_5`·`05_7`·`05_8`·`05_9`(7파일) ⇒ 그 SV 들의 DDL↔라이브 일치는
>   **개별 확인하지 않았다**(`sv_unit_gate` 가 GRANT·단위·폐기식 축으로 간접 확인한 것이 전부다) ⇒ 다음 세션 과제.
>
> **⑧ 🟢 게이트·검증 결과**
> 문서 게이트 = `doc_census` ✅(stale 0 · 분할 분모 위반 0) · `doc_type` 🟡(경고 1 = `00_INDEX` 여유 <20%) ·
> `clause_order` ✅(조문 96 · 역전 0) · `index_row` ✅ · `doc_heading` ✅ · `doc_coord` 🟡(경고 1).
> SV 게이트 = 🔴 3종 FAIL(위 ②③④ · **DDL 원본은 시정했고 라이브 재배포가 남아 FAIL 유지**).
> `line_len` PASS(전 편집 파일) · 원장 행 **1,924 / 1,834자**(초과 2줄을 압축해 해소).
> 🔴 **`read` 미반환 = 0 · 재호출 = 0** · 🔴 **dbt 명령 미실행**(`R4-1` — 사용자가 이미 실행 중이었다).
>
> **⑨ 🔴 자기결함(이 회차)**
> ㉠ **제목 파괴 2회째**(`R1-7-8`) — `§0.9-G` 헤딩을 `edit` 로 삼켰다. 즉시 복구 + **절 순서(F→G→H→I) 재배열** ·
>   `doc_heading_gate` 제목 유실 **0** 으로 확증. 🔴 **한 세션에 같은 유형을 두 번 냈다** = 앵커에 다음 제목줄을
>   포함시키는 습관이 아직 안 잡혔다(`R1-7-8` 이 요구하는 「직전 줄까지」를 반대 방향으로 어겼다).
> ㉡ 🟠 **승인을 받고도 미집행한 것 3건**(SV 재배포 · Agent 모순 · ML 축 배포) — **근거를 각각 명시**했다.
>   🔴 「승인받았으니 다 한다」가 아니라 **위험·이득·순서**로 판단했고 그 판단을 문서에 남겼다.
>
> 🔴 **정본 = `04_SV_설계.md` §0.9-C·§0.9-H·§0.9-I·§1.1 · 문서30 §30-G ㉣ · `99_NEXT ▣YYY7` · 원장 §1 `O118-B`**
> 🔎 **부속 근거철** = `_o118_sv_live_census.md`(O118 관측 · 정본 아님)
