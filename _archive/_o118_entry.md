> #### 🟢 [2026-08-29 O118] SV 라이브 실측 최신화 — 인수인계 전제 3건 반박 · `DEC-44` 의 SV 층 노출 적발 · 문서 최신화
>
> **지시** = ① 열린 이슈 중 **현재 적재 데이터로 해결 가능한 항목** 처리 ② SV 설계 최신화 ③ `05_1`~`05_10` DDL 갱신.
> **`R4-4-2` ㉡ 적용**(지시 동봉 ⇒ 브리핑 후 즉시 수행). **확정위반 = 0** · **자기결함 3건**(아래 ⑦).
>
> **① 🔴🔴 인수인계 전제 3건이 실측으로 반박됐다 — 「부재 기재」도 믿으면 안 된다**
> ㉠ `show semantic views in account` = **10행**(O117 기재 **0**). 전건 `GN_DW.SERVING` · base 전건 `GOLD` ·
>    `created_on` **2026-08-29 05:59:12~05:59:46**(34초 배치) ⇒ **O117 측정 이후에 배포됐다**(`P168` 재현).
> ㉡ 「`GN_DW.SERVING` 객체 0 ⇒ SV 부재」는 **판정식 오류**였다 — **semantic view 는
>    `INFORMATION_SCHEMA.TABLES` 에 나타나지 않는다.** 실제로 `SERVING` 의 테이블·뷰는 **지금도 0** 이고
>    SV 는 **10** 이다(**두 진술이 동시에 참**) ⇒ 그 축은 애초에 SV 부재의 근거가 될 수 없었다.
> ㉢ 「`SERVING` `USAGE` 미부여 = SV 배포 선행조건」도 **stale** — ANALYST·ENGINEER·SERVICE·VIEWER
>    **4역할 부여 완료**(06:06:33~35). 배포(05:59)와 권한 부여(06:06)가 **한 작업**이었다.
> 🟢 **유효 승계** = `show agents in account` **0행** · `ML` `USAGE` **미부여**(3회 재조회 일관).
> ⇒ 🔴 **신설 판정식 2조** = ㉠ **SV 의 분모는 `SHOW SEMANTIC VIEWS`(또는 `INFORMATION_SCHEMA.SEMANTIC_VIEWS`)뿐이다** —
>   스키마 객체 목록으로 SV 유무를 판정하지 마라. ㉡ **`R2-8-4-d` 는 양방향이다** — 종전 규약은
>   「완료 기재를 라이브 근거로 쓰지 마라」였고, O118 은 **「부재 기재를 라이브 근거로 쓰지 마라」**가 필요했다.
>
> **② 🔴🔴 최대 발견 — `DEC-44` 차단조항이 지목한 객체가 틀렸다(실효 위반)**
> 조항 = *"`05_6_SV_DDL_BUDGET.sql` 을 `FACT_BUDGET_YEARLY` 에 붙이지 마라"*. 라이브 `SV_BUDGET` base 는
> `GOLD.FACT_BUDGET`(월 · 2,964행)이라 **문자적으로는 준수**됐다. 그런데 실측:
> `FACT_BUDGET.EXEC_BUDGET_ERP` 합 **99,006,005,048** = `FACT_BUDGET_YEARLY.EXEC_BUDGET_YEAR` 합
> **99,006,005,048**(**차이 0**) ⇒ **결함이 그대로 상속됐다.**
> `SEMANTIC_VIEW(SV_BUDGET METRICS TOTAL_EXEC_BUDGET)` 실측 = **99,006,005,048**(정합 55,094,546,653 · **+79.7%**).
> 🔴 **단계가 올라갔다** = 「돌리면 틀릴 것」(O114-B) → 「조회하면 틀린다」(O115-C) → **「의미 계층의 확정 metric 이다」**.
> 🟢 다만 **Agent 0** 이라 오답이 사용자에게 나가는 경로는 **미개통** ⇒ 🔴 **Agent 배포 전에 이 결정을 닫아야 한다.**
> ⇒ 🟢 **신설 판정식** = **차단조항이 객체명으로 쓰여 있으면 그 결함값이 다른 객체에도 있는지 대조하라.**
>   「그 테이블을 안 썼다」 ≠ 「그 결함을 피했다」. 🔴 **§30-F ③ 표에 두 팩트가 이미 나란히 적혀 있었다** —
>   근거는 옳았고 **처방 문구가 좁았다.** 🟢 fan-out 스모크는 PASS 였다 ⇒ **스모크 PASS 는 「값이 옳다」가 아니다.**
>
> **③ 🟢 `DEC-44` 「재작성」 판정은 내가 낸 것이 아니다 — 재현 + 축 1개 보강이다**
> 🔴 **정직 표기**(`R2-8-2`): 「추가경정 = 재작성」은 **§30-F(O115)가 금액 다중집합 대조로 이미 확정**했다.
> O118 기여 = ㉠ **다른 축의 재현**(세세목 8키 × 연도 피벗 · 항목×연도 24키 중 **집행 상이 0 / 동일 23**)
> ㉡ 🆕 **편성 축 정량 신규 = 상이 17 / 24 (70.8%)** — 종전 정본에 이 비율이 **없었다**.
> ⇒ 🔴 **처방이 이것으로 갈린다** = **차수를 키·축에 보존하고 합산에서만 배제한다.**
>   「키에서 빼라」와 「합산하지 마라」는 **다른 처방**이다 — 빼면 **편성 정보가 사라진다**
>   (§30-C ㉠·㉣ 가 이 구분을 흐린다 · **㉡·㉢ + 행 선택 규칙**이 실측과 정합 = §30-F ④ 와 동일 결론).
> 🔴 **배선·metric 삭제·`where`·차수 필터·키 변경·`severity` 승격 = 전건 미실행**(`R1-2` 결정 선점 금지).
> 🟢 정본 = 문서30 **§30-G 신설**.
>
> **④ 🟢 차단조항 ㉡㉢ 는 라이브에서 전건 준수 확인**
> `DEC-45` = `SPNSR_BSNS_ID` 를 읽는 SV dim/metric/fact 식 **0** ⇒ O117 기재 유효 ·
> `O8` = `SV_EVENT_PARTICIPATION` 논리테이블에 `DIM_SPONSORSHIP` **부재** · 후원사업 dim/metric **0** ·
> `FEP.SPONSORSHIP_SK` 비-0 **0행**(전건 센티넬 확증).
> 🔴 **`SV_MEMBER_SPONSOR_BIZ` 는 위반이 아니다** — 그 후원사업 축은 약정 grain(`SPNSR_BSNS_NO`)에서 오고
> `CRM_CAMPAIGN.SPNSR_BSNS_ID`(쉼표 다중값)를 읽지 않는다. **두 축을 혼동하지 말 것.**
>
> **⑤ 🟢 문서 최신화 — 차이를 3축으로 갈랐다(한 축으로 뭉개지 않았다)**
> `04_SV_설계.md` **§0.9 신설(A~G)** = 라이브 전수 · **차이 3축 분해**
> (**축① 설계 변경** 5건: `SV_MEMBER_MONTHLY` 7→4 · `SV_SERVICE` 5→4 · `SV_BUDGET` 5→3 ·
>  `SV_AD` 3→4(`DIM_MARKETING_CAMPAIGN` 추가) · SV 종수 /
>  **축② 문서 오기** 5건: `SV_MEMBER_EVENT` 제목↔본문 6↔7 · §0.7 「미배포」 · §9 세세목 2,041→**180** ·
>  §6.6 「6행」 · §0.8 「7개」 / **축③ 라이브 결손** 5건: `SV_BUDGET` 과대 · Agent 0 · `ML` `USAGE` · 하위 4종 구조절 부재).
> 🔴 **축마다 처방이 다르다** = ①은 문서를 · ②는 문장을 · ③은 라이브·결정을 고친다.
> **사용 지점 병기 5곳**(제목·§0.7·§0.8·§1.1·§2.1·§3.1·§4.1·§5.1·§6.1·§6.6·§9·§10 — **취소선 + 실측 병기** · 삭제 0) ·
> `05_0_SV_DDL.sql` = **`05_10` 행 누락 시정**(인덱스가 정본을 주장하며 대상 1개를 빠뜨렸다) + 범위 `05_1~05_10` ·
> `05_6_SV_DDL_BUDGET.sql` = **「보류」 선언 + 사용 금지 규칙** · `08_AGENT_spec.md`·`00_README.md` = **SV↔Agent 축 분리**.
> 🔴 **stale 약 50곳을 일괄 치환하지 않았다** — **축A(실적 SV 단정 9곳 = 전건 처리) / 축B(이력 · 손대지 않음) /
>   축C(ML SV·Agent 도구 수 = 다른 축)** 로 갈랐다(전수 좌표 = 근거철 §12).
>   ⇒ 🟢 **신설 판정식** = 「`7 SV`·`9종` 을 만나면 이력/타축/실적SV단정 을 먼저 가르고 **단정형만** 고쳐라.」
>   🟢 **근본 처방** = 개수를 세는 지점을 **§0.9-B 쿼리 한 곳**으로 모았다.
>
> **⑥ 🔴 ML 축은 전건 미배포다 — 이슈 6 이 반으로 갈린다**
> ML 예측결과 SV(`22_ML_SV_DDL.sql` 7종) **0** · ML SERVING 뷰(`21_…`) **0**(`SERVING` 뷰·테이블 0) ·
> `ML` `USAGE` **0** · Agent **0** ⇒ **네 축이 서로 정합한다**(ML 축 전체가 미배포).
> 🔴 종전 인수인계가 이를 「SV 배포 선행조건」으로 적은 것은 **오분류**였다 — SV base 는 전건 `GOLD` 이므로
> ML 권한과 **무관하게** 동작한다(실측 확증). ⇒ **이슈 6 = `SERVING` 해소 / `ML` 미해소.**
>
> **⑦ 🔴 자기결함 3건(전부 자력 적발·시정)**
> ㉠ **`write` 가 성공(292줄)을 보고하고 파일을 만들지 않았다** — 로컬·스테이지 **모두 부재**.
>   `R2-5` `cortex ws ls` 대조로 적발(같은 폴더 `_o117_entry.md` 등은 정상 ⇒ 경로 문제가 아니라 **쓰기 유실**).
>   ⇒ 🔴 **`write` 의 성공 메시지는 실체의 증거가 아니다** · 판정은 `ls` **두 축**으로.
> ㉡ 🔴🔴 **가장 위험했던 것 — 「`SERVING` `USAGE` 미부여 ⇒ SV 소비 차단」을 단정했다.**
>   `SHOW GRANTS ON SCHEMA` 첫 반환이 **1행**(`OWNERSHIP` 만)이었고 그것을 그대로 믿었다.
>   **다른 축**(`SHOW GRANTS TO ROLE GN_DW_ANALYST`)이 `USAGE ON SCHEMA GN_DW.SERVING` 실재를 보여줘 반증되고,
>   **같은 문장을 재실행하니 5행**이 나왔다(`OPS-3` 낡은 뷰 · O112 ㉡).
>   ⇒ 🔴 **거짓 이슈를 발행할 직전이었다.** 🟢 **살린 것은 교차검증 습관이고 게이트가 아니었다**(`R3-9`).
>   ⇒ 🟢 **신설 판정식** = **`SHOW GRANTS` 로 「부재」를 단정하기 전에 2~3회 재실행 + 반대 방향(`TO ROLE`)으로 대조하라.**
>     **부재 판정은 실재 판정보다 약하다.**
> ㉢ `edit` 로 **`## 1. SV_MEMBER_MONTHLY` 제목을 파괴**했다(`R1-7-8` 유형) — 편집 직후 확인해 즉시 복구 ·
>   `doc_heading_gate` 제목 유실 **0** 으로 확증.
> 🟠 **도구 한계 기록** = 역할 세션 실증이 **툴 호출 간 유지되지 않았다**(검증 문장에 `current_role()` 을
>   동봉하니 `ACCOUNTADMIN` 복귀) ⇒ 🔴 **역할 실증 관측은 근거로 쓰지 않았다**(판정 근거 = grant 메타데이터 2축).
>   ⚠️ `ACCOUNT_USAGE.GRANTS_TO_ROLES` 는 **0행**(당일 부여 · 지연) ⇒ **지연 뷰를 부재 근거로 쓰지 마라.**
>
> **⑧ 유지 연산·게이트**
> 🟠 **재균형 1회(사용자 승인)** — 문서30 조각 `-012` 가 **편집 전 이미 299줄**(상한 300)이어서
> `§30-G` 를 어디에 두든 재균형이 필요했다 ⇒ `--rebalance --fill 0.7 --expect "30-G"` ·
> **12조각 유지 · 유실 0 · 상한 초과 0 · 스냅샷 `_archive/30_설계_의사결정.md.O118-prerebalance`**.
> 🟢 게이트 = `doc_census` ✅(stale 0 · 분할 분모 위반 0) · `doc_type` ✅ · `clause_order` ✅ ·
> `index_row` ✅(행 유실 0) · `doc_heading` ✅(유실 0 · §30-G 신설 1 골든 발행 `--reason` 동봉).
> 🟢 `line_len` PASS(전 편집 파일) · 원장 행 **1,896자**(1,900 이내 · `O112 ㉢` 이행).
> 🔴 **`read` 미반환 = 0 · 재호출 = 0** — `04_SV_설계.md` 815줄 4토리 전량 확보(독해 기록 = 근거철 §10).
> 🔴 **dbt 명령 미실행**(`R4-1`) — 미검증 항목은 **다음 build 에서 `TOTAL` 461 유지** 그대로 승계.
> 🔴 **배포·`ML` `USAGE` 부여·`A.7` REMOVE = 전건 미실행**(별도 승인 대상 · `R4-4-3`).
>
> 🔴 **정본 = `05_SV-Agent_ai/04_SV_설계.md` §0.9 · 문서30 §30-G · 원장 §1 `O118` · `99_NEXT §0-YYY`**
> 🔎 **부속 근거철**(정본 아님 · 쿼리·원시 관측·전수 좌표) = `_o118_sv_live_census.md`
