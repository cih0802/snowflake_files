> #### 🔴 [2026-09-16 O166-B] 트랜스크립트 기반 자기검토 — **내 판정 3건 정정** + `06` 산출물 복구
>
> 🔴🔴 **정본 = `20_issue/_o166_evidence.md` §8**(§8-1~§8-5). 이 항목은 서술이다.
> 지시 = ㉠ `20_issue` 독해 ㉡ 트랜스크립트 추출 + `scripts/` 제네레이터 조건부합·stale 검토
> ㉢ 논리 모순·규칙 위반·누락 비판적 자기검토 ㉣ 결함 보완.
>
> **▣ 1. 규칙 준수 축 — 전건 청결(155 `tool_use`)**
>
> `cortex conversations transcript --output=json 87677468` = **JSONL 169줄**.
> 🔴 **자기시정 3(파싱)** = 첫 시도 `json.load()` 가 `Extra data` 로 죽었다(JSONL 이다).
> O165 `D12` 의 「스키마를 먼저 덤프하라」를 지켜 **0건 오판 없이** 정정했다.
> 실측 = bash 82 · edit 33 · read 28 · SQL 6 · write 3 · bash_output 1 · skill 1 · 질문 1.
> 🟢 **SQL 6건 전부 `SELECT`**(DDL·DML 0) · `write` 3건 전부 **신규** · `edit` 12종 전부 **기존** ·
> **허브 직접 편집 0** · **`00_BRIEF` 편집 0** · **dbt 0건**(`R4-1`) · **`rm`·ABORT·discard 0건**.
>
> 🔴 **자기시정 4(판정식)** = 미반환 탐지기가 신호 문자열을 세어 **2건**을 냈는데 둘 다
> **지침 문서 본문 인용**이었다(`R1-3-7` 조문이 그 문구 자체를 적는다) ⇒ **오탐** ·
> 🟢 **`read` 미반환은 실제로 0건**이고 초판 보고가 맞았다.
> ⇒ 🟢 **새 판정식** = **「신호 문자열」로 세면 그 신호를 설명하는 문서가 분모에 있을 때 오탐한다**
> (`O111 ㉢` 의 실물). 🔴 **맞는 보고를 「정정」하지 않도록 먼저 열어 보라** — 자기검토의 역방향 위험이다.
>
> **▣ 2. 🔴🔴 정정 1 — `E3` 「미탐 시정」은 과대 주장이었다**
>
> 실측 = `DIM_MEMBER_STATUS_HISTORY` 로 가는 **FK 0건** · `DK_JOIN` **미등재** ·
> `05_지표GOLD매핑` 등장 **0회** · **`09` 재생성 후 골든 차이 0건**.
> ⇒ 판정기가 그 차원에 **도달하지 않으므로 경고가 발동하지 않는다.**
> 🔴 초판이 원장·근거철·이력에 쓴 *"실제로 위험한 차원은 무경고(미탐)였다 ⇒ 시정"* 은 **과대 주장**이다.
> 🟢 **정확한 문안** = `DIM_MEMBER` **오탐 제거는 유효**(실측 1행/회원)하고 새 등재는 **「잠재 가드」**다.
> 🟢 발동 조건 2개를 코드 주석에 명시했다(㉠ FK 생성 ㉡ `DK_JOIN` 추가).
> 🔴 **㉡ 를 지금 하지 않았다** — 매핑 필드가 0건이라 판정을 바꿀 근거가 없고, 근거 없이 도달 경로를
> 늘리면 `조립가능` 이 실측 없이 증가한다(`R2-3`). 🟢 **`09` 차이 0 이 이 판단의 실증**이다.
>
> **▣ 3. 🔴 정정 2 — `E4` 의 「항상 참」은 부정확했다**
>
> 조건이 `(A and B) or A` 이므로 **무조건 참이 아니라 `A` 조건부로 참**이고, 무력화된 것은 **`B` 검사**다.
> 🔴 이 문안은 원장·이력에 인용됐다 ⇒ **코드 주석·근거철·원장·이력 4곳 전건 정정**(`R2-8-2` 문안 판본).
>
> **▣ 4. 🔴🔴 정정 3 — `06` 산출물이 2주 stale 인데 골든을 발행했다**
>
> 🔎 경위 = 초판이 `gen_bronze_exposure_audit.py`·`run_bronze_audit_host.py` 의 개명 잔여를
> **정정했는데 `06` 을 재생성하지 않았다** ⇒ 골든의 `06` 지표가 **2026-09-02 판본**을 고정했고
> 🔴 **코드·산출물·골든 3자가 어긋난 채 🟢 로 닫혔다.** 사용자 지시 ㉡(제네레이터 조건부합·stale 검토)가
> 정확히 이 결함을 찾아냈다.
> 🟢 **처방** = 정본 경로(무인자 = 라이브 직접조회)로 재생성 ⇒ 개명 반영 확인
> (`FACT_SERVICE_EVENT` **6→0회** · `FACT_MESSAGE_DISPATCH` **0→6회** · `FACT_GA_BEHAVIOR` **0회**)
> 후 골든 재발행 ⇒ `test_generators` **rc=0 · PASS 21**.
> **차이 6건 규명** = 분모 1,153 → **1,204**(실측 CRM **1,014** + AGENCY **106** + ERP **84** ·
> GA4 10·GSC 13 은 **분모 밖**) · 구성 = O161·O162 CRM 개편(신규 4종 **42컬럼** + 누락 보완 2종 **30컬럼**
> + `MKTG_CHANNEL` 1 − 삭제 2종 − `OPEN_DT` 이설) · **합계 정합**(137+15+439+13+542+58 = 1,204) ⇒ 유실·창작 0.
> 🔴 **`노출됨 −1` 은 행 귀속 불가로 관측**으로 남긴다(이전 판본 사본 `_archive/` **0건**) ·
> 🟢 **단 「내 개명 정정이 원인」 가설은 반증**했다(분모에 드는 `LINEAGE_MAP` 수정 2건은 재생성 후에도
> 둘 다 `노출됨(GOLD)` · 나머지 4건은 GA4 축이라 분모 밖).
> 🟢 **근본 처방 집행** = 러너 `main()` 에 **`snapshot_util` 경유 스냅샷** 추가(`R1-7-10`) ⇒
> 🔴 **골든 대조 도구가 있는데 비교 대상이 사라지는 구조**를 닫았다. 다음 갱신부터 행 단위 규명이 된다.
> 🟢 부수 = 그 생성기 docstring 의 「BRONZE 1,121 컬럼 (… GA4 30)」이 **수치 stale + 분모 오기** 2축으로
> 틀렸으므로 **수를 지우고 재는 방법**(러너 로그 줄)으로 교체(`R3-9 ㉦`).
>
> **▣ 5. 🔴 자기시정 5 — `R1-7-8` 제목 삼킴 5회차 + `D13` 재현**
>
> §8 을 삽입하는 `edit` 이 근거철 `## ▣ 7.` 제목을 `old_string` 에 넣고 **되넣지 않아 삼켰다.**
> 🔴 인수인계 §0-LLLL 이 그것을 **명시 경고**했는데도 어겼다(O165 4회 + 1회 = **5회차**).
> 🟢 제목만 되넣어 복구(본문 유실 0 · `doc_heading_gate` 확인).
> 🔴🔴 **복구 시도 1회가 「문자열 없음」으로 실패했는데 파일 문제가 아니라 낡은 마운트 뷰였다**(`D13` 재현) —
> 마운트 **15,296 B / 227줄** ↔ 스테이지 **23,440 B** ⇒ 25초 후 재측정 **23,433 B / 320줄**.
> 🟢 **판정식 재확인** = 「부재·손상」을 단정하기 전에 **스테이지 크기 + 시간 간격**을 교차하라.
>
> **▣ 6. 🟢 새 판정식 3개**
>
> * 🔴🔴 **「등록부의 키를 고쳤다」는 「그 등록부가 작동한다」가 아니다** — 판정기가 그 객체에
>   **도달하는지**를 따로 실측하라(FK · 자연키 조인 · 매핑 등장 횟수).
> * 🔴🔴 **생성기 코드를 고쳤으면 그 생성기의 산출물을 전부 재생성한 뒤 골든을 발행하라**
>   (이번엔 6개를 고치고 4개만 돌렸다). 🟢 판정식 = **「내가 만진 생성기를 세라.」**
> * 🔴 **덮어쓰는 생성기를 스냅샷 없이 두지 마라** — 골든이 차이를 알려 주는데 비교 대상이 없으면
>   그 차이는 **영구히 미귀속**으로 남는다.
>
> **▣ 7. 검증**
>
> 게이트 = `doc_census`·`doc_type_gate`·`clause_order_gate`·`index_row_gate`·`doc_heading_gate`·
> `doc_coord_gate`·`gate_census` **전건 rc=0** · `rename_stale_gate` 🟡(증가 0 · 축1 531) ·
> 음성 테스트 **전건 rc=0**(파이프 없이 · `R0-8-2`) · `test_generators` **rc=0** ·
> `line_len.py` 🟢 PASS(원장 §1 행 **1,985자** ≤ 2,000) · `read` 미반환 **0**.
> 🔴 **확정위반 1건 = `R1-7-8` 제목 삼킴**(자기적발·복구 · 유실 0).
> 🔴 **남긴 것** = `D8`(현업) · 원천 18번 · CSV 재언로드 · 문서20 §M·§N-13 · 착수표 ㉗(사람) ·
> **O164 원장·이력 미등재** · O165 `D11` 후속 · 개명 축1 **531건** · `scripts/` 잔여 5건 ·
> **`06` `노출됨 −1` 미귀속** · O163 세션 날짜 미래 기재.
