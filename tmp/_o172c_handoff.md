## 0-AAAB/O172-C. 🟢 [2026-09-17 O172-C — A(스킬 개선분) 마감 · **A·B 와 함께 읽어라**]

> 🔴 사용자 정정 = *"B는 스킬 개선에 상관 없는데. 브리핑 받아서 생긴 작업이구나. 넌 스킬 개선
> 작업만 하면 되니까 A만 하자."* ⇒ 🟢 **판정식 = 인수인계에 「우리 소관」과 「이 작업의 소관」을
> 섞어 적지 마라.** 브리핑이 준 열린 작업(현업·결정 대기)은 **이 개선 작업의 잔여가 아니다.**
> ⇒ 다음부터 ▣ 「다음이 할 일」을 **㉠ 이 작업의 잔여 / ㉡ 워크스페이스 백로그**로 갈라 적는다.

### ▣ O172-C-1 🟢 A2 집행 — 음성 테스트 축8 신설(O172-B 가 다음 세션에 넘긴 것을 즉시 집행)

· 🔴 `O172-B` 의 *"다음 세션이 축을 보강하려면…"* 은 **이 단위가 집행했다**(이월 아님).
· 신설 축8 = **「다단위 세션에서 후속 단위를 써도 앞 단위가 사라지지 않는가」**
  ⇒ `test_handoff_write.py` 단정 **34 → 41건** · 전건 PASS.
· 🟢 **「고치기 전 구현으로 돌리면 실패」 실증** = 초판 `paths[-1]` 재현 시
  `['…-O0172-B.md']` ↔ 신 `['…-O0172-A.md', '…-O0172-B.md']` (다른 값이 나옴을 단정).
· 🔴🔴 **테스트 자체의 자기결함도 1건 고쳤다** — 초판 축8 은 `label_units()` 의 판정식을
  테스트가 **재현**했다. 🟢 그것이 곧 `J8`「같은 것을 다르게 재는」 결함이므로,
  픽스처 stem 을 `99_NEXT_SESSION` 으로 두고 **실제 함수를 직접 호출**하도록 바꿨다.
  🟢🟢 **판정식 = 테스트가 판정식을 베끼면 그 테스트는 판정식을 검사하지 못한다**(자기 사본을 검사한다).

### ▣ O172-C-2 🟢 A1 회귀 축 — 이 세션에서 이미 실증됐다

| 축 | 결과 |
|---|---|
| 브리핑 §2 가 라벨 파일을 현행으로 뽑는가 | 🟢 `-O0172-A.md:17` · 판정법 = 라벨 최대값 |
| 다단위를 모두 싣는가 | 🟢 단위 목록 표기 · 항목 추출 **5 → 9건** |
| `handoff_gap` 오탐 | 🟢 **경고 0건**(착수표 열린 `⑭`·`⑩` 이 본문에 실려 있다) |

🔴 다음 세션이 확인할 것은 **하나만 남았다** = 「**세션이 바뀐 뒤에도** `O0172-A~C` 3단위가
승계로 내려가고 새 O번호가 현행이 되는가」(O번호 경계 전이 축).

### ▣ O172-C-3 🟢 A3 판정 — 승계 절 은퇴 19건은 **하지 않는다**(실측 근거)

· 실측 = `99_NEXT` 조각 **35개** · 최대 `-022.md` **28,524 B**(여유 **12,436 B** = 상한의 30%)
  ⇒ 축3 기준(여유 20% = 8,192 B)을 **이미 만족**한다.
· 🟢🟢 **더 결정적인 근거** = 라벨 파일 전환으로 **조각은 이제 성장하지 않는다**(인수인계가
  조각에 들어가지 않는다) ⇒ 은퇴의 유일한 근거였던 **「다음 편집에서 초과한다」가 소멸**했다.
· 🟢 **판정식 = 용량 처방의 근거는 「지금 크기」가 아니라 「성장 경로가 살아 있는가」다.**
  성장 경로를 끊으면 **은퇴는 불필요해진다**(은퇴는 성장을 못 끊을 때의 차선책이다).

### ▣ O172-C-4 🔴 A4 — `00_작업지침` 여유 부족: 실측 완료 · **처방은 결정 대기**

· 실측 = **37,025 B / 355줄** · 여유 **3,935 B**(9.6% · 축3 경고 상주) · 갱신형(조문이 추가된다)
  ⇒ 🔴 **다음 조문 신설에서 확정 초과 가능**하다.
· 계열별 실측 = `R1` **15,217 B**(41.1%) · `R3` **7,929**(21.4%) · `R2` **5,843**(15.8%) ·
  `R0` **5,086**(13.7%) · `R4` **2,685**(7.3%) · 머리말 266.
· **처방 2안**(🔴 어느 것도 임의 집행하지 않았다 — 지침 정본을 쪼개는 결정이다 · `R1-2`):

| 안 | 내용 | 예상 여유 | 근거 |
|---|---|---|---|
| ㉠ **`R3` 분리**(권고) | `R3`(단계 종료 게이트) → `00_guides/04_단계종료_게이트.md` | **11,864 B**(29%) | 🟢 **읽는 시점이 「완료 보고 전」**이라 지연 로드에 맞다 · 🟢 `R3-9 ㉠`~`㉨` 매 세션 노출은 **스킬 §5-2 가 이미 담보**(검증기 `I6`) ⇒ 가용성 손실 0 |
| ㉡ `R0` 분리 | `R0`(셸 안전) → 별 문서 | 9,021 B(22%) | ⚠️ `R0` 는 **매 bash 호출**에 적용된다(가드 G1) — 스킬이 요약을 싣지만 상세를 늦게 읽으면 사고 시점에 늦다 |

· 🔴 어느 안이든 **선례(`R1-6`·`R1-7` 분리)의 절차를 그대로** 따라야 한다 =
  `R2-8-1` 토큰 대조(blocking) → 조문 번호 **불변** → 분모 4곳 편입 → **골든 발행** →
  원장 §0 유형 등재 → 스킬 정본(`03_init_ihcho_스킬_정본/본문`)의 포인터 갱신 → 검증기 재실행.
· 🟢 **판정식 = 「분리해도 가용성이 줄지 않는가」는 스킬이 그 조문을 재수록하는지로 판정한다.**

### ▣ O172-C-5 📏 A 마감 기준선

· `test_handoff_write` 단정 **41** · 음성 테스트 **38종 전건 rc=0**
· 게이트 = census ✅(stale 0) · 유형 🟡(경고 1 = `00_작업지침` ⇒ `O172-C-4`) · 조문순서 ✅ ·
  행키 ✅ · 제목 ✅ · 좌표 🟡 ⇒ **전종 rc=0**
· 라벨 파일 **3개**(`O0172-A`·`-B`·`-C`) — 3단위까지 실증
· 🔴 **스킬 개선 잔여는 `O172-C-4` 결정 1건뿐이다**(그 외 A 항목은 이 단위에서 닫혔다).
