# O160 근거 — 착수표 ⑭ / MSTR-4 `FME.SPONSORSHIP_SK(STOP)` 귀속 룰 실측

> 🔴 이 파일은 **판정 근거 고정용 스크래치**다(`R1-3-7-c`). 정본이 아니다.
> 계정 = wz61282 · DB = `GN_DW` · 측정일 = 2026-09-14 · 측정자 = O160
> 🔴 정본 반영(문서50 §BLOCKING-5 표 정정 · 문서20 현업질의)은 **미실행**이다.

## 0. 측정에 쓴 조인 정의 (재현 필수)

- `dsc` = `BRONZE_CRM.TM_MM_FDRM_MBER_SPNSR_DSCNTC`
  · 필터 `MBER_NO IS NOT NULL AND SPNSR_DSCNTC_DE IS NOT NULL AND SER_NO IS NOT NULL`
  · 이 필터는 `SILVER.CRM_MEMBER_DISCONTINUE.sql:24` WHERE 절과 동일하다.
- `bsns` = `TM_MM_FDRM_MBER_SPNSR_BSNS b` ⋈ `TM_MM_FDRM_MBER_SPNSR s` ON `b.SPNSR_NO = s.SPNSR_NO`
  · 필터 `b.SPNSR_DSCNTC_YN='Y' AND b.SPNSR_DSCNTC_DE IS NOT NULL`
- **정상 조인** = `dsc.MBER_NO = bsns.MBER_NO AND dsc.SPNSR_DSCNTC_DE = bsns.SPNSR_DSCNTC_DE`
  (회원 + **중단일 일치**)

## 1. 🔴🔴 기재 수치 3건이 재현되지 않는다 (`R3 1` · `O111 ㉠`)

| 기재(착수표 ⑭ · 문서50 O73-C) | 이번 실측 | 판정 |
|---|---|---|
| 팬아웃 **1.56배**(pairs 1,517,797 / 키 970,486) | **1.1020배**(pairs 1,069,447 / 키 970,486) | 🔴 불일치 |
| 동시중단 **4.39% · 5.2만 건** | 다중사업 키 **37,523**(매칭키의 3.8664%) · 영향 dsc행 **75,660** | 🔴 불일치 |
| 「1.56배 팬아웃 방지」가 차단 사유 | 실제 차단 필요 구간은 **1.2458%**(§3 버킷3) | 🔴 과대 |

- 🟢 **1.56배의 출처를 규명했다** = **중단일 일치를 걸지 않은 회원 단위 조인**이다.
  · 그 변형 실측 = 키 971,590 / pairs **1,626,753** / 팬아웃 **1.6743배**
  · ⇒ O73-C 의 1.56 은 「정상 조인의 팬아웃」이 아니라 **날짜 축을 잃은 조인의 팬아웃**이다.
  🔴 pairs 1,517,797 자체는 두 변형(1,069,447 · 1,626,753) 어느 쪽과도 같지 않다 ⇒ 제3의 변형이며 **미규명**이다.
- 🔴 **4.39% · 5.2만의 분모·정의는 미규명**이다. 자연스러운 분모 3종으로 재현되지 않았다
  (매칭키 970,486 → 3.87% · FME STOP 행 1,038,262 → 7.2872% · 다중pair 키 67,902 → 7.00%).
  ⇒ 이 수치는 **인용하지 마라**(정의 부재).

## 2. 원천 실재 — 「원천 부재」는 여전히 거짓이다 (O73-C ② 는 유효)

- `TM_MM_FDRM_MBER_SPNSR_DSCNTC` 컬럼 9개 전수 = 후원사업·후원번호 컬럼 **없음**(DESC 확인).
- 그러나 `SPNSR_BSNS`(2,170,572행 · `SPNSR_DSCNTC_YN='Y'` **1,337,780행**)에 있다.
- 도달률 = dsc 키 973,215 중 **970,486 매칭 = 99.7196%** · 미매칭 키 2,729(행 2,730 · 0.2629%).
- 후원사업 종수 = `DVLP_DIV_CD='5'` 기준 **27종** · `DIM_SPONSORSHIP` 는 51행.

## 3. 🟢🟢 최대 발견 — 차단 대상은 전체가 아니라 1.2458% 다

정상 조인 매칭 키 970,486 을 **다중사업 여부 × 행수 일치 여부**로 분해했다.
분모 = `FACT_MEMBER_EVENT` STOP 행 **1,038,262**(= `SILVER.CRM_MEMBER_DISCONTINUE` 행수).

| 버킷 | 키 | dsc 행 | biz 행 | FME STOP 대비 | 귀속 룰 필요성 |
|---|---|---|---|---|---|
| ① 단일사업(`DISTINCT_BIZ=1`) | 932,963 | 959,872 | 974,991 | **92.4499%** | 🟢 **불필요** |
| ② 다중사업 · 행수 일치 | 25,251 | 62,725 | 62,725 | **6.0413%** | 🟡 순번 대응 후보 |
| ③ 다중사업 · dsc 행 부족 | 12,272 | 12,935 | 31,731 | **1.2458%** | 🔴 **현업 결정 필요** |
| ④ 미매칭(biz 부재) | 2,729 | 2,730 | 0 | 0.2629% | 🟢 0 센티넬 유지 |

- 합계 검증 = 959,872 + 62,725 + 12,935 + 2,730 = **1,038,262** ✅ 분모 정확 일치.
- ① 은 **사업 ID 가 유일**하다(행은 974,991 로 더 많지만 `DISTINCT_BIZ=1`)
  ⇒ 키별 `MAX(SPNSR_BSNS_ID)` 로 **팬아웃 0 · 오귀속 0** 으로 채워진다. **룰이 필요 없다.**
- ② 는 dsc 행수 = biz 행수라 **키 내부 1:1 순번 대응**이 성립할 수 있다(전체 키 기준 행수 일치율 **97.7805%**).
  🔴 다만 「SER_NO 순서 ↔ 후원사업 순서」가 업무적으로 같은 사건이라는 **보증은 원천에 없다** ⇒ 현업 확인 대상.
- ③ 이 **진짜 다대일**이다 — dsc 12,935 행에 biz 31,731 행이 걸린다(사건보다 사업이 많다).

## 4. 방안 A(대표 1건) 실측 비용

- Grain·기준선 = **불변**(1,038,262 행 유지 · `STOP_CNT` 1,038,262 · FMM 롤업 40,054,883 무영향).
- 🔴 오귀속 = 다중사업 키 37,523(dsc 행 **75,660** = FME STOP 의 **7.2872%**)에서
  대표 1건만 남으므로 **사업 단위 중단사건 18,796 건이 소멸**한다(94,456 − 75,660).
- 🔴 **금액 대표성이 낮다** = 다중사업 키에서 `MAX(ABS(SPNSR_AMT))` 가 그 키 `SUM(ABS(SPNSR_AMT))` 의
  **42.3402%** 뿐이다 ⇒ 「최대 약정금액 대표」는 **57.66% 의 금액 신호를 버린다**.
- 최대 동시중단 사업 수 = **11종**.

## 5. 방안 B(`DVLP_DIV_CD='5'` 전개) 실측 비용

- `TM_MM_FDRM_MBER_DVLP_AMT` `DVLP_DIV_CD='5'` = **1,010,680행** · 사업 grain 키 **1,010,457** · `SPNSR_BSNS_ID` NULL **0건**.
- 🔴🔴 **커버리지 손실** = dsc 회원-일 키 973,215 ↔ DVLP5 회원-일 키 924,044 · 공통 **923,931**
  ⇒ **dsc 에만 있는 키 49,284(5.0640%)** 가 팩트에서 사라진다. DVLP5 에만 있는 키 113.
- 🔴 `STOP_CNT` 기준선이 1,038,262 → 약 1,010,680 으로 **바뀐다**(−27,582 · −2.6566%)
  ⇒ FMM 의 `STOP_CNT` 롤업 일치(이력 `01_세션이력-017.md:106` 「STOP_CNT 1,038,262 동일」)가 **깨진다**.
- 🔴 중단사유(`DSCNTC_RSN_CD`)·중단경로(`DSCNTC_PATH`)는 DVLP5 에 **없다**
  (DVLP_AMT 23컬럼 DESC 확인 · 유사 컬럼 `CANCL_RDCAMT_RSN_CD` 는 **취소감액사유**로 축이 다르다)
  ⇒ `REASON_SK`·`STOP_REASON`·`STOP_CHANNEL`·`DIM_REASON` 배선이 **역행**한다.

## 6. MSTR 레거시가 실제로 한 것 (`COCO_mstr_dw_reference.md`)

- §7.1 「개발구분 5 중단」(`:280`~`:285`) = 원천 코드 5 기반 · dsc 와 `MBER_NO + OCCRRNC_DE=SPNSR_DSCNTC_DE` 로 연결 ·
  **`회원 + 후원번호 + 후원사업번호 + 월`별 `SER_NO DESC` 첫 행**.
  ⇒ 🔴 MSTR 은 **사업 단위를 유지**하고 그 안에서 중복만 제거했다 — **대표 1건으로 접지 않았다.**
- §10.4 감액(`:478`~`:480`) = `회원+월별 SPNSR_NO DESC 첫 행에 전체 감액 귀속` ⇒ **대표 1건은 「감액」 축의 룰**이다.
  🔴 `:278` 이 *"원본 정렬이 `SPNSR_NO DESC`뿐이라 동률 시 차원 귀속이 **비결정적**"* 이라고 **스스로 경고**한다.
- ⇒ 🔴 **방안 A 를 「MSTR 재현」이라 부르는 것은 부정확하다** — A 는 감액 룰을 중단에 전용한 것이고,
  MSTR 의 중단 처리는 오히려 **B 쪽(사업 단위 유지)**에 가깝다.

## 7. 판정 (O160)

- 🔴 **A·B 어느 쪽도 단독 채택 근거가 없다.** A = 금액신호 57.66% 손실 + 사업사건 18,796 소멸 ·
  B = 중단사건 키 5.0640% 소멸 + 기준선 변경 + 사유·경로 축 역행.
- 🟢 **분해 착수를 권고한다** = ① **92.4499% 는 지금 배선 가능**(룰 불요 · 현업 대기 불필요) ·
  ② 6.0413% 는 순번 대응 가설로 현업 확인 · ③ **1.2458% 만 진짜 결정 대기** · ④ 0.2629% 는 0 센티넬 유지.
- 🔴 ①만 배선해도 **팬아웃 0 · Grain 불변 · 기준선 불변**이다 ⇒ P1 차단의 92% 가 풀린다.
- 🔴 **배선은 이 세션에서 하지 않았다**(착수표 ⑭ 정지점 = 「현업 결정 전 배선 금지」).
  ⇒ ① 이 그 정지점의 대상인지(룰이 필요 없으므로 아닐 수 있다) **사용자 판단이 필요하다**.
