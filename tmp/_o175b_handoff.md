### ▣ O175-B-0 🔴🔴 먼저 알아라 — 이 세션이 확정한 판정식 4개

🔴🔴 **㉠ 넘겨받은 전제를 현업 문항·설계 근거로 쓰기 전에 분해해서 다시 재라 — 3/3 기각됐다.**
인수인계가 준 전제 3건이 **전부** 실측으로 무너졌다. O174 가 같은 판정식을 1건으로 세웠고
이번에 3건이 더 나왔다 ⇒ **우연이 아니라 구조적 경향**이다.
· `E-4` 「3종을 어느 grain 으로 `FACT_BUDGET` 에 귀속하는가」 ⇒ **질문 자체가 성립하지 않았다**
  (광고비는 이미 `GOLD.FACT_AD_PERFORMANCE` 에 일 grain 전량 배선돼 있었다).
· `DEC-35` 「라벨 노출 5건 = SV 정의 변경이라 승인 불요 범위를 넘는다」 ⇒ **거짓**
  (라벨 5건이 이미 SV 차원으로 실재했고 잔여는 게이트 등재부의 stale 이었다).
· `_STDR_YM` 「원천 행 생성월인가 · `_LOAD_DT` 와 중복인가」 ⇒ **둘 다 표면**
  (사용자 정정으로 **월 파티션 재적재 키**임이 드러나 판단 기준 자체가 바뀌었다).
🟢 절차 = ㉠ 좌표를 읽고 ㉡ 라이브를 재고 ㉢ **「이 질문이 성립하는가」를 먼저 물어라.**

🔴🔴 **㉡ 「라벨 부재」·「미노출」은 상태가 아니라 등재부의 주장이다.**
`sv_code_label_gate` 정보 6건(`LABEL_PAIRS` 미등재)은 SV 결함이 아니라 **등재부 stale** 이었다
(`P149` 재발 — 등재부가 낡으면 검사가 대상에서 빠져 **침묵**한다).
🟢 판정식 = **게이트가 「미등재」라 하면 등재부와 라이브를 둘 다 열어라** —
어느 쪽이 낡았는지는 게이트가 말해주지 않는다.

🔴🔴 **㉢ 같은 것을 다르게 재는 지점은 반드시 둘 이상 있다.**
BRONZE 노출감사 판정 로직이 `gen_bronze_exposure_audit.py` 와 `run_bronze_audit_host.py`
**두 곳에 병렬로** 있었다(O174 가 `LINEAGE_MAP` 2줄을 놓쳐 「노출됨 137 → 135」로 뒤집힌 것과 같은 구조).
🟢 이번엔 두 곳을 함께 고치고 판정기를 돌려 **노출됨 137 불변**을 확인했다.

🔴 **㉣ 「값 일치」도 인과의 증거가 아니다 — 종수를 양쪽에서 세라.**
`UPPER_CMPGN_NAME` 이 **전건 채움**(829,609/829,609)인데 distinct 는 **993 ≠ 코드 994** 였다
(`청소년 캠페인` = `U0884`+`U1257` · 합 14,581행) ⇒ 라벨로 그루핑하면 두 코드가 **합쳐진다**.

🟢🟢 **㉤ [신설] 컬럼의 정체를 실측으로 확정했어도 「운영에서 무엇에 쓰는가」는 사람에게 물어라.**
`_STDR_YM` 은 실측상 생성월 파생이 맞았지만 **역할은 적재 제어**였고, 그 차이가 쟁점을
「업무 축과 중복인가」에서 **「삭제가 하류로 전파되는가」**로 바꿨다.

🔴 **㉥ [신설] `CREATE OR REPLACE` 를 ACCOUNTADMIN 으로 하면 OWNERSHIP 이 넘어간다.**
배포 전에 `USE ROLE GN_DW_ADMIN` 으로 전환해야 한다(역할은 세션에 지속되며 `CURRENT_ROLE()` 로 확인 가능).
🔴 GRANT 보존만 보지 말고 **OWNERSHIP 행을 함께 보라.**

---

### ▣ O175-B-1 🟢 이 세션이 끝낸 것 — §4 결정대기 8건 중 3건

🔴 진행 방식 = 사용자 지시대로 **항목마다 실측 → 선택지 숫자 제시 → 질문 1건 → 즉시 집행**.
묶어 묻지 않았고 결정 없이 집행하지 않았다. `R4-4-2 ㉡` 분기 · **확정위반 0건**.

| # | 항목 | 사용자 결정 | 결과 |
|---|---|---|---|
| ① | `E-4` `AD_COST` | **A안** 폐기 슬롯 + COMMENT 시정 | 파일 4곳 + dbt build 대사 **4/4 적중** |
| ② | `DEC-35` 라벨축 | **C안** 라벨 컬럼 2건 배선 | 뷰 2 + SV 2 재배포 · 게이트 정보 **10→3** |
| ③ | `_STDR_YM` | **A안** BRONZE 한정 + 런북 | 감사 미노출 **592→542** · 「제외(적재제어메타)」 50 신설 |
| 부수 | 임시 계측기 처분 | **㉠** 기존 도구 일반화 + 삭제 | `extract_sv_deploy --name` + 음성 15단정 신설 |

🔴 **정본 = 이력 §O175-B**(경위·실측 전량). 이 파일은 **다음 세션용 요약**이다.

---

### ▣ O175-B-2 🟠 다음이 할 일 — ㉠ 이 작업의 잔여

🔴 이 4건은 **이번 3건에서 파생된 것**이고 워크스페이스 백로그가 아니다.

1. 🟠 **`GOLD.FACT_BUDGET.AD_COST` 기본 테이블 COMMENT** 가 아직 `'광고비'` 다.
   원인 = `_gold_ready_schema.yml` 의 `FACT_BUDGET` 등재가 **키 6컬럼만**이라 measure COMMENT
   정본 경로가 없다(뷰 쪽은 `_wide_schema.yml` 이 정본이라 반영됐다).
   🔴 결정할 것 = ㉠ 그 yml 에 measure 컬럼을 등재하는가(계약 확대) ㉡ 뷰 COMMENT 로 충분하다고 보는가.
2. 🟠 **`_BATCH_ID` ×50** 이 `_STDR_YM` 과 **같은 class** 인데 「미노출(검토대상)」로 남아 있다.
   🔴 결정 범위 밖이라 집행하지 않았다(`R1-2`) ⇒ 같은 A안을 적용할지 물어라.
   🟢 적용하면 미노출 **542 → 492** · 제외(적재제어메타) **50 → 100** 이 될 것이다(예측값이므로 재라).
3. 🟠 **`UPPER_CMPGN_NAME` 동명 2코드**(`청소년 캠페인`) — SV COMMENT 에 경고는 넣었으나
   현업이 이 둘을 같은 채널로 보는지 다른 채널로 보는지는 **미확인**이다.
4. 🟢 **`SV_SERVICE.SUBTYPE`** 1건이 게이트 정보로 남았다 — 라벨을 **의도적으로** 만들지 않은 건이고
   (등급 D · 코드군 배타 특정 불가) 현업 회신 대기다(문서20 §M-2). 🔴 이것을 「할 일」로 착각하지 마라.

---

### ▣ O175-B-3 ⚪ 이 세션의 소관이 아닌 것 — ㉡ 워크스페이스 백로그

🔴 **§4 결정·설계 대기 5건이 그대로 남아 있다**(8건 중 3건만 닫았다). 좌표는 직전 인수인계와 같다.

| # | 항목 | 좌표 |
|---|---|---|
| ④ | `D8` ⑩ 잔여 2축(이월 묶음) | `99_NEXT_SESSION_조각/99_NEXT_SESSION-023.md:17` |
| ⑤ | `D9` `O91-F` 잔여 2건 · ⚠️ 분모가 둘이다(처방 5 ↔ 승계 3) | 문서50 `-016.md:4` |
| ⑥ | `D11` BRD | 문서50 `-018.md:77` 부근(🔴 grep 으로 절 제목 먼저 확인) |
| ⑦ | 착수표 ⑭ `FME.SPONSORSHIP_SK` 잔여 다중사업 7.29% · 🔴 현업 회신 대기 | `-022.md:114` · 문서20 §N-13 |
| ⑧ | `SEARCH_CONSOLE_DATA2` 드롭 가능 여부 + GSC 두 테이블 COMMENT 오기 | 🔴 `DROP` 은 `R4-4-3` 별도 승인 |

⚠️ **`E-1` `FUNDRAISING_COST` 는 이 목록에 없다** — 사용자가 현업에 직접 확인 중이다.
회신이 오면 그때 분류 확정 + `FACT_BUDGET` 배선으로 내린다(원장 `O175` 행 · 문서20 §N-17).

🔴 **착수 전에 J3 를 적용하라** — 이번 세션에도 「열림」 기재가 실제와 달랐다
(`DEC-35` 열거 부적절 2건은 **면제를 걸 대상조차 아니었다**).

---

### ▣ O175-B-4 🔴 환경 함정 (이번에 실제로 밟은 것)

· `session_brief.py --write` 가 **기본 2분 타임아웃을 넘긴다** ⇒ `timeout_ms` 를 올려 단일 호출로 돌려라.
  🔴 `nohup ... &` 로 백그라운드에 넘기면 **셸 종료와 함께 죽고 0바이트 산출물**이 남는다(실제로 겪었다).
· `scripts/` 전체 `grep -rln` 은 **rc=143**(SIGTERM)이다 ⇒ `ls scripts/ | grep <패턴>` 으로
  **파일명부터** 좁혀라(`R0-8`).
· `cortex ws ls` 에는 **`--pattern` 인자가 없다** ⇒ 주면 사용법만 출력하고 rc=1 이다.
  형식은 `cortex ws ls 'DB.SC.WS:/경로/'` 다(`J1` 「0건 앞에서 인자를 보라」).
· `SNOWFLAKE.ACCOUNT_USAGE` 는 **`GN_DW_ADMIN` 역할에서 보이지 않는다** — 역할 전환 후 조회가 실패한다.
· 🔴 **골든 재발행에도 `export SESSION_LABEL` 이 필요하다** — 안 붙여서 발행 이력이 `UNLABELED` 로
  남았고 라벨을 붙여 재발행해야 했다(기지 함정인데 또 밟았다 · 4번째 계열).

---

### ▣ O175-B-5 📏 종료 기준선 (2026-09-21 · 계정 `RY53492` · 🔴 인용하지 말고 재라)

| 축 | 값 |
|---|---|
| dbt build | PASS **524** · WARN **37** · ERROR **0** · TOTAL **561**(기준선 동일) |
| 음성 테스트 | **41/41**(신설 `test_extract_sv_deploy` 15단정) |
| `gate_census --final` | rc=0 · 미분류 0 · 유령 0 · 중복 0 · 임시 계측기 **0** |
| `sv_code_label_gate` | blocking **0** · advisory **0** · 정보 **3** · 컬럼 배킹 차원 **246** |
| BRONZE 노출감사 | rows **1,254** · 노출됨 **137** · 미노출 **542** · 제외(적재제어메타) **50** |
| `doc_census` | stale **0** · 분할 분모 위반 **0** · 🟠 여유 부족 **2건**(원장 · 스킬 본문) |
| 광고비 정본 | `FACT_AD_PERFORMANCE.AD_COST` 합 **65,046,487,116**(DIGITAL 203,138 · REBRDC 2,149 · VIDEO 37,674) |
| `FACT_BUDGET` | **1,332**행 · `AD_COST` 채움 **0**(폐기 슬롯) · `WIDE_BUDGET` 컬럼 **20** |
| `ML_MEMBER_RISK_V` | **74,949**행 · `CPR_DIV_NM` 전건 · 평균 중단확률 **0.441761**(O118-B 회귀 일치) |
| `ML_SPONSOR_RISK_V` | **829,609**행 · `UPPER_CMPGN_NAME` 전건 · distinct **993**(코드 994) |
| `SILVER.CRM_MEMBER_DEV` | **3,654,929** = BRONZE 적격 3,654,929(유령행 **0**) |
| 조각 수 | 이력 **76** · 원장 **15**(🔴 문서에 적지 말고 `doc_census.py` 로 재라) |
| 남은 §4 결정대기 | **5건** |
