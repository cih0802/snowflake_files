## 🟢 [2026-09-08 O146 은퇴 이관] `07_O작업정리/` 진행 원장 폐기 — O143~O144-F 점검 계열 종결 기록

🔴 **원본 = `07_O작업정리/00_O작업_점검_계획서.md`(폐기됨 · 524줄).**
그 폴더는 O143 점검 세션의 **진행 원장**이었고 Phase 0~5 · 후속과제 후-1~후-7 이 전건 닫혀
**원장으로서의 역할이 끝났다** ⇒ O146 이 폴더를 폐기하고 아래로 이관했다.
· **재사용 절차 2종** → `60_repeat/10_NL스모크_재실행_절차.md` · `60_repeat/11_O누적작업_점검_재현_절차.md`
  (바이트 동일 복사 후 헤더 보강 · MD5 대조 일치)
· **닫힌 진행 원장** → 이 절
· **열린 잔여 4건** → `99_NEXT_SESSION` 신설 인수인계 절 + 착수표

🔴 **`R2-8-4-d`** — 이 절은 **닫힌 항목의 보존소**이고 **라이브 상태의 정본이 아니다.**
아래 라이브 수치는 **2026-09-08 조회 결과**이므로 「지금 그렇다」의 근거로 인용하지 마라.

### 좌표 대응표 (원본 → 이 문서)

| 원본 좌표 | 이관 위치 |
|---|---|
| `00_O작업_점검_계획서.md` §1 범위 실측 | 이 절 「왜 판정식 방식이었나」 |
| 같은 문서 §2 `D1`~`D9` | 이 절 「확정 결함 D1~D9」 |
| 같은 문서 §3 Phase 0~5 · §5 진행표 | 이 절 「Phase 진행표 최종 상태」 |
| 같은 문서 §5 후속과제 후-1~후-7 | 이 절 「후속과제 종결」 |
| 같은 문서 §5-B·§5-D 후-2 재판정 18절 | 이 절 「문서50 열린 절 18/18 재판정」 |
| 같은 문서 §5-C 미봉합 6→3→0 | 이 절 「decision_closure_gate 축E·축F」 |
| 같은 문서 §5-E O144-D·-E·-F | 이 절 「O144-D·-E·-F 등재」 |
| 같은 문서 자기적발 `S1`~`S12` | 이 절 「자기적발 S1~S12」 |
| 같은 문서 §6 독해기록 14건 · 라이브 `L1`~`L7` | 이 절 「근거 기록」 |
| `01_NL스모크_절차서.md` 전문 | `60_repeat/10_NL스모크_재실행_절차.md` |
| `02_O143_작업재현_및_인수인계_가이드.md` 전문 | `60_repeat/11_O누적작업_점검_재현_절차.md` |

### 왜 판정식 방식이었나 (§1 요지)

전 문서군 규모가 커서 이력 전량 독해가 **물리적으로 불가능**했다
(🔴 **당시 기재 수치는 이 자리에 옮기지 않는다** — 규모는 `python3 scripts/doc_census.py` 로 재라 · `R3-9 ㉦`).
⇒ 점검을 **「이력을 읽어 결함을 찾는 방식」이 아니라 「결함 검출 판정식을 세워 정본·도구를 훑는 방식」**으로 했다.

### 확정 결함 D1~D9 (전건 처분 완료)

| # | 결함 | 처분 |
|---|---|---|
| `D1` | 🔴🔴 `session_brief.py` 가 **승계된 인수인계 절을 현행으로 뽑았다** — 원인 2개(㉠ `'~~여기서 시작한다'` 리터럴 인접 의존 ㉡ 동일 날짜 tie-break 부재로 `max` 가 최초 등장을 고름) | 🟢 `struck_out()` 구간 판정 + 정렬 키 `(date, idx)` |
| `D2` | 🔴 `test_session_brief.py` 픽스처가 **전부 구관례**여서 D1 을 원리적으로 못 봤다 | 🟢 오염 기반 음성 축 2개 신설 — **수정 전 코드에서 9건 실패** 실증 |
| `D3` | 🟠 `99_NEXT_SESSION-028.md:17-18` 표가 **본문 행 없이 `\|` 한 글자로 절단**돼 있었다 · 🔴 **원형 복구는 영구 불가**(스냅샷 대조 실패 · 추정 복원 금지 `R2-8-3`) | 🟢 구조만 복구 · 🟢 검출 축 = `split_doc.py` **게이트5b**(후-3) · 🟢 영향도 낮음(이미 승계된 과거 절) |
| `D4` | 🟠 게이트 잔여 경고 2계열(용량 여유 부족 · 좌표 경고) | 🟠 **열림 승계** ⇒ `99_NEXT` O146 절 |
| `D5` | 🔴 `os.path.getsize`(stat) 가 스테이지 cold 파일에서 **16바이트 반올림 오탐**을 냈다(기재가 맞고 「실측」이 틀렸다) | 🟢 `doc_census`·`doc_type_gate` 를 **읽은 바이트**로 교체 + `test_doc_census` 축7 |
| `D6` | 🔴🔴 **착수표 열린 집합 ↔ 인수인계 목록 불일치** — `⑭`(P1 🔴🔴)·`㊳` 가 인수인계에서 조용히 사라졌다 | 🟢 `session_brief.handoff_gap` 신설 + 지침 **`R3-9 ㉨`** + 스킬 자문 ⑨ |
| `D7` | 🔴🔴 `timeout` 에 죽은 검색이 **「0건」으로 보인다**(`_archive/` 대용량 · rc=143 · 파이프가 에러까지 삼킴) | 🟢 지침 **`R0-8`**(+`R0-8-1`~`3`) 신설 · 스킬 Step 0 반영 |
| `D8` | 🔴 착수표 `⑭` 가 주장한 「COMMENT 사유 명시」가 **라이브·DDL 양쪽 다 없었다** · 게다가 DEV 축 한정 서술이 축을 안 밝혀 **STOP 센티넬을 배선 누락으로 오해하게** 만들었다 | 🟢 `06_DDL.sql` FME 이력 블록에 `· SPONSORSHIP_SK:` 항목 신설(DEV/STOP 분리 + 정지점 + 축 한정 명시) |
| `D9` | 🔴🔴 SERVING 표면(SV·Agent)이 **라이브에 없었다** — 계정이 2026-09-07 재구축돼 `SHOW AGENTS` 0행 · SV 1종뿐 ⇒ 착수표 `②` 는 **「사람 소관」이 아니라 「선행 배포 미완」**이었다 | 🟢 **O144 재배포**(SV 17종 · Agent 3종) + NL 스모크 5/5 ⇒ 절차서는 `60_repeat/10_…` |

🔴 **`D9` 의 일반화** = *"계정이 바뀌면 객체는 사라지지만 문서의 「완료」는 남는다"*(`R2-8-4`·`R3-9 ㉤`).
⇒ O134 의 「`AGENT_MEMBER` `VERSION$3` 11도구 일치」·O136 의 「28/28 PASS」는 **라이브 상태의 근거가 아니다**(`R2-8-4-d`).

### Phase 진행표 최종 상태 (§3·§5)

| Phase | 결과 |
|---|---|
| 0 착수 안전 | 🟢 라벨 선점 등재 · 🟢 스냅샷 경로 확인 · 🟠 **0-2 재균형은 의도 보류**(조각 경계 변경이 인용 좌표를 무효화하므로 O112 ㉢ 행 압축으로 대체) |
| 1 브리핑 신뢰성 | 🟢 4/4 — `00_BRIEF` §2 현행 절이 정정됐다 |
| 2 「닫혔다」 역검증 | 🟢 2-1·2-2·2-4 · 🟠 2-3(문서50 절별 재판정)은 당시 범위 초과 ⇒ **후-2 로 승계돼 O144·O144-C 가 18/18 완결** |
| 3 P1 잔여 점검 | 🔴 3-1 은 **원리적으로 에이전트 수행 불가**(브라우저) ⇒ 절차서 산출 · 🟢 3-2 `⑭` 가드 유지 라이브 실증 |
| 4 참고문서·프롬프트 보충 | 🟢 4/4 — 조문 신설 · 스킬 개정 · 프롬프트 템플릿 · 진행 원장 갱신 |
| 5 종료 처리 | 🟢 게이트 6종 + 음성 테스트 전건 · 정본 3곳 갱신 |

### 후속과제 종결 (후-1~후-7 · 전건 🟢)

| # | 무엇이 닫혔나 |
|---|---|
| `후-1` | SV 17종(실적 10 + ML 7) · Agent 3종(`VERSION$3` is_default=true · `GN_DW_ADMIN` · USAGE 3역할 · CoWork 등록) 재배포 |
| `후-2` | 문서50 열린 절 **18/18 재판정** — 닫음 9 · 열림 유지 9 · 부수로 SV 9종 stale 주석 11건 시정 |
| `후-3` | `split_doc.py` **게이트5b** 신설(파이프 한 글자 절단 표 검출) — D3 실물 2건 검출 실증 · 음성 6축 16단정 |
| `후-4` | 원장 §1 `O134`·`O136` 행에 조회 시점 · 재구축 사실 · 인용 금지 병기 |
| `후-5` | `decision_closure_gate` **축F**(세션 귀속 대괄호 제외) ⇒ 미봉합 **3 → 0** · 축13 9단정(재현율 축 포함) |
| `후-6` | O90 문서 시정 7건이 **전건 O91 기시정**임을 파일 좌표로 확인(절 기재가 stale) · 부수로 GOLD·SILVER 스키마 COMMENT stale 시정 |
| `후-7` | SV 5종에 **SV 별로 다른** 판정 라벨 문안 발행 + 라이브 배포 ⇒ O64-D 판정식 재실행 = SV DDL **0 → 2~3파일** |

🔴🔴 **후-7 이 실제 라이브 결함 1건을 적발했다** — `sv_unit_gate` 가 **`SV_AD` GRANT 누락 6건**을 냈다.
원인 = O142 가 `SV_AD` 를 `ACCOUNTADMIN` 으로 생성해 owner 가 17종 중 그 1종만 달랐고,
소유자가 아닌 `GN_DW_ADMIN` 세션의 GRANT 가 걸리지 않아 **소비 3역할이 `SV_AD` 를 읽을 수 없었다**
(= `AGENT_OVERALL`·`AGENT_MARKETING` 의 `analyst_ad` 도구가 그 역할에서 죽는 자리).
🟢 시정 = `GRANT OWNERSHIP … COPY CURRENT GRANTS` 이관 후 3역할 GRANT ⇒ 누락 0건.
🔴 **판정식 = 문안을 발행했으면 그 자리에서 게이트 회귀를 돌려라** — 문안만 넣고 끝냈다면 이 결함은 숨어 있었다.

### 문서50 열린 절 18/18 재판정 (§5-B 8절 + §5-D 10절)

**판정식**(3단계) = ㉠ 그 절의 「조치/잔여」 선언을 찾고 ㉡ 각 항목을 실측하고 ㉢ **표 밖에 남은 항목**을 따로 본다.
🔴 ㉢ 이 실제로 결과를 바꿨다 — `O88-C` 의 `W2`, `O68-B` 의 「SV 9종 주석」이 그 자리였고 후자가 stale 11건이었다.

| 판정 | 절 |
|---|---|
| 🟢 **닫음 9절** | `O90` · `O88-C` · `O71-B` · `O70-B` · `O69` · `O68-B` · `O63` · `O62` · `O91-H` |
| 🟠🟡🔴 **열림 유지 9절** | `O116`(=`DEC-45` 결정 대기) · `O64`→닫음 후 재확인 · `BLOCKING-1`(원천 입고) · `BLOCKING-2`(현업 회신) · `BLOCKING-5`(해소 주체 5축 분산) · `O59-P-1`(현업 회신 §M-6) · `O91-F`(🔴🔴→🟠 강등 · 잔여 2건은 결정·설계 대기) · `결정대기 GOLD`(잔여 4개 모델 작성은 설계 결정) · `순서9-C`(잔여 = 외부 입고 5 + 현업 회신 4) |

🔴 **열림 유지 9절의 공통 성격 = 외부 의존**(현업 회신 · 원천 입고 · 설계·사용자 결정).
🟢 ⇒ **우리가 닫을 수 있는 것은 그 세션에서 다 닫혔다**는 것이 후-2 의 최종 판정이다.

### `decision_closure_gate` 축E·축F — 미봉합 6 → 3 → 0

🔴🔴 **축E 시정이 3회 필요했고 앞의 두 번은 「코드는 있는데 효과가 0」이었다**(`R3-2` 의 실물 3연쇄).

| 회차 | 왜 안 들었나 |
|---|---|
| 1차 | 유형 등재표가 **약칭·계열 표기**를 쓰는데 `path.name in STATIC_DOCS` 로 **완전일치**만 봤다 ⇒ 6건 불변 |
| 2차 | 계열 와일드카드 전개 후 import 경유는 3건으로 줄었는데 **직접 실행은 6건 그대로**였다 |
| 3차 | 직접 실행 시 `sys.path[0]` 이 스크립트 디렉터리라 `import doc_type_gate` 가 `ModuleNotFoundError` 였고 **`except` 가 빈 집합을 조용히 반환**했다 ⇒ 절대경로 주입 + 실패 시 경고 출력 |

🔴 **판정식 2개** = ㉠ 「고쳤다」를 쓰기 전에 **도구 출력의 숫자가 실제로 움직였는지** 보라
㉡ **같은 도구를 두 방식(import 경유 · 직접 실행)으로 돌려 보라**(`R3-9 ㉡`).
🟢 잔여 3건은 후-5 축F 로 닫혔고 **무관한 행에 라벨을 병기해 억지로 닫지 않은 판단이 옳았음**이 확인됐다.

### O144-D · O144-E · O144-F 등재 (§5-E · S8 의 시정)

| 단위 | 무엇을 했나 · 실측 |
|---|---|
| **O144-D** | `30_output_share` 파생 무결성 — `11` 재생성(dbt 18→9 · 착수표 6→5) · `15` 갱신 · `16`/`17` 대조(닫힘 9절 중 7 유지 · 1 인용시정 · **1 닫힘취소**) |
| **O144-E** | 문서17 실증 쿼리 전량 실행 — 문장 **25 → 42**(보강 17) · 실패 **0** · 🔴 **기재 6건이 실측과 불일치**해 시정 |
| **O144-F** | 트랜스크립트 자기검토 — SQL 33 · 파일쓰기 225 · bash 210 감사 · 확정위반 **3계열**(S9~S11) · 음성 축 16→18 |

**O144-D 가 찾은 파생 결함 3유형** — ㉠ **양쪽 누락**(`15` 의 열린 절 4개가 16·17 어디에도 없어 조용히 사라져 있었다)
㉡ **닫힘 오판**(`16` §4-1 이 `L-2` 를 🟢 로 닫았으나 중단 축은 전건 0 센티넬이고 `⑭` 정지점이 살아 있다 ⇒ §4-2 **닫힘 취소**)
㉢ **창작 식별자**(`ACQUISITION_CAMPAIGN_SK`·`STOP_CAMPAIGN_SK`·`FACT_BUDGET_DEPARTMENT.DEPT_CD` **3개가 라이브에 없었다**).
🔴 **㉢ 이 가장 위험하다** — 「🟢 배선 완료」 표기가 붙으면 **아무도 다시 조회하지 않는다.**

**O144-E 가 찾은 「기재 ↔ 실측」 불일치 6건** — 🔴 2건은 현업에 잘못 보낼 뻔했다.

| 절 | 종전 기재 | 실측 | 위험 |
|---|---|---|---|
| `I-2` | `S+8자리` 미매칭 1,016건 | 1,631행/1,016종 · **997종이 일시회원 마스터에 정상 매칭** | 🔴🔴 삭제 승인 요청 시 **정상 회원 기록 삭제** |
| `A` | `LPAD` 보정 승인 | `LPAD('20251',6,'0')` = **`020251`**(0202년) · 올바른 식 `→202501` | 🔴 **잘못된 식 승인** |
| `M-5` | 미등재 17종 | **5종**(505,541행) · 사전은 146코드 보유 | 실재하지 않는 규모 요청 |
| `B,E` | 캠페인행사 전건 100% 미매칭 | 고아 **52행(0.03%)** · 마스터 3,410행 실재 | 해소된 문제를 미해결로 요청 |
| `F-2` | 후원사업 4그룹 | 약어코드 **6종** | 전제 자체가 원천과 불일치 |
| `F-1` | 혼재 `LVL=2~4` | **레벨 2~5** · `STATS_DEPT_LVL` 결측 **99.4%** | 범위 과소 |

🟢 **보강 쿼리가 새 근거를 만들었다** — 채널별 D+5 누적(이메일 **64.8%** vs 알림톡 **77.3%**) ·
동시중단 팬아웃(2개 이상 **4.39% / 51,859 사건**) · 8자리 초수 = **마이크로초**(`60000000÷10⁶=60초`) ·
`N-9` **동결 미실효**(동결명 ≠ 현재명 = **0/1,585,949**).

**O144-F 가 정정한 귀속 오류 1건** — O144-E 추록은 「`Grep` 도구가 거짓 음성을 냈다」고만 적었다.
그 주장 자체는 참이다(서로 다른 문자열로 **2회 재현** · 규약대로 돌린 `bash grep` 은 rc=0·9건 적중인데 도구는 「No matches」).
🔴 **그러나 같은 조사에서 쓴 `bash grep` 도 신뢰할 수 없었다** — 분모에 `_archive/` 를 넣고 **rc 를 파이프 뒤에서** 읽었다
⇒ `R0-8-1`·`R0-8-2` **동시 위반**이었다. 🟢 **정정된 판정식** = 「없다」를 결론으로 쓰려면
㉠ **분모를 좁히고** ㉡ **rc 를 리다이렉트로 받고** ㉢ **두 번째 도구로 대조**한다 — **셋 모두**.

**🔴 동시 세션 경합 (이 워크스페이스 최초 실측)** — 세션 2개가 동시에 활동했고(`87365140` · `87365144`=O145),
O145-B 가 남의 일회성 프로브 6종을 `gate_census` `OBSERVE` 에 등재해 미분류 8→0 을 달성했는데
원작자가 관례대로 `_archive/` 로 내리자 **유령 등재 6건**으로 유형만 바뀌었다.
🟢 처분 = 등재 철회 + 경위 주석 ⇒ 미분류 0 · 유령 0.
🔴🔴 **판정식 = 일회성(`_oNNN*` 접두)은 등재가 아니라 보관이 정답이다** — 남의 세션 산출물이 미분류로 보이면
**등재하지 말고 그 세션에 정리를 넘긴다.**

### 자기적발 S1~S12 (그 계열이 스스로 만든 결함)

| # | 결함 | 처분 |
|---|---|---|
| `S1` | 표에 이스케이프 안 된 파이프를 넣어 **열수를 깨뜨렸다**(D3 동일 계열) | 🟢 문구로 대체 |
| `S2` | 원장 행이 커져 조각이 상한(40,960 B)을 넘겼다 | 🟢 재균형(사전 승인) 후 상세 행 복원 |
| `S3` | 조사 스크립트를 `gate_census` 분모에 등재하지 않았다 | 🟢 일회성이므로 `_archive/` 이관 ⇒ 미분류 0 |
| `S4` | `grep -rlc` 로 세어 「14파일」이 나왔다 — **`-c` 가 `-l` 을 덮어 스캔한 파일 수**를 셌다(실제 1~2) | 🟢 `-l` 단독 + rc 확인 ⇒ 🔴 **플래그를 겹쳐 쓰지 마라** |
| `S5` | 축E 시정 3회 중 앞 두 번이 효과 0 인데 주석에 「고쳤다」가 적혀 있었다 | 🟢 상세 = 위 축E 절 |
| `S6` | 게이트5b 신설 직후 `--verify` FAIL — **도구가 아니라 문서(D3 실물)가 원인** | 🟢 게이트를 되돌리지 않고 문서를 고쳤다(구조만 · 추정 복원 금지) |
| `S7` | 🟠 `gate_census --run-tests` 순회에서 `test_retire_sections` **rc=1** | 🟠 **재현 실패**(3회 재실행 전건 rc=0 · 직접 실행도 rc=0) ⇒ 「고쳤다」로 적지 않고 **「재현 실패 · 원인 미규명」**으로 남겼다 ⇒ **열림 승계** = `99_NEXT` O146 절 |
| `S8` | 🔴 이 진행 원장이 **O144-C 시점에서 멈춰 있었다** — O144-D·-E 가 어디에도 없었다 | 🟢 §5-E 신설 ⇒ 🔴 **판정식 = 진행 원장은 세션마다 갱신해야 원장이다.** 갱신하지 않으면 다음 세션이 **이미 한 일을 다시 계획**한다 |
| `S9` | 🔴 `R0-8-2` **확정 위반 3건** — `grep … \| head; echo rc=$?` 로 **`head` 의 rc** 를 읽었다 | 🔴 **1건은 실해**(0건·rc=0 으로 보여 「어느 파일에도 없다」로 단정 · 실제로는 `gate_census.py` 에 있었다) ⇒ 귀속 오류까지 정정 |
| `S10` | 🔴 `R1-7-9` **quoted heredoc 7건** — `python3 - <<'PYEOF'` 로 yml·SV DDL 본문을 편집했다 | 🟠 실해 0(문안 전건 실재 · `line_len` PASS)이나 **조문은 경로 자체를 금지**한다 ⇒ 확정위반으로 계수 |
| `S11` | 🔴 `R1-7-7` **`rm -rf` 1건** — `os.listdir` 선나열 없이 `scripts/__pycache__` 삭제 | 🟠 실해 0(파이썬 캐시)이나 조문은 개별 삭제를 요구한다 ⇒ 확정위반으로 계수 |
| `S12` | 🔴 감사 도구 픽스처에 quoted heredoc·파이프뒤rc 가 **아예 없어 두 판정식 결함이 영구히 숨었다** | 🟢 축17·축18 신설(재현율·정밀도 동시 단정) ⇒ 🔴 **이것이 `D2` 의 재발**이다 — 픽스처가 실사용 형태를 담지 않으면 게이트는 조용히 분모를 잃는다 |

🟢 **`S3` 은 판정식이 실제로 작동한 사례다** — 파이프 뒤 `$?` 를 쓰지 않고 rc 를 리다이렉트로 받았기 때문에
**1건 실패가 보였다**(`R0-8-2`). `| tail -1` 이었다면 「전건 통과」로 보고했을 것이다.

### 근거 기록 (§6 · `R1-3-7-b`·`R2-8-4-a`)

**독해 기록 14건 · `read` 미반환 = 0 · 재호출 = 0**(판정식 `재호출 ≥ 미반환` 충족).
독해 대상 = 지침 전량 · `00_BRIEF` 전량 · `99_NEXT-028` 전량 · `session_brief.py` · `test_session_brief.py` 전량 ·
`99_NEXT-022`·`-023` · 원장 `-002` · `doc_census.py` · `doc_type_gate.py` · `test_doc_census.py` ·
`03_top-down_gold/06_DDL.sql` · `AGENT_MEMBER/agent_spec.yaml` · `init_ihcho/SKILL.md`.

**라이브 조회** — 계정 `hy04212` / 역할 `ACCOUNTADMIN` / **2026-09-08**.
🔴 계정명은 **맥락이고 판정 근거는 조회 결과다**(`R3-9 ㉤`) · 🔴 **아래를 현재 상태로 인용하지 마라**(`R2-8-4-d`).

| # | 조회 | 그 시점 결과 |
|---|---|---|
| `L1` | `FACT_MEMBER_EVENT` 전체 · SK=0 · NULL | 4,633,105 / 1,038,262 / 0 |
| `L2` | `EVENT_TYPE` 별 SK=0 대조 | STOP 1,038,262 **전건 0** · DEV 3,594,843 **전건 비영** |
| `L3` | `SHOW COLUMNS … SPONSORSHIP_SK` | COMMENT = `후원사업 (FK→DIM_SPONSORSHIP)` — **사유 없음** |
| `L4` | `SHOW AGENTS` · `… IN ACCOUNT` | **0행**(rc 정상) ⇒ D9 |
| `L5` | `SHOW SEMANTIC VIEWS IN DATABASE GN_DW` | **1종 `SV_AD`**(생성 2026-09-07 01:46) |
| `L6` | `SHOW SCHEMAS IN DATABASE GN_DW` | 15종 · **전 스키마 created_on = 2026-09-07 00:23~00:25** ⇒ 재구축 실증 |
| `L7` | `INFORMATION_SCHEMA.TABLES` 집계 | GOLD 37 표 + 14 뷰 · SILVER 43 · ML 16 · OPS 4 |

**인용 원문**(요약 금지 · `R2-8-2`) — `session_brief.py:246` 의 종전 배제 조건은
`if '여기서 시작한다' not in t or '~~여기서 시작한다' in t:` 였고 `:254` 는 `cur = max(cands, key=lambda c: c['date'])` 였다.
그리고 `99_NEXT_SESSION-028.md:19` 의 승계 절 선두가 대괄호 전체를 감싸는 취소선이어서
**`~~여기서 시작한다` 라는 연속 부분문자열이 존재하지 않았다** — 그것이 D1 의 원인 1 이다.
