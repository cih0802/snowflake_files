> #### 🟢 [2026-09-16 O166] 산출물 04·05·09 복구 + 개명 잔여 **분모 결함**(`E1`~`E6`) 적발·시정
>
> 🔴🔴 **수치·판정식 정본 = `20_issue/_o166_evidence.md`**(이 항목은 서술이고 그 문서가 근거다).
> 승계 = §0-LLLL(O165) · 지시 = ㉠ 원천 실데이터 측정 ㉡ build 후 산출물 6종 재생성(순서 고정)
> ㉢ `test_generators` 골든 갱신. `R4-4-2 ㉡`(지시 동봉) 적용 · **확정위반 0건**.
>
> **▣ 1. 라이브 실측 — 사용자 dbt 실행 결과를 재고 있음을 실증했다**
>
> 사용자가 `dbt run`(PASS 98) · `dbt test`(PASS 425 · WARN 37 · ERROR 0)을 직접 실행했다(`R4-1` 준수 ·
> 이 세션의 dbt 명령 실행 **0건**). 🟢 측정 시점의 정당성을 **시각으로 실증**했다 =
> `GOLD.LAST_ALTERED` 최대 **19:34:31(-0700)** · `SILVER` 19:32:13 · 조회 20:05:05.
> 결과 = BRONZE **60테이블 123,850,093행** · SILVER **48 · 132,813,020** · GOLD **37 · 149,106,156** ·
> ML 16 · OPS 4 ⇒ **O165 §▣5 와 전건 동일**. 🟢 사용자 관측 *"이전과 변화는 없는걸로 보인다"* 를
> 실측으로 확증했다. 「bronze 일부 입고」의 실체는 **2026-09-15 00:58~00:59 적재분**이고
> 그 이후 신규 적재는 없다(BRONZE `LAST_ALTERED` 최대 = 09-15 00:59:22).
>
> 🔴 **`D8` 재측정 = 미판정 유지**(`R2-8-4-c`) — BRD **10,337,767행**(O163 기재 11,600,680 대비
> **−1,262,913** 불변) · `LAST_ALTERED` **09-15 01:00:17** ⇒ **그 뒤 외부 적재가 없다**.
> 차이는 줄지도 늘지도 않았고 가릴 수 있는 것은 여전히 **외부 적재기 이력**(현업 소관)뿐이다.
> 🟢 보호 장치(DDL `:1283` 커밋아웃) 생존 재확인 ⇒ **추가 소실 위험은 없다**.
>
> **▣ 2. 산출물 복구 — 그리고 「1차 재생성에서 닫지 않은 것」이 이 세션의 핵심이다**
>
> §1-2 가 고정한 순서를 그대로 지켰다(`dump_schema` → `census_columns` → 생성기 4종 →
> `test_generators`). 🔴 **build 전 재생성 금지**를 지켰다(O163 이 그 순서를 어겨 04·05·09 를 덮었다).
> 1차 재생성 결과 = `test_generators` **rc=1 · 차이 14건 → 6건** · `04.rows` **369 → 563** 으로
> **골든과 정확히 일치**. ⇒ 🟢 **골든 563 이 옳고 O164 산출물 369 가 stale** 이었다
> (O165 의 *"골든을 갱신하지 마라 — 기준선이다"* 판단이 맞았다).
>
> 🔴🔴 **여기서 닫을 수 있었고, 닫았다면 오판이 골든에 굳었다.** 남은 6건의 **부호와 귀속**을
> 파고들자 `09` 산출물의 **타원천 18건이 전건 오판**임이 드러났다 ⇒ `E1`~`E6`.
> 🟢 **새 판정식 = 「골든과 붙는 것」과 「옳아지는 것」은 다르다.** 차이가 줄었다는 사실은
> 방향의 증거가 아니다 — **각 건의 귀속을 대야 한다.**
>
> **▣ 3. `E1` 분모 결함 — `rename_stale_gate.SCAN_DIRS` 에 `scripts/` 가 없었다**
>
> 실측 = `SCAN_DIRS` 11개에 `scripts` 부재 ⇒ 분모를 손으로 넓혀 재니 **개명 잔여 343건 / 51파일**이
> **검사 밖**이었다(`gen_section_assembly.py` 38 · `gen_o53_gold_ddl.py` 26 · `outputs.json` 77 …).
> 🔴 **`R1-6-18 ④`·O165 `D11` 과 같은 형태의 재발**이다 — 게이트가 없어서가 아니라 **분모가 좁아서**
> 조용했다. 🔴 **연쇄 정정** = O165 `D5` 의 *"축1 잔여는 **전건 문서·주석 표기**"* 는
> **`scripts/` 를 제외한 분모에서의 판정**이었다 ⇒ 결론(기준선 계약)은 유지되지만 **근거 문장은 정정 대상**.
>
> **▣ 4. `E2` 실해 — 타원천 18건이 전건 오판이었다(`?` 를 「다르다」로 읽었다)**
>
> 근거 문안 전수 파싱 = **18/18 이 `=?`(원천 미상) 경유**다.
> 원인 = `SRC_SYS` 키가 O163 개명 전 이름이라 `.get()` 이 `?` 를 돌렸고 판정기가 「원천이 다르다」로 읽었다.
> 🔴 **원천이 다른 것이 아니라 원천을 모르는 것이었다**(`O111 ㉠` 의 등록부 판본).
> 🔴 **그 파일 주석이 이미 두 번 경고했다** — O45 *"미등재 시 `?` 를 돌려 타원천 오판(실측 10행)"* ·
> O56 *"**GOLD 에 팩트가 추가되면 이 등록부를 함께 갱신한다**"* ⇒ **경고를 적어 둔 것으로는 멈추지 않는다**(3회차).
> 정정 등록부 6종 = `SRC_SYS` · `IDENTITY_BRIDGE` · `BRONZE_EVIDENCE` · `FACT_PAIR_EVIDENCE` ·
> `ABBREV` · `SCD2_DIM`.
>
> **▣ 5. `E3` — 개명은 이름만 바꾸지 않는다, grain 을 옮긴다**
>
> `SCD2_DIM` 이 **양방향으로 틀려** 있었다(오탐 + 미탐 동시). 실측 =
> `DIM_MEMBER` **1,785,299행 · `MEMBER_DK` 유일 1,785,299 · `MEMBER_SK` 유일 1,785,299 = 1행/회원**
> (팬아웃 0) · SCD2 는 **`DIM_MEMBER_STATUS_HISTORY`** 로 분리됐다(**8,069,279 / 회원당 4.52행**) ·
> `IS_CURRENT` 컬럼과 `DIM_MEMBER_CURRENT` 객체는 **라이브·dbt 모델 모두 부재**(dim 20종 전수 확인).
> ⇒ 🟢 **새 판정식** = 개명 대응표로 기계 치환하면 **이름은 맞고 의미는 틀린** 문안이 남는다.
> 🔴 그래서 `DIM_MEMBER_CURRENT` 를 **자동 치환에서 제외**했다(`MANUAL_ONLY`) — 치환하면
> ㉠ `gen_column_inventory` 의 기존 `"DIM_MEMBER"` 키와 **겹쳐 조용히 덮어쓰고**
> ㉡ 처방이 *"`DIM_MEMBER` 는 위험하니 `DIM_MEMBER` 를 쓰라"* 는 **자기모순**이 된다.
>
> **▣ 6. `E4` — 폴백이 `or` 때문에 항상 참이었다(잠복 결함)**
>
> `table_ddl_column_gate.resolve_ref()` 의 조건이 `(A and B) or A` 형태여서 **B 를 무력화**하고
> 라이브 부재 객체 `GOLD.FACT_MEMBER_LIFECYCLE` 을 반환했다. 현재 계정에서는 앞 분기가 먼저 잡아
> 발현하지 않았다(잠복) ⇒ 그 팩트가 없는 계정에서 실행되면 **존재하지 않는 객체를 조회한다.**
> 처방 = 폴백 제거 · 해석 실패를 `sys.exit` 로 **드러낸다**(폴백으로 감추지 않는다).
>
> **▣ 7. `E5` 차이 20건 전량 규명 후 골든 발행 — rc=1 → rc=0**
>
> 총 **507행 보존 · 순증 0**(+34 = −25 −13 −1 −1 +2 +4 · 부호 합 0 ⇒ 행 유실·창작 0).
> 귀속 = ㉠ **타원천 28→3(−25)** = `SRC_SYS` 정정으로 `?` 소멸 · 🟢 잔여 3건은 **실제 원천 상이**
> (`FACT_EVENT_ATTENDANCE`=CRM ↔ `FACT_BIGQUERY_BEHAVIOR`=**GA4**)로 정당
> ㉡ **도달불가 32→19(−13)** = `IDENTITY_BRIDGE` 키가 죽어 브리지 경유 도달이 **평가되지 않았다**
> (그 주석이 스스로 *"17건 중 7건의 원인"* 이라 적고 있었고 3-7 좌측이 **도달불가 7→0 · 조립가능 6→13**)
> ㉢ **grain부정합 +4 · 배분규칙필요 +2 는 악화가 아니라 재분류** — 「원천 미상」에 가려져 있던
> **진짜 원인**(시간축 일↔월 · 엔티티축 `MEMBER_DK`↔`ORG_SK`)이 드러났다
> ㉣ **앵커 변경 3건 · `tie_rows` 53→34** = 원천 상이 배제가 풀려 동수 경합이 정상 해소됐다
> ㉤ 측정일 09-14→09-16 및 스키마 지문 변경은 재생성·개편 반영의 정상 결과.
> ⇒ `--update-golden --reason "<귀속 전문>"` 발행 후 재실행 = 🟢 **rc=0 · PASS 21 · FAIL 0**
> (승계 목록의 「잔여 FAIL 1종」이 **해소**됐다).
>
> **▣ 8. `E6` 게이트 분모 확장 · 기준선 하향 · 음성 3축**
>
> `SCAN_DIRS` 11→**12**(스캔 501→**645**파일) · 축1 추이 **529(편입 전) → 592(편입 직후) →
> 563(자기참조·픽스처 제외 + 병기 마커 5종) → 531(코드 정정 후)** ⇒ 기준선 533 → **531 재발행**
> (`--reason` 동봉) 🟢 **기준선을 내려 진척을 증명**했다(증가 차단이 아니라 해소).
> 정정 규모 = 활성 생성기 6종 + 게이트·검증기 7종 = **13파일 · 자동 88건 + 수동 7건**.
> `scripts/` 잔여 **5건**은 전부 과거 실측 경위 서사이고 실행 의미가 없다.
> 신설 음성 축(`test_rename_stale_gate.py` 축9·10·11 · 전 **11축 통과**) =
> 축9 분모 편입 · 축10 **오염 기반**(옛 이름을 심어 검출 단정) · 축11 **역방향 오탐**(대응표·픽스처 제외).
> 🟢 **「고치기 전 실패」 실증**(절차서 §5 규약 #1 · O165 `D4` 처방) = 시정 전 `(F,F,F)` ↔
> 시정 후 `(T,T,T)` ⇒ **뒤집힘 2건 이상**.
>
> **▣ 9. 자기시정 2회**
>
> 🔴 ㉠ 축10 초판이 프로브 파일명을 `_o166_probe_planted.py` 로 지어 **`EVIDENCE` 정규식
> (`/_o\d+[a-z]*_`)에 걸려 근거철로 오분류**됐고 **검출 0건**을 냈다 ⇒ 🟢 **새 판정식** =
> **오염 기반 테스트는 「심은 것이 분모에 들어가는 이름인지」를 먼저 확인하라**(제외 규칙에 걸리면
> 그 축은 조용히 무력해진다 · `O111 ㉠` 의 픽스처 판본). 파일명을 `zzprobe_planted_gate.py` 로 교체.
> 🔴 ㉡ `gate_census` 가 신규 스크립트 `_o166_rename_fix` 를 **미분류 FAIL** 로 막았다 —
> 🟢 게이트가 제 일을 했다 ⇒ `MUTATES` 에 축과 함께 등재(다중 파일 재작성 · `R4-4-3`).
>
> **▣ 10. 검증 · 남긴 것**
>
> 게이트 = `doc_census` ✅ · `doc_type_gate` 🟡(경고) · `clause_order_gate` ✅ · `index_row_gate` ✅ ·
> `doc_heading_gate` ✅ · `doc_coord_gate` 🟡(경고) · `gate_census` 🟢 · `rename_stale_gate` 🟡(증가 0) ·
> 음성 테스트 **전건 rc=0**(파이프 없이 · `R0-8-2`) · `line_len.py` 🟢 PASS · `read` 미반환 **0건**.
> ⚠️ 원장 조각-001 이 편집으로 40KB 상한을 넘어 **`--rebalance --fill 0.7`** 을 수행했다
> (사전 승인 범위 · 스냅샷 선행 · 게이트 6축 🟢 PASS · 본문 바이트 동일 · 유실 0).
> 🔴 **남긴 것** = `D8` 외부 적재 이력(현업) · 원천 18번 재공유 · CSV 재언로드 · 문서20 §M·§N-13 ·
> 착수표 ㉗ NL 스모크(사람) · **O164 원장·이력 미등재** · O165 `D11` 후속(`doc_census` 미분할 분모 축) ·
> 개명 축1 **531건** · `scripts/` 잔여 5건 · **세션 날짜 미래 기재**(O163 = 2026-09-17 > 오늘).
> 🔴 **「전부 고쳤다」로 닫지 않는다.**
