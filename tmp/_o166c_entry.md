> #### 🟢 [2026-09-16 O166-C] 남긴 작업 집행 — `D11` 후속(축6-B) · 라벨 축7 신설 · **O164 소급 등재**
>
> 🔴🔴 **정본 = `20_issue/_o166_evidence.md`**(§8 자기검토) + `_o164_inspection_evidence.md`(O164 근거철 전량 독해).
> 지시 = 「남긴 작업에 대해 승인 처리하고 이전 결함과 새 결함을 보완」.
> 🟢 **에이전트 소관 4건을 전건 집행**하고, 현업·사람 소관 5건은 **왜 못 하는지**를 명시해 남겼다.
>
> **▣ 1. 🟢 O165 `D11` 후속 처방 집행 — `doc_census` 축㉣(축6-B) 신설**
>
> O165 가 *"`doc_census` 의 「분할 분모 대조」는 **분할 문서만** 본다 ⇒ 「`doc_heading_gate.DOCS` 에만
> 있고 다른 3곳에 없는 **미분할 문서**」 축을 추가하라 · 🔴 이번엔 게이트가 아니라 **사람이 `grep` 으로**
> 잡았다"* 고 남긴 항목이다.
> 🟢 **집행** = `split_denominator_check()` 에 **축㉣** 을 신설했다 — 기준 분모 = `doc_heading_gate.DOCS`
> 중 분할되지 않은 것, 대조 = `doc_type_gate` 유효 분모 · `doc_line_length_gate.CANON` · `doc_census.SINGLES`.
> 🟢 **음성 테스트 축6-B** 신설(오염 기반 3축 = 제목 축만 편입 검출 · `20_issue` 오탐 0 · 폴더 밖 누락 검출)
> ⇒ `test_doc_census.py` **41개 단정 전건 통과**.
> 🟢 **「고치기 전 실패」 실증** = 축㉣ 이전 판본(`FAMILIES` 순회만)으로 되돌려 재판정 ⇒
> **시정 전 검출 False ↔ 시정 후 True · 뒤집힘 1건**(절차서 §5 규약 #1 · O165 `D4` 처방 준수).
>
> 🔴 **자기시정 1 — 초판이 오탐 6건을 냈다.** 「등재 목록」과 「분모」를 혼동했다:
> `doc_type_gate.EXTRA_DOCS` 멤버십만 봐서 `20_issue/` 문서 6건(`00_BRIEF`·`31_코드군`·`32_컬럼개명표`·
> `40_입고대기`·`91_사고사례집`·`92_실측필요`)을 위반으로 냈는데, 그 게이트는 `DOC_DIR = 20_issue/` 를
> **순회**하므로 이미 분모 안이고 `EXTRA_DOCS` 는 **폴더 밖 전용 보충 목록**이다.
> ⇒ 🟢 **정정 판정식 = 「그 도구의 유효 분모」로 대조한다**(`20_issue/` 순회 ∪ `EXTRA_DOCS`).
> 🔴 **이것이 O165 `D5`(「처방을 문면대로 구현하면 오탐 100%」)와 같은 형태의 재발**이다 —
> `R1-6-18 ④` 가 「4곳」을 **목록으로** 적었기 때문에 목록 멤버십으로 구현하게 된다.
> 🟢 그 오탐 자체를 **회귀 축으로 고정**했다(축6-B ㉡).
>
> **▣ 2. 🟢 O166-B 신규 결함 처방 — `doc_coord_gate` 축7(인수인계 절 라벨 재사용)**
>
> O166-B 가 관측으로 남긴 「라벨 재사용」을 **기계 판정으로 바꿨다.**
> 실측 = 절 라벨 **43개 · 고유 39** ⇒ `0-AAAA`·`0-BBBB`·`0-EEEE`·`0-FFFF` **각 2회**
> (2026-08-30·31·09-01 판 ↔ 09-10·11·14 판) ⇒ 게이트가 **4건을 좌표·날짜와 함께** 열거한다.
> 🔴 **경고 축이다(blocking 아니다)** — 기존 라벨을 바꾸면 **이미 발행된 인용 좌표가 전부 깨진다**
> (`R1-7-4` 「행 키를 보존」과 같은 취지) ⇒ 처방은 **앞으로 병기**(`0-MMMM/O166`)이고 소급 개명이 아니다.
> 🟢 **음성 테스트 축13** 신설(합성 조각 4단정 = 중복만 검출 · 좌표·날짜 동반 · **취소선 승계 절도 센다** ·
> 본문 언급·`###` 하위 제목 오탐 0) ⇒ `test_doc_coord_gate.py` **36개 단정 전건 통과**.
>
> **▣ 3. 🟢 O164 세션 소급 등재 — 결번 해소**
>
> O165 가 *"O164 가 원장 §1·이력에 **0건**이고, 그 세션이 써야 할 것을 O165 가 대신 쓰지 않는다"* 고
> 남긴 항목이다. 사용자 승인으로 **근거철 254줄을 전량 독해**해 원장 §1 행을 등재했다
> (🔴 **창작 0** — `D1`~`D6` 과 실측값을 근거철에서만 옮겼고, 행 머리에 **「O166-C 소급 등재」를 명시**했다).
> 등재한 것 = `D1` SV view-level COMMENT 가 라이브 부재 객체 지시(AI 컨텍스트만 틀렸다 · O155 `D3` 재발) ·
> `D2` 직전 세션이 게이트 FAIL 2건을 남기고 닫음 · `D3` 세션 날짜 미래 · `D4` 기준선이 라이브 결함을 흡수
> (🔴 **O165 `D5` 가 그 처방을 정정한 사실을 병기**했다) · `D5` `DEC-50` 분모 누락 124건 ·
> `D6` 자기시정 4회 + **등급 배분도 실측으로 정하라**.
> 🟢 O164 수치는 O165·O166 이 재측정 확증했으므로 **인용 가능**임을 함께 적었다.
>
> **▣ 4. 🔴 집행하지 않은 것과 그 이유 (에이전트 소관 아님)**
>
> * 🔴🔴 **`D8` BRD −1,262,913 외부 적재 이력** — 외부 적재기 로그가 워크스페이스·라이브 어디에도 없다.
>   O166 이 `LAST_ALTERED` 로 「09-15 01:00 이후 변동 없음」까지 좁혔고 **그 이상은 현업 조회**뿐이다.
> * 🔴🔴 **원천 18번 재공유** · 🔴 **CSV 재언로드** · 🟠 **문서20 §M-1~M-6·§N-13** — 전부 **회신 대기**.
> * 🔴🔴 **착수표 ㉗ NL 스모크 30문항** — CoWork UI 수동 전용(트라이얼은 `DATA_AGENT_RUN` 차단).
> * 🔴 **`06` 「노출됨 −1」 행 귀속** — 이전 판본 사본이 없어 **원리적으로 불가**하다.
>   🟢 O166-B 가 러너에 스냅샷을 넣었으므로 **다음 갱신부터 가능**하다(그때 닫으라고 남겼다).
> * 🟠 **개명 축1 잔여** — `scripts/` 잔여 5건은 **과거 실측 서사**(실행 의미 0)이고, 나머지는
>   문서 표기다. 🔴 **기준선을 또 내리는 것보다 `D11`·라벨 축 신설이 재발 방지 가치가 크다**고 판단해
>   이번 세션은 게이트 쪽에 시간을 썼다(판단 근거를 남긴다 — 다음 세션이 뒤집을 수 있다).
> * 🟠 **O163 세션 날짜 미래 기재**(2026-09-17) — 🔴 **소급 정정하지 않았다.** O164 `D3` 이
>   *"대안 가설 = 환경 날짜가 틀렸을 수 있다 · 판정은 사용자에게 넘긴다"* 로 두었고, 실제 O163 세션의
>   수행 날짜를 **확증할 수단이 없다**(이력·원장 둘 다 같은 값을 적는다) ⇒ **날짜를 창작하지 않는다**(`R2-3`).
>   🟢 대신 O164 행 ③ 에 그 관측과 **위험(브리핑 현행 절 판정 선점)** 을 등재해 노출을 유지했다.
>
> **▣ 5. 검증**
>
> 게이트 = `doc_census`(**축㉣ 포함 위반 0**) · `doc_type_gate` · `clause_order_gate` · `index_row_gate` ·
> `doc_heading_gate` · `doc_coord_gate`(**축7 = 4건 경고**) · `gate_census` **전건 rc=0** ·
> `rename_stale_gate` 🟡(축1 530 · 증감 −1) · 음성 테스트 **전건 rc=0** · `test_generators` **rc=0** ·
> `line_len.py` 🟢 PASS(원장 O164 행 **1,847자** · O166 행 **1,999자**) · `read` 미반환 **0**.
> 🟢 **확정위반 0건**(이 절에서는 제목 삼킴·상한 초과를 **쓰기 전에** 잡았다).
> 🔴 **남는 것** = `D8`·원천18·CSV·문서20 §M·§N-13·㉗(전부 사람 소관) · `06` −1 미귀속(다음 갱신) ·
> 개명 축1 잔여 · O163 날짜(사용자 판단) · 라벨 병기 관례 도입(앞으로).
