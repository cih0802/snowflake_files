## 0-AAAC/O172-D. 🔴🔴 [2026-09-17 O172-D — **스킬 자체 갱신 누락을 사용자가 적발** · A~D 함께 읽어라]

> 🔴 사용자 질의 = *"스킬 개선은..스킬 자체를 바꿔야하나?"* ⇒ **그 질문이 결함을 적발했다.**

### ▣ O172-D-1 🔴🔴 무엇을 빠뜨렸나 — 「정본을 고쳤다」가 「스킬을 고쳤다」가 아니다

· 나는 O172~O172-C 에서 **지침·규약·도구·게이트·테스트**를 고쳤지만
  **스킬 산출물 3파일**(`03_init_ihcho_스킬_본문` · `_참조_세션종료` · `SKILL.md`·`references/`)은
  **손대지 않았다.** 그 상태로 다음 세션이 스킬을 로드하면 **폐기된 6단계 절차**를 그대로 읽는다:
  *"「여기서 시작한다」를 새로 쓰고 이전 절의 그 문구를 `~~취소선~~` 으로 승계 표시"* ·
  *"그 취소선 편집은 `doc_heading_gate` 에 제목 유실로 잡힌다 — `--update-golden` 으로 발행"* ·
  *"새 절을 쓰면 조각이 상한에 걸릴 수 있다 ⇒ 압축 또는 재균형"*.
· 🔴 **더 나쁜 것** = 나는 **최초 리스트업에 이 항목을 스스로 적어 놓고**(A4·A5·A6 = 스킬 참조·본문·정본)
  1단계 범위를 `A1·A3 + B1·B2·B5·B7 + 생성기`로 좁히면서 **그 셋을 빼놓고 「1단계 완료」라고 보고했다.**
· 🟢🟢 **판정식 = 「정본을 고쳤다」와 「그 정본을 읽는 산출물이 바뀌었다」는 다른 사건이다.**
  🔎 이 워크스페이스가 4회 당한 「자동 생성물을 손으로 고쳤다」의 **역방향**이다 —
  이번에는 **정본만 고치고 산출물을 재생성하지 않았다**(빌더가 있는데 돌리지 않았다).
· 🟢🟢 **판정식 = 내가 만든 리스트업이 곧 분모다** — 범위를 좁힐 때는 **뺀 항목을 명시적으로
  「2단계」로 옮겨 적어라**. 조용히 빠뜨리면 **아무 게이트도 그것을 신고하지 않는다**
  (검증기 `I2`~`I8` 은 전건 PASS 였다 — **낡은 내용이어도 형식은 옳기 때문**이다).

### ▣ O172-D-2 🟢 시정 내용

| 파일 | 무엇 |
|---|---|
| `03_init_ihcho_스킬_본문.md` | §2 브리핑 추출 판정식 표 「인수인계」 행 → **라벨 최대값 · 전 단위** · 「필요할 때만 읽는 문서」에 **라벨 파일 행 신설** · 조각 행에 「전부 승계됨」 병기 · §5 종료 문안에 **`handoff_write` 1회** + 「조각에 쓰지 마라 · 취소선 쓰지 마라」 |
| `03_init_ihcho_스킬_참조_세션종료.md` | §1 「정본 3곳」 표 행 교체 + **폐기 절차 4건을 취소선으로 명시**(🔴 「하지 마라」를 본문에 남기는 관례) + 도구 규격·주의 6줄 + §2 도구 표에 `handoff_write` 등재 |
| 산출물 2종 | `build_init_ihcho_skill.py --apply` ⇒ `SKILL.md` **393줄** · `references/session-end.md` **104줄 / 8,905 B** · 스냅샷 선행 |
| 검증 | `verify_init_ihcho_skill.py` **불변식 전건 PASS**(`I2`~`I8` · 경고 0) · 제목 유실 0 · 줄길이 PASS |

🟢 실증 = `SKILL.md:147`·`:165`·`:364~365` 와 `references/session-end.md:42`·`:49`·`:91` 에
새 문안이 실재한다(도달 확인 = grep 6곳).

### ▣ O172-D-3 🔴 이 작업의 잔여 (㉠ 이 개선의 소관)

1. 🔴 **`00_작업지침` 분리 결정 1건** — 상세·2안·근거는 `§0-AAAB/O172-C` ▣ O172-C-4 에 있다.
   🔴 **처방을 다시 만들지 말고 그 절을 읽고 결정만 받아라**(중복 분석 금지).
2. 🟠 **O번호 경계 전이 회귀 축 1개** — 다음 세션 착수 시 브리핑 §2 만 보면 판정된다:
   `O0172-A~D` 4단위가 **승계로 내려가고** 새 O번호가 현행이 되는가.
3. 🟠 **선례 확인 1건** — 이번에 「정본 ↔ 산출물」 짝을 빠뜨렸으므로, **다른 빌더 산출물도
   같은 상태가 아닌지** 한 번 훑어라(대상 = `gate_census` 의 `GEN` 등재 도구 중 산출물이
   워크스페이스 밖·스킬 폴더에 있는 것). 🔴 실측 전에는 「없다」고 쓰지 마라.

### ▣ O172-D-4 ⚪ 이 개선의 소관이 아닌 것 (㉡ 워크스페이스 백로그)

🔴 **아래는 브리핑이 준 열린 작업이고 이 개선의 잔여가 아니다**(O172-C 사용자 정정) —
착수표 `⑭`(현업 회신) · `⑩`(결정 선행) · 문서50 열린 절 6건 · `decision_closure` 경고 2건.
🔴 **착수 전에 실측하라** — 이 워크스페이스는 「현업 소관」 기재가 이미 완료된 사례가 반복됐다
(O166-D 3건 · O167 5건 중 하나는 **27일 전에** 해소돼 있었다 · `J3`).

### ▣ O172-D-5 📏 종료 기준선

· 스킬 = `SKILL.md` 393줄 · 참조 104줄 · 검증기 불변식 전건 PASS
· `test_handoff_write` 단정 **41** · 음성 테스트 **38종 전건 rc=0**
· 게이트 = census ✅(stale 0) · 유형 🟡(경고 1 = `00_작업지침` ⇒ D-3 ①) · 조문순서 ✅ ·
  행키 ✅ · 제목 ✅ · 좌표 🟡 ⇒ **전종 rc=0** · 스테이지 드리프트 0
· 라벨 파일 **4개**(`-A`~`-D`) · 🔴 dbt 0건 · 라이브 SQL 0건 · 확정위반 **0건**
