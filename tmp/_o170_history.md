> #### 🟢 [2026-09-17 O170] 집합 불일치 3건 판정 확정 + `D6` 버킷 전수 재실측(분모 386→598 규명) + `DEC-35` 처방 3분기 + 🔴 원천 회귀 신규 1건 적발 + `O170-B` 자기판정 철회
>
> **지시** = *"우리 소관분만 병행 집행"*(승인 불요분 ④ `D6` · ⑤ 집합 불일치 3건 · ⑥ `DEC-35`) ·
> 이어서 *"그래 승인한다"* 로 유지 연산 2건(문서50 `--republish` · `99_NEXT` 재균형) 집행.
> ⇒ `R4-4-2 ㉡` 적용 · 브리핑 선출력 후 즉시 집행 · **확정위반 3건**(아래 §8).
> 🔴🔴 **정본 = `20_issue/_o170_evidence.md`(§E1~§E7).** 이 항목은 경위이고 수치는 그 문서가 정본이다.
>
> ##### 1. ⑤ 집합 불일치 3건 — 3건 모두 판정 확정 · 🔴 **2건은 애초에 불일치가 아니었다**
>
> | 항목 | 종전 기재 | 판정 |
> |---|---|---|
> | `D7` 결정대기 GOLD | 「인수인계 4 ↔ 문서50 6」 | 🟢 **불일치 아님** — 6=원집합(절 제목 · 2026-07) · 4=현 잔여(`O144-C` 재판정) |
> | `D9` `O91-F` | 「5건 중 4건 종결 · 잔여 2건」 산술 불일치 | 🟢 **오류 아님** — 분모가 둘(**처방 5건** ↔ **미종결 승계 3건**) |
> | `D10` 순서9-C 잔여 4 | 「전부 외부 의존」 | 🔴🔴 **2/4 만 참** |
>
> · `D9` 분해 = 처방 5건(완료 3 + 철회 1 + 승계 1) ⇒ 「4건 종결」 = 완료+철회 · 5−4=**1**(`A1`).
>   별개로 승계 3건(`A1` · `A4`/`A2-B` · `90` 분할)에서 `O144-C` 가 분할을 닫아 **잔여 2**.
>   🔴 `A4`/`A2-B` 는 **처방 5건 목록에 없던 항목**이라 두 수가 같은 분모로 읽히지 않았다.
> · 🟢 **처방 = 제목의 원집합 수를 현 잔여로 고치지 않고 병기**했다(원집합은 감사 이력이다).
>   좌표 = 문서50 `-014.md:182` · `-016.md:6` · `-015.md:24`(제목줄은 건드리지 않아 골든 무영향).
>
> ##### 2. 🔴🔴 `D10` — 「외부 원천 입고 대기」 2건이 실제로는 **미배선**이었다
>
> | 잔여 | 라이브 실측 | 정정된 소관 |
> |---|---|---|
> | `E-1` `FUNDRAISING_COST` | 🔴 원천 실재 = `BRONZE_ERP.BDGT_ACMSLT_LEDGER` **258행** · `INCOME_EXPS_DIV_NM` **전건 「지출」** · `MOK_NM` 15종 중 온라인모금 57 · 미디어모금 64(집행 합 **92,010,814,013**) | **결정**(분류 확정) |
> | `E-4` `AD_COST` | 🔴 원천 **3종** 실재(합 **65,046,487,117**) | **설계 결정**(집계 축) |
> | `E-6` `FACT_TARGET_PROJECT` | 🟢 GOLD **0행** · `BRONZE%` 목표 테이블 **0건** | 외부 유지 |
> | `G-5` GA4 | 🟢 개발계 소관 외(§4 참조) | 외부 유지 |
>
> 🟢 **판정식 = 「외부 원천 입고 대기」는 원천 스캔으로만 단정한다.** GOLD 슬롯이 NULL 인 것은
> 원천 부재의 증거가 **아니다** — **미배선**과 **원천 부재**는 다른 사건이다.
> 🔴 이것이 `J3`(「열려 있다」는 기재다)의 **원천 축 판본**이다.
>
> ##### 3. 🔴🔴 ④ `D6` — 「A18·C11·D14·E4·F8 = 55컬럼」은 **셀 수 없는 집합이었다**
>
> 문서50 `-013.md:126~133` 버킷표는 **분모 386**(2026-08-06 `O43`)이고 라이브 분모는 **598**이다.
> 🔴 **그 표는 건수만 있고 컬럼 배정 목록이 없다** — 목록 좌표(`:172`)는 아카이브로 이관됐고
> 그 문서의 분모도 현행과 다르다(`:173~175` 가 스스로 적는다) ⇒ **그 55건은 특정할 수 없다.**
> 🟢 **판정식 = 건수 표는 집합을 담는 그릇이 아니다**(`J2` 의 버킷 축 판본).
>
> ⇒ 라이브 전수 재측정으로 대체했다(`census_columns.py` + 신설 `_o170_gold_unfilled.py` ·
> 판정식은 O43/O167 판본 **그대로** = `nonzero == 0` · 감사컬럼 `DW_*` 제외):
> **테이블 37 · DATA 컬럼 598 · 주입 503 · 미주입 95(전건0 47 · 전건NULL 40 · 0행 8) · 15.9%.**
> 🔴 **총계는 1건 차이인데 내부 분해는 20건 이상 움직였다** — 모델이 `CAST(NULL AS …)` 로 바뀌면
> 같은 컬럼이 **0 축에서 NULL 축으로 이동**한다 ⇒ 🟢 **「전건0 N · 전건NULL N」은 진척 지표가 아니다.**
>
> **집합 차이로 증명했다**(`_o170_unfilled_diff.py` · O167 열거 95건을 **원문 전사** 후 `assert len == 95`):
> 직전 96(O167 95 + O168 정정 1) → 현재 95 · **해소 2**(`FME.UNPAID_STOP_CNT`·`_MEMBERS` · `DEC-54`) ·
> **신규 1** · **누락 0** ⇒ 🟢🟢 **`O167` 열거와 `O168` 정정이 전건 정확했음이 확증됐다.**
> 🟢 **음성 결과도 실측이다** — 이번엔 「이미 닫혀 있던 것」이 나오지 않았고 그것을 그대로 적었다.
>
> **버킷 재배정** = 근거 좌표가 실재하는 **45건만** 배정하고 **50건은 미배정으로 전량 열거**했다(§E4-5).
> 🔴 종전 표의 *"전량 배정 · 누락 0"* 은 **분모 386 시절의 주장**이고 현 분모에서 성립하지 않는다.
> 🟢 **이 항목의 진짜 잔여는 「55컬럼 재실측」이 아니라 「50컬럼 차단 근거 판정」이다.**
>
> ##### 4. 🔴🔴 `O170-B` 자기판정 철회 — 개발/운영 등급을 묻지 않았다
>
> 내가 발행한 두 문장을 **철회**했다: ㉠ *"문서40 `:20` 의 `G-5` 🟢 해소는 현 계정과 상충한다"*
> ㉡ *"**BRONZE 0 · SILVER 1,000만** 괴리(`D11` 외부 로더 축)"*.
> **사용자 정본** = *"이건 개발account야. BRONZE_BIGQUERY 스키마는 껍데기만 있고, 실제 운영에서는
> 1일마다 테이블이 증가하고 있어. … 개발계에는 bigquery_refined_data 를 (전체가 너무 커서)
> 랜덤하게 추출해 가져오고 있다."*
> ⇒ 🟢 개발계에서 그 스키마가 빈 것은 **정상**이고 BRD 는 **설계된 표본 이관**이다.
> 🔴 **내 결함의 형태** = `R2-8-4-d`(문서의 완료 판정으로 라이브를 단정 금지)는 **적용**했는데
> **그 역방향(라이브 부재로 문서 판정을 반증 금지)을 적용하지 않았다.**
> 🟢 **판정식(신설) = 라이브 부재로 판정을 뒤집기 전에 「이 계정이 그 판정의 분모인가」를 물어라.**
> 🔴 `J2` **6차 재발**이다(O165 `D5` → O167 4차 → O171 5차 → 이번).
> 🟢 부수 = **표본 이관 객체는 행수 증감이 진척·회귀를 뜻하지 않는다**(BRD 10,337,767 → 10,770,355 를
> 「+432,588 증가」로 읽으면 틀린다 · 집합이 다르다).
> ⚠️ **`D10` 의 「2/4 만 참」 결론은 유지된다** — `E-1`·`E-4` 원천은 ERP·AGENCY 이고 이 계정에 실재한다.
> `G-5` 는 **결과가 같고 근거만** 「현 계정 미입고」 → **「운영계 소관 · 개발계로 판정 불가」**로 바뀐다.
>
> ##### 5. 🔴🔴 신규 회귀 적발 — `FACT_AD_DIGITAL.CRM_DEV_CNT` 전건 NULL
>
> | 축 | 실측 |
> |---|---|
> | `GOLD.FACT_AD_DIGITAL` | 203,138행 · `CRM_DEV_CNT` non-NULL **0** |
> | 원천 `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS.CRM_DVLP_CNT` | non-NULL **0 / 203,138** · `SUM` NULL |
> | 대조군 `GA_CONV_MBER_CNT` / `CONV_VU_CNT` | **202,739** / **203,063**(정상) |
>
> 🟢 **이웃 2컬럼이 정상이므로 적재 전면 실패가 아니다** ⇒ **원천의 그 컬럼만 비었다.**
> 🟢 `O167` 열거에 `FACT_AD_DIGITAL` 은 `MEDIA_POTENTIAL_CUST_CNT` **1건만** 있었다
> ⇒ **2026-09-16 시점에는 채워져 있었다** ⇒ **회귀가 실증된다.**
> 🔴🔴 **이것은 §O171 §13~§14 가 열어둔 회귀의 두 번째 증상이다**(`AD_DATE` NULL 194,058 · 행 −32,434).
> 🟢 **판정식 = 한 원천 회귀는 한 컬럼에서 끝나지 않는다.** 폴백으로 한 컬럼을 메운 뒤
> **같은 재적재가 건드린 다른 컬럼을 전수 다시 세라**(이번 census 가 그 역할을 했다).
> 🔴 현업 문항은 **`AD_DATE`·행수·`CRM_DVLP_CNT` 를 한 문항으로 묶어라**(같은 사건이다).
>
> ##### 6. 🔴🔴 ⑥ `DEC-35` — 문면대로 집행하면 결함을 만들 뻔했다
>
> `sv_code_label_gate` 실측(rc=0 · 위반 0 · **정보 8**) = `LABEL_PAIRS` 미등재 **7** + `SV_SERVICE.SUBTYPE` 1.
> 🔴 인수인계 기재 「미등재 6」은 **1건 과소**다 ⇒ 🟢 **총계 8 은 맞고 내부 분해가 틀렸다**
> (`D7`·`D9` 와 **같은 유형**의 결함이 세 번째로 나왔다).
>
> 🔴🔴 **7건 중 6건은 `LABEL_PAIRS` 등재 대상이 아니다.** 라이브 종수 실측 =
> `UPPER_CMPGN_CD` **994종** · `SERIES_CD` **~110~170종** · `CMPGN_CTGR_CD` **55종** ·
> `CPR_DIV_CD` **2종**(유일한 저카디널리티). 근거 3중 =
> ㉠ 열거가 `R2-6` 취지에 맞지 않고 ㉡ 열거하면 데이터가 늘 때마다 stale 이 되며
> **그 stale 을 `sv_code_label_gate` 자신이 잡는다**(🔴 `J8` 자기참조 · O169 자기결함 5중 3이 이것)
> ㉢ 5건은 **라벨 컬럼이 이미 뷰에 실재**한다(`SERIES_NAME`·`CMPGN_NAME`) ⇒ 처방은 **라벨 차원 노출**이다.
> ⇒ 🟢 집행 가능 **1**(`CPR_DIV_CD`) · 라벨 차원 노출 **5** · 열거 부적절 **2**.
> 🟢 **라이브 SV 를 고치지 않았다** — 5건은 SV 정의 변경이라 「승인 불요」 범위를 넘는다.
> ⚠️ `CPR_DIV_CD` 라벨 = `A`=통합 · `I`=사단 · `S`=사복(`GOLD.DIM_CAMPAIGN.CPR_DIV_NM`)인데
> **이 SV 에는 `A` 가 없다**(`I` 8,727 · `S` 66,222) ⇒ 코드값은 `I`·`S` 만 열거하고 `A` 부재 사유는
> **DDL `--` 주석과 근거철**에 둔다(`R2-6`).
>
> ##### 7. 🟢 CRM 개편 라이브 대조 8축 전건 일치 + 승인 집행
>
> 작업자 메시지(2026-09-15 전후)를 라이브와 대조 = `BRONZE_CRM` **50**테이블 · `_STDR_YM` **50/50** ·
> 신규 4종 실재 · 삭제 2종 **0 잔존** · `TM_CM_CMPGN_MNG.MKTG_CHANNEL` 실재 · `SND_MEMBER_LIST.OPEN_DT` 부재 ·
> `BDGT_ACMSLT_LEDGER.VENDOR` 실재(값 4종) · `BRONZE_CRM2` 부재(예정대로) ⇒ **8축 일치**.
> ⚠️ `VENDOR` 는 `MOK_NM='미디어모금'` **64행에만** 값이 있다 ⇒ `E-1` 분류 확정 시 이 비대칭을 함께 본다.
>
> **승인 후 집행**(`R4-4-3`) = 원장 `--republish`(SHA 일치) · 문서50 `--republish`(SHA 일치) ·
> `99_NEXT --rebalance --fill 0.7` **24 → 35조각**(최소 여유 **1,771 → 12,436 B**) ·
> `§0-XXXX/O170` 발행 + `§0-WWWW/O171` 승계 표기.
> 🔴 **절 라벨 `0-XXXX` 인데 세션 라벨은 `O170`** 이다(`O171` 이 먼저 절을 발행했다) — **소급 개명하지 않았다.**
> 🔴 **`cat >>` 를 쓰지 않았다** — 이 마운트는 append 를 조용히 유실한다(O167) ⇒ python 전량 쓰기 +
> **도달 토큰 6축 재검사**(10,289 → 19,815 B).
>
> ##### 8. 🔴 자기결함 4건
>
> · 🔴🔴 **`R1-7-8` 앵커로 줄을 덮어 지웠다 — 2회.** ㉠ 문서50 `-016` 의 `O144-C` **머리말 줄** 전체
>   ㉡ `gate_census.py` 의 **문자열 연속 줄**(파일이 컴파일 불가 상태가 됐다). 🟢 둘 다 즉시 복원하고
>   ㉡ 는 `py_compile` **rc=0** 으로 단정했다. 🔴 **한 세션에 2회는 조문을 읽고도 못 지킨 것이다** ⇒
>   🟢 **판정식 = 「제목·행 앵커」만 위험한 것이 아니다. 이어지는 줄(문자열 연속·표 연속)도 같은 등급이다.**
> · 🔴🔴 **`gate_census` 미분류 3건을 방치했다.** `_o170_reserve`·`_o170_gold_unfilled`·`_o170_unfilled_diff`
>   를 만들고 `R1-6-18 ⑥`(신설 도구는 `gate_census` 에 축과 함께 등재) 을 이행하지 않았다.
>   🔴 **그 결과 §O171 이 「내 것이 아닌 `_o170_*`」 을 보고 라벨을 재확인해야 했다**(§O171 ⑨).
>   🟢 시정 = 3분류 등재(관측 2 · MUTATES 1) ⇒ `gate_census` rc=0 · 음성 **11/11**.
>   🟢 **판정식 = 내가 만든 도구를 등재하지 않으면 그 비용은 다음 세션이 낸다.**
> · 🔴 **틀린 추론을 응답으로 발행했다.** `split_doc --verify` FAIL 을 보고 「게이트2 줄 수 차이가 크니
>   기준선이 원래 stale 이다」로 판정했는데, **미편집 문서30 을 대조하니 SHA 일치**였다
>   ⇒ FAIL 은 **내 편집 때문**이었다. 🟢 즉시 정정했다. 🔴 **줄 수 차이는 재발행 규약상 정상이므로
>   그것으로 SHA 불일치를 설명할 수 없었다** — 🟢 **판정식 = 대조군을 먼저 잡아라**(미편집 형제 문서 1개면 됐다).
> · 🔴 **원장 행 상한 초과** — 처음 쓴 행이 **2,243자**로 `R1-5-1` 상한(2,000)을 넘겼다.
>   🔴 **§O171 이 같은 결함(2,112자)을 바로 앞 세션에 등재해 두었는데 반복했다.**
>   🟢 `line_len.py` 가 잡아 **1,899자**로 축약(상세는 이 항목으로 이관) ⇒ 규약 「1,900자 이내」 충족.
> · ⚠️ 그 밖 = 문서50·원장 재발행 시 스냅샷 라벨이 **`UNLABELED`** 로 찍혔다(`R1-7-10` 은 인자 →
>   `SESSION_LABEL` → `UNLABELED` 순이다) ⇒ 이후 연산에는 `SESSION_LABEL=O170` 을 넘겼다.
>
> ##### 9. 🟢 지킨 것 · 🔴 dbt
>
> `R1-4-3` 라벨 선점 등재(**그 선점이 §O171 의 라벨 오용을 적발하게 했다**) · `R4-4-1` 브리핑 선출력 ·
> `R1-7-2` 편집 전 해시 **배치** 확인(`O91-F` `V1` 처방) · `R1-7-1` `write` 는 신규 파일 전용 ·
> `R1-3-7-c` **판정 전 근거 파일 선기록**(근거철을 먼저 쓰고 판정했다) · `R4-4-3` 유지 연산 3건 전건 승인 후 ·
> `R2-6` SV COMMENT 무변경 · `R1-5-4` 전 편집 후 `line_len`.
> 🔴 **dbt 미실행**(`R4-1`) — 모델을 손대지 않았고 build 대상도 없었다.
> 🔴 **read 미반환 0 · 재호출 0.**

