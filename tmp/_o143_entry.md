> #### 🟢 [2026-09-08 O143] O 누적작업 비판적 점검 — 브리핑 오선택 2건 시정 · 측정축 고정 · 등록부 결합 게이트화 · SERVING 표면 부재 적발
>
> **지시** = 「여태까지의 O 이력을 비판적으로 검토 · 누락된 오류 확인 · 참고 문서와 프롬프트 보충」
> + 「작업량이 많으면 `/07_O작업정리/` 에 계획서를 먼저 만들고 착수」.
> 🟢 **`R4-4-2 ㉡` 분기 적용**(지시 동봉 → 브리핑 출력 후 즉시 수행) · **확정위반 0건** ·
> 사용자가 승인 필요 작업을 **사전 전건 승인**했다.
>
> **§0. 범위 판정 — 왜 계획서를 먼저 썼는가**
> 실측 분모 = 전 문서군 **2,428,268 자** · `01_세션이력` **58조각 1,550,499 B** · O 라벨 정의 최대 **142**.
> ⇒ 이력 전량 독해는 물리적으로 불가능하다. 따라서 이 점검은 「이력을 읽어 결함을 찾기」가 아니라
> **「결함을 검출하는 판정식을 세우고 그 판정식으로 정본을 훑기」**로 설계했다(`R3-9 ㉦` 축).
> 산출물 = `07_O작업정리/00_O작업_점검_계획서.md`(진행 원장 · Phase 0~5 · 판정식·산출물 명시) ·
> `07_O작업정리/01_NL스모크_절차서.md`.
>
> **§1. 🔴🔴 D1 — 브리핑이 승계된 인수인계 절을 현행으로 뽑고 있었다 (원인 2건 · 둘 다 시정)**
> 착수 브리핑의 「현행 시작점」이 **`§0-NNNN`(O140 · 이미 취소선 승계됨 · `-028.md:19`)** 으로 나왔다.
> 실제 현행은 **`§0-PPPP`(O142 · `-028.md:81`)** 였다.
> · **원인 1 = 리터럴 인접 의존.** `session_brief.py:246` 이 `'~~여기서 시작한다' in t` 였다.
>   O129 부터 관례가 **대괄호 전체를 감싸는 형태**(`## 0-NNNN. ~~🔴🔴 [… 여기서 시작한다. …]~~ ➔ 🟢 […]`)로
>   바뀌어 그 **연속 부분문자열이 존재하지 않게** 됐다 ⇒ 배제 실패.
> · **원인 2 = 동점 tie-break 부재.** `max(cands, key=date)` 이고 O140·O141·O142 **날짜가 모두 `2026-09-07`**
>   이라 파이썬 `max` 의 동점 규칙(최초 등장)이 **가장 오래된 절**을 골랐다.
>   🔴 **원인 1 만 고쳐도 재발한다** — 둘은 독립이다.
> · 🔴 **실해** = `§0-NNNN` 의 열린 목록에는 `⑫`·`⑭`·`㊵`·`BLOCKING-5` 가 실려 있고 그중 **`㊵`·`⑫` 는
>   O141 이 이미 종결**했다 ⇒ 브리핑만 읽고 착수하는 세션은 **닫힌 작업을 다시 한다.**
>   O106 이 만든 브리핑 체계(`R1-1` 개정)의 **목적 자체가 무력화된 상태**였다.
> · 🟢 **시정** = `struck_out()` 신설(문구의 **모든 등장**이 `~~ … ~~` **구간 안**에 있을 때만 승계) +
>   정렬 키 `(date, idx)`(동점은 **문서 순서상 마지막** = `99_NEXT` 적층 방향).
>   재생성 후 현행 절 = **`§0-PPPP`(:81)** 로 정정 확인.
>
> **§2. 🔴 D2 — 음성 테스트 29종이 전건 rc=0 인데 D1 이 실재했다**
> 원인 = `test_session_brief.py` 픽스처의 취소선 6건이 **전부 구관례**(`:111`·`:125`)이고
> 신관례 픽스처 **0건** · 동일 날짜 동시후보 픽스처 **0건**이었다.
> ⇒ 🟢 **축 4-C 신설**(픽스처는 `-028.md:19·50·81` **원문 그대로** · 창작 0) + 축10(누락 대조).
> 🔴 **판정식을 「고친 뒤 통과」로 두지 않았다** — 축을 먼저 넣고 **수정 전 코드에서 9건 실패**를
> 실증한 뒤 고쳤다. 오탐 가드 3건(무관한 취소선은 배제 사유가 아니다)은 수정 전에도 통과했다
> ⇒ 픽스처가 과잉이 아니라는 증거다. 최종 = **10축 58단정 전건 통과**.
>
> **§3. 🟠 D3 — `-028.md:17-18` 표가 본문 행 없이 절단돼 있다 (관측 확정 · 원인 미규명)**
> `§0-FFFF` 의 「항목/성격/좌표」 표가 **헤더 1행 + 파이프 1글자**로 끝난다.
> · 🟢 **조각은 결백하다** — `split_doc --verify` **PASS**(본문 바이트 동일 · 유실 0 · 행선지 없음 0종)
>   ⇒ **원문 자체가 이 상태**이고 조각화·재발행이 만든 손상이 아니다.
> · 🔴 **복구 불가** — `_archive/` 대조를 시도했으나 `grep` 이 `timeout` 에 죽었고(그 관측이 **D7**),
>   파일명 기준 탐색으로도 원형을 찾지 못했다 ⇒ **미확보로 명시**하고 추정 복원을 하지 않았다(`R2-8-3`).
> · 🔴 **게이트 맹점 1건** = `--verify` 「게이트5 표 열수 결손」이 **0 으로 통과**했다. 즉
>   **파이프 한 글자만 남은 줄을 결손으로 세지 않는다** ⇒ 후속 과제 **후-3** 으로 등재.
> · 🟢 **영향도 낮음** — `§0-FFFF` 는 O132 에 승계된 과거 절이고 스스로 「착수 항목은 착수표에 있다」고
>   정본을 넘겼다.
>
> **§4. 🔴 D5 — 문서 바이트 실측이 `stat` 에 의존해 stale 오탐을 냈다 (시정 완료)**
> 착수 1차 브리핑이 `31_코드군_매핑등재부` 를 **stale 1건**으로 보고했다(「기재 14,891 ↔ 실측 14,896」).
> 실제 파일은 **14,891 B**(`wc -c`·`read_bytes` 일치)이고 **14,896 은 그 값의 16바이트 블록 반올림값**이다
> (`cortex ws ls` 도 14,896) ⇒ **기재가 맞고 「실측」이 틀렸다.**
> · 원인 = `doc_census.nbytes_of` 가 `os.path.getsize`(stat)였다. `/workspace` 는 스테이지 마운트이고
>   **cold 파일의 stat 이 반올림값을 낸 것**으로 보인다.
> · 🔴 **위험은 오탐이 아니라 오탐이 유도하는 조치다** — 세션이 이를 믿고 기재를 14,896 으로
>   「교정」하면 **정본이 거짓이 된다.**
> · ⚠️ **기전은 유력추정이다** — 읽으면 자기치유되어 사후 재현이 불가능하다
>   (`scripts/_o143_size_axis_probe.py` = 이 시점 `.md` **314개 전건 stat == 실제**).
>   ⇒ 기전 규명 대신 **측정을 stat 비의존으로 교체**했다(결론이 기전과 무관하게 옳다).
> · 🟢 `doc_census.nbytes_of` · `doc_type_gate.nbytes_of` 를 읽은 바이트로 교체 +
>   `test_doc_census.py` **축7** 신설(16의 배수가 아닌 4,005 B 픽스처로 반올림을 구별 · 두 도구 값 일치 단정).
> · 🔎 지침 `R2-5` 가 「`ws ls` size 는 블록 반올림값」이라고 **이미 경고**하는데 도구가 그 함정을 밟았다
>   ⇒ `R3-9 ㉡`(같은 것을 다르게 재는 지점)의 실물이다. 현재 stale **0건**.
>
> **§5. 🔴🔴 D6 — 착수표 열린 집합과 인수인계 목록이 다르다 (게이트화 완료)**
> 실측 = 착수표 열림 **`② ④ ⑪ ⑭ ⑱ ㊳`(6건)** ↔ `§0-PPPP` 목록 **`② ④ ⑪ ⑱`**
> ⇒ **`⑭`·`㊳` 누락**이고 `⑭` 는 **P1 🔴🔴**(FME STOP 팬아웃 귀속 · 현업 결정 대기)다.
> · 경로 = `⑭` 는 `§0-NNNN`(O140)에 있었고 `§0-OOOO`(O141)에서 사라졌다. O141 은 **「점검」만** 했고
>   착수표 행(`-022.md:114`)은 그대로 열려 있다 ⇒ **닫지 않은 항목이 인수인계에서 조용히 사라졌다.**
> · 🔴 **선례가 문서에 이미 있었다** — 착수표 `㊲` 행이 *"O128 이 먼저 해소했으나 취소선 처리가
>   누락됐다"* 고 스스로 적는다 ⇒ 두 등록부의 결합이 **사람 손에만** 있었고 어느 게이트도 이 축을 안 봤다.
> · 🟢 **시정** = `session_brief.handoff_gap` 신설 → `00_BRIEF` §2 에 **좌표와 함께** 경고
>   (blocking 아님 · 판정은 사람 · 방향은 한쪽 = 인수인계에만 있는 문서50 계열은 정상).
>   재생성 결과 **실제 정본에서 `⑭`·`㊳` 2건 정확 검출**. 음성 테스트 축10(역방향 오탐 가드 포함).
> · 🟢 지침 **`R3-9 ㉨`** 신설 + 스킬 자문 **⑨** 추가(㉠~㉧ **8개 → ㉠~㉨ 9개**로 양쪽 범위 갱신).
>
> **§6. 🔴🔴 D7 — `timeout` 에 죽은 검색이 「0건」으로 보인다 (지침 `R0-8` 신설)**
> `grep -rn "FFFF1" 99_NEXT_SESSION_조각/ _archive/` = **0건**인데 단일 경로로는 **1건**이었다.
> 원인 = `_archive/` 가 **447파일 19,214,080 B** 이고 60초를 넘겨 `timeout` 이 `SIGTERM` 으로 죽였다
> (**rc=143**). 출력은 빈 문자열이고 `2>&1 | head` 를 거쳐 **에러도 보이지 않았다.**
> 🔴 **이 함정은 지침이 만든다** — `R0-4`(`timeout 60` 의무) + `R0-2`(다중 경로 권장)의 결합이라
> **분모가 큰 검색은 구조적으로 조용히 0 을 낸다.**
> 🟢 **`R0-8` + 부속 3조 신설** = ㉠ 0건은 `rc` 로 검증(`0` 만 「없다」 · `143`·`124`·`2` 는 **미판정**)
> ㉡ 파이프 뒤 `$?` 금지(O120 의 게이트·테스트 규칙을 **검색·조회로 확장**) ㉢ 대용량 경로 분리.
> 스킬 **Step 0** 에도 실었다(첫 bash 호출 **전에** 노출되어야 하는 규칙이다).
> 🟢 **같은 세션에서 효과가 실증됐다** — 직후 `grep … 06_DDL.sql` 이 **rc=2** 를 냈고, 「부재」로 읽지
> 않고 경로를 찾아 `03_top-down_gold/06_DDL.sql` 을 발견했다.
>
> **§7. 🔴 D8 — 착수표 `⑭` 의 「COMMENT 사유 명시」가 정본에 없었다 (보강 완료)**
> `⑭` 는 *"현재 처리 = 0 센티넬 + COMMENT 사유 명시(`R2-7`)"* 라 적지만 실측 결과 **양쪽 다 없었다**:
> · 라이브 컬럼 COMMENT = `후원사업 (FK→DIM_SPONSORSHIP)` — **사유 없음**
> · `06_DDL.sql` FME `[컬럼별 설계 및 실측 이력]` 블록에 **`· SPONSORSHIP_SK` 항목 자체가 없음**
>   (전 파일 `· SPONSORSHIP_SK` 4건 = 전부 다른 테이블 소관)
> 🔴 **더 나쁜 것** = 같은 파일 후반 변경이력이 *"사건 grain 에서는 후원사업이 하나로 확정되므로
> 귀속 규칙이 필요 없고"* 라고 적는다. **DEV 축 한정 서술인데 축을 밝히지 않아**, 읽는 사람이
> **STOP 센티넬을 「배선 누락」으로 오해해 고치게** 만든다 ⇒ `⑭` 의 「현업 결정 전 배선 금지」
> 정지점과 **정면 충돌하게 읽힌다.**
> 🟢 **시정** = FME 이력 블록에 `· SPONSORSHIP_SK` 신설 — DEV 전용 배선 / STOP 전건 0 센티넬 +
> 사유(동시중단 다중사업 · O73-C 1.56배 = pairs 1,517,797 / 키 970,486) + `⑭` 정지점과 선택지 4종 +
> **DEV 축 한정임을 명시** + B2(FMM)와 grain 이 다르다는 경고 + O143 라이브 실측 병기.
> 선례 = 같은 블록의 `· ORG_SK` 항목이 동일 패턴(DEV 전용 / STOP 전건 0 / 사유 / 금지)을 이미 쓰고 있었다.
>
> **§8. 🟢 착수표 `⑭` 가드 유지 — 라이브 실증 (`R2-8-4-a` 3요소 기재)**
> 계정 `hy04212` / `ACCOUNTADMIN` / 2026-09-08 조회. 🔴 계정명은 맥락이고 **판정 근거는 조회 결과다**.
> · `FACT_MEMBER_EVENT` 전체 **4,633,105행** · `SPONSORSHIP_SK=0` **1,038,262** · NULL **0**
> · `EVENT_TYPE` 별 = **STOP 1,038,262행 전건 SK=0**(비영 **0**) · **DEV 3,594,843행 전건 비영**
> ⇒ 🟢 **0 센티넬 가드 유지 확정 · 사업 축 전개(팬아웃) 없음.** 배선은 하지 않았다(정지점 준수).
> 🔎 O137 기재(「STOP 1.04M 센티넬 0」)와 정합한다.
>
> **§9. 🔴🔴 D9 — SERVING 표면(SV·Agent)이 라이브에 없다 ⇒ 착수표 `②` 는 실행 불가**
> · `SHOW AGENTS` · `SHOW AGENTS IN ACCOUNT` = **0행**(rc 정상)
> · `SHOW SEMANTIC VIEWS IN DATABASE GN_DW` = **1종 `SV_AD` 뿐**(생성 `2026-09-07 01:46` = O142 가 만든 것)
> · `SHOW SCHEMAS IN DATABASE GN_DW` = 15종 · **전 스키마 `created_on` 이 `2026-09-07 00:23~00:25`**
> · DW 본체는 정상 = GOLD **37 테이블 + 14 뷰** · SILVER **43** · ML **16** · OPS **4**
> 🔴 **판정** = 계정이 **2026-09-07 에 재구축**됐고 O139~O142 가 손댄 GOLD/SILVER/`SV_AD` 만 재생성됐다.
> **나머지 SV 와 Agent 3종은 재배포되지 않았다.**
> 🔴 **결과** = 착수표 `②`(NL 스모크)의 정지점은 **「사람 소관」이 아니라 「선행 배포 미완」**이다.
> 여러 세션 동안 「사람이 UI 에서 하면 된다」로 이월돼 왔지만 **UI 에 Agent 가 없어 사람도 못 한다.**
> 🔎 지침 `R2-8-4`·`R3-9 ㉤` 가 경고한 바로 그 상황이다 —
> *"계정이 바뀌면 객체는 사라지지만 문서의 「완료」는 남는다."*
> ⇒ 🔴 **O134 「`AGENT_MEMBER` `VERSION$3` 11도구 일치」·O136 「28/28 PASS」는 라이브 근거가
> 아니다**(`R2-8-4-d` — `90` 계열은 닫힌 항목의 보존소이고 라이브 상태의 정본이 아니다).
> 🟢 **산출물** = `07_O작업정리/01_NL스모크_절차서.md` — 차단 사실(§0 실측표) · 선행조건 5단계 ·
> 판정 4종(`집계필요`·`배분규칙필요`·`형제팩트중복`·`앵커_경합`) · 질의 5축 · 결과 기록표.
> 🔴 **Q5 = 과잉 차단 오탐 가드**(자기 grain 질의는 정상 답변이어야 한다) — **Q1 만 보면 「거부만 잘하는
> Agent」를 통과시킨다.** 🔴 재배포는 이 세션이 하지 않았다(라이브 객체 생성 + 배포 절차 준수 ⇒ 별도 단위).
>
> **§10. 판단 기록 — 하지 않은 것과 그 이유**
> · **원장 재균형 보류(사전 승인은 받았다)** — 재균형은 조각 경계를 바꿔 **인용 좌표(`파일:행`)를
>   무효화**한다(`doc_coord_gate` 축3 경고 증가). 대신 **O112 ㉢ 처방**대로 원장 행을 압축해 썼다.
>   🔴 **승인이 있다는 것이 실행 근거는 아니다** — 필요성이 실증되지 않았다. 원장 `-002` 여유 **447 B**.
> · **문서50 열린 절 18건 절별 재판정 미실행** — 목록·좌표만 확보했다(후속 **후-2**).
>   🔴 「전건 검토」라고 쓰지 않는다(`R3-9 ㉥` 부분 종결 금지).
> · **dbt 명령 0회 실행**(`R4-1`) · **현업 결정 대기 항목 배선 0건** · **라이브 DDL 변경 0건**.
> · **`_o125e_entry.md`(㊳) 에 `rm` 시도 0회**(`R1-7-7` · 스테이지에 실체가 없다).
>
> **§11. 검증 (rc 는 전부 리다이렉트로 수집 · `R0-8-2`)**
> · 게이트 6종 = `doc_census` ✅(stale **0** · 분할 분모 위반 **0**) · `doc_type_gate` 🟡(여유 경고 4건) ·
>   `clause_order_gate` ✅(조문 **96 → 100** · 역전 0 · 중복 0) · `index_row_gate` ✅(행 유실 0) ·
>   `doc_heading_gate` ✅(제목 유실 0 · 1,467/1,467) · `doc_coord_gate` 🟡(깨진 좌표 0 · 경고 11건)
> · 음성 테스트 **전건 rc=0** · `test_session_brief` **10축 58단정** · `test_doc_census` **33단정**
> · `split_doc --republish --expect` = 원장(`착수 선점(진행중)`) · `99_NEXT`(`0-QQQQ`·`QQQQ2`) 둘 다
>   **영속성 가드 통과** · 게이트1 concat SHA256 일치 · 게이트5 표 열수 결손 증가 0 · 게이트6 상한 초과 0
> · `line_len.py` 🟢 PASS(신규·수정 파일 전건 · 2,000자 초과 0줄) · `cortex ws ls` 로 스테이지 실체 확인
> · **`read` 미반환 0건 · 재호출 0건**(판정식 `재호출 ≥ 미반환` 충족) ·
>   독해 좌표·고유 토큰 **14건** 기록 = `07_O작업정리/00_O작업_점검_계획서.md` §6
>
> **§12. 자기검토 — 이 세션이 남긴 약점**
> · 🟠 **D3 는 닫지 못했다** — 원인·원형 모두 미확보다. 「낮은 영향도」는 **추정이 아니라 그 절이
>   스스로 정본을 착수표로 넘겼다는 문장에 근거**하지만, 잃은 내용이 무엇이었는지는 **모른다**.
> · 🟠 **D5 의 기전은 실증되지 않았다** — 「cold stat 반올림」은 유력추정이다. 판정과 시정은
>   기전과 무관하게 옳지만, **기전을 확정했다고 쓰지 않는다.**
> · 🟠 **`handoff_gap` 은 문자열 등장 판정이다** — 인수인계 절이 순번을 다른 표기로 쓰면 놓친다.
>   ⇒ 경고 전용이고 blocking 이 아닌 이유이기도 하다.
> · 🔴 **가장 큰 것** = D9 로 드러난 구조적 문제는 **「라이브 실재를 주장하는 문안이 조회 시점을
>   달고 있지 않다」**는 것이다. 이 세션은 새로 쓰는 문안에만 3요소를 달았고 **소급 정비는 하지 않았다**
>   (후속 **후-4**). ⇒ 다른 「배포 완료」 문안들도 같은 상태일 가능성이 높다.
