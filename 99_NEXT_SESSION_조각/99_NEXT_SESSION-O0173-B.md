<!-- LLM-METADATA
doc_id: HANDOFF_O0173_B
doc_role: 인수인계 — 세션 `O173-B` 가 다음 세션에 넘기는 것(라벨 파일 · O172 규격)
project: GN_DW (굿네이버스)
created: 2026-09-21
created_by: O173-B
parent: 99_NEXT_SESSION.md
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

<!-- HANDOFF-LABEL O0173-B -->

> 🔴🔴 **이 파일은 인수인계 라벨 파일이다 — 조각이 아니다.**
> 허브 목차·`--verify` concat·`발행 SHA256` 과 **무관**하다(`O172` 규격).
> 🟢 **현행 판정식 = 라벨 최대값**(O번호 → 접미 길이 → 접미) ⇒ 취소선 승계 표기가 필요 없다.
> 🔴 다음 세션은 **이 파일 1개만** 읽으면 된다. 조각에 남은 옛 절은 승계된 것이다.
## 0-AAAD/O173-B. 🟢 [2026-09-21 O173-B — 여기서 시작한다 · **`O173-A` 와 함께 읽어라**]

> 🔴 이 세션은 단위 **2개**(`-A`·`-B`)다 — 접미가 다른 것은 승계가 아니라 **형제**다.
> 🟢 `-A` = 브리핑 성능 + `R3` 분리 집행 · `-B` = **그 작업의 감사와 시정**.

### ▣ O173-B-0 🔴🔴 먼저 알아라 — 감사가 확정한 판정식 4개

· ㉠ **「분모 4곳」은 문서 일반의 최소치이고, 조문 문서는 +1 이다**
  (`clause_order_gate.DOCS`). 🔴 `R1-6-18 ④` 문면만 따르면 **조문 순서·중복이 무검사로 남는다.**
  🟢 **조문을 정의하는 문서를 만들면 분모는 5곳이다.**
· ㉡ **「무변경」은 블록 주장이 아니라 「내가 아무것도 더하지 않았다」는 주장이다.**
  🔴 `R2-8-1` 기계 대조(토큰 부재 0 · 블록 포함 True)는 **삭제를 잡고 추가를 못 잡는다**
  ⇒ 그 대조 통과가 「무변경」을 증명하지 않는다(`R2-8-2` 는 가필을 따로 신고하라고 한다).
· ㉢ 🔴🔴 **「판정식을 인용하는 것」과 「판정식을 실행하는 것」은 다르다.**
  실사고 = `O173-A` 가 `J5`(폐기 문안 전수 검색)를 **인용하면서** 같은 패턴(`Step 0.7`)을
  **전수 검색하지 않았다**(29곳 잔존 · 그중 계약 문장 5곳). ⇒ `P154`·`J8` 의 자기적용 판본.
  🟢 **조문을 인용한 그 문장이 그 조문의 위반일 수 있다.**
· ㉣ **「여유 부족」의 처방은 문서 유형이 정한다** — `--rebalance` 는 **분할 문서만** 된다.
  미분할 문서(빌더 마커 때문에 분할이 금지된 것 포함)는 **지연 로드 분리**가 처방이다.

### ▣ O173-B-1 🟢 이 단위가 끝낸 것

· **감사 4건 시정** = ㉠ `clause_order_gate.DOCS` 등재(조문 103→**112** · `R3` 정본 9개 인식)
  ㉡ 정본에 `R3-1`~`R3-9` 라벨 부여 + 선례 형식 정렬 + **「이관+가필」로 문안 정정**
  ㉢ `Step 0.7` 죽은 좌표 **계약 5곳** 소거(이력·사례집·골든 24곳은 **역사라서 보존**)
  ㉣ `판정식 8종` 개수 제거 + 골든 개명 발행.
· **승인분 집행** = 원장 `--rebalance --fill 0.7`(조각 17→**15** · 최소 여유 6,145→**12,701 B**
  · concat SHA256 일치 · 토큰 유실 0) ⇒ 유형 여유부족 **2→1건**.

### ▣ O173-B-2 🟠 다음이 할 일 — ㉠ 이 작업의 잔여

· 🔴 **`03_init_ihcho_스킬_본문` 여유 6,634 B (< 20%) — 유일한 잔여 경고.**
  🔴 **`--rebalance` 불가**(미분할 · 분할하면 빌더 마커가 갈린다) ⇒ 처방은 **지연 로드 분리**다.
  🟢 선례 = `O167` 이 §5·§5-1·§4-1 도구표를 `references/session-end.md` 로 뺐다(452→389줄).
  🔴 **무엇을 뺄지는 결정 사항이다** — 후보 = §3 판정식(J1~J8 · 약 6KB · 「판정·보고 전마다」라
  지연 로드 위험) · §4-2 신규 문서 절차 · §2 Step 3 브리핑 출력 형식.
  🔴🔴 **계약은 뺄 수 없다** = `I5`(`R1-7-1`~`R1-7-10`) · `I6`(`R3-9 ㉠`~`㉨`) 는 본문 상주다.
· 🟠 **`I7` 분모 확대 검토** — 이 감사에서 `I7` 이 「판정식 8종」을 **통과시켰다**(조각 수·바이트
  패턴만 본다). 🟢 후보 = `\d+종`·`\d+개 조문`·`\d+곳` 류를 경고로 올린다.
  🔴 오탐이 많을 수 있으니 **경고(비blocking)로 시작**하라.
· 🟠 **`R1-6-18 ④` 문면 개정 검토** — 「분모 4곳」이 조문 문서에서 부족하다는 것이 실증됐다
  ⇒ 그 조문에 **「조문 문서는 `clause_order_gate.DOCS` 까지 5곳」**을 병기할지 결정.
  🔴 `01_문서분할_규약` 을 고치는 일이므로 지침 개정 절차를 따른다.
· 🟠 **cold 성능 재측정**(`O173-A` 에서 이월) — 다음 세션 **첫 `doc_census` 실행이 유일한 cold
  측정 기회**다. 착수 즉시 시간을 재서 `census()` **78.39s**(0.32s/파일)와 대조하라.

### ▣ O173-B-3 🔴 내가 못 여는 것 · 승인·결정 대기

· 위 ▣ 2 의 4건 전부 **결정 선행**이다(지연 로드 범위 · `I7` 분모 · `R1-6-18` 개정 · 측정 우선순위).
· 🔴🔴 **환경**(`O173-A` 에서 이월 · 우리 소관 아님) = `/workspace` 심링크가 세션 중 갈린다.
  🟢 **백그라운드 셸은 `default` 를 받는다** ⇒ **백그라운드 실행을 쓰지 마라** · 포그라운드 +
  `timeout_ms`. 🔴 최초 브리핑이 **0바이트**로 끝난 원인이 이것이다.
· ⚠️ `gate_census` **미분류 강제 구조**는 여전히 미설계다(`O173-A` ▣ 3 과 동일).

### ▣ O173-B-4 ⚪ 이 개선의 소관이 아닌 것 — ㉡ 워크스페이스 백로그

· 착수표 열린 **2건**(`⑭`·`⑩`) · 문서50 열린 절 **6건** — 손대지 않았다.
· 현업 문항(AGENCY 재적재 회귀 · VIDEO.CPC `#DIV/0!` · D1~D4 · CPC 개명 등)은
  **`O172` 계열 인수인계와 브리핑 §1·§3 에 그대로 살아 있다**.

### ▣ O173-B-5 📏 종료 기준선 (🔴 인용하지 말고 재라)

· 게이트 **13종 rc=0** · 음성 테스트 **38/38 rc=0** · 검증기 불변식 전건 PASS(경고 0)
· 조문 **112개**(역전 0 · 중복 0) · 제목 골든 **문서 24종 · 1,840개** · 이 세션 발행 **2회**
· 원장 조각 **15** · 최소 여유 **12,701 B** · 유형 여유부족 **1건**
· `SKILL.md` **402줄 / 33,216 B** · `read` 미반환 **0** · 재호출 **0**
· 🔴 다음 세션 라벨 = **O174**

_Co-authored with CoCo_

_Co-authored with CoCo_
