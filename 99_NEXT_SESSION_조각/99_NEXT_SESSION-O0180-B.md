<!-- LLM-METADATA
doc_id: HANDOFF_O0180_B
doc_role: 인수인계 — 세션 `O180-B` 가 다음 세션에 넘기는 것(라벨 파일 · O172 규격)
project: GN_DW (굿네이버스)
created: 2026-09-23
created_by: O180-B
parent: 99_NEXT_SESSION.md
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

<!-- HANDOFF-LABEL O0180-B -->

> 🔴🔴 **이 파일은 인수인계 라벨 파일이다 — 조각이 아니다.**
> 허브 목차·`--verify` concat·`발행 SHA256` 과 **무관**하다(`O172` 규격).
> 🟢 **현행 판정식 = 라벨 최대값**(O번호 → 접미 길이 → 접미) ⇒ 취소선 승계 표기가 필요 없다.
> 🔴 다음 세션은 **이 파일 1개만** 읽으면 된다. 조각에 남은 옛 절은 승계된 것이다.
### ▣ O180-B-0 🔴🔴 먼저 알아라 — 이 단위가 확정한 판정식

| # | 판정식 | 근거(실사고) |
|---|---|---|
| ㉠ | **「선언」과 「강제」는 다른 것이다** | 원장 §0 유형 등재표가 존재했는데 `split_doc.py` 는 그것을 **한 번도 읽지 않았다** ⇒ 유형 선언이 `--rebalance` 를 막지 못했다 |
| ㉡ | 🔴🔴 **「안전측 폴백」이 가드를 죽이는 가장 흔한 경로다** — 폴백은 **「고장」과 「미등재」를 구별해야 한다** | 내가 넣은 `except Exception: return None` 이 함수명 오류(`read_registry`)를 삼켜 가드가 통째로 건너뛰어졌다 |
| ㉢ | 🔴🔴 **「가드를 심었다」는 「가드가 동작한다」가 아니다** — 심은 직후 **거부되는지 실증하라** | 실증을 했기 때문에 사고를 즉시 발견했다. 안 했으면 다음 세션이 「가드가 있다」고 믿고 좌표를 계속 깼다 |
| ㉣ | **`doc_coord_gate` 는 경로만 보고 줄 내용을 보지 않는다** ⇒ 재균형이 깬 좌표를 **탐지하지 못한다** | `_o170_evidence.md` 좌표 2건이 **이전 세션 재균형으로 이미 깨져** 있었고 게이트는 계속 🟢 였다 |
| ㉤ | **`--verify` 는 원문이 아니라 「발행 기준 concat SHA256」과 대조한다** ⇒ 수기·rollover 조각에도 성립한다 | 【2】㉢ 의 전제(*"수기 조각엔 성립하지 않는다"*)가 **기각**됐다(`J4` class) |
| ㉥ | **도구 보고를 위치 근거로 쓰지 마라** — `grep -n` 으로 재확인하라 | `edit` 컨텍스트 미리보기를 읽고 **정상 편집을 사고로 오진**했다(즉시 정정) |

🔴 **신설 환경 함정 1건**(인수인계 `-O0180-A` 【4】에 없던 것)
· **`build_init_ihcho_skill.py` 는 dry-run 이 기본이다** — `--apply` 없이 돌리면
  *"🟠 DRY-RUN — 변경 대상 1종"* 만 내고 아무것도 쓰지 않는다. 🔴 그 상태로 검증기를 돌리면
  `I1 정본 ↔ 산출물 불일치` FAIL 이 나는데 **원인은 내 문안이 아니라 미집행**이다.

---

### ▣ O180-B-1 🟢 이 단위가 끝낸 것 — 인수인계 【2】 전건 종결

| 단계 | 내용 | 상태 |
|---|---|---|
| ㉠ | 원장 §0 에 **`증분형`** 신설(다섯째 유형) + 문서20·문서50 전환 | ✅ |
| ㉡ | `doc_type_gate` 증분형 축(`TAIL_KINDS` · 유형별 처방 문안) + **`split_doc` `--rebalance` 거부 가드** + 음성 **19단정** | ✅ |
| ㉢ | 수기 조각 정규 편입 — 🟢 **전제가 기각돼 조치가 축소됐다**(`--verify` rc=0 실증 · 헤더 문안만 정리) | ✅ |
| ㉣ | `00_guides/03_init_ihcho_스킬_본문.md` 3곳 갱신 + 재바상 + 검증기 rc=0 | ✅ |

**증분형 정의(정본 = 원장 §0)** = 항목 본문은 **append**(꼬리로만) + **판정 줄만 제자리 갱신**.
· 처리 = 신규 항목 `--rollover` · 판정 갱신은 그 조각 `edit` · 🔴🔴 **`--rebalance` 금지**(도구가 거부).
· 여유 판정 = **꼬리 기준**(문서20 29,451→**34,415** · 문서50 30,204→**35,942**).

**신설 파일 1 · 수정 파일 5**
- 🆕 `scripts/test_doc_type_increment.py`(19단정 · 등재4·판정5·여유2·**회귀3**·차단5)
- `scripts/doc_type_gate.py` — `KINDS`+`증분형` · `INCREMENTAL` · `TAIL_KINDS` · 상한초과 처방 분기
- `scripts/split_doc.py` — `declared_kind()` 신설 · `rebalance(force=)` 거부 · `--force-rebalance`
- `00_guides/03_init_ihcho_스킬_본문.md` 3곳(브리핑 템플릿 · `R4-4-3` 절 · §4-2)
- 원장 `-002`(§0 등재표 + §1 행 2개) · 이력 `-082` ·
  `20_issue/20_현업확인_요청_조각/20_현업확인_요청-010.md` 헤더

---

### ▣ O180-B-2 🔴🔴 이 단위가 낸 사고와 그 복구 (숨기지 않는다)

**무엇이 일어났나** = `declared_kind` 1차 구현이 `doc_type_gate.read_registry` 를 불렀다
(정답 = **`parse_registry`**). 그리고 **같이 넣은 `except Exception: return None` 이 그
`AttributeError` 를 삼켜** 전건 `None` ⇒ 가드 분기가 건너뛰어졌고, 「거부되는지 확인」하려고
돌린 `--rebalance` 가 **문서50 에 실제로 집행됐다.**

📏 **피해 실측 (즉시 전수)**

| 축 | 결과 |
|---|---|
| 조각 수 | **23 → 24** 재기록 |
| 본문 | 🟢 **바이트 동일**(concat SHA256 일치) · 유실 **0** · 상한 초과 **0** |
| 좌표 인용 19건 | 🟢 **17건 정확 유지** · 🟠 2건 빈 줄 |
| 그 2건 | 🟢 **내 사고가 아니다** — 이전 세션 재균형이 이미 깼다(`-014:180`→현재 `-017:180`) |
| 부수 효과 | 🟢 문서50 최대 조각 **327줄 → 203줄**(여유 회복) · `-024` 헤더가 **도구 생성 형태로 정규화** |
| 🔴 내 피해 | 이번 세션에 쓴 `-023` 인용이 `-024` 로 밀렸다 ⇒ **4곳 전수 정정 완료** |

🟢 **역설** = 그 사고가 가드를 정당화했다 — 좌표가 **이전에도 깨져 있었고 게이트가 못 잡았음**을
같은 조사에서 발견했다(`doc_coord_gate` 는 경로만 본다).

---

### ▣ O180-B-3 🟠 다음이 할 일 — ㉠ 착수 실측이 끝난 것부터

> 🟢 **I2 는 실측·처방까지 끝났다** — 집행만 남았다(가장 싼 항목).

| # | 항목 | 착수 실측 결과 |
|---|---|---|
| **1** | 🟠 **I2 MUTATES 무플래그 11건 처분** | 🟢 전건 실재 · 전건 무플래그 확인. 진입점 = `main()`+`__main__` **8건** / 🔴 **최상위 실행 3건**(`_o169_sv_redeploy`·`_o170_reserve`·`_o170_handoff_append` ⇒ **실행 = 즉시 집행**이라 가장 위험). 🟢 처방 = **공용 `--apply` 드라이런 가드**(`scripts/mutating_guard.py` LIB 신설 후 11곳 3줄 삽입) ⇒ 기지목록을 **0** 으로. 🔴 분류 재판정 = `nl_routing_smoke` 는 DDL 은 없지만 **LLM 대량 호출(과금)** 이라 `R4-4-3` 대상이 맞다 ⇒ **MUTATES 유지**(OBSERVE 로 내리지 마라) · `gen_o53_ad_combined` 는 LIVE-DDL 보유 ⇒ MUTATES 유지. 🔴 목록에서 **이름을 빼는 것이 종결**이고 추가는 금지다 |
| **2** | 🟠 `jinja_config_gate` **축5 를 models 한정으로 좁힐지** | 경고 8건 전부 `macros` 정상 관용구(오탐) |
| **3** | 🟠 `jinja_config_gate` **축3 2건 처분** | `{% macro %}` 정의 앞 문서 주석 ⇒ 미렌더·실해 0 |
| **4** | 🟠 `SILVER_2.CRM_MEMBER_DEV` **`GRANT UPDATE` 처분** | merge 근거 소멸 · 🔴 REVOKE 는 `R4-4-3` 승인 대상 |
| **5** | 🔴 **`--vars` 가 무시되는 원인 미규명** | 현상만 실측됐다. `vars:` 선언 부재인지 Snowsight 가 인자를 못 넘기는지 **원인 축이 다르고 처방도 다르다** ⇒ `--vars` 의존 설계 전수 점검 |
| **6** | 🟠 **`doc_coord_gate` 에 「줄 내용」 축을 넣을지** | 🆕 O180-B 발견 — 경로만 봐서 **재균형이 깬 좌표를 못 잡는다**(실물 2건). 🟢 판정식 후보 = 인용 줄이 **빈 줄이면 경고** |

---

### ▣ O180-B-4 ⚪ 미착수 — ㉡ 워크스페이스 백로그 (무변경 승계)

> 🔴 `-O0180-A` ▣3 의 **B2~B12 가 그대로 유효하다**(B1 = 이 단위가 종결했다).

| # | 항목 | 정본 좌표 |
|---|---|---|
| **B2** | 🔴 **열린 문항 28건 전수 `J3` 실측** — O179 는 3건만 표본 실측 | 문서20 조각 11개 · 판정식 = `**판정**:` 공란 절 |
| **B3** | 🔴 **M-4 문안 정정** — 라벨 문제가 아니라 **코드 미적재**(`SEND_RESULT_CD` 전건 NULL) | 문서20 §M-4 |
| **B4** | ⚠️ 문서20 `-001:167` `**판정**` 줄 **2개**(문서 결함) | 문서20 `-001.md:167` |
| **B5** | 🔴 **회신 대기 6건** = §D-2 · §W-1 · §N-19 · §N-20 · §N-21(불요) · §N-22 | 문서20 |
| **B6** | 🟠 **§N-13 grain 3종 병기 정리** — 착수표 ⑭ 닫기 전 O160 산출식 **분모 대조** | 문서20 §N-13 |
| **B7** | 🟠 **W3 결정·설계 대기 8건** — GOLD 6개(`-017:180`) · 순서9-C(`-018:36`) · O91-F 2(`-019:4`) · O99 SV 3 · 스냅샷 11종 · check_cols RM · G-5 identity 2 · PM040 🔴 **좌표는 재균형으로 밀렸을 수 있다 — 절 제목으로 찾아라** | 문서50 |
| **B9** | 🔴 **W1 배선하지 마라** — 실측 기각(1 UTM ↔ 최대 902 캠페인) · §W-1 회신 선행 | 문서20 §W-1 |
| **B10** | 🟠 `02_상태상세_대시보드_갱신형` 에도 증분형 판단을 적용할지 = **사용자 미결** | 원장 §0 |
| **B11** | 🔴 `TASK_DW_*` **파이프라인 Task 부재**(사고 위험 잠재) · `BRONZE_GA4` 현황 유지 | O178 실측 |
| **B12** | 🟠 `_BATCH_ID` **50건** `_STDR_YM` 동일 class 판정 대기 | 문서50 · O175-B |

---

### ▣ O180-B-5 📏 종료 기준선 (2026-09-22 · 🔴 인용하지 말고 재라)

- 게이트 **7종 rc=0** = `doc_census`·`doc_type_gate`·`doc_coord_gate`·`doc_heading_gate`·
  `index_row_gate`·`clause_order_gate`·`jinja_config_gate`
- 🆕 `test_doc_type_increment` **19/19** · 스킬 검증기 rc=0(`I1`~`I8` · 420줄 35,347 B)
- `declared_kind` 5축 = 문서20 **증분형** · 문서50 **증분형** · 이력 **append형** ·
  원장 **갱신형** · 해소완료 **append형** (🔴 종전 전건 `None`)
- 가드 실증 = 문서50·문서20 `--rebalance` **rc=1 거부** · 타 유형 3종 비적용
- 문서50 조각 **24** · max **203줄** · 문서20 조각 **11** · max 297줄 ·
  이력 조각 **82**(최소 여유 **2,735 B** 🟠)
- 원장 행 = O180-B **1,453자** · O180 **1,525자**(둘 다 ≤1,900 · 파이프 5)
- 🟠 잔여 경고 = `doc_type_gate` 여유부족 2 · `doc_coord_gate` 사람판단 1 ·
  `jinja_config_gate` 오탐성 10 · `decision_closure_gate` 2 — 전부 blocking 아님
- 🔴 **라이브(dbt·SILVER·GOLD) 기준선은 `-O0180-A` ▣5 가 정본이다** — 이 단위는
  문서·도구만 만졌고 **라이브를 재조회하지 않았다**(그래서 여기 옮겨 적지 않는다 · `R2-8-4-d`)

🔴 **확정위반 2건** = ㉠ **가드 구현 결함으로 보호 대상에 파괴 연산을 집행**(조각 23→24 · 복구 완료)
㉡ **`edit` 컨텍스트 미리보기를 위치 근거로 읽어 정상 편집을 사고로 오진**(`grep` 으로 즉시 정정).
🟢 `R4-4-2` ㉡ 분기 적용 · 파괴 작업은 승인을 받았다(`_scratch_*` 제거).

_Co-authored with CoCo_
