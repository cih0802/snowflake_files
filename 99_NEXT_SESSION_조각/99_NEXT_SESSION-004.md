<!-- SPLIT-CHUNK 99_NEXT_SESSION.md | 004/028 | 허브 = 99_NEXT_SESSION.md | 원문 548~693행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## ~~0-DDDD. 🔴🔴 [2026-08-31 O126 — ~~여기서 시작한다~~ · §0-EEEE 로 승계됐다]~~


> 🟢 **절차 불변** = `export SESSION_LABEL=O1NN` → 원장 §1 선점(`R1-4-3`) →
> `gate_census.py --run-tests`(🔴 rc 는 리다이렉트로) → `session_brief.py --write` → `00_BRIEF.md` 1회 `read`.
> 🔴 **이 절은 좌표만 운반한다** — 정본 = 이력 **§O126**.

### ▣ DDDD1 🔴🔴 먼저 알아라 — 「문법이 유효하다」와 「의미가 옳다」는 다른 판정이다

O126 은 GOLD ERD 생성기를 만들었고, **초판이 `DIM_MONTH` 를 관계선 0개의 고립 섬으로 그렸다.**
🔴 그 초판은 **Mermaid 문법 38/38 유효** · FK 병합 산술 정확 · 게이트 전부 통과 상태였다.
⇒ **틀린 것은 「무엇을 세는가」였다.**

원인 = 월 conform 축 **7건**이 dbt `relationships` 에도, 물리 FK 에도 **둘 다 없다**
(`06_DDL` [관계 제약] 이 비유일 참조로 보아 선언 보류 + yml 테스트 미작성)
⇒ **두 소스 합집합으로도 잡히지 않는다.**

⚠️ **왜 최악인가** — `DIM_MONTH` 는 **fan-out 차단 차원**이다. 그 컬럼 COMMENT 가 🔴🔴 로
「월 팩트는 `DIM_DATE` 를 직접 조인하지 말라 — 월당 일수만큼 증폭된다」고 경고하는데,
초판 ERD 는 `DIM_MONTH` 를 아무것과도 연결하지 않고 `FACT_MEMBER_FEE → DIM_DATE` 만 그렸다
⇒ **독자가 설계가 막으려던 그 조인을 하도록 유도**했다.

🔴 **이것은 §0-CCCC `CCCC1`(「객체가 다 있다」≠「설계대로 작동한다」)의 재현이다.**
⇒ 🟢 **판정식 = 산출물이 「무엇을 보증하고 무엇을 보증하지 않는지」를 스스로 선언하게 만들어라.**

### ▣ DDDD2 🟢 O126 이 남긴 도구 3종 — 쓰는 순서가 정해져 있다

| 순 | 도구 | 역할 |
|---|---|---|
| ① | `scripts/gold_erd_coverage_gate.py` | FACT 키 컬럼의 **고립 분류 강제**. 🔴 **먼저 통과시켜라** |
| ② | `scripts/gen_gold_erd.py` | ERD HTML 발행(3중 합집합). 미분류 고립이면 **`return 1` 로 차단** |
| ③ | `scripts/test_gold_erd.py` | 오염 기반 음성 테스트 + 산출물 회귀 가드 |
| ⚪ | `scripts/test_gold_erd_mermaid.py` | 문법 축(🔴 **의미 축과 일부러 분리**했다) |

🔴 **「고립 0」을 목표로 삼지 마라** — degenerate key 는 관계가 없는 것이 설계다.
목표는 **「모르는 고립 0」**이고, 분류는 `DEGEN` / `CONFORM`(ERD 에 논리 관계 표기) / `SELFREF` 다.
🔴 **`CONFORM` 을 `DEGEN` 으로 잘못 적으면 그 축이 ERD 에서 영구히 사라진다.**

### ▣ DDDD3 🟠 O126 이 남긴 신규 착수 항목 4건

| 순 | 항목 |
|---|---|
| ㉞ | 🟠 `_gold_ready_schema.yml` **`relationships` 18건 미작성** — 물리 FK 에만 있는 축. 추가하면 dbt 가 실제 검증한다(현재 ERD 표기만 되고 **검증 안 됨**). 🔴 추가 전 **위반 행 수 실측** — `FACT_TARGET_BIZ` 0행은 공허 통과 · 이름 기반 `ORG` 축은 미매칭 예상 |
| ㉟ | 🟠 **FACT 11개 물리 PK 미선언**(DIM 20/20 · FACT 6/17). `06_DDL` 실행규칙 8 과 정합 ⇒ **결함 아니다**. 🔴 다만 ERD 가 물리 PK 부재 시 **dbt 추론 PK 로 대체 표기**한다 ⇒ 그것이 「grain 확정」으로 읽힐 위험을 판정하라 |
| ~~㊱~~ | ~~🟠 **산출물이 Mermaid CDN 의존**~~ → 🟢 [2026-09-02 O132 완료] `/libs/mermaid@10.9.6/mermaid.min.js` 로컬 번들 사용으로 외부 네트워크 의존성 완전 해소 |
| ㊲ | 🟠 **커버리지 게이트가 FACT 만 본다** — DIM→DIM 참조(`DIM_CAMPAIGN.ORG_SK` 등)는 **고립 판정 분모 밖**이다. 🔴 「0건」이 「없다」가 아니라 **「내 판정식이 못 본다」**일 수 있다(`O111 ㉠`)의 실물 후보 |

### ▣ DDDD4 🔴 O126 확정위반 4건 — 같은 실수를 반복하지 마라

· **브리핑 미출력**(`A5` · `R4-4-1`) — 초판은 `init_ihcho` 없이 착수했다.
  🔴 **그 미독이 `DDDD1` 의 원인과 연결된다** — `CCCC1` 이 바로 그 함정을 경고하고 있었다.
· **라벨 선점 누락**(`R1-4-3`) — 검토 지시를 받은 뒤에야 `O126` 을 선점했다.
· **`line_len`·`cortex ws ls` 사후 실행**(`R1-5-4`·`R2-5`) — 통과했으나 **늦게 돌렸다**
  (「게이트를 언제 돌리는지가 게이트의 성능이다」).
· **`edit` 앵커 부족으로 `def eff_pk` 정의줄 덮음**(`R1-7-8`) — 즉시 복구·`py_compile` 확인.
  🔴 조문이 말한 그대로다: **앵커는 직전 줄까지 포함하라.**

🟠 **`R4-4-4` 롤백 근거**: 이 세션은 `R4-4-2 ㉡`(지시 동봉 즉시 수행) 분기로 진행 · 확정위반 **4건**.

---

## ~~0-CCCC~~. 🔴🔴 [2026-08-31 O125 · O125-B — ~~**여기서 시작한다.**~~ 🟢 **§0-DDDD 로 승계됨(O126)**]

> 🟢 **절차 불변** = `export SESSION_LABEL=O1NN` → 원장 §1 선점(`R1-4-3`) →
> `gate_census.py --run-tests`(🔴 rc 는 리다이렉트로) → `session_brief.py --write` → `00_BRIEF.md` 1회 `read`.
> 🔴 **이 절은 좌표만 운반한다** — 정본 = 이력 **§O125**(작업) · **§O125-B**(자기검토).
> 🔴 **이 조각에 넣은 이유** = `-002`(§0-BBBB 조각)가 **292/300줄**로 새 절을 담을 수 없었다.

### ▣ CCCC1 🔴🔴 먼저 알아라 — 「객체가 다 있다」와 「설계대로 작동한다」는 다른 판정이다

O125 는 SILVER·GOLD·SERVING 의 **객체 실재 + 컬럼 이름 집합**을 대조해 **미생성 0 · 불일치 0** 을 얻었다.
🔴 **이것을 「데이터웨어하우스가 정상」으로 읽지 마라.** 같은 세션 재실측(조회 시점):

- `FMM.CAMPAIGN_SK`·`PAYMENT_SK` **전건 0** · `FSE.CAMPAIGN_SK` **전건 0** ·
  `FEP.CAMPAIGN_SK`·`SPONSORSHIP_SK` **전건 0** · `FBD.CAMPAIGN_SK`·`ORG_SK` **전건 0**
- ⇒ **캠페인 축은 5개 팩트에서 지금도 전건 0** 이다(정본 = 문서50 **`BLOCKING-5`** · 열림).
- 0행 테이블 **2건** = `GOLD.FACT_TARGET_BIZ` · `SILVER.CRM_BIZ_TARGET`(원천 미입고 `E-6`).

🟢 판정 도구 = `python3 scripts/o125_layer_census.py` — **자기가 판정하지 않는 축을 머리말에 선언**하고
말미에 **값 적재 축 [관측]** 을 함께 낸다. 🔴 **수치를 문서에서 읽지 말고 이 도구로 재라**(`R3-9 ㉦`).

🔴🔴 **SERVING 도 같은 구조다 — 「전건 실재」와 「틀린 값 발행 중」이 동시에 참이다.**
`SV_BUDGET.TOTAL_EXEC_BUDGET` 이 **+79.7% 과대값을 발행 중**이고 정본은 `05_SV-Agent_ai/04_SV_설계.md`
**§0.9-D**(`DEC-44` · **사람 결정 대기** · 회계 정책)다. 🔴 **배선·metric 삭제·필터 추가 금지**(승계 유효).
🔴 **배포 상태를 인용할 때는 §0.9 만 인용하라** — O125 초판이 이 정본을 우회해 결손을 보고에서 빠뜨렸다.

### ▣ CCCC2 🟢 O125-B 신설 판정식 3조 (승계 · 성격 불변)

㉨ 🔴🔴 **실재 판정과 값 판정을 한 문장에 담지 마라** — 「생성됐다」는 「채워졌다」를 함의하지 않는다.
  도구는 **판정하지 않는 축을 스스로 선언**해야 한다. 선언이 없으면 사용자는 ✅ 를 전체 판정으로 읽는다.
㉩ 🔴 **분모를 파일 파싱으로 만들기 전에 「그 분모의 정본이 이미 있는가」를 물어라** —
  있으면 정본을 인용하고, 만들었으면 **정본 좌표를 출력에 실어라**.
㉪ 🔴 **표의 「전건 0」 열은 부분 적재를 표현하지 못한다** — 한정 표기(`(DEV 한정)`)가 없는 셀은
  재실측 대상이다. 실제로 `BLOCKING-5` 표의 **2셀**(`FMM.REASON_SK` 7.90% · `FME.ORG_SK` DEV 한정)이
  그렇게 낡아 있었고 이번에 취소선 + 실측 병기로 정정했다.

### ▣ CCCC3 🟠 O125 가 남긴 신규 항목 · 승계 미결

- 🆕 착수표 **㉜ 완료**(dbt yml 미등재 7모델 등재 · 사용자 `build` 로 grain 가드 전건 통과) ·
  🆕 착수표 **㉝ 신설** = 루트 잔존 원고 `_o114b_dec44_entry.md` 미이관분 판정(포함율 78%).
- 🆕 🟢 **[O125-E · 승인 ㉠] 원장 §1 여유를 은퇴로 회복했다** = **7,706 → 12,139 B**(경고 1 → 0) ·
  닫힌 `O124`·`O124-B` **장문 셀만** `02_상태상세` 로 이관(행 키·열 수 불변 · 스냅샷 2건).
  🔴 **㉡ `--rebalance` 는 실행하지 않았다** — 필요해지면 **은퇴를 먼저** 쓰라(㉫).
- 🆕 🔴🔴 **다음 세션 최우선 과제(사용자 지시) = `20_issue/` 문서 일자 최신화**(아래 ▣ CCCC4).
- 🟠 DDL 선언 **순서 드리프트 2건**(`DIM_MEMBER_ACQUISITION`·`FACT_EVENT_PARTICIPATION`) —
  `table_ddl_column_gate` advisory · **적재 위험이 아니다**(dbt insert 는 양측 이름 지정).
- 🔴 착수표 **② ④ ⑤ ⑦ ⑪ ⑫ ⑭ ⑰ ⑱ ㉙ ㉚ ㉛** 는 **O125 계열이 손대지 않았다**(성격 불변) ·
  **㉛(개명 판정식·경계 2건)은 여전히 사용자 답변 대기**이고 그 전에 `ALTER … RENAME COLUMN` **금지**.
- 🔴 파괴 가능 연산(`rm` · discard · `--rebalance` · `--rollover` · `retire_*`)은 **별도 승인**(`R4-4-3`).

### ▣ CCCC4 🔴🔴 [사용자 지시] `20_issue/` 일자 최신화 — 도구로 판정하라

🔴 **이 절은 제목을 하나만 쓴다** — 허브가 100% 색인이라 제목 1개가 여유 약 70 B 다(O124-B `D5`).
**요구** = 「`20_issue/` 폴더 문서들을 2026-08-30 일자로 최신화 · **파이썬 스크립트처럼 고정된 재현 가능한
방법을 선호**」. ⇒ 🔴 **손 편집은 금지에 가깝다** — 이 워크스페이스는 손으로 적은 수치가 **3곳에서 17건
stale** 이 된 전력이 있다(`R3-9 ㉦`).

🟢 **착수 전 실측(O125 관측 · 재현 = 아래 도구를 만들어 다시 재라)** — 파일명 정규화 기준 문서군 **24**,
최신 날짜 토큰이 `2026-08-30` 미달 **13**:

| 문서 | 최신 날짜 토큰 | 🔴 성격 판정(먼저 해야 할 일) |
|---|---|---|
| `41_입고요청서_CRM_BIZ_TARGET_BRONZE_DDL` | 2026-04-10 | **동결 정적** — 날짜는 예시 데이터다. 갱신 대상 아님 |
| `41_입고요청서_CRM_BIZ_TARGET` | 2026-07-20 | **정적** — 「2026-07-20 원천 정정」은 **사실 기록**이다. 갱신 아님 |
| `31_코드군_매핑등재부` | 2026-08-11 | 갱신형 — 실제 stale 후보 |
| `03_이슈상세` | 2026-08-14 | 갱신형 — 이관부 · 상위 문서 변경 반영 여부 확인 |
| `40_입고대기_원천의존` | 2026-08-18 | 갱신형 — `E-6` 등 블로커 상태 재확인 |
| `90_해소완료_로그` | 2026-08-20 | append형 — 은퇴 이관이 있었으면 늘어난다 |
| `20_현업확인_요청` | 2026-08-26 | 갱신형 — 현업 회신 대기 항목 재판정 |
| `91_사고사례집` | 2026-08-28 | append형 — O124~O125 사고 서사 미이관분 확인 |
| `_o114_measure`·`_o115_measure`·`_o116_warn_census`·`_o118_sv_live_census`·`_o114b_silver_gold_impact` | 2026-08-29 | 🔴🔴 **근거철 = 정적 · 발행 후 갱신 금지** ⇒ **절대 「최신화」하지 마라**(원장 §0 `_o1….md` 행) |

🔴🔴 **판정식을 먼저 세워라 — 「날짜 토큰이 낡았다」 ≠ 「내용이 stale 하다」.**
위 13건 중 **7건은 정적·append형이라 갱신 대상이 아니다.** 날짜만 보고 고치면 **정적 문서를 오염**시킨다.

🟢 **착수 형태(권장)**
1. `scripts/doc_date_census.py` **신설** — ㉠ 문서별 최신 날짜 토큰 ㉡ 원장 §0 등재표의 **유형**(갱신형/
   append형/정적)을 조인해 **「갱신형인데 낡은 것」만** 판정 축으로 내고 정적·append형은 **관측 버킷**으로 분리
   (`O111 ㉢` = 판정 축과 관측 축을 쌍으로 낸다). ㉢ `--strict` 로 exit 1.
2. 그 판정 목록에 대해서만 **내용 재확인 → 정본 갱신**. 🔴 갱신은 **조각을 `edit` → `--republish --expect`**.
3. 음성 테스트 `scripts/test_doc_date_census.py` — 오염 기반 축(정적 문서를 판정 축에 넣으면 FAIL).
4. 🔴 **날짜를 일괄 치환하지 마라** — `fix_stale_counts.py` 선례처럼 **포인터로 대체**하거나
   **취소선 + 실측 병기**가 정답이다(`§C24`).


