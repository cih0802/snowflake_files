<!-- SPLIT-CHUNK 99_NEXT_SESSION.md | 002/024 | 허브 = 99_NEXT_SESSION.md | 원문 95~294행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## 0-BBBB. 🔴🔴 [2026-08-30 O124 · O124-B 필독 — **여기서 시작한다.** §0-AAAA 는 승계됐다]

> 🟢 **절차 불변** = `export SESSION_LABEL=O1NN` → 원장 §1 선점(`R1-4-3`) →
> `gate_census.py --run-tests`(🔴 rc 는 리다이렉트로) → `session_brief.py --write` → `00_BRIEF.md` 1회 `read`.
> 🔴 **이 절은 좌표만 운반한다** — 정본 = 이력 **§O124** · 근거철 **`20_issue/_o124_evidence.md`**(E0~E2).

### ▣ BBBB1 🟢 O124 가 닫은 것 — 다시 열지 마라

- `decision_closure_gate` **미봉합 34 → 0**. 🔴 「닫았다」가 아니라 **「셀 것이 아니었다」**다 —
  34건 **전건 오탐**이고 원인은 판정식이다 ⇒ 제외 5축 신설(A 괄호스코프 **15** · B 미확정절/표행 **15** ·
  C 적발표 **4** · D 허브 · E 근거철). 정본 = 그 스크립트 축A~E 주석 + `_o124_evidence.md` §E1.
  회귀방어 = `test_decision_closure_gate.py` **46단정**(오탐축+재현율축 쌍 · 진성 쌍 `DEC-41 ▸ O8` 은 그대로 잡힌다).
- 골든 `UNLABELED` **0** — `scripts/golden/outputs.json` history 2건에 `label`+`label_note` 병기
  (🔴 발행 재실행 **0** · `metrics`·`reason` **무변경** · 라벨 1개만 채웠다).
- 산출물 생성기 **1종 신설** = `gen_unresolved_issue_summary.py` → `30_output_share/11_미해결이슈_요약.md`
  (현업 **42** · dbt **18** · 착수표 **13**) · 음성 테스트 **40단정**. 🔴 `미해결이슈_요약_O102.md` 는
  **은퇴 표기만** 했다(지우지 않았다 · stale 2행은 취소선 + 병기로 닫았다).
- stale 최신화 = `01_DW_현업활용가이드` 부록 B **8행**(`_archive` → 루트 2026-08-30 판) +
  WIDE 표 **재집계**(`04` CSV `상태` 열 전수 = 565 · OK 127 · PARTIAL 343 · WAIT 95) ·
  `10_원천입고_결손요약` 머리말에 **09 판정 분포 12종**(합 507) 병기.
  🔴 분모가 548→565 로 함께 커졌으므로 `WAIT` 108→95 를 **「개선 13건」으로 읽지 마라.**
- 착수표 **⑨ 부분수행** — 기재 20 은 **stale**, 실측 **10 → 5**. 잔여 5는
  `gen_section_assembly.code_cell()` 로 **근본 시정**했고 **09 재생성 1회**로 0 이 된다.

### ▣ BBBB2 🔴🔴 O124 가 거부한 처방 1건 — 같은 지시를 그대로 실행하지 마라

> 🆕 🟢🟢 **[2026-08-30 O124-B] 이 항목은 해소됐다 — 다시 열지 마라.**
> O124 는 처방이 무효임을 실증하고 3안을 사용자에게 넘겼다. **O124-B 가 승인을 받아 ㉮ 를 실행했다.**
> · 🔴 먼저 자기신고 = 여유 **6,994 → 6,468 B** 로 **O124 자신이 악화**시켰다(인수인계 절이 색인을 키웠다).
> · 🟢 판정식 = **「변별 붕괴 0」**(서로 다른 절 제목이 같은 문자열로 잘려 조각을 못 고르게 되는가).
>   실측 = 허브 **11종** · 절+`▣` 제목 **967개** · `TITLE_MAX` **70·60·55·50·45·40·35 전부 붕괴 0**.
> · 🟢 `split_doc.TITLE_MAX` **70 → 50** + 허브 11종 재발행 ⇒ `99_NEXT` **34,492 → 32,230**
>   (여유 **8,730 B · 21.3%**) · **`doc_type_gate` 여유 부족 경고 1 → 0** ⇒ 판정 **🟡 → ✅**.
> · 🔴 **45 이하로 더 내리지 마라** — 붕괴는 0 이지만 사람이 제목을 **읽고 고를** 여유가 사라진다.
> · 🟢 회귀방어 = `test_split_doc_toc.py` **「변별 붕괴」 축**(음성으로 `TITLE_MAX=4` 를 넣어 축 생존까지 단정).
> · 🔴 **`--rebalance` 금지는 그대로 유효하다** — 조각을 늘려 색인을 **키운다**.
> ⇒ 🔴 아래 원문은 **경위 기록**이다. 「후보 3안」은 이미 결정됐으니 다시 고르지 마라.

- 🔴 **경위·실측·후보 3안의 원문은 이력 §O124-B `D5` 에 있다 — 여기 다시 펼치지 마라**(프롬프트는 좌표만 운반한다).
- 한 줄 요약 = 「허브는 100% 자동 색인 · 승계 절 본문은 조각에 있다 · `retire_sections` 는 제목을 안 건드린다
  ⇒ 은퇴로는 여유 회복 0」 이었고, 그래서 **색인 밀도 인하(㉮)** 가 채택·실행됐다.
- 🔴 `--rebalance` 를 쓰지 마라 — 조각을 늘려 색인을 **키운다**(인수 경고 그대로 유효).

### ▣ BBBB3 🔴 사용자 답변 대기 — 개명(b) 축

- O124 는 b 축을 **실행하지 않고 권장안·차선안만 제시**했다. 답이 오기 전 `ALTER … RENAME COLUMN` **금지**.
- 상세 = 이력 **§O124 「b 권고」** · 정본 = `32_컬럼개명표.md` §5·§7·§8 · 착수표 **㉙㉚㉛**.

### ▣ BBBB4 🟢 O124 신설 판정식 3조 (승계 · 성격 불변)

㉠ **「N건」 기재는 재실측 없이 인용하지 마라** — ⑨ 가 20→10 이었고 그 차 10건은
  「거짓」이 아니라 **「그 사이에 해소됐다」**였다. ⇒ 기재를 반박할 때도 분모를 먼저 세라.
㉡ **영구히 🟠 인 게이트는 판정식을 의심하라** — 오탐을 사람이 매 세션 다시 판정하는 것은
  판정식의 대체물이 아니다(`P130` 「항상 빨간 게이트는 무시된다」). 🔴 제외를 넓힐 때는
  **오탐 축과 재현율 축을 쌍으로** 단정하고 **제외분을 관측 버킷으로 출력**하라(`O111 ㉢`).
㉢ **자동 생성물의 결함은 생성기에서 고쳐라** — 산출물을 손으로 고치면 재생성에서 되살아난다.
  실물 = ⑨ 의 09 5건(`` `{값[:60]}` `` 절단이 코드 스팬을 깨 열수가 4→2 로 무너졌다).
㉣ 🔴🔴 **인수인계 절을 승계 표기한 뒤 브리핑을 재생성해 「현행 시작점」을 눈으로 확인하라.**
  실사고(O124 · 게이트 6종 🟢 상태) = §0-AAAA 를 **「제목 전체를 `~~` 로 감싸기」**로 표기했더니
  `session_brief.current_handoff` 의 승계 판정(**`~~여기서 시작한다` 인접**만 본다)이 그것을 **취소선으로 보지 않았고**,
  두 절의 **날짜가 같아** `max` 가 **승계된 절을 현행으로 골랐다**.
  🟢 관례는 **`~~여기서 시작한다.~~` 인라인 취소선**이다(§0-PPP·§0-NNN 형) — 이 형식을 쓰라.
  🔴 **판정식 = 「관례를 따랐다」는 자기신고이고, 도구가 무엇을 보는지는 별개다.**

### ▣ BBBB5 🟠 승계 미결 (O124 가 손대지 않았다 · 성격 불변)

- 착수표 **② ④ ⑤ ⑦ ⑪ ⑫ ⑭ ⑰ ⑱ ㉙ ㉚ ㉛** — 정본 = `99_NEXT_SESSION-020.md` 착수 순서.
- **② 는 사람 소관**(CoWork UI) · **⑫⑭ 는 정의·현업 결정 전 배선 금지** · **GA4 는 결정 ④ 로 범위 제외**.
- 🔴 파괴 가능 연산(`rm` · discard · `--rebalance` · `--rollover` · `retire_*`)은 지시가 동봉돼도 **별도 승인**(`R4-4-3`).

### ▣ BBBB6 🆕 🔴🔴 [O124-B] O124 자기검토에서 나온 판정식 3조 — **이 셋이 가장 비싸다**

㉤ 🔴🔴 **「없다」는 분모와 함께만 말할 수 있다** — **경로·폴더를 한정한 검사 결과를 「부재」로 쓰지 마라.**
  실사고 = O124 가 근거철 `_o123c_evidence.md` 를 **`20_issue/` 한 폴더만** 보고 「실재하지 않는다」고
  **문서에 발행**했다. 실제로는 **루트에 11,600 B · 166줄 · `## E0`~`## E11` 12절**로 있었다.
  🔴 **더 나쁜 것** = O124 는 같은 세션에서 `P132`·`O111 ㉠` 를 근거로 게이트를 고치고 남의 기재를
  「stale」로 판정했다 ⇒ **자기가 쓴 판정식을 자기가 어겼다.**
  🟢 판정법 = **전역 인덱스로 확인**하라(`doc_coord_gate.basename_index` 형). 못 했으면
  「없다」가 아니라 **「그 경로에 없다」**로 적어라.
㉥ **잘못된 좌표는 설명하려고 적을 때도 좌표다** — 오류 경로를 본문에 문자열로 인용하지 마라.
  실사고 = 오판 경위를 적으면서 그 틀린 경로를 적었고 `doc_coord_gate` 축1a 가 **blocking** 을 냈다(**옳은 동작**).
㉦ **코드가 통과해도 서술은 따로 검증하라** — 테스트는 **동작만** 보고 「무엇을 고쳤는가」는 보지 않는다.
  실사고 = `code_cell` 의 근본 원인 서술이 **방향이 거꾸로**였는데 음성 테스트는 전건 🟢 였다.
  🟢 처방 = 판정을 **도구에 물어보는 축**을 넣어라(축5 = `split_row` 직접 대조 ⇒ 자기신고 제거).
㉧ 🔴🔴 **「손상」은 재현되어야 손상이다** — 쓰기 직후 읽기 **1회**로 파일 손상을 단정하지 마라.
  실사고 = 골든 재발행 직후 `json.load` 가 **`Unterminated string`** 으로 죽고 `wc -c` 가 **145,165 B**
  를 냈다. 🔴 「깨졌다」로 판정해 **꼬리를 손으로 봉합하려던 순간** `R1-7-3` 을 이행했더니
  재측정 3회 전부 **146,158 B · 해시 동일 · JSON 정상 · `_updates` 62회 보전**이었다 = **torn read**(`OPS-3`).
  🔴 **복구 시도 자체가 유일한 실제 손상이 될 수 있다.** 🟢 판정 = 2~3회 재읽어 **해시 대조** 후에만 손상이라 부른다.

## ~~0-YYY. 🔴🔴 [2026-08-29 O118 필독 — **여기서 시작한다.** §0-XXX 는 승계됐다]~~ → 🟢 **[O119 승계] §0-ZZZ 로 이관**

> 🟢 **세션 시작 절차 불변** = `python3 scripts/session_brief.py --write` → `20_issue/00_BRIEF.md` 1회 `read`.
> 🔴 **`export SESSION_LABEL=O1NN` 을 착수 직후 먼저 하라**(`R1-7-10`) ·
> 🔴 **라벨은 착수 전에 원장 §1 에 선점 등재**(`R1-4-3` · O112~**O118** **8세션 연속**).

### ▣ YYY1 🔴🔴 먼저 알아라 — **이 계정에 SV 10종이 실재한다. §0-XXX 의 「SV 0」은 stale 이다**

`show semantic views in account` = **10행** · 전건 `GN_DW.SERVING` · base 전건 `GOLD` ·
`created_on` **2026-08-29 05:59:12~05:59:46** ⇒ **O117 측정 이후에 배포됐다.**
🟢 `SERVING` `USAGE` 도 **4역할 부여 완료**(06:06) ⇒ §0-XXX ■A 의 「미배포·선행조건」 서술은 **전건 stale**.
🔴 **유효 승계** = `show agents in account` **0행** · `ML` `USAGE` **미부여** · ML SV·SERVING 뷰 **0**.
🟢 **배포 상태 정본 = `05_SV-Agent_ai/04_SV_설계.md` §0.9**(O118 신설 A~G) · **개수는 §0.9-B 쿼리로 재라.**

🔴🔴 **왜 「0건」이 나왔나 — 두 원인이 겹쳤다**: ㉠ **시점**(측정 후 배포 · `P168`)
㉡ **판정식** — **semantic view 는 `INFORMATION_SCHEMA.TABLES` 에 없다** ⇒ 「스키마가 비었다」는
애초에 근거가 될 수 없었다(`SERVING` 테이블·뷰 **0** 과 SV **10** 이 **동시에 참**).
⇒ 🔴 **`R2-8-4-d` 는 양방향이다** = 「완료 기재」도 「**부재 기재**」도 라이브 근거가 아니다.

### ▣ YYY2 🔴🔴 최우선 — `DEC-44` 는 이제 **의미 계층에 노출된 오류**다 (긴급도 상승)

`SV_BUDGET.TOTAL_EXEC_BUDGET` 이 **99,006,005,048**(정합 **55,094,546,653** · **+79.7%**)을 발행 중이다.
🔴 **차단조항이 지목한 객체가 틀렸다** — 「`FACT_BUDGET_YEARLY` 에 붙이지 마라」였고 라이브 base 는
`FACT_BUDGET`(월)이라 **문자적으로는 준수**됐으나 **두 팩트 집행액 합이 완전 동일**(차이 0)이다.
🟢 **Agent 가 0 이라 오답 경로는 미개통** ⇒ 🔴🔴 **Agent 배포 전에 이 결정을 닫는 것이 정답 순서다.**
🔴 **여전히 사람 결정**(회계 정책) — 정본 = 문서30 **§30-F**(판정 · O115) + **§30-G**(O118 보강).
🔴 **배선·metric 삭제·`where`·차수 필터·`severity` 승격 금지**(O118 도 전건 미실행).
🟢 **실측이 지지하는 방향** = **차수를 키·축에 보존하고 합산에서만 배제**(편성 상이 **17/24** 이므로
차수를 빼면 정보가 사라진다). 🔴 「키에서 빼라」와 「합산하지 마라」는 **다른 처방**이다.

### ▣ YYY3 🟢 O118 이 한 것 — **차이 3축 분해**가 정본이다(§0.9-C)

**축① 설계 변경**(라이브가 옳다 ⇒ **문서를 고친다**) 5건 · **축② 문서 오기**(서술만 틀림 ⇒ **문장을 고친다**) 5건 ·
**축③ 라이브 결손**(⇒ **라이브·결정을 고친다**) 5건 = O118 은 ①② 만 집행하고 ③ 은 **표기만** 했다(승인·회신 대기).
🔴 **축을 섞지 마라 — 처방이 다르다.**
🟢 고친 파일 = `04_SV_설계.md`(§0.9 + 사용지점 취소선·병기) · `05_0`(**`05_10` 행 누락 시정**) ·
`05_6`(**「보류」 선언**) · `08_AGENT_spec`·`00_README`(SV↔Agent 축 분리) · 문서30 §30-G.
🔴 **낡은 문안은 지우지 않았다** — 전건 **취소선 + 실측 병기**(O112 §C24).

### ▣ YYY4 🔴 다음 세션의 열린 과제 (O118 이 의도적으로 남긴 것)

㉠ 🟠 **`04_SV_설계.md` 에 하위 4 SV 구조 절이 없다**(`SV_MEMBER_COHORT`·`SV_MEMBER_FEE`·
  `SV_DEV_ACHIEVEMENT`·`SV_MEMBER_SPONSOR_BIZ`) — **라이브는 정상이고 문서가 결손**(§10 DoD 미충족).
  🟢 실측 구성은 §0.9-B 에 있으니 시작점으로 써라.
㉡ 🟠 **「SV 종수」 stale 약 50곳 중 축B(이력)·축C(ML·Agent 타축)는 손대지 않았다**(좌표 = 근거철 §12).
  🔴 **일괄 치환 금지** — 이력/타축/실적SV단정 을 가르고 **단정형만** 고쳐라.
㉢ 🟠 **§9 Cortex Search 후보 카디널리티 미재검증** — 실측 1건이 어긋났다(`DIM_BUDGET_ITEM` 2,041 → **180**
  ⇒ Search 불요). 🔴 나머지 행도 신뢰하지 마라 · **Search service 실재는 O118 이 미측정**이다.
㉣ ~~🔴 **`ML` `USAGE` 부여 · Agent 배포 · `A.7` REMOVE = 별도 승인 대상**(`R4-4-3`) · O118 미실행.~~
  🟢🟢 **[2026-08-29 O118-B 최신화] 사용자가 ML 축을 배포했다** — ML SERVING 뷰 **7종**(08:19) ·
  ML SV **7종**(08:20~08:21) · GRANT 전건 ⇒ **Semantic View 합계 17종**(실적 10 + ML 7).
  🟢 `sv_identifier_gate` **PASS**(미배포 드리프트 0) ⇒ **「죽은 도구 7개」 차단 종결.**
  🟢 `ML` 스키마 `USAGE` 는 **미부여지만 불요**다 — SERVING 뷰가 ML 을 캡슐화하고 소비 역할은
  그 뷰의 `SELECT` 를 갖는다 ⇒ **이슈 6 은 「소비 차단」이 아니라 「관리자 편의」로 강등**된다.
  🔴 **남은 미배포 층 = Agent 2종뿐**(`show agents in account` = **0**) · `A.7` REMOVE 는 승인 대기 유지.
  🔴 **`21_ML_SERVING_뷰_DDL.sql` 은 `head` 판본이 정본이다**(사용자 확정) — `ML_MEMBER_RISK_V` 의
  `spell` CTE 가 `ML.MBER_MONTHLY_INFO`(계정 부재) → **`SILVER.CRM_MEMBER_STATUS_HIST`** 로 교체돼 있다.
  🔴 **되돌리지 마라**(되돌리면 `does not exist` 로 실패). 등가·커버리지 실증은 그 파일 CTE 주석.
㉤ ~~🔴 **dbt 미검증 승계** = 다음 build 에서 **`TOTAL` 461 유지**되는가(`R4-1` · 사용자 실행)~~
  🟢🟢 **[2026-08-29 O118-B 종결] 사용자가 실행해 확인됐다** =
  `07:24:34 Done. PASS=425 WARN=36 ERROR=0 SKIP=0 TOTAL=461` ⇒ **461 유지 확정.**
  ⇒ 🟢 **O117 의 「테스트 선언 무변경」 단정(주석만 고쳤다)이 검증됐다** — 이 정지점은 **닫혔다**.
  🔴 **WARN 36 도 그대로다** ⇒ 문서50 §O116-6 의 「미규명 0」 상태가 유지된다(새 WARN 유입 없음).
  🟢 **부수 확증** = 전체 재구축 후에도 GOLD 채움률·SV 불변식이 **전건 동일**(§0.9-I 재측정표).
  🔴 **이 항목을 「미검증」으로 다시 승계하지 마라.**

### ▣ YYY5 🟠 승계 미결 (성격 불변)

`DEC-45` 회신 대기(§31 · 🟢 라이브 준수 = `SPNSR_BSNS_ID` 읽는 SV 요소 **0**) ·
`O8`(🟢 라이브 준수 = `FEP.SPONSORSHIP_SK` 비-0 **0행** · 후원사업 축 **0**) · `BLOCKING-1` 보류 ·
현업 질의 7 · `decision_closure_gate` 19 + `DEC-45` 오탐 15(조치 대상 아님) · 착수표 열린 항목 ·
`_o114b_silver_gold_impact.md` 대조 미실시.
🆕 ⚠️ **용량** = `01_세션이력` 조각 여유가 얇다 ⇒ 다음 `--rollover` 가 조각 신설·재균형을 유발할 수 있다
(`R4-4-3` 승인). 🔴 **수치는 `python3 scripts/doc_census.py` 로 재라.**

### ▣ YYY6 🔴 판정식 — O118 신설 4조

㉠ **SV 의 분모는 `SHOW SEMANTIC VIEWS` 뿐이다** — 스키마 객체 목록으로 SV 유무를 판정하지 마라.
㉡ **「부재 기재」도 라이브 근거가 아니다**(`R2-8-4-d` 양방향).
㉢ **차단조항이 객체명이면 그 결함값이 다른 객체에도 있는지 대조하라** —
  「그 테이블을 안 썼다」 ≠ 「그 결함을 피했다」.
㉣ 🔴🔴 **`SHOW GRANTS` 로 「부재」를 단정하기 전에 2~3회 재실행 + 반대 방향(`TO ROLE`)으로 대조하라 ·
  부재 판정은 실재 판정보다 약하다.** — O118 이 첫 반환 1행을 믿고 **거짓 이슈 직전까지 갔다**
  (재실행 5행 · `OPS-3`). 🟢 **살린 것은 교차검증이고 게이트가 아니었다**(`R3-9`).
🟢 **승계 유효** = `▣XXX6` · `▣WWW2`·`▣WWW6` · `O111 ㉠~㉢`.
🔴 **스모크 PASS 는 「값이 옳다」가 아니다** — 「base 를 왜곡하지 않았다」일 뿐(§0.9-D 가 실물 반례).

### ▣ YYY7 🔴🔴 [O118-B 자기검토] O118 초판이 틀린 것 3건 — **그대로 인용하지 마라**

O118 은 같은 세션 안에서 자기검토를 받아 아래를 정정했다. **초판 문안을 인용하면 틀린다.**

**㉠ 🔴 절차 결함 = SV 전용 게이트를 하나도 돌리지 않았다.**
문서 게이트 6종은 전부 🟢 였는데 **SV 게이트 3종은 전부 FAIL** 이었다(§0.9-H). 잡힌 실질 결함 =
· **Agent 스펙이 미배포 ML SV 7종을 도구로 참조**(배포 시 죽은 도구 7개 · `sv_identifier_gate`)
· **라이브 SV COMMENT 에 수치 7건**(`05_0` §공통규약 (1) 직접 위반 · `sv_unit_gate`)
· **Agent 스펙 간 「후원사업별」 가능/불가 모순 1건**(`agent_tool_claim_gate`)
⇒ 🔴 **판정식 신설** = **착수 시 `ls scripts/` 로 주제 영역 전용 게이트를 확인하라.**
  **문서 게이트 통과는 도메인 게이트의 대체물이 아니다**(`R3-9` 실물).

**㉡ 🔴 실측 없이 라이브 COMMENT 를 베껴 §0.9-C 를 틀리게 썼다.**
초판은 `SV_MEMBER_MONTHLY` 의 CAMPAIGN·SPONSORSHIP·PAYMENT **3종 제외에 하나의 이유**(「grain 이 깨진다」)를
붙였다. 실측하니 **이유가 둘**이었다 = `CAMPAIGN_SK`·`PAYMENT_SK` **전건 0**(적재 대기) ↔
`SPONSORSHIP_SK` **대부분 채움**(다중 후원 접힘 = 귀속 오류 · m:1 이라 fan-out 은 나지 않는다).
⇒ 🔴 **판정식** = **「제외 이유」를 문서·COMMENT 에서 베끼지 말고 그 컬럼의 채움률을 재라.**
  0 이면 **적재 대기**, 0 이 아니면 **의미(귀속) 문제** — 처방이 다르다.
🟢 라이브 COMMENT 자체도 같은 오류를 갖고 있었고 `05_1` DDL 원본에서 시정했다(재배포 대기).

**㉢ 🔴🔴 측정 중에 `dbt build` 가 라이브에서 돌고 있었다 — 수치의 재현성이 깨진다.**
`insert into GOLD.FACT_MEMBER_MONTHLY` **RUNNING**(07:21:59) · GOLD WIDE 뷰 07:22 재생성 ⇒
같은 객체가 두 조회 사이에 바뀌었다(`WIDE_MEMBER_FEE` **0 → 40,262,076** · `FACT_MEMBER_MONTHLY` **40,054,883 → 0**).
🔴 **O118-B 는 그 0행을 보고 「`SV_MEMBER_FEE` 가 아무것도 답하지 못한다」는 거짓 결함을 발행할 직전까지 갔다.**
🟢 살린 것 = **재조회 + `QUERY_HISTORY` 로 원인 축 확인**(`O111 ㉠`).
⇒ 🔴 **판정식 2조** = ㉠ **「0행」을 결함으로 단정하기 전에 빌드 진행 여부를 보라**
  (`QUERY_HISTORY` 에서 `execution_status='RUNNING'`) ㉡ **`INFORMATION_SCHEMA.TABLES.ROW_COUNT` 를
  행수 근거로 쓰지 마라 — 양방향으로 stale 이었다.** 행수는 `COUNT(*)`, 금액은 `SUM(<실컬럼>)`.
🟢 **핵심 발견(`DEC-44`)은 빌드 후 재검증에서 전건 재현**됐다 ⇒ 그 판정만은 환경 변동에 무관하다.
🔴 **재측정 대기** = `FMM.*_SK` 채움률 분모 · `WIDE_AD_COMBINED` 행수 · `SV_AD` 광고비 절대값(§0.9-I).

**㉣ 🟠 O118-B 가 남긴 것**(승인받았으나 의도적으로 미집행 · 근거 = §0.9-H ㉣)
· **SV 재배포 미실행** ⇒ 지금은 **「DDL 원본 = 규약 준수 / 라이브 = 위반」 드리프트** 상태다.
  🔴 빌드 종료 후 `05_1`·`05_10` 을 `GN_DW_ADMIN` 으로 실행하고 `sv_unit_gate` 가 **검출 0** 이 되는지 보라.
· **Agent 스펙 모순 미시정**(업무 판정 필요) · **ML 축 미배포**(순서상 `DEC-44` 뒤).


## ~~0-XXX. 🔴🔴 [2026-08-29 O117 필독 — **여기서 시작한다.** §0-WWW 는 승계됐다]~~ → 🟢 **[O118 승계] §0-YYY 로 이관**

> 🟢 **세션 시작 절차 불변** = `python3 scripts/session_brief.py --write` → `20_issue/00_BRIEF.md` 1회 `read`.
> 🔴 **`export SESSION_LABEL=O1NN` 을 착수 직후 먼저 하라**(`R1-7-10`).
> 🔴 **라벨은 착수 전에 원장 §1 에 선점 등재하라**(`R1-4-3` · O112~**O117** **7세션 연속** 이행 중).

### ▣ XXX1 🟢 상태가 또 바뀌었다 — **잔여 미규명은 0이고 열린 것은 결정 1건**이다

`dbt build` 성공 상태는 그대로다(`PASS=425 WARN=36 ERROR=0 SKIP=0 TOTAL=461` · 2026-08-29 03:59:59) · GOLD 소비 가능.
🟢 **O117 이 §O116-5 잔여 6종을 6/6 종결**했다 ⇒ 정본 = **문서50 §O116-6**(분모·원인 병기).
⇒ 🔴 「WARN 을 규명하라」·「잔여 미규명을 보라」류 지시는 **소진됐다** — WARN 36건 중 **미규명 0**이다.
🔴 남은 dbt 축 열린 항목 = **`DEC-45` 회신**(XXX2) · **`DEC-44` 회신** · `BLOCKING-1` 승격 보류 · `A.7` 승인.
🔴 **`04_..BRONZE_DDL` 재실행 금지**(`create or replace schema` ⇒ 적재분 전량 삭제) · `A.4`/`A.5`/`A.5-B` 재실행 금지.

### ▣ XXX2 🔴🔴 최우선 — `DEC-45` 는 **사람 결정**이고 에이전트가 더 할 일은 없다

정본 = **문서30 §31**(`DEC-45` · §31-A~§31-E). §0-WWW `▣WWW3` 의 프레이밍은 **두 곳이 틀렸으니 그대로 읽지 마라**:
· ~~「② 는 소비 계약이 바뀐다」~~ ⇒ 🔴 **실측 미지지** — `CRM_CAMPAIGN.SPNSR_BSNS_ID` 를 읽는 GOLD·SERVING 객체가
  **0건**이다(`DIM_CAMPAIGN` 에 후원사업 FK **부재** · `FACT_EVENT_PARTICIPATION.SPONSORSHIP_SK` 는 하드코딩 `0` 센티넬
  = `O8` 차단분) ⇒ ②안(브릿지)은 **파괴가 아니라 추가**다. **결정의 난이도가 내려갔다.**
· ~~「`CRM_PAYMENT_BILLING`(1)이 같은 축인지 먼저 보라」~~ ⇒ 🟢 **확인 완료 = 같은 축이다**(그 1행 값 `4,43` · 성분 전건 매칭).
  🔴 다만 **1 / 47,439,264 = 0.000002%** 라 설계 축이 아니라 **원천 오염 1건**이다 ⇒ `DEC-45` 에서 **분리**했다.
🔴 **결정 없이 배선·`where` 축소 금지** — 좁히면 18,091행이 관측에서 사라진다.
🟠 사람에게 물어야 할 것은 **2개뿐**이다(§31-B) = ㉠ 캠페인 1건이 후원사업 N개를 가리키는 것이 업무상 정상인가
㉡ 정상이면 그 N 을 분석에 노출할 것인가. 🔴 ㉠ 없이 ㉡ 를 정할 수 없다.

### ▣ XXX3 🟢 O117 규명 6건의 결론 (🔴 수치 인용은 **문서50 §O116-6** 에서 하라 · 분모 병기 필수)

| 대상 | 결론 |
|---|---|
| `RQEST_RST_CD` 1,096 | `SETLE_CD='8'`(**OCR**)에 **100% 집중** · 그 수단 고유 사유코드 **0종** ↔ 나머지 4수단 NULL **0.000%** ⇒ **구조적 부재** |
| `not_null MBER_NO` 745 | `MSG_AT` 741 + `EMAIL` 4 · **BRONZE 동수** · 공백 유래 인공 NULL **0** ⇒ **원천 NULL 전파**(마스터 재추출로 안 풀린다) |
| `warn_fep_nonnumeric_member_dk` 62 | 🔴 **두 성격 혼합** = 스캐너 30 + 정규 `S`+8자리 고아 **29** + 기타 3 ⇒ 「스캐너 62건」은 **과대 진술** |
| `CMPGN_CD` 관계 2종(18·4) | 🔴 **두 건이 아니라 한 축**(합집합 5종) · 원천 마스터에도 **0건** · 마스터 전량입고(36,163=36,163) ⇒ **결번** |
| `PART_STATUS` accepted_values 1 | 🟢 `OPS` **store_failures 에 답이 이미 있었다** = 값 `)` · 2행 ⇒ §O116-2 #29~31 과 **동일 사건** |
| `PAYMENT_BILLING SPNSR_BSNS_ID` 1 | 🟢 다중값 **동축**(위 XXX2 · 원천 오염 1건으로 분리) |

🟢 **신설 방법 1조** = **`store_failures` 가 붙은 테스트는 분모를 재기 전에 그 감사 테이블을 먼저 조회하라.**
🔴 **신설 경고 1조** = **「미규명」 표기를 그대로 믿지 마라** — 6종 전부 **건수는 이미 주석·헤더에 적혀 있었고**
빠져 있던 것은 **분모와 원인**이었다. 그 중 2종은 **읽기만 하면 닫히는 것**이었다(㉢ 테스트 헤더 · ㉤ 감사 테이블).

### ▣ XXX4 🔴 O117 이 코드에서 고친 것 — 다음 build 에서 확인할 조건

시정 = `_crm_schema.yml` **5곳 + 헤더 정책**(축 ㉤ `CMPGN_CD` · ㉥ `not_null` 계열 **신설**) ·
`tests/warn_fep_nonnumeric_member_dk.sql` **헤더**(분류별 실측 + 두 성격 혼합 적발).
🔴🔴 **검증 조건 = 다음 `dbt build` 의 `TOTAL` 이 461 로 유지되는지 확인하라.** 근거 = **주석만** 고쳤고
YAML 파서로 **모델 86 · 테스트 선언 351**(`_crm_schema.yml` 은 **20 / 117**)이 **편집 전후 동일**함을 단정했다.
🔴 **`TOTAL` 이 바뀌면 그 단정이 틀린 것이다.**
🔴 **dbt 명령은 에이전트가 실행하지 않는다**(`R4-1`) ⇒ 명령문만 제시하고 사용자 입력을 기다려라:
`dbt build --project-dir 10_dbt_pipeline`
🟢 **교훈(P154 축)** = O116-B 가 `SPNSR_BSNS_ID` 5곳을 고치면서 **`CRM_PAYMENT_BILLING` 1곳을 빠뜨렸고**
기각된 처방이 코드에 살아 있었다 ⇒ **축을 고칠 때는 같은 축의 다른 테이블 주석도 함께 `grep` 하라.**

### ▣ XXX5 🟠 승계 미결 — 성격이 바뀌지 않은 것 (§0-WWW·§0-VVV 를 그대로 읽어라)

`DEC-44` 회신 대기(문서30 §30 · 라이브 `SUM(EXEC)` **+79.7%** 과대 · 🔴 회신 전 SV·Agent 를 이 팩트에 붙이지 마라) ·
`BLOCKING-1` 승격 보류(탈퇴·삭제 원천 대조 미실시 · 🆕 **XXX3 의 `ALT_MEMBER_SCHEME` 29종이 같은 축이다**) ·
`A.7` REMOVE 승인 대기(2,643파일 39.01 GB · GOLD 소비 검증 후 권고) ·
현업 질의 승계 + 🆕 **신설 2건** = ㉠ **OCR 수납이 사유코드를 발행하지 않는 것이 정상인가**
㉡ **캠페인 코드 5종(188·3358·3484·4178·11954)이 왜 마스터에서 사라졌나**(`EVENT_105`·`106` 과 같은 질의로 묶어라) ·
계정·접근 2건 · 착수표 열린 10건 ·
`decision_closure_gate` 미봉합 **19건**(기존 · `DEC-41`·`DEC-42`·`DEC-43` 축) + `DEC-45` **15건**
(🔴 **조치 대상이 아니다** — 이유를 문서30 §31 머리말에 적어 뒀다).
🆕 ⚠️ **운영 주의(용량)** = O117 이 이력 항목(약 10 KB)을 append 한 뒤 `01_세션이력` 조각과 `99_NEXT` 허브가
**둘 다 여유가 얇아졌다** ⇒ 다음 세션의 `--rollover`·인수인계 절 작성이 **조각 신설 또는 재균형**을 유발할 수 있다
(`R4-4-3` **승인 대상**). 🔴 **여유 수치를 여기 적지 않는다**(적으면 stale 이 된다 · `R3-9 ㉦`) ⇒
**현재 값은 `python3 scripts/doc_census.py` 로 재라**(그 출력의 「여유 B」·「여유 부족」 축). 🟢 새 절을 쓰기 전에
**압축을 먼저** 고려하라 — §0-VVV 이하는 이미 승계됐다.

### ▣ XXX6 🔴 판정식 3조는 유효하다 + O117 이 하나 더 얹는다

`▣WWW2`·`▣WWW6` 을 그대로 지켜라(`-a` 필수 · 회차 분리 · 날짜 없음 · 형태 검사 · 기존 진단 불신).
🆕 **㉦ 게이트가 「종결 선언」이라 부르는 것이 실제 종결이 아닐 수 있다** — `decision_closure_gate` 가
**열린 절의 표 행**을 종결 선언으로 읽고 `DEC-45` 를 엉뚱한 `O8` 축에 붙였다(15건 경고).
⇒ 🔴 **게이트 출력의 「무엇을」도 의심하라** — 종전 3조는 「몇 건을」만 의심하게 했다.
