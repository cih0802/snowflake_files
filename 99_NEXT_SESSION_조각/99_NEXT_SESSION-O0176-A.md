<!-- LLM-METADATA
doc_id: HANDOFF_O0176_A
doc_role: 인수인계 — 세션 `O176-A` 가 다음 세션에 넘기는 것(라벨 파일 · O172 규격)
project: GN_DW (굿네이버스)
created: 2026-09-22
created_by: O176-A
parent: 99_NEXT_SESSION.md
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

<!-- HANDOFF-LABEL O0176-A -->

> 🔴🔴 **이 파일은 인수인계 라벨 파일이다 — 조각이 아니다.**
> 허브 목차·`--verify` concat·`발행 SHA256` 과 **무관**하다(`O172` 규격).
> 🟢 **현행 판정식 = 라벨 최대값**(O번호 → 접미 길이 → 접미) ⇒ 취소선 승계 표기가 필요 없다.
> 🔴 다음 세션은 **이 파일 1개만** 읽으면 된다. 조각에 남은 옛 절은 승계된 것이다.
### ▣ O176-A-0 🔴🔴 먼저 알아라 — 이 세션이 확정한 판정식

1. 🔴🔴 **「내가 약속한 검사가 실제로 도는가」를 기계가 단정하게 만들어라.**
   `id_collision_gate` 머리말은 「미정의 번호」 검사를 약속했는데 **구현이 없었다**(계열 최대값 비교뿐).
   그래서 `P33`(115회)·`P106`(75회)·`P102`(47회)·`DEC-40`(54회)이 **정의 0 으로 장기 생존**했다.
   ⇒ 문서·헤더의 약속과 구현은 **자동으로 일치하지 않는다.** 게이트를 신뢰하기 전에 **주장과 코드를 대조**하라.
2. 🔴🔴 **뷰 문안을 base 로 승계할 때 「그 뷰에서 참인 이유가 base 에서도 성립하는가」를 먼저 재라.**
   내가 `PLAN_BUDGET_MONTH` base COMMENT 에 *"차수별 분해 불가"* 를 심었는데 **실측은 반대**였다
   (`BUDGET_PROCEDURE` 실재 · 연사업 816행/68과목 · 추가경정 516행/43과목).
   뷰에서는 참이다 — `WIDE_BUDGET` 이 그 컬럼을 노출하지 않기 때문이다(실측 0) ⇒ **`J2` 재발**.
3. 🔴🔴 **`R1-7-3` 의 「2~3회 재읽어 해시 대조」는 「같은 낡은 값을 3번 주는 경우」를 잡지 못한다.**
   `-019.md` 를 3회 읽어 **20,880 B 동일 해시 + 동일 decode 실패**를 받고 손상으로 판정했으나
   다음 읽기는 **21,957 B 로 정상**이었다 ⇒ 마운트가 **일관되게 낡은 뷰**를 줬다.
   🟢 처방 = 손상 판정 전에 **크기 변화**를 함께 보고 `stage_mount_size_gate` 로 **다른 축**을 대라.
4. 🟢 **「GOLD 로 올릴 수 있는가」와 「GOLD 에 둘 근거가 있는가」는 다른 질문이다.**
   ② 등급(SERVING helper 뷰) 폐지는 옳았고, ML 계열은 **원천이 GOLD 가 아니므로** 올릴 근거가 없다 ⇒ 등급 ④ 신설.
5. 🟢 **「결정 대기」와 「기본값 없음」은 다른 상태다.** 현업 회신이 무한히 늦어도 **기본값을 명문화하면 데이터는 일관**된다.
6. 🟢 **「변동이 설계인 값」과 「변동이 사고인 값」을 같은 기준선 표에 두지 마라.**
   BRD 행수는 **월당 1일 랜덤 표본**이라 재추출마다 바뀐다 — 감소를 회귀로 읽으면 반드시 오판한다(`D11` 이 그 오판으로 열려 있었다).
7. 🔴 **같은 「미정의」·「정보량 0」이 원인·처방이 다르다** — 한 숫자로 뭉개서 판정하지 마라.
   미정의 334종 = 3상태 · 정보량0 168건 = 4분류(ㄱ10·ㄴ85·ㄷ20·ㄹ53).

### ▣ O176-A-1 🟢 이 세션이 끝낸 것 — §4 결정대기 5건 전건 + A 파생 2건

🔴 **상세 정본 = 이력 `§O176`**(원장 §1 O176 행은 색인이다). 사용자 결정 **8건**.

| 항목 | 결정 | 결과 |
|---|---|---|
| `A1` `FACT_BUDGET` COMMENT | B안 = measure **5컬럼** | 드리프트 **5건 예측 적중** → rc=0 · OWNERSHIP 보존 · base↔뷰 비대칭 **4→0** |
| `A2` `_BATCH_ID` ×50 | A안 | 미노출 **542→492** · 제외 **50→100** · 합 보존 검산 성립 · 쌍둥이 2곳 동시 수정 |
| ④ `D8 ㉤` | B안 | **미정의 번호 전수 열거 축 신설** + 음성 4축(`T4` 회귀 실증) · 정의 2줄은 이력 `§O176 ▣7` |
| ④ `D8 ㉥` | ㅢ안 | **`DEC-34` 개정 · 등급 ④ 신설**(`04_SV_설계.md §0.8-F`) ⇒ ㉥-1 **선행 해제** |
| ⑤ `D9 A4/A2-B` | A′안 | **6:8 분리 재정의** = `A4-W` 배선 6 / `A4-X` 원천부재 8 |
| ⑤ `D9 A1` | B안 | `store_failures` + 행수준 격리 테스트 신설 · 🔴 **`dbt build` 미실행** |
| ⑥ `D11` | A안 | **종결** + 「BRD 행수 인용금지」 규약 |
| ⑦ 착수표 ⑭ | A안 | **현업 문항 전제 기각 실증** + 문항② 기본값 명문화 |
| ⑧ GSC | A안 | **`§N-18` 발행**(드롭 근거 3/4축) · 🔴 드롭 미집행 |

🟢 **기각한 전제 5건** = ㉠ measure COMMENT 정본경로 부재 ㉡ yml 등재로 해결 ㉢ `A4` grain 전제
㉣ §N-13 `SER_NO 1..N` ㉤ GSC COMMENT 「우리가 고칠 오기」. 🔴 **전부 문서에 기재돼 있던 것이다.**

### ▣ O176-A-2 🟠 다음이 할 일 — ㉠ 이 작업의 잔여

1. 🔴🔴 **최우선 = `dbt build` 대사**(`A1` 정지점 · 내가 실행하지 않았다).
   ```
   dbt build --project-dir /10_dbt_pipeline
   ```
   **기대값(미리 계산 · 대사하라)** = TOTAL 561→**562** · PASS 524→**525** · WARN **37 유지** · ERROR **0** ·
   `OPS` 테이블 5→**7**(`WARN_GA4_NULL_USER_PSEUDO_ID` + `..._ROWS`) · **두 신설 테이블 0행**.
   🔴 어긋나면 그 차이를 규명하기 전에 「완료」로 쓰지 마라.
2. 🟠 **`④ D8 ㉥-1` 로드맵 10 문서화** — 선행(`DEC-34`)은 **해제됐다**. 본체 = 문서 정리
   (`02_원천결손_Gap분석.md` 처분 · `09_보고서필드_조립가능성.csv` 2,008자 생성기 개선) ⇒ **별도 세션 규모**.
   🔴 `㉥-2` 로드맵 9 현업 질문서는 **현업 소관**이다 — 우리가 닫을 수 없다.
3. 🟠 **`A4-W` 배선 6건** = `FACT_BIGQUERY_BEHAVIOR`(🟢 `SILVER.BIGQUERY_EVENT.UTM_CAMPAIGN` 실재 ⇒ **확실**) +
   `FACT_AD_*` 4건(🟠 `Q10` 이름매칭 선행 · 대안축 `MKTG_CAMPAIGN_SK` 89.7% 가동).
   🔴 **`A4-X` 8건은 제외/영구상태 판정 대상**이고 배선 대상이 아니다(원천에 캠페인 키가 없다).
   🔴 뷰 제외는 `CREATE OR REPLACE` 라 **GRANT 를 잃는다** ⇒ 별도 승인.
4. 🟠 **정보량0 잔여 분해** = ㄷ 의 비-CAMPAIGN 6건(`AD_CREATIVE_BK`·`DEVICE_TYPE`·`ORG_DEPARTMENT`·
   `SPONSORSHIP_BK`·`SPONSORSHIP_NAME`·`PAYMENT_METHOD`) · ㄴ 85건 · ㄹ 53건.
   🔴 **이 숫자를 「제외 대상 건수」로 인용하지 마라 — 분모이지 처방이 아니다.**
5. 🟠 **미정의 P 잔여** = `--undefined-all` 로 목록을 받아라. 🔴 **정의를 창작하지 마라** —
   원문 근거(발급 세션의 서술)를 확보한 것만 복원한다. 🔴 이 축은 **절제목형 정의를 인정하지 않는다**
   (`DEC-40` 은 `30_설계_의사결정-011.md:4` 에 `## 27. ✅ 결정 — DEC-40:` 로 **실재**한다) ⇒ 축 개선 여지.

### ▣ O176-A-3 ⚪ 이 세션의 소관이 아닌 것 — ㉡ 워크스페이스 백로그

· 🔴 **현업 회신 대기** = `§N-13`(문항 교체본) · **`§N-18`**(GSC 용도 + 원천 COMMENT) · `§N-17`(`FUNDRAISING_COST`) ·
  `§M-2`(`SV_SERVICE.SUBTYPE`) · `§M-6`(`SEND_STATUS2`) · `Q10` · `AGENCY_CONV_CNT` 어의 · 이슈 A/C/D.
· 🟠 **§4 밖 미결 결정 2건** = **개명 런북 3안**(`O174-B-3` · dbt role 이 소유자가 아니라 항상 선행 `ALTER` 필요) ·
  **스킬 본문 분할 여부**(여유 5,592 B).
· 🟠 **원장 조각 여유 +223 B** — 다음 세션이 행을 추가하면 **즉시 초과**한다.
  🔴 재균형은 `R4-4-3` 승인 대상이다. 🟢 대안 = 행을 색인으로 짧게 쓰고 상세는 처음부터 이력으로.
· 🟠 `decision_closure_gate` **미봉합 2건**(`DEC-41 ▸ O8` 표행 = `90_해소완료_로그-010.md:178,180`) —
  **이번 세션 결정과 무관한 기존 백로그**다.
· 🟠 **신규 발견 8건**(전부 이력 `§O176 ▣5`):
  ㉠ 🔴 `GN_DW.ML.ML_RST_DATA_SPNSR_CHURN_12M` **0행** ⇒ `SV_ML_SPONSOR_RISK` 라이브 전건 0
    (ML 16테이블 중 **유일** ⇒ 계정 이동으로 설명 안 된다 · 기준선 829,609 와 불일치)
  ㉡ 🔴 `O167` 이 **이력 항목 없이** 완료(참조 109회 · `####` 0건)
  ㉢ 🔴 `SV_SERVICE` COMMENT 가 **거짓 grain** 발행(「캠페인」 ↔ `CAMPAIGN_SK` 전건 센티넬 · 원천에 키 없음)
  ㉣ 🟠 `DEC-44` 「42개 키 붕괴」가 현재 데이터에서 **재현되지 않는다**
  ㉤ 🟠 `_o169_evidence.md:387` **오기**(`D8` = 「컬럼 개명」 ✗)
  ㉥ 🟠 인수인계 좌표 stale(`D9` = `-016.md:4` ✗ → **`-019.md:4`**)
  ㉦ 🔴 `new_tool.py` 가 `test_*` 를 등재표에 넣는다(`test_gate_census` 축⑨ 와 충돌)
  ㉧ 🔴 `apply_table_comment_drift.py` 에 **dry-run 경로가 없다**(`--help` 도 즉시 집행)

### ▣ O176-A-4 🔴 환경 함정 (이번에 실제로 밟은 것)

· 🔴 **`grep -rn` 에 `logs/` 를 섞으면 `rc=143`** 이다(`10_dbt_pipeline/logs/dbt.log` 가 분모를 키운다)
  ⇒ `models`·`tests`·`macros`·`dbt_project.yml` 로 좁혀라. 🔴 **0건을 받으면 rc 를 먼저 보라**(`R0-8`).
· 🔴 **BusyBox `grep` 에 `--include` 가 없다** ⇒ python `os.walk` 순회로 우회(`_archive`·`logs`·`target` 제외).
· 🔴 **CSV 헤더가 3행 뒤에 있다** — `06_BRONZE노출감사.csv` 는 주석 2행 + 공백 1행 후 헤더다
  ⇒ `csv.DictReader` 를 바로 쓰면 키가 주석이 된다(`utf-8-sig` 로도 해결 안 된다).
· 🔴 **`apply_table_comment_drift.py --help` 가 즉시 집행한다** — argparse 가 없다. 사용법 확인 목적으로 부르지 마라.
· 🔴 **`new_tool.py` 로 음성 테스트를 만들지 마라** — `test_*` 는 패턴 단일 관리라 등재하면 축⑨ 가 FAIL 한다.
· 🟢 **마운트가 낡은 뷰를 준다**(O167 `EEEE3` 재확인) — 위 판정식 3 참조.
· 🟢 `SESSION_LABEL=O176` 을 `export` 로 붙여야 스냅샷·골든 발행 이력에 라벨이 남는다(붙이지 않은 1회가 `UNLABELED` 로 남았다).

### ▣ O176-A-5 📏 종료 기준선 (2026-09-22 · 계정 `LK96056` · 🔴 인용하지 말고 재라)

🔴🔴 **계정이 인수인계 기준선(`RY53492`)과 다르다** — 착수 시 **계정부터 확인하라**.
대부분 값은 이월됐으나 `ML_RST_DATA_SPNSR_CHURN_12M` 만 0행이다.

· 음성 테스트 **42/42** rc=0 · `gate_census --final` rc=0(임시 계측기 **0**)
· `comment_drift_gate` rc=0 = GOLD 테이블 746/746 · SILVER 892/892 · GOLD 뷰 574/574 · 금지문안 0
· `doc_census` stale **0** · 분할 분모 위반 **0** · 여유 부족 2(원장 **+223 B** · 스킬 본문 5,592 B)
· `stage_mount_size_gate` **592건 · 드리프트 0** · 행키·제목·좌표·조문순서 게이트 rc=0
· BRONZE 노출감사 = rows **1,254** · 노출 **137** · 미노출 **492** · 제외(적재제어메타) **100**
· `id_collision_gate` = 정의 식별자 **364** · 미정의 **334종/5,879회** · `O` 정의·참조 **175** ⇒ 다음 **O177**
· GOLD `WIDE_*` 14뷰 **575컬럼** · 정보량0 **168(29.2%)**
· SV **17종 / 논리테이블 42** = ① 33 · ③ 2 · ④ 7
· `FACT_MEMBER_EVENT` **4,716,360** · STOP **1,061,431** · 배선 **92.30%** · 센티넬 **7.70%**
· `FACT_BUDGET` **1,332** · `FACT_BUDGET_YEARLY` **111**(연사업 68 + 추가경정 43)
· `BIGQUERY_REFINED_DATA` **10,332,737**(33개월×1일) → `BIGQUERY_BASIC` **1,299,412**(3일)
· `BRONZE_GSC` = `DATA` **958,419** / `DATA2` **941,298** · `DATA2 EXCEPT DATA` **0**
· `ML_MEMBER_RISK_V` **74,949** · 🔴 `ML_SPONSOR_RISK_V` **0**(기준선 829,609)
· 🔴 **dbt build 미실행** — PASS/WARN/ERROR 는 **이번에 재측정하지 않았다**(기준선 524/37/0/561 은 O175-B 값이다)

_Co-authored with CoCo_

_Co-authored with CoCo_
