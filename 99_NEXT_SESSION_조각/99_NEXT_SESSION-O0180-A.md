<!-- LLM-METADATA
doc_id: HANDOFF_O0180_A
doc_role: 인수인계 — 세션 `O180-A` 가 다음 세션에 넘기는 것(라벨 파일 · O172 규격)
project: GN_DW (굿네이버스)
created: 2026-09-23
created_by: O180-A
parent: 99_NEXT_SESSION.md
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

<!-- HANDOFF-LABEL O0180-A -->

> 🔴🔴 **이 파일은 인수인계 라벨 파일이다 — 조각이 아니다.**
> 허브 목차·`--verify` concat·`발행 SHA256` 과 **무관**하다(`O172` 규격).
> 🟢 **현행 판정식 = 라벨 최대값**(O번호 → 접미 길이 → 접미) ⇒ 취소선 승계 표기가 필요 없다.
> 🔴 다음 세션은 **이 파일 1개만** 읽으면 된다. 조각에 남은 옛 절은 승계된 것이다.
### ▣ O180-A-0 🔴🔴 먼저 알아라 — 이 세션이 확정한 판정식

| # | 판정식 | 근거(실사고) |
|---|---|---|
| ㉠ | **모델 주석이 인용한 현업 발언은 정본이 아니다** — 회신 문서에 없으면 검증 불가다 | 「1개월 주기 증분」 요건이 현업 회신 47줄·`20_issue/` 4축에서 **0건**. 출처 미기재 요건으로 28일간 미집행 |
| ㉡ | **부분 분모 build 는 전체 축 기준선을 갱신하지 못한다** ⇒ 기준선 인용 시 **그 build 의 분모를 함께 적어라** | O179 의 「251/13/264」와 전체 「542/22/564」는 대조 대상이 아니다. O179 는 부분 분모만 돌려 PASS +18 을 못 봤다 |
| ㉢ | **dbt 파일에서 Jinja 구분자를 글자로 적으면 그것이 동작한다** — `--` 도 백틱도 막지 못한다 | 같은 class 를 **4회** 재생산(그중 1건이 사용자 빌드 실패) |
| ㉣ | **「같은 모양」이 「같은 결함」은 아니다** — 따옴표 안인지·주석인지를 먼저 본다 | 게이트 1차 구현이 정상 관용구 4건을 결함으로 오탐. 믿었다면 **정반대 사고**(유일 hook 상실 ⇒ append 무한 누적) |
| ㉤ | **이 결함 class 는 눈으로 읽으면 통과하고 세면 잡힌다** | 4회 전부 육안 통과 → 축 신설 즉시 적발 |
| ㉥ | 🔴 **`R4-1` 정지점이 결함 class 를 구조적으로 생산한다** — 편집자가 컴파일로 확인할 수 없고 PASS 기준선은 편집 이전 값이다 ⇒ **컴파일 전에 잡는 게이트**가 유일한 처방 | `O180-1` 전체 |

🔴 **이 세션이 실제로 밟은 환경 함정 2건 (인수인계 【7】에 없던 것)**

1. 🔴🔴 **`INFORMATION_SCHEMA.QUERY_HISTORY(RESULT_LIMIT=>2000)` 는 조용히 분모를 자른다** —
   `CRM_MEMBER_DEV` DML 조회가 **0행**을 냈고 나는 「`QUERY_TYPE` 값 추측 오류」로 오진했다.
   `10000` 으로 올리자 **64건** 실재. 🟢 `J1` 대로 **분모를 먼저 의심하라**(rc 는 0 이었다).
2. 🔴 **`grep -rn` 에 `10_dbt_pipeline/` 전체를 넘기면 `logs/` 때문에 rc=143 이다** —
   인수인계는 `logs/` 를 명시했지만 **부모 폴더를 넘기는 것도 같은 결함**이다.
   🟢 `10_dbt_pipeline/models/ macros/ dbt_project.yml` 처럼 **하위를 열거**하라.

---

### ▣ O180-A-1 🟢 이 세션이 끝낸 것

| 항목 | 상태 |
|---|---|
| **문서50 `O179-1`** CRM_MEMBER_DEV 증분 설계 | ✅ **종결** — 사용자 결정 `ⓐ′`(표준 패턴 정렬) |
| **문서50 `O180-1`** Jinja config 결함 (신규) | ✅ **종결** — 코드 5파일 + 게이트 신설 |
| `O175` 마감월 **유령행 이월** (`delete+insert` 설계 결정) | ✅ **소멸** — 전환이 구조적으로 닫았다 |
| `O175` `--vars` 마감월 **런북** | ✅ **폐기** — 이 환경에서 무시된다(집행된 적 없음) |
| 🆕 `scripts/jinja_config_gate.py` | 🟢 `JUDGE` 등재 · `session_brief.GATES` 편입 · rc=0 |
| 🆕 `scripts/test_jinja_config_gate.py` | 🟢 **14단정** (오염 6 + 오탐 3 + 회귀 2 + 라이브 1) |

**코드 변경 5파일**
- `models/silver/crm/CRM_MEMBER_DEV.sql` — 모델 `config()` **전체 제거**(폴더 기본값 `TRUNCATE`+`append`
  승계) · `is_incremental()` 분기 제거 · `unique_key`·`pre_hook=[]`·`--vars` 경로 전폐.
  🟢 `SRC_LOAD_DT` 컬럼은 **유지**(워터마크 → 계보 컬럼 · DDL 구조 무변경 · 하류 영향 0).
- `models/gold/fact/FACT_{EVENT_ATTENDANCE,MESSAGE_DISPATCH,MEMBER_EVENT,MEMBER_DEV_ACHIEVEMENT}.sql`
  — O179 가 `config()` **안**에 남긴 `--` 주석을 밖으로 이동(컴파일 불가 상태였다).

**문서 변경**
- 문서50 `-022` 종결 기재 + **꼬리 조각 신설**(당시 `-023` · 🔴 O180-B 재균형 사고로 현재 **`-024`**)(300줄 27줄 초과 ·
  🔴 `--rebalance` 는 좌표를 깨므로 배제 · `--rollover` 는 append형 전용이라 갱신형에 불가).
- `11_일적재pipeline 구성/00_개요_및_실행순서.md` 마감월 런북 → **종결 절**(실행 금지 경고) ·
  기준선 3,654,929 → **3,654,658** 정정(🔴 종전 값으로 대조하면 271행 차이를 사고로 오판한다).
- `11_일적재pipeline 구성/02_권한부여_LOADER_ENGINEER_ADMIN.sql` — `GRANT UPDATE` merge 근거 소멸 명시
  (🟠 **줄은 보존** — REVOKE 는 `R4-4-3` 승인 대상 ⇒ 처분은 사용자 결정으로 남겼다).
- 정본 3곳 = 원장 §1 행(`-002.md:87`) · 이력 §O180(`01_세션이력-081.md`) · 이 라벨 파일.

---

### ▣ O180-A-2 🟠 다음이 할 일 — ㉠ 이 세션에서 생긴 것

> 🔴 전부 **blocking 아님**이고 판단이 필요한 것만 남겼다.

| # | 항목 | 왜 열려 있나 |
|---|---|---|
| **1** | 🟠 `jinja_config_gate` **축5 를 models 한정으로 좁힐지** | 경고 8건 전부 `macros` 의 **정상 공백제어 관용구**(오탐) ⇒ 노이즈다. 🔴 좁히면 models 의 진짜 사고를 놓칠 수 있는지 먼저 판정하라 |
| **2** | 🟠 `jinja_config_gate` **축3 2건 처분** | `ga4_union_shards.sql:15` · `gn_view_commented.sql:27` — 둘 다 `{% macro %}` 정의 **앞**이라 dbt 가 렌더하지 않는다(실해 0 · `:15<:36`, `:27<:32` 로 실증). 문안만 정리할지 판단 |
| **3** | 🟠 `SILVER_2.CRM_MEMBER_DEV` **`GRANT UPDATE` 처분** | merge 근거가 소멸했으나 **REVOKE 는 파괴 작업**(`R4-4-3`) ⇒ 사용자 결정 대기. 🟢 새 환경 구축 시에는 이 줄을 복사하지 마라(주석에 명시) |
| **4** | 🟠 **`--vars` 가 무시되는 원인 미규명** | O179·O180 이 「무시된다」는 **현상**만 실측했다. `dbt_project.yml` 의 `vars:` 선언 부재인지, Snowsight `EXECUTE DBT PROJECT` 가 인자를 못 넘기는지 **원인 축이 다르고 처방도 다르다** ⇒ 다른 곳에서 `--vars` 에 의존하는 설계가 또 있는지 전수 점검 필요 |
| **5** | 🟠 **문서50·문서20 유형 재정의** (증분형/혼합형) | 이 세션이 문서50 에 **수기 꼬리 조각**을 또 만들었다(문서20 `-010` 에 이어 2번째) ⇒ 🔴 `--verify` 원문 바이트 대조가 이 두 조각에 성립하지 않는다. 정규화는 원장 §0 유형 등재표 + `doc_type_gate` 축 신설이 선행이다(인수인계 O179-A 【2】 4단계 그대로 유효) |

---

### ▣ O180-A-3 ⚪ 이 세션의 소관이 아닌 것 — ㉡ 워크스페이스 백로그 (무변경 승계)

> 🟢 **O179-A 의 【2】~【4】가 그대로 유효하다** — 이 세션은 【1】만 처리했다.

| # | 항목 | 정본 좌표 |
|---|---|---|
| **B1** | 🟠 **문서20 을 「증분형」으로 재정의 + `--rollover` 정규화**(4단계 ㉠~㉣) · 🔴 `--rebalance` 금지 | 인수인계 `-O0179-A.md` 【2】 |
| **B2** | 🟠 **열린 문항 28건 전수 `J3` 실측** — O179 는 3건만 표본 실측했다 | 문서20 조각 10개 · 판정식 = `**판정**:` 공란 절 |
| **B3** | 🔴 **M-4 문항 문안 정정** — 라벨 문제가 아니라 **코드 미적재**다(`EMAIL`·`PSTMTR` 의 `SEND_RESULT_CD` 가 SILVER 에서 전건 NULL · 0/7,823,562 · 0/1,969,939) | 문서20 §M-4 |
| **B4** | ⚠️ **문서20 `-001:167` 에 `**판정**` 줄이 2개** (문서 결함 · 정리 대상) | 문서20 `-001.md:167` |
| **B5** | 🔴 **회신 대기 6건** = §D-2 · §W-1 · §N-19 · §N-20 · §N-21(회신 불요) · §N-22 | 문서20 |
| **B6** | 🟠 **§N-13 grain 3종 병기 정리** — 착수표 ⑭ 를 닫기 전에 O160 산출식을 열어 **분모를 대조**하라(FME STOP 92.31% · 중단회원 84.89% · 인수인계 92.45%) | 문서20 §N-13 · 착수표 ⑭ |
| **B7** | 🟠 **W3 결정·설계 대기 8건** — GOLD 6개(`-017:180`) · 순서9-C 외부의존(`-018:36`) · O91-F 2건(`-019:4`) · O99 SV 3종(`-022:4`) · 스냅샷 11종(`-022:172`) · check_cols RM(`-022:182`) · G-5 SILVER identity 2모델(`-020:64`) · O51-D-P1 PM040(`-015:4`) | 문서50 |
| **B8** | 🟠 **I2 MUTATES 무플래그 기지목록 11건 처분** — 일회성 7 · SV배포 2(`extract_sv_deploy` 통합 후보) · 재판정 2. 🔴 **목록에서 이름을 빼는 것이 종결이고 추가는 금지**(stale 축이 FAIL 로 알린다) | `scripts/test_mutating_tool_safety.py` `KNOWN_NO_FLAG` |
| **B9** | 🔴 **W1 배선하지 마라** — O179 가 실측 기각(1 UTM ↔ 최대 902 캠페인 ⇒ `CAMPAIGN_SK` FK 불가) · 현업 회신(§W-1) 선행 | 문서20 §W-1 |
| **B10** | 🟠 `02_상태상세_대시보드_갱신형` 에도 증분형 판단을 적용할지 = **사용자 미결** | 인수인계 `-O0179-A.md` 【2】 |
| **B11** | 🔴 **`TASK_DW_*` 파이프라인 Task 부재**(사고 위험 잠재) · 🔴 `BRONZE_GA4` 현황 유지 | O178 실측 |
| **B12** | 🟠 `_BATCH_ID` **50건** `_STDR_YM` 동일 class 판정 대기 | 문서50 `-018` · O175-B |

---

### ▣ O180-A-4 🔴 환경 함정 (이번에 실제로 밟은 것)

> 🔴 인수인계 `-O0179-A.md` 【7】 전건이 **그대로 유효하다** — 아래는 **신설·보강**만 적는다.

1. 🔴🔴 **`QUERY_HISTORY(RESULT_LIMIT=>N)` 이 분모를 조용히 자른다** — `2000` 으로 **0행**,
   `10000` 으로 **64건**. rc 는 둘 다 0 이다 ⇒ **0건 앞에서 `RESULT_LIMIT` 을 먼저 의심하라.**
2. 🔴 **`grep -rn 10_dbt_pipeline/` 은 rc=143** — `logs/` 가 분모에 들어간다.
   🟢 `models/ macros/ dbt_project.yml` 로 **하위 열거**하라.
3. 🔴 **`Grep` 툴도 `/workspace` 전체를 주면 120초 타임아웃**이다(`crm_member_dev_full_reload` 검색 실패).
   🟢 `20_issue` 같은 **단일 하위 경로**로 좁혀라.
4. 🔴 **`{#-` 는 앞 줄바꿈을 먹고 `-#}` 는 뒤 줄바꿈을 먹는다** — `--` SQL 주석 바로 뒤에 `{#-` 를 두면
   **다음 실행문이 그 주석에 흡수**된다(에러 메시지는 엉뚱한 줄을 가리킨다).
   🟢 `jinja_config_gate` 축4 가 이제 막는다.
5. 🔴 **`new_tool.py --promote-scratch` 는 `_scratch_<name>.py` → `<name>.py` 규칙이다** —
   이름이 다르면 rc=1(`승격 원본이 없다`). 🟢 `cp` 로 사본을 만들어 승격하면 되지만
   **원본 `_scratch_*` 가 남아 `gate_census --final` 이 FAIL 로 잡는다** ⇒ 제거 승인을 함께 받아라.
6. 🔴 **원장 행은 1,900자 이내 · `line_len` 은 2,000자** — 두 상한이 다르고 **엄격한 쪽이 규약**이다.
   🟢 **쓰기 전에 `len()` 을 재라**(이번에 2,089자를 써서 둘 다 넘겼다).

---

### ▣ O180-A-5 📏 종료 기준선 (2026-09-22 · 계정 `LK96056` · 🔴 인용하지 말고 재라)

> 🔴 **`in-flight non-SELECT = 0` 을 먼저 확인한 뒤 재라**(O179 신설 판정식 · 이번에도 이행했다).

- `dbt build`(전체 · 사용자 실행) = **PASS 542 / WARN 22 / ERROR 0 / SKIP 0 / TOTAL 564**
  🟢 종전 전체 축 **PASS 524**(O175-B) 대비 **+18** · WARN **40→22** · TOTAL 불변
  🔴 **분모를 함께 적어라** — O179 의 「251/13/264」는 `--select CRM_MEMBER+` 부분 분모다
- `QUERY_HISTORY` = `TRUNCATE_TABLE`(17:25:24) → `INSERT` **3,654,658**(17:25:26) ·
  🟢 `MERGE` 최신 = `00:41:14`(O179) ⇒ **MERGE 부재**
- SILVER = `CRM_MEMBER` **1,785,299** · `CRM_MEMBER_DEV` **3,654,658**(고아 **0** · 회원 1,606,871) ·
  `CRM_MEMBER_STATUS_HIST` **7,644,142** · `CRM_SEND_MEMBER` **41,935,344** ·
  `CRM_EVENT_PARTICIPATION` **1,248,727**
- GOLD = `FACT_MESSAGE_DISPATCH` **41,935,344** · `FACT_MEMBER_MONTHLY` **41,508,824** ·
  `FACT_MEMBER_EVENT` **4,716,088** · `FACT_BIGQUERY_BEHAVIOR` **1,620,857** ·
  `FACT_EVENT_ATTENDANCE` **1,248,727** · `FACT_MEMBER_DEV_ACHIEVEMENT` **29,442** ·
  `DIM_MEMBER` **1,785,299** · `DIM_DATE` **73,415**(1945~2145)
- BRONZE 적격 **3,654,929** − SILVER **3,654,658** = **271**(이슈 B 고아 제거분 · 🔴 **두 값은 이제
  일치하지 않는 것이 정상이다**)
- 게이트 **8종 rc=0** · 음성 **44종 rc=0**(43→44) · `_scratch_*` 잔존 **0**
- 🆕 `jinja_config_gate` = 분모 **109파일** · blocking **0** · 경고 **10**(축3 2 + 축5 8 · 전부 오탐성)
- 문서 = 문서50 조각 **23**(`-023` 신설) · 이력 조각 **81**(`-081` 신설 · 최소 여유 **2,735 B** 🟠) ·
  제목 골든 **1908**(1907 → +1)
- 🟠 잔여 경고 = `doc_type_gate` 여유부족 2(`00_INDEX_이슈원장` 8,191 B · `03_init_ihcho_스킬_본문`
  5,592 B) · `decision_closure_gate` 2 — 전부 blocking 아님
- 이슈 A·D·E · V1 · 배포(`LOCK_BACKFILL_SLOT_20260922`) = **O179-A 기준선 무변경**(이 세션 미착수)

🔴 **확정위반 4건** = ㉠ 같은 Jinja class **3회 재생산**(1건이 사용자 빌드 실패)
㉡ 게이트 1차 구현 **오탐 5건**(믿었다면 정반대 사고) ㉢ 원장 행 **2,089자**(두 상한 초과 · 압축)
㉣ `QUERY_HISTORY` 0행을 **분모 절단이 아니라 값 오류로 오진**(즉시 정정).
🟢 `R4-4-2` ㉡ 분기 적용 세션(지시 동봉 ⇒ 브리핑 후 즉시 수행).

_Co-authored with CoCo_
