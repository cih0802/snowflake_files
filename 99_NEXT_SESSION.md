# 다음 세션 작업 지시서 (작성: 2026-07-31 세션 종료 · 사용자 `dbt build` 실행 직후 시작점)

> 이 문서가 시작점이다. 정본 위치 — 결정 = `20_issue/30_설계_의사결정.md` / 이슈 = `20_issue/00_INDEX_이슈원장.md` §2-B / 설계 = `03_top-down_gold/03_테이블 설계.md` / 현업 정본 = `99_provided_definition/`

---

## 0. 시작 전 필수 절차 (건너뛰지 말 것)

아래 문서를 **중간 스킵 없이 정독**한다.

| 순서 | 문서 |
|---|---|
| 1 | 이 문서 (`99_NEXT_SESSION.md`) 전문 |
| 2 | `20_issue/00_INDEX_이슈원장.md` §2-B (O22·O23·CONF-4·PROC-3 신규) |
| 3 | `03_top-down_gold/03_테이블 설계.md` §5 O16·O17 |
| 4 | `99_provided_definition/02_지표사전 공통.md`·`06_지표용어사전 20260624.md` |

🔴 **`read` 결과가 비어 있거나 `edited out`으로 오면 반드시 다시 읽는다.** 재읽기 없이 "정독 완료"라고 선언하지 말 것 — **직전 세션이 이걸 어겼다**(4개 문서 전부 `edited out`인데 "read completely"라고 단언). 내용을 갖고 있다고 느껴도 확인 없이 진행 금지.

---

## 1. 첫 작업 — `dbt build` 결과 검증

사용자가 아래 두 커맨드를 실행한 상태로 세션이 시작된다.

```bash
dbt build --select DIM_REASON FACT_MEMBER_MONTHLY warn_fmm_reason_unmapped_f_rows assert_fmm_w4_ml_columns --project-dir 10_dbt_pipeline
dbt build --select DIM_ORG --project-dir 10_dbt_pipeline
```

**빌드 성공 여부와 무관하게 아래 기대값을 실제 쿼리로 검증한다.** 빌드 로그만 보고 통과 판정하지 말 것.

### 1-A. `GOLD.FACT_MEMBER_MONTHLY` — W3 + W4 동시 반영

| 검증 항목 | 기대값 |
|---|---|
| 총 행수 | **40,054,883** (기준선 불변 — 어긋나면 fan-out 발생, 즉시 중단) |
| `REASON_SK` > 0 | **3,164,724** |
| `REASON_SK`=0 AND `UNPAID_FLAG_EOM` AND `HAS_BILLING` | **137,811** |
| distinct `REASON_SK`(>0) | **89** |
| `AMT_INCREASE_CUM_CNT` > 0 | **4,096,650** · 최대 **441** |
| `AMT_DECREASE_CUM_CNT` > 0 | **2,164,357** · 최대 **67** |
| `PAID_SPONSOR_BIZ_CNT` non-null | **37,792,336** = `HAS_BILLING`=TRUE 정확 일치 |
| `IS_MULTI_PAID_BIZ` TRUE | **1,899,415** |
| FK orphan (`REASON_SK` ∉ `DIM_REASON`) | **0** |
| 불변량 | `REASON_SK`>0 ⊂ `UNPAID_FLAG_EOM`=TRUE **100%** |

빌드 전 상태는 `REASON_SK` 전건 0 · W4 4컬럼 전건 NULL이었다. 이 값이 그대로면 **빌드가 반영되지 않은 것**이다.

### 1-B. `GOLD.DIM_ORG` — W5/CONF-4 반영

| 검증 항목 | 기대값 |
|---|---|
| 총 행수 | **1,315** (원천 1,314 + unknown SK=0) |
| `ACMSLT_UPPER_DEPT_ID` non-null | **1,014** |
| `ACMSLT_DEPT_YN`='Y' | **455** |
| `USE_YN`='N' | **764** |
| `CORP`·`DIVISION`·`TEAM` | **전건 NULL이 정상** (의도적 — §3 참조) |
| `DEPARTMENT` distinct | **695** (694 + '(미매핑)') |

### 1-C. 테스트 2종

| 테스트 | 기대 |
|---|---|
| `warn_fmm_reason_unmapped_f_rows` | **PASS** (현 4.17% < 임계 5%) |
| `assert_fmm_w4_ml_columns` | **PASS** (0행 — NULL 일관성·플래그 정합·음수 불가) |

실패하면 기대값과 실측 차이를 먼저 규명한다. 테스트를 완화하지 말 것.

---

## 2. 작업 조건 (전부 직전 세션 실제 위반 기반 — 반복 금지)

### 2-A. 데이터 검증 계층

1. 🔴 **"BRONZE 원천 확인" 지시를 하류(SILVER/GOLD)로 대체 금지.** 직전 세션이 C-9-R 검증에서 BRONZE 테이블명 조회에 실패하자 포기하고 `GOLD.FACT_SERVICE_EVENT`로 우회했다. 테이블명을 모르면 `INFORMATION_SCHEMA.COLUMNS`에서 **컬럼명으로 검색**해 특정한다.
2. 🔴 **"입고 대기"·"전건 NULL" 류 이슈는 BRONZE를 전수 재조회한다.** 이슈 문서에 stale이 있다. 직전 세션이 E-6에서 SILVER·GOLD 0행만 보고 BRONZE 입고 여부를 조회하지 않았다.
3. 🔴 **키·코드의 형태로 의미를 추정하지 않는다.** 마스터/코드사전과 대조한다. 직전 세션이 `S` 접두 9자리를 형태만 보고 "별개 키체계·정규화 누락"이라 결론했으나, 실제로는 **정상 일시회원(ONCE) 체계**였고 정규화 문제는 143명 중 1명뿐이었다.
4. 🔴 **동일 대상의 두 측정치가 어긋나면 원인 규명 전 어느 쪽도 채택하지 않는다.** 직전 세션이 BRONZE 중복을 `CONCAT_WS`로 8,270,695, SILVER를 `ROW_NUMBER`로 1,831,286으로 측정하고 4.5배 괴리를 규명하지 않은 채 후자를 채택했다(재측정 결과 `CONCAT_WS`가 오측정).
5. 🔴 **중복·유일성 측정은 문자열 결합이 아니라 `ROW_NUMBER() OVER (PARTITION BY 컬럼목록)`.** NULL 결합 왜곡을 피한다.

### 2-B. 측정 도구 자체의 함정

6. 🔴 **정규식으로 컬럼명을 뽑을 때 `[A-Z_]+`는 숫자 포함 이름을 놓친다.** `[A-Z_][A-Z0-9_]*`를 쓴다. 직전 세션이 이 때문에 DDL 컬럼수를 52로 세고 DB 56과 불일치한다고 오판했다(실제 56=56).
7. 🔴 **`head`·`-m` 절단 상태로 "0건"·"전수" 결론 금지.** 먼저 `grep -c`로 총량을 확인한다.
8. 🔴 **`&&` 체인은 매칭 0에서 끊긴다.** "명령 실패"와 "0건"을 구별하려면 `;`로 분리하거나 `grep -c`를 쓴다. 직전 세션이 이 때문에 "용어사전에 팀 없음"을 근거 없이 판정했다(실제 1건 존재 — 내용은 무관한 편집주석이라 결론만 우연히 맞았다).

### 2-C. 정본·결정

9. 🔴 **지표·측정 의미가 걸린 결정은 `99_provided_definition/` 대조를 선행한다**(PROC-2). 내부 문서·데이터만으로 확정 금지.
10. 🔴 **근거 계층을 구분해 기록한다.** ① 현업 정본 ② 내부 문서 ③ 데이터. 내부 문서에 적혀 있다는 것은 검증됐다는 뜻이 아니다.
11. 🔴 **조인 커버리지 100%는 의미 정합 100%가 아니다.**
12. 🔴 **"라벨 완비"·"다른 곳에도 있을 것" 류 전제는 전수 스캔·조인 실측으로 확인 후 기록.**
13. 🔴 **정답 없는 문제는 임의 확정 금지 — 질문한다.** 사용자 지시라도 데이터가 불가능을 보이면 실행 전 보고한다(직전 세션 ③ `CORP`←`CPR_DIV_CD` 건이 이 경우였다).

### 2-D. DDL·빌드

14. 🔴 **`CREATE OR REPLACE` 금지.** 자식 FK 전소실(`06_DDL.sql:721~726`). 구조 변경은 **`ALTER TABLE ADD COLUMN`**.
15. 🔴 **순서: DDL/ALTER 선행 → `dbt build`.** SILVER·GOLD fact 모두 `incremental + append + pre-hook TRUNCATE + full_refresh:false`.
16. 🔴 **컬럼을 추가·수정하면 DDL 파일과 DB COMMENT를 같은 커밋에 동기화한다.** 문구까지 동일하게(이모지 포함 — DB에 정상 저장된다. GOLD 858컬럼 중 18개가 이미 이모지 보유). 직전 세션이 이모지를 임의 제거해 문구 불일치를 만들었다.
17. 🔴 **의미가 확정된 컬럼은 COMMENT도 갱신한다.** "컬럼이 이미 있으니 ALTER 불요"로 넘기면 안 된다 — W3 배선 후 `REASON_SK` COMMENT가 구 문구로 남아 있었다.
18. 🔴 **DEC/O/CONF/PROC 번호 발급 전 `grep` 최대값 확인.** 현재 최대 = **DEC-24 · O23 · CONF-4 · PROC-3**.
19. 🔴 **문서를 legacy·`_archive`로 옮길 때 미결항목은 이슈원장 승계 후 이동**(PROC-1).

---

## 3. 착수 가능 작업

### 3-A. 🟢 O22 후속 — 현업 회신 도착 시 (최우선)

`PAID_FEE`가 **339억원(4.415%) 과대계상** 중이다. 원인은 재청구가 아니라 **BRONZE 논리적 중복**이다.

- BRONZE `TM_PM_MBRFEE_ACMSLT`: 업무 속성(회원·사업·회비월·차수·청구액·청구일·납입액·납입일·상태) **전건 동일** 행이 연속 PK로 발행 — **1,831,286행(3.947%)**, 최대 **400행/업무키**(예 `MBRFEE_184973586~597`)
- `PAID_FEE` 768,800,286,349 → 734,855,893,986 · `BILLED_AMT` 과대 **39,100,344,913원**
- 🟢 W3 `REASON_SK`·`UNPAID_FLAG_EOM`은 **영향 없음**(`ROW_NUMBER`/`BOOLOR_AGG`) · 🔴 `SUM` 계열만 영향
- 🟠 **확인 전 dedup 금지.** 회신에 따라 dedup 적용 범위와 기존 배포 금액지표 재산출 범위가 결정된다.

### 3-B. 🟢 O23 후속 — 즉시 가능한 것 1건

`FMM.MEMBER_DK` 고아 **161명**(0.009%). BRONZE 마스터 대조로 3원인 분리 완료:

| 원인 | 인원 | 조치 |
|---|---|---|
| 소문자 `s` 정규화 누락 | **1** | 🟢 **즉시 가능** — SILVER 적재에 `UPPER()` 추가하면 BRONZE 일시회원 마스터에 매칭됨 |
| 마스터 스냅샷 누락 | **159** | 🟠 현업 확인 (대문자 `S` 142 = billing 기원 / 7자리 17 = FME 기원, BRONZE 정기·일시 마스터 양쪽 부재) |
| 더미 `999999999` | **1** | 제외 규칙 |

키 체계 확정 사실: **`S`+8자리 9자 = 일시회원(ONCE) · 7자리 숫자 = 정기회원(FDRM)**. BRONZE 정합 — `TM_MM_ONCE_MBER_INFO` 175,722 = `CRM_MEMBER` ONCE 175,722 / `TM_MM_FDRM_MBER_INFO` 1,587,343 = FDRM 1,587,343.

### 3-C. 🟠 W5 잔여 — "실적지부" 산출규칙 1건 (CONF-4)

직전 세션에 **미결 3건 → 1건으로 축소**됐다. 완료된 것:

- ① `DIVISION` → **실적지부 재정의** 완료(DDL·DB COMMENT·모델). 정본 근거 = 용어사전 **430**(실적 지부 명)·**431**(실적지부(본부/지부) 구분) + 회원보고서 `05:275`·`05:311`. 정본 보고서에 "본부/지부" **단독 사용 0건**.
- ② `TEAM` → **보류** 명시 완료. 정본 근거는 지표 #152~155 "각 팀별"뿐이고 용어사전·회원·마케팅 보고서 실질 0건. 원천 `CRM_BIZ_TARGET` 미입고(E-6) → 소비처 부재. **E-6 입고 시 재개.**
- ③ `CORP` → **부서 차원 산출 불가** 확정. 부서→법인 1:1 아님(2종 혼재 69부서/23,130명 · **3종 혼재 131부서/1,544,005명=98.6%**). 법인 정본 원천 = **회원 속성 `CPR_DIV_CD`**(CM019: `I`=사단·`S`=사복·`A`=통합). → **`DIM_MEMBER` 또는 팩트 degen 배속 판단 필요**(미결).
- ④ 원천 계통 3컬럼 노출 완료(`ACMSLT_UPPER_DEPT_ID`·`ACMSLT_DEPT_YN`·`USE_YN`) — 추론 0.

**잔여 미결**: "실적지부" 값 산출 규칙. 명칭 기반 최근접 본부/지부 도달률 **418/455 = 91.9%**(미도달 37)이고, 명칭 판정은 O16이 이미 범주오류를 낸 방식이다 → **규칙 확정 전 값 채우기 금지.**

핵심 사실(재조회 불요): **부서 트리가 2개**다.

| 구분 | 조직 트리 `UPPER_DEPT_ID` | 실적 트리 `ACMSLT_UPPER_DEPT_ID` |
|---|---|---|
| 채움 | 1,314 (100%) | 1,014 (77.2%) · NULL 300 |
| 레벨 분포 | 9/38/351/672/238/6 | 300/10/72/422/**509**/1 |
| 실적부서(455) LVL5 집중 | 118 = 26% | **446 = 98.0%** |
| 두 트리 상이 | — | **441건**(동일 573) |

→ DEC-5 "5th=실적부서"는 **실적 트리 기준으로 98% 성립**(O16 미결3 해소). `USE_YN='N'` 764개는 **제외 금지** — 팩트가 대량 참조(`ACT_DEPT_CD` 미사용 156부서에 410,506명) + 필터 시 트리 파편화(LVL1 9→42).

### 3-D. 🔴 차단 작업

| 작업 | 차단 사유 |
|---|---|
| **W2** FMM 활동 스냅샷 (DEC-19) | **CONF-3** — 정본 #51 판정 조건 내부 모순(§4) |
| **W4 요건2** 연속미납횟수 | 스파인 sparse(gap>1 **3.940%**·최대 **370개월**) → run-length 오판. dense 스파인 = W2와 동일 트랙 |
| **W6** 납입 이행 카테고리 | 정본 215지표 외 신규 → 현업 제안 필요 + **O22 미해소로 실측 분포 재산출 필요**. 📌 컬럼 5종 설계안(`PAYMENT_FULFILLMENT`·`BILLED_OBLIGATION_CNT`·`FULFILLED_OBLIGATION_CNT`·`UNFULFILLED_OBLIGATION_CNT`·`BILLING_ATTEMPT_CNT`)과 실측 분포는 **이슈원장 §2-B `W6-설계`에 승계**됨 |
| **O21 잔여** `DIM_REASON` 중단/미납 분류 컬럼 | 현업 확인 대기 |
| **E-6** `CRM_BIZ_TARGET` | BRONZE_CRM **43테이블 전수 확인** 결과 사업목표(#152~155) 테이블 미입고 확정. `TM_CM_MBER_DVLP_GOAL` 25,344행은 **개발**목표(`CRM_DEV_TARGET` 원천)로 별개 |

---

## 4. 현업 확인 문항 (우선순위 순)

1. 🔴 **[O22·최우선] BRONZE 회비원천의 완전 동일 행 중복이 정상인가?**
   - 정본 #64·#65의 "재청구 중복 포함"은 **차수가 다른 재청구**를 뜻하는가, **동일 차수의 동일 행 반복**까지 포함하는가?
   - 실제 복수 청구·복수 납입인가, 원천 시스템 중복 발행 결함인가?
   - → dedup 적용 여부 + 배포 금액지표 재산출 범위가 여기서 결정된다.
2. 🟠 **[CONF-3] 정본 #51 월말활동회원 판정 조건 모순** — 원문 두 문장이 상반된 방향을 지시한다(§아래).
3. 🟠 **[CONF-4] "실적지부" 산출 규칙** — 실적 트리에서 지부를 어떻게 특정하는가? 명칭 기반은 91.9%만 도달.
4. 🟠 **[CONF-4] 법인 축 배속** — `CPR_DIV_CD`(회원 속성)를 `DIM_MEMBER`에 둘지 팩트 degen으로 둘지.
5. 🟠 **[O23] 마스터 스냅샷 누락 159명** — 추출 시점·범위 문제인가?
6. 공64·65 "재청구 중복 포함"이 **확정 규약**인가 **합의 필요 쟁점**인가?(컬럼명이 "합의 필요사항"이라 문면상 단정 불가)
7. 의무 대비 이행률 신규 지표(전부/일부/완납 3분류) 필요 여부.
8. `DIM_REASON` 중단/미납 구분 분류 축 필요 여부(O21 잔여).
9. **W2 착수 시**: 상태이력 없는 '후원중단' 3,575명의 활동 구간 처리(전 기간 비활동 vs 마지막 납입월까지 활동).

### CONF-3 원문 (판단 보류 상태)

> 해당 월을 기준으로 했을 때 **마지막 중단일이 마지막 재후원일보다 커야함**. 같은 날이면 시간으로 구분하기 어려우므로, **재후원을 진행한 후원사업의 넘버링이 중단 후원사업의 넘버링보다 크다는 조건**이 필요

앞 문장 → 중단 우세(중단 상태) / 뒤 문장 → 재후원 우세(활동 상태). **방향 상반.** 월말활동회원 조건이라면 뒤 문장이 맞아야 논리가 성립하나 **단정 금지.**

---

## 5. 확정 결정 (재론 불요)

| ID | 확정 내용 |
|---|---|
| **DEC-19** | `MONTH_END/PREV_MONTH_END_ACTIVE_CNT` = **(d) 원천 as-of 재평가** · 활동 = `MBER_STAT_CD` **1~11**(중단12만 제외) · FDRM 한정 |
| **DEC-20** | `YEAR_START/END_ACTIVE_CNT` = 12개월 반복 + "SUM 금지" 명시 · DDL 무변경 |
| **DEC-21** | SILVER 결제결과코드 2컬럼 추가 · DEC-3 판정 유지 · 사유축 전용 → **W1 완료** |
| **DEC-22** | ML 요건1 = FMM 월말 스냅샷 / 요건3 = "그 달 납입한 사업 수"(회비 기준) → **W4 완료** |
| **DEC-23** | `MBRFEE_SQNC` = **재청구/재시도** ⚠️ 근거였던 "완납 차수 예외없이 1"은 **실측 반박됨**(차수2에서 11,244,327행 납입) — 결론은 정본 근거로 유지되나 데이터 근거는 폐기 |
| **DEC-24** | 월×회원 대표 미납사유 = **최종 차수(`MBRFEE_SQNC` 최대)** · 정렬 `SQNC DESC NULLS LAST, RQEST_DE DESC, RQEST_RST_CD` → **W3 완료** |
| **DEC-5** | `TEAM`=5th 레벨 — ✅ 실적 트리 기준 98% 성립 확인(O16 미결3 해소). 단 정본 용어는 "실적 팀"이 아니라 **"실적지부"** |
| **DEC-2** | `DIM_ORG` = SCD1 |

---

## 6. 현업 정본 확보 사실 (재확인 불요)

| 지표/용어 | 정본 기재 | 함의 |
|---|---|---|
| #40·#51·#52 | 활동회원 = 활동 + 미납1~5 | DEC-19 근거 |
| #42 | 미납회원 = 미납1~5 | 활동회원의 **부분집합** → 배타 아님(CONF-1) |
| #36·#37·#52·#84·#157 | `(건)` = **약정금액 ÷ 10,000** | 🔴 건수 아님(CONF-2) |
| #64·#65 | 청구회비 = 재청구 중복 포함 | #71 결함 아님(철회) |
| #71 | 청구 = "납입방식별 결제 요청 프로세스" | 재청구 포함이 정의상 정합 |
| #62 | 납입율 = 납입회비 ÷ (활동회원 건 × 10,000) | 공64와 **분모 다름** |
| #82 | 미납사유 존재 · **"캠페인별 미납사유는 볼 수 없음"** | W3 근거 + SV 제약 |
| #114 | 법인 = **사단법인, 사회복지법인** 2종 | 데이터 일치(사단 1,528,654 / 사복 30,350 / 재단법인 2개는 **회원 0**) |
| #115 | 본부/지부 | 🟠 보고서 단독 사용 0건 → "실적지부"로 대체(CONF-4) |
| #150·#151 | 증액(명/건) = **전월 대비 활동(건) 증가** | 🔴 W4 누적 횟수와 별개 — 혼용 금지 |
| #38 | 감액(건) = **감액금액 ÷ 10,000** | 🔴 W4 누적 횟수와 별개 |
| #152~155 | 연사업/추경 목표 = "본부/지부별, **각 팀별**, 후원사업별" | `TEAM`의 유일 근거 · 원천 미입고(E-6) |
| 용어 121·390·391 | 부서 | ✅ `DEPARTMENT` 정본 정합 |
| 용어 430·431 | **실적지부** · **실적지부(본부/지부) 구분** | ✅ `DIVISION` 재정의 근거 + O16 미결2 답 |
| `CPR_DIV_CD` (CM019) | **I=사단, S=사복, A=통합** | `A`는 제3법인 아님 = 통합 집계 구분 |

---

## 7. 문서 동기화 상태 (2026-07-31 검증 완료)

| 파일 | 상태 |
|---|---|
| `03_top-down_gold/06_DDL.sql` | ✅ 테이블 **27 = DB 27** · FMM **56 = 56** · DIM_ORG **13 = 13** · COMMENT 문구 동기화 완료 |
| `04_silver_design/08_SILVER_테이블DDL_20260714.sql` | ✅ 테이블 **38 = DB 38** · `CRM_PAYMENT_BILLING` **21 = 21** · `CRM_ORG` **12 = 12** |

특이사항(조치 불요·인지용):
- GOLD DB에 **VIEW 12개**가 있으나 `06_DDL.sql`은 VIEW 0건 — WIDE 뷰는 dbt 모델(`models/gold/wide/` + `_wide_schema.yml`) 소관으로 분리.
- SILVER DDL 파일명이 `20260714`인데 최종 변경은 2026-07-31 — 내용은 반영됨, 파일명 날짜만 불일치.

---
_Co-authored with CoCo_
