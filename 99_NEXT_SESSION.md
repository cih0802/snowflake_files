<!-- LLM-METADATA
doc_id: NEXT_SESSION_PROMPT
doc_role: 다음 세션 착수 프롬프트 — 이 파일만 읽고 바로 시작할 수 있게 구성
project: GN_DW (굿네이버스 데이터웨어하우스)
updated: 2026-08-07 (**O49 작업2 실측완료·반입미완 — ⛔계정 정지** · 09 판정 오류 17건 적발 · 신규 P104 / **O48 작업1 완료** — 생성기 회귀 골든 테스트 신설·라벨 도달불가 14건 교정·신규 P103 / **O47/O47-B 완료** — grain 라벨 분해 · 검사기 오판 9건 교정 · DEC-31 종결 · DEC-32 철회 · DEC-33 성격 변경 · O45-C 종결 · 신규 P95~P102)
정본: 이슈=20_issue/00_INDEX_이슈원장.md · 진단·교훈=20_issue/10_진단_원인분석.md · 운영=02_GN_DW_building/06_RUNBOOK.md
편집 규칙: 이 문서는 **포인터 + 미결 항목 + 착수 프롬프트**만 담는다. 완료 작업의 경위는 정본에 두고 복사하지 않는다.
END-METADATA -->

# 99. 다음 세션 착수 프롬프트

## 0. 작업 규칙 (사용자 상시 지시)

1. 문서를 읽을 때 **중간에 끊기면 추론으로 채우지 말고 전부** 읽는다.
2. 작업 전에 `20_issue/` 를 읽고 **미해결·pending** 을 파악한다.
3. 데이터 작업은 COMMENT 를 근거로 단정하지 말고 **BRONZE 원본을 스캔해 실측**한다.
4. 큰 작업이 끝나면 `20_issue/` 를 갱신한다.
5. **`dbt build` 순서에 도달하면 멈추고 사용자 입력을 기다린다.**
6. 작업 완료 후 **단답형으로 짧게** 답한다.
7. **COMMENT 에 실측 수치를 넣지 않는다**(코드값만). 데이터가 계속 입고돼 수치는 낡는다 → 수치는 문서10·이슈원장에.
8. 🔴 **파일을 만들거나 고쳤으면 `cortex ws ls` 로 스테이지 실체를 확인한다** — 아래 §9 P102 참조.
   `/workspace` 마운트의 `ls` 성공은 **스테이지 반영을 뜻하지 않는다.**

---

## 1. 직전 세션(O47/O47-B)에서 끝난 것 — 다시 하지 말 것

| 끝난 것 | 결과 |
|---|---|
| `09` 검사기 교정 | FK **실선언 읽기**(`show imported keys` 50) · degenerate 우선 · 1:1 위성 등록부 · 방향성 판정 · 축 병기 후보 전량 판정 · **앵커 결정적 정렬** |
| 신규 판정 4종 | `집계필요` · `배분규칙필요` · `형제팩트중복` · `앵커_경합` |
| `09` 최종 판정 | **조립가능 267/507 = 52.7%** · `grain부정합` **35 → 2** · 앵커 경합 **34행** |
| `dbt build`(사용자 실행) | `PASS=375 WARN=27 ERROR=0 SKIP=0 TOTAL=402` → **O45-C 종결**(`FMF` 40,262,076 · `MEMBER_DK` NULL 0) |
| `DEC-31` | 🟢 **종결 = (a) 현행 유지.** 근거 = FMF 롤업 **재현 불가 4종**(`ONETIME_ONETIME_FEE`·`PAID_SPONSOR_BIZ_CNT`·`IS_MULTI_PAID_BIZ`·`UNPAID_FLAG_BOM`) |
| `DEC-32` | ❌ **자체 철회** — STOP 브랜치 배선은 O24 이중계상 재도입. 중단 분해는 **코드5 경로**(100% 보유) |
| `DEC-33` | 🔴 **성격 변경** — 「귀속창 며칠?」이 아니라 **「어떤 발송을 코호트에 넣는가」**가 선행 질문. 현업 1건 → **3건** |
| `O28` | 오염 4행 전량 특정(회원 2명×2행 · 2024-02-14 · HomePage · 정정 후보 `110`=`Success`) |
| 산출물 | `30_output_share` **17종 전량 재생성 + 파일 단위 크기 대조 검증** |
| 소비 문서 | `01` §0-B(판정 4종을 현업 언어로) · `02` §I(02=컬럼 결손 ≠ 09=grain) · `02` 내부 불일치 교정(106→**105**) |
| WARN 27 | ✅ 회귀 0 확인(O41 기재 기지값과 항목·건수 전량 일치) + `10_dbt_pipeline` 에 **포인터 추가** |

⚠️ **`dbt build` 는 이 세션에 실행 불요** — GOLD·SILVER 모델 변경이 0이다.

---

## 2. 🔴 직전 세션 비판적 검토 — 다음 세션 작업의 근거

> 아래 7건은 **실측으로 확인한 결함**이다(추정 아님). §3 작업은 전부 여기서 나온다.

### 2-A. 🔴🔴 같은 실패 유형이 한 세션에서 5번 반복됐다 — 교훈 축적이 재발을 막지 못한다
| # | 자기적발 | 무엇을 안 읽고 썼는가 |
|---|---|---|
| 1 | **DEC-32 철회**(P97) | `FACT_MEMBER_EVENT.sql` 주석이 *"두 축을 합산하면 이중계상"*을 이미 규정 |
| 2 | **낙관 편향 교정**(P94 재발) | GA4 를 회원-월로 집계하려면 identity 브리지(G-5)가 필요 |
| 3 | **1-2 경합 = DEC-31 증거 철회**(P100) | 그 섹션의 **필드 21개를 열어보지 않았다** |
| 4 | **P98 편집 부작용** | `05_필드 인벤토리.md` 가 **생성기의 라벨 사전**이라는 사실 |
| 5 | **DEC-33 인과 역전**(P101) | D+0 발송의 **제목**(= 중단 통보) |

🔴 공통 원인은 하나다 — **집계 수치·채움률을 보고 결론을 먼저 쓰고, 근거가 되는 개체(모델 주석·필드 목록·발송 제목·생성기 입력 계약)를 나중에 열었다.**
O44 → O45 → O46 → O47 **네 세션 연속** 같은 계열이다(P76·P90·P94 의 재발).
⇒ **P 번호를 하나 더 쌓는 것은 대책이 아니다.** 교훈을 **문장이 아니라 절차·코드로 강제**해야 한다 → §3 작업 1.

### 2-B. 🔴 검사기에 회귀 테스트가 0이다 — 조용한 실패를 우연으로 발견했다
- `scripts/` 에 test·golden 파일 **0건**(실측).
- **O47-B**(앵커 동점 임의 → 34행이 삽입 순서 의존)는 *재빌드로 매핑 2건이 우연히 움직여서* 발견됐다.
- **P98**(문서 편집이 매핑을 바꿈)은 *diff 를 떠 봐서* 발견됐다.
- 🔴 **둘 다 오류를 내지 않는다.** 지금 구조로는 다음 사고도 우연에 의존한다.

### 2-C. 🔴 「집계필요」·「앵커_경합」은 판정만 있고 **처방이 없다** — P90 자기 위반
- `09_보고서필드_조립가능성.md` 의 SQL 블록 수 = **0건**(실측).
- 「사전집계 후 조인하라」·「경합 팩트 수만큼 쿼리를 나눠 회원·월에서 합쳐라」는 **산문**이다.
- 팬아웃 0 실측은 `FSE`·`FEP` **2건**뿐이고, **집계필요 6건·경합 34행의 실제 쿼리를 실행해 본 적이 없다.**
- 🔴 P90 이 정확히 이것을 금지한다 — *"검사기가 「가능」이라 하면 그 조인을 실제로 실행해 행수·합계를 재라."*

### 2-D. 🔴 또 GOLD·문서까지만 끝났다 — SERVING 미반영 (O46 이 해소한 실패의 재발)
- SV 9종·Agent 스펙에 `집계필요`·`앵커_경합`·`형제팩트중복`·`배분규칙필요` **grep 0건**(실측).
- ⇒ 현업이 Agent 에 물으면 ① 「집계필요」 6건은 **여전히 답 못 하고** ② **1-2·3-7 경합 섹션은 한쪽 팩트만 골라 답한다** = 조용한 오답.
- O45(GOLD only) → O46(소비계층 반영) 을 거쳤는데 **O47 이 같은 자리에서 멈췄다.**

### 2-E. 🟠 DEC-31 을 닫으면서 스스로 적은 조치를 안 했다
- DEC-31 결론에 *"10종은 어느 쪽을 써도 값이 같다는 사실을 SV COMMENT 에 명시하면 Agent 라우팅이 쉬워진다"* 라고 적었다.
- 실측: SV COMMENT 에 `동일값`·`어느 쪽을 써도` **grep 0건**. 현재는 **「합산 금지」만 있고 「동일값」 정보가 없다.**
- ⇒ Agent 는 두 SV 중 **어느 쪽을 골라도 되는 상황**에서 근거 없이 고른다.

### 2-F. 🟠 관문 자동화가 여전히 0 (이월 항목이 세 세션째 방치)
- `O45_VERIFY.sql` GATE-A~G 가 아카이브되어 **매 build 회귀검증 0**.
- 이번에 O47 근거 5종을 **손으로** 재검증했다 — 그 5종이 곧 dbt test 승격 후보다.

### 2-G. 🔵 「집계필요」 6건 중 2건은 라벨이 의심된다
- `2-1` 의 **「가입일」·「중단일」** 은 사전집계 문제가 아니라 **대표값 규칙**(일자값을 월/주 팩트에 어떻게 실을지) 문제로 보인다 — D유형 잔재.

---

## 3. 다음 세션 작업 — 착수 프롬프트

> 순서대로 진행한다. 각 항목의 ▶ 블록이 **그대로 붙여 쓸 프롬프트**다.
> 🔴 **1 → 2 → 3 순서를 지킬 것.** 1이 없으면 2·3의 결과를 검증할 수단이 없다.
> ✅ **작업 1 완료(O48)** · 🔴 **작업 2 = 실측 완료·반입 미완(O49, 계정 정지 차단)**. 다음은 **작업 2 재개**부터. 작업 2·3 이 산출물을 바꾸면 `--update-golden` 으로 골든을 갱신한다.

### 작업 1. ✅ **완료 (2026-08-07 O48)** — 산출물 생성기 회귀 골든 테스트 신설 — §2-A·2-B

> 🟢 **다시 하지 말 것.** 정본 = `20_issue/00_INDEX_이슈원장.md` §O48 · 실행법 = `06_RUNBOOK.md` §3.5.
> · 신설 `scripts/test_generators.py`(17 assertion · **PASS 17 / FAIL 0**) + `scripts/golden/outputs.json`(23지표)
> · 요건 ⓐⓑⓒ 전부 구현 + **일부러 깨뜨려 3종 전부 검출 확인**(`--self-check`)
> · 요건 3(P97·P100 기계 검사) = `T4` — 불가·경합 **79행 전부 근거에 백틱 식별자** 실측
> · 생성기 최소 리팩터 2건 — `pick_anchor()` 분리 · `SV_METRIC_PREFIX` 상수화
> · 🔴 **첫 실행에서 신규 결함 적발** — 편집 마커 접두가 라벨 **14건을 영구 도달불가**로 만들고 있었다.
>   교정 후 **보고서필드 신규·소멸 매칭 둘 다 0** 을 실측해 산출물 불변을 확정한 뒤 반영(재생성 불요).
> · 🆕 **P103** · ⬜ 잔여(경미) = 설명 셀 53 · 라벨 충돌 14 — **골든이 수를 고정**하므로 증가만 잡힌다.
> · 🔴 **작업 2·3 을 하고 나면 골든을 갱신해야 한다** — `--update-golden` 은 **차이를 전량 규명한 뒤에만**.

<details><summary>원 프롬프트(이력)</summary>

```
20_issue 를 읽고 미결을 파악한 뒤, 99_NEXT_SESSION.md §2-A·§2-B 를 근거로
scripts/ 에 산출물 생성기 회귀 테스트를 신설해라.

요건:
1. 골든 파일 — 30_output_share/{04,05,06,07,08,09} 의 현재 판정 분포와
   행수를 골든으로 고정한다(09 = 507행 · 조립가능 267 · 앵커_경합 34 · 판정 11종).
2. 다음 3가지를 자동으로 잡아야 한다. 각각에 대해 **일부러 깨뜨려 테스트가
   실패하는지 확인**하고 그 결과를 보고해라(테스트가 통과만 하면 검증이 아니다):
   ⓐ 앵커 동점 시 판정이 뒤집히는 사고(O47-B) — 삽입 순서를 섞어도 결과 동일
   ⓑ 수기 문서(05_필드 인벤토리.md)가 생성기 라벨 사전을 오염시키는 사고(P98)
      — 쓰레기 라벨 색인 0건
   ⓒ 생성기 간 출력 문자열 계약 파손(O46 §3-①의 'SV metric — ' 접두 계약)
3. 🔴 P97·P100 을 절차로 강제하는 체크리스트를 테스트에 포함한다 —
   「불가/경합의 원인을 서술한 행은 그 섹션의 필드 목록을 열거했는가」를
   기계 검사할 수 있는 형태로(근거 칸에 필드명이 실제로 등장하는지).
4. 실행 방법을 02_GN_DW_building/06_RUNBOOK.md 에 1개 절로 기재한다.

제약: dbt build 불요. 생성기 수정 시 즉시 스테이지 업로드 후 cortex ws ls 로 확인(§9 P102).
```
</details>
20_issue 를 읽고 미결을 파악한 뒤, 99_NEXT_SESSION.md §2-A·2-B 를 근거로
scripts/ 에 산출물 생성기 회귀 테스트를 신설해라.

요건:
1. 골든 파일 — 30_output_share/{04,05,06,07,08,09} 의 현재 판정 분포와
   행수를 골든으로 고정한다(09 = 507행 · 조립가능 267 · 앵커_경합 34 · 판정 11종).
2. 다음 3가지를 자동으로 잡아야 한다. 각각에 대해 **일부러 깨뜨려 테스트가
   실패하는지 확인**하고 그 결과를 보고해라(테스트가 통과만 하면 검증이 아니다):
   ⓐ 앵커 동점 시 판정이 뒤집히는 사고(O47-B) — 삽입 순서를 섞어도 결과 동일
   ⓑ 수기 문서(05_필드 인벤토리.md)가 생성기 라벨 사전을 오염시키는 사고(P98)
      — 쓰레기 라벨 색인 0건
   ⓒ 생성기 간 출력 문자열 계약 파손(O46 §3-①의 'SV metric — ' 접두 계약)
3. 🔴 P97·P100 을 절차로 강제하는 체크리스트를 테스트에 포함한다 —
   「불가/경합의 원인을 서술한 행은 그 섹션의 필드 목록을 열거했는가」를
   기계 검사할 수 있는 형태로(근거 칸에 필드명이 실제로 등장하는지).
4. 실행 방법을 02_GN_DW_building/06_RUNBOOK.md 에 1개 절로 기재한다.

제약: dbt build 불요. 생성기 수정 시 즉시 스테이지 업로드 후 cortex ws ls 로 확인(§9 P102).
```

### 작업 2. 🔴 **진행중 — 실측 완료 · 반입 미완** (2026-08-07 O49) — §2-C

> ⛔ **차단 사유: 계정 정지** — `000666 (57014) Your account is suspended due to lack of payment method`.
>   웨어하우스 실행 불가 → `dump_schema.py`·`census_columns.py` 재생성과 `09` 재생성이 막혔다.
>   `show imported keys` 는 되고 **집계·INFORMATION_SCHEMA 는 안 된다**(WH 필요 여부로 갈린다).
>
> ✅ **정지 이전에 실측은 전부 끝냈다** — 정본 = `20_issue/00_INDEX_이슈원장.md` **§O49**.
>   착수 실측에서 **처방을 심기 전에 판정 자체가 틀린 것 17건**이 나왔다(원 범위 밖).
>   | 발견 | 실측 |
>   |---|---|
>   | ① 2-1 「**주간**」 중단보고 앵커가 **월 팩트**(`FMM`) → 주차 measure **6건 오판** | `FME` 월 롤업 = `FMM` **466개월 전부 일치**(STOP 1,038,262·DEV 2,291,878) → **앵커 교체 손실 0**. 센티넬 `DATE_SK=0` **90행**(DEV 롤업 차 88 과 일치) |
>   | ② 3-7좌 「도달불가」 **7건 오판** | `DIM_MEMBER_IDENTITY` **1,763,066 전건 유일 = 1:1** · 2홉 조인 **68,836 불변** · 성별 실회원 **100%** · ⚠️ `IDENTITY_SK=0` **31.6%** 는 분모 주의 |
>   | ③ 「중단(명)」 **4건 처방 오류**(P79 재발) | `SUM(STOP_MEMBERS)=SUM(STOP_CNT)=COUNT(*)` · 주차 「명」 합계 **972,942 vs 실제 903,064 = 7.7% 과대** → **비가산 `COUNT(DISTINCT)`** |
>   | ④ 「가입일」·「중단일」 = §2-G 해소 | 중단일은 **원천·값 동일**(불일치 **0**/898,425) · 가입일은 **원천 다름**(불일치 27,305 = 처리지연 median +2일) · `FMM.JOIN_DATE`/`STOP_DATE` **전건 NULL** ⇒ **앵커 교체로 흡수, 신규 라벨 불필요** |
>   | ⑤ 1-2 경합 처방 = ✅ **성립** | 사전집계 후 결합 → **40,054,883 불변** · DEV·청구액 불변 · `fmf_only_keys` **0** |
>   🆕 **P104**(컬럼명이 다르다는 것은 다른 축의 증거가 아니다 — dbt `as` 절까지 계보를 본다).
>
> ▶ **재개 시 이것부터** (계정 복구 후)
> ```
> 1. python3 scripts/dump_schema.py && python3 scripts/census_columns.py
>    ⚠️ /workspace 에서 실행하면 스테이지 마운트 I/O 오류가 난다 — $HOME 에 복사해 실행할 것
> 2. gen_section_assembly.py 에 등록부 3종 + 신규 판정 신설
>      SECTION_ANCHOR_OVERRIDE  2-1 → FACT_MEMBER_EVENT (근거: 월 롤업 466개월 전부 일치)
>      REQUIRED_TIME_GRAIN      필드명 '*주차'·섹션명 '주간' → 요구 grain 파싱 후 앵커 grain 과 대조
>                               → 신규 판정 「요구grain부정합」
>      IDENTITY_BRIDGE          FGA.IDENTITY_SK → DIM_MEMBER_IDENTITY.MEMBER_DK → DIM_MEMBER/ACQ (1:1)
>      NON_ADDITIVE_MEASURE     *_MEMBERS 계열 → COUNT(DISTINCT) 처방 · 주차→월 재합산 금지
> 3. 검증된 SQL 을 등록부에 코드블록으로 반입(§O49 의 값이 곧 기대값 = 회귀 기준)
> 4. 09 재생성 → 판정 diff 를 전량 규명 → python3 scripts/test_generators.py --update-golden
> 5. 🔴 F4 미검증 — 「기준일자」가 1-2(FMF)=조립가능 vs 2-1(FMM)=도달불가로 갈린다.
>    FMF 의 DIM_DATE 경로는 LAST_PAY_DATE_SK(최종납입일)·LAST_BILL_DATE_SK 뿐 = 「기준일자」가 아니다.
>    FK 존재만 보고 의미를 안 보는 오판 의심 — 규모 미측정.
> ```
> 🔴 **경고** — 재생성 전까지 `09` 는 위 17건이 **틀린 상태**다. 골든은 그 상태를 고정하고 있으므로
>   **골든 통과는 정확함을 뜻하지 않는다**(골든은 회귀만 잡는다).

<details><summary>원 프롬프트(이력)</summary>

▶ **프롬프트**
```
99_NEXT_SESSION.md §2-C 를 근거로, 09 의 ◐집계필요 6건과 앵커_경합 34행에 대해
"처방을 실제로 실행해" 검증하고 그 결과를 09 에 심어라. P90 자기적용이다.

요건:
1. 집계필요 6건(2-1 중단(명) 3종·가입일·중단일 / 2-2 중단(명)) 각각에 대해
   앵커 grain 으로 사전집계한 뒤 조인하는 SQL 을 **실제로 실행**하고
   ⓐ 행수 불변 ⓑ 측정치 합계 불변 을 실측해라. 하나라도 깨지면 판정을 되돌린다.
2. 앵커 경합 2섹션(1-2 FMF/FMM 19행 · 3-7좌 FGA/FSE 15행)에 대해
   "쿼리를 나눈 뒤 공통 축에서 합친다"를 **실제 SQL 로 작성·실행**해
   양쪽 결과가 정합하는지 실측해라.
3. 검증된 SQL 을 09 의 §3-B(집계필요)·앵커 경합 전용 절에 **복사해 쓸 수 있는
   코드 블록으로** 넣어라. 생성기(gen_section_assembly.py)의 등록부에 넣어
   재생성해도 유지되게 한다 — 수기로 md 를 고치지 말 것.
4. 🔵 §2-G 재검토: 2-1 「가입일」·「중단일」이 사전집계 문제인지 대표값 규칙
   문제인지 실측으로 판정하고, 후자면 라벨을 옮겨라.

제약: dbt build 불요. 결과 확정 후 작업 1의 골든 파일을 갱신한다.
```
</details>

### 작업 3. 🔴 SERVING 반영 — SV 9종·Agent 에 판정 4종·경합·DEC-31 동일값 — §2-D·2-E

▶ **프롬프트**
```
99_NEXT_SESSION.md §2-D·2-E 를 근거로 SERVING 계층에 O47 결과를 반영해라.
O45→O46 에서 이미 한 번 겪은 실패(GOLD 까지만 끝남)의 재발이다.

착수 전 필독: 05_SV-Agent_ai/04_SV_설계.md §6.9(구조적 제약 8건)

요건:
1. SV COMMENT 회수·보강(P61) —
   ⓐ SV_MEMBER_MONTHLY · SV_MEMBER_FEE: 「합산 금지」에 더해
      **DEC-31 실측 = 회비 measure 10종은 어느 쪽을 써도 값이 같다**를 명시.
      단 FMF 로 재현 불가한 FMM 고유 4종(ONETIME_ONETIME_FEE·PAID_SPONSOR_BIZ_CNT·
      IS_MULTI_PAID_BIZ·UNPAID_FLAG_BOM)은 **FMM 전용**임을 함께 적는다.
   ⓑ 「집계필요」에 해당하는 질문(주차별 중단 등)은 "불가"가 아니라
      **사전집계 경로**로 라우팅하도록 지침을 넣는다.
   ⓒ 「중단 분해」는 코드5 경로(TOTAL_CAMPAIGN_STOP_CNT)로 라우팅 — DEC-32 철회 반영.
      STOP 브랜치 축은 앞으로도 (미매핑)이 정상이라는 사실을 적는다.
2. Agent instruction — 앵커 경합 2섹션 유형의 질문(회비+개발실적 동시 · CRM+GA 혼합)에서
   **한쪽 팩트만 골라 답하지 말고 표를 분리**하도록 규칙을 넣는다.
3. 🔴 수치는 COMMENT 에 넣지 않는다(작업 규칙 7). 「동일값」은 사실 서술로만 적는다.
4. 배포·검증 — SV 파일 수정 → 스모크 → Agent 버전업(§5 절차)
   → SHOW VERSIONS IN AGENT 로 is_default **실측**(P66).
5. SV_MEMBER_FEE 스모크 1회(O45-C 재빌드 후 미실시).

제약: SV 는 CREATE OR ALTER(GRANT 보존). cortex_agent_save/deploy 금지.
```

### 작업 4. 🟠 관문을 dbt 테스트로 승격 — §2-F

▶ **프롬프트**
```
99_NEXT_SESSION.md §2-F 를 근거로, 손으로 반복 검증하던 관문을 dbt 테스트로 승격해라.

승격 후보(직전 세션에 손으로 재검증한 O47 근거 5종 + GATE-D):
1. GATE-D — FMF·FMM·SILVER 청구 총계 3자 일치
2. FMF→(회원,월) 롤업 행수 = FMM.HAS_BILLING 건수
3. FMM ⋈ (FSE→회원·월 사전집계) 행수·측정치 불변 (팬아웃 0)
4. FAP ⋈ FAD_B 행수 불변 (1:1 위성)
5. 코드5 경로 SPONSORSHIP_SK 채움 100% (DEC-32 근거의 회귀 감시)
6. 형제팩트 naive 조인이 과대계상되는지 — **가드가 살아 있는지**를 감시하는 형태로

요건: singular test 로 작성 · severity 는 회귀 성격에 따라 판단하고 근거를 적어라.
🔴 기대값을 테스트에 하드코딩하면 데이터 입고로 낡는다 — **관계식으로** 쓴다
   (예: "두 값이 같다"는 되지만 "891,959,790,888 이다"는 안 된다).

⛔ 작성 후 dbt build 순서에서 멈추고 사용자 입력을 기다린다(작업 규칙 5).
```

### 작업 5. 🟠 현업 질문서 마감 — 회신 없이 진행할 수 없는 것 확정

▶ **프롬프트**
```
20_issue/20_현업확인_요청.md 를 전부 읽고, 회신 대기 항목을 사용자가 그대로
현업에 보낼 수 있는 상태로 마감해라.

요건:
1. §L-1(DEC-33 발송 유형 3건) · §L-2(중단보고 후원사업 = 끊은 사업 vs 데려온 사업)
   · §I-2 #4(O28 오염 4행) 를 점검한다.
2. §I-2 #4 에 정정 후보 `110` 의 **코드 라벨(`Success`, MS304)** 을 명기해라 —
   현재 저장값만 있어 현업이 무슨 값인지 알 수 없다.
3. 각 질문에 **"회신이 없으면 무엇이 막히는가"** 를 한 줄로 붙여라.
4. 회신 없이도 진행 가능한 항목과 하드블로커를 분리해 표로 정리해라.
```

### 작업 6. ⬜ 미착수 이월 (여력이 있으면)

| # | 항목 | 근거 |
|---|---|---|
| 1 | **O42-B** 조용한 반올림 20컬럼 스케일 검토 | 00 §O42 |
| 2 | **O39-B** 전건 0 컬럼군 정리 | 00 §O39 |
| 3 | **O44-B** 마케팅캠페인 축을 FAD 위성 3종에 전파 | 00 §O44 |
| 4 | **O39-C** 개명 결정 `*_MEMBERS`→`*_FLAG` (WIDE·SV·문서·저장쿼리를 깬다 → 영향범위 조사 후 사용자 결정) | 10 §23-E |
| 5 | **O32** COMMENT 수치 제거 (사용자 수동 · 🔴자동화 금지 — 코드값·한국어가 깨진다, P58) | 06_DDL·08_SILVER·dbt |
| 6 | 거짓 주석 회수 1건 — `models/gold/dim/DIM_MEMBER_CURRENT.sql` 헤더가 없는 3컬럼을 열거(P33) | — |
| 7 | `FK_DIM_CAMPAIGN_MKTG`(차원→차원 = 스노플레이킹) **설계 판단 미기록** | 06_DDL §관계제약 |
| 8 | **GA4 샤드 2일** — `events_20260719` 에 컬럼 1개 추가. 문서의 「1일 샤드·4.22%」는 stale | 00 §O43 |
| 9 | 「브랜드2」 정체 현업 확인 — CRM `BRAND`(69종) vs AGENCY `MEDIA_NAME` | 05_필드 인벤토리 |
| 10 | `SV_AD` 제약 재검증 · 다른 SV 의 **시점 축** 점검(P60) · `FALLBACK` 회원 처리 | — |
| 11 | 🔴 **교훈 정본이 갈라져 있다** — 문서맵은 *"교훈 정본 = 문서10"* 이라 하는데 문서10 은 **P83 에서 멈췄고 P84~P102 19건이 원장에만** 있다(실측). 정본을 원장으로 바꾸거나 문서10 으로 이관하거나 **택일**해야 한다 — 지금은 어느 쪽을 봐도 결손이 생긴다 | 00 문서맵 vs 10 |

---

## 4. 현재 상태 (2026-08-07 실측)

| 계층 | 상태 |
|---|---|
| BRONZE | **51**테이블 (CRM 43 · AGENCY 4 · ERP 1 · GA4 3) |
| SILVER | **39**테이블 |
| GOLD | **31**테이블(DIM 17 + FACT 14) + 뷰 **16** · 정보성 FK **50** |
| SERVING | 뷰 **3** · SV **9**종 · Agent **2**종(`AGENT_MEMBER`·`AGENT_OVERALL` · VERSION$4) |
| 최근 build | `PASS=375 WARN=27 ERROR=0 SKIP=0 TOTAL=402` · 모델 86 · 테스트 316 · source 40 |
| `30_output_share` | **17종 전량 최신**(2026-08-07 재생성 · 파일 단위 크기 대조 완료) |

**`09` 판정 분포 (507행)** — 조립가능 **267(52.7%)** · 판정불가(SV파생) 92 · 값없음 85 · 도달불가 21 ·
원천부재 10 · 판정불가 9 · 배분규칙필요 7 · 형제팩트중복 6 · **집계필요 6** · grain부정합 2 · 타원천 2
· **앵커_경합 34행**(1-2 회원 19 · 3-7좌 15)

**SV 9종** — `SV_MEMBER_MONTHLY` · `SV_MEMBER_FEE` · `SV_MEMBER_EVENT` · `SV_MEMBER_COHORT` ·
`SV_SERVICE` · `SV_EVENT_PARTICIPATION` · `SV_BUDGET` · `SV_AD` · `SV_DEV_ACHIEVEMENT`

> 🔴 **재구축(`06_DDL`·`08_SILVER`)을 평상시에 다시 실행하지 말 것** — 정본 ↔ 물리 불일치 0이다.
> 재실행하면 데이터만 날아간다(런북 §11).

**신규 계정 재현 순서**
`07_ENVIRONMENT_RBAC_setup` → `06_DDL` → `08_SILVER_테이블DDL` → `deploy_dbt_project` → **dbt build**
→ `08_After_Deploy_DBT` → `05_1`~`05_9_SV_DDL_*`(독립) → `09_1_AGENT` → **`09_2_AGENT_버전업`**

🔴 자주 빠지는 3곳: ① `07` 누락 → `06_DDL.sql` 즉사 ② `dbt build` ③ **`09_2`**(`09_1` 은 껍데기).
⛔ 실행 금지(포인터 스텁): `13_SV_AD_배포_추가작업.sql` · `09_AGENT_spec_구현.sql` · `02_SERVING_setup.sql`.

---

## 5. Agent 스펙 — 정본 경로와 배포 절차

🔴 **정본은 `cortex_project/agents/<AGENT>/agent_spec.yaml` 하나다.** 파일명 반드시 `agent_spec.yaml`
(공식 문서의 `agent.yaml` 예시는 부정확 — 틀리면 `No spec file present` 로 실패).

⛔ 루트 `cortex_project/AGENT_*.agent.yaml` 은 `cortex_project/_archive/` 로 이관 — **복원 금지**.
🔴 **`cortex_agent_save`/`cortex_agent_deploy` 를 쓰지 말 것** — live 버전을 만들어 `ADD VERSION FROM` 을 거부시킨다.

**절차**: 스펙 수정 → `ALTER WORKSPACE USER$.PUBLIC."snowflake_files" COMMIT` → `09_2_AGENT_버전업.sql`
→ `SHOW VERSIONS IN AGENT` 로 `is_default` **실측** → 아니면 `SET DEFAULT_VERSION = 'VERSION$n'`(P66).

---

## 6. 현업 회신 대기 (사용자 소관)

| # | 항목 | 회신이 없으면 막히는 것 | 문서 |
|---|---|---|---|
| 1 | 🔴 **DEC-33 발송 유형 3건** — 코호트 모집단 정의 / 귀속 기간 / 채널 차등 | `FSE.D5_*` 8컬럼 = 발송 효과성 필드군 | 20 §L-1 |
| 2 | 🔴 **중단보고 「후원사업」** = (가) 끊은 사업(코드5) vs (나) 데려온 사업(획득 차원) | 중단보고 후원사업 축 확정 | 20 §L-2 |
| 3 | 🟠 **O28 오염 4행 정정** — 후보 `110`(`Success`) | `PART_STATUS` 정합 | 20 §I-2 #4 |
| 4 | 🟠 **O28 소정수 1~6 의미**(캠페인행사) | FEP 상태별 카운트 5종 | 20 §I-2 |
| 5 | 🟠 **O45-B** `SETLE_CD` 미라벨 5종(225,855행 · 0.48%) | `SV_MEMBER_FEE` 결제수단 라벨 | 00 §O45 |
| 6 | 🟠 **Q10** 광고비 배분 규칙 | 개발캠페인 grain ROAS | 20 |
| 7 | 🟠 **O19** `DIM_SPONSORSHIP` 6종 코드 라벨 · **O5** `GA_CONV_CNT` 어의 | SV 차원 라벨 | 20 |
| 8 | 🟠 **CONF-3** `ACTIVE_CNT`(활동회원 분모) · **CONF-4** 상위 조직 산출규칙 | 활동회원 지표 · 본부/지부 분해 | 10 §25-C |
| 9 | 🟠 **O38-D** 개발목표가 「신규」에만 편성되는 것이 방침인가 · `GOAL_CNT=0` 의 의미 | `SV_DEV_ACHIEVEMENT` 달성율 비대칭 | 00 §O38-D |

**🔴 외부 입고 하드블로커 4** — **G-5** GA4 전기간 · **E-6** CRM 사업목표(`FACT_TARGET_BIZ` 0행) ·
**E-1** ERP 모금성비용 · **BLOCKING-1** 회원 마스터 9,247명(해소 시 WARN 14건 자동 소멸)

**사용자 소관 기타** — **SERVING NL 스모크**(트라이얼 `DATA_AGENT_RUN` 차단 → paid 계정) ·
🔴 **OPS-1 GitHub PAT revoke·재발급**(보안 · 미처리)

---

## 7. 필독 문서 (순서대로)

| # | 문서 | 왜 |
|---|---|---|
| 1 | `20_issue/00_INDEX_이슈원장.md` | **이슈 정본**. 최상단 「최신 상태」가 시간 역순. O47 = 최상단 블록 §1~§18 |
| 2 | `20_issue/30_설계_의사결정.md` §22 | DEC-31(종결) · DEC-32(철회) · DEC-33(성격 변경) · §22-A 검사기 오판표 |
| 3 | `20_issue/10_진단_원인분석.md` | 진단·교훈 전문 |
| 4 | `30_output_share/09_보고서필드_조립가능성.md` §1·§3-B | 판정 11종의 의미표 · 집계필요 전량 · 앵커 경합 |
| 5 | `05_SV-Agent_ai/04_SV_설계.md` **§6.9** | 🔴 **SV 를 만지기 전 필독** — 반복 위반되는 구조적 제약 8건 |
| 6 | `02_GN_DW_building/06_RUNBOOK.md` §3.3·§11 | 평상시 컬럼 변경 / 전체 재구축 |
| 7 | `20_issue/50_dbt_파이프라인_미결조치.md` §warn→error 추적표 | WARN 27 정본·복귀조건 |

---

## 8. 교훈 색인 — 직전 세션 신규 (P95~P102)

> 상세는 이슈원장 최상단 O47 블록. 🔴 **P95~P101 의 공통 원인은 하나다: 결론을 먼저 쓰고 근거 개체를 나중에 열었다.**

- **P95** **「grain 부정합」은 판정이 아니라 질문이다.** 축이 다르다는 사실만으로 닫으면 ①집계로 해결되는 것
  ②형제 팩트 중복 ③배분규칙 부재 ④축 부재가 한 라벨에 섞인다 — 해소 주체가 쿼리·설계·현업·외부로 **전부 다르다.**
  → 「불가」를 쓸 때 **방향**(coarse→fine 인가 fine→coarse 인가)을 반드시 적고, 팬아웃은 **사전집계 전/후 두 값**으로 적는다.
- **P96** **팩트 내 degenerate 와 접두어 붙은 FK 는 이름 규칙 검사기에 보이지 않는다.**
  → ① FK 후보는 **`show imported keys` 실선언**에서 읽는다 ② 차원 탐색 전에 **앵커 팩트 컬럼 집합을 먼저** 본다
  ③ 1:1 위성은 grain 비교 대상이 아니다 — **키 유일성**으로 판정한다.
- **P97** **「가능하다」와 「해야 한다」는 다르다.** 원천 채움률만 보고 「축이 살아 있다」로 판정하면
  그 축이 **이미 다른 브랜치·팩트에 배선돼 있을 때 이중 축**을 만든다.
  → ① 축 신설 전에 **같은 개념이 다른 이름으로 이미 있는지** 모델 주석·census 를 훑는다
  ② **「합산 금지」 주석이 붙은 measure 는 대체 경로의 존재 신호다.**
- **P98** **수기 문서가 생성기 입력이면, 문서 편집은 코드 변경이다.** `05_필드 인벤토리.md` 는 사람이 읽는
  설계문서이자 생성기의 **라벨 사전**이다 — 「주석을 하나 더 달았다」가 매핑을 바꿀 수 있다.
  → ① 편집 후 **생성기를 재실행해 산출물 diff 확인** ② 파서는 라벨 칸과 주석 칸을 구별한다
  ③ `setdefault` first-wins 인덱스는 **문서 순서에 판정을 의존**시킨다 — 충돌 시 경고를 낸다.
- **P99** **스테이지 마운트에서는 「복사했다」가 「존재한다」를 뜻하지 않는다.** `cp` 종료코드와 직후 `ls` 가
  **둘 다 통과하면서** 파일이 남지 않을 수 있다(실측: 산출물 3종 유실).
  → ① 파일 단위 복사 + **크기·해시 대조** ② 배치 후 **기대 목록 전수 대조**(결손·여분) — 개수만 세면 못 잡는다.
- **P100** **경합·차단의 「원인」을 쓸 때는 그 섹션의 필드 목록을 실제로 열어봐야 한다.**
  집계 수치(동수 2:2)만 보고 원인을 서사로 채우면 **그럴듯하지만 틀린 인과**가 만들어진다.
  → **파생 컬럼(월 롤업 등)이 경합을 만들 수 있다** — 원천 소재지와 파생 소재지를 구별한다.
- **P101** **시간 근접성은 인과가 아니다.** D+0 73.1% 는 강력한 신호처럼 보였지만 그 발송의 정체는 **중단 통보**였다.
  → ① 귀속창을 정하기 전에 **원인 후보의 내용(제목·유형)을 열거** ② 처리 통보성 이벤트는 코호트에서 먼저 배제
  ③ 분포가 한 버킷에 쏠리면 **동일 배치를 먼저 의심**한다.
- **P102** 🔴 **`/workspace` 마운트는 스테이지가 아니다 — `ls` 로는 반영을 확인할 수 없다.**
  P99 를 한 단계 더 좁힌 실측: `write` 로 루트 파일을 쓰면 **마운트에서는 `ls`·`wc` 가 정상 통과**하는데
  **스테이지에는 아예 존재하지 않았고**(구판조차 없었다), 몇 분 뒤 **마운트에서도 사라졌다.**
  같은 세션에 하위폴더(`20_issue/`)의 `edit` 는 **정상 반영됐다.**
  → ① **검증은 `cortex ws ls` 로 한다**(마운트 `ls` 는 근거가 아니다)
  ② 스테이지 파일 크기는 **16바이트 배수로 패딩**된다 — 크기 대조는 `0 ≤ 스테이지−원본 < 16` 으로 판정한다
  ③ 반영이 없으면 **`cortex ws cp <원본> 'USER$.PUBLIC."snowflake_files":/<폴더>/'`** 로 명시 업로드한다
  ④ 🔴 `cp` 의 원본은 `/workspace`·`/tmp` 를 쓰지 말고 **`$HOME`** 에 두고 올린다 — 전자는 둘 다 사라진다.

**기존 핵심 색인**
- **P57** 물리 `ALTER` 만 하고 정본 DDL 에 접지 않으면 다음 재구축에서 소실
- **P61** 부정형 서술(*"미적재/비활성/불가"*)은 **축 활성화 때마다 회수** — SV·Agent instruction·**도구 description** 까지
- **P62-B** **자기교정은 전파되지 않는다** — 인용처를 `grep` 으로 찾아 함께 고친다
- **P76** 회수 판정 = 「사실 여부」가 아니라 **「그 답으로 사용자가 옳은 답을 얻는가」**
- **P79** metric 정의가 옳아도 **synonym 이 틀리면 오답**
- **P82** `ERROR=0` 은 성공 신호가 아니다 · **P85** 「자동 생성물」 표기가 신선함을 보장하지 않는다
- **P90** 검사기가 「가능」이라 하면 **그 조인을 실제로 실행해 행수·합계를 재라** · `try/except import` 는 조용한 실패
- **P94** **가용성 추정은 낙관 편향**이다 — 실측하면 방향이 한쪽(OK→PARTIAL/WAIT)으로만 움직인다
- **P67** 합계 일치는 grain 정확성의 근거가 아니다 · **P60** 시점 스냅샷을 「현재 속성」으로 노출하면 값이 맞아도 답이 틀린다
- **PROC-3** 기대값이 어긋나면 **원인 규명 전에 어느 쪽도 인용하지 말 것**. (c) 손계수 ↔ 프로그램 실측이 다르면 **실측이 정본**

---

## 9. 운영 환경 지뢰

- 🔴🔴 **P102 — `/workspace` 마운트의 `ls` 는 반영 증거가 아니다.** 반영 확인은 **반드시**
  `cortex ws ls 'USER$.PUBLIC."snowflake_files":/'` 로 한다. 루트 파일 쓰기가 특히 자주 소실된다.
  ⚠️ 스테이지 크기는 **16바이트 배수로 패딩** → 대조는 `0 ≤ 스테이지−원본 < 16`.
- 🔴 **작업 사본은 `$HOME/work/` 에 둔다.** `/tmp` 는 세션 중에도 초기화되고(이 세션에 2회),
  `/workspace` 루트는 쓴 파일이 사라진다(P102). 스테이지 업로드의 원본은 `$HOME` 을 쓴다.
- **업로드**: `cortex ws cp $HOME/work/<파일> 'USER$.PUBLIC."snowflake_files":/<폴더>/'`
  (⚠️ 목적지는 **폴더**이고 `/` 로 끝나야 하며 **리네임 불가** · 다운로드는 `/versions/head`, 업로드는 `/versions/live`)
  ⚠️ `cortex ws ls` 에 `/versions/head/` 를 직접 붙이면 경로가 **중복**돼 0건이 나온다 — `:/` 만 쓴다.
- 🔴 **생성기를 수정하면 즉시 업로드하고 `cortex ws ls` 로 확인**한다 — 과거에 이 장애로 생성기 소스 1종이 실제로 유실됐다.
- 🔴 **샌드박스 python 은 `sys.path[0]` 이 runfiles 로 고정** → `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))`.
- 산출물 재생성 순서: `dump_schema.py` → `census_columns.py`(수 분 · 백그라운드 권장) → `gen_column_mapping.py`(04)
  → `gen_metric_gold_mapping.py`(05) → `gen_section_assembly.py`(09) → 그 외 독립(03·06·07·08).
  🔴 재생성 전에 **입력 파일 존재를 먼저 확인**한다(`05` 생성기가 `04` CSV 를 출력 디렉터리에서 찾는다).
- SQL 도구 토큰 만료 시 python `snowflake.connector` + `/snowflake/session/token`(매번 새로 읽는다) = `scripts/sfconn.py`.
- `hashlib.md5` 는 FIPS 모드에서 실패 → `sha256` · CTE 명에 `asof` 금지(`ASOF JOIN` 예약어 충돌).
- 트라이얼 계정: `SNOWFLAKE.CORTEX.DATA_AGENT_RUN` **차단** → NL 검증은 사람이 Snowsight UI 에서.

---
_Co-authored with CoCo_
