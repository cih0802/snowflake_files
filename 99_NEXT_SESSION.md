<!-- LLM-METADATA
doc_id: NEXT_SESSION_HANDOFF
doc_role: 다음 세션 인수인계 — 착수 순서·필독 문서·미결 목록
project: GN_DW (굿네이버스)
written: 2026-08-03 (O27·O28·O29 세션 종료 시점 · 전면 재작성)
updated: 2026-08-04 (종반 — 센티넬 전수감사 · 미주입 재계수 · GA 데이터검토 종결 · 귀속처 14건 결정. 정본 = 문서10 §18)
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# 99. 다음 세션 인수인계

## 🟢 현재 상태 (2026-08-04 종반 · 문서10 **§18** 이 정본)

**2차 `dbt build` 완료** — `PASS=329 WARN=26 ERROR=0 TOTAL=355`. §17-E 기대치 **전 항목 일치**(`EVENT_BK` 100% · 3키 유일 PASS · WARN 26).

🔴 **사용자 결정(2026-08-04): GA 는 「지금 만들어진 것의 데이터 검토」까지만 하고, 설계 수정·구현은 phase-2 로 이월한다.**

| 이번 세션에 한 일 | 결과 |
|---|---|
| 🔴 **센티넬 축 전수감사**(P51) | **100% 센티넬 FK 13건 / 7팩트** · 부분 7건 · **도달 불가 차원 2건**(`DIM_PAYMENT` 7행 · `DIM_AD_CREATIVE` 8,716행) → 🆕 **P52** |
| 🟢 **GOLD 미주입 재계수** | **106 / 371 (28.6%)** = ALL_NULL 44 + ALL_ZERO 62. 그중 8건은 **0행 테이블**(`FACT_TARGET_BIZ`) → 실질 **98/363(27.0%)** → 🆕 **P53**. ⚠️ 종전 `125/454` 는 **인용 금지**(분모 구성 불명) |
| 🟢 **GA 데이터검토 종결** | measure 가산성 전수(가산 3종 배수 **정확히 1.0000** · `SESSION_CNT` **3.1508** · `ENGAGED_SESSIONS` **4.2959**) · `PAGE_PATH` 정본 #105 **미충족**(거짓주석) · `PAGE_LOCATION` URL **88.9% 소실**(MAX 대표) · #122·#111 원천 실재 확인 · **URL `memnum` identity 여력 +77.1%**(충돌 4) → 🆕 **P54·P55** |
| ✅ **COMMENT 가드 21컬럼 배포** | 산식·구조 변경 **0**. FACT/WIDE/DIM 3계층 + 정본 `06_DDL.sql` 동기화. 검증 21/21 |
| 🟢 **귀속처 14건 결정** | **11 귀속확정 + 3 잔여** — 판정표 CSV 반영 완료(§18-E). 구현은 phase-2 |
| 🔴 **최종검토에서 내 오판정 1건 자체정정** | `PAGE_REFERRER` 귀속처를 「`DIM_GA_PAGE` 역할수행」→ **`DIM_GA_REFERRER` 신설**로 철회·정정(§18-H). 근거: referrer host **206종 중 자사 11종** · path 정확일치 **857/1,678(51.1%)** · 외부 **425종(25.3%)** → 🆕 **P56**. 자가검토 결함 **10건** 전수 기록(§18-G) |

🔴 **다음 `dbt build` 가 필요하다** — `WIDE_GA_BEHAVIOR` 의 경고 COMMENT 는 지금 **물리 ALTER 로만** 보호된다. 모델 `post_hook` 은 수정·`dbt compile` 검증 완료이므로 다음 빌드에서 확정된다. **빌드 전에 GOLD 뷰를 재생성하는 작업을 하면 경고문이 사라진다.**

⬜ **남은 결정 1건(사용자)**: 잔여 3건(`GA_SESSION_NUMBER`·`LINK_TEXT`·`LINK_URL`)은 **원인이 하나다** — GOLD 에 GA 원자 grain 팩트가 없다. → *"`FACT_GA_SESSION`·`FACT_GA_CLICK` 을 신설할 것인가"* 단일 질문. phase-2.

---

## 🗄️ [이력] 종전 지시 — `dbt build` 1회 더 (✅ 완료됨)

**1차 빌드는 완료됐다**(`PASS=319 WARN=25 ERROR=0 TOTAL=344`) — 검증 결과 예측치와 **전 항목 정확 일치**(문서10 §17-A). 그 검증 과정에서 결함 1건이 드러나 **추가 수정**했고, 그 적재를 위해 빌드가 1회 더 필요하다.

```
dbt build                                   ← 사용자 실행
```

| 추가 반영분 | 내용 |
|---|---|
| 🔴 `FEP.EVENT_BK` **신설** | 고아 행사 **53개·263,611행(23.2%)** 이 `EVENT_SK=0` 으로 뭉개져 **서로 구별되지 않았다**(정보 손실). 원천 업무키를 degen key 로 보존 |
| `WIDE_EVENT_PARTICIPATION` | `PART_EVENT_BK` 노출(차원측 `EVENT_BK` 와 구분) |
| 🔴 테스트 **14종** | 1차 빌드 때 **파일쓰기 실패로 하나도 등록되지 않았다**(문서10 §17-C). 복구 후 `dbt list` 로 등재 확인 완료 |

기대: `EVENT_BK` 채움 **1,134,126(100%)** · `unique(EVENT_BK,MEMBER_DK,PARTCPT_SEQ)` **PASS**(사전검증: SILVER 3키 distinct = 행수) · `PART_STATUS` accepted_values **WARN 1건**(오염값 `)` 2행) → **WARN 25→26** · TOTAL 344→358 내외.

🔴 **빌드 후 반드시 확인**: `PASS`/`TOTAL` 증가분이 위 기대와 맞는지. `ERROR=0` 만 보면 **테스트가 0개여도 통과로 보인다**(P50 — 이번에 실제로 당했다).

---

## 🟢 1차 빌드 검증 결과 — 전 항목 정확 일치 (2026-08-04)

| 항목 | 기대 | 실측 |
|---|---:|---:|
| `FSE` 행수 | 38,470,780 | **동일** |
| `FSE.SEND_TYPE_SK` 비센티넬 | 8,300,272 (21.58%) | **동일** · 고아 0 · NULL 0 |
| `DIM_SEND_TYPE` | 66행(65+센티넬) | **동일** · SK 유일 |
| `DIM_EVENT.RECRUIT_HEADCOUNT` SUM | 4,513,184 | **동일**(행사 grain 참값) |
| `DURATION_SEC` | {30,60,90,120} 초 | **동일** · 채움 1,151 → **32,739** |
| `FEP` 행수 | 1,134,126 | **동일** |

→ **O29 96.6% 무성 소실 복구 완료** · µs 미확정값은 의도대로 NULL 유지.

---

## 🆕 직전 세션(2026-08-04)에서 무슨 일이 있었나 — 먼저 읽을 것

**구조 변경(ALTER/CREATE)은 이미 물리에 적용됐고 모델·정본DDL·테스트도 수정됐다. 적재만 남았다.**

```
dbt build                                   ← 사용자 실행
그 후 03_top-down_gold/DEC30_STRUCTURE_ALTER.sql §6 검증 9종 실행
```

🔴 **빌드 전에 GOLD 뷰를 재생성하는 다른 작업을 하지 말 것** — 물리 컬럼 DROP 이 이미 일어났으므로 **모델 수정본이 반영되기 전에는 WIDE 2뷰가 깨진 상태**다(빌드가 고친다).

| 반영 대상 | 변경 |
|---|---|
| 🆕 `GOLD.DIM_SEND_TYPE` | **신설**(12컬럼) — 발송구분 대/중/소 65경로 + 센티넬 |
| `FACT_SERVICE_EVENT` | `SEND_TYPE_SK` 신설(FK) |
| `DIM_EVENT` | `RECRUIT_HEADCOUNT` 신설 |
| `FACT_EVENT_PARTICIPATION` | `PARTCPT_SEQ` 신설(degen key) · **`RECRUIT_CNT` DROP** |
| `DIM_GA_SOURCE` | `DEFAULT_CHANNEL_GROUP` 신설 |
| `DIM_SERVICE` | **`SEND_TYPE_L/M/S` DROP**(→ `DIM_SEND_TYPE` 이관) |
| `DIM_AD_CREATIVE` | **`DURATION_SEC` DROP**(오배치 중복축) |
| `SILVER.AGENCY_AD_BROADCAST` | O29 **HH:MM:SS 파싱 배선** — 커버리지 3.2% → **93.1%** 예상 |
| WIDE 2뷰 | `WIDE_EVENT_PARTICIPATION`·`WIDE_SERVICE_EVENT` 재배선(파괴 의존성 해소) |
| 테스트 | `_gold_ready_schema.yml` — 신설 차원 PK/grain · degen key 유일성 · O28 오염값 warn · FSE FK |

기대 검증치: `DIM_SEND_TYPE` **66행**(65+센티넬) · `FSE.SEND_TYPE_SK` 비센티넬 **8,300,272(21.58%)** · 고아 **0** · `DIM_EVENT.RECRUIT_HEADCOUNT` SUM **4,513,184** · `FEP` 행수 **1,134,126 불변** · `FACT_AD_BROADCAST.DURATION_SEC` 값 **{30,60,90,120}** 만.

---

## 🆕 직전 세션(2026-08-04)에서 무슨 일이 있었나 — 먼저 읽을 것

**사용자가 `dbt build` 를 실행했고**, 본 세션은 **빌드 반영 검증 → O28·O29 COMMENT 가드 배포 → 문서 갱신** 을 했다. 진단 정본 = **20_issue/10_진단_원인분석.md §15** · 결정 정본 = **30 DEC-29**.

| 구분 | 내용 |
|---|---|
| 🟢 **빌드 반영 확인** | §2-4 사전검증치와 **전 항목 완전 일치**. `DIM_MEMBER` 7,925,716행·회원 1,763,065 불변 · 신규 4컬럼 적중 전부 일치 · `FMM` 회비 3분해 3값 일치 · `DIM_EVENT` 3,787 · **GOLD `'미상'` 완전 소멸** → **O27 산출물 확정**(§15-A) |
| 🟢 **O28·O29 COMMENT 가드 배포** | 코드 로직 변경 0 · 재빌드 불요. `FEP` 17컬럼+테이블 · `FACT_AD_BROADCAST`·`DIM_AD_CREATIVE` · **WIDE 2뷰 17컬럼** · 정본 `06_DDL.sql` 동기화 · **거짓 주석 3건 회수 + 1건 보강**. 검증 = `INFORMATION_SCHEMA` **17/17·16/16·3/3** |
| 🔴 **선행 조치안 오류 4건 정정** | §14-H·§14-I 의 **결론은 옳았으나 조치안 세부 지정이 틀렸다** — ① MS304 `120=Fail` 누락(11종→**12종**) ② 범위 "100~220"→**110~220** ③ 판별자 `EVENT_SOURCE` **실행 불가**(참여 테이블에 없음·고아 23.2% 조인 실패) → **`EVENT_KEY` 접두**로 교체 ④ 오염값 `)` *"SILVER 파싱 결함"* **가설 폐기** → BRONZE 원천 입력 오류(§15-B) |
| 🔴 **신규 발견 3건** | ① **`WIDE_EVENT_PARTICIPATION.EVENT_KIND` COMMENT 가 거짓**(*"온라인/오프라인"* → 실제 EVENT/CRMN) — **하필 O28 판별자 축** ② `99_NEXT_SESSION` §3 이 **WIDE 노출을 누락**(분석가·SV 가 보는 계층) ③ **CRMN 참여일 원천 전건 NULL** → 캠페인행사 152,127행 `DATE_SK` 가 행사시작일로 대체 귀속(미등재)(§15-D) |
| 🟢 **`TRY_TO_*` 전수 스캔 종결** | 14파일 14용례 실측 → **무성 소실은 `AD_SEC` 1건뿐**. `CPC` 482건은 `#DIV/0!` 엑셀 오류(양성) · CRM 날짜/월 캐스팅 6종 **전건 손실 0**. *"더 있을 것"* 우려를 **실측으로 반증**(§15-C) |
| ⚠️ **행수 드리프트 발견** | `FACT_AD_BROADCAST` 실측 **38,486** vs 문서 **37,886**(2026-07-28) — **원인 미규명** → PROC-3 (c) 로 어느 쪽도 인용 금지. 뷰 COMMENT 의 행수 하드코딩 제거 |
| 🆕 **신규 교훈 P41~P44** | 문서10 §15-F |

> 🔴 **다음 세션이 가장 먼저 알아야 할 것**: **WIDE 2뷰의 COMMENT 는 아직 물리 ALTER 로만 보호되고 있다.** 모델 `post_hook` 은 수정 완료됐으므로 **다음 `dbt build` 에서 확정**된다. 빌드 전에 뷰를 재생성하는 다른 작업을 하면 경고문이 사라진다.
> 🟢 **환경**: `/workspace` 마운트는 **정상 복구**됐다(2026-08-04 확인). 스테이지 경유 불요.

---

## 요약 — 선행 세션(2026-08-03)에서 무슨 일이 있었나

**O27** SILVER→GOLD 컬럼 보존율을 **최초로 측정**했다(54.7%). 그 과정에서 **O28·O29 두 개의 신규 결함**이 파생됐고, **사용자 요청 자기검토에서 내 판정 오류 6건**이 드러나 판정표를 재계수했다.

| 구분 | 내용 |
|---|---|
| 🟢 **구현 완료(코드)** | `DIM_MEMBER` 대개편 · WIDE 4종 · `FACT_MEMBER_MONTHLY` 회비 3분해 · `DIM_EVENT` 센티넬 · 가드 6종 · `ALTER` 스크립트 |
| 🟢 **빌드 완료** | ~~미실행~~ → **2026-08-04 사용자 실행 · 반영 검증 완료**(위 블록 참조) |
| 🟡 **신규 결함** | **O28** 참여상태 이질 코드체계 · **O29** 광고초수 96.6% 무성 소실 + 단위 오류 → **둘 다 COMMENT 가드로 오답 경로 차단 완료**(값 배선은 잔여) |
| ⚠️ **자기검토** | 판정 오류 6건 정정 · 재계수 A12·B6·C1·D12·**E134** (합계 165 불변) · 신규 교훈 **P38~P40** |

> 🟢 **환경**: `/workspace` 마운트는 **2026-08-04 정상 동작 확인**. 스테이지 경유 불요.
> ⚠️ 선행 세션(2026-08-03)에는 마운트가 I/O 오류로 사망해 스테이지를 경유했다. 다시 죽으면 같은 방법을 쓴다:
> `cortex ws ls|cp 'USER$.PUBLIC."snowflake_files":/<경로>'` (⚠️`cp` 는 **폴더 대상 전체 파일 교체**라 부분 편집 불가 → 다운로드 → 로컬 수정 → 업로드)

---

## 0. 시작 전 필독 (읽지 않고 인용 금지)

| 문서 | 왜 |
|---|---|
| `20_issue/00_INDEX_이슈원장.md` §1 **거버넌스 변경 블록** | 🔴 **가장 먼저.** 작업 방식 자체가 바뀌었다(8항) |
| `20_issue/10_진단_원인분석.md` **§15** | 🔴 **직전 세션(2026-08-04) 정본.** §15-A(빌드 검증) · §15-B(O28 정정 4건) · §15-C(O29 + `TRY_TO_*` 종결) · §15-D(신규 3건) · 교훈 **P41~P44** |
| `20_issue/10_진단_원인분석.md` **§14** | 🔴 **O27·O28·O29 진단 정본.** §14-G(판정 오류 6건) · §14-H(O28) · §14-I(O29) · 교훈 P34~P40. ⚠️§14-H·§14-I 의 **조치안 세부는 §15-B·§15-C 가 정정**했다 |
| `20_issue/30_설계_의사결정.md` **§15~§19** | DEC-25(명명 2계층) · DEC-26(컬럼정의서 강등) · DEC-27(분석가 대응) · **DEC-28**(O27 결정 · §18-D 판정순서) · **DEC-29**(O28·O29 가드 범위·판별자) |
| `20_issue/20_현업확인_요청.md` **§H·§I·§J** | §H CM017 성별축(회신 없이 적용됨) · **§I O28 참여상태**(회신 없이는 진행 불가) · **§J O29 광고 초수 단위**(신규) |
| `30_output_share/08_SILVER→GOLD_보존율.csv` | 판정표 정본 519행 |

> 📌 **문서가 길다고 끊지 말 것.** 잘리면 이어서 끝까지 읽는다.
> 📌 **추론으로 채우지 말 것.** 모르면 측정하거나 "모른다"고 쓴다. **비율은 분모를 병기**한다.
> 📌 **2차 인용 주의** — §13-A G1~G5 는 선행 세션 측정치의 인용이다. **§14 는 전 항목 직접 실측**이나, 그 안의 **A~E 짝짓기는 추론**이었고 6건이 틀렸다(§14-G).

---

## 1. 지금 이 순간의 상태 (실측 2026-08-04 · 빌드 반영 완료)

| 대상 | 값 |
|---|---|
| `GOLD.DIM_MEMBER` | 7,925,716행 / 회원 1,763,065명 (SCD2 · 평균 4.50버전 · 최대 218) — **2026-08-04 재실측 불변** |
| `MEMBER_TYPE` | 100% 채움 — FDRM 1,587,343 / ONCE 175,722 |
| `GOLD.DIM_MEMBER_CURRENT` | dbt 뷰 · 1,763,065행 = 회원수 **1:1** · GRANT 자동(future grant) |
| `'미상'` 라벨 | 🟢 **GOLD 전체 0건 — 완전 소멸 확인**(2026-08-04). `DIM_EVENT` 마지막 1행이 빌드로 `(미매핑)` 전환됨 |
| **SILVER→GOLD 보존율** | **54.7%** (199/364) · 탈락 165 = A**12**·B6·C1·D**12**·**E134 미판정** |
| **GOLD 물리 미주입** | **106 / 371 (28.6%)** = ALL_NULL 44 + ALL_ZERO 62 (실측 2026-08-04 종반 · 문서10 §18-C). 0행 테이블 `FACT_TARGET_BIZ` 8건 분리 시 **98 / 363 (27.0%)**. 🔴 종전 `125/454` **인용 금지** |
| **센티넬 축** | 🔴 **100% 센티넬 FK 13건 / 7팩트** · 도달 불가 차원 **`DIM_PAYMENT`·`DIM_AD_CREATIVE`** (문서10 §18-B) |
| `FACT_EVENT_PARTICIPATION` | 1,134,126행 · 미주입 14컬럼(카운트 6·횟수 4·degen NULL 2·FK 센티넬 2) — **전건 COMMENT 경고 완료** |
| `FACT_AD_BROADCAST` | ⚠️ **38,486행 실측**(문서 기재 37,886 과 불일치·원인 미규명 → 인용 금지) · `DURATION_SEC` 채움 1,151(3.2%) |
| 최종 빌드 | ~~`PASS=312 WARN=25 ERROR=0 SKIP=0 TOTAL=337` (미반영)~~ → **2026-08-04 사용자 실행 완료 · 반영 검증 전 항목 일치**(문서10 §15-A) |

**분석가 안내 문구(그대로 전달 가능)** — 변경 없음
- 회원 속성은 **`GOLD.DIM_MEMBER_CURRENT`** 를 쓴다(1인 1행). `DIM_MEMBER` 직접 조인 금지 — 실측 3.60배 팬아웃(납입회비 171.3억→507.5억).
- 과거 시점 상태·예측 피처는 `DIM_MEMBER` 를 `EFFECTIVE_FROM`/`EFFECTIVE_TO` 로 **시점조인**.
- 상태 기반 모집단은 **`MEMBER_TYPE='FDRM'`** 한정(ONCE 는 상태 개념 부재).
- 회원상태(MM010)와 개발구분(MM015)은 **다른 축** — 둘 다 `후원중단` 을 갖지만 값이 다르다(958,668행 vs 1,010,680건).

---

## 2. ✅ 완료 — 빌드 실행 및 반영 검증 (2026-08-04)

> **본 절은 이력으로 남긴다.** ①②③ 전부 완료됐고 검증 결과는 최상단 블록·문서10 §15-A 에 있다.
> 🔴 **다음 세션은 이 절을 실행하지 말 것** — `ALTER` 는 이미 반영됐고 재실행하면 컬럼 중복 오류가 난다.

### 2-1. 실행 순서 (완료)

```
① 03_top-down_gold/O27_DIM_MEMBER_ALTER.sql  §1~§4 실행   ✅ 완료
② dbt build                                              ✅ 완료(사용자 실행)
③ 반영 검증                                              ✅ 완료
```

⚠️ **③ 의 정확한 범위**: `O27_DIM_MEMBER_ALTER.sql` **§6 스크립트를 파일째로 실행한 것은 아니고**, 그 검증 항목을 동등한 쿼리로 **직접 재측정**했다(문서10 §15-A). 확인된 항목 = 행수 7,925,716 · **`MEMBER_SK` 유일 7,925,716** · 현재행 1,763,065(=회원수) · 신규 4컬럼 적중 전부 일치 · **`PREV_*` NULL 498,312 = ONCE 175,722 + FDRM 무이력 322,590 정확 정합** · `FMM` 회비 3분해 3값 일치 · `DIM_EVENT` 3,787 · **`'미상'` 성별·상태 각 0** · `(해당없음)` 175,722. → **§6 항목은 전부 통과**했다.

🔴 **① 을 먼저 하지 않으면 빌드가 실패한다.** `DIM_MEMBER` 는 `incremental + append + pre-hook TRUNCATE` 라 **dbt 가 구조를 만들지 않는다.** 모델이 출력하는 신규 4컬럼이 물리에 없으면 append 가 컬럼 부재로 실패한다(A1 선례).
🔴 **`CREATE OR REPLACE TABLE` 금지** — FK·GRANT 파괴. `ALTER` 만 쓴다.
🟢 dbt append 는 **이름 기반**임이 O25 에서 실측 확증됐다 → 컬럼 순서는 무관.

### 2-2. 변경된 파일 (전부 업로드 완료)

| 파일 | 변경 |
|---|---|
| `models/gold/dim/DIM_MEMBER.sql` | 채움 4 · 신설 2 · DROP 3 (아래 표) |
| `models/gold/wide/WIDE_MEMBER_MONTHLY.sql` | `MEMBER_AREA_CD`·`MEMBER_AGE_CD`·`PREV_*` 2·`MEMBER_LAST_STOP_DATE` 노출 · DROP 컬럼 참조 제거 |
| `models/gold/wide/WIDE_MEMBER_EVENT.sql` | 동일(+`PREV_*`) |
| `models/gold/wide/WIDE_EVENT_PARTICIPATION.sql` | `MEMBER_AREA_CD`·`MEMBER_AGE_CD` |
| `models/gold/wide/WIDE_SERVICE_EVENT.sql` | `MEMBER_AREA_CD`·`MEMBER_AGE_CD` |
| `models/gold/fact/FACT_MEMBER_MONTHLY.sql` | **회비 3분해**(#66·#67·#68) 실배선 — 종전 `0` 하드코딩 폐기 |
| `models/gold/dim/DIM_EVENT.sql` | 센티넬 `'미상'`→`'(미매핑)'` · `ELSE` 제거 |
| `models/gold/_gold_ready_schema.yml` | `accepted_values` 가드 6종 |
| `03_top-down_gold/O27_DIM_MEMBER_ALTER.sql` | **신규** — ALTER + COMMENT DoD + 검증 §6 |

### 2-3. `DIM_MEMBER` 변경 내역과 판정 근거

| 컬럼 | 조치 | 근거 |
|---|---|---|
| `AREA_CD`(신설)·`REGION`(채움) | **as-of 시점귀속** | CM018 **18/18 일치** · 적중 96.91% |
| `AGE`(신설)·`AGE_BAND`(채움) | **as-of 시점귀속** | 🔴 `AGE` 는 나이가 아니라 **CM014 12종 코드** · **12/12 일치** · 적중 97.63% |
| `PREV_MBER_STAT_CD`·`PREV_MEMBER_STATUS_NAME` | **신설(상태전이 축)** | `BF_STAT_CD` 100%·MM010 **12/12 일치** · 같은 행에서 파생하므로 fan-out 0 |
| `LAST_STOP_DATE` | **as-of max 채움** | 단순 max 는 **미래정보 누설**이라 금지 · 적중 19.73% |
| `FIRST_SPONSORSHIP` | **채움(SCD1)** | 최소 발생일의 `SPNSR_BSNS_ID` · 적중 97.76% |
| `NEW_EXISTING_FLAG` | **DROP** | 시점귀속(#113) → 차원 grain 부적합. 정소재지 = FMM |
| `LAST_CAMPAIGN` | **DROP** | 대표규칙이 **O8 현업 미결** · 소비처 0 |
| `CURRENT_SPONSORSHIP` | **DROP** | 동시 다중후원이 정상(14.2%·최대 14) → 단일값 불성립 |

🔷 **시점귀속은 설계 정본의 복원이다** — `08_silver의존.md §5-A` 가 이미 *"개발·증감 **시점 AREA_CD/AGE 스냅샷**"* 으로 규정하고 *"AGE 코드화로 충족"* 이라 적어 두었다(2026-07). DEC-27 이 그 문서를 읽지 않아 "구간 정의 없음→보류"로 오판한 것이다.
🔷 **구현 = `ASOF JOIN`** — 좌측 1행당 우측 최대 1행이 **구조적으로 보장**되어 fan-out 이 원리적으로 0.
🔷 **원천 = `CRM_MEMBER_DEV` 단독.** `CRM_MEMBER_AMT_CHANGE` 는 실측 후 제외 — 커버리지 **증가 0** · 값 차이 **7,925,716행 중 1행**.
🔷 **ONCE 175,722행은 NULL** — `(해당없음)` 이 아니다. 상태·경로는 원천에 컬럼이 없어 개념 부재지만, 지역·연령대는 **개념은 있고 ONCE 가 개발약정에 행이 없다**(원천 미보유). `CRM_MEMBER_DEV`·`AMT_CHANGE` 양쪽 모두 FDRM 전용임을 실측 확인.

### 2-4. 사전검증 결과 (컴파일 후 실제 실행 · 빌드 전)

**`DIM_MEMBER`**

| 검증 | 결과 |
|---|---|
| 행수 | 7,925,716 **불변** (fan-out 0) |
| `MEMBER_SK` 유일 | 7,925,716 = 행수 |
| 현재행 | 1,763,065 = 회원수 (1인1행) |
| 적중 | `AREA_CD` 7,738,245 · `REGION` 7,681,020 · `AGE`/`AGE_BAND` 각 7,738,250(**라벨 100% 매칭**) · `PREV_*` 각 7,427,404 · `LAST_STOP_DATE` 1,563,872 · `FIRST_SPONSORSHIP` 7,748,482 |
| `PREV` NULL 분포 | 498,312 = ONCE 175,722 + FDRM 무이력 322,590 **정확 정합** |
| `'미상'` | 0 |
| 가드 6종 | 실측 distinct **전건 가드 내** → 빌드 노이즈 0 |

**`FACT_MEMBER_MONTHLY` 회비 3분해**

| 검증 | 결과 |
|---|---|
| 행수 | 40,054,883 **불변** |
| #66 정기회비 | 759,530,956,167 |
| #67 정기회원 일시회비 | 5,365,351,828 |
| #68 일시회원 일시회비 | 126,337,814,788 |
| 검산 | PAID−3분해 = 3,944,186,325 = G 3,408,190,000 + U 495,788,354 + 마스터부재 74,880,671 − 불량5행 34,672,700 **정확 일치** |

**`DIM_EVENT`** — 3,787행 · SK 유일 · `'미상'` **0** · 라벨 100% 채움(`ELSE` 제거로 NULL 발생 0)

🔴 **회비 3분해 주의 — 3컬럼 합 ≠ `PAID_FEE`**: 정본 #67 이 열거한 *"기타·국내사업·해외사업"* 에 **선물금(G)·긴급구호(U)가 없다.** 선물금은 별도 지표(#90·#91 → `FSE.GIFT_PART_*`)가 있고 긴급구호는 대응 지표가 없다. 억지 귀속은 정의 창작이다. 검산은 **모델 출력 기준**으로 한다(SILVER 원표 기준으로 하면 불량 5행 34,672,700 만큼 어긋난다).

---

## 3. 🟡 O28·O29 — COMMENT 가드 완료 / 값 배선 잔여

> ✅ **2026-08-04 COMMENT 가드 배포 완료.** 두 건 모두 **무증상 오답 경로는 차단됐다.** 정본 스크립트 = `03_top-down_gold/O28_O29_COMMENT_GUARD.sql` · 진단·정정 = 문서10 **§15** · 결정 = **30 DEC-29**.
> 🔴 **이 절의 종전 조치안에 오류가 있었다** — 아래 각 항의 ⚠️ 를 반드시 읽을 것(그대로 실행하면 판별 실패·오진 수정).

### 3-1. O28 — `PARTCPT_STAT_CD` 한 컬럼에 두 코드체계 혼입
정본 = 문서10 **§14-H** + **§15-B**(정정) · 현업 질문 = 문서20 **§I**

| 판별(`EVENT_KEY` 접두) | 코드체계 | 행수(SILVER 전체) |
|---|---|---:|
| `CRMN_`(캠페인행사) | 소정수 `1`~`6` | 152,096 (+NULL 31) |
| `EVENT_`(일반행사) | **MS304 12종** `110`=Success·`120`=Fail·`130`~`220`=N_step_right/fail | 970,788 (+NULL 11,209) |
| `EVENT_` | 🔴 오염값 `)` | 2 |

⚠️ **종전 서술 정정 4건(§15-B)**: ① MS304 는 **12종**이다 — 종전 열거는 **`120=Fail` 누락** ② 저장값 범위는 **110~220**(100 없음) ③ 🔴 **판별자는 `EVENT_SOURCE` 가 아니라 `EVENT_KEY` 접두다** — `EVENT_SOURCE` 는 참여 테이블에 없고(`CRM_EVENT` 마스터 소속) 마스터 조인은 **고아 23.2% 에서 실패**한다 ④ 🔴 오염값 `)` 은 **BRONZE 에 이미 존재**하므로 *"SILVER 파싱 결함"* 이 아니다 → **원천 입력 오류**

- ✅ **완료**: `FEP.PART_STATUS` + 미주입 14컬럼 + 테이블 COMMENT · **`WIDE_EVENT_PARTICIPATION` 16컬럼**(선행 지목이 누락한 소비 계층) · 정본 `06_DDL.sql` 동기화 · 모델 헤더 *"입고 후"* 오진 회수 · 🆕 `WIDE.EVENT_KIND` *"온라인/오프라인"* 거짓 교정
- 🔴 **잔여 = 현업 회신**(문서20 §I): 소정수 1~6 의미. **대조 판별력 0 확정** — `CRM_CODE` 336그룹 중 1~6 포함 **118그룹**·정확히 1~6 인 것 **14그룹**. 회신 후에야 `FEP` 카운트 5종 배선 가능
- 🟢 **회신 없이 가능한 것**: `PARTICIPATION_TIMES`·`CUM_APPLY_TIMES` — `PARTCPT_SEQ` 채움 100% 이고 **상태체계와 무관**
- 🟠 오염값 `)` 2행 → 원천 정정 vs 센티넬 라우팅 **결정 필요**

### 3-2. O29 — `DURATION_SEC` 96.6% 무성 소실 + 단위 오류
정본 = 문서10 **§14-I** + **§15-C**

| 계층 | 상태 |
|---|---|
| `BRONZE_AGENCY.VIDEO_AD_CMPGN_DTLS.AD_SEC`(TEXT) | 33,890/36,416 채움 · **두 표기 혼재** — HH:MM:SS **32,739(96.6%)** / 숫자 **1,151(3.4%)** |
| `SILVER.AGENCY_AD_BROADCAST.DURATION_SEC` | 🔴 1,151행만 생존 (`AGENCY_AD_BROADCAST.sql:25` = **결함 지점**) |
| `GOLD.FACT_AD_BROADCAST.DURATION_SEC` | 🔴 값 30,000,000~90,000,000 — **COMMENT 경고 완료** |
| `GOLD.WIDE_AD_BROADCAST.DURATION_SEC` | 🔴 소비 계층 노출 — **COMMENT 경고 완료**(선행 지목 누락분) |
| `GOLD.DIM_AD_CREATIVE.DURATION_SEC` | ALL_NULL **중복축** — 조회 금지 명시 완료 |
| `SERVING.FACT_AD_COMBINED.DURATION_SEC` | ⚠️ **COMMENT 공백** — SV helper, **사용자 실행 범위** |

- ✅ **완료**: 단위 경고 3계층 + 커버리지 병기(분모=VIDEO 36,416 → 현 **3.2%** · 파싱 시 **93.1%**) · 정본 DDL 2건 · 거짓 주석 *"원천 부재(초수)"* 회수 · 뷰 COMMENT 행수 하드코딩 제거
- 🟢 **`TRY_TO_*` 전수 스캔 종결(조치 ⑤)** — 14용례 실측, **무성 소실 `AD_SEC` 1건뿐**. `CPC` 482건은 `#DIV/0!` 엑셀 오류(정상 NULL 화)
- ⬜ **잔여**: ① **HH:MM:SS 파싱 배선** = 무추론·즉시 가능하나 **모델 수정 + build 필요** ② 숫자 3종 µs **현업 확인**(문서20 **§J** 신설) — 확인 전 NULL 유지 ③ `DIM_AD_CREATIVE` 중복축 **DROP vs 통합 사용자 결정**

---

## 4. 잔여 작업

### 4-1. 🔴 방향 확정 · **구현 미착수** — `DIM_SERVICE` 발송구분 대/중/소
정본 = 문서30 **§18-C** · 미착수 확인 = 문서10 **§15-I**. **사용자 판정 = `DIM_SEND_TYPE` 별도 차원 신설**(근거: 설계 정본이 grain 을 판단한 바 없고, `SERVICE_SK` 보존이 파괴 범위가 작다).

🔴 **2026-08-04 확인: `DIM_SEND_TYPE` 은 아직 존재하지 않는다.** 물리 0건(전 스키마) · dbt 모델 파일 없음 · `06_DDL.sql` 정의 없음 · 유일한 언급은 `_archive/GOLD_차원 설계.md` 1건. **방향만 정해졌고 DDL·모델·FK·WIDE 배선 전부 미착수다.** `DIM_SERVICE` 는 11행 · `SEND_TYPE_L/M/S` 전건 NULL 그대로.

- `SEND_TYPE_L/M/S` 는 **정본 지표 #133·#134·#135** 다 → **DROP 금지**(초판 DROP 판정은 철회됨)
- `SEND_GBN_TOP` 값은 코드가 아니라 **`CRM_CODE.CD_ID` 자체**(MS046 결연·MS047 회원·MS048 회비·MS049 서비스·MS050 사업보고 … 12종) · MID 16종 · BOT 42종(`UPPER_CD_ID` 계층) · 조합 65
- 현 `DIM_SERVICE` grain(`SEND_CHANNEL`,`SNDNG_TY_CD`) = **10행** → 계층 추가 시 74행(함수종속 불성립) · 커버리지 **0.106%**(`SND` 채널 전용)
- ⚠️ `SNDNG_TY_CD` 도메인이 `0`~`3` 4종뿐이라 **P29 3원 대조가 판별력을 갖지 못한다**(227그룹 매칭) — `SVL-1` 동근
- ⚠️ `CRM_CODE.UPPER_CD_ID` 는 판정표에서 E군(미판정)으로 옮겼으나 **이 작업에 필요한 컬럼**이다

### 4-2. 🔴 E군 134 컬럼 개별 수요 판정 (~~+ A군 12건 재검증~~ ✅ 2026-08-04 완료)
정본 = `30_output_share/08_SILVER→GOLD_보존율.csv` 의 `판정군`.
분포: `GA4_EVENT` 20 · `CRM_MEMBER` 17 · `CRM_PAYMENT_METHOD` 12 · `CRM_CAMPAIGN` 11 · `CRM_MEMBER_AMT_CHANGE` 10 · `CRM_PAYMENT_BILLING` 10 · `AGENCY_AD_PERFORMANCE` 10 · `CRM_CODE` 3 · 기타.

🔴 **배선 전 3단 확인 필수(P38)** — 미이행 시 "결손 발견"이 아니라 **결손 창작**이다:
1. 대상 GOLD 컬럼을 **실제로 계산하는 식**을 모델에서 읽는다(이름으로 추측 금지)
2. **같은 역할 컬럼이 이미 있는지** `INFORMATION_SCHEMA` 로 확인한다(§18-D ①)
3. 모델 주석·DDL 에 **이미 사유가 적혀 있지 않은지** grep 한다("조용한 탈락"인지 확인)

✅ **A군 12건 재검증 완료(2026-08-04 · 문서10 §15-H)** — 종전 서술 *"통과 4건뿐 · 나머지 8건 재검증 대상"* 은 **낡은 수치였다**:
- 🟢 **해소 8건** — 배선완료 6(`AGE`·`AREA_NM`·`AREA_CD`·`SPNSR_BSNS_ID`·`MBRFEE_DIV_CD`·`BF_STAT_CD`) + **사전조인 대체 2**(`BF_STAT_NM`·`CHN_STAT_NM` — 라벨은 코드사전 조인이 정답이므로 **애초에 탈락이 아니었다**, P31)
- 🟠 **판정변경 2건** — `AD_SEC_NM`(중복축·O29) · **`RCRIT_PSNNL_CO`(grain 불성립 — 참여 grain 배선 시 `SUM` 101.0배 과대계상)**
- 🔴 **잔여 2건** — `PARTCPT_SEQ`(FEP 행식별자 부재) · `UPPER_DEPT_ID`(ORG-H 현업 차단)
- 🆕 **P45**: 판정표의 "탈락"은 재검증 없이 배선 근거로 쓰지 말 것 — 3단 확인에 **④ 대상 컬럼이 지금 이미 채워져 있지 않은가** 를 추가한다

### 4-3. SV 갱신 + 재배포 [P19 결손 · 미해소]
- `member.MEMBER_STATUS_NAME` COMMENT 가 새 값 **`(해당없음)` 을 열거하지 않는다** → `='미상'` 생성 시 **0행 무증상 오답**
- 신규 `MEMBER_TYPE`·`REGION`·`AGE_BAND`·`PREV_*` 를 **SV 차원으로 노출** 권장 → 빌드 후에 하는 것이 효율적
- 실행: `05_SV-Agent_ai/05_SV_DDL.sql` 수정 후 **`GN_DW_ADMIN` 역할로 전체 실행**. §7 GRANT 가 같은 파일에 있어 자기완결적(522~539행)

### 4-4. P27/P29 대조 스캔 정례화 + G5 확장 [🔴 필수]
컬럼정의서가 코드 정본이 아니게 되어(DEC-26) **대조가 코드 정본 확정의 유일한 수단**이다.
- 🟢 **O27 이 유용성을 실증했다** — `AGE`→CM014 · `AREA_CD`→CM018 확정이 전부 이 방식. **CM014·CM017 은 컬럼정의서 미지정 그룹**이다
- ⚠️ `USE_YN='Y'` 를 **필터로 쓰지 말 것** — 폐지코드가 실적재에 남아 라벨이 조용히 사라진다. 관측 축으로만
- 🔴 **G5 확장(P40)** — 다원천 통합 테이블의 코드 컬럼을 **구분 컬럼별 파티션 분포로 대조**한다. 후보 구분 컬럼: `SEND_CHANNEL`·`EVENT_SOURCE`·`SOURCE_SYSTEM`·`AD_SOURCE_TYPE`
- 🔴 **`TRY_TO_*` 전수 스캔(O29 파생)** — TEXT→NUMBER/DATE 캐스팅으로 무성 소실이 일어난 컬럼 탐색

### 4-5. 기타 (문서10 §13-I · §14-F)
G3 라벨 미배선 잔여 **재계수 후** 배선("21"은 자기집계 인용값) · `SVL-1`·`SVL-2` 대조 확정 · 감사 정본 `06_BRONZE노출감사` 재생성(2026-07-28 이후 배선분 미반영) · `06_DDL.sql`·`08_SILVER_테이블DDL` 정본 동기화 · 소비처 0 테이블 **4종**(28컬럼) 수요 확인

---

## 5. 사용자(현업) 소관 — 코드로 해결 불가

| # | 항목 | 상태 |
|---|---|---|
| 1 | **CM017 성별축 확정**(공#130 값 일치하나 컬럼정의서 미지정) | 문서20 §H · 이미 적용·배포됨(차단 아님) |
| 2 | **O8 다중 캠페인 귀속 규칙** | 🔴 `FMM.CAMPAIGN_SK`·`LAST_CAMPAIGN` 배선을 직접 차단 |
| 3 | **[O28] 행사 참여상태 코드 의미** — 캠페인행사 `1~6` / 일반행사 `N_step_right`·`Success` | 🔴 **대조로 확정 불가** · 문서20 §I · FEP 참여지표 5종 차단 |
| 4 | **[O29] 광고 초수 숫자표기 단위** — `30000000`/`60000000`/`90000000` 이 µs 인가 | 1,151행 · HH:MM:SS 집합과 정합하나 **추론** · 확인 전 NULL 유지 |
| 5 | **O24 중단 이중원천**(동일 사건 이중기록인가) | 합산 금지 가드 배포됨 |
| 6 | **증액·감액 정의**(사건 기준 vs 월말 스냅샷) | 정본 공#150·#151·#38 |
| 7 | **`(건)` 단위** = 금액÷10,000 인가 행수인가 | P6 · 공#4 vs #149 |
| 8 | **생년월일(`MBER_BIRTHDAY`) 입고** | ⚠️ **[DEC-28 사유 축소]** `AGE_BAND` 선행조건이 **아니다**(CM014 코드로 해결). 필요한 것은 **시점정확 연령**(SCD2 과거 버전의 당시 나이·LTV 코호트) |
| 9 | **`DIM_PAYMENT.FEE_TYPE` 정기/일시 이중표현** | 원천(`MBRFEE_DIV_CD` PM010 4종·97.6%)은 실재. 설계 §8 결정 필요 |
| 10 | **`DIM_SPONSORSHIP` 6종↔4그룹↔11개 매핑** | O19 · 문서20 §F-2 |

⚠️ **컬럼정의서 갱신 요청은 하지 않는다** — 갱신 의지 없음이 확인되어 코드 관련 위상을 참고본으로 강등했다(DEC-26). 문서를 기다리지 말고 **대조로 확정**한다.

---

## 6. 지뢰 (선행 세션들이 실제로 밟은 것)

| 함정 | 실측 |
|---|---|
| **탈락 발견 ≠ 원인 규명** | `PARTCPT_SEQ` 를 "FEP 카운트 원천"이라 적었으나 카운트는 `PARTCPT_STAT_CD` 소관이었다. **대상 컬럼의 계산식을 읽지 않고 이름으로 짝지으면 결손을 창작**한다(**P38**) |
| **커버리지 분모를 원천별로 쪼개라** | `AD_SEC_NM` 을 "14.0%" 로 적어 *원천이 빈약하다*는 오독을 만들었다. 실제 **VIDEO 안에서는 95.2%** (**P39**) |
| **"축이 없다"고 쓰기 전에 `INFORMATION_SCHEMA` 를 보라** | `CANCL_RDCAMT_RSN_CD` 를 "FME 사유축 부재"라 적었으나 `REASON_SK`·`STOP_REASON` 이 실재했다 |
| **한 컬럼 안에서 코드체계가 갈릴 수 있다** | `PARTCPT_STAT_CD`(O28). 개명 불가 · `accepted_values` 는 합집합 통과 (**P40**) |
| **grain 부적합 ≠ DROP** | `SEND_TYPE_L/M/S` 를 DROP 판정했는데 **정본 지표 #133~135** 였다. DROP 전에 **지표사전·WIDE 노출을 역추적**할 것 |
| **도메인이 작으면 대조가 무력하다** | `SNDNG_TY_CD`(0~3)·`PARTCPT_STAT_CD`(1~6) → 수백 그룹 매칭. **대조의 판별력은 도메인 크기에 의존** |
| **컬럼명·주석·타입이 의미를 보증하지 않는다** | `AGE` 는 이름·COMMENT("연령")·타입(NUMBER) 셋 다 연속형을 가리켰으나 **12종 코드**였다(**P36**) |
| **원천이 있어도 채우면 안 되는 경우** | grain 함수종속 판정이 채움 판정보다 **앞선다**(**P37**) |
| **계층별 보존율은 곱해진다** | BRONZE→SILVER 37.6% 통과분이 SILVER→GOLD 에서 **다시 45.3% 탈락**(**P34**) |
| **미주입 판정은 정적+동적 둘 다** | 정적 스캔은 조인실패 NULL 을 놓치고, 동적 실측은 원천이 빈 정당한 NULL 과 구별 못 한다(**P35**) |
| `COUNT(col)` 로 적재 판정 | 0 상수 주입군을 "적재됨"으로 오판 → **`COUNT_IF(col<>0)` 병행 필수** |
| `TRY_TO_NUMBER` 캐스팅 | **에러 없이 NULL** — 96.6% 무성 소실이 어떤 테스트에도 안 걸렸다(O29) |
| `DIM_MEMBER` 직접 조인 | 3.60배 팬아웃 · 금액 2.96배 과대 · **에러 없음** |
| `ALTER TABLE RENAME COLUMN` | **구 COMMENT 를 승계**한다 → 물리 16컬럼이 낡아 있었다(**P33**) |
| DDL 정본 수정 | `CREATE OR REPLACE` 금지라 **물리에 전파되지 않는다** → 별도 `ALTER … COMMENT` 필요 |
| 뷰 COMMENT | 물리만 고치면 **다음 빌드가 되돌린다**(소유주 = 모델 `post_hook`) |
| `COALESCE(라벨,'미상')` | **개념 부재를 결측으로 뭉갠다**(DEC-17-B 창작금지 위반) |
| `ERROR=0` | 의도한 대상이 실행됐음을 뜻하지 않는다(`--select` 오지정 시 TOTAL=15 인데도 ERROR=0) |
| 계층별 소유주 혼동 | GOLD **테이블**=`06_DDL.sql` / GOLD **뷰**=dbt 모델 / SERVING 헬퍼뷰=`02_SERVING_setup`·`08_After_Deploy_DBT` |
| GOLD 뷰 GRANT | **수동 부여 불요** — GOLD 스키마 VIEW future grant 로 매 build 자동 |
| 워크스페이스 마운트 사망 | `/workspace` I/O 오류 → **`cortex ws cp` 스테이지 경유**. `cp` 는 전체 파일 교체 |

---

## 7. 세션 시작 시 붙여넣을 프롬프트

```
GN_DW 프로젝트를 이어서 진행한다. 먼저 99_NEXT_SESSION.md 를 끝까지 정독하고,
§0 필독 문서(이슈원장 §1 거버넌스 블록 · 문서10 §15 → §14 → §13 · 문서30 §15~19 ·
문서20 §H·§I·§J)를 읽은 뒤 진행해줘.

【현재 상태】 dbt build 는 실행됐고 O27 산출물(DIM_MEMBER 대개편·WIDE 4종·FMM 회비3분해·
DIM_EVENT 센티넬)이 사전검증치와 전 항목 일치로 물리 확정됐다. GOLD '미상' 은 완전 소멸했다.
O28·O29 는 COMMENT 가드가 배포되어 무증상 오답 경로는 막혔고, 값 배선만 남았다.
🔴 단 WIDE 2뷰(WIDE_EVENT_PARTICIPATION·WIDE_AD_BROADCAST)의 경고문은 아직 물리 ALTER
로만 보호된다 — 모델 post_hook 은 수정됐으므로 다음 build 에서 확정된다.

【이번에 할 일 — 우선순위】
1. §4-1 DIM_SEND_TYPE 신설(정본 지표 #133~135). DROP 금지. 사용자 판정 = 별도 차원 분리.
2. §4-2 E군 134 판정 + A군 8건 재검증. 반드시 §4-2 의 3단 확인을 밟아라.
3. O29 HH:MM:SS 파싱 배선(96.6% 회복) — 무추론이라 현업 회신 불요. 단 숫자 3종은 손대지 마라.
4. §4-3 SV 갱신(MEMBER_TYPE·REGION·AGE_BAND·PREV_* 노출 + (해당없음) 열거) — 실행은 내가 한다.
5. GOLD 물리 미주입 재계수 — O27 배선 반영 후 125/454 가 얼마로 줄었는지 아직 아무도 안 셌다.

【작업 원칙】
- 문서가 길다고 끊지 말고 끝까지 읽어라. 추론으로 채우지 말고 모르면 측정하거나
  모른다고 말해라. 수치는 재측정 여부를 밝히고 분모를 함께 적어라.
- 🔴 인수인계·진단 문서의 "조치안"은 결론과 별개로 재검증해라(30 DEC-29 §19-B).
  직전 세션에서 §14-H·§14-I 의 조치안 세부 지정 4건이 틀렸다 — 결론은 옳았는데
  "어느 컬럼을 판별자로 쓰라"·"어느 계층이 범인이다" 가 틀렸다. 그대로 실행하면
  판별 실패하거나 정상 코드를 고친다.
- 컬럼 의미는 이름·주석·타입으로 판단하지 말고 count(distinct)+값 열거로 확정하고,
  독립 축과 교차검증해라(P36).
- GOLD 빈 컬럼의 원천을 지목할 때는 ①계산식을 모델에서 읽고 ②같은 역할 컬럼이
  이미 있는지 INFORMATION_SCHEMA 로 보고 ③주석·DDL 에 사유가 있는지 grep 해라(P38).
- 🔴 COMMENT·경고를 넣거나 고칠 때는 노출 계층을 전수 열거해라(P41) —
  INFORMATION_SCHEMA 에서 컬럼명으로 GOLD·SERVING 전 스키마를 역검색하고,
  각 지점의 COMMENT 소유주를 판정해라(테이블=DDL 정본 / 뷰=모델 post_hook /
  SV=05_SV_DDL). 뷰는 물리만 고치면 다음 빌드가 되돌린다.
- 🔴 판별자·구분 컬럼을 지정할 때는 ①그 컬럼이 대상 테이블에 실재하는가(조인 불요인가)
  ②결손·고아 비율이 얼마인가 를 먼저 봐라(P42). 조인이 필요한 판별자는 그 조인의
  실패율만큼 판별력을 잃는다.
- 🔴 오염값을 만나면 BRONZE→SILVER→GOLD 순으로 최초 출현 계층을 확정하고 나서
  범인을 지목해라(P43). 인접 컬럼·타입 정상성으로 필드 밀림 여부를 판별해라.
- 🟢 전수 스캔의 음성 결과도 결론이다(P44) — "더 있을 것 같다"는 불안은 수치로 닫아라.
- 커버리지는 "그 개념이 존재하는 모집단"을 분모로 써라(P39).
- 코드그룹은 문서를 믿지 말고 CRM_CODE × 실적재 distinct × 지표사전 3원 대조로
  확정하고 근거를 COMMENT 에 남겨라(DEC-26/P29). 단 도메인이 4~6종뿐이면
  대조는 판별력이 없다 — 그때는 현업 확인 사안이다(O28 실측 118그룹 매칭).
- 개명·값체계 변경은 ①물리 ALTER COMMENT ②소유 산출물 ③거짓이 된 경고문 회수
  3종을 다 하고, 완료 판정은 INFORMATION_SCHEMA 스캔으로 해라(P33).
- CREATE OR REPLACE TABLE/SEMANTIC VIEW 는 FK·GRANT 를 파괴한다. ALTER 를 써라.
- ⚠️ dbt post_hook 문자열에 이중인용부호(\")를 넣지 마라 — 「」로 대체하고 dbt compile
  로 검증해라.
- dbt build 와 05_SV_DDL.sql 실행은 내가 직접 한다. 그 전까지만 작업하고,
  실행이 필요하면 무엇을 왜 실행해야 하는지 알려줘.
- 작업 후 반드시 자기검토해라. 선행 세션들은 매번 자기 규칙을 스스로 위반했고
  검토에서 잡혔다.
```

---
_Co-authored with CoCo_
