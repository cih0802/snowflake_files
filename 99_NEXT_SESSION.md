<!-- LLM-METADATA
doc_id: NEXT_SESSION_PROMPT
doc_role: 다음 세션 착수 프롬프트 — 이 파일만 읽고 바로 시작할 수 있게 구성
project: GN_DW (굿네이버스 데이터웨어하우스)
written: 2026-08-04 (O35 완료 시점 · 사건시점 축 전파 + SV/Agent 배포까지 종결)
index: 20_issue/00_INDEX_이슈원장.md   ← 이슈 정본
runbook: 02_GN_DW_building/06_RUNBOOK.md  ← 운영·재구축 정본
END-METADATA -->

# 99. 다음 세션 착수 프롬프트

## 0. 이 세션에서 지킬 작업 규칙 (사용자 상시 지시)

1. **관련 문서를 읽을 때 중간에 끊기면 추론으로 채우지 말고 전부 읽을 것.**
2. **작업 전에 `20_issue/` 문서를 읽고 미해결·pending 항목을 파악할 것.**
3. **데이터 관련 작업은 COMMENT 를 근거로 단정하지 말고 BRONZE 원본을 스캔해 실측할 것.**
4. **큰 작업이 끝나면 `20_issue/` 문서를 갱신할 것.**
5. **`dbt build` 순서에 도달하면 멈추고 사용자 입력을 기다릴 것.**
6. **COMMENT 에는 실측 수치를 넣지 않는다** — 코드값만. 데이터가 계속 입고되므로 수치는 반드시 낡는다.
   (수치는 `20_issue/10_진단_원인분석.md` 에 둔다. 근거 = P58)

---

## 1. 현재 상태 — 전부 살아 있음 (2026-08-04 실측)

| 계층 | 상태 |
|---|---|
| BRONZE | 51테이블 (CRM 43 · AGENCY 4 · ERP 1 · GA4 3) |
| SILVER | 38테이블 |
| GOLD | 28테이블 + **뷰 13**(WIDE 12 + `DIM_MEMBER_CURRENT`) |
| SERVING | 뷰 3(`DIM_MONTH`·`DIM_MEMBER_CURRENT`·`FACT_AD_COMBINED`) · **SV 6종** · **Agent 2종** |
| 빈 테이블 | 예상된 3건뿐 — `CRM_BIZ_TARGET`·`FACT_TARGET_BIZ`(E-6 원천 미입고) · `BRONZE_GA4.SYNC_ERR_INFO` |

> 🔴 **재구축(①`06_DDL` ②`08_SILVER`)을 다시 실행하지 말 것.** 정본 DDL ↔ 물리 **66테이블 전수 대조
> 불일치 0**이다. 재실행하면 데이터만 날아간다. 상세·예외 = 런북 **§11**.

> 🆕 **[2026-08-04 O36] 신규 계정(BRONZE 만 있는 계정) 재현 순서 = 런북 §11.2-C** (신설).
> 기존 계정 복구 최소경로(§11.2-B)와 **용도가 다르다.** 순서 요약:
> `07_ENVIRONMENT_RBAC_setup` → `06_DDL` → `08_SILVER_테이블DDL` → `deploy_dbt_project` → **dbt build**
> → `08_After_Deploy_DBT` → `05_SV_DDL` → `09_1_AGENT` → **`09_2_AGENT_버전업`**
> 🔴 **자주 빠지는 3곳**: ① `07` 을 빼면 `06_DDL.sql` 41~42행(`USE ROLE`·`USE WAREHOUSE`)에서 즉사한다
> ② `dbt build` ③ **`09_2`** — `09_1` 은 껍데기만 만들어서 빼면 Agent 가 도구 0개로 남는다.
> ⛔ **`13_SV_AD_배포_추가작업.sql`·`09_AGENT_spec_구현.sql`·`02_SERVING_setup.sql` 은 실행하지 않는다**
> (전부 DEPRECATED 포인터 스텁 · 실행 라인 0개). 근거 = 문서10 §20-G.

**소비 경로**: 현업은 GOLD 라벨 컬럼과 SV/Agent 로 조회한다. GOLD 코드축 41곳 전수 대조 결과
**코드↔라벨 페어링 결함 0** → `DIM_CODE` 신설 불요(권고 철회됨).
역할 차이만 유의: `GN_DW_ANALYST`=GOLD·SERVING·**SILVER** / `GN_DW_VIEWER`=GOLD·SERVING 만.

---

## 2. 직전 세션(O30~O34)이 한 일 — 요약

| ID | 내용 |
|---|---|
| **O30** | 전체 환경 재구축이 「정본 DDL 미동기화」 구조변경을 되돌려 `dbt build` 가 깨졌다(ERROR 3·SKIP 68). 소실분 = O26 SILVER 개명 2컬럼 · O27 `DIM_MEMBER` 4추가/3삭제. → 복구 + **정본 DDL 동기화** 완료 |
| **O31** | SERVING helper 뷰 정본이 `08_After_Deploy_DBT.sql` §G 에 있었다(내 "유실" 판정은 오판). 실제 결함은 §G.2 가 O27 미반영이라 `invalid identifier` 로 실패하던 것 → 수정 완료 |
| **O32** | COMMENT 실측수치 제거 — **사용자가 수동 진행하기로 결정**(자동화 2회 실패, P58) |
| **O33** | SV 「비활성」 선언 전수조사 → stale 5건 발견. 지역·연령대·캠페인·미납사유·중단사유 **활성화 완료** / 모집인원 **보류** |
| **O34** | `AGE`·`AREA_CD` 가 **약정 시점 스냅샷**임을 확정(내 "기본값 오염" 가설은 오판). 차원명 `_AT_PLEDGE` 로 개명 |
| **O34-B** | ''10대 미만 최다''의 원인을 **편지쓰기대회 계열 캠페인**으로 실증(그 외 캠페인 대비 약 5배). SV COMMENT 에 *"오류 아님 · 약정 당시 연령 · 결측으로 설명 금지"* 명시 배포 |
| **O35** | 🔴 **연령대 × 캠페인 교차 불가** — 두 축이 서로 다른 SV 에 갈라져 있어 Agent 가 위 설명의 근거를 스스로 산출하지 못한다. 해법은 원천에 이미 있다(아래 3-A 1번) |

신규 교훈 **P57~P60** 등재. 상세 = `20_issue/10_진단_원인분석.md` **§19**.

---

## 3. 🔴 다음 세션에서 할 일

### 3-A. 사용자 결정이 필요한 것 (먼저 물어볼 것)

| # | 사안 | 배경 |
|---|---|---|
| **1** | 🔴 **`FACT_MEMBER_EVENT` 에 사건시점 `AGE`·`AREA_CD` 전파 (O35 · 최우선 · 방향 확정됨)** | 아래 §3-A-1 상세 참조. 사용자 승인 완료(2026-08-04) — `dbt build` 시점만 대기 |
| 2 | **현재 연령·현주소 원천 입고 요청 여부** | O34 확정: `AGE`·`AREA_CD` 는 약정 시점 값이다. **BRONZE 전체에 생년월일·현주소 컬럼이 없어** 현재 값 산출이 원천적으로 불가하다. "현재 연령대별" 질문에 답하려면 원천 입고가 선행돼야 한다 → 현업 요청서 작성 대상 |
| 3 | **`FEP` 모집인원 노출** | 보류 결정됨. `DIM_EVENT.RECRUIT_HEADCOUNT` 는 채워져 있으나 참여 팩트와 조인하면 대부분 NULL 이라 노출하면 오답이 된다. 재검토 시점만 정하면 됨 |

#### 3-A-1 상세 — 왜 「전파」인가 (분석가 관점 · 대안 비교 완료)

**문제**: *"왜 10대 미만이 이렇게 많나?"* 에 대한 답(= 편지쓰기대회 계열 아동 모집 캠페인)을
**Agent 가 스스로 계산해 보여줄 수 없다.** 연령대와 캠페인이 서로 다른 SV 에 갈라져 있다.

| SV | grain | 연령대 | 캠페인 |
|---|---|---|---|
| `SV_MEMBER_MONTHLY` | 월 × 회원 | 있음(`_AT_PLEDGE`) | 🔴 **없음** — `FMM.CAMPAIGN_SK` 는 센티넬 단일값이다 |
| `SV_MEMBER_EVENT` | 일 × 회원 × 사건 | 🔴 없음 | 있음(개발 사건 전용) |

**검토한 대안 3가지 — 재론 불요**

| 안 | 내용 | 판정 |
|---|---|---|
| **A. 사건시점 속성 전파** ✅ | `SILVER.CRM_MEMBER_DEV.AGE`·`AREA_CD`(사건행별·채움 100%)를 `FACT_MEMBER_EVENT` 로 전파 | **채택.** ① 속성이 **측정된 grain 에 놓인다** — 개발약정 이벤트에서 관측된 값이므로 사건 팩트의 속성이 맞다(Kimball 의 트랜잭션 시점 속성) ② `DIM_MEMBER_CURRENT` 경유 스냅샷의 시점 왜곡이 **원천적으로 사라진다**(P60 해소) ③ 같은 SV 안에서 **연령대 × 캠페인 교차**가 성립한다 |
| B. multi-fact SV (FMM+FME 를 한 SV 에 conformed dim 공유) | 문법적으로 가능한 패턴이다 | **기각.** 교차는 되지만 연령이 여전히 `DIM_MEMBER_CURRENT` 의 **낡은 스냅샷**이라 **의미 결함이 그대로 남는다**. 게다가 서로 다른 grain 의 두 팩트를 `MEMBER` 로 묶으면 **fan trap** 위험이 생긴다. 기계적 해결이지 의미적 해결이 아니다 |
| C. Agent 가 SV 끼리 join | — | **불가.** `SEMANTIC_VIEW()` 는 **단일 뷰**를 대상으로 하는 테이블 함수다. Agent 는 SV 를 각각 호출해 결과를 **문장으로 합칠 뿐 행 단위 조인을 하지 못한다.** 설령 된다 해도 **`FMM` 에 캠페인 축이 없어**(distinct=1) 조인의 의미가 없다 |

**✅ 실행 완료 (2026-08-04) — 상세 정본 = `20_issue/10_진단_원인분석.md` §20**

| 단계 | 결과 |
|---|---|
| ① SILVER 실측 | BRONZE 직접 스캔. `AGE` 채움 100%·CM014 12/12 · `AREA_CD` = CM018 + 센티넬 `'0'` · SILVER 가 `AREA_NM` 라벨 보유 |
| ② 물리 ALTER | `AGE_AT_EVENT`·`AGE_BAND_AT_EVENT`·`AREA_CD_AT_EVENT`·`REGION_AT_EVENT` + COMMENT 4. `CREATE OR REPLACE` 미사용 |
| ③ 정본 DDL 동기화 | `06_DDL.sql` 같은 세션 처리(P57) · 정본↔물리 **31==31 차집합 0** |
| ④ 모델 + 소비뷰 | `FACT_MEMBER_EVENT.sql`(CM014 라벨 CTE) · `WIDE_MEMBER_EVENT.sql` · `accepted_values` 4 · 필드 인벤토리 |
| ⑤ dbt build | **PASS=333 · WARN=26 · ERROR=0** · FME **4,633,105 불변**(fan-out 0) · 신규 가드 전건 PASS |
| ⑥ SV 확장 | `SV_MEMBER_EVENT` 차원 4 + `AI_SQL_GENERATION` 규칙(7) · GRANT 3줄 재실행 |
| ⑦ 검증 | 편지쓰기대회 계열 ''10대 미만'' **50.79%** vs 그 외 **4.25%**(약 12배) · 상위 10 캠페인 전부 이 계열 |
| ⑧ Agent | 정본 yaml 교정 + `09_2` 배포 → 두 Agent **VERSION$3** · 도구 4개 · GRANT 보존 |

🔴 **라벨 병설을 채택**했다(핸드오프가 「조인 비용 보고 판단」으로 남긴 건) — 지역 라벨은 SILVER 가 이미
보유해 추가 조인 0, 연령대는 CM014 12행 조인이며 fan-out 0 을 사전 검증했다.

🔴 **전파의 가치가 수치로 확인됐다**: `_AT_PLEDGE` 스냅샷과 **연령대 283,067행(7.95%)·지역 141,279행(3.99%)
불일치**. 스냅샷 축만 있었다면 개발 사건 28만 건의 연령대가 틀린 답이 됐다.

🔴 **주의**: FMM 의 `_AT_PLEDGE` 축은 **성격이 다르다**(월 팩트 × 현재행 스냅샷). 제거하지 않고
**이름으로 구분해 공존**시켰다 — `_AT_EVENT`(사건 시점·정확) vs `_AT_PLEDGE`(최근 약정 스냅샷).

---

### 3-A-2. 🔴 다음 세션 첫 작업 — NL 스모크 (사람만 할 수 있다)

`SNOWFLAKE.CORTEX.DATA_AGENT_RUN` 이 **트라이얼 계정에서 차단**된다(`Access denied for trial accounts`).
따라서 Agent 자연어 경로는 **Snowsight UI·CoWork 에서 사람이 확인**해야 한다. SQL 계층은 검증 완료이므로
남은 위험은 **Agent 의 도구 선택·instruction 준수**뿐이다.

`AGENT_MEMBER` 에 아래를 물어 기대 동작을 확인한다:

| 질문 | 기대 |
|---|---|
| *"10대 미만 개발건이 왜 많아? 전체 기간 캠페인별로 보여줘"* | `analyst_member_event` 단일 호출 · 연령대 × 캠페인 교차 표 + 편지쓰기대회 설명. 🔴 "데이터 오류"·"미적재"로 답하면 실패 |
| *"지역별 개발건수 상위 5곳"* | `REGION_AT_EVENT` 사용 + 약정시점 전제 명시 |
| *"현재 연령대별 개발건"* | **산출 불가**를 밝히고(원천에 생년월일 없음) 약정시점 기준으로 대체 제시 |
| *"캠페인별 중단건"* | ''(미매핑)'' 한 덩어리임을 설명(중단 원천에 캠페인 부재) |

실패 시 조치 지점은 ① SV 차원 COMMENT ② `AI_SQL_GENERATION` 규칙(7) ③ Agent `orchestration`·도구
description 순이다. 수정 후에는 `09_2` 재실행(정본 yaml → 새 버전)이다.

---

### 3-B. 이월 작업 (착수 가능)

| # | 작업 | 위치·근거 |
|---|---|---|
| 1 | **O32 COMMENT 수치 제거** (사용자 수동) | 대상 = `06_DDL.sql` · `08_SILVER_테이블DDL` · dbt 모델. 🔴 자동화 금지(코드값·한국어 문장이 깨진다 — P58). 필요하면 **파일·컬럼 단위 목록**을 뽑아 줄 수 있다 |
| 2 | **거짓 주석 회수 1건** | `10_dbt_pipeline/models/gold/dim/DIM_MEMBER_CURRENT.sql` 헤더가 *"전건 NULL 7컬럼 미노출"* 로 **이제 존재하지 않는 3컬럼**(`NEW_EXISTING_FLAG`·`LAST_CAMPAIGN`·`CURRENT_SPONSORSHIP`)을 열거한다. build 실패 요인은 아님(P33 ③) |
| 3 | **`SV_AD` 제약 재검증** | SV 6종 중 `SV_AD` 만 「비활성」 절이 없고 ⚠ 제약 문구로만 되어 있다. O33 전수조사에서 제외됐다 — 광고 팩트(FAP·FAD·FAB)에 대해 같은 조사를 하면 stale 제약이 더 나올 수 있다 |
| 4 | **다른 SV 의 시점 축 점검 (P60 적용)** | O34 는 `AGE`·`AREA_CD` 만 확인했다. **다른 회원속성도 「언제 굳는 값인가」를 확인**해야 한다 — 특히 `MBER_STAT_CD`·`JOIN_PATH_CD`·`FIRST_SPONSORSHIP` 이 현재/시점 중 무엇인지. SV COMMENT 는 *"현재 스냅샷 기준"* 이라고만 적혀 있다 |
| 5 | **Agent NL 스모크** → **§3-A-2 로 승격**(다음 세션 첫 작업) | 트라이얼 계정 제약으로 프로그램 실행 불가 — 사람이 UI 에서 확인 |
| 6 | **`_AT_PLEDGE` 쪽 서술 재확인** | `SV_MEMBER_MONTHLY` COMMENT 에 *"교차 확인 불가"* 계열 표현이 남아 있는지 점검(이제 `SV_MEMBER_EVENT` 에 축이 있으므로 거짓이 될 수 있다 — P33 ③·P61) |

### 3-C. 장기 미결 (Phase-2)

- **GA 영역** — 사용자 결정으로 「데이터 검토만」 하고 설계·구현은 phase-2 이월. 잔여 질문 1개:
  *"`FACT_GA_SESSION`·`FACT_GA_CLICK`(GA 원자 grain 팩트)을 신설할 것인가"*
- **E-6 원천 미입고** — `CRM_BIZ_TARGET`·`FACT_TARGET_BIZ` 0행. 입고 요청서 = `20_issue/41_입고요청서_CRM_BIZ_TARGET.md`
- **미주입 컬럼군** — GOLD 다수 컬럼이 전건 NULL/0(센티넬). SV 「비활성」 선언과 대응된다

---

## 4. 필독 문서 (순서대로)

| 순서 | 문서 | 왜 |
|---|---|---|
| 1 | `20_issue/00_INDEX_이슈원장.md` | **이슈 정본**. 최상단 「최신 상태」 블록이 시간 역순이라 위에서부터 읽으면 된다 |
| 2 | `20_issue/10_진단_원인분석.md` **§19~§20** | O30~O35 진단 전문 + 자기검토 + **P57~P61**. §19-J(P60)는 2026-08-04 회수 신설분이다 |
| 3 | `02_GN_DW_building/06_RUNBOOK.md` **§3.3·§11** | 평상시 컬럼 변경 절차 / 전체 재구축 순서. 🔴 §3.3 은 종전에 파괴적 지시를 담고 있었고 교정됐다 |
| 4 | `05_SV-Agent_ai/05_SV_DDL.sql` | SV 정본. O33 확장분이 반영돼 있다 |
| 5 | `20_issue/50_dbt_파이프라인_미결조치.md` · `40_입고대기_원천의존.md` | 파이프라인·원천 미결 |

---

## 5. 이 프로젝트에서 반복 발생한 함정 (읽고 시작할 것)

| 교훈 | 내용 |
|---|---|
| **P57** | 정본 DDL 은 재구축의 유일한 진실이다. 물리 `ALTER` 만 하고 정본에 접지 않으면 **다음 재구축에서 경고 없이 소실**된다. 드리프트 방향에 따라 증상이 갈린다 — 물리에만 있으면 에러, **모델에만 있으면 dbt 가 조용히 버린다** |
| **P58** | COMMENT 의 실측 수치는 **자동으로 지울 수 없다**. 정규식으로 지우면 코드값(`1개인`)과 한국어 문장이 깨진다. 애초에 넣지 말 것 |
| **P59** | 구조 변경은 **소비 계층까지 역추적**해야 완결된다. `dbt build` 통과는 **dbt 가 아는 범위 안에서만** 참이다 — dbt 밖 산출물(환경 setup SQL·SV DDL·Agent spec)은 `grep -r <컬럼명>` 으로 직접 회수해야 한다 |
| **P60** | **시점 스냅샷을 「현재 속성」으로 노출하면 값이 맞아도 답이 틀린다.** 값·매핑·채움률이 전부 정상이라 품질 테스트로 잡히지 않는 **의미 결함**이다. 분포가 이상하면 값 오류를 먼저 의심하지 말고 **시점 정의를 먼저 확인**하라 |
| **P61** | **소비 끝단 역추적(P59)은 SV 에서 멈추면 안 된다** — Agent instruction·도구 description 까지 본다. 특히 *"미적재/비활성 목록"* 같은 **부정형 서술은 축을 활성화할 때마다 반드시 회수**해야 한다(활성화는 새 것을 켜는 일이면서 **옛 금지문을 끄는 일**이다). 도구 description 누락은 "축은 있는데 도구가 안 골라지는" 무증상 비활성을 만든다 |
| **P62** | **실행 순서 문서는 「파일이 실행 가능한지」까지 검증해야 정본이다.** 폐기 스텁이 순서에 남아 있으면 파괴는 없지만 **「했다」는 착각**을 만들고, 필수 단계 누락은 **무증상 미완성**을 만든다(`09_2` 누락 → Agent 껍데기). 판정법 = `grep -vc "^--\|^$" <file>` 로 실행 라인 수를 세고 헤더의 `DEPRECATED` 표기를 확인한다 |
| **P62-B** | **자기교정은 전파되지 않는다.** 한 문서에서 오류를 바로잡았으면 **그 오류를 인용하던 다른 파일을 `grep` 으로 찾아 함께 고쳐야** 완결이다(런북 §11.3-B 가 교정한 내용이 `deploy_dbt_project.sql` 에 전파되지 않은 사례) |
| **PROC-3** | 기대값이 어긋나면 **원인 규명 전에는 어느 쪽도 인용하지 말 것**. 특히 재구축 이전에 기록된 행수·적중률은 무효일 수 있다 |
| **P33** | 개명·값 변경은 COMMENT 를 데려오지 않는다. `RENAME COLUMN` 은 **구 COMMENT 를 승계**한다. 그리고 **이제 거짓이 된 기존 경고문을 회수**하는 것까지가 DoD 다 |

### 운영 환경 지뢰

- **`/workspace` 마운트가 세션 중 사망할 수 있다**(I/O error). 그때는 `cortex ws ls|cp 'USER$.PUBLIC."snowflake_files":/<경로>'` 로 스테이지 경유한다. 단일 파일 업로드는 형제 파일을 지우지 않는다(검증됨)
- **`/tmp` 는 세션 중에도 초기화될 수 있다.** 작업 사본은 언제든 다시 내려받을 수 있게 스크립트화할 것
- **SQL 도구 토큰이 만료**되면 python `snowflake.connector` + `/snowflake/session/token` 경로로 우회한다(토큰을 매번 새로 읽는다)
- **워크스페이스 파일에 새로 쓰지 말 것** — `/tmp` 에서 만들고 `cortex ws cp` 로 올린다

---

_Co-authored with CoCo_
