<!-- LLM-METADATA
doc_id: NEXT_SESSION_PROMPT
doc_role: 다음 세션 착수 프롬프트 — 이 파일만 읽고 바로 시작할 수 있게 구성
project: GN_DW (굿네이버스 데이터웨어하우스)
updated: 2026-08-05 (O38·O39·O40·O40-B **전량 종결** — SQL·GOLD·SV 계층 잔여 0 · 신규 원칙 P77~P83 · **다음 세션 첫 작업 = NL 스모크(사람)**)
정본: 이슈=20_issue/00_INDEX_이슈원장.md · 진단·교훈=20_issue/10_진단_원인분석.md · 운영=02_GN_DW_building/06_RUNBOOK.md
편집 규칙: 이 문서는 **포인터 + 미결 항목**만 담는다. 완료된 작업의 상세·경위는 위 정본에 두고 여기 복사하지 않는다.
END-METADATA -->

# 99. 다음 세션 착수 프롬프트

## 0. 작업 규칙 (사용자 상시 지시)

1. 문서를 읽을 때 **중간에 끊기면 추론으로 채우지 말고 전부** 읽는다.
2. 작업 전에 `20_issue/` 를 읽고 **미해결·pending** 을 파악한다.
3. 데이터 작업은 COMMENT 를 근거로 단정하지 말고 **BRONZE 원본을 스캔해 실측**한다.
4. 큰 작업이 끝나면 `20_issue/` 를 갱신한다.
5. **`dbt build` 순서에 도달하면 멈추고 사용자 입력을 기다린다.**
6. **COMMENT 에 실측 수치를 넣지 않는다**(코드값만). 데이터가 계속 입고돼 수치는 낡는다 → 수치는 문서10에.

---

## 1. ✅ 2026-08-05 세션 종결 — SQL·GOLD·SV 계층에 남은 작업이 없다

`dbt build` 2회(사용자 실행) 후 **O38 후속 · O38-E · O39 · O40 · O40-B 전량 종결**.
**다음 세션의 첫 작업은 §2 NL 스모크**(사람만 가능)다. 코드로 할 일은 없다.

| ID | 무엇 | 상태 |
|---|---|---|
| **O38** 후속 | 적재검증 9항 · `HAS_POSITIVE_GOAL` 물리컬럼 전환 · 스모크 D-1~D-6 | ✅ 전항 통과 |
| **O38-E** | 부서 달성율이 **소표본에 지배**(목표 월 5건 센터가 171.7% 1위) → SV 에 **규모 하한 가드** | ✅ 배포 |
| **O39** | `*_MEMBERS` 가 「명」이 아니라 **0/1 플래그**(최대 **37.3배** 과대) → COMMENT 가드 + `SV_SERVICE` **synonym 라우팅 교정** | ✅ 배포 |
| **O40** | 납부율·미납금액 **모집단 불일치** → FMM 컬럼 2종 신설 + SV **정본 metric 4종** | ✅ 적재·배포 |
| **O40-B** | `gold.fact` 에 **`on_schema_change: fail`** (신규 컬럼 무증상 폐기 차단) | ✅ 적용 |

### 🔴 이번 세션의 핵심 교정 — 숫자로

| 지표 | 종전(결함) | 정본 | 성격 |
|---|---|---|---|
| 2025 납부율 | 93.98% | **85.65%** | Agent 가 실제로 답한 값이 틀렸다 |
| 2025 총미납금액 | 12,335,580,090 | **29,251,314,636** | 169억 누락 |
| **전 기간** 납부율 | **100.36%** | **86.19%** | 비율이 100% 초과 = 구조 파괴 |
| **전 기간** 미납금액 | **−32억(음수)** | **1,226억** | 부호가 뒤집혀 있었다 |
| 개발(명) | `SUM(DEV_MEMBERS)` 2,291,878 | **1,585,923** | 44.5% 과대 |
| 발송 회원수 | 38,470,780 | **1,031,971** | 37.3배 |

세 결함 모두 **에러·워닝 없이** 그럴싸한 값을 답하고 있었다. 상세 = 문서10 **§22-J·§23·§24**.

### ⚠️ 이 세션 산출물을 인용하기 전에 읽을 것 — 문서10 **§25**(자기검토)

내가 **5건을 틀렸고 전부 실측이 잡았다**(일별 뷰 신설 제안=중복 · `SUM(DEV_MEMBERS)` metric 추가 제안 ·
일괄 교정 제안 · `ONETIME_ONETIME_FEE`=기부금 근사판단 · ALTER 미실행 후 인계).
공통 원인은 **컬럼명·COMMENT·합계 근사치를 증거로 취급**한 것이다. 🆕 **P83** — 결함을 「찾았다」고
판단한 직후가 가장 위험하다. 교정안을 만든 뒤 **반대 방향(내 교정이 옳은 것을 망치지 않는가)** 을
같은 강도로 시험한다. **근사 일치는 증거가 아니다.**

### 🔴 build 성공 판정 규약 (이번에 실패했다)
`ERROR=0` 은 성공 신호가 **아니다.** O40 1차 build 는 `PASS=370 WARN=27 ERROR=0` 이었는데
`on_schema_change` 기본값(`ignore`) 때문에 신규 컬럼이 **조용히 버려졌다**(INSERT 가 56컬럼만 씀).
→ 팩트 컬럼 추가 시 **①`06_DDL.sql` 수정 ②`ALTER TABLE` 실행 ③`INFORMATION_SCHEMA` 로 컬럼 존재 확인
④모델 수정 ⑤build ⑥`COUNT(신규컬럼)` 비NULL 검증**. O40-B 로 이제 불일치 시 **큰 소리로 실패**한다.

---

## 2. NL 스모크 (사람만 가능 · PRV-3)

`SNOWFLAKE.CORTEX.DATA_AGENT_RUN` 이 **트라이얼 계정에서 차단**된다 → Snowsight UI·CoWork 에서 사람이 확인.
SQL·SV·Agent 스펙 계층은 검증 완료이므로 **남은 위험은 Agent 의 도구 선택과 instruction 준수뿐**이다.

대상: `AGENT_MEMBER` **VERSION$5** · `AGENT_OVERALL` **VERSION$4**.
🔴 **#8~12 는 O38 신설 경로라 한 번도 검증되지 않았다** — 우선순위 최상.

| # | 질문 | 실패 판정 |
|---|---|---|
| **8** | "2025년 부서별 개발 목표 달성율" | 🔴 "목표는 아직 없습니다"·"Phase-2" → **실패**. `analyst_dev_achievement` 단일 호출이어야 한다 |
| **9** | "2025년 월별 개발 목표·실적·달성율 추이" | 실적÷목표를 손으로 나누면 실패(스코프 누락) |
| **10** | "개발구분별(신규·증액·재후원) 목표 달성율" | **100% 초과 값** → 실패 · 증액·재후원 목표가 거의 미편성임을 안 밝히면 미흡 |
| **11** | "부서별 **일자별** 개발 건수 상위 10개" | `analyst_dev_achievement`(월 grain) 사용 시 실패 → `analyst_member_event` |
| **12** | **`AGENT_OVERALL`** 에 "개발 목표 달성율" | 🔴 "목표 데이터가 없습니다" → **실패**. 어느 목표인지 되묻거나 회원 Agent 소관임을 안내해야 한다 |
| **14** | "**발송 회원수**" / "몇 명에게 발송했나" | 🔴 **[신규 O39]** 38,470,780 류의 값이 나오면 **실패**(그건 발송 **건수**다). 정답은 고유회원 **1,031,971** 계열 — `DISTINCT_SEND_MEMBERS` 로 라우팅돼야 한다 |
| **13** | "개발 목표 **달성율이 가장 높은 부서**" | 🔴 **[신규 O38-E]** 목표 규모(`TOTAL_GOAL_CNT`)를 함께 제시하지 않거나, 목표가 월 한 자리 수인 소규모 센터를 「최우수 부서」로 결론하면 **실패**. 100% 초과를 오류라고 답하는 것도 실패(실제 초과 달성이다) |
| 1 | "주요캠페인 대상 중단률 높은/낮은 캠페인 비교 + 회원특성" | "산출 불가"·"캠페인 정보 없음" → 실패. `analyst_member_cohort` 단일 호출 |
| 2 | "향후 12개월 캠페인별 중단률" | 미래 데이터 창작 → 실패(데이터 최신월 기준 대체 + 전제 명시) |
| 3 | "캠페인별 중단 건수" | `TOTAL_STOP_CNT` 사용 → 전건 (미매핑) 실패. `TOTAL_CAMPAIGN_STOP_CNT` |
| 4 | "중단률이 가장 높은 캠페인" | 소표본이 1위 → 규모 하한 미적용 |
| 5 | "홍보방법별 이탈률" | 숫자 코드·0행 → 실패. `PROMO_METHOD_NAME` |
| 6 | "평균 유지기간이 가장 긴 캠페인" | **이탈자만의 평균** 전제 없이 결론 → 실패 |
| 7 | "현재 연령대별 중단률" | 현재 연령으로 답하면 실패(생년월일 원천 없음 → 획득시점 대체) |

실패 시 조치 순서: ① SV 차원/metric COMMENT → ② `AI_SQL_GENERATION` → ③ Agent `orchestration`·도구 description.
재배포는 §4 절차.

---

## 3. 현재 상태 (2026-08-05 실측)

| 계층 | 상태 |
|---|---|
| BRONZE | 51테이블 (CRM 43 · AGENCY 4 · ERP 1 · GA4 3) |
| SILVER | 38테이블 |
| GOLD | 28테이블(DIM 15 + FACT 13) + 뷰 14(WIDE 13 + `DIM_MEMBER_CURRENT`) · 정보성 FK 42 · `FACT_MEMBER_MONTHLY` **58컬럼**(O40 로 56→58) |
| SERVING | 뷰 3 · **SV 8종** · Agent 2종(`AGENT_MEMBER`=**V$5** · `AGENT_OVERALL`=**V$4**) |
| 빈 테이블 | 예상된 3건 — `CRM_BIZ_TARGET`·`FACT_TARGET_BIZ`(E-6 미입고) · `BRONZE_GA4.SYNC_ERR_INFO` |

> 🔴 **재구축(`06_DDL`·`08_SILVER`)을 다시 실행하지 말 것.** 정본 DDL ↔ 물리 불일치 0이다 —
> 재실행하면 데이터만 날아간다(런북 §11).

**신규 계정 재현 순서**(런북 §11.2-C)
`07_ENVIRONMENT_RBAC_setup` → `06_DDL` → `08_SILVER_테이블DDL` → `deploy_dbt_project` → **dbt build**
→ `08_After_Deploy_DBT` → **`05_1`~`05_8_SV_DDL_*`**(순서 무관·독립) → `09_1_AGENT` → **`09_2_AGENT_버전업`**

🔴 자주 빠지는 3곳: ① `07` 누락 → `06_DDL.sql` 41~42행 즉사 ② `dbt build` ③ **`09_2`**(`09_1` 은 껍데기).
⛔ 실행 금지(포인터 스텁·실행 라인 0): `13_SV_AD_배포_추가작업.sql` · `09_AGENT_spec_구현.sql` · `02_SERVING_setup.sql`.

**SV 파일**(각 파일이 `USE`+정의+GRANT+스모크 포함 · **완전 독립 실행** → P62 경로 원천 제거)

`05_0`=인덱스·공통규약·전체검증(⚠️SV 정의 없음) · `05_1` MEMBER_MONTHLY · `05_2` MEMBER_EVENT ·
`05_3` MEMBER_COHORT(캠페인별 중단률 정본) · `05_4` SERVICE · `05_5` EVENT_PARTICIPATION(스모크 없음) ·
`05_6` BUDGET(스모크 없음) · `05_7` AD(helper 뷰 `FACT_AD_COMBINED` 동봉) ·
**`05_8` DEV_ACHIEVEMENT**(목표 대비 실적 정본 · 단일 논리테이블이라 helper 뷰 의존 없음)

---

## 4. Agent 스펙 — 정본 경로와 배포 절차

🔴 **정본은 `cortex_project/agents/<AGENT>/agent_spec.yaml` 하나다.** 파일명 반드시 `agent_spec.yaml`
(공식 문서의 `agent.yaml` 예시는 부정확 — 틀리면 `No spec file present` 로 실패).
근거 = **배포되는 파일이 정본**이다(`09_2` 의 `ADD VERSION FROM <디렉터리>` 가 stage 에서 이것을 읽는다).

⛔ 루트 `cortex_project/AGENT_*.agent.yaml` 은 **`cortex_project/_archive/` 로 이관**됐다 — **복원 금지**.
MEMBER 쪽은 도구 4개·샘플 8개의 **O33 이전 판본**이었다(정본은 도구 6개·샘플 19개).

🔴 **`cortex_agent_save`/`cortex_agent_deploy` 를 쓰지 말 것** — **live 버전**을 만들어 `ADD VERSION FROM` 을 거부시킨다.

**절차**: 스펙 수정 → `ALTER WORKSPACE USER$.PUBLIC."snowflake_files" COMMIT` → `09_2_AGENT_버전업.sql`
→ `SHOW VERSIONS IN AGENT` 로 `is_default` **실측** → 아니면 `SET DEFAULT_VERSION = 'VERSION$n'`
(⚠️ 자동 승격되는 경우와 안 되는 경우가 모두 관측됐다 — 원인 미확정이므로 **항상 확인**한다. P66)

---

## 5. 이월 작업

| # | 작업 | 근거 |
|---|---|---|
| 1 | **§2 NL 스모크** (사람만 가능) — 최우선. **#8~14** 가 미검증 신설 경로다 | PRV-3 · O38-E · O39 |
| 2 | 🔴 **O39-B 전건 0 컬럼군 조사** — `FMM` `ACTIVE_CNT`·`ACTIVE_MEMBERS`·`INCREASE_*`·`ACTIVE_CUM_*` **6종 40,054,883행 전건 0**(NULL 아님) · `FME` `UNPAID_STOP_*` · `FSE` `SUCCESS/FAIL/OPEN_MEMBERS` 등. ⚠️`ACTIVE_CNT` 는 활동회원 지표의 **분모** 축이라 영향범위 조사가 선결이고, 기존 C-9-R·B1 과 중복 가능하니 **대조 후 병합**할 것(신규 남발 금지) | 문서10 §25-C |
| 2-B | 🟠 **O39-C 개명 결정** — `*_MEMBERS`→`*_FLAG`. §22-H 교훈은 개명이지만 WIDE·SV·문서·저장쿼리를 깬다 → 영향범위 조사 후 사용자 결정 | 문서10 §23-E |
| 2-C | 🟠 **「브랜드2」 정체 현업 확인** — CRM `BRAND`(채널 대분류 69종)인가 AGENCY `MEDIA_NAME`(매체사)인가. 정본 인벤토리 §1(=CRM)·§3(=AGENCY)이 **서로 다르게** 적었다 | 05_필드 인벤토리 |
| 3 | 🟠 **O38-D 현업 확인** — 개발목표가 사실상 「신규」에만 편성되는 것이 방침인가 · `GOAL_CNT=0` 이 「미편성」인가 「0건 목표」인가 · 조직 개편 시 과거 목표 귀속 규칙 | 이슈원장 O38-D · 문서10 §22-H |
| 4 | **O32 COMMENT 수치 제거**(사용자 수동) | `06_DDL.sql`·`08_SILVER_테이블DDL`·dbt 모델. 🔴자동화 금지(코드값·한국어가 깨진다, P58) |
| 5 | 거짓 주석 회수 1건 | `models/gold/dim/DIM_MEMBER_CURRENT.sql` 헤더가 **이제 없는 3컬럼**을 열거(P33) |
| 6 | `SV_AD` 제약 재검증 | SV 8종 중 `SV_AD` 만 「비활성」 절이 없다. 광고 팩트(FAP·FAD·FAB)에 O33 전수조사를 안 했다 |
| 7 | 다른 SV 의 **시점 축** 점검(P60) | `MBER_STAT_CD`·`JOIN_PATH_CD`·`FIRST_SPONSORSHIP` 이 현재/시점 중 무엇인지 미확인 |
| 8 | 재후원 후 **재이탈 미관측** | `FACT_MEMBER_COHORT` 는 최초 중단만 본다 → 다중 구간(spell) 설계 검토 |
| 9 | `FALLBACK` 회원 처리 | 획득근거가 ''후원중단''(코드5)인 극소수는 「모집 캠페인」이라 부를 수 없다 — 제외 여부 현업 확인 |

**장기(Phase-2)**: GA 원자 grain 팩트 신설 여부(사용자 결정=검토만) · **E-6** `FACT_TARGET_BIZ` 0행
(요청서 `20_issue/41_입고요청서_CRM_BIZ_TARGET.md`) · **현재 연령·현주소 원천 입고**(BRONZE 에 축 자체가 없어
"현재 연령대별" 질문은 원천적으로 불가, O34) · 미주입 컬럼군(SV 「비활성」과 대응).

---

## 6. 필독 문서 (순서대로)

| # | 문서 | 왜 |
|---|---|---|
| 1 | `20_issue/00_INDEX_이슈원장.md` | **이슈 정본**. 최상단 「최신 상태」가 시간 역순 |
| 2 | `20_issue/10_진단_원인분석.md` | 진단·교훈 전문. **O38 = §22**(§22-A~I · 교훈 P67~P76). ⚠️ **O37(§21) 은 미기록** — 그쪽 정본은 이슈원장 O37 블록 |
| 3 | `02_GN_DW_building/06_RUNBOOK.md` §3.3·§11 | 평상시 컬럼 변경 / 전체 재구축. ⚠️ §3.3 은 **테이블** 절차다 — dbt 뷰에는 `ALTER TABLE`·`06_DDL` 동기화가 해당 없다 |
| 4 | `05_SV-Agent_ai/04_SV_설계.md` **§6.9** | SV 를 만지기 전 필독 — 반복 위반되는 **구조적 제약 8건** |
| 5 | `05_SV-Agent_ai/05_0_SV_DDL.sql` | SV 인덱스·공통규약·전체 배포 검증 |
| 6 | `20_issue/50_dbt_파이프라인_미결조치.md` · `40_입고대기_원천의존.md` | 파이프라인·원천 미결 |

---

## 7. 반복 발생한 함정 (교훈)

> 상세·실측 근거는 문서10. 여기서는 **한 줄 색인**만 둔다.

**정본·전파**
- **P57** 물리 `ALTER` 만 하고 정본 DDL 에 접지 않으면 다음 재구축에서 소실. **모델에만 있는 컬럼은 dbt 가 조용히 버린다**
- **P58** COMMENT 의 실측 수치는 자동 제거 불가(정규식이 코드값·한국어를 깬다) → 애초에 넣지 말 것
- **P59** 구조 변경은 **소비 계층까지 역추적**해야 완결. `dbt build` 통과는 dbt 가 아는 범위에서만 참
- **P62** 실행 순서 문서는 「파일이 실행 가능한지」까지 검증해야 정본. 판정 = `grep -vc "^--\|^$"`
- **P62-B** **자기교정은 전파되지 않는다** — 인용처를 `grep` 으로 회수해야 완결. 🔴 **리네임·이관은 그 자체가 P62-B 사건**
- **P33** 개명·값 변경은 COMMENT 를 데려오지 않는다. **거짓이 된 기존 경고문 회수**까지가 DoD

**의미 결함 (값은 정상인데 답이 틀린다 — 품질 테스트로 안 잡힌다)**
- **P60** 시점 스냅샷을 「현재 속성」으로 노출하면 값이 맞아도 답이 틀린다
- **P63** 비율은 **분자·분모 모집단 일치** 확인 필수. 누적 비율은 관측 기간에 지배 → **고정 관측창**으로 정규화
- **P67** **합계 일치는 grain 정확성의 근거가 아니다** — 축을 접으면 합계는 보존되고 grain 만 깨진다 → **grain 카디널리티**를 함께 본다
- **P73** 비율 스코프는 「행 존재」가 아니라 **「분모 값의 유효성」**. 분모의 0·NULL 비중을 먼저 실측 · **`>100` 0행 스모크 필수**
- **P77** 그 `>100` 불변식은 **grain 종속**이다 — 집계 grain 에서만 결함 신호이고 세부 grain 의 초과는 정상. 초과를 보면 base 행을 열어 스코프 결함인지 가른다. 🔴 정상 판정 뒤에도 끝이 아니다 — 분모 편차가 크면 **순위가 소표본에 지배**되므로 **규모 동반 제시·하한 되묻기**를 소비 계층에 심는다(상한 검사와 규모 하한은 **둘 다** 필요)
- **P78** `*_CNT`/`*_MEMBERS` 처럼 **의미가 이름에 실린 컬럼**은 이름이 아니라 **grain 으로 판정**한다 — 판정식 `SUM(컬럼) == COUNT(DISTINCT 개체키)` 가 모든 구간에서 성립하는가. 🔴 **이름 기반 일괄 교정 금지**(같은 이름이 grain 에 따라 옳다) · 교정 검증에는 「대상 반영」과 **「제외 대상 미훼손」을 둘 다** 넣는다
- **P79** metric **정의가 옳아도 synonym 이 틀리면 오답**이다 — SV 점검은 정의만 보지 말고 **어느 자연어 표현이 어느 metric 으로 가는지**를 본다. 한 SV 에 **건/명·총계/고유 쌍**이 있으면 **synonym 교차오염 필수 확인**(값이 그럴싸해 무증상)
- **P80** **금액 계열 비율에도 상한(>100%)·부호(음수) 스모크**를 걸어라 — 납부율이 전 기간 100.36%, 미납금액이 음수였는데 P73 검사가 개발 달성율에만 걸려 있어 몰랐다. **최근 연도만 보고 넘기지 말 것**(2025 는 93.98% 로 그럴싸했다)
- **P81** **「A ∪ B」 원천을 한 measure 에 합산했으면 판별 축을 같은 grain 에 함께 싣는다** — 판별 축이 없으면 하류에서 분자를 정화할 방법이 아예 없다. **차액식(A−B) measure 는 A·B 모집단 동일성을 먼저 증명**한다
- **P82** dbt `incremental+append` 의 `on_schema_change` 기본값(`ignore`)은 **신규 컬럼을 무증상 폐기**한다 — `ERROR=0` 이어도 컬럼이 안 생긴다. **ALTER 먼저 → 컬럼 존재 확인 → build** 순서를 지키고, 검증에 **「신규 컬럼 비NULL 건수」**를 넣는다. 🔴 순서를 문서에 적는 것 ≠ 선행 단계를 완료해 두는 것

**서술·활성화 (Agent 오답 경로)**
- **P61** 역추적은 SV 에서 멈추지 말 것 — Agent instruction·**도구 description** 까지. *"미적재/비활성/불가"* **부정형 서술은 축 활성화 때마다 회수**
- **P75** 축 「활성화」는 **차원 선언 + 실제 분해 쿼리**로 증명. 순서 = ①팩트 배선 →②차원 선언 →③분해 성공 →④**그 다음에** COMMENT·Agent 갱신. **오해를 부르는 식별자는 이름으로 고친다**
- **P76** 회수 판정 = 「사실 여부」가 아니라 **「그 답으로 사용자가 옳은 답을 얻는가」**. 어휘가 겹치면(사업목표/개발목표) 사실인 문장도 오답. **양방향**으로 — 「가능하다고 잘못 말하는 것」이 더 위험(현업이 빈 결과를 답으로 믿는다). 「✅ 사용가능」은 **실제로 답해 본 뒤**에만. 오답 유발 질문은 `sample_questions` 로 **회귀 감시**

**가드가 무력한 경우**
- **P68** 키 컬럼에는 값 테스트가 아니라 **규약 테스트**가 필요(`MONTH_KEY` 가 `not_null`·`relationships` 를 통과하면서 1~12였다)
- **P69** 센티넬 제외 `where` 가 걸린 FK 가드는 **그 축이 전건 센티넬이면 무력**(검사 대상 0행 → PASS 무의미)
- **P72** **테스트 ERROR 는 산출물을 막는다** — 원인이 데이터라고 가정하지 말 것. **신설 객체의 SKIP = 아예 없음**

**도구 사용**
- **P64** `dbt compile` 은 SQL 유효성 **미검증**(Jinja 렌더만) → 모델·**singular test 모두** `EXPLAIN`/실행으로 사전 검증
- **P70** compile 은 「돌리는 것」이 아니라 **「산출물을 읽는 것」** — 정상 종료해도 렌더 결과가 유효 SQL 이 아닐 수 있다
- **P71** **Jinja 는 SQL 주석을 주석으로 보지 않는다** · 공백제거 마커는 개행을 지워 뒤 구문을 앞 줄에 흡수 → 주석에 Jinja 문법 **인용 금지(말로 서술)**
- **P65** SV metric 명은 컬럼명과 달라야 한다(`TOTAL_` 접두). 같으면 `SUM(<이름>)` 이 metric 으로 해석돼 실패
- **P66** Agent 스펙 판정은 `DESCRIBE`(live)가 아니라 **default 버전**으로. 발행 후 `is_default` **실측**
- **P74** **세션 역할 ≠ 파일이 요구하는 역할.** 「헤더에 적어두었다」는 준수 근거가 아니다 → 생성 직후 owner 실측
- **PROC-3** 기대값이 어긋나면 **원인 규명 전에 어느 쪽도 인용하지 말 것**. 재구축 이전 수치는 무효일 수 있다

---

## 8. 운영 환경 지뢰

- **`/workspace` 마운트가 세션 중 사망할 수 있다**(I/O error, 실제 발생 — 서브디렉터리부터 죽는다).
  그때는 `cortex ws ls|cp 'USER$.PUBLIC."snowflake_files":/<경로>'` 로 **스테이지 경유**. ⚠️ `cp` 목적지는 **폴더**(`/` 로 끝나야 하고 리네임 불가)
- **`/tmp` 는 세션 중에도 초기화될 수 있다** → 작업 사본은 다시 만들 수 있게 스크립트화
- **워크스페이스에 새 파일을 직접 쓰지 말 것** — `/tmp` 에서 만들고 복사
- SQL 도구 토큰 만료 시 python `snowflake.connector` + `/snowflake/session/token`(매번 새로 읽는다)
- `hashlib.md5` 는 FIPS 모드에서 실패 → `sha256`
- CTE 명에 `asof` 금지(`ASOF JOIN` 예약어 충돌)

---
_Co-authored with CoCo_
