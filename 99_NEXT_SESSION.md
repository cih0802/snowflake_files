<!-- LLM-METADATA
doc_id: NEXT_SESSION_PROMPT
doc_role: 다음 세션 착수 프롬프트 — 이 파일만 읽고 바로 시작할 수 있게 구성
project: GN_DW (굿네이버스 데이터웨어하우스)
written: 2026-08-05 (O37 완료 시점 — 캠페인별 중단률 산출 가능화 · SV 파일 분할 · Agent VERSION$4 배포)
index: 20_issue/00_INDEX_이슈원장.md   ← 이슈 정본
runbook: 02_GN_DW_building/06_RUNBOOK.md  ← 운영·재구축 정본
END-METADATA -->

# 99. 다음 세션 착수 프롬프트

## 0. 이 세션에서 지킬 작업 규칙 (사용자 상시 지시)

1. **관련 문서를 읽을 때 중간에 끊기면 추론으로 채우지 말고 전부 읽을 것.**
2. **작업 전에 `20_issue/` 문서를 읽고 미해결·pending 항목을 파악할 것.**
3. **데이터 관련 작업은 COMMENT 를 근거로 단정하지 말고 BRONZE 원본을 스캔해 실측할 것.**
4. **큰 작업이 끝나면 `20_issue/` 문서를 갱신할 것.**
5. **`dbt build` 순서에 도달하면 멈추고 사용자 입력을 기다릴 것.**
6. **COMMENT 에는 실측 수치를 넣지 않는다** — 코드값만. 데이터가 계속 입고되므로 수치는 반드시 낡는다.
   (수치는 `20_issue/10_진단_원인분석.md` 에 둔다. 근거 = P58)

---

## 1. 🔴 다음 세션 첫 작업 — NL 스모크 (사람만 할 수 있다)

`SNOWFLAKE.CORTEX.DATA_AGENT_RUN` 이 **트라이얼 계정에서 차단**된다(`Access denied for trial accounts`).
따라서 Agent 자연어 경로는 **Snowsight UI·CoWork 에서 사람이 확인**해야 한다(PRV-3).
SQL·SV·Agent 스펙 계층은 전부 검증 완료이므로 **남은 위험은 Agent 의 도구 선택과 instruction 준수뿐**이다.

`AGENT_MEMBER`(VERSION$4)에 아래를 물어 기대 동작을 확인한다.

| # | 질문 | 기대 동작 | 실패 판정 |
|---|---|---|---|
| 1 | *"주요캠페인(캠페인카테고리)을 대상으로 중단률이 높은 캠페인과 낮은 캠페인을 비교하고 캠페인별 회원 특성을 분석해줘"* | **`analyst_member_cohort` 단일 호출** · 12개월 이탈률 표 + 획득시점 회원특성(연령대·지역·성별) | 🔴 "산출 불가"·"캠페인 정보 없음" 으로 답하면 **실패**(이번 작업의 직접 대상) |
| 2 | *"향후 12개월 캠페인별 중단률"* | 미래 데이터 없음을 밝히고 **데이터 최신월 기준 직전 기간**으로 대체 + 전제 명시 | 미래 데이터를 창작하면 실패 |
| 3 | *"캠페인별 중단 건수"* | `analyst_member_event` 의 **`TOTAL_CAMPAIGN_STOP_CNT`** 사용 | `TOTAL_STOP_CNT` 를 쓰면 전건 ''(미매핑)'' → 실패 |
| 4 | *"중단률이 가장 높은 캠페인은?"* | 모집 규모 하한을 적용했음을 밝힘 · 획득근거=NEW 한정 권고 | 소표본 캠페인이 1위로 나오면 규칙(9) 미준수 |
| 5 | *"홍보방법별 이탈률"* | `PROMO_METHOD_NAME` 라벨로 그루핑 | 숫자 코드로 답하거나 0행이면 실패 |
| 6 | *"평균 유지기간이 가장 긴 캠페인"* | **이탈자만의 평균**이라는 전제를 밝히고 이탈률과 함께 제시 | 전제 없이 "오래 유지된다"로 결론하면 실패 |
| 7 | *"현재 연령대별 중단률"* | **산출 불가**를 밝히고(원천에 생년월일 없음) 획득 시점 기준으로 대체 | 현재 연령으로 답하면 실패 |

실패 시 조치 지점은 ① SV 차원/metric COMMENT ② `AI_SQL_GENERATION` 규칙 ③ Agent `orchestration`·도구 description 순이다.
수정 후에는 `05_SV-Agent_ai/09_2_AGENT_버전업.sql` 경로로 재배포한다(아래 §4 절차 주의).

---

## 2. 현재 상태 (2026-08-05 실측)

| 계층 | 상태 |
|---|---|
| BRONZE | 51테이블 (CRM 43 · AGENCY 4 · ERP 1 · GA4 3) |
| SILVER | 38테이블 |
| GOLD | **28테이블**(DIM 15 + FACT **13**) + 뷰 13(WIDE 12 + `DIM_MEMBER_CURRENT`) · 정보성 FK **42** |
| SERVING | 뷰 3 · **SV 7종** · Agent 2종(`AGENT_MEMBER`=VERSION$4 · `AGENT_OVERALL`=VERSION$3) |
| 빈 테이블 | 예상된 3건뿐 — `CRM_BIZ_TARGET`·`FACT_TARGET_BIZ`(E-6 미입고) · `BRONZE_GA4.SYNC_ERR_INFO` |

> 🔴 **재구축(①`06_DDL` ②`08_SILVER`)을 다시 실행하지 말 것.** 정본 DDL ↔ 물리 대조 불일치 0이다.
> 재실행하면 데이터만 날아간다. 상세·예외 = 런북 **§11**.

**신규 계정 재현 순서** = 런북 **§11.2-C**
`07_ENVIRONMENT_RBAC_setup` → `06_DDL` → `08_SILVER_테이블DDL` → `deploy_dbt_project` → **dbt build**
→ `08_After_Deploy_DBT` → **`05_1`~`05_7_SV_DDL_*`**(순서 무관·독립 실행) → `09_1_AGENT` → **`09_2_AGENT_버전업`**

🔴 **자주 빠지는 3곳**: ① `07` 누락 → `06_DDL.sql` 41~42행에서 즉사 ② `dbt build` ③ **`09_2`**(`09_1` 은 껍데기만 만든다).
⛔ **실행 금지(DEPRECATED 포인터 스텁 · 실행 라인 0)**: `13_SV_AD_배포_추가작업.sql` · `09_AGENT_spec_구현.sql` · `02_SERVING_setup.sql`.

### SV 파일 구조 (2026-08-05 O37 분할)

| 파일 | SV | 비고 |
|---|---|---|
| `05_0_SV_DDL.sql` | — | **인덱스 + 공통규약 정본 + 전체 배포 검증**. ⚠️ SV 정의가 없다(실행해도 SV 가 안 만들어진다) |
| `05_1_SV_DDL_MEMBER_MONTHLY.sql` | `SV_MEMBER_MONTHLY` | |
| `05_2_SV_DDL_MEMBER_EVENT.sql` | `SV_MEMBER_EVENT` | O37 확장(캠페인 축 4종·사건시점 성별·캠페인 귀속 중단건) |
| `05_3_SV_DDL_MEMBER_COHORT.sql` | `SV_MEMBER_COHORT` | **O37 신설** — 캠페인별 중단률 정본 |
| `05_4_SV_DDL_SERVICE.sql` | `SV_SERVICE` | |
| `05_5_SV_DDL_EVENT_PARTICIPATION.sql` | `SV_EVENT_PARTICIPATION` | 스모크 절 없음(분할 전에도 없었다) |
| `05_6_SV_DDL_BUDGET.sql` | `SV_BUDGET` | 스모크 절 없음(동일) |
| `05_7_SV_DDL_AD.sql` | `SV_AD` | helper 뷰 `FACT_AD_COMBINED` 동봉(유일한 교차 의존) |

🟢 **각 파일이 완전 독립 실행**된다(`USE` + SV 정의 + 자기 GRANT + 자기 스모크). 파일 간 순서 의존이 0이므로
순서 문서가 stale 되는 실패 경로(P62)가 원천적으로 없다. 원본 = `_archive/05_SV_DDL_ORIGINAL_BACKUP_20260805.sql`.

---

## 3. 직전 세션(O37)이 한 일 — 요약

**트리거**: Agent 가 *"중단 사건에는 캠페인 정보가 원천에 없어 캠페인별 중단률은 구조적으로 산출 불가"* 라고 답했다. **틀린 판정이었다.**

| 발견 | 내용 |
|---|---|
| 🔴 원천 재스캔 | 중단원천(`TM_MM_FDRM_MBER_SPNSR_DSCNTC`)에는 확실히 캠페인이 없다(전 9컬럼). **그러나 개발원천(`TM_MM_FDRM_MBER_DVLP_AMT`)의 `DVLP_DIV_CD='5'`(MM015 후원중단) 행이 `CMPGN_CD` 를 전건 보유**하고 이미 `FME.CAMPAIGN_SK` 로 배선·적재까지 끝나 있었다 — **축은 있었고 measure 만 없었다** |
| 🔴 오답의 직접 원인 | SV COMMENT 의 *"캠페인별 중단건은 답이 나오지 않는다"* → **P61 재발**(축이 활성인데 부정형 서술 미회수) |
| 🔴 함정 ① | 코드5 의 캠페인은 **중단 시점** 캠페인 → 신규 건수로 나누면 모집단이 달라 비율이 **100% 초과**(실측) |
| 🔴 함정 ② | **누적 이탈률은 관측 기간에 지배**된다(획득이 이를수록 높다) → 실행 연도가 다른 캠페인 비교를 왜곡. 값·매핑·채움률이 전부 정상이라 어떤 품질 테스트로도 안 잡히는 **의미 결함** |
| ✅ 해법 | `FACT_MEMBER_COHORT`(회원 grain) 신설 + **12개월 고정 이탈률**을 정본으로. 분자를 관측 가능 코호트로 제한해 **분모 오선택을 데이터로 차단** |

**산출물**

- GOLD: `FACT_MEMBER_COHORT` 신설(PK·FK 3) · `FACT_MEMBER_EVENT` +3컬럼 · `DIM_CAMPAIGN` +2컬럼 · **정본 `06_DDL.sql` 같은 세션 동기화**(P57)
- dbt: 모델 3건 + yml 가드 + **singular test 2건**(분자⊆분모 불변식 · 캠페인/중단 measure 상호배타)
- SV: `SV_MEMBER_COHORT` 신설 · `SV_MEMBER_EVENT` 확장 · `SV_MEMBER_MONTHLY`·`SV_MEMBER_EVENT` **거짓가드 회수**
- Agent: `analyst_member_cohort` 도구 신설 + 라우팅·부정형 서술 회수 → **VERSION$4 is_default**(GRANT 4종 보존)
- 파일: SV DDL 단일 85KB → SV 단위 7파일 + 인덱스 · 인용처 회수 **19파일**

**검증 실측** — 전항 통과

| 항목 | 결과 |
|---|---|
| `FACT_MEMBER_EVENT` | 4,633,105 **불변**(fan-out 0) |
| `FACT_MEMBER_COHORT` | 1,585,949 = distinct MEMBER_DK(grain 유일) |
| `DIM_CAMPAIGN` | 36,144 불변 · `PROMO_METHOD_NAME` 34,686 채움·94종 |
| 12개월 분모/분자 | 1,465,547 / 293,818 |
| singular test 2건 | **위반 0**(직접 실행 확인) |
| SV 스모크 | fan-out 0 · **`max_rate = 0.7395 ≤ 1.0`** · 캠페인 카테고리 26종 **4.05% ~ 73.95%** |
| SV 분할 무손실 | `GET_DDL` 전후 대조 **6종 byte-identical** · owner·GRANT 보존 |

🔴 **관측창 정규화가 결론을 바꾼다는 실증**: `기존회원캠페인 및 기타` 누적 65.50% → 12개월 **4.05%** ·
`가두캠페인` 30.15% → **46.87%**. 누적률로 비교했다면 순위가 반대로 나왔다.

**부수 해소**

- ⚠️ `PROMO_METHOD` 가 **라벨 아닌 숫자코드**(95종)임을 발견 → 그대로 노출하면 Analyst 가 코드를 추측해 **0행 무증상 오답**.
  코드사전 탐색으로 **CM008 이 도메인 전량(94/94) 커버**를 확인해 `PROMO_METHOD_NAME` 배선(fan-out 0).
- ⚠️ **거짓 주석 회수**: `INFLOW_PATH` 를 *현업 "주요캠페인" 분류축*이라 적은 표기는 **거짓**이었다 —
  실제값이 디지털·방송·지역개발·대면모금 등 **모집 채널**이다. "주요캠페인(캠페인카테고리)" = **`CAMPAIGN_TYPE`(MM294)**.

---

## 4. 🔴 이 세션에서 내가 틀렸던 것 (같은 실수 반복 금지)

작업 중 발생한 결함 **5건**을 전수 기록한다. 4건은 자체 발견·수정, 1건은 빌드가 잡았다.

| # | 결함 | 원인 | 교훈 |
|---|---|---|---|
| 1 | singular test 에 `IIF` 사용 → build **ERROR 1** | T-SQL 함수(Snowflake 는 `IFF`). **모델은 `EXPLAIN` 으로 검증했는데 test 는 안 했다** | **P64**: `dbt compile` 은 Jinja 렌더만 하고 SQL 유효성을 DB 에 묻지 않는다 → singular test 도 모델과 동일하게 `EXPLAIN`/실행으로 사전 검증 |
| 2 | SV metric 이 컴파일 실패 | metric 명을 **원본 컬럼명과 동일**하게 선언 → metric 식의 `SUM(<이름>)` 이 컬럼이 아니라 metric(이미 집계값)으로 해석 | **P65**: metric 명은 컬럼명과 반드시 다르게(`TOTAL_` 접두). SV_AD 는 이름이 달라 무증상이었다 |
| 3 | SV 에 **캠페인 차원을 선언하지 않음** | `TABLES` 에 등록하고 `DIMENSIONS` 블록에서 누락. 이 SV 의 목적 자체였다 | 논리테이블 등록 ≠ 축 노출. 배포 후 **목표 질문을 실제로 실행**해야 드러난다 |
| 4 | Agent FQN 을 `SNOWFLAKE_INTELLIGENCE.AGENTS` 로 가정 | 실제는 **`GN_DW.SERVING`** | 객체 위치를 문서 기억으로 단정하지 말고 `SHOW ... IN ACCOUNT` 로 확인 |
| 5 | "Agent 도구 0개" 라고 **오판정** | `DESCRIBE AGENT` 는 **live** 버전을 반환한다. 이 프로젝트는 명명 버전 + `DEFAULT_VERSION` 방식이라 live 는 원래 비어 있다 | **P66**: Agent 스펙 판정은 `DESCRIBE`(live) 가 아니라 **default 버전**을 읽는다. O35 기록과 대조해 자체 교정했다 |

### 남아 있는 판단 유보·한계 (숨기지 않는다)

- ⚠️ **사용자 최종 `dbt build` 의 PASS/WARN/ERROR 카운트를 나는 보지 못했다.** 불변식 2건과 적재 결과는
  **직접 SQL 실행으로 검증**했으나, dbt 리포트 자체를 근거로 삼은 것은 아니다. 다음 세션에서
  `dbt build` 리포트로 재확인할 것을 권한다(기대: ERROR=0).
- ⚠️ **COMMENT 에 `"실제값 18종"` 같은 카디널리티 수치를 넣었다.** 코드값 열거는 규약 (3)이 요구하는 것이지만
  개수는 새 코드가 생기면 낡는다. 기존 SV 도 같은 형식을 쓰고 있어 일관성을 택했으나 **규칙 6의 경계선**이다 —
  차기 정리 시 개수를 빼고 값만 남기는 것을 검토할 것.
- ⚠️ `_gold_ready_schema.yml` 에 **`DIM_CAMPAIGN.PROMO_METHOD_NAME` 도메인 가드를 넣지 않았다**(94종·라벨이라
  열거가 비현실적). 필요하면 `not_null` 대신 `relationships`(→ SILVER CRM_CODE CM008) 형태를 검토한다.
- ⚠️ `05_5`·`05_6` 은 스모크 절이 없다(**분할 전에도 없었다** — 내가 뺀 것이 아니다). 필요하면 신설한다.

### 재검증한 것 (추론으로 남기지 않았다)

| 의심 | 결과 |
|---|---|
| 코호트 grain 에서도 `'10대 미만'` 이 편지쓰기대회 계열인가(O34-B 를 그대로 전용했다) | 🟢 **참** — 상위 10 획득캠페인 **전부** 희망편지쓰기대회·가족그림편지·세계시민교육편지 계열 |
| `DIM_MEMBER` 성별 라벨 = `CM017` 이라 단정했다 | 🟢 **참** — CM017 이 5개 라벨 전량 유일 커버(CM013 은 3/5) |
| `PROMO_METHOD` 라벨 부재 | 🟢 **참** — SILVER `CRM_CAMPAIGN` 에 `PR_MTH_NM` 없음(카테고리·유입경로는 코드/라벨 쌍 존재) |

---

## 5. 이월 작업 (착수 가능)

| # | 작업 | 위치·근거 |
|---|---|---|
| 1 | **NL 스모크** → §1 (최우선, 사람만 가능) | PRV-3 |
| 2 | **O32 COMMENT 수치 제거**(사용자 수동) | 대상 = `06_DDL.sql`·`08_SILVER_테이블DDL`·dbt 모델. 🔴 자동화 금지(코드값·한국어 문장이 깨진다 — P58). 파일·컬럼 단위 목록 추출은 가능 |
| 3 | **거짓 주석 회수 1건** | `models/gold/dim/DIM_MEMBER_CURRENT.sql` 헤더가 *"전건 NULL 7컬럼 미노출"* 로 **이제 없는 3컬럼**을 열거(P33 ③). build 실패 요인은 아님 |
| 4 | **`SV_AD` 제약 재검증** | SV 7종 중 `SV_AD` 만 「비활성」 절이 없고 ⚠ 제약 문구로만 되어 있다. O33 전수조사에서 제외됐다 — 광고 팩트(FAP·FAD·FAB)에 같은 조사를 하면 stale 제약이 더 나올 수 있다 |
| 5 | **다른 SV 의 시점 축 점검(P60 적용)** | O34~O37 은 `AGE`·`AREA_CD`·`SEX` 만 확인했다. **`MBER_STAT_CD`·`JOIN_PATH_CD`·`FIRST_SPONSORSHIP` 이 현재/시점 중 무엇인지** 미확인 — SV COMMENT 는 *"현재 스냅샷 기준"* 이라고만 적혀 있다 |
| 6 | **재후원 이후 재이탈 미관측** | `FACT_MEMBER_COHORT` 는 **최초 중단만** 본다. 재후원(MM015 코드4) 후 다시 이탈하는 회원의 2차 이탈은 코호트에 반영되지 않는다 — 수요 확인 후 다중 구간(spell) 설계 검토 |
| 7 | **`FALLBACK` 회원 처리 재검토** | 획득근거 FALLBACK 중 극소수는 근거 사건이 **''후원중단''(코드5)** 이라 「모집 캠페인」이라 부를 수 없다. 현재는 COMMENT·instruction 으로 `NEW` 한정을 권고만 한다 — 아예 제외할지 현업 확인 |

## 6. 장기 미결 (Phase-2)

- **GA 영역** — 사용자 결정으로 「데이터 검토만」. 잔여 질문: *"`FACT_GA_SESSION`·`FACT_GA_CLICK`(GA 원자 grain 팩트)을 신설할 것인가"*
- **E-6 원천 미입고** — `CRM_BIZ_TARGET`·`FACT_TARGET_BIZ` 0행. 요청서 = `20_issue/41_입고요청서_CRM_BIZ_TARGET.md`
- **현재 연령·현주소 원천 입고 요청** — BRONZE 전체에 생년월일·현주소 축이 없어 "현재 연령대별" 질문은 원천적으로 답할 수 없다(O34). 현업 요청 사안
- **미주입 컬럼군** — GOLD 다수 컬럼이 전건 NULL/0(센티넬). SV 「비활성」 선언과 대응된다

---

## 7. 필독 문서 (순서대로)

| 순서 | 문서 | 왜 |
|---|---|---|
| 1 | `20_issue/00_INDEX_이슈원장.md` | **이슈 정본**. 최상단 「최신 상태」 블록이 시간 역순 |
| 2 | `20_issue/10_진단_원인분석.md` §19~ | 진단 전문 + 교훈. ⚠️ **O37(§21) 는 아직 이 문서에 미기록** — 이슈원장 O37 블록이 현재 정본이다 |
| 3 | `02_GN_DW_building/06_RUNBOOK.md` §3.3·§11 | 평상시 컬럼 변경 절차 / 전체 재구축 순서 |
| 4 | `05_SV-Agent_ai/05_0_SV_DDL.sql` | SV 분할 인덱스·공통규약·전체 배포 검증 |
| 5 | `05_SV-Agent_ai/04_SV_설계.md` **§6.9** | 재현·확장 시 반복 위반되는 **구조적 제약 8건** — SV 를 만지기 전에 반드시 읽는다 |
| 6 | `20_issue/50_dbt_파이프라인_미결조치.md` · `40_입고대기_원천의존.md` | 파이프라인·원천 미결 |

---

## 8. 이 프로젝트에서 반복 발생한 함정

| 교훈 | 내용 |
|---|---|
| **P57** | 정본 DDL 은 재구축의 유일한 진실이다. 물리 `ALTER` 만 하고 정본에 접지 않으면 다음 재구축에서 경고 없이 소실된다. 드리프트 방향이 증상을 결정한다 — 물리에만 있으면 에러, **모델에만 있으면 dbt 가 조용히 버린다** |
| **P58** | COMMENT 의 실측 수치는 **자동으로 지울 수 없다**(정규식이 코드값·한국어 문장을 깬다). 애초에 넣지 말 것 |
| **P59** | 구조 변경은 **소비 계층까지 역추적**해야 완결된다. `dbt build` 통과는 dbt 가 아는 범위 안에서만 참이다 |
| **P60** | **시점 스냅샷을 「현재 속성」으로 노출하면 값이 맞아도 답이 틀린다.** 값·매핑·채움률이 정상이라 품질 테스트로 안 잡히는 **의미 결함**이다 |
| **P61** | 소비 끝단 역추적은 SV 에서 멈추면 안 된다 — Agent instruction·**도구 description** 까지 본다. *"미적재/비활성/불가"* 같은 **부정형 서술은 축을 활성화할 때마다 반드시 회수**한다(활성화는 새 것을 켜는 일이면서 **옛 금지문을 끄는 일**이다) |
| **P62** | 실행 순서 문서는 「파일이 실행 가능한지」까지 검증해야 정본이다. 판정법 = `grep -vc "^--\|^$" <file>` |
| **P62-B** | **자기교정은 전파되지 않는다.** 한 문서에서 오류를 바로잡았으면 그것을 인용하던 파일을 `grep` 으로 찾아 함께 고쳐야 완결이다. 🔴 **파일 리네임은 그 자체가 P62-B 사건**이다 — 정본 이름이 바뀌면 그것을 가리킨 모든 문서가 즉시 거짓이 된다(O37 에서 실증) |
| **P63** | 🆕 **비율 지표는 분자·분모의 모집단이 같은지 먼저 확인한다.** 축이 존재한다는 것과 그 축으로 비율이 성립한다는 것은 다르다. 그리고 **누적 비율은 관측 기간에 지배**되므로 시점이 다른 대상을 비교할 때는 **고정 관측창**으로 정규화한다. 분자를 관측 가능 집합으로 제한해 **분모 오선택을 데이터로 차단**하는 것이 가장 안전하다 |
| **P64** | 🆕 **`dbt compile` 은 SQL 유효성을 검증하지 않는다**(Jinja 렌더만 한다). 모델뿐 아니라 **singular test 도** `EXPLAIN`/직접 실행으로 사전 검증한다 |
| **P65** | 🆕 **SV metric 명은 원본 컬럼명과 달라야 한다.** 같으면 metric 식의 `SUM(<이름>)` 이 컬럼이 아니라 metric(집계값)으로 해석돼 `Invalid metric definition` 으로 실패한다 |
| **P66** | 🆕 **Agent 스펙 판정은 `DESCRIBE AGENT`(live)가 아니라 default 버전으로 한다.** 이 프로젝트는 명명 버전 + `DEFAULT_VERSION` 방식이라 live 는 비어 있는 것이 정상이며, live 를 보고 "도구 0개"라 판정하면 오진이다 |
| **P33** | 개명·값 변경은 COMMENT 를 데려오지 않는다. **이제 거짓이 된 기존 경고문을 회수**하는 것까지가 DoD 다 |
| **PROC-3** | 기대값이 어긋나면 **원인 규명 전에는 어느 쪽도 인용하지 말 것**. 재구축 이전에 기록된 행수·적중률은 무효일 수 있다 |

### 운영 환경 지뢰

- **`/workspace` 마운트가 세션 중 사망할 수 있다**(I/O error, O37 세션에서 실제 발생 — 서브디렉터리부터 죽었다).
  그때는 `cortex ws ls|cp 'USER$.PUBLIC."snowflake_files":/<경로>'` 로 **스테이지 경유**한다.
  ⚠️ `cp` 의 목적지는 **폴더**여야 한다(`/`로 끝나야 하고 리네임은 불가).
- **`/tmp` 는 세션 중에도 초기화될 수 있다.** 작업 사본은 다시 내려받을 수 있게 스크립트화할 것
- **SQL 도구 토큰이 만료**되면 python `snowflake.connector` + `/snowflake/session/token` 으로 우회한다(토큰을 매번 새로 읽는다)
- **`hashlib.md5` 가 FIPS 모드에서 실패**한다(`digital envelope routines unsupported`) → `sha256` 을 쓴다
- **CTE 명에 `asof` 를 쓰면 안 된다** — `ASOF JOIN` 예약어와 충돌한다
- **워크스페이스 파일에 새로 쓰지 말 것** — `/tmp` 에서 만들고 `cortex ws cp` 로 올린다

---
_Co-authored with CoCo_
