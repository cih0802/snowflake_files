> #### 🟢 [2026-08-28 O111-B] 승인 전건 집행 + 유지 연산 가드 2종 신설 + 도구 5종 결함 시정 + 스킬 개선
>
> **입력** = 사용자 「이번 세션 작업을 비판적으로 검토하고, 승인이 필요한 작업은 전부 승인하니 작업과 문서를 개선하고 그 후 스킬도 개선하라」.
> 라벨 = **같은 세션의 후속 단위**이므로 새 번호를 발급하지 않고 접미로 이었다(`O111` → `O111-B` · 경합·과대 회피).
>
> ##### ① 🔴🔴 최우선 규명 — O111 의 「원장 행 소실」 원인
>
> 🔴 **먼저 코드를 읽었다**: `republish()` 는 `write_text(src, build_hub(...))` **1곳만** 쓰고
> 조각 파일은 한 바이트도 건드리지 않는다 ⇒ **`--republish` 는 범인이 아니다**(스크립트 소관 배제).
> 🔴 그러나 `--republish`·`--rebalance` 는 **둘 다 `collect_bodies()` 로 「현재 조각」을 읽어**
> 그것을 기준으로 쓴다 ⇒ **O107 사고(낡은 내용을 읽어 전 조각 재기록)와 같은 지점을 지난다.**
> 🔴 **게이트가 이 축을 원리적으로 못 본다**: `verify()` 는 **자기일관성**(concat ↔ 허브 해시)만 보고
> (내용이 낡아도 일관되면 🟢), `R1-7-2`(해시 안정성)는 **같은 시점 2회 읽기**만 본다.
> 두 사고는 **시점 간 불일치**라 두 축 모두 밖이다 ⇒ **별도 가드가 필요하다는 결론**.
>
> 🟢 **처방 = `--expect TOKEN` 신설**(`split_doc.py` · 반복 가능 · `R1-6-23` 신설):
> ㉠ **쓰기 전** = 방금 수집한 내용에 그 토큰이 있는가(**낡은 읽기 차단** · O107 축)
> ㉡ **쓴 뒤** = 디스크에서 되읽어도 있는가(**조용한 소실 차단** · O111 축)
> 🔴 없으면 **쓰지 않고 exit 1**. 🟢 음성 테스트 `test_split_doc_expect.py` **5축 14단정** —
> **두 사고를 주입해** 차단을 단정한다(축3 = 낡은 토큰 기대 시 조각 **한 바이트도 안 씀** + 대조군).
> ⚠️ **선언 기반의 한계를 명시**했다 — 주지 않으면 검사할 것이 없으므로 조문이 의무화한다.
>
> ##### ② 승인 집행 1 — 재균형 2건(`--expect` 로 보호)
>
> · `00_INDEX_이슈원장` = 조각 8→**9** · 여유 **7,370 → 16,815 B** ⇒ `doc_type_gate` 🟠 **해소**
> · `99_NEXT_SESSION` = 조각 19 · 여유 **16,537 B**(O111 이 절 압축으로 피했던 상한 근처 해소)
> 두 건 모두 신선도·영속성 가드 **전건 통과**(토큰 3종·4종) · 재균형 후 `doc_coord_gate` 축1a **0** 확인.
>
> ##### ③ 승인 집행 2 — 골든 3종 발행(전부 사유 기록)
>
> · **`test_generators`** = 🔴 **O111 이 「귀속 불가」로 남긴 3건을 규명했다** —
>   `[O89]` 마커를 가진 엔트리가 **정확히 3건**(`DIM_SPONSORSHIP.SPONSORSHIP_DIV_CD`·`DIV_NAME`·`GROUP_NAME`)이고
>   세 지표가 모두 +3 인 이유는 그 3건이 **색인·설명셀 자격을 함께** 갖기 때문임을 **3/3/3 으로 실측**했다.
>   🟢 동시에 골든 구조를 **카운트 → 키 목록**으로 바꿨다(`inv.entry_keys`·`inv.index_key_list`·`inv.desc_cell_list`)
>   ⇒ `diff()` 가 이미 리스트를 원소 단위로 열거하므로 **다음 차이는 그 자리에서 귀속된다.**
>   🟢 `--reason`/`--label`/`history` 신설(O109 가 다른 골든 2종에만 넣었던 계약을 이 골든에도 적용).
>   ⇒ `test_generators` **21/21 PASS**(O111 착수 시점 20/1 FAIL).
> · **`index_row_gate`** = 누적 841→**909**(+68)을 전수 분해 ⇒ 🟢 **사람 원장 행 유실 0 전건**
>   (유실로 보인 23건은 **전부 자동 생성 표** = 재균형의 정상 산물) · 신설 91건 내역까지 사유에 적어 발행.
> · **`doc_heading_gate`** = 승계 취소선 1건(§0-PPP 제목 변경)을 사유와 함께 발행 · 실유실 0.
>
> ##### ④ 🔴 자기시정 — 골든 발행이 스키마 지문을 지웠다
>
> `test_generators --update-golden` 이 `/tmp/schema.json` 부재 시 `fp=None` 을 그대로 써서
> **살아 있던 지문 `3581c638b4260bd9` 을 조용히 지웠다.** 🔴 `/tmp` 는 세션마다 비므로 이것은
> **예외가 아니라 기본 상황**이고, 지워지면 `T5.freshness` 가 「지문 없음」으로 바뀌어
> **신선도 검사가 꺼진다**(검사가 꺼지는 것은 FAIL 보다 나쁘다 · `P106`).
> ⇒ 🟢 **이전 값 보전 가드** 신설 + 지운 값을 그 자리에서 복원(JSON 파싱·지문 재확인).
>
> ##### ⑤ 승인 집행 3 — 절 은퇴 실적용, 그리고 그 과정에서 나온 최대 발견
>
> 🔴 **후보를 「읽었더니」 은퇴하면 안 되는 절이었다.** `50_dbt_…-014.md` §`✅ [DONE 순서9-C]` 는
> 제목이 ✅ 인데 본문에 ㉠ **`warn→error 복귀 추적표`**(복귀 트리거가 아직 열린 7행 · `BLOCKING-1` 등)
> ㉡ **WARN 27건 전량 목록**(*"🔴 이 표가 없어서 같은 질문이 두 번 나왔다"* 라고 **존재 이유를 스스로 적은** 표)
> 을 담고 있었다 ⇒ 옮기면 **살아 있는 추적을 닫힌 로그로 보낸다.**
> 🔴 **실측 = 그 조각의 닫힘 후보 5개 중 4개가 열림 신호 보유** ⇒ 종전 도구로 대량 은퇴했다면 **4/5 가 오이관**.
> 🟢 **처방 = 열림 신호 5종 탐지**(🔴/🟠/🟡 · 복귀·전환 트리거 · 미결·대기 · `BLOCKING-N` · 존재 이유 자기선언)
> ⇒ `--list` 에 숫자로 표시 + 적용 경로 **기본 차단** + `--force`(읽었다는 선언 · 사용 시 출력에 남는다) · `R1-6-24` 신설.
> 🟢 **실적용은 유일한 청정 후보 1건으로 실증**(§`✅ [DONE 2026-07-15] 검토 항목 4` = drift 0 완료 보고 ·
> 열림 신호 0 · 본문을 읽고 판단) ⇒ **O107·O108 이 dry-run 까지만 했던 경로를 최초로 끝까지 통과**시켰다
> (토큰 4종 부재 0 · 제목 유실 0 · 허브 재발행 PASS).
> ⚠️ **절감 실측 = 418 B 이관 → 원본 절감 27 B**(포인터가 본문만큼 길다) ⇒ 🔴 **은퇴는 용량 회복 수단이
> 아니라 가독성 수단**임을 조문에 명시했다. 그리고 **여유 부족이 0 인 상태에서 대량 은퇴는 하지 않았다** —
> 승인은 **허가**이고 **비용·편익 판단을 대신하지 않는다**.
>
> ##### ⑥ 승인 집행 4 — 임시 스크립트 정리(`R1-7-7` 준수)
>
> 접두 나열 → **개별 삭제 28건**(`rm -rf` 미사용) → **두 축 확인**(`os.listdir` = 잔존 0 · `cortex ws ls` = 스테이지 반영).
> 회수 **207,190 B**. 🟢 **삭제 전 병합 검증**을 먼저 했다 — `_o*_entry.md` 15종의 고유 토큰을 이력 전문과
> 대조해 **2건이 미확인**으로 떴고, 규명 결과 ㉠ `_o108_next.md` 는 **99_NEXT 에 전건 병합**
> ㉡ `_o85_entry.md` 는 이력 `01_세션이력-028.md:4` 에 **🟢 개정본으로 실재**(파일은 🟡 초안 = 대체됨)였다.
> 🟢 **`_o111_entry.md` 는 보존**(현 세션 인수 원본 · 늦은 정정 여지). ⚠️ **승인 범위 밖 잔존** =
> `gen_o53_*`·`patch_o63*`·`move_o63_*`·`run_o53_*` 8종(접두가 `_o` 가 아니어서 `▣WWW ⑦` 대상이 아니었다).
>
> ##### ⑦ 도구 5종 결함 시정 (전부 이 세션 실측)
>
> | 도구 | 결함 | 처방 |
> |---|---|---|
> | `split_doc` | 유지 연산이 낡은 내용을 읽어도 · 쓴 뒤 사라져도 **아무 축도 안 본다** | `--expect` 2단 가드 + 테스트 5축 |
> | `retire_sections` | 「닫힘」을 **제목 이모지로만** 판정 ⇒ 4/5 오이관 위험 | 열림 신호 5종 + 기본 차단 + `--force` + 테스트 축10 |
> | `test_generators` | 골든이 **카운트만** 보관 ⇒ 차이 귀속 불가 · 사유 이력 없음 · 지문 소실 | 키 목록화 + `--reason`/`history` + 지문 보전 |
> | `split_narrative` | 처방 줄이 **블록의 시작**이 될 수 있었다(불릿 뒤 `⚠️` 미탐) · 불변식1 FAIL 시 **우회로 부재** | `PRESCRIPTION` 에 선행 불릿 허용 + 시작 차단 · `--exclude` 신설(앵커는 제외 **후** 발급 = 결번 0) |
> | `decision_closure_gate` | O111 이 제외 규칙만 넣고 **음성 테스트를 안 만들었다**(`R3-2` 확정위반) | `test_decision_closure_gate.py` **5축 20단정**(오탐·재현율 양축 + 축5 = 제외를 `.*` 로 넓히면 재현율 단정이 깨지는지) |
>
> ##### ⑧ 조문 2종 신설 + 스킬 개선
>
> 🟢 **`R1-6-23`**(유지 연산 `--expect` 의무) · **`R1-6-24`**(「닫힘」은 제목 판정일 뿐) ⇒ 조문 **93 → 95**
> (역전 0 · 중복 0). ⚠️ **자기시정** = 처음 쓴 조문 헤더가 `🆕 🔴🔴 **R1-6-23`  형태여서
> `clause_order_gate` 의 `DEF_RX`(`🆕` 뒤 곧바로 `**`)에 **잡히지 않았다** — 조문 수가 23 그대로였다
> ⇒ 🔴 **「조문을 썼다」와 「게이트가 그것을 조문으로 센다」는 다르다**(형식이 계약이다) ⇒ 형식을 맞춰 재확인.
>
> 🟢 **스킬 개선 6지점** = ㉠ **판정 일반화 3조 신설**(상시 준수 절 머리) — 「0건은 없다가 아닐 수 있다」·
> 「제외 규칙은 근거가 성립하는 축에만」·「판정 문구가 세는 것과 읽는 뜻이 다를 수 있다」, 셋 다 **이 세션의
> 실측 오판**을 근거로 달았다 ㉡ `--expect` 절(O107·O111 두 사고 + 게이트가 못 보는 이유) ㉢ 절 은퇴 경고
> (4/5 실측 · 절감 27 B · 부분 은퇴 권고) ㉣ 종료 절차에서 **테스트 목록을 지우고** `for` 한 줄 실행으로 대체
> (🔴 O110 이 8종을 적어 뒀는데 실제로는 13종이었다 = `R3-9 ㉦` 재발) ㉤ 골든 `--reason` 의무 + 지문 소실 경고
> ㉥ 정본 3곳 절에 **원장 행 2,000자 상한**·**취소선이 제목 게이트에 유실로 잡히는 것은 정상**·
> **새 절이 300줄 상한에 닿을 수 있음**을 추가. 🟢 스킬 = 27,222자(O111 종료 24,208자에서 증가 —
> **다이어트와 반대 방향이므로 그대로 신고한다**).
>
> ##### 🔴 자기결함·미달 (전건 자기신고)
>
> ㉠ 🔴 **스키마 지문을 한 번 지웠다**(위 ④) — 가드로 막고 복원했으나 **내가 만든 회귀**다.
> ㉡ 🔴 **조문 헤더 형식을 틀려 게이트가 조문을 세지 못했다**(위 ⑧) — 즉시 시정.
> ㉢ 🔴 **`--exclude` 초판이 첫 줄만 매칭**해서 막아야 할 토큰(2번째 줄의 `R1-3-2`)을 놓쳤다 — 본문 전체로 교정.
> ㉣ 🟠 **스킬이 커졌다**(24,208 → 27,222자). `▣IIII ①`(스킬 다이어트)은 **더 멀어졌다** —
>   내용은 전부 실측 근거가 있지만 **부담 축에서는 역행**이므로 재판단이 필요하다.
> ㉤ 🟠 **`01_문서분할_규약` 여유 부족을 내가 만들었다**(조문 2종 추가) — 서사 이관으로 4,437 → **6,035 B**
>   까지 회복했으나 **20% 문턱(8,192 B) 미달**이다. 🔴 남은 것은 조문 본문이라 서사 이관으로는 못 줄인다
>   ⇒ 처방은 **분할(허브+조각)**이고 그것은 4곳 분모 편입 + 골든 발행을 요구한다 ⇒ **다음 세션 판단**.
> ㉥ 🟠 **`retire_sections` 테스트를 내 가드가 깨뜨렸다**(픽스처가 정당하게 차단됨) ⇒ `--force` 명시 +
>   **축10 신설**로 해소(55 → **62단정**). 🟢 이것은 「테스트가 가드를 잡은」 정상 신호였다.
>
> ##### 🟢 게이트·검증 (종료 시)
>
> `doc_census` stale **0** · `doc_type_gate` 🟠 **1**(위 ㉤ · O111 이 닫은 지침은 그대로 ✅) ·
> `clause_order_gate` ✅ **조문 95** · `index_row_gate` ✅ 유실 0 · `doc_heading_gate` ✅ 유실 0 ·
> `doc_coord_gate` ✅ 축1a **0** · `decision_closure_gate` 🟠 21(전부 이전 세션 소관 · 변동 없음).
> 음성 테스트 **13종 전건 PASS**(O111 종료 시 10/1 FAIL → **13/0**) · `test_generators` **21/21**.
> `read` 미반환 **0건** · 재호출 **0건**. 🔴 **`R4-4-2 ㉡` 적용 · 확정위반 1**(㉡ 조문 형식 오기로 인한
> `R3-9 ㉣` 미충족을 자기적발·즉시 시정 ⇒ 잔여 0).
> 🔴 **이 표를 「지침 준수」의 증거로 쓰지 마라**(`R3-9`) — 위 자기결함 6건은 이 표가 🟢 일 때 났다.
