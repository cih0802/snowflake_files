<!-- LLM-METADATA
doc_id: O170_EVIDENCE
doc_role: O170 근거철 — 집합 불일치 3건(D7·D9·D10) · 버킷 전수 재실측(D6) · DEC-35 정보 8건
project: GN_DW (굿네이버스)
created: 2026-09-17
created_by: O170
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# `_o170_evidence.md` — O170 근거철

> 🔴 이 파일이 O170 의 **수치·판정 정본**이다. 원장·인수인계는 색인이다.
> 🔴 `R1-3-7-c` 에 따라 **판정보다 먼저** 기록한다. 인용은 **원문 구절 그대로 + 좌표**로 둔다.
> 라이브 = 계정 `RY53492` · `GN_DW` · 2026-09-17 실측(`R2-8-4-a` 3요소).

---

## §E0. 독해 근거 (`R1-3-7-b`)

| # | 파일 | offset/limit | 도착 | 그 구간 고유 토큰 |
|---|---|---|---|---|
| 1 | `00_guides/00_작업지침_세션운영규칙.md` | 1/355 | O | `R0-8-4` 3조건 · `R1-7-10` · `R3-9 ㉨` handoff_gap |
| 2 | `20_issue/00_BRIEF.md` | 1/135 | O | 열린 착수표 2 · 인수인계 5 · 문서50 열린 절 6 · stale 0 |
| 3 | `20_issue/00_INDEX_이슈원장_조각/-002.md` | 1/40 | O | `O165` 유실 9→0 · `O163` FK 20→56 · `O155` 축1 536건 |
| 4 | `50_dbt_…-016.md` | 1/60 · 60/112 | O | `V7 ga4_range_predicate` · `A2 철회 블록` · `O91-G` 표 5행 |
| 5 | `50_dbt_…-014.md` | 170/47 | O | `:180` 누락/과잉 GOLD 6개 · `O144-C` 6중 2 처리 |
| 6 | `50_dbt_…-015.md` | 1/107 | O | `:18` O167 「5건 중 1건 빠진다 ⇒ 4건」 · WARN 27 전량표 |

read 미반환 **0** · 재호출 **0**.

---

## §E1. `D7` — 결정대기 GOLD 「4 ↔ 6」 집합 불일치

🟢 **판정 = 불일치가 아니다. 두 수는 서로 다른 집합이고 둘 다 참이다.**

정본 = `50_dbt_파이프라인_미결조치-014.md:180` 절 제목 = *"🟡 결정 대기 — 누락/과잉 GOLD 6개"*.
같은 절 `:183`~`:187` 이 스스로 분해한다:

- `:184` *"🟢 `DIM_MEMBER_IDENTITY` = 본문이 이미 **2026-07-15 활성화 완료**로 적는다."*
- `:185` *"🟢 `FACT_TARGET_PROJECT` = **스켈레톤 모델이 저작·배포됐다**"* ⇒ *"「모델 부재」가 아니라 **데이터 부재**"*
- `:187` *"🟡 **잔여 = 소스 준비 4개의 「모델 작성 여부」**"*

⇒ **6 = 원집합**(절 제목 · 2026-07 발행) · **4 = 현 잔여**(2026-09-08 `O144-C` 재판정분).
인수인계의 「4」가 현행이고 문서50 제목의 「6」은 **원집합 표기**다.

🔴 **잔여 결함 = 절 제목이 현 잔여를 말하지 않는다.** 제목만 읽는 독자는 6건이 열려 있다고 읽는다
(`O91-F` 의 `W4` 「판정 표만 읽는 독자는 요구가 바뀐 것을 모른다」와 같은 축).
🟢 처방 = 제목에 **원집합 6 · 잔여 4** 를 병기한다. **집합을 줄이지 말고 병기한다**(원집합은 감사 이력이다).

---

## §E2. `D9` — `O91-F` 「5건 중 4건 종결 · 잔여 2건」 산술 불일치

🟢 **판정 = 산술 오류가 아니다. 제목이 분모가 다른 두 집합을 한 문장에 섞었다.**

정본 = `50_dbt_파이프라인_미결조치-016.md`. 두 집합이 실재한다:

**집합 ①  = §「다음 세션이 먼저 할 것」 처방 5건**(`:148`~`:154`)
`:156`~`:164` 의 `O91-G` 집행 결과 표 =

| # | 처방 | 결과 |
|---|---|---|
| 1 | A3 문안 정정 | 🟢 완료 |
| 2 | A2 센티넬 통일 | 🔴 **철회**(전제가 거짓 · 시정하면 `P21` 위반) |
| 3 | A1 격리 테이블 | 🟠 승계 |
| 4 | V8 음성 테스트 | 🟢 완료 |
| 5 | V1 재발 방지 | 🟢 적용 시작 |

⇒ **5 = 3 완료 + 1 철회 + 1 승계.** 제목의 「4건 종결」 = **완료 3 + 철회 1**(처리가 끝난 것) ·
5 − 4 = **1**(승계 = `A1`). 🟢 **여기까지 산술은 맞다.**

**집합 ② = `:167` 「미종결 승계 3건」** = `A1`(격리 테이블 · 결정) · `A4`/`A2-B`(정보량 0 컬럼 제외 · 설계) ·
`90_해소완료_로그` 분할. 🔴 이 중 `A4`/`A2-B` 는 **집합 ①의 5건 목록에 없다**(별도 축인 아키텍처 결함이다).

`:9`~`:11`(2026-09-08 `O144-C`) 이 집합 ②를 줄였다 —
*"「미종결 승계 3건」 중 **`90_해소완료_로그` 분할**은 🟢 **완료**다"* ⇒ **잔여 2 = `A1` · `A4`/`A2-B`**.

⇒ 🟢 **「4건 종결」은 집합 ① 기준 · 「잔여 2건」은 집합 ② 기준.** 5−4=1 과 2 는 **다른 분모**라 충돌이 아니다.
🔴 **잔여 결함 = 한 제목에 두 분모를 섞었다** ⇒ 독자가 산술 불일치로 읽는다(실제로 이번에 그렇게 읽혔다).
🟢 처방 = 제목을 **분모별로 두 조각으로 가른다**(처방 5건: 4 처리 · 1 승계 / 결함 승계: 잔여 2).
🟢 판정식 = **「N건 중 M건」을 쓸 때 N 의 정의를 같은 문장에 적어라**(`O111 ㉠` 분모 병기의 제목판).

---

## §E3. `D10` — 순서9-C 잔여 4 「전부 외부의존인가」 라이브 실측

🔴🔴 **판정 = 거짓이다. 4건 중 2건은 원천이 이미 실재한다.**

종전 기재 = `50_dbt_파이프라인_미결조치-015.md:18`~`:21`(2026-09-16 `O167`) =
*"잔여 = `FUNDRAISING_COST`(E-1) · `AD_COST`(E-4) · `FACT_TARGET_PROJECT`(E-6) · GA4 분석(G-5) **4건**"*
및 `:35` *"⏳ **잔여 외부 원천 입고**"*.

### E3-1. `E-1` `FACT_BUDGET.FUNDRAISING_COST` — 🔴 「외부 원천 입고 대기」가 거짓

- GOLD 채움 = `GN_DW.GOLD.FACT_BUDGET` **1,332행** · `FUNDRAISING_COST` non-NULL **0** · non-zero **0**
- 🟢 그런데 원천이 실재한다 = `BRONZE_ERP.BDGT_ACMSLT_LEDGER` **258행** ·
  `INCOME_EXPS_DIV_NM` **전건 「지출」**(1종 · 258/258) · `MOK_NM` **15종**
- 모금 축 실측(`MOK_NM` · 편성 `YEAR_BDGT_TOT_AMT` / 집행 `EXEC_TOT_AMT`):

| `MOK_NM` | 행 | 편성 | 집행 |
|---|---:|---:|---:|
| 온라인모금 | 57 | 50,716,939,600 | 54,553,600,496 |
| 미디어모금 | 64 | 41,856,946,159 | 37,457,213,517 |
| 기관홍보사업 | 46 | 1,178,936,568 | 195,840,457 |

⇒ 🟢 **원천 부재가 아니다.** 모금비용 2종 **121행 · 집행 92,010,814,013** 이 지출로 실재한다.
🔴 잔여는 **「어느 `MOK_NM` 을 `FUNDRAISING_COST` 로 볼 것인가」 = 분류 확정**이고 이는 **결정 사안**이다.
⚠️ 「기관홍보사업」을 모금비용에 넣을지가 갈린다(홍보 ↔ 모금은 다른 축일 수 있다) ⇒ 현업 1문항으로 좁혀진다.

### E3-2. `E-4` `FACT_BUDGET.AD_COST` — 🔴 「외부 원천 입고 대기」가 거짓

- GOLD 채움 = `AD_COST` non-NULL **0** · non-zero **0** (분모 1,332행)
- 🟢 원천 실재 **3종**(`BRONZE_AGENCY`):

| 원천 컬럼 | 행 | non-NULL | non-zero | 합계(KRW) |
|---|---:|---:|---:|---:|
| `DGT_AD_CMPGN_DTLS.GA_AD_COST` | 208,373 | 208,368 | 196,640 | 31,035,346,288.36 |
| `REBRDC_AD_CMPGN_DTLS.BRDC_SCHDL_COST` | 2,149 | 2,104 | 2,069 | 16,074,184,814 |
| `VIDEO_AD_CMPGN_DTLS.ACTL_PUR_AD_COST_KRW` | 37,674 | 36,311 | 36,150 | 6,507,350,560 |

⇒ 🟢 합계 **53,616,881,662** · 🔴 잔여는 **집계 축 확정**(디지털·영상·재방송 3종을 어떤 grain 으로
`FACT_BUDGET` 에 귀속하는가 · `DEC-8` 위성 분리와 `DEC-13` measure 노출 규칙에 걸린다) = **설계 결정**.
⚠️ `GA_AD_COST` 는 `FLOAT` 이고 나머지는 `NUMBER`/`FLOAT` 이다 ⇒ 합산 전 타입·통화 축을 명시해야 한다.

### E3-3. `E-6` `FACT_TARGET_PROJECT` — 🟢 외부의존 참

- `GN_DW.GOLD.FACT_TARGET_PROJECT` **0행**
- 원천 후보 전수 검색(`BRONZE%` · `%COST%`·`%BUDGET%`·`%TARGET%`·`%FDRM%`) = **목표(TARGET) 테이블 0건**
  (적중 10건은 전부 `BRONZE_CRM.TM_MM_FDRM_*` 모금회원 계열이며 목표 테이블이 아니다)
⇒ 🟢 **입고 대기 유지**(원천 = CRM 신규 목표 테이블 · `E-6`).

### E3-4. `G-5` GA4 전기간 샤드 — 🟢 현 계정 미입고(참) · 🔴 문서40 기재가 라이브와 상충

🔴🔴 **[2026-09-17 O170-B 철회] 이 소절의 「상충」 판정은 틀렸다 — 개발계 설계다. 정정 = 아래 §E7-1.**

- 라이브 = `GN_DW.INFORMATION_SCHEMA.SCHEMATA` 에 **`BRONZE_BIGQUERY` 실재**하나
  `INFORMATION_SCHEMA.TABLES` 기준 **테이블 0건 · 0행**
- 🔴 그런데 `20_issue/40_입고대기_원천의존.md:20` 은
  *"~~🔴 G-5~~ → 🟢 **해소** … 🟢 **전기간 입고·적재 완료 (2026-08-18 O86)** … A 실측(과거 계정) 911테이블 / 285,676,588행"*
  로 적는다 ⇒ **그 「해소」는 과거 계정 기재이고 현 계정 `RY53492` 에는 객체가 없다.**
- 🟢 그 줄이 스스로 「재는 방법」을 적어 둔 것이 판정을 가능하게 했다
  (*"재는 방법 = `SELECT TABLE_SCHEMA, TABLE_NAME, ROW_COUNT FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA LIKE 'BRONZE_BIGQUERY%'`"*)
- ⚠️ 하류는 **적재돼 있다** = `SILVER.BIGQUERY_REFINED_DATA` **10,337,767행**(기준선 무변) ·
  `SILVER.BIGQUERY_EVENT` **1,066,880** · `BIGQUERY_BASIC` **1,066,880** · `BIGQUERY_EVENT_DIM` 9,116 ·
  `BIGQUERY_IDENTITY` 4,926 · `BIGQUERY_TRAFFIC_SOURCE` 1,391 · `BIGQUERY_DEVICE` 301
  ⇒ 🔴 **BRONZE 0행인데 SILVER 가 1,000만행이다** — `D11`(BRD 외부 로더) 축과 같은 사실이고,
  이는 「dbt 계보 밖에서 적재됐다」는 뜻이다. `G-5` 를 「해소」로 읽으면 이 괴리가 감춰진다.

### E3-5. `D10` 종합

| 잔여 | 종전 기재 | 라이브 실측 | 소관 정정 |
|---|---|---|---|
| `E-1` `FUNDRAISING_COST` | 외부 원천 입고 | 🔴 **거짓** — ERP 지출 원장 258행 실재 · 모금 2종 121행 | **결정**(분류 확정) |
| `E-4` `AD_COST` | 외부 원천 입고 | 🔴 **거짓** — AGENCY 3종 실재 · 합 536.2억 | **설계 결정**(집계 축) |
| `E-6` `FACT_TARGET_PROJECT` | 외부 원천 입고 | 🟢 참 — GOLD 0행 · 원천 0건 | 외부 유지 |
| `G-5` GA4 | 외부 원천 입고 | 🟢 참(현 계정 0건) · 🔴 문서40 은 「해소」 | 외부 유지 + 문서40 정정 |

⇒ 🟢 **「잔여 4 전부 외부의존」은 2/4 만 참이다.**
🟢 판정식 = **「외부 원천 입고 대기」는 원천 스캔으로만 단정한다** — GOLD 슬롯이 NULL 인 것은
원천 부재의 증거가 아니다(**미배선**과 **원천 부재**는 다른 사건이며, 이번에 2건이 미배선이었다).
🔴 이것이 `J3`(「열려 있다」는 기재다)의 **원천 축 판본**이다.

---

## §E4. `D6` — 버킷 6군 전수 재실측

### E4-1. 🔴🔴 착수 전 판정 — 「A18·C11·D14·E4·F8 = 55컬럼」은 **지금 셀 수 없는 집합이다**

근거 = `50_dbt_파이프라인_미결조치-013.md:126`~`:133` 버킷표는 **2026-08-06 O43 판본**이고
그 분모는 **386** 이다(`:117`). 라이브 분모는 **598** 이다(`_o167_evidence.md:68`).
🔴 **버킷표는 건수만 있고 컬럼 배정 목록이 없다.** 목록 좌표(`:172`)는
*"`30_output_share/_archive/20260806/02_원천결손_Gap분석.md` §부록"* 이고 `:173`~`:175` 가 스스로
**아카이브 이관 · 분모가 현행과 다름(105/411 vs 108/492)**을 적는다.

⇒ 🟢 **판정 = 「55컬럼」은 낡은 분모의 산물이다.** `A18`·`C11`·`D14`·`E4`·`F8` 의 **컬럼이 어느 것인지
정본에 열거된 적이 없으므로** 그 55건을 「전수 재실측」하는 것은 착수 불가다.
🟢 대신 **라이브 미주입 전건에 버킷을 새로 배정**하는 것이 이 항목의 실제 잔여 작업이다.
🔴 이것이 `J2`(「내가 본 그릇이 그것을 담는 그릇인가」)의 버킷 축 판본이다 —
**건수 표는 집합을 담는 그릇이 아니다.**

### E4-2. 라이브 전수 재측정 (판정식 = O43/O167 판본 그대로)

도구 = `scripts/census_columns.py`(무인자 재현) + `scripts/_o170_gold_unfilled.py`.
판정식 = `nonzero == 0` · 감사컬럼 `DW_*` 제외 · 타입별
(수치 `<> 0` · BOOLEAN `IS TRUE` · TEXT `<> ''` · 그 외 `IS NOT NULL`).

| 구분 | O170 (2026-09-17) | O167 (2026-09-16) | O43 (2026-08-06) |
|---|---:|---:|---:|
| GOLD 기본테이블 | **37** | 37 | — |
| DATA 컬럼(`DW_*` 제외) | **598** | 598 | 386 |
| 값 주입됨 | **504** | 503 | 280 |
| 전건 `0` | **47** | 59 | 62 |
| 전건 `NULL` | **39** | 28 | 36 |
| 0행 테이블(`FACT_TARGET_PROJECT`) | **8** | 8 | 8 |
| **미주입 합계** | **94** | 95 | 106 |
| 미주입 비중 | **15.7%** | 15.9% | 27.5% |

🔴 **총계는 1건 차이인데 내부 분해는 23건이 움직였다**(전건0 59→47 · 전건NULL 28→39).
🟢 원인은 회귀가 아니다 — 모델이 `CAST(NULL AS …)` 로 바뀌면 같은 컬럼이 **0 축에서 NULL 축으로 이동**한다
(`O169` 가 `ACTIVE_CUM_*` 에서 이미 규명한 축 · 문서50 `-013.md:154`~`:157`).
🔴 **따라서 「전건0 N건」·「전건NULL N건」은 진척 지표가 아니다.** 진척은 아래 집합 차이로만 읽는다.

### E4-3. 집합 차이 (`㉢` 판정식 — 총계가 아니라 집합으로 증명)

도구 = `scripts/_o170_unfilled_diff.py`(O167 열거 95건을 **원문 전사** 후 `assert len == 95`).
직전 집합 = O167 열거 **95** + O168 정정 **1**(`DIM_DATE.IS_HOLIDAY` 열거 누락) = **96**.

| 축 | 건수 | 내역 |
|---|---:|---|
| 해소(직전 有 · 현재 無) | **2** | 🟢 `FACT_MEMBER_EVENT.UNPAID_STOP_CNT` · `_MEMBERS`(`DEC-54` 배선) |
| 신규(현재 有 · 직전 無) | **0** | — |
| 누락(배정 밖) | **0** | — |

⇒ 🟢🟢 **96 → 94 · 해소 2 · 신규 0 · 회귀 0.** `O167` 열거와 `O168` 정정이 **전건 정확**했음이 확증됐다.
🟢 이번에는 「이미 닫혀 있던 것」이 나오지 않았다 — `J3` 를 적용했고 **결과가 음성**이었다.
🔴 **음성 결과도 실측이다**(적발이 없다는 것이 실측을 생략해도 된다는 뜻이 아니다).

### E4-4. 버킷 재배정 — 근거 좌표가 실재하는 것만 배정한다

🔴 **추론으로 배정하지 않는다**(`R2-3`). 정본 좌표가 있는 것만 배정하고 나머지는 **미배정**으로 센다.

| 버킷 | 건 | 컬럼 | 근거 좌표 |
|---|---:|---|---|
| **A** 원천 부재 | 1 | `DIM_DATE.IS_HOLIDAY` | `40_입고대기_원천의존.md:28`(`HOL-1`) |
| **A/F** 0행 테이블 | 8 | `FACT_TARGET_PROJECT` 8컬럼 | 문서50 `-014.md:186`(`E-6` 데이터 부재) |
| **B** 내부 구현(원천 실재) | 2 | `FACT_BUDGET.FUNDRAISING_COST` · `AD_COST` | 🔴 **본 근거철 §E3-1·§E3-2 (O170 신규 이동)** |
| **C** 정의 부재 | 2 | `FMM.ACTIVE_CUM_CNT` · `ACTIVE_CUM_MEMBERS` | 문서50 `-013.md:149`~`:153`(`D12`) |
| **D** 연결키(`Q10` 캠페인) | 6 | `CAMPAIGN_SK` = `FACT_AD_PERFORMANCE`·`FACT_BIGQUERY_BEHAVIOR`·`FACT_BUDGET`·`FACT_BUDGET_YEARLY`·`FACT_EVENT_ATTENDANCE`·`FMM` | 문서50 `-013.md:131` · `-015.md:12`(`Q10`) |
| **D** 연결키(`Q10` 캠페인) | 1 | `FACT_MESSAGE_DISPATCH.CAMPAIGN_SK` | 〃 |
| **D** 연결키(소재 부분키) | 1 | `FACT_AD_PERFORMANCE.AD_CREATIVE_SK` | 문서50 `-015.md:36` |
| **D** 연결키(`O8` 후원사업) | 3 | `SPONSORSHIP_SK` = `FACT_BUDGET`·`FACT_BUDGET_YEARLY`·`FACT_EVENT_ATTENDANCE` | 문서50 `-013.md:131`(🔴 `DEC-41` 은 후원사업 **일부만** 닫았다) |
| **E** `CONF-2` 「(건)」 | 3 | `FMM.INCREASE_CNT` · `INCREASE_MEMBERS` · `DECREASE_CNT` | 문서50 `-013.md:167`(모델 `FACT_MEMBER_MONTHLY.sql:26` 혼용 금지) |
| **E** `CONF-2` 미납 | 3 | `FMM.UNPAID_CNT` · `CAMPAIGN_UNPAID_CNT` · `STATUS_UNPAID_CNT` | 문서50 `-013.md:161` |
| **E** `CONF-4` 상위조직 | 6 | `DIM_ORG.CORP`·`DIVISION`·`TEAM` · `FACT_MEMBER_DEV_ACHIEVEMENT.ORG_CORP`·`ORG_DIVISION`·`ORG_TEAM` | 문서50 `-013.md:132` |
| **E** `DEC-33` 코호트 | 8 | `FMD.D5_LETTER_PART_MEMBERS`·`_CNT` · `D5_GIFT_PART_MEMBERS`·`_CNT` · `D5_INCREASE_PART_MEMBERS`·`_CNT` · `D5_STOP_MEMBERS`·`_CNT` | 문서50 `-013.md:166` · 문서20 §L-1 |
| **C** `O59-P-1` | 1 | `FMD.SEND_STATUS2` | 문서50 `-012.md:90` |
| **소계 배정** | **45** | | |
| 🟠 **미배정(차단 근거 미확인)** | **49** | 아래 E4-5 | — |

🔴 **배정률 45/94 = 47.9%.** 🟢 **미배정 49건을 「배정 0」으로 적는다** —
종전 버킷표가 *"전량 배정 · 누락 0"*(문서50 `-013.md:124`)이라고 적은 것은 **분모 386 시절의 주장**이고
현 분모 598 에서는 **성립하지 않는다.**
🔴 **이것이 이 항목의 진짜 잔여다** — 「55컬럼 재실측」이 아니라 **49컬럼 차단 근거 판정**이다.

### E4-5. 미배정 49건 (전량 열거 · 다음 세션의 착수 단위)

| 테이블 | 건 | 컬럼 |
|---|---:|---|
| `FACT_MEMBER_MONTHLY` | 14 | `PAYMENT_SK`·`CHURN_CNT`·`INBOUND_CALL_CNT`·`TS_CALL_CNT`·`SPONSOR_MONTHS`·`SPONSOR_YEARS`·`PAID_MONTHS`·`DEV_TYPE`·`NEW_FLAG`·`INCREASE_FLAG`·`REDONATE_FLAG`·`NEW_EXISTING_FLAG`·`JOIN_DATE`·`STOP_DATE` |
| `FACT_MEMBER_MONTHLY`(밴드) | 4 | `AMOUNT_BAND1`·`AMOUNT_BAND2`·`PERIOD_BAND1`·`PERIOD_BAND2` |
| `FACT_MESSAGE_DISPATCH` | 8 | `LETTER_PART_MEMBERS`·`LETTER_PART_CNT`·`GIFT_PART_MEMBERS`·`GIFT_PART_AMT`·`SERVICE_MEMBERS`·`SERVICE_CNT`·`MAIL_RECEIVE_FLAG`·`MEMBER_STOP_FLAG` |
| `FACT_EVENT_ATTENDANCE` | 6 | `CONFIRM_CNT`·`PARTICIPATION_TIMES`·`WAIT_TIMES`·`ABSENT_TIMES`·`CUM_APPLY_TIMES`·`SELF_PART_FLAG` |
| `FACT_BUDGET` | 2 | `EXEC_BUDGET_EST`·`ORG_SK` |
| `FACT_BUDGET_YEARLY` | 3 | `CHN_BUDGET_YEAR`·`ADJ_BUDGET_YEAR`·`ORG_SK` |
| `DIM_AD_CREATIVE` | 2 | `PLATFORM_TYPE`·`TARGET_GROUP` |
| `DIM_MEMBER_IDENTITY` | 2 | `MEMNUM`·`CHILD_CODE` |
| `FACT_BIGQUERY_BEHAVIOR` | 2 | `AVG_SESSION_DURATION`·`BOUNCE_RATE` |
| `FACT_AD_BROADCAST`·`FACT_AD_DIGITAL` | 2 | `CONV_CALL_CNT` · `MEDIA_POTENTIAL_CUST_CNT` |
| `DIM_CAMPAIGN`·`DIM_EVENT`·`DIM_PAYMENT`·`FACT_MEMBER_EVENT` | 4 | `ORG_SK` · `APPLY_CHANNEL` · `FEE_TYPE` · `NEW_EXISTING_FLAG` |

📏 **합계 검산** = 14 + 4 + 8 + 6 + 2 + 3 + 2 + 2 + 2 + 2 + 4 = **49** · 배정 45 + 미배정 49 = **94** ✅
🔴 `D9`(한 문장에 두 분모) 재발을 막기 위해 **검산식을 표 아래에 명시**한다.

🟢 **다음 세션의 판정식** = 미배정 49건 각각에 대해
㉠ 원천에 그 항목이 있는가(스캔) ㉡ 산출 규칙이 확정됐는가 ㉢ 연결키가 있는가 를 **컬럼 단위로** 묻는다.
🔴 **묶음 이름(「서신·선물금 8종」류)으로 세지 마라** — `O168` 이 그것으로 18종 중 16종을 틀렸다.

---

## §E5. `DEC-35` 1~2단계 — 정보 8건 실측

도구 = `python3 scripts/sv_code_label_gate.py`(rc=0 · 위반 0 · **정보 8건**).

### E5-1. 🔴 집합 정정 — 「미등재 6」이 아니라 **7** 이다

게이트 출력 실측(SV 17종 · 컬럼 배킹 차원 244개):

| # | SV.컬럼 | 게이트 판정 |
|---|---|---|
| 1 | `SV_ML_DVLP_FORECAST.SERIES_CD` | `LABEL_PAIRS` 미등재 |
| 2 | `SV_ML_FEE_FORECAST.CMPGN_CTGR_CD` | 〃 |
| 3 | `SV_ML_LTV_FORECAST.SERIES_CD` | 〃 |
| 4 | `SV_ML_LTV_SCORE.SERIES_CD` | 〃 |
| 5 | `SV_ML_MEMBER_RISK.CPR_DIV_CD` | 〃 |
| 6 | `SV_ML_SPONSOR_RISK.CMPGN_CD` | 〃 |
| 7 | `SV_ML_SPONSOR_RISK.UPPER_CMPGN_CD` | 〃 |
| 8 | `SV_SERVICE.SUBTYPE` | 라벨 컬럼 부재 확인됨 |

⇒ 🟢 **총계 8 은 맞고 내부 분해가 틀렸다**(미등재 **7** + 라벨부재 1). 인수인계 기재 「미등재 6」은 **1건 과소**.
🔴 `D9`·`D7` 과 **같은 유형의 결함**이다 — 총계는 맞는데 분해가 틀린다.

### E5-2. 🔴🔴 처방 판정 — 7건 중 **6건은 `LABEL_PAIRS` 등재 대상이 아니다**

라이브 종수 실측(`SERVING` 뷰 직접 조회):

| SV.컬럼 | 행 | 코드 종수 | 라벨 컬럼 | 처방 |
|---|---:|---:|---|---|
| `SV_ML_MEMBER_RISK.CPR_DIV_CD` | 74,949 | **2**(`I`·`S`) | 🔴 뷰에 없음 · 🟢 `GOLD.DIM_CAMPAIGN.CPR_DIV_NM` 실재 | 🟢 **등재 가능** |
| `SV_ML_FEE_FORECAST.CMPGN_CTGR_CD` | 660 | **55** | 🔴 없음 | 🔴 열거 부적절 |
| `SV_ML_LTV_SCORE.SERIES_CD` | — | **~110** | 🟢 `SERIES_NAME` 뷰에 실재 | 🟢 **라벨 차원 노출** |
| `SV_ML_LTV_FORECAST.SERIES_CD` | — | **~110** | 🟢 `SERIES_NAME` 실재 | 〃 |
| `SV_ML_DVLP_FORECAST.SERIES_CD` | — | **~170** | 🟢 `SERIES_NAME` 실재 | 〃 |
| `SV_ML_SPONSOR_RISK.CMPGN_CD` | 829,609 | — | 🟢 `CMPGN_NAME` 실재 | 〃 |
| `SV_ML_SPONSOR_RISK.UPPER_CMPGN_CD` | 829,609 | **994** | 🔴 없음 | 🔴 열거 부적절 |

🔴🔴 **판정 = 「정보 8건을 등재해 닫는다」를 그대로 집행하면 결함을 만든다.**
근거 3중:
㉠ `UPPER_CMPGN_CD` **994종**·`SERIES_CD` **~170종** 을 COMMENT 에 열거하는 것은 물리적으로도
  `R2-6`(COMMENT 에는 코드값만 · 수치·경위 금지) 취지에서도 성립하지 않는다.
㉡ 열거하면 **데이터가 늘 때마다 stale** 이 되고, 그 stale 을 **`sv_code_label_gate` 자신이 잡는다**
  ⇒ 🔴 **감시 분모에 든 곳에 그 값을 쓰는 행위**다(`J8` 자기참조 · O169 자기결함 5건 중 3건이 이것이다).
㉢ 5건은 **라벨 컬럼이 이미 뷰에 실재**한다(`SERIES_NAME`·`CMPGN_NAME`) ⇒ 올바른 처방은
  COMMENT 열거가 아니라 **SV 에 라벨 차원을 노출**하는 것이다(게이트가 요구하는 것도 라벨축 노출이다).

⇒ 🟢 **집행 가능분 = 1건**(`CPR_DIV_CD` · 2종) · 🟢 **라벨 차원 노출 = 5건** · 🔴 **열거 부적절 = 2건**.
🔴 **이 세션은 라이브 SV 를 고치지 않았다** — 처방이 3갈래로 갈렸고 그중 5건은 **SV 정의 변경**이므로
「승인 불요」 범위를 넘는다. 🟢 **집합·처방을 확정한 것이 이 항목의 산출물이다.**

### E5-3. `CPR_DIV_CD` 라벨 쌍 (등재 시 쓸 값 · 라이브 실측)

| 코드 | 라벨(`GOLD.DIM_CAMPAIGN.CPR_DIV_NM`) | `DIM_CAMPAIGN` 행 | `ML_MEMBER_RISK_V` 행 |
|---|---|---:|---:|
| `A` | 통합 | 27,319 | **0**(부재) |
| `I` | 사단 | 7,772 | 8,727 |
| `S` | 사복 | 207 | 66,222 |
| `NULL` | — | 1,471 | 0 |

🔴 **`A`(통합)가 `ML_MEMBER_RISK_V` 에 없다** ⇒ 등재할 때 「이 SV 의 현 코드값은 `I`·`S` 2종」으로 쓰면
원천이 3종이라는 사실이 감춰지고, 「3종」으로 쓰면 게이트의 **코드값 실재 축**에 걸린다.
🟢 처방 = **코드값은 `I`·`S` 만 열거**하고 `A` 의 부재 사유는 **DDL `--` 주석과 이 근거철**에 둔다(`R2-6`).

---

## §E6. 신규 관측 (이 세션에 부수 적발 · 백로그 등재 대상)

### E6-1. 🔴 예산 팩트 규모가 문서 기재의 1/20~1/38 이다

| 객체 | 라이브(2026-09-17) | 문서 기재 | 좌표 |
|---|---:|---:|---|
| `GOLD.FACT_BUDGET` | **1,332** | 24,480 | 문서50 `-015.md:32` |
| `GOLD.FACT_BUDGET_YEARLY` | **111** | 4,298 | 문서50 `-015.md:8` |
| `FACT_BUDGET_YEARLY.PLAN_BUDGET_YEAR` non-NULL | **111** | 4,255 | 〃 |
| `SILVER.ERP_BUDGET` | 3,096 | — | — |
| `SILVER.ERP_BUDGET_YEARLY` | 258 | — | — |
| `BRONZE_ERP.BDGT_ACMSLT_LEDGER` | **258** | — | — |

🟢 계보는 정합하다 — `BRONZE` 258 → `SILVER.ERP_BUDGET_YEARLY` 258 → `FACT_BUDGET_YEARLY` 111 →
`FACT_BUDGET` 1,332(= 111 × 12 월 전개). ⇒ **모델 결함이 아니라 원천 규모가 다르다.**
🔴 **문서의 24,480·4,298·4,255 는 과거 계정 판본이고 현 계정에서 stale 이다.**
🔴 `R2-8-4-d` 축 — 문서의 수치를 근거로 「라이브에 있다」고 쓰지 마라.
🟢 처방 = 그 좌표에 **수치를 다시 박지 말고**(`R3-9 ㉦`) 「재는 방법」을 적는다.

### E6-2. 🔴 `G-5` 「해소」 기재가 현 계정과 상충 (§E3-4 재수록)

🔴🔴 **[2026-09-17 O170-B 철회] 이 소절 전체가 틀렸다 — 개발계 설계다. 정정 = 아래 §E7-1.**

`40_입고대기_원천의존.md:20` 은 `G-5` 를 🟢 **해소**(911테이블 / 285,676,588행)로 적지만
현 계정 `BRONZE_BIGQUERY` 는 **테이블 0건**이다. 반면 `SILVER.BIGQUERY_REFINED_DATA` 는 **10,337,767행**.
⇒ 🔴 **BRONZE 0 · SILVER 1,000만** = `D11`(외부 로더) 과 같은 사실이고, 「해소」 표기가 이 괴리를 감춘다.

---

## §E7. 🔴🔴 정정 (2026-09-17 사용자 지시 + §O171 반영 · O170-B)

🔴 **이 절은 §E3-4 · §E6-2 · §E4-2 · §E4-3 을 정정한다.** 원문은 **지우지 않았다**(`R2-8` 축) —
아래 정정과 **함께 읽어야** 무엇을 왜 틀렸는지가 남는다.

### E7-1. 🔴🔴 판정 철회 — `BRONZE_BIGQUERY` 「테이블 0건」은 결함이 아니라 **개발계 설계**다

**사용자 정본 진술(2026-09-17)** = *"이건 개발account야. BRONZE_BIGQUERY 스키마는 껍데기만 있고,
실제 운영에서는 BRONZE_BIGQUERY 스키마에 1일마다 테이블이 증가하고 있어. 그런데 silver 스키마의
bigquery_refined_data가 BRONZE_BIGQUERY 데이터를 취합, 1차 정제한 테이블이어서, 이 개발계에는
bigquery_refined_data를 가져오고 있어. 단지, bigquery_refined_data 전체가 너무 커서 전체 데이터 중
랜덤하게 추출해 가져오고 있다."*

⇒ 🔴 **내가 §E3-4·§E6-2 에서 쓴 두 문장은 철회한다**:
- ✗ *"`40_입고대기_원천의존.md:20` 의 「🟢 해소」는 **현 계정과 상충한다**"*
- ✗ *"**BRONZE 0 · SILVER 1,000만** 괴리가 있다(`D11` 외부 로더 축)"*

🟢 **올바른 판정** = 개발계에서 `BRONZE_BIGQUERY` 가 비어 있는 것은 **정상 상태**다.
`SILVER.BIGQUERY_REFINED_DATA` 는 **운영계 산출물을 랜덤 표본으로 이관**한 것이므로
㉠ BRONZE↔SILVER 행수 대조는 **이 계정에서 성립하지 않는 판정식**이고
㉡ 「외부 로더」가 아니라 **설계된 이관 경로**이며
㉢ `G-5`(GA4 전기간 입고)는 **운영계 축의 항목**이라 개발계 조회로 판정할 대상이 아니다.

🔴🔴 **내 결함의 형태** = 나는 `R2-8-4-d`(문서의 완료 판정으로 라이브 실재를 단정하지 마라)를 **적용**했는데,
그 역방향인 **「라이브 부재로 문서의 판정을 반증하지 마라」**를 **적용하지 않았다.**
라이브가 비어 있는 이유가 **환경 등급(개발/운영)** 일 수 있는데 그 축을 묻지 않았다.
🟢 **판정식(신설) = 라이브 부재를 근거로 판정을 뒤집기 전에 「이 계정이 그 판정의 분모인가」를 물어라.**
개발계는 운영계의 **부분집합 표본**이므로 **부재가 부재를 증명하지 않는다.**
🔴 이것은 `J2`(「내가 본 그릇이 그것을 담는 그릇인가」)의 **환경 축 판본**이고,
이 워크스페이스에서 `J2` **6차 재발**이다(O165 D5 → O167 4차 → O171 5차 → 이번 6차).

⚠️ **살아남는 것** = `E-6`(`FACT_TARGET_PROJECT` 0행)은 개발계 표본 문제가 아니다 —
GOLD 모델이 스켈레톤이고 원천 목표 테이블이 **CRM 신규 입고 대기**이므로 §E3-3 판정은 **유효**하다.
⚠️ `E-1`·`E-4` 판정(§E3-1·§E3-2)도 **유효**하다 — 원천이 ERP·AGENCY 이고 둘 다 이 계정에 실재한다.
⇒ 🟢 **§E3-5 의 「2/4 만 참」은 유지된다.** 다만 `G-5` 의 사유가 「현 계정 미입고」에서
**「운영계 소관 · 개발계로 판정 불가」**로 바뀐다(판정 결과는 같고 **근거가 다르다**).

### E7-2. 원천 재적재로 갱신된 수치 (사용자 = *"데이터를 가져와서 다시 채워넣었어"*)

| 객체·축 | O170 초판 | **재측정(2026-09-17)** | 비고 |
|---|---:|---:|---|
| `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS` | 208,373 | **203,138** | §O171 §13 과 일치(−32,434 회귀는 열려 있다) |
| `DGT.GA_AD_COST` 합 | 31,035,346,288 | **42,439,374,553** | 행은 줄고 금액은 늘었다 |
| `REBRDC.BRDC_SCHDL_COST` 합 | 16,074,184,814 | **16,099,762,004** | 2,149행 무변 |
| `VIDEO.ACTL_PUR_AD_COST_KRW` 합 | 6,507,350,560 | **6,507,350,560** | 무변 |
| **`E-4` 광고비 3종 합** | 53,616,881,662 | **65,046,487,117** | — |
| `SILVER.BIGQUERY_REFINED_DATA` | 10,337,767 | **10,770,355** | 🟢 랜덤 표본 재추출(§E7-1) |
| `SILVER.IDENTITY_MEMBER_XREF` | 4,926 | **3,142** | 〃 표본 변동의 하류 |
| `BRONZE_ERP.BDGT_ACMSLT_LEDGER` | 258 | **258** | 무변 · `VENDOR` 컬럼 추가 확인 |
| `E-1` 모금 2종 집행 합 | 92,010,814,013 | **92,010,814,013** | 무변 |
| `GOLD.FACT_BUDGET` / `_YEARLY` / `FACT_TARGET_PROJECT` | 1,332 / 111 / 0 | **1,332 / 111 / 0** | 무변 |

🔴 **`BIGQUERY_REFINED_DATA` 를 「+432,588 증가」로 읽지 마라** — 랜덤 표본이므로 **집합이 다르다.**
🟢 판정식 = **표본 이관 객체는 행수 증감이 진척·회귀를 뜻하지 않는다.**

### E7-3. `D6` 재측정 — 미주입 **94 → 95** · 🔴 신규 회귀 1건

`census_columns.py` 재실행(원천 갱신 후) 결과:

| 구분 | O170 초판 | **재측정** |
|---|---:|---:|
| DATA 컬럼(`DW_*` 제외) | 598 | **598** |
| 값 주입됨 | 504 | **503** |
| 전건 `0` | 47 | **47** |
| 전건 `NULL` | 39 | **40** |
| 0행 테이블 | 8 | **8** |
| **미주입 합계** | 94 | **95** |
| 비중 | 15.7% | **15.9%** |

**집합 차이(직전 96 대비)** = 해소 **2**(`FME.UNPAID_STOP_CNT`·`_MEMBERS`) · 신규 **1** ·
🟢 이 신규 1건이 총계를 94 에서 95 로 올린 전부다(다른 이동 0).

🔴🔴 **신규 = `FACT_AD_DIGITAL.CRM_DEV_CNT` 전건 NULL** — **원천 회귀이고 배선 결함이 아니다**:

| 축 | 실측 |
|---|---|
| `GOLD.FACT_AD_DIGITAL` | 203,138행 · `CRM_DEV_CNT` non-NULL **0** |
| 원천 `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS.CRM_DVLP_CNT` | non-NULL **0 / 203,138** · `SUM` **NULL** |
| 같은 테이블 대조군 `GA_CONV_MBER_CNT` | non-NULL **202,739** |
| 같은 테이블 대조군 `CONV_VU_CNT` | non-NULL **203,063** |

⇒ 🟢 **원천의 그 컬럼만 비었다**(이웃 2컬럼은 정상이므로 적재 전면 실패가 아니다).
🔴 `O167` 열거에 `FACT_AD_DIGITAL` 은 `MEDIA_POTENTIAL_CUST_CNT` **1건만** 있었다
⇒ **`CRM_DEV_CNT` 는 2026-09-16 시점에 채워져 있었다** ⇒ **회귀가 실증된다.**

🔴🔴 **이것은 §O171 §13~§14 가 규명한 회귀의 두 번째 증상이다.**
§O171 은 같은 원천 재적재에서 `AD_DATE` NULL **194,058(95.53%)** 과 행 감소 **−32,434** 를 적고
*"🟠 **원천 회귀 자체는 열려 있다** … **행 감소는 설명되지 않았다** ⇒ 🔴 **현업 확인 대상**"*
(`01_세션이력-071.md:227`~`:229`)으로 닫아 두었다. ⇒ 🟢 **그 열린 항목에 컬럼 1건을 추가 등재한다.**
🟢 판정식 = **한 원천 회귀는 한 컬럼에서 끝나지 않는다.** `AD_DATE` 를 폴백으로 메운 뒤
**같은 재적재가 건드린 다른 컬럼을 전수 다시 세라**(이번 census 가 그 역할을 했다).
🔴 현업 확인 문항에 **`CRM_DVLP_CNT` 공급 중단 여부**를 함께 넣어야 한다
(`AD_DATE`·행수와 **같은 재적재 사건**이므로 따로 묻지 말고 한 문항으로 묶는다).

⚠️ **버킷 재배정(§E4-4)에 미치는 영향** = 미배정이 49 → **50** 이다
(`FACT_AD_BROADCAST`·`FACT_AD_DIGITAL` 행이 2 → **3**: `CONV_CALL_CNT` · `MEDIA_POTENTIAL_CUST_CNT` ·
🆕 `CRM_DEV_CNT`). 📏 검산 = 배정 45 + 미배정 50 = **95** ✅
🟢 다만 `CRM_DEV_CNT` 는 **차단 근거가 이미 규명됐다**(원천 회귀 · 현업 확인) ⇒
**버킷 A(원천 부재 · 단 「회귀에 의한 일시 부재」)** 로 배정 가능하고, 그러면 배정 46 · 미배정 49 다.
🔴 **「부재」와 「회귀에 의한 부재」는 처방이 다르다**(전자는 입고 요청 · 후자는 **원상 복구 요청**)
⇒ 버킷 A 를 그대로 쓰지 말고 **A-R(회귀)** 로 갈라 적을 것을 제안한다(등재는 결정 사안).

### E7-4. CRM 개편 라이브 반영 확인 (작업자 메시지 대조 · 전건 일치)

| 작업자 메시지 항목 | 라이브 실측 | 판정 |
|---|---|---|
| `BRONZE_CRM` 50 테이블 | **50** | 🟢 |
| 전 테이블 `_STDR_YM` 기준월 컬럼 생성 | **50 / 50** | 🟢 |
| 신규 4종(`TC_MKTNG_DTL_CD`·`SND_MEMBER_OPEN_LOG`·`SND_MEMBER_MAIL_LINK_LOG`·`TM_MM_FDRM_MBER_DT_DTLS`) | **4 실재** | 🟢 |
| 삭제 2종(`TM_CM_MKTNG_CMPGN_MNG`·`TM_CM_MKTNG_UTM`) | **0 잔존** | 🟢 |
| `TM_CM_CMPGN_MNG.MKTG_CHANNEL` 추가 | **실재** | 🟢 |
| `SND_MEMBER_LIST.OPEN_DT` 삭제 | **부재** | 🟢 |
| `BDGT_ACMSLT_LEDGER.VENDOR` 추가(2026-09-15) | **실재** · 값 4종(미디어모금 축에만) | 🟢 |
| `BRONZE_CRM2` 스키마(테스트 예정) | **부재** | 🟢 예정대로 |

🟢 **8축 전건 일치** ⇒ 원천 개편은 라이브에 반영돼 있다.
🔴 **다만 SILVER/GOLD 취급은 별건이다** — `_STDR_YM` 설계 판정은 §O171 §12 ②로 **열려 있다**
(`01_세션이력-071.md:171`~`:173` · *"미루면 CRM 계열 build 가 대신 적발한다"*).
⚠️ `VENDOR` 는 `MOK_NM='미디어모금'` **64행에만** 값이 있다(4종) ⇒
`E-1` 모금비용 분류를 확정할 때 **VENDOR 축이 미디어모금에만 있다는 비대칭**을 함께 봐야 한다.

---

## §E8. 승인 불요 4건 집행 결과 (O170-D)

### E8-1. ① 미배정 50컬럼 — 🟢 **판정 완결 · 미배정 0**

도구 = `scripts/_o170_unassigned_probe.py`. 판정 축 = 모델이 그 컬럼을 **어떻게 채우는가**
(리터럴 주입 ↔ 실산출식) + 실산출식이면 **원천 값을 라이브로 실측**.

| 버킷 | 건수 | 성격 | 소관 |
|---|---:|---|---|
| **B** 산출 로직 미구현(리터럴 주입) | **45** | `CAST(NULL AS …)` · `0` · `FALSE` 리터럴 | 🟢 **내부 구현 · 외부 의존 0** |
| **A-R** 원천 값 부재(배선 정상) | **5** | 모델은 원천 컬럼을 실제로 읽는데 **원천이 비어 있다** | 🔴 현업(복구·미보고 확인) |
| 미배정 | **0** | — | — |

📏 검산 = 45 + 5 = **50** ✅ · 종전 배정 45 + 이번 50 = **95**(라이브 미주입 전건) ✅

**버킷 A-R 5건 전수 (원천 라이브 실측)**

| GOLD 컬럼 | 모델이 읽는 원천 컬럼 | 원천 실측 |
|---|---|---|
| `FACT_AD_BROADCAST.CONV_CALL_CNT` | `VIDEO_AD_CMPGN_DTLS.CONV_CALL_CNT` | non-NULL **0 / 37,674** |
| `FACT_AD_DIGITAL.MEDIA_POTENTIAL_CUST_CNT` | `DGT_AD_CMPGN_DTLS.MEDIA_PTNT_CUST_CNT` | non-NULL **0 / 203,138** |
| `FACT_AD_DIGITAL.CRM_DEV_CNT` | `DGT_AD_CMPGN_DTLS.CRM_DVLP_CNT` | non-NULL **0 / 203,138** |
| `FACT_BUDGET_YEARLY.CHN_BUDGET_YEAR` | `BDGT_ACMSLT_LEDGER.CHN_BDGT_TOT_AMT` | non-NULL 258 · **non-zero 0** |
| `FACT_BUDGET_YEARLY.ADJ_BUDGET_YEAR` | `BDGT_ACMSLT_LEDGER.ADJ_BDGT_TOT_AMT` | non-NULL 258 · **non-zero 0** |

🔴🔴 **AGENCY 3건은 같은 재적재 사건이다** — §E7-3 은 `CRM_DVLP_CNT` **1건**만 적었으나
같은 원천의 `MEDIA_PTNT_CUST_CNT`(DGT) · `CONV_CALL_CNT`(VIDEO)도 **전건 NULL** 이다.
⇒ 🟢 **현업 문항을 3컬럼 + `AD_DATE` + 행 감소로 묶어라**(§O171 §14 와 한 사건).
🟢 판정식(강화) = **회귀는 한 컬럼에서 끝나지 않는다** — 이번에 1 → **3** 으로 늘었다.
⚠️ ERP 2건은 성격이 다르다 = 컬럼은 있고 **값이 전건 0** ⇒ 「추경·조정 미발생」일 수 있다
(회귀가 아닐 수 있다) ⇒ **현업 확인 문항을 AGENCY 축과 섞지 마라.**

🔴 **도구 자기결함 = 파서를 4회 고쳤다**(각 판이 다른 오류를 냈다):
㉠ 콤마까지 → `CAST(NULL AS NUMBER(18,2))` 에서 **`2))` 만** 잡음(오분류 1건)
㉡ 고정폭 200자 → **앞 컬럼까지 삼켜** B 44 → **33 회귀**
㉢ 줄 전체 → **한 줄에 여러 컬럼**인 파일에서 앞 항이 붙음
㉣ 최상위 콤마 + `$` 앵커 → 주석 제거 후 꼬리가 남아 **판정불가 급증**
🟢 최종 = **괄호 인식 최상위 콤마 분할 + 식별자 경계 앵커** ⇒ 44 B / 6 ? · 잔여 2건은 모델을 직접
읽어 확정(`EXEC_BUDGET_EST`·`PAYMENT_SK` 둘 다 리터럴 ⇒ B) ⇒ **45 / 5**.
🟢 **판정식(신설) = 파서의 원자는 「구분자」도 「줄」도 「고정폭」도 아니라 「문법」이다.**

### E8-2. ② `SV_ML_MEMBER_RISK.CPR_DIV_CD` — 🟢 등재 완료(라벨 부재 확인됨)

`sv_code_label_gate.LABEL_PAIRS` 에 `'CPR_DIV_CD': None` 으로 등재했다 ⇒ 상태가
「**미등재**(등재 필요)」에서 「**라벨 컬럼 부재 확인됨**」으로 전환(침묵이 아니라 판정이다).
🔴 라벨명을 적지 않은 이유 = 라벨 원천은 `GOLD.DIM_CAMPAIGN.CPR_DIV_NM` 에 실재하지만
**base 뷰 `SERVING.ML_MEMBER_RISK_V` 에 그 컬럼이 없다** ⇒ 적으면 **거짓 주장**이다(`R2-7-4`).
🔴 코드값 열거는 COMMENT 가 아니라 **§E5-3** 에 둔다(`A`=통합이 이 SV 에 부재하므로 「2종/3종」
어느 쪽을 써도 한쪽이 틀린다 · `R2-6`).
🟠 라벨을 노출하려면 그 뷰에 `CPR_DIV_NM` 을 추가해야 한다 = **SERVING 뷰 정의 변경** ⇒ 결정 대기.

⇒ 🟢 `DEC-35` 정보 8건 내부 분해 = **미등재 6 · 부재 확인됨 2**(종전 미등재 7 · 부재 1).

### E8-3. ③ `SEARCH_CONSOLE_DATA2` — 🟢 분모 편입 확인 · 🔴 §O171 판정 반증

- 분모 = `handoff_ddl_gate.TARGETS` 에 `BRONZE_GA4`·`BRONZE_GSC` **편입돼 있다**(O171) ·
  축1 테이블 집합 **0건** · 게이트 **rc=0** ⇒ 🟢 **편입 완료**.
- 🔴🔴 **그런데 §O171 §2 ③ 의 판정이 틀렸다** — *"원천 16번에 이 테이블을 채우는 적재 경로가 없다
  ⇒ 용도는 현업 확인 대상"*. 라이브 실측 = **941,298행 실적재**다.
- 🟢 **관계 확정** = `SEARCH_CONSOLE_DATA2` 는 `SEARCH_CONSOLE_DATA` 의 **2026-09-08 시점 사본**이다:

| 축 | V1 | V2 |
|---|---:|---:|
| 행 | 951,157 | 941,298 |
| 기간 | 2025-04-08 ~ **2026-09-14** | 2025-04-08 ~ **2026-09-08** |
| 일수 | 525 | 519 |
| 컬럼 구성 | 10 | **동일 10** |

겹치는 구간(`DATE <= 2026-09-08`) 대조 = 행수 **941,298 일치** · `EXCEPT` **양방향 0** ⇒ **완전 동일**.
⇒ 🟢 **신규 개념이 아니라 중복 사본**이다. 현업 문항이 「용도 미상」에서
**「중복 사본 확인 — 드롭 가능한가」**로 바뀐다(원천 소관 결정).
🟢 판정식 = **「적재 경로가 없다」는 파일 근거이고 「비어 있다」는 라이브 근거다** — 둘을 바꿔 쓰지 마라.

### E8-4. ④ 버킷 `A-R` 신설 — 🟢 **신설한다**(우리 소관 판정)

근거 = §E8-1 실측으로 **5건이 이 성격**이고, 종전 버킷 `A`(원천에 항목 자체가 없음)와 **처방이 다르다**:

| 버킷 | 뜻 | 처방 |
|---|---|---|
| **A** | 원천에 **항목(컬럼)이 없다** | **입고 요청**(신설) |
| 🆕 **A-R** | 항목은 있고 **값이 비었다**(회귀·미보고) | **원상 복구 요청** · 회귀면 재적재 범위 규명 |

🔴 **A 로 뭉개면 「없는 것을 만들어 달라」고 요청하게 된다** — 실제로는 있던 값이 사라진 것이다.
🟢 판정식 = **「부재」와 「회귀에 의한 부재」는 다른 사건이다**(`J3` 의 값 축 판본).
⚠️ **A-R 안에서도 두 갈래다** = AGENCY 3건(전건 NULL · 회귀 실증) ↔ ERP 2건(전건 0 · 미발생 가능)
⇒ 🔴 **한 문항으로 묻지 마라.**

### E8-5. 🔴🔴 신규 발견 — `SV_AD` 라이브 COMMENT 3컬럼이 stale (원천 재적재 하류)

`sv_code_label_gate` **rc=1 · blocking 위반 5건**(전부 `SV_AD` · `CPR_DIV_CD` 등재와 **무관**):

| 축 | 컬럼 | COMMENT 기재 | 라이브 실제 |
|---|---|---|---|
| 코드값 부재(0행 오답) | `SV_AD.AD_GROUP_NM` | `nf1834a` 열거 | 실제값에 **없다** |
| 코드값 부재(0행 오답) | `SV_AD.AD_TYPE_NM` | `하단DA` 열거 | 실제값에 **없다**(`BSA`·`CPM`·`DA`·`SA` 계열) |
| 종수 불일치 | `SV_AD.AD_GROUP_NM` | 「14종」 | **13종** |
| 종수 불일치 | `SV_AD.AD_TYPE_NM` | 「6종」 | **7종** |
| 종수 불일치 | `SV_AD.PAGE_TYPE` | 「2종」 | **81종** |

🔴 **원인은 내 작업이 아니다** — AGENCY 원천 재적재(§E7-2 · `DGT` 208,373 → 203,138)로 값 분포가
바뀌었고 SV COMMENT 가 따라가지 못했다. 🔴 **SV COMMENT 는 Analyst·Agent 컨텍스트**이므로
「0행 오답 경로」다(존재하지 않는 값을 LLM 에게 정답으로 제시한다 · O155 `D3` 와 같은 축).
🔴 **미시정 상태로 남겼다** — 라이브 SV 재배포는 O169 가 1차 실패한 경로이고(COMMENT 에 수치를 쓰면
`sv_unit_gate` 가 잡는다) **종수를 다시 박으면 다음 재적재에 또 stale 이 된다.**
🟢 **처방 후보 = 종수·열거를 COMMENT 에서 빼고 「재는 방법」을 적는다**(O167 이 GOLD 스키마 COMMENT 에서
한 것과 같은 해법) ⇒ 🔴 **설계 결정 사안**이므로 다음 세션에 상정한다.
🟢 판정식 = **원천 재적재는 COMMENT 를 stale 로 만든다** — 재적재 뒤에는 `sv_code_label_gate` 를 돌려라.

---

_Co-authored with CoCo_
