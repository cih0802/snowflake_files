<!-- SPLIT-CHUNK 02_상태상세_대시보드_갱신형.md | 007/008 | 허브 = 02_상태상세_대시보드_갱신형.md | 원문 882~1029행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### 🔴 [2026-08-20 O90] `07` 전량 실행 — 적재 완료 · `07` 결함 2건 · `dbt build` 차단 3건

> 🔒 **라벨** = `id_collision_gate.py --next O` 실측 = **정의 최대 89 · 참조 89 ⇒ O90**.
> 🟢 `P222`(참조축 과대)가 **17연속 끝에 해소**됐다 — 정의축과 참조축이 처음으로 일치했다.
> 🔴 **다만 `R1-4-3` 은 위반했다** — 게이트를 돌리기 전에 `07` 정본에 **`O89` 를 먼저 새겼고**
> 그것은 **타 세션 라벨 도용**이었다(O89 = 후원사업 분류 3계층 · `01_세션이력-031.md:67`).
> 본 세션에서 `O90` 으로 정정했고 `07:634` 에 철회 병기를 남겼다.
> 🔴 **이 절은 `R1-3-7-c` 소급 이행이다** — 판정을 먼저 하고 근거를 나중에 파일에 썼다.
> 조문은 「근거를 못 썼으면 판정하지 않는다」이고, 본 세션은 그 순서를 어겼다(확정위반 목록 참조).

#### ▣ 🟢 착수표 ② 완료 — `07` 1~7장 실측 전건

| 장 | 실측값 |
|---|---|
| 1장 REFRESH | **106폴더 / 817파일 / 8,100,866,944 B** (직전 `DIRECTORY()` = 348파일 stale) |
| 3-B 뷰 | **31컬럼 · VARIANT 11 · pos `4,11,12,14,15,16,18,22,23,24,29`** = 문서 기대치 일치 |
| 6장 (1) DRY_RUN | 19개월 / 106폴더 인식 · 선행 `/` 없음 확인 |
| 6장 (2) 스모크 202406 | **134파일 / 6,481,202행 / 59초 / errors 0** (`GN_DW_ETL_WH` Small) |
| 6장 (3) 본적재 | **19개월 / 679파일 / 26,125,089행 / errors 0** |
| **7장 (2-B)** | **0건** · 총계 스테이지 106/817 ↔ 적재 106/817 |
| 최종 | **817파일 / 32,718,672행 / 106폴더 / LOAD_TS 배치 817** |
| 7장 (3)(5) | 중복파일 **0** · `EVENT_DT` NULL **0** · `event_date`↔`EVENT_DT` 불일치 **0** |
| 컬럼 정렬 확증 | `ep_type=ARRAY` · `dev_type=OBJECT` **19개월 전수 동일**(VARCHAR 오염 0) |

· 🟢 **8장 부재의 대체 사전검사**(O88-B 가 주석 처리한 방어선) = 3개월 각 1파일 헤더를
  31컬럼 상위집합과 `STARTSWITH` 대조 ⇒ `events_20240601` **25** · `events_20250601` **30** ·
  `events_20260611` **30** · **전건 TRUE**. ⇒ 위치 기반 적재가 3개월 구간에서 성립한다.
· ⚠️ 스테이지에 **3개월 90폴더 외 16폴더**(비-6월 각 1일 · 2024-01~05 · 2024-07~12 · 2025-01~05)가
  있어 `CALL(NULL,NULL,FALSE)` 가 함께 적재했다. (2-B) 는 기준선이 스테이지 자신이라 **0건 정상**이고,
  `ga4_dt_ranges` 는 3개월뿐이므로 **SILVER 대상은 29,279,246행**이다(BRONZE ⊃ SILVER · 구조상 정상).

#### ▣ 🔴🔴 발견1 — `07` 5장 `PATTERN` 기재가 틀렸다 (O86 ④ 철회)

· 원문(철회 대상 · `07` 구 633~637행): *"PATTERN 은 FROM 경로(@stage/BRONZE_BIGQUERY/) 기준
  **상대 경로**에 매칭된다. 1판은 '스테이지 루트 기준'이라고 썼다. 틀렸다."*
· 🔴 **1판이 맞았고 O86 의 「정정」이 오히려 오류를 넣었다.** 실측 2개 —
  ㉠ 스모크 1회차 = `files=0 / rows=0 / errors=0` · `MIG_GA4_LOAD_LOG.STATUS='NO_FILES'` ·
     실행 후 `EVENTS` `COUNT(*)=0` (**0장 (4) 가 경고한 「조용한 실패」의 실제 발현**)
  ㉡ 같은 COPY 에서 `PATTERN` 만 `.*events_20240107/.*[.]csv[.]gz` 로 바꿔 재실행
     → **4파일 LOADED · 9,981+28,672+36,864+36,864 = 112,381행 · errors 0**
· 🟢 교차 확증 = 적재된 `SRC_FILE_NAME`(=`METADATA$FILENAME`) 이
  `mig_load_stage/BRONZE_BIGQUERY/events_20240107/…` 로 **스테이지명 접두까지** 포함한다.
· ⇒ 시정 = `'.*BRONZE_BIGQUERY/events_' || :v_ym || '[0-9]{2}/.*[.]csv[.]gz'`(스키마 명시로 오매칭 차단).
· 🟢 **교훈 = `P22` 계열** — 「안전장치라고 쓴 것이 실제로 작동하는지 실측하라」. 이번은 그 변형인
  **「정정이라고 쓴 것이 정말 정정인지 실측하라」** 다. O86 은 실측 근거를 적어 두었는데도 틀렸다.

#### ▣ 🔴 발견2 — 10장 1214행이 검증 수단을 스스로 파괴했다

· `ALTER STAGE … SET DIRECTORY = (ENABLE = FALSE);` 가 **10장의 유일한 비주석 문장**이었다.
· 이 문서는 「위에서 아래로 전량 실행」이 전제인데, 그러면 마지막에 디렉터리가 꺼지고
  **7장 (2-B) 가 `DIRECTORY()` 를 쓰므로 완료 게이트를 다시 돌릴 수 없다.**
· ⇒ 주석 처리 + 되살릴 조건 명시(`07` 1217~1224행). 본 세션에서는 실행하지 않았다.

#### ▣ 🔴🔴 발견3 — `dbt build` 차단 3건 (③ 미착수 사유)

| # | 블로커 | 실측 근거 |
|---|---|---|
| ㉠ | `GN_DW.dbt_test__audit` 스키마 부재 | `logs/dbt.log:455~460` = `create schema if not exists GN_DW.dbt_test__audit` → `003001 (42501)`. `create schema` 시도는 **로그 전체에서 이것 하나**(정규식 전수 카운트 1). `target/manifest.json` = 테스트 **343노드**가 이 스키마(`OPS` 5 · `GOLD` 49 · `SILVER` 40) |
| ㉡ | `GN_DW.SILVER` ENGINEER grant **전무** | `SHOW GRANTS ON SCHEMA GN_DW.SILVER` = `OWNERSHIP`(ADMIN) **1건뿐** · `SHOW FUTURE GRANTS IN SCHEMA GN_DW.SILVER` = **0건** ↔ GOLD 는 FUTURE **35건** 생존 |
| ㉢ | `GN_DW.OPS` `USAGE` 누락 | `SHOW GRANTS ON SCHEMA GN_DW.OPS` = `OWNERSHIP` + `CREATE TABLE` 뿐. `07 §D.6:327` 이 `CREATE TABLE` 만 부여 |

· 🔴 **㉠ 은 `07 §D.6` 의 전제를 반증한다.** 원문(`07:319~320`) = *"⇒ 스키마가 이미 존재하므로 dbt 는
  create schema 를 호출하지 않고, DB 레벨 CREATE SCHEMA 없이 **스키마 1개 범위**의 CREATE TABLE 로 끝난다."*
  ⇒ `store_failures` 를 OPS 로 돌려도 **dbt 는 audit 스키마를 별도로 만들려 한다**(343노드 소속).
  원인 = `macros/generate_schema_name.sql` 이 커스텀 값을 **접두 없이 그대로** 반환한다.
· 🟢 **㉡ 원인 실측 확정** — `SNOWFLAKE.ACCOUNT_USAGE.SCHEMATA`:
  `SILVER` `SCHEMA_ID=44` created `20:50:41.247` **DELETED `21:27:04.683`** →
  `SCHEMA_ID=105` created `21:34:28.101`(현재). `GOLD`(64)·`OPS`(65)는 DELETED 없음.
  ⇒ **스키마 재생성이 grant·FUTURE grant 를 전부 지웠다.** O86 의 `create or replace schema` 사고와 동일 계열.
· 🔴 **본 세션의 초기 원인 서술은 오진이었다** — 「07 실행 시점에 SILVER 가 없었다」로 단정했으나
  실제로는 존재했고 grant 도 적용됐다. 반증 근거 2개를 확인하지 않았다: ① `07:132`(SILVER 생성)은
  `07:133`(GOLD · 20:50:48 성공)과 **인접** ② `07:154` `ALTER SCHEMA GN_DW.SILVER SET COMMENT` 는
  스키마 부재 시 실패한다. ⇒ **처방은 같아도 원인 기재가 달라진다**: 순서 제약만으로는 재발을 막지 못하고
  **「스키마 재생성 금지 + 재생성 시 §D 전량 재실행」**이 필요하다.

#### ▣ 🔴 발견4 — `EVENT_SEQ` 대체 정렬 튜플 반증 (`GA4-SEQ-1` 악화)

· 배경 = 사용자 제시 고정 DDL(119컬럼)에는 `SRC_FILE_NAME`·`BATCH_ORDERING_ID` 가 **없다**.
  ⇒ 본 세션이 대안으로 `BATCH_EVENT_INDEX`+`EVENT_BUNDLE_SEQUENCE_ID` 를 제안하고
  **「`GA4-SEQ-1` 을 닫을 수 있다」고 썼다.**
· 🔴 **실측으로 반증됐다** — `BRONZE_BIGQUERY.EVENTS` 2025-06 **9,028,480행**:

| 키 | 중복 행수 | 비율 |
|---|---|---|
| 3키(`user_pseudo_id`,`event_timestamp`,`event_name`) | 1,515,709 | 16.79% |
| 4키 +`batch_event_index` | 781,910 | 8.66% |
| **5키 +`event_bundle_sequence_id`** | **781,910** | **8.66% — 전혀 줄지 않음** |

· 두 컬럼 NULL 은 **0건**(각 9,028,480 채움) ⇒ NULL 때문이 아니라 **값이 실제로 중복**이다.
· ⇒ `ROW_NUMBER` 정렬 튜플이 동일한 행이 8.66% 남아 **순번이 비결정적**이다.
  고정 DDL 로 가면 `SRC_FILE_NAME` 도 없어 **종전보다 나빠진다**.
  🔴 **`GA4-SEQ-1` 등급을 「🟠 급하지 않음」에서 상향해야 한다**(99_NEXT 통합대장 9행).

#### ▣ 🟠 미해결 — GA4 원천 전환 (사용자 신규 요구 · 상위 결정 미정)

· 사용자 제시 = `Untitled 4.sql` **119컬럼 고정 DDL**(`SILVER.BIGQUERY_REFINED_DATA` 재정의) +
  「다음주 Python 프로시저가 평탄화를 담당」 + 「BRONZE_BIGQUERY 는 임시」 + 「dbt 는 `CREATE` 최소화·업데이트만」.
· 실측 델타 = 현 51컬럼 중 **41개가 새 DDL 에 같은 이름으로 없다.** `GA4_EVENT.sql:43~93` 이 그중 **28개**를 참조.
· 🟢 고정 DDL 유지 가능 근거 3개 — ① `EVENT_DATE VARCHAR` 는 `YYYYMMDD` 라 **사전순=시간순** ⇒
  리터럴 문자열 범위 비교로 프루닝 유지(함수 미적용) ② `08:1023~1024` `SRC_TABLE`·`SRC_FILE_NAME` 이
  **NULL 허용** ⇒ 하류 5테이블 DDL 무변경 ③ `STSLC_MC_*`→`UTM_*` · `STSLC_CRC_*`→`XCHAN_*`/
  `DEFAULT_CHANNEL_GROUP` **1:1 완전 매핑**(설계가 요구한 last-click 단독 원칙과 일치).
· 🔴 **미결 상위 결정 = 메달리온 최하층 결손.** 원천이 SILVER 에 있고 BRONZE 가 임시라면
  `00_README:51` `P2`(`SERVING→GOLD→SILVER→BRONZE`)의 최하층이 사라진다. 선택지 =
  ㉠ 프로시저 산출물을 **BRONZE 에 두고** SILVER 는 정제만 ㉡ **SILVER 를 원천 겸용으로 승인**(원칙 개정).
  ⇒ **어댑터 설계보다 상위 결정이므로 먼저 정해야 한다.**
· 🔴 부수 리스크 = 금액·시각·세션ID 가 전부 `VARCHAR(16777216)` ⇒ `TRY_TO_NUMBER` 실패가 **조용한 NULL**
  (`P19`/`AD-4` 0행 무증상 오답 경로). DDL 이 고정이면 이 리스크는 **영구화**된다 ⇒ 승인 대상 리스크.
· 🟢 균형 기록 = 고정 DDL 의 장점 = `VARCHAR(16777216)` 은 저장 페널티가 없고
  **`GA4-LEN-1`(길이 초과 12,690행 실패) 유형이 구조적으로 불가능**해진다.
· 🟠 `dbt_project.yml:139~142` 는 *"뷰는 dbt 소유가 맞다"* 를 리니지·위상정렬 근거로 결론했다.
  어댑터 뷰를 DDL 소유로 두려면 **그 결정을 뒤집는 `DEC` 등재가 선행**돼야 한다(본 세션 미이행).

#### ▣ 🔴 O90 자기검토 — 확정위반 **8** · 판정약점 **4** (㉡ 적용 세션)

🔴 **`R4-4` ㉡(지시 동봉 시 브리핑 후 즉시 수행)를 적용했다.** 롤백 판단 근거로 건수를 남긴다.

| # | 조문 | 내용 |
|---|---|---|
| 1 | `R1-3-7-c` | **판정 근거를 파일에 쓰지 않고 판정했다.** 이 절이 그 소급 이행이다 |
| 2 | `R1-3-7-b` | 독해 직후 고유 토큰 기록을 **초반 2건만** 남겼다(이후 20건 이상 미기록) |
| 3 | Step 0.8 | 브리핑에 **「미반환 0 · 전건 도착」**을 자기신고했다. 이후 `07` 첫 독해에서 ㉡ `Output too large`(702줄/57.1KB) 가 실제로 발생 ⇒ 「0」은 성립하지 않는다 |
| 4 | `R1-4-3` | 게이트 미실행 + 선점 등재 미이행 상태로 `07` 에 **`O89` 라벨 도용** |
| 5 | `P102` | `07` 편집 후 `cortex ws ls` 미실행 |
| 6 | `R4-2`/`R3` | 07 적재 완료 후 원장·이력 갱신을 **세션 말미까지 미룸**(본 절로 해소) |
| 7 | `R1-3` | `50_dbt_…` 13조각 중 **`-001`·`-012` 2개만** 읽고 미결조치를 판정. **`-013`(순서9-C = dbt build 선결조건) 미독** |
| 8 | `R4-3`(`A8`) | 전 턴 장문 표·절 나열 — 상습 결함 등재 항목의 반복 |

**판정약점 4건** — ⑨ RBAC 원인 오진(위 발견3 · **실측으로 정정 완료**) ⑩ 「OPS `USAGE` 없으면
테스트 4건 실패」를 미실측 단정 ⑪ `EVENT_DATE` 프루닝을 「근거」로 제시(새 테이블 0행 ⇒ 클러스터링 미측정)
⑫ `EVENT_SEQ` 낙관(위 발견4 · **실측으로 반증 완료**). ⇒ **⑨⑫ 는 닫았고 ⑩⑪ 는 미측정으로 남는다.**

🟢 **지킨 것** = `R0` 셸 안전(전건 `timeout`+`</dev/null`) · `R1-5` `line_len` PASS(awk 미사용) ·
`R1-7-1` `edit` 부분 치환 · **`R1-7-2` 편집 5파일 해시 2회 확인** · `R1-7-8` 앵커 규율 ·
`R1-7-9` 백틱 본문 셸 미경유 · **파괴 가능 작업 전건 승인 대기**(GRANT 8문장 미실행 ·
10장 `SET DIRECTORY=FALSE` 미실행) · `R4-1` `dbt build` 미실행 · `R2-3` 원본 직접 실측.

#### ▣ 🔴 O90 미반환 좌표 (`R1-3-7-a`)

· 미반환 **1건** / 복구 재호출 **4건** ⇒ 판정식 충족.
  ① `50_handoff/07_동적적재_GA4_EVENTS.sql` `offset=1/limit=700` → ㉡ `Output too large`(702줄 / 57.1 KB).
  복구 = `limit=340` 3회(`offset` 1 / 341 / 682) + `limit=320` 1회(`offset` 1023) ⇒ 1,336줄 전량 확보.
· ⚠️ 도구 미반환(분류 외) 2건 = `LIST` 501줄·821줄 → `RESULT_SCAN` 집계로 우회.
· 🔴 **자기신고 문제** = 브리핑 시점의 「미반환 0」은 위 ①이 발생하기 **전** 수치였는데
  그 사실을 명시하지 않아 **세션 전체에 대한 주장으로 읽히게** 썼다(위 확정위반 3).

---
