<!-- SPLIT-CHUNK 01_세션이력.md | 070/070 | 허브 = 01_세션이력.md | 원문 1~107행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### 🟢🟢 [2026-09-16 `O167`] 열린 작업 전건 실측 재판정 — **「이미 닫혀 있던 것」 5건 종결** · 라이브 AI 컨텍스트 정정 · `㉗` NL 스모크 집행

> 🔴🔴 **사용자 지시·적발** = *"그 외에 건너 뛴 작업들 작업량 많아도 괜찮으니까 실측하고 완료할 수 있는 작업 완료해.
> 뭐 오픈된게 이렇게 많아 맨날 내가 업데이트해도 안 했다고 하니 원."*
> ⇒ O166-D 가 「사람 소관」 3건에서 받은 것과 **같은 적발이 열린 전건으로 확장**됐다.
> 🔴 **정본 = `20_issue/_o167_evidence.md`**(§0 라이브 · `E1`~`E10` · §11 스모크 · §12 자기결함 · §13 남은 것).

#### 1. 판정 설계 — 「소관 분류는 상태가 아니라 가설」을 전건에 적용했다

O166-D 가 세운 판정식을 **열린 13건 전건**(착수표 `⑭`·`㊳`·`⑬`·`⑩`·`㉗` + 문서50 8절)에 적용했다.
🟢 **결과 = 5건이 「이미 닫혀 있었다」** — `㊳` · `⑬` · `BLOCKING-1` · `O64 D1` · `BLOCKING-4` 잔여 1건.
🔴 **그중 `⑬` 은 2026-08-20 O95 가 정정을 끝냈고 27일간 「🟡 열림」이었다.**
원인은 게으름이 아니라 **선행조건 문면**이었다: 셀이 *"문서20 1,071줄 전량 독해 선행"* 을 달았고,
그 뒤 문서20 이 조각으로 분할돼 **해당 조각이 299줄**이 됐는데 **아무도 선행조건을 재평가하지 않았다.**
🟢 **판정식(신규) = 「선행조건이 「전량 독해」면 문서 크기를 다시 재라.」**

#### 2. 종결 5건 (근거)

* **`㊳`** 낡은 마운트 엔트리 — 3축 실측(마운트 루트 `.md` 3개로 부재 · 스테이지 루트 부재 · 실체
  `_archive/_o125e_entry.md` **2,743 B 정상 읽힘**). 이 행이 정한 착수 형태 ②*"사라지면 종결"* 그대로다.
* **`⑬`** — ㉠ `06` 재생성은 **O166-B** 가, ㉡ 문서20 §N-6 정정은 **O95** 가 집행. O167 재측정으로 O95 의
  4테이블 판정이 **오늘도 맞다**(`SPNSR_AMT` = BRONZE 4 + SILVER 4 + GOLD 4 + ML/SERVING 6).
* **`BLOCKING-1`** — O154 가 `MASTER-ORPHAN-POLICY` 로 **폐기한 처방**이었다. 재측정 = 입고 3축 일치
  **1,785,299**(O154 기재 1,763,065 는 stale) · 고아 **10,048행 / 8,299종 · 형식이상 0** ⇒ 현업 지침
  *"무시하고 파이프라인을 구성하라"* 에 따라 **영구 관측**이 정답이다.
  🔴 **적발** = 폐기 선언 뒤에도 **같은 파일 :10 에 폐기 문안이 살아 있었다.** :55 가 *"그것은 폐기된 문안이다"*
  라고 경고하면서 **자기 헤더 안의 인용을 못 잡았다** ⇒ 🟢 **판정식 = 「예약 조치를 폐기했으면 그 조치를
  적은 곳을 전수 세라.」**
* **`O64 D1`** — O144-C 판정식 재실행 = 파일 층 4종 전건 실재 + 라이브 `DESCRIBE SEMANTIC VIEW` 에서
  **파일과 동일 집합 실재** ⇒ **양층 발행 완료**.
* **`BLOCKING-4`** 잔여 — 라이브 `GOLD` 스키마 COMMENT 가 *"객체 수는 `INFORMATION_SCHEMA.TABLES` 를
  집계해 조회한다"* 로 **하드코딩 수치를 이미 제거**했다(「9→13」보다 좋은 해법 · 실제 WIDE 뷰는 지금 **14종**).
  🟢 **판정식 = 「N 을 N' 로 고쳐라」 백로그는 「N 을 지워라」가 이미 집행됐는지 먼저 보라.**

#### 3. `⑩` 이월 묶음 — 4축 판정 종결

* **㉠ 스테이지 내용 해시** = 🟢 **대체 축 신설**. `cortex ws ls` 의 `md5` 는 **암호화 blob 해시**여서 평문과
  `.md` **423건 전건 불일치**(일치 0) ⇒ 해시 대조 **원리적 불가**(착수표 `㉔` 결론에 실측 근거가 붙었다).
  🟢 대신 **`stage == 16*(local//16)+16` 이 2,741/2,741 성립** ⇒ `scripts/stage_mount_size_gate.py` 신설
  (`.md` **506건 · 드리프트 0 · 스테이지 전용 0 · 미발행 0 · PASS`) + 음성 **4축 12단정** + `gate_census` 등재.
* **㉡ 1,000~2,000자 관측** = 🟢 게이트 직접 실행(`doc_line_length_gate --all` PASS · 381파일).
  🔴 건수는 **자기참조 계측**이라 근거철에만 둔다(그 행이 정한 규약).
* **㉢ Agent 버전·롤백 정책** = 🟢 실측 = 3종 × 3버전이나 **유효 스펙은 각 `VERSION$3` 뿐**이고
  `VERSION$1·2` 는 **전부 35자 빈 스펙 = 6종**(O73 기재 「2종」은 stale) ⇒ **롤백 후보가 0개**이므로
  정책의 전제가 성립하지 않는다.
* **㉣ 계보 원천 교체** = 🟢 `SNOWFLAKE.CORE.GET_LINEAGE` 는 **함수로 실재하나** 호출 시
  **`Unsupported feature 'Data Lineage'`** · `SYSTEM$GET_LINEAGE` 는 **아예 없다** ⇒ **에디션 제약**이고
  대기 대상이 아니다. 🟢 **판정식 = 「함수 부재」와 「기능 차단」을 구별하라**(처방이 다르다).

#### 4. 🔴🔴 라이브 AI 컨텍스트 결함 — `IS_CURRENT=TRUE 투영`

`GOLD.DIM_MEMBER` **24컬럼에 `IS_CURRENT` 가 없는데** SV 논리테이블 COMMENT 가 *"회원 1명 = 1행,
IS_CURRENT=TRUE 투영"* 이라고 적고 있었다 — 파일 **4종** · 라이브 **4종**(`SV_MEMBER_MONTHLY`·`SV_MEMBER_EVENT`·
`SV_SERVICE`·`SV_EVENT_PARTICIPATION`).
🔴 **실해** = 이 문안은 **Agent 가 SQL 을 쓰기 전에 읽는 컨텍스트**다 ⇒ `WHERE IS_CURRENT = TRUE` 를 만들어
**invalid identifier** 로 죽거나 조건을 지어내 **행을 잃는다**.
🟢 **집행** = 파일 4종 정정 → `CREATE OR ALTER SEMANTIC VIEW` 문만 추출해 라이브 4종 재집행 →
`DESCRIBE` 전수 재측정 **잔존 0 / SV 17종**. 🔴 `scripts/_o166_rename_fix.py` 주석에도 같은 거짓이 있어 함께 고쳤다.
🟢 **판정식 = 「같은 거짓 문안은 여러 층에 복제돼 있다」** — 파일·라이브·도구 주석 3층을 함께 세라(`R3-9 ㉥`).
⚠️ 검증 직후 탐지기가 **True** 를 냈는데 그것은 **내 새 문안("IS_CURRENT 컬럼은 없다")이 스스로 걸린 것**이었다
⇒ 정확한 토큰(`IS_CURRENT=TRUE`)으로 재판정해 0 을 확인했다(**부정문을 심으면 탐지기에 걸린다**).

#### 5. 개명 축1 — 530 → **469**(−61 · 기준선 하향 발행)

🔴 먼저 **도구 결함**을 찾았다 = `_o166_rename_fix.py --apply` 가 **치환 0건**인데 게이트는 **530건**을 보고한다.
원인 = 그 도구의 `TARGETS` 가 O166 이 손으로 나열한 13개였고 **소진**됐다 ⇒ **도구 분모 ≠ 게이트 분모**.
🟢 처방 = `--from-gate`(게이트 `bucket()=='LIVE'` 인 파일만) + `--only <구 이름>`(어의 변경 개명 배제).
🔴🔴 **그 처방의 초판도 틀렸다** — `SCAN_DIRS` 전량을 훑어 **1,048건**을 냈고 그것은 `_archive/`·이력·근거철까지
포함이었다(**고치면 경위가 사라진다**). 게이트와 같은 `SKIP_DIR`·`EXT`·`bucket()` 으로 맞춰 **61건 / 26파일**.
🟢 집행 대상 = **순수 개명 4종**(GA→BIGQUERY). 순수성 근거 = 구명 4종 **라이브 전건 부재** · 신명 4종 **실재**.
🟢 **부수 성과** = `10_dbt_pipeline/tests/warn_gold_view_comment_coverage.sql:63` 이
`-- depends_on: {{ ref('WIDE_GA_BEHAVIOR') }}` 였고 **그 모델은 dbt 에 없다**(dangling ref) ⇒ 치환이 이를 복구했다.
🔴 주석처럼 보이지만 **dbt 가 파싱하는 DAG 힌트**라 모델 실명을 먼저 확인했기 때문에 안전했다.
🔴 **잔여 469건은 기계 치환 금지** — 최다 파일(`05_SV-Agent_ai/04_SV_설계.md` 44건)이 전부
`DIM_MEMBER_CURRENT` 이고 그것은 **개명이 아니라 객체 소멸**이다(문안을 사람이 다시 써야 한다).

#### 6. 착수표 `㉗` NL 라우팅 스모크 — 33문항 집행

🟢 문항 = 스펙 `sample_questions` **전량 33**(MEMBER 12 · MARKETING 11 · EXECUTIVE 10).
🔴 그 셀의 종전 기재 **3건이 전부 stale** 이었다 = 트라이얼 차단(→ 실행된다) · `AGENT_OVERALL`(→ `AGENT_EXECUTIVE`) ·
30문항(→ 33). 🟢 **라우팅 전건 적중**(회비→`analyst_member_fee` · 개발구분→`analyst_member_event` ·
이탈률→`analyst_member_cohort` · 목표/실적→`analyst_dev_achievement` · 활동회원→`analyst_member_sponsor_biz` ·
발송→`analyst_service` · 행사→`analyst_event_participation` · 예측→`analyst_ml_member_risk` ·
예산→`analyst_budget` · 광고→`analyst_ad`) · 오류 표면 **0** · 상태 전건 `completed`.
🟢 **판정 축 ㉡ 충족** = *"원천(bronze)은 어디야?"* 계열 문항은 **도구 0 · 서술만**이다(SQL 을 만들지 않는 것이 정답).
🟢 **비용 설계** = 응답 원문을 `tmp/nlsmoke/` 로 흘리고 판정기는 요약만 낸다 ⇒ **33문항의 세션 적재 = 요약 33줄**.
🟢 **판정식 = 「과금 축과 컨텍스트 축은 다르다」** — 되돌릴 수 없는 것은 크레딧이고, 줄일 수 있는 것은 적재량이다.

#### 7. 🔴 자기결함 6건 (전건 자체적발 · 상세 = 근거철 §12)

① `grep` 이 착수표 글리프 `⑬⑭㉗㊳` 를 **0건**으로 냈다(python 순회로 전환) ②`cortex ws ls` 파서를 **공백 분리**로
써서 신설 게이트가 **0건 = 미판정**(구분자는 **탭** · 음성 축2 로 회귀 고정) ③`SHOW SEMANTIC VIEWS.comment` 로
`O64 D1` 을 **오탐 판정**(토큰은 논리테이블 COMMENT) ④개명 치환 분모를 **1,048건**으로 과대 ⑤스모크 판정기가
**「SQL 생성 0」** 오보(`tool_use.input.sql` 로 정정) ⑥SV DDL 4종을 `edit` 대신 python 치환으로 고쳤다(`R1-7-1`
문면 위반 · 앵커 유일성 선검증·잔여 0 확인으로 **실유실 0**).
🔴 **6건 중 4건이 「분모·그릇 오독」**이고 `O165 D5` · `O165 D11` · `doc_census 축㉣` 초판 · O166 `SCD2_DIM` 과
**같은 계열**이다 ⇒ 🟢 **판정식 = 「「없다」를 말하기 전에 「내가 본 그릇이 그것을 담는 그릇인가」를 물어라.」**

#### 8. 검증

6게이트 전건 `rc=0`(`doc_census`·`doc_type_gate`·`clause_order_gate`·`index_row_gate`·`doc_heading_gate`·
`doc_coord_gate`) · `gate_census` **미분류 0 · 유령 0 · 중복 0** · `doc_line_length_gate --all` **PASS(381파일)** ·
`stage_mount_size_gate` **PASS** · `test_stage_mount_size_gate` **4축 12단정 PASS** ·
`rename_stale_gate` 기준선 **469 하향 발행**(`--reason` 동봉).

_Co-authored with CoCo_

> #### 🟢 [2026-09-16 O169] CONF-3 종결 재판정 · 개명 축1 B군 68→0 전량 해소 · 축2 재생성 · 결정 문항 D1~D13 발행

> 🔴🔴 **수치·판정식 정본 = `20_issue/_o169_evidence.md`** — 이 항목은 **경위 서술**이고 수치는 그 근거철에 있다.
> 🔴 `R4-4-2 ㉡` 적용 세션이다(지시 동봉 ⇒ 브리핑 출력 후 되묻지 않고 즉시 수행) · **확정위반 0건**.

> **▣ 착수 · 독해**
> · 브리핑 실측 = 열린 착수표 4 · 문서50 열린 절 6 · 인수인계 항목 2 · stale 0 · `read` 미반환 **0** · 재호출 **0**.
> · 독해 좌표·고유 토큰 6건은 근거철 §E0 에 기록했다(`R1-3-7-b`).
> · 라벨 = `id_collision_gate --next O` → 「정의 166 · 참조 168 ⇒ **O169**」.

> **▣ 1. `CONF-3` 은 닫혀 있었다 — 세 기재는 상충이 아니라 한 축의 3단계다**
> 사용자가 「상충하는 3건」으로 지목한 `O93-B`·`O95`·`O141` 을 전량 독해하니 **모순이 아니었다**:
> ㉠ `O93-B` = 「정본 #51 두 문장은 모순이 아니라 **층위가 다르다**」 ⇒ **기술적 해소**
> ㉡ `O95` = 그 전제에서 나온 「방향 상반」 판정 **철회** ⇒ **물음 폐기**
> ㉢ `O141` = 그럼에도 **업무 규칙을 회신받아 확정** ⇒ `DEC-47 §33-B`(당월 말 `SPNSR_DSCNTC_YN='N'` 후원사업
>   1개 이상 보유 시 월말활동회원 · 문서20 §N-11 회신 2026-09-07).
> ⇒ 🟢 **`CONF-3` 종결이 옳다.** 그리고 문서50 `-013.md:137` 은 **두 축이 동시에 stale** 이었다.

> **▣ 2. 「활동회원 8종」의 6종은 이미 적재돼 있었다 — 새 판정식**
> 컬럼을 실제로 특정하고(8컬럼) `COUNT_IF(<>0 AND NOT NULL)` 로 재니 **6종 적재 · 2종 미주입**이었다.
> 🟢 **새 판정식 = 「묶음의 차단 근거는 묶음 단위로 상속되지 않는다」** — 묶음 표기는 **최초 등재 시점의
> 최대공약수**일 뿐이다. 🔴 음성 축 = **일부 적재를 근거로 묶음을 「해소」로 닫지 마라**(집합 차이로 적어라).
> 🔴 그리고 사용자 지시의 「전건 0」도 **그릇이 달랐다** — 실측은 **전건 NULL** 이다(`J2` 축).
> 과거 기재(「전건 0(NULL 아님)」)는 **그 시점엔 참**이었고 그 뒤 모델이 `CAST(NULL AS …)` 로 바뀌었다.
> ⇒ 잔여 2종의 차단 근거는 `CONF-3` 이 아니라 **버킷 C 「정본 정의 부재」** ⇒ **`D12`** 로 승격했다
> (선례 = `_o159_mstr_impact_measure.md:141` 이 그 정의 시도를 **🔴 창작**으로 판정했다).

> **▣ 3. 개명 축1 B군 68건 — 기계 치환하지 않고 줄마다 갈랐다**
> 먼저 라이브를 재서 **「개명이 아니라 객체 소멸」**임을 확증했다(구 `DIM_MEMBER_CURRENT` **부재** ·
> `DIM_MEMBER` **1,785,299** = distinct `MEMBER_DK` 동수 ⇒ **팬아웃 0** · 이력은 별 객체 **8,069,279행**).
> ⇒ *"`DIM_MEMBER` 는 SCD2라 팬아웃한다"* 는 **라이브에서 거짓**이고, 게이트 자신이 같은 경고를
> 이미 주석에 적고 있었다(`rename_stale_gate.py:63~67`) — **독립 일치**다.
> · **기록 60줄** = 원문 문자를 **하나도 지우지 않고** 병기만 덧붙였다(축A 병기어 삽입 57 · 축B 3).
> · **지시 6줄** = 손으로 현행 객체로 고치거나 **폐기 선언**했다(`10_성능검토` 권고(b) = `J5` 예약조치).
> · 🔴 **분모를 내가 세지 않았다** — 게이트의 `scan()` 을 import 했다. 초판은 `--list` 출력을 파싱하고
>   파일을 직접 재스캔해 **71 vs 68** 로 어긋났다(`J8` 「같은 것을 다르게 재지 마라」 실물).

> **▣ 4. 축2 — 「재생성으로 해소」가 참인 부분과 거짓인 부분을 갈랐다**
> 🟢 참 = 인벤토리 CSV(재실행 → **0건** 실증). 🟢 문서 산출물 2건 해소.
> 🔴 거짓 = 골든 json 18건 중 6건이 **개명을 기록한 감사 이력 자체**다 ⇒ 재생성해도 남는다(`J8` json 판본)
> ⇒ 처방은 재생성이 아니라 **분모 정정**. 🔴 202 는 **stale 날짜 파일**이라 **은퇴**가 필요하고 그것은
> **파괴 가능 작업**이므로 승인 없이 하지 않았다(`R4-4-3`) ⇒ **`D13`**.
> 🟢 골든 대조 `test_generators` **PASS 21 · FAIL 0** ⇒ 드리프트 0 이므로 **재발행하지 않았다**
> — 「재발행 사유를 남겨라」에 대한 답은 **「발행할 것이 없으면 사유도 없다」**다.

> **▣ 5. 🔴 자기결함 3건 · 쓰기 사고 1건 (전부 자체적발)**
> · **결함1** 병기 삽입점을 `m.start()` 로 두면 `` `GOLD.DIM_MEMBER_CURRENT` `` 가
>   `` `GOLD.<병기어>DIM_MEMBER_CURRENT` `` 가 되어 **식별자 내부를 깨뜨린다** ⇒ 예행에서 잡아 집행 전 수정 ·
>   음성 테스트 축2 가 **고치기 전 구현의 실패를 실증**한다.
> · **결함2** 인용문(`*"…"*`) 내부에 병기를 끼우면 그것은 병기가 아니라 **인용 개변**이다(`R2-8-2`)
>   ⇒ `quote_spans()` 로 감지해 **원문 무변경 + 줄말미 병기**(축B)로 전환했다.
> · **결함3** 🔴🔴 `gate_census` 에 신설 도구를 등재하며 쓴 **주석 문장이 자기 탐지기에 걸려** 축1 을
>   **0 → 1** 로 올렸고, 나는 **그 상태로 기준선을 발행**했다. 시정도 한 번 빗나갔다(같은 문단의 `:208` 만
>   고치고 `:206` 을 남겼다) ⇒ **3차 발행**이 됐다. 🟢 처방 = ㉠ 감시 분모에 든 파일에 그 이름을 쓰려면
>   **병기어를 함께** 써라 ㉡ **기준선 발행은 잔여를 고정한다** ⇒ 발행 직전과 시정 직후에 다시 재라.
> · **쓰기 사고** 🔴🔴 `edit` 가 *"File now has 169 lines"* 를 보고했으나 실제 파일은 **127줄에서
>   문장 중간에 절단**돼 있었다. `R1-7-3` 대로 3회 재읽어 **해시 동일**(5,632 B)을 확인 ⇒ torn read 가 아닌
>   **실손상**. 복구 = 전량 재작성 + `R1-7-1` 복구 4축(꼬리·`py_compile`·`def` 개수·해시).
>   🟢 **새 판정식 = 「도구의 성공 보고는 도달의 증거가 아니다」** — O167 의 *「썼다」와 「닿았다」는 다르다*
>   가 `cat >>` 판본이었고 이번은 **`edit` 판본**이다. 🟢 같은 축 2차 = 생성기를 `--write` 없이 돌려
>   **표준출력에만 찍고 파일은 그대로**였다(게이트가 그 1건을 계속 보고해서 잡았다).

> **▣ 6. 결정 문항 D1~D13 — 근거 있는 것만 권장했다**
> 🟢 권장 5건 = `D1`(CONF-2 = **지표별로 다름** · 금액환산 축은 ÷10,000 · 미납 축은 실건수 — 근거 =
> `DECREASE_CNT` COMMENT 가 이미 산식 명문이고 미납에는 `UNPAID_BILLED_AMT` 금액 슬롯이 **따로 있다**) ·
> `D3`(**마스터 미입고 → 입고 요청** · 근거 3중 = `DIM_EVENT` 에 `EVENT_100~104`·`107~110` 실재하고
> **105·106 만 구멍** · BRONZE `TM_MS_EVENT` 에 **0행** · 참여 사실 28만 건 실재 ⇒ 폐지·타시스템 둘 다 기각) ·
> `D5`(**상수 유지 + COMMENT** 단 노출 차단 조건 2개) · `D12` · `D13`.
> 🔴 **권장을 비운 8건** = `D2`·`D4`·`D6`~`D11`. 이유는 **근거 미확보**다(*"근거를 못 썼으면 판정하지 않는다"*)
> ⇒ 각 문항에 **「무엇을 재면 갈리는가」**만 적었다. 🟢 그 과정에서 **집합 불일치 2건**을 발견했다:
> `D7` 결정대기 GOLD **4 ↔ 문서50 은 6** · `D9` `O91-F` **「5건 중 4건 종결 · 잔여 2건」**(5−4=1 ↔ 2).

> **▣ 7. 종료 확인**
> · 게이트 = `doc_type`·`clause_order`·`index_row`·`doc_heading`·`gate_census`·`id_collision`·
>   `verify_init_ihcho_skill`·`comment_drift`·`table_ddl_column`·`doc_census`·`doc_coord`·
>   `sv_comment_object`·`sv_identifier`·`sv_unit` **전종 rc=0** · `line_len` 🟢 PASS.
> · `scripts/test_*.py` **37/37 PASS** · `test_generators` **PASS 21 · FAIL 0**.
> · 🔴 **`sv_code_label_gate` 선행 FAIL 3건**(라이브 SV COMMENT 코드값 오기 = `SV_MEMBER_EVENT.DAY_OF_WEEK`
>   열거 `['Fri'…]` ↔ 실제 `['0'…'6']` · `CMMN_BRND_NM` 열거 `['일시']` ↔ 실제 8종 이상 · 종수 14 ↔ 16).
>   🔴 **이 세션이 만진 축이 아니다**(SV COMMENT·DDL 미변경) ⇒ **선행 상태**로 등재하고 지시를 기다린다.
> · 🔴 **dbt 미실행·모델 미저작**(`R4-1`) — 저작 대상이 결정 대기로 판정돼 **없었다**.
> · 정본 3곳 갱신 = 원장 §1 O169 행 · 이 이력 항목 · `99_NEXT` **§0-VVVV/O169**(§0-UUUU 승계 표기).

> #### 🟢 [2026-09-16 O169-B] 승인 후 집행 — 축2 0 달성 · 99_NEXT 재균형 · SV 라이브 코드값 정정 · 자기결함 3건 추가 적발

> 🔴🔴 **수치·판정식 정본 = `20_issue/_o169_evidence.md` §E4.** 이 항목은 경위 서술이다.
> 승인 = 사용자 「승인. FAIL 2건 등 내 의견이 필요하지 않은 부분 전부 진행」 ⇒ `R4-4-3` 대상 3종을 집행했다.

> **▣ 1. `D13` — 축2 301 → 0. 처방이 하나가 아니었다**
> 착수 시 문서는 축2 를 **「자동 생성 산출물(재생성으로 해소)」** 한 덩어리로 적었다. 실측하니 **세 갈래**였다:
> · ㉠ **재생성이 듣는 것** = 문서 산출물 2건(`92`·`11`) + 인벤토리 CSV(재실행 → 옛 이름 0건 실증).
> · ㉡ **재생성이 안 듣는 것 1 — stale 날짜 파일**(`…인벤토리_20260904.csv` 202건).
>   날짜가 파일명이므로 재생성은 **새 파일을 만들 뿐 그 파일을 덮지 않는다** ⇒ **은퇴**가 유일한 처방.
>   집행 = `R1-7-7` 절차(스냅샷 선행 → **바이트 동일 확인 후에만** 삭제 → `os.listdir` 재확인).
>   ⚠️ SILVER 쌍둥이는 옛 이름 **0건**이라 **옮기지 않았다**(`R2-8-3`).
> · ㉢ **재생성이 안 듣는 것 2 — 골든·측정 스냅샷 json**(97건).
>   🔴 `golden/doc_headings.json` 의 옛 이름 다수가 **골든 자신의 `_updates` 감사 이력**이었다
>   (*개명을 기록한 문장*) ⇒ append-only 라 영구 잔존한다. `outputs.json` 의 62건은
>   `metrics/inv.entry_keys` 이고 원천이 **수기 문서**(`05_필드 인벤토리.md`)라 복사를 되풀이할 뿐이다.
>   ⇒ 처방 = **`SNAPSHOT` 축 신설**(축3 취급 · `bucket()` 에서 `GEN` **앞**에 판정 — 두 축에 다 걸리는
>   경로에서 **처방이 통하는 쪽**을 택해야 한다).
> 🟢 **판정식(신설) = 「버킷은 생성 여부가 아니라 처방으로 가른다.」**
> 🔴 **자기결함 추가 1** — 축5 를 7→14건으로 확장하자 **축11 의 기존 단정과 충돌**했다
> (`golden/outputs.json` 을 `GEN` 으로 단정) ⇒ 🟢 **축을 늘릴 때 기존 축의 단정도 함께 재라**(`R3-9 ㉡`).

> **▣ 2. `99_NEXT_SESSION` 재균형 — 축이 옮겨 갔고, 그 다음이 함정이었다**
> `--rebalance --fill 0.7 --apply`(예행 선행 · 스냅샷 `O169-prerebalance`) ⇒ 조각 상한 초과 0 ·
> concat SHA256 일치 · 유실 0 · max 28,524 B.
> 🔴🔴 **직후 `doc_type_gate` 가 「허브 44,521 B > 40KB」로 FAIL 했다.** 내 직접 실측은 **38,620 B** 였다.
> 규약(`session-end.md` §2)이 *"유지 연산 직후의 FAIL 을 곧바로 믿지 마라 — 2~3회 재실행 후 판정"* 이라
> 적어 두었고 나는 2회 재실행했는데 **둘 다 rc=1** 이었다 ⇒ 3회차에 **rc=0 · 상한 초과 0**.
> 🟢 **판정식(보강) = 「2~3회」의 하한을 믿지 마라 — rc 가 뒤집힐 때까지 시간을 두고 다시 재라.**
> 🔴 만약 2회차에서 멈췄다면 **없는 결함을 고치려고 문서를 또 건드렸을 것**이다(규약이 금지한 그 행동).
> 🟢 부수 = 채움률 예행(0.80/0.90/0.95 → 27/25/24조각 · 상한 초과 전건 0)으로 현 상태가 안정 구간임을 확인.
> 🟢 편집한 조각의 **허브 7종 재발행**(`--republish --expect` 전건 통과).

> **▣ 3. 🔴 라벨 충돌 자체적발 — `0-VVVV` 는 이미 O149 가 썼다**
> 재균형 후 좌표를 다시 잡다가 `▣ VVVV2` 가 **`-021.md:105`(O149 · 2026-09-08)** 에도 있는 것을 발견했다.
> 절 라벨 자체는 세션 병기(`0-VVVV/O169`)로 유일하지만 **하위 `▣ VVVVn` 이 겹쳤다.**
> ⇒ 🟢 **기존 라벨은 건드리지 않고**(`소급 개명 금지`) **내 하위 라벨만** `▣ O169-0`~`O169-4` 로 바꿨다.
> 🟢 교훈 = O167 이 신설한 **세션 라벨 병기 관례가 실제로 충돌을 막았다**(절 라벨은 무사했다) —
> 다만 **하위 라벨에도 같은 관례를 적용해야** 한다.

> **▣ 4. `sv_code_label_gate` 선행 FAIL 3건 — 라이브까지 정정**
> 🔴 **먼저 그릇을 틀렸다** — `CMMN_BRND_NM` 을 `DIM_CAMPAIGN` 에서 재서 17종을 얻었으나 게이트의
> backing 은 `FACT_MEMBER_EVENT.CMMN_BRND_NM_AT_EVENT` 였다. SV DDL 문법
> (`<논리테이블>.<차원명> AS <별칭>.<물리컬럼>`)을 읽고 물리 컬럼을 특정한 뒤 재측정했다(**16종** · NULL 1,061,449).
> 실측 결론 = COMMENT 가 ㉠ 종수를 `14종`으로 틀리고 ㉡ **라이브에 없는 값을 열거**하고
> ㉢ **실재 3종을 빠뜨렸다**. `DAY_OF_WEEK` 는 **요일 약칭을 열거하고 「+NULL」이라 적었는데** 실제는
> **코드 `0`~`6` · NULL 0건**이었다 ⇒ 요일 이름으로 필터하면 **0행 무증상 오답**(AD-4 유형).
> 대응은 창작하지 않고 **코드별 `DAYNAME` 교차**로 확정했다(0=일 … 6=토).
> 🔴 **부수 발견** = 코드 `0` 에 시드 행 `1900-01-01`(실제 **월요일**) **1건**이 섞여 있다
> (`0`=2,349 · 나머지 각 2,348) ⇒ **일요일 집계가 1행 과대**다. COMMENT 에 경고로 남겼다.
> 🟢 **파괴 위험을 먼저 확인했다** — 본문이 `CREATE OR ALTER SEMANTIC VIEW` 라 GRANT·소유권이 보존된다
> (`P125` 의 `CREATE OR REPLACE` 경로가 아니다). 재배포 후 `SHOW GRANTS` **7건 전건 잔존** ·
> `created_on` **2026-09-15 불변** · OWNERSHIP `GN_DW_ADMIN` **실측 확인**.
> 🔴🔴 **자기결함 추가 2 — 1차 재배포는 실패했다.** 정정 사실을 남기려고 **옛 값 목록을 COMMENT 본문에
> 인용**했고, 게이트의 열거 추출기가 **그 옛 목록을 현재 열거로 집어** 위반이 남았다(3→1 에서 멈춤).
> 이것은 O167 이 이미 적어 둔 *「부정문을 심으면 그 부정문이 탐지기에 걸린다」* 의 **재발**이다.
> ⇒ 🟢 **규약 = COMMENT 에는 현재 코드값만 두고 경위·수치·유령값 이름은 DDL `--` 주석과 근거철에 둔다**
> (`R2-6` 과 같은 축). `CMMN_BRND_NM` 의 같은 잠재 함정도 **함께** 정리했다. 2차 재배포 후 **위반 0 · rc=0**.

> **▣ 5. 종료 확인**
> · 개명 = 축1 **0** · 축2 **0** · 병기 270 · 축3 542(스캔 666파일).
> · 게이트 **전종 rc=0**(`sv_code_label_gate` 포함) · `scripts/test_*.py` **37/37** ·
>   `test_generators` **PASS 21 · FAIL 0** · `line_len` 🟢 PASS.
> · 신설 도구 2종을 `gate_census` 에 등재(`_o169_axis1_biwi` · `_o169_sv_redeploy` — 둘 다 `MUTATES`) ·
>   관측 1종(`_o169_dump_axis1`).
> · 🔴 **dbt 미실행·모델 미저작 유지**(`R4-1`).
> · 🟢 **우리 소관 열린 작업 = 0.** 남은 것은 결정 문항 `D1`~`D13` 중 **미회신 8건**과 현업 회신 대기다.

> **▣ 6. 이 세션 자기결함 총계 = 5 + 쓰기사고 1 (전부 자체적발 · 확정위반 0)**
> ㉠ 병기 삽입점 오류 → 식별자 파손(예행에서 차단) ㉡ 인용문 내부 병기 = 인용 개변(`R2-8-2`)
> ㉢ `gate_census` 주석이 자기 탐지기에 걸린 채 **기준선 발행**(시정도 빗나가 3차 발행)
> ㉣ **정정 서술의 옛 값 인용**이 추출기에 걸려 SV 1차 재배포 실패 ㉤ **축 신설이 기존 축 단정과 충돌**
> · 쓰기사고 = `edit` 가 「169 lines」를 보고했는데 파일은 **127줄에서 절단**(해시 3회 동일 ⇒ 실손상).
> 🟢 **다섯 중 셋(㉢㉣㉤)이 「내가 쓴 문장이 내 계측에 걸린다」는 한 형태다** — `J8` 의 세 가지 판본이다.
> ⇒ 🟢 **판정식 = 감시 분모에 든 곳에 그 이름·그 값을 쓰려면, 쓰는 순간 그 계측을 다시 돌려라.**
