<!-- SPLIT-CHUNK 01_세션이력.md | 045/045 | 허브 = 01_세션이력.md | 원문 1~144행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢🟢 [2026-08-29 O115-C] `dbt build` 최초 성공 — GOLD 적재 완료 · WARN 36건 전수 확보 · `BLOCKING-1` 전제 소멸 적발 · 인수인계 §0-VVV 신설
>
> **트리거** = 사용자가 `dbt build` 를 직접 실행하고 결과를 붙여 주며 「다음 세션용 복붙 프롬프트」를 요청.
> **결과** = `Done. PASS=425 WARN=36 ERROR=0 SKIP=0 TOTAL=461` (2026-08-29 03:59:59)
> 🟢 **`R4-1` 정지점이 설계대로 작동했다** — 에이전트가 명령을 제시하고 사용자가 실행했다.
> 🟢 **이 워크스페이스에서 파이프라인이 처음으로 끝까지 돌았다.**

---

##### 1. 🟢 적재 실측 — 「dbt 를 돌리기 전에」 전제가 소진됐다

| 축 | O115 시점 | build 이후 |
|---|---|---|
| `SILVER` | 42/43 **0행** | 🟢 **42/43 적재** · 457,928,282행 |
| `GOLD` | 37/37 **0행** | 🟢 **36/37 적재** · 146,457,668행 |
| 0행 잔여 | — | **2건** = `SILVER.CRM_BIZ_TARGET` · `GOLD.FACT_TARGET_BIZ` |

🟢 **그 2건은 결함이 아니다** — `_silver_bridge_schema.yml` 이 *"원천 0행(`WHERE 1=0` 스캐폴드) → 통과"*
라고 이미 선언한 의도된 스캐폴드다(원천 미입고 · `CRM_BIZ_TARGET` → 하류 `FACT_TARGET_BIZ`).
🔴 **0행을 보고 결함이라 쓰지 않았다** — 스키마 yml 을 먼저 읽어 확인했다.

##### 2. 🟢 O115 사전 예측이 전건 적중 — 「dbt 없이 시뮬레이션」이 유효했다

| 예측 대상 | O115 예측 | build 실제 | 판정 |
|---|---|---|---|
| `SILVER.ERP_BUDGET` | 2,964 | **2,964** | 🟢 |
| `SILVER.ERP_BUDGET_YEARLY` | 247 | **247** | 🟢 |
| `SILVER.ERP_BUDGET_ITEM` | 179 | **179** | 🟢 |
| `GOLD.DIM_BUDGET_ITEM` | 180 | **180** | 🟢 |
| `GOLD.FACT_BUDGET_YEARLY` | 247 | **247** | 🟢 |
| 예산 error 테스트 8종 | 전건 통과 | **ERROR=0** | 🟢 |
| GA4 `not_null` 3종 | 통과(`where` 창 제외) | **ERROR=0** | 🟢 |
| 예산 singular 3종 | WARN 발화 | **44 · 42 · 528** | 🟢 |

🟢 **부수 적중 1건이 특히 의미 있다** — `not_null_CRM_CAMPAIGN_MKTG_UTM_NM` WARN = **21,682** 이고
이것은 O115 가 **고아 UTM 코드 `192`** 를 실측했을 때 나온 행수와 **정확히 같다**
⇒ 🟢 **원천 실측 → dbt WARN 예측이 성립함을 실증**했다(다음 세션이 같은 방법을 쓸 근거).
🟢 **`RBAC 5건이 유일한 차단이었다」는 판정도 확증됐다** — 부여 후 `ERROR=0`.
🔴 **다만 `month_grain` 528 은 예측하지 않았다** — O115 는 그 축의 위반 행수를 계산하지 않았다.
⇒ 🔴 「전건 적중」이라 쓸 때 **예측하지 않은 항목을 적중에 포함하지 않았다**(분모를 정직하게 유지).

##### 3. 🔴🔴 `DEC-44` 의 성격이 바뀌었다 — 이제 **틀린 값이 조회된다**

라이브 `GOLD.FACT_BUDGET_YEARLY` 실측(build 후):

| 측정 | 라이브 | 실제 | 과대 |
|---|---|---|---|
| `SUM(EXEC_BUDGET_YEAR)` | **99,006,005,048** | 55,094,546,653 | **+79.7 %** |
| `SUM(PLAN_BUDGET_YEAR)` | **106,085,664,326** | 62,100,245,921 | **+70.8 %** |
| 행수 / 고유 `BUDGET_ITEM_SK` | 247 / 179 | 1:1 | 초과 **68** |
| `CHN_BUDGET_YEAR`·`ADJ_BUDGET_YEAR` | 전건 **0** | — | 추경이 별도 **행** |

🔴 종전 표현은 「돌리면 틀린 값이 나올 것이다」였고 **지금은 「이미 나오고 있다」**다.
🔴 **신규 경고를 인수인계에 추가했다** = **SV·Agent 를 이 팩트에 붙이지 마라** —
붙이면 재무 수치가 **+79.7 %** 로 발행된다. 종전 인수인계에는 이 문장이 없었다
(build 전에는 소비가 불가능해서 필요하지 않았다 · **상태 변화가 새 위험을 만든 사례**).
🔴 **키는 여전히 고치지 않았다**(현업 회신 선행 · `§30-B`).

##### 4. 🔴🔴 최대 신규 발견 — `BLOCKING-1` 의 warn 유지 근거가 소멸했다

`_silver_bridge_schema.yml` 은 `MEMBER_DK` 관계 테스트를
*"순서9-B: 회원 마스터 미완전 고아 정책 일관(warn). **전량입고 후 제거→error 복귀**"* 로 두었다.
🔴 **그 조건이 충족됐는지 아무도 확인하지 않았다** ⇒ 확인했다:

| 축 | 실측 |
|---|---|
| 브론즈 회원 | 정기 1,587,343 + 일시 175,722 = **1,763,065** |
| `SILVER.CRM_MEMBER` | **1,763,065** · `MEMBER_DK` **전건 유일**(1,763,065) |

⇒ 🔴🔴 **전량입고가 확인됐으므로 아래 고아는 「미입고」가 아니라 「실제 고아」다.**

| 테스트 | WARN |
|---|---|
| `CRM_SEND_MEMBER.MBER_NO → CRM_MEMBER` | **31,486** |
| `FACT_SERVICE_EVENT.MEMBER_DK → DIM_MEMBER` | **31,486** |
| `CRM_EVENT_PARTICIPATION.MBER_NO → CRM_MEMBER` | **9,480** |
| `FACT_EVENT_PARTICIPATION.MEMBER_DK → DIM_MEMBER` | **9,480** |
| 그 외 9종 | 309 · 271 · 270 · 199 · 153 · 85 · 37 · 16 · 1 · 1 |

🟢 **진단 단서 = 상·하류가 동수다**(31,486 ↔ 31,486 · 9,480 ↔ 9,480)
⇒ **SILVER 고아가 GOLD 로 그대로 전파되며 증폭되지 않는다** ⇒ **배선은 정상이고 원천 문제**다.
🔴 **그래서 `error` 로 즉시 올리면 안 된다** — 고아의 정체(탈퇴·삭제분 / 타 원천 소속 / 형식 오류)를
먼저 규명해야 한다. 올리면 build 가 멈춘다. 🔴 **순서를 인수인계에 명시했다.**

##### 5. 🟢 WARN 36건 전수 확보 — 분모를 확정했다

🟢 재현법 = `grep -oE "WARN [0-9]+ [A-Za-z0-9_]+" 10_dbt_pipeline/logs/dbt.log | sort -u`
⇒ **정확히 36건** 이 나왔고 **보고값 WARN=36 과 일치**한다 ⇒ **분모 확정**(추정 아님).
🟢 분류해 `▣VVV3` 에 등재 = **예산 그레인 3 / `MEMBER_DK` 고아 13 / 채움률·라벨 20**.
🔴 **최대 2건은 미규명이다** = `not_null_CRM_SEND_MEMBER_SEND_RESULT_NAME` **1,401,419** ·
`relationships_CRM_EVENT_PARTICIPATION_EVENT_KEY → CRM_EVENT` **263,611**.
🔴 **분모를 함께 적으라고 지시해 두었다** — 「1,401,419건 NULL」은 그 테이블 총 행수 없이는
과장으로도 축소로도 읽힌다(`R2-6`).
🔴 **`logs/dbt.log` 는 회전된다**(`.1`~`.4` 실재) ⇒ **다음 build 전에 정본화해야 한다**고 적었다.

##### 6. 🟠 경보 1건을 스스로 냈다가 철회했다 (자기결함)

`GOLD.DIM_MEMBER` = **7,925,716** 을 보고 원천 `CRM_MEMBER` **1,763,065** 와 비교해
**「4.5배 = 중대 결함」**으로 경보했다. ⇒ 🔴 **모델 헤더를 읽고 철회했다**:
`DIM_MEMBER.sql:1` = *"회원 차원 **SCD2** (CRM_MEMBER 현재값 + STATUS_HIST 이력)"* ·
같은 헤더에 *"실측(2026-08-03): **7,925,716** → 7,925,716 불변"* 이 적혀 있었다.
⇒ 🟢 **설계값이고, 게다가 다른 계정의 2026-08-03 실측치와 정확히 일치한다**
⇒ **파이프라인이 재현됐다는 강한 증거**였다(결함의 반대).
🔴 **교훈 = 차원 행수를 원천 행수와 비교하기 전에 그 차원이 SCD2 인지 먼저 읽어라.**
🟢 판정식 3조 `㉢` 으로 `▣VVV6` 에 등재했다.
🟠 **다만 중간 보고에서 「중대 발견」이라고 먼저 말한 뒤 철회했다** — O115-B `①` 과 같은 형태다
(성급한 경보 → 철회). 🔴 **모델을 읽는 데 든 비용은 1회 `head` 였다** ⇒ **먼저 읽었어야 했다.**

##### 7. 🟢 인수인계 재편 — §0-VVV 신설

🟢 `99_NEXT §0-VVV`(`▣VVV1`~`▣VVV6`) 신설 + **§0-UUU 를 승계 표기**(취소선 + 「§0-VVV 로 승계됐다」)
⇒ `session_brief.py` 가 현행 절만 뽑는다.
🟢 **미결을 소관별로 갈랐다** = **사람·현업 7 / 계정·접근 2 / 에이전트 가능 5**
⇒ 종전 인수인계는 이 구분이 없어 「에이전트가 못 하는 일」과 「안 한 일」이 섞여 있었다.
🟢 `▣UUU6`·`▣UUU7`·`▣UUU9` 는 **여전히 정본**임을 §0-UUU 병기에 명시(지우지 않았다).
🟠 조각이 300줄 상한을 넘겨 **재균형**했다(21 → **22조각** · max 27,637 B · 유실 0).

##### 8. 🔴 O115-C 자기결함

㉠ 🔴 **원장 행을 또 상한 초과로 썼다**(2,217자) — `O112 ㉢` 이 *"쓰기 전에 항목당 1문장 초안"*
   이라고 경고했고 이번이 **3회째 위반**이다(O115 본체 4회 · O115-C 1회).
   🔴 **경고를 읽는 것으로는 안 고쳐진다** ⇒ 🟢 **기계 강제가 필요하다**:
   다음 세션 후보 = 원장 행 작성 시 `line_len.py` 를 **쓰기 전에** 초안에 돌리는 절차 또는
   `index_row_gate` 에 「행 문자수 상한」 축 추가.
㉡ 🟠 **`--expect` 토큰을 또 장식 포함으로 선언했다** — `dbt build 가 성공했다` 는 백틱이 끼어
   연속 리터럴이 아니었다 ⇒ 신선도 FAIL. 🟢 **가드가 정확히 잡았고** 장식 없는 토큰으로 통과했다.
   🔴 `O112 ㉠` 이 이미 기록한 함정이다 — **파일보다 토큰을 먼저 의심하라**는 처방을 못 지켰다.
㉢ 🟠 **경보 선행 · 확인 후행**(위 §6) — O115-B `①` 과 같은 축이 이번에도 나왔다.
㉣ 🟢 **`read` 미반환 0건 · 재호출 0건.**
㉤ 🔴 **WARN 36건 중 33건은 분류만 했고 규명하지 않았다** — 그 사실을 「분류 완료」로 쓰지 않고
   **「미규명」으로 표기**했다(㉠㉡ 규명 · 나머지 미규명).

##### 9. 다음 세션에 넘기는 것

🔴 **정본 = `99_NEXT §0-VVV`**(여기서 시작한다). 요지:
1. 🔴🔴 **WARN 36건 분류·규명** — 최대 작업(`▣VVV3`).
2. 🔴🔴 **`DEC-44` 현업 회신** — 🔴 회신 전 SV·Agent 를 예산 팩트에 붙이지 마라(`▣VVV2`).
3. 🔴🔴 **`BLOCKING-1` 판단** — 전제가 소멸했으므로 고아 규명 후 `error` 승격 여부 결정(`▣VVV3 B`).
4. 🟠 **`A.7 REMOVE`** — 전제 2건 충족 · 승인 대상 · GOLD 소비 검증 후 권고(`▣VVV4`).
5. 🟠 사람·현업 7건 / 계정 2건 / 에이전트 5건(`▣VVV5`).
6. 🔴 **판정식 3조를 읽고 시작하라**(`▣VVV6`) = 권한 검증 · 컴퓨트 생존 · 차원 행수.

_Co-authored with CoCo_

> #### 🟢 [2026-08-29 O116] WARN 36건 정본화 + 미규명 최대 2건 규명 완료 + 판정식 결함 1건 적발
>
> **범위** = `▣VVV3`(WARN 36건 분류·규명) 주작업 + `▣VVV3 B`(`BLOCKING-1` 고아 정체) 부수 1축.
> `R4-4-2 ㉡` 적용(지시 동봉 ⇒ 브리핑 후 즉시 착수) · 확정위반 **0**.
> 🔴 `DEC-44`(`▣VVV2`)는 **현업 회신 대기**라 손대지 않았다 · `A.7 REMOVE`(`▣VVV4`)는 **승인 대상**이라 미실행.
>
> **① 🔴🔴 판정식 결함 1건 자력 적발 — 인수인계의 재현법이 그대로는 `0건` 을 낸다**
> `▣VVV3` 이 준 `grep -oE "WARN …" logs/dbt.log` 를 실행하니 **0건**이 나왔다.
> 🟢 **「없다」로 결론내지 않고 분모·판정식을 먼저 의심**했다(`O111 ㉠`) ⇒ 원인 = 로그에 ANSI
> 제어문자가 있어 **BusyBox `grep` 이 binary 로 판정**하고 `-o` 출력을 버린다. **`-a` 를 붙이면 36건**이다.
> 🔴 **파일은 정상이고 판정식이 틀렸다** — `O111 ㉠` 의 실물 사례가 하나 더 늘었다.
> ⇒ `▣VVV3` 의 재현법을 **교정해 병기**했다(`-a` + 회차 분리 2단계).
>
> **② 🔴 회차 혼입 발견 — `dbt.log` 한 파일에 회차가 10개 있다**
> 종전 재현법은 파일 전체를 세므로 **다른 회차의 WARN 이 섞인다**(같은 파일에
> `WARN=33 TOTAL=447` 등 실재). ⇒ 회차 경계(`Running with dbt=`)로 분할해
> **채택 회차 = 줄 28,999~57,198 · `Done. PASS=425 WARN=36 ERROR=0 SKIP=0 TOTAL=461`**
> 을 특정하고, 그 블록의 **WARN 출현 36 / 고유 테스트 36**(1:1)을 확인해 분모를 확정했다.
>
> **③ 🟢 정본화 완료 — `_o116_warn_census.md` 신설**(근거철 · 272줄)
> §1 판정식 결함 · §2 **36건 전수표**(건수·계열) · §3 **계열 분류 2축** · §4 누락 적발 ·
> §5·§6 미규명 2건 규명 · §7 `BLOCKING-1` 형태 축.
> 🔴 `logs/dbt.log` 는 회전되므로(`.1`~`.5` 실재) 이 파일이 이제 그 목록의 정본이다.
>
> **④ 🔴 `▣VVV3` 전수표 누락 1건 적발** — A+B+C 표가 담은 테스트는 **35**였고 실측은 **36**이다.
> 누락 = **`not_null_CRM_PAYMENT_BILLING_MBER_NO` 5건**. 같은 표에 관계 고아 309 는 있는데
> **NULL 축이 빠졌다**(두 테스트는 다른 축이다) ⇒ C 표에 행을 보완했다.
> 🟢 부수 = 「예산 3 / MEMBER_DK 13 / 채움률 20」이 **표 행 수**였음을 밝히고
> **테스트 수 = A 3 / B 14 / C 19**(합 36)를 병기했다(`O111 ㉢` — 판정 축과 관측 축 분리).
>
> **⑤ 🟢🟢 미규명 ㉠ 규명 완료 — `not_null_CRM_SEND_MEMBER_SEND_RESULT_NAME` 1,401,419**
> 분모(라이브 실측) = 총 **38,467,887**행 · **테스트 분모 26,252,471**(코드 보유 · `where` 절)
> ⇒ **5.34%** · 총행 대비 3.64% · 라벨 커버리지 **94.66%**. 🟢 **WARN 값이 정확히 재현**됐다.
> 🔴 **「채움률 결손」으로 읽으면 틀린다** — 테스트에 `where: SEND_RESULT_CD is not null` 이 걸려
> 있어 세는 대상은 **「코드는 있고 사전 라벨이 없는 행」**이다. `EMAIL`·`PSTMTR` 은 모델이
> `CAST(NULL)` 로 두는 **구조적 부재**이고 `where` 가 이미 배제한다(채널별 합 = 1,401,419 정확 일치).
> **원인 17종이 2군으로 갈렸다** = ㉠ `SND` 단자리 코드(`0,3,4,5,6,8,9`)가 `MS056` 에 미등재
> **824,030**(58.8%) ㉡ 4자리(`7319·7320·7321·7205·9034`)가 **사전에 아예 없음** **577,389**(41.2%).
> 사전 실측 = `MS056` 단자리는 **`1`·`2` 만** · `MS057` 은 `7318`·`7322` 는 있고 **`7319`~`7321` 결번**.
> ⇒ 🟢 **판정 = 코드사전 미등재(데이터 실상) · 테스트도 모델도 낡지 않았다** ⇒ warn 유지 정당 ·
> 🔴 라벨 창작 금지(`DEC-17-B`·`R2-7-1`) · 사전 추가는 현업 소관(제시할 코드값 목록 확보).
> 🔴 **자기경계 1건** = 초기 조회에서 단자리 코드가 `MM294`·`RM004` 등 **타 코드군에 실재**한다고
> 나왔지만, 단자리는 **여러 코드군이 공유하는 일반 연번**이라 라벨 후보가 아니다.
> 「사전에 있다」와 「이 코드군에 있다」는 다른 축이다 ⇒ 그 관측을 판정 근거로 쓰지 않았다.
>
> **⑥ 🟢🟢 미규명 ㉡ 규명 완료 — `relationships_CRM_EVENT_PARTICIPATION_EVENT_KEY` 263,611**
> 분모 = 참여 총 **1,134,126**행 ⇒ **23.24%**(yml 주석 「23%」와 일치) · 고유 고아 키 **53**(직접 실측).
> 🔴 **고아의 99.5%가 키 2개**다 = `EVENT_105` **195,544**행(고유회원 148,446) +
> `EVENT_106` **66,642**행(41,738) · 둘 다 참여기간이 **2022-10-16 ~ 2026-07-01**(4년).
> 원천 직접 실측(`R2-3`) = `BRONZE_CRM.TM_MS_EVENT` **376**행 · `EVENT_CD` **전건 수치·유일** ·
> 범위 1~431 ⇒ **결번 55개** · 🔴 **`105`·`106` 은 0건**이고 인접 `100~104`·`107~110` 은
> **이름·기간까지 정상 보유**한다. ⇒ 🟢 **판정 = 원천 마스터 결번**이고 배선·키 생성식
> (`'EVENT_'||EVENT_CD`)은 정상 ⇒ yml 주석의 *"키체계 미확정"* 은 **기각**된다.
> 🟠 사람 소관 1건 신설 = 「`EVENT_105`·`106` 이 무엇이었고 왜 마스터에서 사라졌나」 —
> 4년에 걸친 상시 채널로 보이고 **전 참여의 23.1%가 이름 없는 행사에 묶여 있다.**
> 🔴 마스터 2행이 들어오면 **23%가 일괄 해소**되므로 회신 전 `error` 승격 금지(`BLOCKING-2` 유지).
>
> **⑦ 🟢 부수 — `BLOCKING-1` 고아 정체의 3지선다 중 ㉢ 배제**(`▣VVV3 B`)
> `CRM_SEND_MEMBER.MBER_NO` 고아 31,486 의 형태 분해 = **7자리 정상 31,089행(98.7%) · 고유 7,431**
> ⇒ **형식 오류는 1.3% 미만** ⇒ 🟠 **㉠(탈퇴·삭제분) 가설 유력**.
> 🔴 확정에는 「탈퇴·삭제 회원 원천」 대조가 필요하고 그 원천의 이 계정 실재가 미확인이다
> ⇒ **`error` 승격 보류 유지**(`▣VVV3 B` 순서 준수 — 규명 전 승격 금지).
> 🔴🔴 **신규 발견 = 원천 오염**: `MBER_NO` 자리에 **한글 인명**(`김근영`·`최혜원` 등)이 든 행이 있다.
> 종전 문서에 없던 축이고 원천 정정 사안이다 ⇒ 문서20 질의 후보로 등재했다.
>
> **⑧ 게이트·규약** — 착수 시 `id_collision_gate --next O`(정의 115·참조 115) 후 원장 §1 **선점 등재**
> (`R1-4-3` 5세션 연속). `line_len` 🟢 · `index_row_gate` **행 유실 0 · 중복 증가 0** ·
> 허브 재발행 2건 모두 `--expect` **영속성 가드 통과**(`R1-6-23`) · 조각 상한 초과 0.
> 🔴 원장 §1 행은 **1,900자 이내 초안 우선** 원칙을 적용했다(`O112 ㉢` 재발 방지 — O115-C 가 2,217자로 어겼다).
>
> **⑨ 🔴 남긴 것 (다음 세션)** = ㉠ `▣VVV3 C` 잔여 미규명 3건(`SPNSR_BSNS_ID` 18,091 ·
> `SNDNG_KEY` 11,313 · `MK_CMPGN_NM` 7,052) ㉡ `BLOCKING-1` 탈퇴·삭제 원천 대조(계정 소관 확인 선행)
> ㉢ 신설 현업 질의 3건(`EVENT_105/106` 정체 · `SND.CALL_STATUS` conformed 여부 · 한글 인명 오염)
> ㉣ `DEC-44` 회신 대기(불변) ㉤ `A.7 REMOVE` 승인 대기(불변).
