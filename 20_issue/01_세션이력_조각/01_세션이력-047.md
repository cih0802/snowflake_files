<!-- SPLIT-CHUNK 01_세션이력.md | 047/047 | 허브 = 01_세션이력.md | 원문 1~99행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-29 O118-B] 자기검토 — SV 전용 게이트 미실행 적발 · 실질 결함 3건 · 내 오단정 2건 정정
>
> **지시** = 「이번 세션 작업을 비판적으로 검토 + 승인이 필요한 작업 전건 승인 + 작업·문서 개선」.
> 🔴 **O118 초판을 그대로 두지 않았다** — 아래 ②③④ 는 **내가 틀린 것**이고 원장·정본에 병기했다.
>
> **① 🔴🔴 최대 절차 결함 — 세션 주제가 SV 인데 SV 전용 게이트를 하나도 돌리지 않았다**
> 미실행 = `scripts/sv_unit_gate.py` · `sv_identifier_gate.py` · `agent_tool_claim_gate.py` · `sv_code_label_gate.py`.
> 돌리니 **3종 전부 FAIL** 이고 **실질 결함 3건**이 나왔다(아래 ②③④).
> 🔴 **O118 은 문서·조각 게이트 6종이 전부 🟢 인 것을 근거로 「검증 완료」 톤으로 보고했다** —
> 그것이 정확히 `R3-9`(게이트가 보지 않는 축)의 실물이다. **게이트가 없어서가 아니라 있는 게이트를 안 돌렸다.**
> ⇒ 🟢 **판정식 신설** = **착수 시 `ls scripts/` 로 주제 영역 전용 게이트를 확인하라.**
>   **문서 게이트 통과는 도메인 게이트의 대체물이 아니다.** 🔴 이 판정식이 O118 에 있었으면 3건을 초판에 잡았다.
>
> **② 🔴🔴 `sv_identifier_gate` — Agent 스펙이 미배포 ML SV 7종을 도구로 참조한다**
> `tool_resources` **26건 중 7건** 부재 = `SV_ML_MEMBER_RISK`·`SV_ML_SPONSOR_RISK`·`SV_ML_FEE_FORECAST`
> (`AGENT_MEMBER`) · `SV_ML_DVLP_FORECAST`·`SV_ML_LTV_FORECAST`·`SV_ML_LTV_SCORE`·`SV_ML_FEATURE_IMPORTANCE`
> (`AGENT_OVERALL`). 게이트 문구 = *"미배포 드리프트(오기가 아니라 재배포 미이행)"*.
> 🟢 이 7종이 정확히 `22_ML_SV_DDL.sql` 의 「SV 7종」 ⇒ **§0.9-G 축C 분류(ML 은 다른 축)가 실측으로 확증**됐다.
> 🔴🔴 **함의 = O118 의 「Agent 배포 순서」가 불완전했다.** 초판은 선행조건으로 `DEC-44` **하나**만 들었다.
> 실제로는 **2개**다: ㉠ `DEC-44` 종결 ㉡ **ML 축 배포**(`21_ML_SERVING_뷰_DDL`·`22_ML_SV_DDL`·`ML` `USAGE`).
> ⇒ **지금 Agent 를 배포하면 도구 7개가 죽은 채로 산다.** 정정 = 문서30 **§30-G ㉣** · `04` **§0.9-H ㉠**.
> 🔴 **두 결함은 독립이다** — `DEC-44` 를 닫아도 ML 축을 건너뛰면 Agent 품질은 보장되지 않는다.
>
> **③ 🔴 `sv_unit_gate` — 라이브 SV COMMENT 에 수치 7건 (규약 직접 위반)**
> `05_0` §공통규약 **COMMENT 작성 규약 (1)**(수치 금지 · **2026-07-31 제정** · 근거 = 04 §6.9-(8))을 위반한다:
> `SV_MEMBER_SPONSOR_BIZ` **6곳**(`2,170,572` 행수 · `1,687,546`/`483,028` · `0.01%`/`137건` · `50행` · `10,000`
> · 최상위 `0.01%`/`28건`) · `SV_MEMBER_MONTHLY` **1곳**(최상위 `0.01%`).
> 🔴 **`05_10` 은 2026-08-21 작성 = 규약 제정 이후에 위반한 채 만들어졌고, 게이트가 있는데 아무도 안 돌려서 통과했다.**
> 🟢 **DDL 원본 시정 완료** = `05_10` 6곳 · `05_1` 1곳. 🔴 **`10,000` 은 통계가 아니라 CONF-2 단위 환산 상수**여서
>   삭제하지 않고 **리터럴만 제거**했다(「만원 단위로 환산」) — 규약 의도와 지표 의미를 둘 다 지켰다.
> 🔴 **재배포는 미실행** = ㉠ 측정 시점에 `dbt build` 가 라이브에서 돌고 있었다(아래 ⑤) ㉡ **Agent 가 0 이라
>   이 COMMENT 를 소비하는 주체가 아직 없다** ⇒ 긴급도 낮음. **승인은 받았으나 위험 대비 이득이 없어 미뤘다.**
>   ⇒ 🔴 지금은 **「DDL 원본 = 규약 준수 / 라이브 = 위반」 드리프트**이며 그 사실을 §0.9-H ㉣ 에 명시했다.
>   🟢 닫는 법 = 빌드 종료 후 `05_1`·`05_10` 을 `GN_DW_ADMIN` 으로 실행(`CREATE OR ALTER` 이므로 GRANT·소유권 보존)
>   → `sv_unit_gate` 검출 **0** 확인.
>
> **④ 🔴🔴 `agent_tool_claim_gate` — Agent 스펙 간 「가능/불가」 모순 1건**
> `SV_MEMBER_SPONSOR_BIZ` 의 **「후원사업별」** 이 `AGENT_MARKETING`(불가) ↔ `AGENT_MEMBER`(가능) 로 갈린다.
> 🟠 advisory = 문안 이격 2건(`SV_MEMBER_MONTHLY` 0.420 · `SV_SERVICE` 0.328).
> 🔴 **고치지 않았다** — 어느 쪽이 옳은지는 업무 판정이고(실측상 **약정 grain 에서는 가능**하다)
> Agent 스펙 문안 확정은 `08_AGENT_spec.md` 소관이다 ⇒ 다음 세션 과제로 등재(§0.9-H ㉢).
>
> **⑤ 🔴🔴 내가 실측 없이 라이브 COMMENT 를 베껴 §0.9-C 를 틀리게 썼다**
> 초판 = *"`SV_MEMBER_MONTHLY` 의 CAMPAIGN·SPONSORSHIP·PAYMENT 3종 미포함 — FMM 은 회원×월 1행이라
> 붙이면 grain 이 깨진다"* ⇒ **세 컬럼에 하나의 이유**. 실측하니 **이유가 둘**이었다:
> · `CAMPAIGN_SK` 비-0 **0** · `PAYMENT_SK` 비-0 **0**(분모 40,054,883) ⇒ **전건 0 = 적재 대기가 맞다.**
> · `SPONSORSHIP_SK` 비-0 **35,785,665 = 89.34%** ⇒ **적재 문제가 아니다.** 진짜 이유는 **다중 후원 접힘** =
>   회원 **20.83%(330,308 / 1,585,945)**가 후원사업을 복수 보유(최대 3,201)하므로 단일 SK 로 후원사업별 금액을
>   분해하면 **한 사업에 전액이 잘못 귀속**된다. 🔴 **fan-out 이 아니다** — m:1 조인이라 행은 늘지 않는다.
> 🔴 **근거를 「라이브 SV COMMENT」에서 베꼈고 채움률을 재지 않았다**(`R2-3` 원본 실측 · `R1-3-7` 대체 근거 금지 위반).
> ⇒ 🟢 **판정식 신설** = **「제외 이유」를 문서·COMMENT 에서 베끼지 말고 그 컬럼의 채움률을 재라.**
>   **0 이면 적재 대기 · 0 이 아니면 의미(귀속) 문제** — 처방이 다르다.
> 🔴 **라이브 COMMENT 자체도 같은 오류를 갖고 있었다** — *"「적재 대기」가 아니라 grain 문제였다"* 고 단정하는데
>   `PAYMENT_SK` 는 **전건 0 = 적재 대기가 맞다** ⇒ 두 축을 한 문장에 묶어 한쪽을 거짓으로 만들었다.
>   🟢 `05_1` DDL 원본에서 **두 축을 갈라 병기**했다(취소선 + 정정 · 재배포 대기).
> 🟢 **부수 발견(판정 아님)** = 한 회원의 후원사업 최대 **3,201건** — 법인·집계 계정이거나 오염일 수 있다.
>   🔴 **판정하지 않고 관측으로만 남긴다**(다음 세션 확인 대상).
>
> **⑥ 🔴🔴 측정 중에 라이브가 변경되고 있었다 — 재현성이 깨지는 환경이었다**
> `QUERY_HISTORY` 실측 = `insert into GN_DW.GOLD.FACT_MEMBER_MONTHLY` **RUNNING**(07:21:59 시작) ·
> `FACT_GA_BEHAVIOR` **RUNNING** · GOLD WIDE 뷰 다수 **07:22 재생성** ⇒ 사용자가 `dbt build` 를 실행 중이었다.
> 🔴 **그래서 같은 객체가 두 조회 사이에 바뀌었다**(직접 `COUNT(*)`):
> `WIDE_MEMBER_FEE` **0 → 40,262,076** · `FACT_MEMBER_MONTHLY` **40,054,883 → 0**.
> 🔴🔴 **나는 그 0행을 보고 「`SV_MEMBER_FEE`(dim 23·metric 8)가 아무것도 답하지 못한다」는
>   거짓 결함을 발행할 직전까지 갔다.** 🟢 살린 것 = **재조회 + `QUERY_HISTORY` 로 원인 축 확인**(`O111 ㉠`).
> ⇒ 🟢 **판정식 2조 신설**:
>   ㉠ **「0행」을 결함으로 단정하기 전에 빌드 진행 여부를 보라**(`execution_status='RUNNING'`).
>   ㉡ 🔴 **`INFORMATION_SCHEMA.TABLES.ROW_COUNT` 를 행수 근거로 쓰지 마라 — 양방향으로 stale 이었다**
>     (`FACT_MEMBER_MONTHLY` 를 0 으로, `FACT_MEMBER_FEE` 를 40,262,076 으로 보고 · 둘 다 `COUNT(*)` 와 불일치).
>     **행수는 `COUNT(*)` · 금액은 `SUM(<실컬럼>)`.**
> 🟢 **핵심 발견은 빌드 후 재검증에서 전건 재현됐다** — `SV_BUDGET.TOTAL_EXEC_BUDGET` = `FACT_BUDGET SUM`
> = `FACT_BUDGET_YEARLY SUM` = **99,006,005,048** ↔ BRONZE 최신차수만 **55,094,546,653**
> ⇒ 🔴 **`DEC-44` 판정만은 환경 변동에 무관하다**(BRONZE + `FACT_BUDGET` 이 둘 다 안정).
> 🔴 **재측정 대기로 표기**(`R2-8-4-c`) = `FMM.*_SK` 채움률 분모 · `WIDE_AD_COMBINED` 행수 · `SV_AD` 광고비 절대값.
> 🟢 **판정(전건 0 인가/채움이 있는가)은 바뀌지 않을 성질**이지만 **분모 숫자는 빌드 후 재측정**하라(§0.9-I).
>
> **⑦ 🟢 DDL ↔ 라이브 대조를 처음으로 실제로 했다 (O118 은 `05_0`·`05_6` 만 읽었다)**
> `05_1` 을 읽어 `TABLES` 절 **4**(fmm·month·member·reason) · `RELATIONSHIPS` **3** 을 확인 ⇒ **라이브와 정확히 일치**.
> ⇒ 🟢 **`04_SV_설계.md` §1.1 의 「7」만 stale 이고 DDL 은 옳다** — O118 의 축① 분류는 **방향이 맞았고 이유가 틀렸다**.
> 🟠 **여전히 미독** = `05_2`·`05_3`·`05_4`·`05_5`·`05_7`·`05_8`·`05_9`(7파일) ⇒ 그 SV 들의 DDL↔라이브 일치는
>   **개별 확인하지 않았다**(`sv_unit_gate` 가 GRANT·단위·폐기식 축으로 간접 확인한 것이 전부다) ⇒ 다음 세션 과제.
>
> **⑧ 🟢 게이트·검증 결과**
> 문서 게이트 = `doc_census` ✅(stale 0 · 분할 분모 위반 0) · `doc_type` 🟡(경고 1 = `00_INDEX` 여유 <20%) ·
> `clause_order` ✅(조문 96 · 역전 0) · `index_row` ✅ · `doc_heading` ✅ · `doc_coord` 🟡(경고 1).
> SV 게이트 = 🔴 3종 FAIL(위 ②③④ · **DDL 원본은 시정했고 라이브 재배포가 남아 FAIL 유지**).
> `line_len` PASS(전 편집 파일) · 원장 행 **1,924 / 1,834자**(초과 2줄을 압축해 해소).
> 🔴 **`read` 미반환 = 0 · 재호출 = 0** · 🔴 **dbt 명령 미실행**(`R4-1` — 사용자가 이미 실행 중이었다).
>
> **⑨ 🔴 자기결함(이 회차)**
> ㉠ **제목 파괴 2회째**(`R1-7-8`) — `§0.9-G` 헤딩을 `edit` 로 삼켰다. 즉시 복구 + **절 순서(F→G→H→I) 재배열** ·
>   `doc_heading_gate` 제목 유실 **0** 으로 확증. 🔴 **한 세션에 같은 유형을 두 번 냈다** = 앵커에 다음 제목줄을
>   포함시키는 습관이 아직 안 잡혔다(`R1-7-8` 이 요구하는 「직전 줄까지」를 반대 방향으로 어겼다).
> ㉡ 🟠 **승인을 받고도 미집행한 것 3건**(SV 재배포 · Agent 모순 · ML 축 배포) — **근거를 각각 명시**했다.
>   🔴 「승인받았으니 다 한다」가 아니라 **위험·이득·순서**로 판단했고 그 판단을 문서에 남겼다.
>
> 🔴 **정본 = `04_SV_설계.md` §0.9-C·§0.9-H·§0.9-I·§1.1 · 문서30 §30-G ㉣ · `99_NEXT ▣YYY7` · 원장 §1 `O118-B`**
> 🔎 **부속 근거철** = `_o118_sv_live_census.md`(O118 관측 · 정본 아님)

> #### 🟢 [2026-08-29 O119] SV COMMENT 규약 위반 종결 · SV 게이트 분모 미달 적발 · 게이트 오탐 1건 시정

**지시** = ① SV COMMENT 규약 위반 재배포 ② Agent 배포 선행조건 정리 ③ Agent 스펙 모순 1건
④ `04_SV_설계.md` 문서 결손 절 ⑤ DDL↔라이브 대조 미독 7파일 ⑥ §9 Cortex Search 재검증.
**적용 분기** = `R4-4-2 ㉡`(지시 동봉) — 브리핑 출력 후 즉시 착수. **확정위반 0건.**
`read` 미반환 **0건** / 재호출 **0건**.

**① 🟢 지시 ① 종결 — `sv_unit_gate` FAIL → PASS**

착수 기준선 = `sv_unit_gate` **FAIL** 이고 원인은 **COMMENT 수치 검출 7건 단독**이었다
(나눗셈 단위 위반 **0** · GRANT 누락 **0** · 폐기식 노출 **0** · 필수 문안 소실 **0** · 분모 SV **17종**).
귀속 = `SV_MEMBER_MONTHLY` **1건** + `SV_MEMBER_SPONSOR_BIZ` **6건** ⇒ O118-B 기재와 일치.

🟢 **배포 전에 예측 검사를 먼저 했다** — `sv_unit_gate` 의 `NUM_BAN`·`NUM_EXEMPT` 를 **import** 해
(패턴을 복제하지 않았다 · `R3-9 ㉡`) 두 DDL 의 **SQL 주석이 아닌 구문 줄의 문자열 리터럴만** 재검하고
「재배포하면 검출 0건」을 단정한 뒤 배포했다. 실측 결과가 예측과 일치했다.

배포 = `scripts/deploy_sv.py` 로 `05_1`(DDL 8) · `05_10`(DDL 7). 판정 = `sv_unit_gate` **PASS**
(COMMENT 수치 **7 → 0**). 보존 확인 = `SHOW SEMANTIC VIEWS IN ACCOUNT` **17행** · owner 전건
`GN_DW_ADMIN` · `created_on` **05:59 그대로**(= `CREATE OR ALTER` 가 소유권·GRANT·생성시각을 보존한다).
⇒ O118-B 가 명시한 **「DDL 원본 준수 / 라이브 위반」 드리프트가 종결**됐다.

**② 🔴🔴 최대 발견 — 인수인계의 「SV 게이트 3종」이 분모 미달이었다**

O118-B 가 신설한 판정식은 「착수 시 `ls scripts/` 로 주제 영역 전용 게이트를 확인하라」였다.
🔴 **그것을 지켰는데도 같은 유형의 결함이 다시 나왔다** — O118-B 가 말한 「SV 게이트 3종」은
**그 세션이 우연히 돌린 것들의 집합**이었고 **도구 인벤토리가 아니었다.** 실제 SV 축 게이트는 **4종**이고
빠진 것이 **`sv_code_label_gate`** 였다. 돌리니 **FAIL 24건**(코드값부재 2 · 종수 6 · 열거누락 16)이었다.

🟢 **판정식 신설** = **「게이트를 돌렸는가」와 「돌릴 게이트를 다 셌는가」는 다른 질문이다.**
처방이 「목록을 확인하라」이면 **그 목록의 출처**를 보라. `R3-9`(게이트가 보지 않는 축)의
**2세션 연속 실물**이다.

**③ 🔴🔴 0행 무증상 오답 경로 2건 — 라이브로 확증**

㉠ `SV_BUDGET.BUDGET_CATEGORY` COMMENT = 「실제값 2종: '수입'·'지출' + NULL」.
  라이브 실측(`GOLD.DIM_BUDGET_ITEM`) = **`지출` 179행 + NULL 1행 = 180행** ⇒ **`'수입'` 은 존재하지 않는다.**
  Agent 가 「수입 예산」에 `BUDGET_CATEGORY='수입'` 을 걸면 **0행을 사실로 답한다**(`O58-C` 계열).
  🔴 「수입 예산이 왜 없나」는 **적재 축 질문**이고 미규명이다 — 값을 창작하지 말고 그 사실을 답해야 한다.
㉡ `SV_MEMBER_COHORT.CAMPAIGN_INFLOW_PATH` COMMENT = 「실제값 16종」.
  라이브(`GOLD.FACT_MEMBER_COHORT.ACQ_MBER_INFLOW_PATH_NM`) = **12종 + NULL**.
  선언에만 있고 라이브에 없는 것 **7종** = `회원 온라인개발`·`회원 콜개발`·`기업`·`회원 기타`·
  `마케팅콜개발`·`회원 오프라인개발`·`직원개발` / 라이브에만 있고 미열거인 것 **3종** =
  `콜개발`·`기타 기업개발`·`오프라인`.
  🔴 **원인은 결손이 아니라 MM293 라벨 개칭**이다(`회원 콜개발`→`콜개발` 등) ⇒ **양방향으로 위험하다**:
  없는 값으로 필터하면 0행이고, 새 값이 미열거라 Agent 가 그 채널을 누락한 목록을 낸다.

**④ 🟢 blocking-class 8건 전건 종결 (코드값부재 2→0 · 종수 6→0)**

사용자 승인 = 「지금 라이브 실측으로 시정 후 재배포」. 6차원을 **라이브 distinct 실측**으로 교체했다 =
`SV_BUDGET.BUDGET_CATEGORY`(2종→**1종**) · `SV_AD.PAGE_TYPE`(1→**2** · `전체` 신규) ·
`SV_AD.AD_GROUP_NM`(13→**14** · `na_veteran26` 신규) · `SV_MEMBER_COHORT.CAMPAIGN_INFLOW_PATH`(16→**12**) ·
`SV_MEMBER_COHORT.DOMESTIC_OVERSEAS`(3→**4** · `전체사업` 신규) · `SV_MEMBER_EVENT.DOMESTIC_OVERSEAS`(3→**4**).
재배포 = `05_2`·`05_3`·`05_6`·`05_7`. 판정 = 위반 **24 → 16**(전부 🟠 열거 누락) · **blocking-class 0** ·
`sv_unit_gate` **회귀 0**(여전히 PASS).

🔴 **`05_6` 은 O118 이 「보류」 선언한 파일이라 특별 절차를 밟았다** — 재배포는 라이브를 파일로 덮으므로
덮기 전에 `DESC SEMANTIC VIEW GN_DW.SERVING.SV_BUDGET`(76행)으로 **metric 3 · relationship 2 ·
dimension 5 · table 3 · SV COMMENT · `AI_SQL_GENERATION` 전건 일치**(드리프트 **0**)를 확인했다.
고친 것은 차원 COMMENT 열거 하나뿐이고 **차수 필터·`where`·metric 삭제는 0** ⇒ `DEC-44` 무영향.
🟢 **판정식 신설** = **라이브를 덮는 재배포 전에 `DESC` 로 드리프트를 먼저 재라** —
「파일이 정본이다」와 「라이브가 파일과 같다」는 **다른 주장**이다.

**⑤ 🔴🔴 지시 ③ 은 실행하지 않았다 — 전제가 틀렸다 (승인받은 지시를 거부)**

사용자가 「`AGENT_MARKETING` 의 「후원사업별」을 **가능**으로 시정」을 승인했다. 문안을 고치기 전에
`agent_tool_claim_gate.stance()` 의 **판정 근거 문장을 그대로 재현**했더니 결론이 뒤집혔다.

- **두 스펙 모두** 첫 등장 문장에서 「캠페인별/**후원사업별** 활동회원 수의 **정본 도구**다 …
  대표캠페인 규칙으로 **분해 가능하다**」로 **가능**을 명시한다 ⇒ 문장 [5] 에서 둘 다 **+1**.
- **두 스펙 모두** 뒤에 「**후원사업별** 합계는 … N **비가산** · SUM **금지**」라는 **가산성 경고**를 단다.
- 판정이 갈린 **유일한 원인 = YAML 물리적 줄바꿈 위치**다.
  `AGENT_MARKETING` 은 `SUM 금지`·`말 것` 이 「후원사업별」과 **같은 물리 줄**이라 문장 [29] 에서 **-1**,
  `AGENT_MEMBER` 는 줄바꿈이 `전체 활동회원수보다` 뒤에 떨어져 그 단서가 문장 [27] 에서 **빠져** 최종 **+1**.
  (이 게이트는 파서 결함에 죽지 않도록 **의도적으로 yaml 파서를 쓰지 않으므로** `\n` 이 문자열에 남고
  `stance()` 의 문장 분해 정규식이 그것을 문장 경계로 썼다.)

⇒ 🔴 **스펙을 고치면 유효한 가드(비가산 metric 을 SUM 하지 말라)를 파서 인공물 때문에 지우는 일**이 된다.
근거를 제시하고 **재질문**했고 사용자가 **「게이트를 고친다 — 줄바꿈 정규화」**를 택했다.
🟢 시정 = `stance()` 에 `re.sub(r'\n[ \t]+', ' ', desc)` **1줄**(YAML folding 과 같은 효과) ⇒
`agent_tool_claim_gate` **PASS**(blocking 모순 **1 → 0**) · **검사 쌍 6 → 7**(분모 확대).
🟢 **음성 테스트 신설** = `scripts/test_agent_tool_claim_gate.py`(**7단정** · 5축).
  🔴 **축2 가 핵심이다** — 시정 전 구현을 `legacy_stance()` 로 보존해 **줄바꿈만 옮긴 두 판본이
  갈렸음(-1 ↔ +1)을 실증**한다. 정상 입력만으로는 이 결함이 0건으로 보인다.
⚠️ **남은 한계(의도적)** = 접은 뒤 두 스펙은 **함께 -1** 이 된다. 모순은 사라지지만 `stance()` 는
  「가산성 경고」와 「축 자체가 불가」를 여전히 구별하지 못한다. 분리안(`ADDITIVITY_CUES` 를 NEG 에서
  제외)을 선택지로 제시했으나 **사용자가 줄바꿈 정규화만 택했다** ⇒ 임의 진행하지 않았다.

🟢 **판정식 신설 2조** = ㉠ **게이트 FAIL 도 오탐일 수 있다 — 생산물을 고치기 전에 판정 근거 문장을
재현하라**(`▣XXX6 ㉦` 의 **역방향**. 종전 조문은 「0건」과 「무엇을 세는가」만 의심하게 했다)
㉡ **승인받은 지시라도 전제가 실측과 어긋나면 실행하지 말고 근거를 들어 되물어라**(`R1-2`).

**⑥ 🟢 지시 ⑥ 부분 종결 · 지시 ⑤ 진척**

- `SHOW CORTEX SEARCH SERVICES IN ACCOUNT` = **0행** ⇒ `▣YYY4 ㉢` 의 「service 실재 미측정」이 닫혔다.
  **계정에 Cortex Search service 는 실재하지 않는다.**
- `DIM_BUDGET_ITEM` **180행** 재확인 ⇒ 인수인계의 「2,041 → 180」과 일치(§9 후보 재검증의 근거 1건 확정).
- **전량 독해** = `05_1`(200줄) · `05_10`(151줄) · `05_2`(210줄) · `05_3`(239줄) · `05_6`(103줄) ·
  `05_7`(235줄). 🔴 **잔여 미독 = `05_4`·`05_5`·`05_8`·`05_9`.**
  독해 고유 토큰(대조용) = `TOTAL_PAID_FEE_BILLABLE` · `fmsb_to_sponsorship` · `(8-3) 차원 조인 스모크` ·
  `(C-5) 이탈 사유 분해` · `상이 0 / 동일 23` · `(8-9) 미노출 metric 확인`.

**⑦ 🟠 미착수·잔여**

- **지시 ④ 문서 결손 절 미착수** = `04_SV_설계.md` 의 하위 4 SV + ML SV 7종 구조 절.
- `sv_code_label_gate` **🟠 열거 누락 16건**(ML 7 + 실적 계열 9 · advisory · 승인 범위 밖이라 손대지 않았다).
  🟢 가장 값싼 것 = `SV_MEMBER_EVENT.CAMPAIGN_INFLOW_PATH`(형제 축 열거를 이미 확정했다 · `05_2` 주석에 좌표).
- 🔴 **신규 발견 = Agent 스펙 description 에 `0.01%` 수치가 살아 있다**(양 스펙). O118-B 가 SV COMMENT 에서
  제거했지만 **Agent 스펙은 다른 표면**이라 `sv_unit_gate` 분모 밖이다(`P194` 형제 표면 계열).
  규약 (1) 의 취지는 「Agent 가 인용하는 문안에 적재량을 박지 마라」이고 **스펙 description 이 정확히 그 문안**이다.
- **Agent 2종 미배포** — 선행조건은 `DEC-44` **하나**이고 그것은 사람 결정이다(`R4-4-3` 별도 승인 대상).

**⑧ 🟢 문서·게이트 정리**

원장 §1 = 착수 시 **선점 등재** → 종료 시 **결과 행으로 대체**(1,898자 · 상한 이내 · 행 유실 0).
🔴 행이 2,001 → 1,966 → 1,929 → **1,898** 로 세 번 깎였다(`O112 ㉢` 재현) — 가드가 초과분을 **쓰지 않고 중단**했다.
`99_NEXT` = **§0-ZZZ 신설**(ZZZ1~ZZZ7) + §0-YYY **취소선 승계 표기**. 🔴 조각 001 이 **정확히 300줄 상한**에
닿아 삽입 전 상한을 사전 단정하는 가드를 두고, 재발행에서 301줄이 나오자 **한 줄을 병합**해 맞췄다
(재균형을 쓰지 않았다 = 승인 대상 회피). 제목 게이트 유실 1건은 **취소선 승계의 정상 관례**이므로
실유실 0 을 확인하고 `--reason` 을 달아 골든을 발행했다.
게이트 = 문서 6종 전건 통과(`doc_type_gate` 🟡 여유 경고 1) · SV 4종 중 **3종 PASS · 1종 🟠 advisory** ·
음성 테스트 **16종 전건 통과**(신설분 포함) · `doc_census` stale **0** · `decision_closure_gate` **34건 승계**(불변).
🔴 **파괴 가능 작업 0** — `--rebalance`·`retire_rows`·`rm`·`DROP`·`CREATE OR REPLACE` 를 쓰지 않았다.
