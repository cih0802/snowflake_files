<!-- SPLIT-CHUNK 01_세션이력.md | 073/073 | 허브 = 01_세션이력.md | 원문 1~173행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-17 O170-C·D·E] 문서 4층 구조 확정(`R1-6-26`) + 선택표 사이드카 분리 + 승인 불요 4건 판정 완결 + 승계 절 은퇴 44/63 + `SV_AD` COMMENT 열거 제거
>
> **지시** = ① *"O154-B가 적어둔 근본 2안 = 인수인계 절 은퇴(retire_sections.py)를 승인"* ②
> *"우리 소관·승인 불요 4건 착수"* ③ *"결정·설계 대기는 하나씩 물어보고 작업"* ④
> *"99문서는 어떤 구성인거지? … 문서 로직을 빠삭하게 세워서 계속 늘어날 문서 관리 잘 할 수 있도록"*
> ⑤ 자기검토 4단계(트랜스크립트 추출 → 제네레이터 최신성 → 비판적 검토 → 결함 보완).
> 🔴🔴 **정본 = `20_issue/_o170_evidence.md`(§E1~§E8)** · 이 항목은 경위다.
>
> ##### 1. 🔴🔴 승인된 처방(㉡ 절 은퇴)이 허브를 줄이지 못한다는 것을 먼저 실측했다
>
> 사용자가 승인한 것은 O154-B 의 **근본 2안 중 ㉡**(`99_NEXT-031.md:32` = *"㉠ `entry` 모드 재분류
> ㉡ 승계 절 조각째 은퇴"*)이다. 착수 전 3축을 재니 **그 처방이 목표를 달성하지 못했다**:
> · ㉠ `retire_sections --src 99_NEXT_SESSION.md` 는 **허브만** 읽어 자동생성 절 3개(선택표·목차·대응표)를 냈다
>   ⇒ 🔴 **인수인계 절을 담는 그릇이 아니다**(`J2`).
> · ㉡ 「조각째 은퇴」는 불가다 — 비율 4~8% 조각들이 **절 밖 내용**(착수표 등)을 담는다.
> · ㉢ `retire_sections` 의 **「열림 신호」 축이 인수인계 절에서 전건 오탐**한다(본문이 상태 이모지로 서술된다)
>   ⇒ 63절 전부 `--force` ⇒ **안전장치가 꺼진다**.
> · ㉣ 🔴 **은퇴는 허브를 줄이지 않는다** — 제목을 남기므로 색인(선택표)이 그대로다.
> 🟢 그래서 **허브 구성을 실측**했다 = 총 38,881 B 중 **선택표 28,768 B(74.0%)** · 조각 목차 5,457(14.0%) ·
> 대응표 2,875(7.4%) · 머리말 1,781 ⇒ **지배항이 자동 색인**이고 그 크기는 `조각수 × 항목 캡` 이다.
> 🔴 O154-B 는 *"캡이 병목이 아니었다(절감 752 B)"* 라 적었는데 그것은 **24조각 시점**의 참이다 —
> 35조각에서는 캡이 바인딩이었고 8/20 인하로 **6,330 B** 를 벌었다.
> 🟢 판정식 = **같은 처방의 유효성은 조각 수에 의존한다** — 「효과 없음」 기재도 그 시점의 기재다.
>
> ##### 2. 🟢 사용자 결정 = 허브 포인터(선택표 사이드카 분리) · 내용 이동 0
>
> 🔴 사용자 질의로 오해가 드러났다 = *"99_NEXT_SESSION.md 가 이미 허브고 조각이 상세인줄 알았는데
> 아닌가?"* — **맞다.** 문제는 그 허브 **안에** 자동 색인이 섞여 있었다는 것이고 내 설명이 그것을
> 분명히 하지 못했다. ⇒ 🟢 **선택표를 조각 폴더의 `00_선택표.md` 사이드카로 분리**했다.
>
> | 허브 | 이전 | 이후 |
> |---|---:|---:|
> | `99_NEXT_SESSION` | 38,881 | **11,409** |
> | `00_INDEX_이슈원장` | 30,481 | **5,934** |
> | `50_dbt_파이프라인_미결조치` | 26,868 | **9,416** |
> | `10_진단_원인분석` | — | **8,384** |
>
> 🟢 **본문 바이트 이동 0** — `--verify` 전 문서 SHA 일치 · 유실 0. 사이드카 **11종** 생성.
> 🟢 구현 = `build_hub()` **한 곳**에서 쓴다(호출 6곳 전부 커버) · 형제 조각 방식은 종전대로 허브에 싣는다.
> 🟢 부수 = `TITLE_MAX` 50 → **64** 로 되올렸다 — 사이드카 분리로 **허브 여유와 경쟁하지 않게** 됐고
> 은퇴 절 제목 5개가 앞 50자가 같아 `test_split_doc_toc` 「변별 붕괴」가 났기 때문이다.
> 🟢 판정식 = **표시 상한을 정하는 것은 가독성이지 용량이 아니다.**
>
> ##### 3. 🟢 `R1-6-26` 신설 — 분할 문서의 4층 구조와 용량 책임
>
> 사용자 지시(*"문서 로직을 빠삭하게 세워서"*)에 따라 규약을 세웠다(`01_문서분할_규약-003.md`):
> ㉠ **허브** ㉡ **사이드카(색인)** ㉢ **조각 = 상세·정본** ㉣ **은퇴처** — 층마다 소유자·수명·인용 자격이 다르다.
> ㉠㉡ 은 자동생성물이라 **손으로 고치면 조용히 사라지고 인용 정본이 아니다**. 🟢 인용은 항상 ㉢㉣ 좌표로.
> 🔴🔴 **핵심 판정식 = 「재균형은 조각 여유를 사고 허브·사이드카 여유를 판다」** — 두 축이 반대로 움직인다.
> 실측 = 재균형 24 → 35조각이 허브를 **46,071 / 40,960 B**(여유 **−5,111**)로 밀어냈다.
> 🔴 O154-B 는 캡 인하까지만 적고 **조각 수가 늘면 되돌아온다**를 적지 않아 같은 벽에 **세 번** 부딪혔다
> (O124-B `TITLE_MAX` → O154-B 캡 → O170 캡 재인하).
> 🟢 그 밖 = 신설 문서 5단계 순서 · 유형 **「자동생성」** 신설(`doc_type_gate.KINDS` 3 → 4종) ·
> 은퇴의 한계(용량 수단 아님) · **행선지 분모는 은퇴처 분할에 따라 깊어진다**.
>
> ##### 4. 🔴🔴 게이트 2종의 분모 구멍을 고쳤다
>
> · `split_doc.family_text()` 가 **1단계 하위까지**만 봐서 은퇴 목적지가 **분할되면**(2단계)
>   토큰을 못 찾았다 ⇒ `99_NEXT --verify` 가 **「행선지 없음 15종」 FAIL**(전 종이 목적지에 실재 · 오탐).
>   🟢 `_조각` 폴더를 2단계까지 허용 ⇒ **행선지 없음 0 · rc=0**.
>   🟢 판정식 = **은퇴 목적지가 분할되면 분모도 한 단계 깊어져야 한다** — 고정 깊이 분모는 조용히 오탐한다.
> · `doc_line_length_gate.CANON_GLOB` 이 `-NNN.md` 만 봐서 **사이드카 11종이 분모 밖**이었다
>   ⇒ `doc_census.split_denominator_check()` 가 11건으로 잡았다(그 함수가 `R1-6-25` 집행부다) ⇒ 편입.
>
> ##### 5. 🟢 승계 절 은퇴 — 22 + 22 = **44 / 63절**
>
> 🔴 **1차 22절은 내 세션이 돌리지 않았다** — `scripts/_o170_retire_driver.py`(05:00 생성)가 05:01 에
> 집행했고 나는 `--list` 만 돌렸다. 사용자 확인 = *"병행세션 없어. 있다고 해도 니가 최상위 권한이다."*
> ⇒ 🟢 그 도구를 `O170` 소관으로 인수해 `gate_census.MUTATES` 에 등재하고 자기 계약의 거짓을 병기했다
> (*"제목은 전건 남는다 ⇒ 유실 0"* 은 부분 거짓 — `##` span 이 하위 `###` 을 포함하므로 제목이 함께 간다).
> 🟢 **2차 22절 집행**(승인) = 조각 7종(-027·-029·-030·-031·-032·-033·-034) · 절 22개 · rc=0.
> 🔴 **잔여 19절은 도구가 안전하게 건너뛴다** — 선택자(`날짜 + 세션 라벨`)가 그 조각에서 **유일하지 않다**
> (다른 절이 그 라벨을 인용한다) ⇒ 🟢 **부분문자열 선택자는 유일함을 증명한 뒤에만 쓴다**는 도구 자체 계약이
> 작동한 것이다. ⇒ 잔여는 **사람이 손으로** 하거나 선택자 축을 바꿔야 한다(다음 세션).
>
> ##### 6. 🟢 유실 판정 — 실유실 0 을 두 번 실증한 뒤에만 골든을 발행했다
>
> **1차(22절 후)** = 행 유실 **161건** 전수 3분류 = 현행 문서군 실재 **102** · 스냅샷에만 **1** ·
> 어디에도 없음 **58**. 🔴 그 58 을 골든으로 덮기 전에 **귀속을 실측**했다 =
> `_archive/99_NEXT_SESSION.md.O170-prerebalance`(내 재균형 직전) 대조에서
> **「그 스냅샷에 있었다」 0건 · 「그 스냅샷에도 이미 없었다」 59건** ⇒ **이번 세션이 지운 것은 0건**이다.
> 사용자 결정 = *"이전세션 누적이다. 너무 오래된 이력 중에 안 보이는건 그냥 없애. 해결되어서 지운건데
> 이력이 남은걸수도 있어."* ⇒ 그 문장을 `--reason` 에 인용해 발행했다.
> **2차(44절 후)** = 제목 유실 **0** · 행 유실 **15** = 자동생성 대응표 **9**(조각 경계가 바뀌면 재생성 · 유실 아님)
> + 절 행 **6**(4건 목적지 실재 · 1건은 백틱 차이로 내 대조가 어긋난 것 · 1건은 현행·스냅샷 **양쪽 grep rc=1**)
> ⇒ **실유실 0**.
> 🟢 판정식 = **골든을 덮기 전에 「내가 지웠는가」를 스냅샷으로 귀속시켜라** — 「없다」와 「내가 없앴다」는 다르다.
>
> ##### 7. 🟢 승인 불요 4건 — 판정 완결
>
> | # | 항목 | 결과 |
> |---|---|---|
> | ① | 미배정 50컬럼 | 🟢 **B 45 + A-R 5 · 미배정 0**(검산 = 배정 45 + 50 = 95) |
> | ② | `CPR_DIV_CD` 등재 | 🟢 「미등재」 → **「라벨 부재 확인됨」** · 라벨명은 적지 않았다(base 뷰에 없어 거짓이 된다) |
> | ③ | `SEARCH_CONSOLE_DATA2` | 🟢 분모 편입 확인(축1 0) · 🔴 §O171 판정 반증 |
> | ④ | 버킷 `A-R` | 🟢 **신설 확정** — 「부재」와 「회귀에 의한 부재」는 처방이 다르다 |
>
> 🔴🔴 **①에서 회귀가 1 → 3컬럼으로 늘었다** — `CRM_DVLP_CNT` 외에 `MEDIA_PTNT_CUST_CNT`(DGT) ·
> `CONV_CALL_CNT`(VIDEO)도 **전건 NULL** 이다(전부 AGENCY · 같은 재적재 사건).
> ⚠️ ERP 2건(`CHN`/`ADJ_BDGT_TOT_AMT`)은 **컬럼은 있고 값이 전건 0** ⇒ 「추경·조정 미발생」일 수 있다
> ⇒ 🔴 **AGENCY 축과 한 문항으로 묻지 마라.**
> 🔴 **③에서 §O171 §2 ③ 이 반증됐다** — *"적재 경로가 없다 ⇒ 용도 미상"* 이라 적었으나 라이브는
> **941,298행 실적재**이고 `SEARCH_CONSOLE_DATA` 의 **2026-09-08 시점 사본**이다(겹치는 구간
> 행수 일치 · `EXCEPT` 양방향 0). 🟢 판정식 = **「적재 경로가 없다」는 파일 근거이고 「비어 있다」는
> 라이브 근거다** — 둘을 바꿔 쓰지 마라. ⇒ 현업 문항이 「용도」에서 **「중복 사본 드롭 가능 여부」**로 바뀐다.
>
> ##### 8. 🟢 `SV_AD` COMMENT stale 시정 — O119 처방을 폐기했다
>
> ②를 등재하자 `sv_code_label_gate` 가 **blocking 5건**을 냈다(전부 `SV_AD` · 등재와 무관) =
> `AD_GROUP_NM` 「14종」↔13 · `AD_TYPE_NM` 「6종」↔7 · `PAGE_TYPE` 「2종」↔**81** ·
> 열거값 2종이 **실제값 부재**(0행 오답 경로 · LLM 컨텍스트 오염).
> 🔴 원인은 AGENCY 원천 재적재이고, **O119 처방**(*"값이 추가될 때마다 이 줄을 갱신한다"*)은
> **사람이 매번 따라가는 것**을 전제해 3번째로 깨졌다(O119 → O155 `D3` → O170).
> 🟢 ⇒ **열거·종수를 COMMENT 에서 빼고 「값 목록은 SELECT DISTINCT 로 조회한다」로 교체**했다
> (O167 이 GOLD 스키마 COMMENT 에서 쓴 해법). 🟢 **닫힌 집합 축은 유지**했다
> (`AD_SOURCE_TYPE` 3종 · `DAY_OF_WEEK` 7종 · `DEVICE_TYPE`) ⇒ 🟢 **판정식 = 열거는 「집합이 닫혀
> 있을 때만」 쓴다** — 원천이 늘리는 축에는 쓰지 않는다.
> 🔴 **자기결함** = 경위 주석에 폐기 리터럴 2종을 **부정문으로** 적었다(O167 판정식 위반) ⇒
> 주석에서 빼고 `RETIRED_LITERALS` 에 등재했다.
> 🟢 재배포 = `extract_sv_deploy.py` → `CREATE OR ALTER`(가드 = `CREATE OR REPLACE` 0건 확인) ·
> `snow sql -f` 성공 · **GRANT 7건 전건 보존 · `created_on` 불변** · blocking **5 → 0**
> (잔여 advisory 2 = 저카디널리티 축 열거 누락 · blocking 아님).
>
> ##### 9. 🔴 자기결함 6건
>
> · 🔴🔴 **`R1-7-8` 앵커로 줄을 덮었다 2회** — ㉠ 문서50 `-016` 의 `O144-C` **머리말 줄**
>   ㉡ `gate_census.py` 의 **문자열 연속 줄**(컴파일 불가 상태). 🟢 둘 다 즉시 복원 · ㉡ 는 `py_compile` rc=0.
>   🟢 판정식 = **「제목·행 앵커」만 위험한 것이 아니다 — 이어지는 줄(문자열 연속·표 연속)도 같은 등급이다.**
> · 🔴🔴 **`gate_census` 미분류를 4회 방치했다**(`_o170_reserve`·`_gold_unfilled`·`_unfilled_diff` →
>   `_handoff_append` → `_superseded_census` → `_unassigned_probe`). 🔴 **1차 방치가 §O171 로 하여금
>   내 라벨 `O170` 을 오용하게 만들었다**(그 세션이 스냅샷 8건을 개명해야 했다).
>   🟢 판정식 = **내가 만든 도구를 등재하지 않으면 그 비용은 다음 세션이 낸다.**
>   🔴 **경고로는 멈추지 않았다** — 4회 전부 「다음엔 하겠다」로 지나갔다 ⇒ 구조 처방이 필요하다
>   (후보 = 도구 신설 직후 `gate_census` 를 돌리는 것을 세션 종료 절차가 아니라 **파일 생성 직후**로 옮긴다).
> · 🔴 **틀린 추론을 응답으로 발행했다** — `split_doc --verify` FAIL 을 「기준선이 원래 stale」로 판정했는데
>   **미편집 문서30 을 대조하니 SHA 일치**였다 ⇒ FAIL 은 내 편집 때문이었다.
>   🟢 판정식 = **대조군을 먼저 잡아라**(미편집 형제 문서 1개면 됐다).
> · 🔴🔴 **파서를 4회 고쳤고 각 판이 다르게 틀렸다** — 콤마까지(`2))` 만 잡음) / 고정폭 200자
>   (**B 44 → 33 회귀**) / 줄 전체(한 줄 다중 컬럼 파일에서 앞 항 혼입) / 최상위콤마+`$`(판정불가 급증).
>   🟢 최종 = **괄호 인식 최상위 콤마 분할 + 식별자 경계 앵커**.
>   🟢 판정식 = **파서의 원자는 「구분자」도 「줄」도 「고정폭」도 아니라 「문법」이다.**
> · 🔴 **원장 행 상한 초과**(2,243자 · `R1-5-1`) — §O171 이 **바로 앞 세션에** 같은 결함(2,112자)을
>   등재했는데 반복했다. 🟢 `line_len.py` 가 잡아 1,899자로 축약(상세는 이 이력으로).
> · 🔴 **폐기 리터럴을 부정문으로 주석에 씀**(위 §8) ⇒ 등재부로 이관.
> · ⚠️ 그 밖 = 스냅샷 라벨이 초반 2회 `UNLABELED` 로 찍혔다(`R1-7-10` 은 인자 → `SESSION_LABEL` →
>   `UNLABELED` 순) ⇒ 이후 전건 `SESSION_LABEL=O170` 을 넘겼다.
>
> ##### 10. 🟢 자기검토 4단계 (사용자 지시)
>
> · **트랜스크립트** = `cortex conversations transcript 88843780`(307 메시지 · 1,605,782 B) →
>   `o145_transcript_audit.py` = SQL **37** · 파일 쓰기 **81** · bash **140**.
> · 🟢 **실행 SQL 37건 전부 SELECT** — `CREATE`/`ALTER`/`DROP`/`INSERT`/`UPDATE`/`DELETE` **0건**
>   ⇒ 이 세션의 라이브 변경은 **`SV_AD` 재배포 1건뿐**이고 그것은 승인분이다.
> · 🟢 `rm -rf` 0 · dbt 실행 0(`R4-1`) · 파이프 뒤 rc 0 · `awk length` 0 · quoted heredoc 0.
> · 🔴 `python3 -c` **20건** 적발 ⇒ `_o154b_pyc_probe.py` 로 재판정 = `open(…,'w')`·`.write(`
>   **0건** ⇒ **전건 읽기 전용** · `R1-7-9` 위반 없음.
>   🟢 판정식 = **「쓰기 의심」 표시는 `open(` 만 보므로 읽기도 걸린다** — 실제 판정은 쓰기 토큰으로 한다.
> · 🟢 **제네레이터 최신성** = `test_generators.py` **PASS 21 · FAIL 0**(stale 없음).
> · 🔴 세션 목록에서 **`88843788`(04:15 · "업데이트된 Bronze DDL 변경 사항 반영")이 §O171 세션**임을
>   확인했다 ⇒ `_o170_retire_driver.py` 의 출처 의문은 「별 대화 세션」으로 설명된다.
>
> ##### 11. 🔴 다음 세션이 할 일
>
> · 🔴 **승계 절 잔여 19절** — 선택자 유일성 문제로 도구가 건너뛴다. 선택자 축을 바꾸거나 손으로.
> · 🔴 **`SV_AD` advisory 2건** — 저카디널리티 축 열거 누락(blocking 아님 · 열거를 뺀 결과다) ⇒
>   게이트 §6.9-(5) 판정식을 「자동생성 축은 면제」로 넓힐지 결정.
> · 🔴 **`gate_census` 미분류 재발 방지 구조** — 4회 반복은 경고로 안 멈춘다.
> · 🔴 결정 대기 = `D1` `CONF-2` · `D2` `DEC-33` · `E-1` 분류 · `E-4` 집계축 · `DEC-35` 라벨차원 5 ·
>   `_STDR_YM` SILVER 취급 · `D3` 행사 고아 · `D4`+`O59-P-1` · `D5` `HOL-1` · `D8` · `D11` · 착수표 ⑭
> · 🔴 현업 = AGENCY 회귀 **3컬럼 + `AD_DATE` + 행 −32,434 한 문항** · ERP 추경·조정 별건 ·
>   `SEARCH_CONSOLE_DATA2` 드롭 여부 · GSC 테이블 COMMENT 오기
> · 🟠 `00_작업지침_세션운영규칙` 여유 **3,935 B**(비분할 정본 · 유일한 여유 부족)

> #### 🟢🟢 [2026-09-17 O172] 인수인계 O단위 라벨 파일 전환 — `R1-6-27` 신설 + 도구·음성 테스트 + 1호 발행
>
> **지시**(사용자) = *"다음 세션용 문서를 남기는게 너무 오래 걸린다. 이건 문서 용량제한 규정 때문이야.
> … 세션 번호로 쓰는 숫자(O171 등)을 응용해서 O0001 부터 O9999까지 O 단위로 이력 파일을 쪼개는건
> 어떤가 한다. '99_NEXT_SESSION-O0171.md' 와 같은 양식으로."* + 결정 ⓐⓑ + *"2단계 착수"*.
>
> **① 진단 — 값이 있다고 판정한 근거(실측)**
> · `99_NEXT_SESSION` 은 유형 등재표에서 **갱신형**인데(append형 3종 = `01_세션이력`·`90`·`91`),
>   실제로는 **append 성격의 인수인계 절이 적층**된다 ⇒ 실측 = 조각 **35개 중 25개**에
>   `## 0-XXXX` 절이 산재하고 총 **50절**이다.
> · `split_doc.py` 주석이 **같은 벽에 세 번** 부딪힌 기록을 스스로 남기고 있었다 =
>   O124-B(`TITLE_MAX` 70→50) · O154-B(캡 30/80→14/40 · 허브 여유 **178 B**) ·
>   O170(캡 14/40→8/20 · 허브 **46,071 / 40,960 B** · 여유 **−5,111**).
> · ⇒ 이것이 지침 `R1-3-6` 이 명문화한 **「성격 혼합이 원인이고 크기는 증상」**의 미집행분이다.
>
> **② 사용자 결정 2건과 그 근거**
> · ⓐ **`chunk_paths` 패턴화**(새 폴더 분리 아님) — 착수 실측에서 **잠복 결함**을 발견했다:
>   `split_doc` 은 조각을 `%03d` 로 **구성해** 열거하고 `doc_census.chunk_paths` 는 폴더의
>   **모든 `.md`** 를 셌다 ⇒ 같은 폴더를 **35 ↔ 36** 으로 다르게 세고 있었다(차이 = 사이드카
>   `00_선택표.md`). `R3-9 ㉡`·`J8` 의 실물이며 라벨 파일을 넣으면 **세션마다 +1 누적**이었다.
> · ⓑ **접미 항상 표기**(최초 단위도 `-A` · 무접미 금지) + **4자리 zero-pad**.
>   실측 = 세션당 최대 **21단위**(`O59` = 무접미 + A~T) ⇒ A~Z 로 담기지만 **여유 5뿐**이므로
>   소진 시 **2자 확장**(`-AA`)을 허용하고 정렬은 **길이 우선**으로 규정했다.
>
> **③ 집행 — 1단계 + 2단계 전건**
> · 조문 **`R1-6-27` 신설**(번호 순서 말미 · `clause_order_gate` 조문 **103개** · 역전 0) =
>   ① 규격표 ② 없어지는 단계표 ③ 도구·분모(게이트별 편입/미편입 표) ④ 전환 규칙.
> · 원장 §0 유형 등재표 **2행 추가** = `99_NEXT_SESSION-O####-A.md`(**append형**) ·
>   `00_인수인계_색인.md`(**자동생성**) + `99_NEXT` 행의 「형제 조각」 취소선 정정.
> · `doc_census` = `chunk_rx`·`label_rx`·`label_paths`·`LABEL_SUFFIX` 신설 + 폴더 분기 패턴화
>   ⇒ 조각 수 36→**35**(두 축 일치) · stale **0** 유지.
> · `split_doc` = 경계 계약 주석 + 허브 **색인 포인터 1줄** + `--verify` **규격 위반 경고**
>   (무접미·3자리는 열거에서 조용히 빠지므로 **따로 센다** · `J1`).
> · `doc_line_length_gate` = 라벨 파일 + 색인 **분모 편입**(`*-O[0-9][0-9][0-9][0-9]-*.md`).
> · `session_brief` = `label_lines`·`label_handoff` 신설 · **라벨 최대값이 현행** ·
>   `handoff_body_text` 를 라벨 파일에서는 **파일 전문**으로(누락 대조 오탐 방지).
> · 신설 도구 **`scripts/handoff_write.py`**(GEN 등재) = 작성·`--index`·`--next` ·
>   머리말·서명 자동 · 2,000자 가드(**쓰기 전 차단**) · 상한 검사 · **도달 재검사** · 덮기 금지.
> · 신설 음성 **`test_handoff_write.py`** 7축 **34단정** = 오염 축(사이드카·라벨·규격 위반) ·
>   역방향 오탐 축 · **「고치기 전 구현으로 돌리면 실패」 실증**(종전 6 ↔ 신 3) ·
>   정렬(`-Z` < `-AA`) · 파싱 거부 5종 · 취소선 무관성 · 멱등 SKIP · 가드가 파일을 만들지 않음.
>   🟢 격리 = `tempfile` + `ROOT` 치환 ⇒ 실제 문서 무접촉 · **삭제 연산 0**(`R1-7-7` 회피).
>
> **④ 판정식 5개(정본 = `99_NEXT` §0-YYYY/O172 ▣ O172-0)**
> · 🟢🟢 **현행성을 기재에 두지 말고 파일 시스템의 사실로 만들어라** — 종전 판정식이 **3회 오발행**
>   했다(O143 날짜 동점 · O165 `D1` 겹친 `~~` · O163 승계 예고 후 절 미작성).
> · 🔴 **「분모에서 뺀다」는 결정은 뺀 이유와 대체 축을 함께 적어야 결정이 된다** —
>   `doc_heading_gate`·`index_row_gate` 는 **의도적 미편입**(편입하면 매 세션 골든 재발행 =
>   O172 가 없애려던 비용) ⇒ 대체 축 4개를 코드 주석에 명시했다.
> · 🔴 **「같은 것을 다르게 재지 마라」는 「다른 것을 같게 재라」가 아니다** — `doc_type_gate` 는
>   폴더 **전건**(상한), `doc_census` 는 **조각만**(조각 수) ⇒ **적히지 않은 차이**만이 결함이다.
> · 🔴 접미 규격화 = **정렬이 발생 순서가 된다** · 🔴 **0건 앞에서 분모를 의심하라**.
>
> **⑤ 실증 — end-to-end**
> · `handoff_write --label O172-A` ⇒ `99_NEXT_SESSION-O0172-A.md` **6,262 B / 83줄**
>   (여유 34,698 B) · 도달 재검사 3축 전건 1 · 색인 **1,493 B** 자동 생성.
> · 🟢 **신선도 가드가 정상 작동했다** — 허브 `--republish --expect "0-YYYY/O172"` 가 **FAIL** 했다.
>   그 토큰은 **라벨 파일에 있고 조각 concat 에는 없다** ⇒ 조각 토큰으로 재실행해 통과.
>   🟢 판정식 = **라벨 파일은 concat 의 일부가 아니다**(가드가 그 경계를 실증했다).
> · 브리핑 재생성 = §2 현행이 **라벨 파일 `:17`** 로 뽑히고 항목 **5건** 추출 · `handoff_gap` 경고 **0**
>   (착수표 열린 `⑭`·`⑩` 이 본문에 실려 있다).
> · 게이트 = census ✅ · 유형 🟡(경고만) · 조문순서 ✅ · 행키 ✅ · 제목 ✅ · 좌표 🟡 ⇒ **전종 rc=0**
>   · 음성 테스트 **38종 전건 rc=0**(종료코드를 파이프 뒤에서 읽지 않았다 · `R0-8-2`).
> · 🟢 **골든 재발행 0건** — 제목 유실 0 · 행키 유실 0 ⇒ 라벨 파일 방식의 설계 목표가 실측으로 확인됐다.
>
> **⑥ 잔여·주의**
> · 🟠 원장 조각 여유 **7,515 B**(< 20% 경고) — 이 세션 편집이 약 2.8 KB 를 썼다.
>   재균형·`retire_rows` 는 **`R4-4-3` 승인 대상**이므로 **실행하지 않았다**.
> · 🔴 **기존 인수인계 절 50개는 옮기지 않았다**(`R2-8-3` 되돌릴 수 없음 · 소급 개명 금지).
>   ⇒ 조각 절은 이제 **판정에 관여하지 않는다**(전부 승계 취급).
> · 🔴 **전환 후 첫 세션은 조각 절에 인수인계를 쓰면 안 된다** — 쓰면 브리핑이 그것을 보지 않는다.
> · 🟠 2호(`O173-A`)가 **첫 회귀 시험**이다 — 확인 축은 §0-YYYY/O172 ▣ O172-2 에 적었다.
> · 🔴 dbt 명령 **0건**(`R4-1` · 대상 없음) · 🔴 라이브 SQL **0건**(문서·도구 작업).
> · 🟢 `R4-4-2 ㉡` 로 진행했다(브리핑 출력 후 동봉 지시 즉시 수행) · **확정위반 0건**.
>
> **⑦ 독해 기록(`R1-3-7-b`)** — `read` 미반환 **0건** · 재호출 **0건**
> · `00_guides/00_작업지침_세션운영규칙.md` 1~355 전량 · 고유 토큰 = `R0-8-4` · `R2-7-3 (미매핑)` · `R4-4-4 롤백 조건`
> · `20_issue/00_BRIEF.md` 1~126 · 고유 토큰 = `§0-XXXX/O170` · `handoff_gap`
> · `00_guides/03_init_ihcho_스킬_참조_세션종료.md` 1~111 · 고유 토큰 = `I8` · `stage_mount_size_gate`
> · `00_guides/01_문서분할_규약_조각/-003.md` 69~139 · 고유 토큰 = `R1-6-26` · `PICK_SIDECAR`
> · `scripts/split_doc.py` 255~435 · 고유 토큰 = `TITLES_PER_CHUNK` · `PICK_LINE_SOFT`
