<!-- SPLIT-CHUNK 01_세션이력.md | 080/080 | 허브 = 01_세션이력.md | 원문 1~135행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-22 O179] 이슈 B·C 종결 · I1 규명 · 🔴 내 예측 2건이 기각되고 그 기각이 더 큰 결함을 드러냈다
>
> **지시** = ㉠ 넘겨받은 O178-C 인수인계 전건 진행 ㉡ 추가 프롬프트(§3 배선 · §4 현업 회신 5건)
> ㉢ 권고 순서 ①~⑤ 로 진행 ㉣ `W3` 8건 · `I2` 11건은 다음 세션 이월.
> 🔴 `R4-4-2 ㉡` 적용(지시 동봉 ⇒ 브리핑 후 즉시 수행) · **확정위반 0**.
>
> ### 1. 이 세션이 확정한 판정식 (먼저 읽어라)
>
> ㉠ 🔴🔴 **「라이브 재조회」 전에 build in-flight 를 먼저 확인하라 — 신설 판정식이다.**
>   실사고 = `GOLD.FACT_MEMBER_MONTHLY` 를 조회해 **0행**을 받고 「41.5M행 소실」로 보고했다.
>   실제로는 `dbt build` 가 **실행 중**이었고(`00:40:44` 시작 · `TRUNCATE` 00:42:25 · `INSERT` RUNNING)
>   `TRUNCATE`+`append` 팩트는 **적재 중에 0행으로 보인다**. 직전 성공 INSERT 는 41,508,842 였다.
>   🔴 그 0행은 **사고와 구별되지 않는다** ⇒ 판정식 = `QUERY_HISTORY` 에서
>   `EXECUTION_STATUS IN ('RUNNING','QUEUED')` 를 **먼저** 본다. 🟢 자체 적발·즉시 정정했다.
>
> ㉡ 🔴🔴 **「증분이 도는가」는 행수로 검증할 수 없다 — pre-hook 이 무엇을 냈는지 보아야 한다.**
>   `CRM_MEMBER_DEV` 는 `merge`+`pre_hook=[]` 로 「1개월 주기 증분」을 선언하는데, `QUERY_HISTORY`
>   실측은 매 run `TRUNCATE` 가 돈다고 답했다. 원인 = **dbt 는 hook 을 누적(append)** 하므로
>   `pre_hook=[]` 는 폴더 `+pre-hook` 을 **취소하지 못한다**(빈 목록을 덧붙일 뿐).
>   🔴 그런데 **무증상이었다** — TRUNCATE 로 비면 증분 필터의 `MAX(SRC_LOAD_DT)` 가 NULL →
>   `COALESCE('1900-01-01')` → **전량 통과** ⇒ 결과는 항상 맞고 비용만 늘며 테스트가 안 깨진다.
>
> ㉢ 🔴 **「같은 이름」이 「같은 grain」을 뜻하지 않는다.** `UTM_CAMPAIGN` 은 실재하지만
>   `DIM_CAMPAIGN.MKTG_UTM_NM` 이 **1 UTM ↔ 최대 902 캠페인**이라 `CAMPAIGN_SK` FK 가 될 수 없다.
>   🟢 판정식 = **전건 센티넬 컬럼에 배선 후보를 붙이기 전에 후보 키의 유일성을 먼저 재라.**
>
> ㉣ 🔴 **「N건 중 M건」을 쓸 때 N 의 정의를 같은 문장에 적어라**(재확인). 내가 문서20 열린절을
>   **54건**으로 발행했는데 그것은 `###`/`####` 제목 전수였고, 실제 열린 **문항**은 `**판정**:`
>   줄이 공란인 **28건**이었다. 🟢 자체 적발·정정. 판정식 = **상태는 제목이 아니라 판정 줄에 있다**(`J2`).
>
> ### 2. 이 세션이 끝낸 것
>
> **`P0` 백필 슬롯 잠금** — `ALTER DBT PROJECT … ADD VERSION LOCK_BACKFILL_SLOT_20260922`.
> 착수 전 실측으로 정지점을 확인했다(live comment 가 `SRCSYS_BIGQUERY_BACKFILL_20260922` =
> 슬롯 열린 시점 스냅샷 ↔ 파일은 `bigquery_lookback_days: 3` + `bigquery_dt_ranges` 주석 = 잠김).
>
> **이슈 `B` 종결(현업 회신 집행)** — 회신 = *"삭제된 이벤트·캠페인에 귀속된 회원이 남은 데이터다.
> 회원 마스터를 정본으로 하고 고아 회원은 SILVER 정제 시 없앤다."*
> · 마스터 = `SILVER.CRM_MEMBER.MEMBER_DK` 실측 **1,785,299 전건 유일·NOT NULL**(= `DIM_MEMBER`).
> · 술어 = `macros/gn_member_master_filter.sql` **신설**(정의 지점 1곳 · `R1-6-17`).
> · 🟢 **`INNER JOIN` 이 아니라 semi-join(`EXISTS`)을 골랐다** — 지금은 마스터가 유일하지만
>   SCD 화되면 INNER JOIN 은 **조용히 팬아웃**하고 구조적으로 막을 수 없다. `EXISTS` 는 정의상
>   행을 늘릴 수 없고 SELECT 목록을 건드리지 않아 diff 가 WHERE 한 줄이다.
>   ⇒ 판정식 = **같은 결과를 내는 두 방법 중 되돌릴 수 없는 사고를 못 내는 쪽을 고른다.**
> · SILVER **10모델** 배선 · `dbt build` PASS 251 / WARN 13 / ERROR 0 / SKIP 0(사용자 실행 · `R4-1`).
> · 📏 **대사 10/10 적중** · 제거 **45,544행** · **고아 잔존 0 전 모델**:
>   `CRM_SEND_MEMBER` 41,935,344(−34,246−NULL 746) · `CRM_EVENT_PARTICIPATION` 1,248,727(−10,048) ·
>   `CRM_MEMBER_DEV` 3,654,658(−271) · `CRM_MEMBER_SPONSOR_SPAN` 2,201,695(−106) ·
>   `CRM_MEMBER_STATUS_HIST` 7,644,142(−86) · `CRM_PAYMENT_METHOD` 2,588,966(−39) ·
>   `CRM_MEMBER_DISCONTINUE` 1,061,430(−1) · `CRM_MEMBER_RESPONSOR` 118,198(−1) ·
>   `CRM_MEMBER_AMT_CHANGE`·`CRM_SPONSOR_RELATION` 불변(고아 0 · **회귀 방어로 붙였다**).
> · 🟢 하류 GOLD 전부 설명됨 = `FACT_MESSAGE_DISPATCH` 41,935,344(1:1) ·
>   `FACT_EVENT_ATTENDANCE` 1,248,727(1:1) · `FACT_MEMBER_EVENT` 4,716,088(**−272 = 271+1**) ·
>   `FACT_MEMBER_MONTHLY` 41,508,824(−18 · 집계 grain 이라 1:1 아님).
>
> **이슈 `C` 종결** — 회신 = *"입력 오류거나 테스트 데이터로 실제로 소비하진 않는다."*
> 🟢 우리 판정(센티넬 쓰레기)이 확인됐고 **처리 변경 0**(현행 `DATE_SK=0` 라우팅이 정답).
> 🔴 **삭제하지 않았다** — 회신은 「소비하지 않는다」이지 「제거하라」가 아니고 원천 무변경이 우선한다.
> 🟢 `warn_gold_fact_bigquery_date_sk_zero` 감시 유지(88행이 늘면 새 입력오류라는 신호).
>
> **`I1` 규명** — `FACT_EVENT_ATTENDANCE.DATE_SK=0` **162,888 → 50** 의 원인은
> `cal_start` **1991-01-01 → 1945-01-01** 확장이다(O178 의 별건 결정). 실측 =
> 1991 미만 **162,805행**(최소 `19700821`) · 종전 범위 내 1,085,872 · 2035 초과 0.
> 산술 **162,805 + 50 + 33 = 162,888** 으로 닫힌다(잔여 33 = 이슈 B 로 제거된 고아행 · 산술 추론).
> 🔴 GOLD 팩트 리터럴 `pre_hook` 은 **4종**이었다(인수인계 「5종」은 과대 — `FACT_BIGQUERY_BEHAVIOR`
> 는 리터럴 hook 이 없고 정상이었다). 🟢 피해 0 이었던 이유 = 4종 전부 `RANGED_FACTS` 에 없어
> 매크로도 `TRUNCATE` 를 내고 **TRUNCATE 는 멱등**이다. 🔴 제거한 이유 = 그중 하나를
> `RANGED_FACTS` 에 넣는 순간 TRUNCATE + 범위 DELETE 가 함께 돌아 **에러 없이 창 크기로 쪼그라든다**.
> 🟢 GOLD `dim` 4건은 `gold.dim:` 에 프로젝트 hook 이 **없어** 유일 hook 이므로 **건드리지 않았다**
> (🔴 다음 세션이 「같은 결함」으로 오판하지 말 것).
>
> **`W2`** — 사용자 회신「원천이 freeze 입고라 lookback 불요 · 개념 구현만 두고 객체에 코멘트」.
> 롤링 윈도우 객체 3종(`SILVER.BIGQUERY_BASIC`·`BIGQUERY_EVENT`·`GOLD.FACT_BIGQUERY_BEHAVIOR`)
> 테이블 COMMENT 에 「원천 freeze ⇒ lookback 은 지연도착 방어가 아니고 부분적재·중단 run 재처리다 ·
> 요건 시 `bigquery_lookback_days` 값만 올려 대응 가능」을 DDL 정본 + 라이브 양쪽에 반영.
> `comment_drift_gate` rc=0(테이블레벨 37/37 · 47/47).
>
> **`§5` 문서 정정** — 🔴 인수인계가 지목한 AGE 3테이블은 **전부 BRONZE**(원천 무변경 대상)였고,
> DW 소관 오기는 **SILVER 2건**이었다 ⇒ `CRM_MEMBER_DEV.AGE`·`CRM_MEMBER_AMT_CHANGE.AGE` COMMENT 를
> `'연령'` → `'연령대 코드(CM014) raw … 🔴 연수가 아니다 ⇒ 평균·합계 금지'` 로 정정(파일+라이브 ·
> `comment_drift_gate` 892/892). 실측 = SILVER AGE 는 **1~12 = CM014 코드 12종 정확 일치**
> (40대 회원이 `5` ⇒ `AVG(AGE)` 가 4.7 같은 무의미 값을 낸다 · 에러 없이 보고서가 틀린다).
> 🟢 `stage_mount_size_gate` 드리프트 「8건」은 **이미 0 이었다**(604건 전건 일치 · `J3` 재실례).
>
> **문서20 조각 구조** — `-001` 이 389줄까지 커져 300줄 불변식을 넘겼다. 사용자 결정으로
> ㉠ 신설 절을 `-009` 로 이전(297/292줄) ㉡ 그 뒤 `-009` 가 355줄이 되어 **`-010` 수기 신설**.
> 🟢🟢 **판정식 = `--rebalance` 는 조각 번호를 밀어 타 문서의 `-00N.md:행` 인용 좌표를 전부
> 깨뜨리지만, 꼬리 조각 신설은 기존 조각을 한 바이트도 건드리지 않아 좌표가 보존된다**
> ⇒ `--rollover`(`R1-6-15`)와 동일한 결과다. `--republish` 통과(조각 10 · 토큰 유실 0 · 상한 초과 0).
> 🔴 센티넬은 문자열이 **정확히** `<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->` 여야 한다
> (`<!-- BODY-BEGIN -->` 로 쓰면 「센티넬 부재」로 rc=1).
>
> ### 3. 발행한 현업 문항 5건 (전부 실측 동봉)
>
> · **`§D-2`** 사업목표 원천 CRM/ERP — 🔴 **되돌림 충돌**이다. `ERP_BIZ_TARGET → CRM_BIZ_TARGET`
>   개명이 **2026-07-20 에 현업 확언 근거로 이미 집행**됐고, `BRONZE_ERP` 에는 목표 테이블이 없다
>   (`BDGT_ACMSLT_LEDGER`·`EXPENSE_RESOLUTION` 2종뿐). ⇒ 사용자 결정 = **개명 보류 · 재확인 후 착수**.
> · **`§W-1`** UTM ↔ 캠페인 축 — 실측 참고자료 동봉(distinct 700 · 매칭 108/700 · 행 52.4% ·
>   `DIM_MARKETING_CAMPAIGN` 에 UTM 컬럼 없음). 사용자 결정 = 마케팅 캠페인코드 작업과의 관계를 문의.
> · **`§N-19`** 「그 밖」 9항목 BRONZE 전수 — 🟢 **「데이터가 없다」는 `AGENCY_CONV_CNT` 1건뿐**이고
>   나머지는 데이터가 있고 **뜻·규칙·키가 없다**(입고 요청이 아니라 판정 회신이 필요하다).
>   실측 = `SNDNG_TY_CD` 전건 채움인데 코드사전에 코드군 **0건** · 축B `TRNSMS_FAILR_CD_ID`
>   **19,613,208/33값** · `CALL_STATUS` **9,699,141/34값** · GSC 2테이블이 **부분집합이 아니다**
>   (958,419 ↔ 941,298) · `Q10` 세세목 48종 중 캠페인 이름 일치 **1/48** ⇒ 이름매칭 불가.
> · **`§N-20`** BRONZE AGE COMMENT 오기 + 코드/라벨 혼재(`SND_MEMBER_LIST` 에 코드 `'12'` 8행 ·
>   NULL 53.26%). · **`§N-21`** PII 전제(생년월일 컬럼 전 스키마 0건 ⇒ 적재분 비민감 · 회신 불요).
> · **`§N-22`** `BRONZE_GA4` 유지 확인 + `GA4_USER` 명칭 미결.
>
> ### 4. 실측으로 기각·정정된 인수인계 기재
>
> | 기재 | 실측 | 판정 |
> |---|---|---|
> | GOLD 팩트 `pre_hook` **5종** | **4종**(`FACT_BIGQUERY_BEHAVIOR` 정상) | 🔴 과대 |
> | `stage_mount_size_gate` 드리프트 **8건** | **0건**(604 전건 일치) | 🔴 stale |
> | `A4-W` 배선 6건 중 1번 「실재」 | 원천 컬럼은 실재하나 **grain 부적합**(1:902) | 🔴 전제 오류 |
> | `§N-13` 단일사업 **92.45%** | FME STOP 행 grain **92.31%** · 중단회원 grain **84.89%** | 🟠 grain 3종 병기 |
> | 이슈 E 「3축 전부 증가」 | 2축 **감소**(272,063 · 11,421) · 1축 불변(21) | 🟢 우리 작업이 원인 |
> | 이슈 D 「bronze 에 데이터 있을 것」 | `CONV_CALL_CNT` **0/37,769** · `TOT_CLICK_CNT_CTNT` **0/1,194,376** | 🔴 BRONZE 도 전건 NULL |
>
> ### 5. 종료 기준선 (2026-09-22 · 계정 LK96056 · 🔴 인용하지 말고 재라)
>
> · `dbt build --select CRM_MEMBER+` PASS **251** / WARN **13** / ERROR **0** / SKIP **0** / TOTAL 264
> · 음성 테스트 **43종 rc=0** · `gate_census --final` PASS(미분류 0 · 유령 0 · `_scratch_*` 0)
> · 게이트 = `comment_drift` rc=0(746/746 · 892/892 · 뷰 574/574 · 테이블레벨 37/37·47/47) ·
>   `doc_census --strict` rc=0(stale 0) · `doc_heading` rc=0 · `doc_coord` rc=0 · `doc_type` rc=0 ·
>   `index_row_gate` rc=0 · `stage_mount_size_gate` rc=0(604/604) · `decision_closure_gate` 🟠 경고 2
> · GOLD = `FACT_MESSAGE_DISPATCH` 41,935,344 · `FACT_MEMBER_MONTHLY` 41,508,824 ·
>   `FACT_MEMBER_EVENT` 4,716,088 · `FACT_BIGQUERY_BEHAVIOR` 1,620,857 · `FACT_EVENT_ATTENDANCE` 1,248,727 ·
>   `FACT_MEMBER_DEV_ACHIEVEMENT` 29,442 · `DIM_MEMBER` 1,785,299 · `DIM_DATE` 73,415
> · SILVER = `CRM_MEMBER` 1,785,299 · `CRM_MEMBER_DEV` 3,654,658 · `CRM_MEMBER_STATUS_HIST` 7,644,142
> · 이슈 A 3버킷 = 정상 41,507,501 / 미래월 1,185(223명) / `MONTH_KEY=0` 138(138명) · 합 검증 통과
> · 이슈 E = `EVENT_SK` 센티넬 272,063/1,248,727(21.79%) · `SNDNG_KEY` 11,421 · 21
> · 문서20 = 조각 **10** · 최대 297줄 · 열린 문항 **28**(종결절 10) · 여유 11,509 B
> · `id_collision_gate` O 정의 **179** · 참조 179 ⇒ 다음 **O180**
