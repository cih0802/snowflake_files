<!-- SPLIT-CHUNK 01_세션이력.md | 074/074 | 허브 = 01_세션이력.md | 원문 1~49행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-17 O172-B] 사용자 승인분 집행(원장 재균형) + 자기결함 1건 시정 + 확대 불채택 판정
>
> **지시**(사용자) = *"좋아. 전부 승인한다. 웬만하면 다른 문서들도 적용하고 싶긴한데..낭비일수 있겠군.
> 아무튼, 작업 다 하고 남은 작업을 다시 알려줘."*
>
> **① 원장 재균형 — 1차 FAIL 후 채움률로 해소**
> · 1차 `--fill 0.7` = **FAIL**. 게이트5b 「절단 표 1건」 = 조각 경계가 `## 1. 상태 대시보드` 의
>   **표 헤더 직후**에 떨어져 구분행·본문행이 다음 조각으로 갔다(`-002.md:137`).
> · 🔴 `R1-7-3`(낡은 뷰) 을 배제하려고 `--verify` 를 **2회** 돌렸고 두 번 다 rc=1 ⇒ **실결함**으로 판정.
> · 2차 `--fill 0.62` = **PASS**. 조각 **14 → 17** · max **25,280 B** · 최소 여유 **7,515 → 15,680 B**
>   ⇒ `doc_type_gate` 축3 경고 **2건 → 1건**(남은 1건 = `00_작업지침` 미분할 정본 · 기존 항목).
> · 🟢🟢 **판정식 = 채움률은 용량 파라미터가 아니라 「경계가 어디 떨어지는가」다.**
>   ⇒ 재균형이 표를 절단하면 **파일을 고치기 전에 채움률을 먼저 바꿔라**(내용 편집은 최후 수단).
> · 🟢 `R1-6-26` 대로 **재균형 직후 허브를 다시 쟀다** = 원장 허브 **6,613 B**(여유 34,347) — 안전.
> · 🟢 재균형은 좌표를 이동시키므로 3축 재검 = 좌표 깨짐 **0** · 행 유실 **0** · 제목 유실 **0**.
>
> **② 🔴🔴 자기결함 1건 — 내가 없애려던 결함을 다른 층에서 재현했다**
> · **무엇** = `label_handoff` 초판이 `paths[-1]` **한 파일만** 현행으로 실었다.
> · **왜 결함** = 한 세션 다단위는 **정상이고 명문화돼 있다**(`R1-4-3` · 실측 최대 **21단위** = `O59`)
>   ⇒ `O172-B` 를 쓰는 순간 **`O172-A` 인수인계가 브리핑에서 사라진다**. 🔴 O143(닫지 않은 항목이
>   인수인계에서 조용히 사라짐)과 **같은 형태**이고, 그 사고를 없애려고 만든 설계가 그것을 재현했다.
> · **시정** = `session_brief.label_units()` 신설 — 현행의 단위를 **파일이 아니라 세션(O번호)** 으로
>   잡고 최대 O번호의 **전 단위**를 읽는다 + §2 에 단위 목록을 싣는다.
>   🟢 실증 = 브리핑 §2 가 **단위 2개**를 나열하고 항목 추출이 **5 → 9건**으로 늘었다.
> · 🟢🟢 **판정식 = 「현행」을 정의할 때 그 단위가 파일인지 세션인지 먼저 정하라.**
>   🔴 판정식을 파일명으로 옮기면 **파일이 곧 단위라고 착각하기 쉽다** — 규격이 옳아도
>   **단위를 잘못 잡으면 같은 사고가 다른 층에서 재발한다.**
> · ⚠️ 음성 테스트는 **이 축을 잡지 못했다**(축6 은 「최대값이 뽑히는가」만 단정했다) ⇒
>   🔴 다음 세션이 축을 보강하려면 **「후속 단위를 쓰면 앞 단위가 사라지는가」**를 단정으로 넣어라.
>
> **③ 「다른 문서에도 적용할까」 — 불채택(사용자 직감이 맞다)**
> · 비교 실측 = `99_NEXT` 인수인계 **6단계** ↔ `01_세션이력`(73조각)·`90`(16조각) **1단계**(`--rollover`).
> · `--rollover` 는 이미 「꼬리에 넣고 차면 새 조각 신설」의 **O(1) 연산**이고 기존 조각을 한 바이트도
>   건드리지 않는다(`R1-6-15` docstring). ⇒ 전환 이익 **≈ 0**.
> · 🟢🟢 **판정식 = 비용의 원인은 「조각 방식」이 아니었다** — ㉠ 인수인계가 **갱신형 문서에 얹혀
>   있었고** ㉡ **현행 판정이 문면(취소선)에 있었다** 는 두 가지다. 이력·`90` 은 둘 다 아니다
>   (**아무도 「현행 이력」을 판정하지 않는다**) ⇒ **같은 처방을 성격이 다른 문서에 확대하지 마라.**
>
> **④ 다단위 경로 실증**
> · `handoff_write --next --label O172-A` ⇒ `O172-B` 제안 · 발행 **5,022 B / 71줄** · 도달 3축 전건 1
> · 색인 자동 재생성(라벨 **2개** · 1,703 B) · 현행 = `O0172-B`
> · 🟢 게이트 = census ✅ · 유형 🟡(1건) · 조문순서 ✅ · 행키 ✅ · 제목 ✅ · 좌표 🟡 ⇒ **전종 rc=0**
> · 🟢 음성 테스트 **38종 전건 rc=0** · `test_handoff_write` **34단정** 유지
> · 🟢 스테이지 대조 **564건 일치 · 드리프트 0** · 🔴 dbt 0건 · 라이브 SQL 0건
>
> **⑤ 독해 기록(`R1-3-7-b`)** — `read` 미반환 **0건** · 재호출 **0건**
> · `scripts/split_doc.py` 1225~1260(`rollover` docstring) · 고유 토큰 = `O(1) 국소 연산 · 재균형 불요`
> · `20_issue/00_INDEX_이슈원장_조각/-002.md:133~140` · 고유 토큰 = `## 1. 상태 대시보드 (한눈에)` 절단 지점

> #### 🟢 [2026-09-17 O172-C] A(스킬 개선분) 마감 — 음성 축8 신설 · 은퇴 불채택 판정 · 지침 여유 처방 발행
>
> **지시**(사용자) = *"B는 스킬 개선에 상관 없는데. 브리핑 받아서 생긴 작업이구나. 넌 스킬 개선
> 작업만 하면 되니까 A만 하자. 작업 완료 후 스킬개선에 더 필요한 작업이 뭐가 남았나 알려줘."*
>
> **① 🔴 사용자 정정이 드러낸 결함 — 인수인계 항목 분류**
> · `O172-A` ▣ O172-2 「다음이 할 일」에 **이 개선 작업의 잔여**와 **워크스페이스 백로그**(현업·결정
>   대기)를 섞어 적었다 ⇒ 사용자가 「B는 스킬 개선에 상관 없다」로 적발.
> · 🟢🟢 **판정식 = 인수인계의 「다음이 할 일」은 ㉠ 이 작업의 잔여 / ㉡ 워크스페이스 백로그로 갈라 적어라.**
>   🔎 왜 = 브리핑이 준 열린 작업은 **내 작업의 산출물이 아니다** — 섞으면 다음 세션이
>   「이 개선이 아직 안 끝났다」로 읽는다(범위가 조용히 번진다).
>
> **② A2 집행 — 음성 테스트 축8 신설 + 테스트 자체의 자기결함 시정**
> · 축8 = 「다단위 세션에서 후속 단위를 써도 앞 단위가 사라지지 않는가」 ⇒ 단정 **34 → 41건**.
>   `O172-B` 가 「다음 세션이 보강하라」고 넘긴 것을 **이 단위가 즉시 집행**했다(이월 아님).
> · 🟢 「고치기 전 실패」 실증 = 초판 `paths[-1]` 재현 시 `['-O0172-B']` ↔ 신 `['-O0172-A','-O0172-B']`.
> · 🔴🔴 **초판 축8 이 판정식을 테스트 안에서 재현**했다(`label_paths`+`label_rx` 로 최대값 계산을
>   다시 씀) ⇒ 그것이 곧 `J8` 결함이다. 픽스처 stem 을 `99_NEXT_SESSION` 으로 두고
>   **`session_brief.label_units()` 를 직접 호출**하도록 고쳤다.
> · 🟢🟢 **판정식 = 테스트가 판정식을 베끼면 그 테스트는 판정식이 아니라 자기 사본을 검사한다.**
>
> **③ A1 회귀 축 — 이 세션에서 실증 완료**
> · 브리핑 §2 현행 = `-O0172-A.md:17`(라벨 최대값) · 단위 목록 표기 · 항목 **5 → 9건**
> · `handoff_gap` **경고 0건**(착수표 열린 `⑭`·`⑩` 이 본문에 실려 있다 · `grep -c` = 0)
> · 🔴 잔여 축 1개 = **O번호 경계 전이**(세션이 바뀐 뒤 `O0172-A~C` 가 승계로 내려가는가).
>
> **④ A3 판정 — 승계 절 은퇴 19건 불채택(실측)**
> · `99_NEXT` 조각 **35개** · 최대 `-022.md` **28,524 B** · 여유 **12,436 B**(상한의 30%)
>   ⇒ 축3 기준(20% = 8,192 B)을 **이미 만족**.
> · 🟢🟢 결정적 근거 = 라벨 파일 전환으로 **조각이 더 이상 성장하지 않는다** ⇒ 은퇴의 유일한 근거
>   「다음 편집에서 초과한다」가 **소멸**했다.
> · 🟢 **판정식 = 용량 처방의 근거는 「지금 크기」가 아니라 「성장 경로가 살아 있는가」다** —
>   성장 경로를 끊으면 은퇴는 불필요해진다(은퇴는 성장을 못 끊을 때의 차선책이다).
>
> **⑤ A4 — `00_작업지침` 여유 부족: 실측 완료 · 처방 2안 발행 · 🔴 집행은 결정 대기**
> · 실측 = **37,025 B / 355줄** · 여유 **3,935 B**(9.6%) · 갱신형 ⇒ 다음 조문 신설에서 확정 초과 가능.
> · 계열별 = `R1` **15,217 B**(41.1%) · `R3` **7,929**(21.4%) · `R2` **5,843** · `R0` **5,086** ·
>   `R4` **2,685** · 머리말 266.
> · ㉠ **`R3` 분리 권고**(예상 여유 **11,864 B** = 29%) — 읽는 시점이 「완료 보고 전」이라 지연 로드에
>   맞고, `R3-9 ㉠`~`㉨` 매 세션 노출은 **스킬 §5-2 가 이미 담보**한다(검증기 `I8`·`I6` 축) ⇒ 가용성 손실 0.
> · ㉡ `R0` 분리(9,021 B = 22%) — ⚠️ `R0` 는 **매 bash 호출**에 적용되므로 상세를 늦게 읽으면 늦다.
> · 🔴 **임의 집행하지 않았다**(`R1-2` 임의 판단 금지 · 지침 정본을 쪼개는 결정) ⇒ 사용자 결정 대기.
> · 🟢 **판정식 = 「분리해도 가용성이 줄지 않는가」는 스킬이 그 조문을 재수록하는지로 판정한다.**
>
> **⑥ 종료 실측**
> · `test_handoff_write` 단정 **41** · 음성 테스트 **38종 전건 rc=0**(종료코드 파이프 뒤 미판독 · `R0-8-2`)
> · 게이트 = census ✅(stale 0) · 유형 🟡(경고 1 = `00_작업지침` ⇒ ⑤) · 조문순서 ✅ · 행키 ✅ ·
>   제목 ✅ · 좌표 🟡 ⇒ **전종 rc=0** · 스테이지 대조 드리프트 **0**
> · 라벨 파일 **3개**(`-A`·`-B`·`-C`) — **3단위까지 실증** · 🔴 dbt 0건 · 라이브 SQL 0건
> · 🟢 **스킬 개선 잔여 = ⑤ 결정 1건 + ③ 잔여 축 1개**(그 외 A 항목은 이 단위에서 닫혔다)
>
> **⑦ 독해 기록(`R1-3-7-b`)** — `read` 미반환 **0건** · 재호출 **0건**
> · `scripts/test_handoff_write.py` 축8 신설 구간 · 고유 토큰 = `label_units()` 직접 호출 · `J8` 회피
> · 계열별 바이트 실측(python 순회) · 고유 토큰 = `R1 15217` · `R3 7929`

> #### 🔴🔴 [2026-09-17 O172-D] 스킬 자체 갱신 누락을 사용자가 적발 — 정본만 고치고 산출물을 재생성하지 않았다
>
> **지시**(사용자) = *"스킬 개선은..스킬 자체를 바꿔야하나? 아무튼 이번 세션에서 할 수 있는건 다
> 한건가? 남은 작업 있으면 알려주고, 다음 세션에 꼭 추가해야할 프롬프트가 있다면 알려줘."*
>
> **① 🔴🔴 결함 — 「정본을 고쳤다」가 「스킬을 고쳤다」가 아니다**
> · O172~O172-C 는 지침·규약·도구·게이트·테스트를 고쳤으나 **스킬 산출물 계열 3파일**
>   (`03_init_ihcho_스킬_본문` · `_참조_세션종료` · 그 산출물 `SKILL.md`·`references/session-end.md`)을
>   **손대지 않았다** ⇒ 다음 세션이 스킬을 로드하면 **폐기된 6단계 절차**를 읽는다:
>   「여기서 시작한다를 새로 쓰고 이전 절을 `~~취소선~~`」 · 「그 취소선 때문에 `--update-golden`」 ·
>   「새 절을 쓰면 조각이 상한에 걸리니 압축 또는 재균형」.
> · 🔴 **더 나쁜 것** = **최초 리스트업에 그 3파일을 내가 스스로 적어 놓고**(A4·A5·A6) 1단계 범위를
>   좁히면서 **조용히 빼고 「1단계 완료」로 보고**했다.
> · 🟢🟢 **판정식 = 「정본을 고쳤다」와 「그 정본을 읽는 산출물이 바뀌었다」는 다른 사건이다.**
>   🔎 이 워크스페이스가 4회 당한 「자동 생성물을 손으로 고쳤다」의 **역방향**이다 —
>   이번에는 **정본만 고치고 빌더를 돌리지 않았다.**
> · 🟢🟢 **판정식 = 내가 만든 리스트업이 곧 분모다** — 범위를 좁힐 때는 뺀 항목을 **명시적으로
>   「2단계」로 옮겨 적어라.** 조용히 빠뜨리면 **아무 게이트도 신고하지 않는다**:
>   실측 = `verify_init_ihcho_skill` 의 `I2`~`I8` 이 **전건 PASS** 였다(낡은 내용이어도 **형식은 옳다**).
>   ⇒ 🔴 **형식 검증기는 「내용이 최신인가」를 보지 않는다.**
>
> **② 시정**
> · 본문 = §2 추출 판정식 표 「인수인계」 행 → **라벨 최대값 · 전 단위** · 「필요할 때만 읽는 문서」에
>   **라벨 파일 행 신설** · 조각 행에 「전부 승계됨」 병기 · §5 에 **`handoff_write` 1회** +
>   「조각에 쓰지 마라 · 취소선 쓰지 마라」.
> · 참조 = §1 「정본 3곳」 표 행 교체 + **폐기 절차 4건을 취소선으로 명시**(「하지 마라」를 본문에
>   남기는 관례) + 규격·주의 6줄 + §2 도구 표에 `handoff_write` 등재.
> · 산출물 = `build_init_ihcho_skill.py --apply`(스냅샷 선행) ⇒ `SKILL.md` **393줄** ·
>   `references/session-end.md` **104줄 / 8,905 B** · `verify_init_ihcho_skill.py` **불변식 전건 PASS**.
> · 🟢 도달 확인 = `SKILL.md:147`·`:165`·`:364~365` · `references:42`·`:49`·`:91`(grep 6곳).
>
> **③ 🟢 선례 확인 — 같은 단위에서 종결**
> · 실측 = 정본↔산출물 짝은 **2쌍뿐**(`build_init_ihcho_skill`/`verify_init_ihcho_skill` ·
>   `build_wide_doc`/`verify_wide_doc`) · `verify_wide_doc.py` = **575컬럼 · 불일치 0**
>   ⇒ 🟢 **stale 은 스킬 1쌍뿐이었고 닫혔다.**
> · 🟢 판정식 = **짝을 찾을 때 `build_*` ↔ `verify_*` 를 세면 된다**(빌더가 있으면 검증기도 있다).
>
> **④ 이 개선의 잔여 = 2건**(🔴 워크스페이스 백로그와 **분리해** 적는다 · O172-C 판정식)
> · 🔴 `00_작업지침` 분리 결정 1건 — 처방·2안·근거 정본 = `99_NEXT` §0-AAAB/O172-C ▣ O172-C-4
>   (🔴 **재분석 금지** · 그 절을 읽고 결정만 받아라).
> · 🟠 O번호 경계 전이 회귀 축 1개 — 다음 세션 브리핑 §2 로 자동 판정.
>
> **⑤ 종료 실측**
> · 스킬 `SKILL.md` 393줄 · 참조 104줄 · 검증기 전건 PASS · 제목 유실 0 · 줄길이 PASS
> · `test_handoff_write` 단정 **41** · 음성 테스트 **38종 전건 rc=0** · census stale **0**
> · 라벨 파일 **4개**(`-A`~`-D`) · 🔴 dbt 0건 · 라이브 SQL 0건 · 확정위반 **0건**
>
> **⑥ 독해 기록(`R1-3-7-b`)** — `read` 미반환 **0건** · 재호출 **0건**
> · `03_init_ihcho_스킬_본문.md` 160~190 · 고유 토큰 = `브리핑에 실리는 것(전부 정본에서 기계 추출` · `운영 환경 지뢰`
> · `03_init_ihcho_스킬_참조_세션종료.md` 54~81 · 고유 토큰 = `정본 3곳을 갱신한다` · `1,900자 이내`

> #### 🟢 [2026-09-21 O173] 브리핑 성능 병목 실측·처방 + `R3` 분리 집행 + `handoff_ddl_gate` 죽은 좌표 복구

> **착수 경위** = 사용자가 *"Poll brief again 이게 계속 나오는데 이거 왜 이렇게 오래걸리냐?
> brief 생성때문인가? 개선할 수 있나?"* 라고 물었다. ⇒ 원인이 **두 갈래**였다.

> **㉠ 내 폴링 방식(제 결함)** — `sleep 100`→`110`→`115`→`115` 를 순차로 돌려 프로세스 종료와
> 무관하게 **약 7분을 무조건 대기**했다. 🟢 처방 = `bash(run_in_background)` + `bash_output(wait=true)`
> 또는 **포그라운드 + `timeout_ms`**(이 세션은 후자를 썼다 · 아래 ㉡ 때문에).

> **㉡ 🔴🔴 환경 결함 — 백그라운드 셸이 다른 마운트를 받는다.** `/workspace` 심링크가 이 세션에서
> **2회** `user__public__snowflake_files_` ↔ `user__public_default_` 로 갈렸다. 세션 최초의
> `nohup … session_brief.py --write &` 가 **0바이트 출력**으로 끝난 것이 이것이다 — 백그라운드 셸이
> `default` 를 받아 **스크립트를 못 찾았다**(에러도 그 stdout 으로만 갔다).
> 🔴 **판정식 = 「0바이트 출력」은 「도구가 조용했다」가 아니라 「도구가 그 자리에 없었다」일 수 있다**
> (`J1`「0건 앞에서 rc·경로를 함께 보라」의 마운트 판본 · `J2`「내가 본 그릇이 그 그릇인가」).
> 🟢 처방 = 이 워크스페이스에서 **백그라운드 실행을 쓰지 마라** · 포그라운드 + `timeout_ms`.

> **1. 성능 — 🔴 내 가설이 실측에 2회 뒤집혔다(그 경위가 이 항목의 본체다)**

> | 단계 | 실측 | 판정 |
> |---|---|---|
> | 게이트별 벽시계 | `doc_census` **115.52s** / 6종 합 **136.95s** = **84%** | 병목 확정(나머지 5종 합 21.4s) |
> | 가설㉠ **이중 읽기** | warm 구/신 패턴 비교 **1.7x · 0.173→0.103s** | ❌ **틀림** |
> | 가설㉡ **대역폭 바운드** | dbt 디렉터리(평균 530KB) 정규화 시 1.83→2.26 MB/s = 1.2x | ❌ **틀림**(그 구간만 그렇다) |
> | 실제 원인 | distinct **241파일 / 4,737,502 B** · warm **0.30s** ⇒ **파일당 ~0.48s** · 실효 41KB/s | **지연 바운드** |
> | 가설㉢ **병렬화** | 진짜 cold 소파일 60개: 직렬 **36.38s** ↔ 16워커 **9.79s** = **3.7x**(병렬군이 바이트를 **더** 읽고도 빨랐다) | ✅ 채택 |

> 🔴 **가설㉠ 이 틀린 이유를 기록해 둔다** = cold 에서 **두 번째 읽기는 어차피 warm** 이므로
> 이중 읽기를 없애도 **cold 비용은 줄지 않는다**. 「읽기 횟수를 줄였으니 빨라진다」는 직관이
> **캐시 계층이 있는 곳에서는 성립하지 않는다.**
> 🔴 **가설㉡ 이 틀린 이유** = 같은 스테이지가 **파일 크기에 따라 다른 구간**에 있었다.
> 평균 530KB 에서는 대역폭, 평균 20KB 에서는 지연이 지배한다 ⇒ **한 코퍼스의 측정으로
> 다른 코퍼스의 처방을 정하지 마라.**
> 🔴🔴 **초기 보고 오류(자기시정)** = 1차에 「115.52s → 0.64s」라고 봤는데 그것은
> **워밍 캐시 교란**이었다(2회차는 warm). ⇒ 🟢 **판정식 = I/O 성능 측정은 「온도」를 통제하지 않으면
> 전부 거짓이다.** 그래서 이후 비교는 ㉠ 둘 다 warm 에서 번갈아 재거나 ㉡ **미접촉 파일**로 잡았다.

> **처방 3건(`scripts/doc_census.py`)**
> · `_BLOB` **프로세스 1회 바이트 캐시** — `census()` 가 같은 파일을 `nbytes_of`+`read_text` 로 2회,
>   append형 꼬리는 3회 열고 있었다. 🔴 `O143` 불변식 유지 = 바이트 수는 **여전히 읽은 결과**이고
>   `os.path.getsize`(블록 반올림 오탐 경로)에 의존하지 않는다.
> · **16워커 `prefetch()`** — `census()` 가 읽을 파일을 먼저 모아 cold 지연을 중첩시킨다.
>   실패 파일은 캐시에 넣지 않아 **기존 예외 경로가 그대로 살아난다**.
> · 🔴 **잠복결함 1건 시정** = `read_text` 를 바이트 `decode` 로 바꾸면서 텍스트 모드의
>   **universal newlines**(`\r\n`·단독 `\r` → `\n`) 번역이 사라졌다. 실측 = `.md` **354개에 `\r` 0건**이라
>   지금은 값이 같지만, CRLF 파일 1개가 들어오면 **문자 수 판정이 갈라진다** ⇒ 명시적으로 재현했다.
>   🟢 **판정식 = 「지금 값이 같다」는 등가의 근거가 아니다**(`J8` 자기참조 축과 같은 형태).

> 🔴🔴 **cold 개선폭은 미확정으로 남긴다** — 재마운트 후 얻은 cold 실측은 `census()` **78.39s**
> (= 0.32s/파일 · 개선 전 0.48s/파일)이고, **격리 벤치 3.7x 와 격차가 크다**. 원인은 규명하지 못했다
> (후보 = FUSE 계층 직렬화 · prefetch 앞의 직렬 `os.listdir`/`outdir_marker`).
> 🔴 **워커 수 최적값은 판정 보류**다 — 48워커 측정은 앞선 실행이 같은 파일을 warm 하게 만들어 **무효**였다.
> 🟢🟢 **판정식 = 이 측정은 자기 분모를 소모한다**(한 번 읽은 파일은 warm 이 되어 재실험 불가) ⇒
> **cold 측정 기회는 마운트가 새로 붙을 때 1회뿐이다. 그 1회를 무엇에 쓸지 미리 정하라.**
> 🟢 부수 관측 = 음성 테스트 38종 전건이 **340.9s → 74.6s**(warm + prefetch).

> **2. `R3` 분리 집행 — 처방 `O172-C-4` ㉠안(재분석 없이 그대로 따랐다)**

> · 실측 착수값 = `00_작업지침` **37,025 B / 355줄** · 여유 **3,935 B(9.6%)** · 갱신형.
> · 신설 = `00_guides/04_단계종료_게이트.md`(**9,255 B / 100줄**) · 조문 번호 **`R3-N` 불변**.
> · 🟢 **전사 오류를 원리적으로 없앴다** — 본문을 손으로 옮기지 않고 **원문에서 기계 추출**했다
>   (`## R3.` ~ `## R4.` 경계 · 257~328행 · 7,928 B).
> · `R2-8-1` 기계 대조 = **토큰 66종 · 목적지 부재 0** · **블록 무변경 포함 단정 `True`**.
> · 원본은 **포인터 스텁**으로 축소(조문 제목 9개 + 읽는 시점 + 가용성 담보 근거).
>   결과 **31,232 B / 305줄** · 여유 **9,728 B(23.7%)** ⇒ **축3 경고 해소**.
>   ⚠️ 처방 예상치는 11,864 B 였다 — **내 스텁이 처방 가정보다 크다**(조문 제목을 남겼다).
> · 분모 **4곳 편입** = `doc_type_gate.EXTRA_DOCS` · `doc_heading_gate.DOCS` ·
>   `doc_line_length_gate.CANON` · `doc_census.SINGLES` + 원장 §0 유형 등재표.
> · **골든 발행** = 제목 유실 1건이 검출됐고(의도된 이동), 목적지 제목 실재·원본 이중화 부재를
>   확인한 뒤 `--reason` 으로 발행했다(문서 **24종** · 제목 **1,839개** · 유실 승인 1건).
> · 스킬 갱신 3곳 = Step 1 분리 정본 표 행 추가 · §4 경로 병기 · **§5-2 머리글 정정**.
>   🔴🔴 **§5-2 는 *"그 문서는 Step 1 에서 전량 독해된다"* 고 적고 있었고 분리가 그 전제를 거짓으로
>   만들었다** — 이것이 `J5`(「예약 조치를 폐기했으면 그 문장을 전수 찾아라」)의 실물이다.
>   🟢 `I6`(`R3-9 ㉠`~`㉨` 매 세션 노출)은 §5-2 표가 그대로 담보한다 ⇒ **가용성 손실 0**.
> · 🔴 **`I7` 위반이 실제로 터졌다** = 스킬 본문에 「분리된 정본 **3종**」이 박혀 있어 4종이 되는 순간
>   stale 이 됐다 ⇒ **개수를 지우고** 「개수를 적지 않는다」로 교체했다.

> **3. 별건 발견 — `handoff_ddl_gate` 가 크래시 중이었다(브리핑 분모 밖이라 무관측)**

> · 증상 = `FileNotFoundError: 99_provided_definition/11_bronze_crm_ddl.sql` 로 **rc=1 크래시**.
>   원인 = 폴더가 **`99_` → `90_` 로 개명**됐는데 게이트가 따라가지 않았다(죽은 좌표 **8곳**).
> · 🔴🔴 **왜 아무도 못 봤나** = `session_brief.GATES` 는 **6종**이고 이 게이트는 그 분모 밖이다.
>   인수인계 기준선은 「게이트 9종 rc=0」이라 적고 있었다 ⇒ **기재와 상태가 갈렸다**(`J3`).
> · 🟢 치환 8곳 후 rc=0 · **판정 축 7종 전건 0건**(O171 기준선과 일치) · 관측 축 = COMMENT 양쪽 부재 9건.
> · 🟢 **판정식 = 「게이트 N종 rc=0」을 근거로 쓰려면 그 N 이 실행 목록의 N 인지 보라.**
>   브리핑이 돌리는 집합과 기준선이 주장하는 집합이 다르면 **기준선은 검증되지 않은 문장이다.**

> **4. 🔴 자기결함 2건(전부 즉시 시정 · 은폐하지 않는다)**

> · ㉠ **`R1-7-8` 앵커 사고 재발** — 원장 유형표에 행을 삽입하려다 `edit` 앵커가 넓어
>   기존 행의 `🆕 **[2026-09-16 O167]**` 토큰을 **삭제**했다. 즉시 복원하고, 이후 삽입은
>   **python 줄 인덱스**로 바꿨다. 🔴 이 결함은 조문에 명문화돼 있는데도 **또** 났다.
> · ㉡ **내 등재 행이 새 경고를 만들었다** — 유형표 행이 667자(1,084 B)여서 원장 여유를
>   8,904 → **7,819 B** 로 밀어 축3 경고를 **새로 발생**시켰다(`00_작업지침` 경고 1건을 없애고
>   원장 경고 1건을 만든 셈). 행을 **331자(532 B)** 로 축약해 회수했다.
>   🟢 **판정식 = 「경고 건수」가 아니라 「어느 문서가 경고인가」를 보라** — 총계는 같아도
>   원인이 옮겨간 것이면 해소가 아니다(`㉤` 집합 차이 축의 문서 판본).

> **5. 📏 종료 기준선(실측 · 🔴 인용하지 말고 재라)**

> · 게이트 **13종 rc=0** = census · 유형(🟡 경고 2건) · 조문순서 · 행키 · 제목 · 좌표 ·
>   gate_census · sv_code_label · handoff_ddl(복구) · 줄길이 · ID충돌 · 결정종결 · 마운트크기
> · 음성 테스트 **38/38 rc=0** · `test_handoff_write` 단정 **41 → 44**(축9 신설)
> · 검증기 `verify_init_ihcho_skill` **불변식 전건 PASS**(경고 0) · `SKILL.md` **399줄 / 32,854 B**
> · `00_BRIEF` **12,201 B / 7,986자**(여유 20,567) · 생성 **24.9s**
> · 🟠 **여유 부족 2건 잔존** = `00_INDEX_이슈원장` **6,304 B** · `03_init_ihcho_스킬_본문` **6,996 B**
>   🔴 둘 다 **이 세션이 더 좁혔다**(대시보드 행 + 스킬 포인터 3곳) — 처방은 `--rebalance` 이고
>   **집행하지 않았다**(다중 파일 재작성 · `R4-4-3` · 사용자 판단 사항).
> · `read` 미반환 **0건** · 재호출 **0건**
> · 🔴 라벨 = `id_collision_gate --next O` 가 **O174** 를 제안했으나 **정의 축 최대는 172** 이고
>   참조 173 은 **내가 이 세션에 쓴 주석**이었다(`P222` 부풀림) ⇒ **O173** 을 썼다.

_Co-authored with CoCo_
