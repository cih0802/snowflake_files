<!-- SPLIT-CHUNK 01_세션이력.md | 041/041 | 허브 = 01_세션이력.md | 원문 1~136행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-29 O114] O113 승계 미결 6건 착수 — 라이브 3축 실측으로 1.5건 종결 · CSV 헤더 68/68 대조 · 적재는 사용자 결정으로 미실행
>
> **인수** = `99_NEXT §0-TTT ▣TTT4`(O113) · **정본 행** = 원장 §1 `O114` · **다음 세션** = `99_NEXT §0-UUU`
> **라벨** = `id_collision_gate --next O` 실측 「정의 113 ↔ 참조 113 ⇒ O114」
> 🟢 **착수 전 원장 §1 선점 등재**(`R1-4-3`) — O113 이 끊은 순서를 복구했다.
> ⚠️ **`R4-4-2` ㉡ 분기 적용**(지시 동봉 ⇒ 브리핑 후 즉시 착수) · **확정위반 0** · 되물음 1회(아래 D3).
>
> ##### 1. 계정 성격 — 이 세션은 「문서작업 전용」이 아니었다
>
> 🔴 **계정명은 맥락이고 판정 근거가 아니다**(`R3-9 ㉤` · O106 사용자 결정) ⇒ 아래는 **조회 결과**다.
> `SHOW DATABASES` = 7건(`ADMIN`·`GN_DW`·`SANDBOX`·`SNOWFLAKE`·`SNOWFLAKE_LEARNING_DB`·
> `SNOWFLAKE_SAMPLE_DATA`·`USER$TRIALADMIN`). `GN_DW` = owner `GN_DW_ADMIN`.
> `GN_DW` 스키마별 BASE TABLE = `BRONZE_CRM` **46** · `BRONZE_ERP` **2** · `BRONZE_AGENCY` **4** ·
> `ML` **16** · `SILVER` 43 · `GOLD` 37 ⇒ 🟢 **04·05·06번 DDL 의 O113 갱신본이 이미 집행돼 있었다.**
> 🔴 **A 원천 DB 는 없다** ⇒ A 소관 작업은 이 계정에서 원리적으로 불가하다.
> 🔴 **전 스키마 총 0행** ⇒ **적재 전**이다(「대상 아님」이 아니다).
>
> ##### 2. `▣TTT4 ㉤` 종결 — 「값이 한 칸씩 밀렸다」가 이 계정에서는 성립하지 않았다
>
> 인수 문안은 「2026-08-29 이전 적재분이 있으면 값이 한 칸씩 어긋나 있다 = 전량 재적재 대상」이었다.
> **3축으로 재판정**했고 세 축이 모두 같은 결론을 냈다:
>
> | 축 | 실측 |
> |---|---|
> | 라이브 구조 | `INFORMATION_SCHEMA.COLUMNS` **67행** · `BDGT_PRCD_NM` **3번째 실재** · `MNYRS_COST_DIV_YN` **부재** · 말미 64~67 = `YEAR_BDGT_AMT_12`·`CHN_BDGT_AMT_12`·`ADJ_BDGT_AMT_12`·`EXEC_AMT_12` |
> | 데이터 | 메타 `ROW_COUNT` 0 · `BYTES` 0 · 🟢 **직접 `COUNT(*)` 도 0**(메타만 믿지 않았다) · `CREATED` 2026-08-28 22:23:08 -0700 |
> | CSV | 07번 A.1 (4) 를 `BRONZE_ERP` 로 범위 한정 실행 ⇒ `BDGT_ACMSLT_LEDGER` **67/67 `MATCH`** · `EXPENSE_RESOLUTION` **16/16 `MATCH`** |
>
> ⇒ 🟢 **오염 없음 확정.** 🔴 **`TRUNCATE` 를 실행하지 않았다** — 대상 0행이라 무효과이고,
> 실행하면 「했다」는 기록만 남아 다음 세션을 오도한다. 남은 일은 **재적재가 아니라 최초 적재**다.
> 🟢 **인수 문안을 지우지 않았다** — 다른 계정·다른 시점에는 여전히 유효하므로 **병기**로 남겼다
> (`▣TTT1` 표 · `▣TTT4 ㉤` 취소선 + 대체 처방 · 07번 A.1 [원인 B]).
>
> ##### 3. `▣TTT4 ㉥` 절반 종결 — 「0건」이 「아직 오지 않았다」였음을 실물로 확인했다
>
> O113 실측(04:37~04:44) = `ML/` 16/54/28.9MB 완료 · `BRONZE_ERP/` 2 완료 ·
> `BRONZE_CRM/` **측정 중 계속 증가** · `SILVER/` **0건**.
> O114 재실측(`LIST` 전량 1,968행 → 접두 집계 · 조회 시각 05:30:02 UTC):
>
> | 접두 | 디렉터리 | 파일 | MB | 최종 수정 | 판정 |
> |---|---|---|---|---|---|
> | `BRONZE_AGENCY/` | 4 | 5 | 4.3 | 04:37:51 | 🟢 완료 |
> | `BRONZE_CRM/` | 46 | 280 | 3,101.4 | 04:44:23 | 🟢 **완료**(약 47분 무변화 + 스테이지 46 = DDL 46 집합 일치) |
> | `BRONZE_ERP/` | 2 | 2 | 0.7 | 04:37:43 | 🟢 완료 |
> | `ML/` | 16 | 54 | 28.9 | 04:38:14 | 🟢 완료 |
> | `SILVER/` | 1 | 1,623 | 25,973.4 | 05:29:43 | 🔴 **진행 중** |
>
> 🟢 **판정을 1회 값으로 하지 않았다** — 약 105초 후 `SILVER/` 재실측 = **1,687파일 / 26,984.9MB /
> 최종 수정 05:31:28 UTC**(+64파일 · +1,011.5MB) ⇒ **증가 중**이 실측으로 확정됐다.
> ⚠️ **O113 의 「SILVER 0건」은 「대상 아님」이 아니라 「아직 오지 않았다」였다** —
> `▣TTT3`(「0건을 받으면 판정식을 먼저 의심하라」)의 실물 사례가 하나 늘었다.
> 🔴 **역방향도 성립한다**: 지금 「1,687파일」도 **최종값이 아니다** ⇒ 다음 세션은 반드시 재측정한다.
>
> ##### 4. 부수 성과 — 07번이 「유일한 방어선」이라 선언한 검사를 처음으로 전 범위 실행했다
>
> 07번 A.1 (4)(CSV 헤더 ↔ 대상 테이블 컬럼 이름·순서 대조)는 `ORDER_OR_NAME_MISMATCH` —
> **COPY 가 오류 없이 통과하면서 값이 밀리는 유형** — 을 잡는 유일한 검사다. 결과:
>
> | 범위 | 결과 |
> |---|---|
> | `BRONZE_ERP` 2 | **2/2 MATCH** |
> | `BRONZE_AGENCY` 4 + `ML` 16 | **20/20 MATCH**(다른 진단값 0건) |
> | `BRONZE_CRM` 46 | **46/46 MATCH** — `TM_CM_CMPGN_MNG`(36/36) · `TM_CM_MKTNG_UTM`(12/12) 포함 |
> | `SILVER` 1 | 🔴 **미실행**(업로드 중이라 헤더 미확정) |
>
> ⇒ **68/68 `MATCH`** · `COUNT_MISMATCH` 0 · `ORDER_OR_NAME_MISMATCH` 0 · `TABLE_MISSING` 0 · `FILE_MISSING` 0.
> 🟢 **O113 이 문서에서 판정한 원천 델타가 실물 CSV 헤더와 일치**했다(`TM_CM_CMPGN_MNG` 34→36 · 신규 12컬럼).
> 🟢 **범위 한정이 핵심이었다** — 07번 원문 주석은 「헤더 1줄만 읽으므로 비용이 거의 없다」고 했지만
> 실제 쿼리는 `MIN(CASE WHEN METADATA$FILE_ROW_NUMBER = 1 …)` + `GROUP BY` 라 **파일 전량을 스캔**한다.
> `SILVER/` 만 26,984.9MB 이므로 전량 실행은 비싸다 ⇒ **스테이지 경로에 접두를 붙여** 좁혔다.
> 🟢 `FF_CSV_PEEK` 는 `CREATE OR REPLACE` 대신 **`IF NOT EXISTS`** 로 만들었다
> (응답 = 「already exists, statement succeeded」 ⇒ 기존 객체를 교체하지 않았다).
> ⇒ 🔴 **07번 주석의 「비용이 거의 없다」는 범위 한정을 전제로만 참이다** — 다음 세션이 오해할 수 있으므로
>    `▣UUU2` 에 재현법으로 명시해 두었다.
>
> ##### 5. 닫지 못한 4건 — 「무시」가 아니라 「열린 이유를 그 자리에 적었다」
>
> `R3-9 ㉥`(「무시는 답이 아니다 — 병기해 닫거나 열린 이유를 그 자리에 적는다」)를 이행했다.
> 🔴 **네 건 다 「도구가 없어서」가 아니라 「이 계정·이 세션의 권한 밖」이다.**
>
> | # | 무엇 | 열린 이유 | 어디서 닫히나 |
> |---|---|---|---|
> | `㉠` | SILVER 컬럼 COMMENT 공백 9건(`STSLC_` 계열) | 🟢 **축을 하나 늘렸다** — `GN_DW.SILVER.BIGQUERY_REFINED_DATA` 9컬럼 `COMMENT` 조회 = **전부 `None`** ⇒ 부재는 **원천·인수인계·라이브 3곳 전부**다. 🔴 그래도 「빠진 것 ↔ 의미 미확정」을 구별해 주지 않는다 ⇒ **문안 창작 금지** | 원천 소관자 회신 후 `99_provided_definition/18` → `06`번 순서 |
> | `㉡` | `02_1_A DB정보.sql` 재추출 | 🔴 **A 원천 DB 가 이 계정에 없다**(`SHOW DATABASES` 7건) — 「없다」가 아니라 **「접근 불가」**다 | A 접근 세션 · 그때 04번 병합 규칙 우선순위를 **함께** 되돌린다 |
> | `㉢` | 신규 2테이블 행수 실측 | 🔴 **C 도 전 스키마 0행**이라 못 잰다. ⚠️ 스테이지 파일 수·MB 는 **행수의 대체물이 아니다** | 적재 후(`▣UUU3 ⑥`) · 01번 §6.2 표 |
> | `㉣` | 신규 CRM 컬럼 3종 한글 COMMENT | 🔴 **사람 소관** — 확정 문안을 받기 전에 04번을 고치면 **창작이 정본이 된다** | 현업 회신 |
>
> ##### 6. 적재 미실행 — 사용자 결정
>
> `▣TTT4 ㉥` 정본은 「실재를 확인한 뒤 07번 A.5 / A.4 를 실행한다」였고, 확인 결과
> **68테이블이 적재 가능**(헤더 대조 통과 · 스테이지 정지)이었다. 그러나 **약 3.1GB 라이브 쓰기**는
> `R4-4-3`(파괴 가능 작업 별도 승인) 인접이라 판단을 물었고, 사용자는 **「적재하지 않는다 — 판정만
> 남긴다」** + **「SILVER 업로드 종료를 이번 세션에서 대기하지 않는다」**를 선택했다.
> ⇒ 🟢 **그 결정을 실행 지시로 문서화**했다 = `99_NEXT ▣UUU3` 7단계(① SILVER 정지 재확인 →
> ② A.1 (4) SILVER → ③ A.4 → ④ A.5-B → ⑤ A.5 → ⑥ 01번 §6.2 채움 → ⑦ A.6 검증).
> 🔴 그 표에 **금지 사항도 함께** 적었다 = `LOAD_BRONZE_SCHEMA('ML')`·`('SILVER')` 금지(반정형 문자열화) ·
> `04_..BRONZE_DDL` 적재 후 재실행 금지(`DDL-ORDER-1` · `create or replace schema`).
>
> ##### 7. 자기결함 (전건 자기신고 · 🔴 제목에 건수를 적지 않는다 — O112 가 그렇게 stale 을 만들었다)
>
> `D1` 🟠 **스냅샷 라벨을 비운 채 유지 연산을 돌렸다.** 첫 원장 `--republish` 를 `--label` 없이 실행해
>   `_archive/00_INDEX_이슈원장.md.UNLABELED-prehub.2` 가 남았다 — `R1-7-10` 이 금지한 「스냅샷의
>   「누가」가 거짓이 되는」 상태다. 도구가 🟠 경고를 냈는데 **그 경고를 그 자리에서 처리하지 않았다**.
>   ⇒ 🟢 이후 연산은 전부 `--label O114` 로 돌렸고, `▣UUU5 ㉠` 에 **착수 직후 `export SESSION_LABEL`**
>   을 다음 세션 처방으로 적었다.
> `D2` 🟠 **`read` 미반환 1건** = `50_handoff/07_..C_CONSUMER.sql`(579줄) 전량 1회 호출.
>   재호출 **4건**(`offset` 1·151·301·451 · `limit` 150·150·150·130)으로 전량 확보했다.
>   판정식(재호출 ≥ 미반환) **충족**. 🔴 500줄 넘는 파일은 **처음부터 분할**해야 했다(`R1-3-3` 취지).
> `D3` 🟠 **`R4-4-2` 상 되물음 1회**(적재 범위). 정본에 「실행한다」가 있었으므로 형식상 물을 필요가
>   없었다. 🔴 다만 **3.1GB 라이브 쓰기**라 `R4-4-3` 예외 판단은 유지한다 ⇒ 🟢 **다음 세션은 `▣UUU3`
>   이 승인 이력이므로 다시 묻지 말고 ③④ 부터 실행하라**고 명문화했다(같은 질문을 두 번 하지 않도록).
> `D4` 🟠 **내 원장 행이 「여유 부족」 경고를 유발했다.** `00_INDEX_이슈원장` max 조각
>   31,982 → **35,094 B** · 여유 8,978 → **5,866 B**(20% 미달). O111-B 자기결함 ⑤ 와 **같은 유형**이다
>   (그때는 `01_문서분할_규약`). 🔴 **경고(🟠)는 보고 대상이고 임의 재균형은 `R4-4-3` 승인 대상**이라
>   이 세션은 **돌리지 않고 보고했다**(스킬 Step 3 「경고는 보고만」). ⇒ 🟢 다음 세션 처방 = `▣UUU5 ㉣`.
>   ⚠️ **원인은 「행이 길다」가 아니라 「긴 행이 한 조각에 몰린다」**다 — 1,900자 상한을 지켜도
>   세션이 쌓이면 같은 경고가 다시 난다(O111·O112·O113·O114 행이 전부 이 조각에 있다).
> `D5` 🟢 **음성 테스트 1종이 `SESSION_LABEL` 을 켜면 실패했다 — 발견하고 시정했다.**
>   `test_split_doc_toc.py` 가 스냅샷 이름을 `'T_문서.md.UNLABELED-prehub'` 로 **하드코딩**해,
>   `R1-7-10` 처방대로 `export SESSION_LABEL=O114` 를 한 상태에서는 실제 파일이 `.O114-prehub` 로
>   생겨 **`FileNotFoundError`** 가 났다(전건 실행 14종 중 **1종 탈락**).
>   🔴 **이것이 `R3-9 ㉡`(같은 것을 「다르게 재는」 지점)의 실물이다** — 도구는 「인자 → 환경변수 →
>   폴백」으로 라벨을 정하는데 **테스트는 상수를 읽고 있었다** ⇒ 지침이 권하는 관행을 지키면
>   테스트가 깨지는 역설이 생겼다. 🟢 시정 = `snapshot_util.resolve_label()` 로 기대값을 계산
>   (도구와 **같은 규칙**을 쓰게 했다 · 2곳). 🟢 검증 = `SESSION_LABEL` **있을 때·없을 때 둘 다**
>   76단정 PASS. ⚠️ **이 결함은 D1(라벨 누락)을 고치려다 드러났다** — 첫 연산을 라벨 없이 돌렸을 때는
>   테스트가 통과했고, 라벨을 켠 뒤에 실패했다.
> ⚠️ **`R4-4-4` 롤백 판단용 기록** = 이 세션은 `R4-4-2` ㉡ 분기로 진행했고 **확정위반 0**이다.
>
> ##### 8. 갱신한 정본
>
> | 무엇 | 어디 |
> |---|---|
> | 원장 §1 상태 대시보드 `O114` 행 | 착수 전 **선점 등재** → 종료 시 판정으로 교체(1,902자 · 3칙 ㉢ 준수) |
> | `99_NEXT` | **§0-UUU 신설**(시작점 · `▣UUU1`~`▣UUU5`) + **§0-TTT 취소선 승계 표기** |
> | `99_NEXT §0-TTT` | `▣TTT1` 표 병기 · `▣TTT4` 6행 전건 병기(㉤ 는 취소선 + 대체 처방) — 🔴 **지우지 않았다** |
> | `50_handoff/07` | A.1 [원인 B] 에 O114 실측 병기(3축 근거 + `TRUNCATE` 금지 + 「다른 계정에는 유효」 보존) |
> | `50_handoff/01` | §6.2 브론즈 행수표에 병기(C 0행 = 적재 전 · 🔴 파일 수·MB 를 행수 대신 적지 마라) |
> | 근거 파일 | `_o114_measure.md` — 🔴 **판정보다 먼저** 원문 구절 + 좌표로 기록(`R1-3-7-c`) |
>
> ##### 9. 유지 연산에서 재현한 3칙 (O112 신설분)
>
> 🔴 **㉠ 이 세션도 `--expect` 토큰에서 한 번 멈췄다.** `㉤ 종결` 을 토큰으로 줬는데 실제 문안은
>   `` **`㉤` 종결`` — **백틱이 문구 중간에 끼어** 연속 리터럴이 아니었다. 게다가 `O114 병기` 는
>   **다른 문서(`99_NEXT`)의 토큰**을 원장에 준 것이었다 ⇒ **FAIL 원인이 파일이 아니라 선언 2건**이었다.
>   🟢 `착수 전 선점 등재` · `적재 미실행 — 사용자 결정.` 로 교체해 통과했다.
>   ⇒ **O112 의 경고가 그대로 재현됐다**(「가드가 멈추면 파일보다 토큰을 먼저 의심하라」).
> 🟢 **㉡ 유지 연산 직후 FAIL 을 「내가 깼다」로 오판하지 않았다** — 가드가 **쓰지 않고 멈췄으므로**
>   파일은 애초에 손상되지 않았다. 문서를 고치지 않고 **토큰만 고쳤다.**
> 🟠 **㉢ 원장 행이 1,952 → 1,904 → 1,902 로 두 번 깎였다**(상한이 아니라 **1,900 목표** 때문).
>   ⇒ O112 는 세 번, O114 는 두 번이다. 🟢 다음 세션은 **①~⑥ 각 항목 1문장** 초안을 먼저 잡아라.

> #### 🟢 [2026-08-29 O114-B] 브론즈 적재 후 SILVER·GOLD 영향 검토 + 자기검토 승인 집행 — 예산 그레인 결함 적발(DEC-44) · 게이트 분모 결함 시정 · 재균형 중단 사고 복구
>
> **인수** = 같은 세션 후속 단위이므로 **접미로 이었다**(새 번호 미발급 · `O111-B` 관례).
> **정본 행** = 원장 §1 `O114` 행에 병기 · **다음 세션** = `99_NEXT §0-UUU`(`▣UUU6`~`▣UUU8` 신설)
> **사용자 지시 2건** = ㉠ 「브론즈 적재했다 — SILVER·GOLD 반영·수정 항목 검토」
> ㉡ 「이번 세션을 비판적으로 검토하고 **승인 필요 작업은 전부 승인** · 작업과 문서를 개선하라」
>
> ##### 1. 🔴🔴 최대 발견 — 브론즈 적재가 예산 팩트의 그레인을 깼다 (정본 = `DEC-44`)
>
> 원천 `BDGT_ACMSLT_LEDGER` 가 신설한 **`BDGT_PRCD_NM`(편성 차수)** 이 `BUDGET_ITEM_DK`
> **MD5 10컬럼 산식에 없다.** 키 카디널리티 실측(NULL-safe) =
> 원장 **247행** → 현행 10컬럼 **179**(중복 68행) → +`BDGT_PRCD_NM` 221 →
> +`BDGT_ITEM_NM` 223 → +`DVLP_INBOUND_PATH` **247 = 유일**.
>
> * 🔴 **44 키 중복 · 그중 42 키가 `연사업`+`추가경정`을 한 키에 합친다.**
>   섞인 키의 연 편성 **₩84,838,474,405** = 전체 **₩106,085,664,326** 의 **80%** · 키당 최대 6행.
> * 차수별 = `연사업` 198행/편성 ₩62,100,245,921/집행 ₩55,094,546,653/2024~2026 ·
>   `추가경정` 49행/편성 ₩43,985,418,405/집행 ₩43,911,458,395/2024~2025.
> * 🔴 **추경 표현이 「컬럼」에서 「행」으로 바뀌었다** — `CHN_BDGT_TOT_AMT`·`ADJ_BDGT_TOT_AMT`
>   **247행 전건 0**(월 12컬럼도 0) ⇒ `FACT_BUDGET_YEARLY.CHN_BUDGET_YEAR`·`ADJ_BUDGET_YEAR`
>   **전건 0** 이 되고 실제 추경은 `PLAN_BUDGET_YEAR` 에 **본예산과 합산**돼 들어간다.
> * 🔴 `FACT_BUDGET_YEARLY` 헤더가 선언한 **「1행 = 1(연×예산과목) · SUM 이 항상 안전하다」
>   불변식이 깨졌다** — 그 선언을 확인하는 테스트는 **없었다**(모델이 스키마 yml 미등재).
> * ⚠️ **월 편성 합 ≠ 연 편성 총액** = ₩66,971,333,168 vs ₩106,085,664,326(**₩39.1B·37% 미배분**).
>   🟢 **집행은 연=월 정합** ⇒ 원천 특성이며 모델 결함이 아니다(소비 안내로 처리).
> 🔴 **결정은 하지 않았다** — 「`추가경정` 이 증분인가 재작성인가」가 갈리면 답이 반대가 되고
>   그것은 **현업 소관**이다(`DEC-44` §30-B). ⇒ 선택지 4안을 정리해 두고 **키는 건드리지 않았다.**
>
> ##### 2. 🔴🔴 두 번째 발견 — `EXPENSE_RESOLUTION` 은 기회이자 함정
>
> 신규 `BRONZE_ERP.EXPENSE_RESOLUTION`(24,933행·16컬럼)은 **source 미선언 · 소비 모델 0** 이었다.
> 🟢 `RESOLUTION_DEPT_NM`(결의부서 **52종**)·`WRITE_DATE`(2026-01-01~07-22)·적요를 가지고 있어
> `_wide_schema.yml` 이 **「예산 원장은 부서별 분해가 현재 불가능하다」**(O51-F)로 적어 둔 축을 풀 수 있다.
> 🔴 **그러나 원장과 합산 불가** = 원장 2026 집행 ₩11,183,088,258 ↔ 지출결의 ₩110,291,190,623(**9.9배**) ·
> 과목 6키 고유 **2,272 ↔ 69** · **24,933행 중 5행(0.02%)만 일치** ⇒ **조인 근거가 없다.**
> ⚠️ 지출결의에 `INCOME_EXPS_DIV_NM`·`BDGT_PRCD_NM`·`JANG/KWAN/HANG_NM` 이 **없어** 13컬럼
> 그레인으로는 **원리적으로 조인 불가**다. ⇒ source 선언만 하고(계보 노출) **관계는 창작하지 않았다.**
>
> ##### 3. 🟢 CRM 축은 이미 배선돼 있었다 — 미검증 커버리지를 실측했다
>
> `CMMN_BRND`(MM297)·`MKTG_UTM`(`TM_CM_MKTNG_UTM`)은 `CRM_CAMPAIGN` 에 **이미 조인·라벨 산출**된다.
> 실측 = `TM_CM_CMPGN_MNG` 36,163행/`CMPGN_CD` **36,163 유일** · UTM 사전 191/`TRY_TO_NUMBER` **191 유일**
> ·비숫자 0 ⇒ **fan-out 없음** · `CMMN_BRND` 14종/범위(1~14) 밖 **0** ⇒ `accepted_values` **통과**.
> 🟢 **O101 이 「커버리지 미검증」으로 남긴 값을 실측했다** = UTM 고아 **21,682행 / 코드 1종 = `192`**
> (`MKTG_UTM_NM` 63.8% NULL). `MKTG_UTM_NM` `not_null` 은 **`severity: warn`** 이라 build 를 막지 않는다.
> 🔴 **`192` 를 NULL 로 정규화하거나 라벨을 창작하지 않았다**(§N-8 미확정 · `R2-7-1`).
> 🟢 **삭제된 `MNYRS_COST_DIV_YN` 은 dbt 참조 0건 ⇒ 파손 없다.** 신규 3컬럼도 참조 0(미배선).
>
> ##### 4. 🟢 시정·개선 (승인 범위 집행)
>
> | # | 무엇 | 비고 |
> |---|---|---|
> | ① | `ERP_BUDGET_ITEM.BUDGET_ITEM_DK` **`unique`** 신설 | 종전 `not_null` 뿐 |
> | ② | `ERP_BUDGET` **`relationships`** + `MONTH_NO` `not_null`·`accepted_values 1~12` | FK 계약을 기계화 |
> | ③ | `ERP_BUDGET_YEARLY` **스키마 등재** | 종전 항목 자체가 없어 **테스트 0건** |
> | ④ | `FACT_BUDGET_YEARLY` **`_gold_ready_schema.yml` 등재** | 테스트 + **컬럼 설명 신설**(종전 0건) |
> | ⑤ | singular test **3종 신설**(`warn_erp_budget_procedure_merge`·`_month_grain`·`_yearly_grain`) | `dbt_utils` 미설치라 복합키 유일성을 스키마 테스트로 못 쓴다 |
> | ⑥ | `_wide_schema.yml` `PLAN_BUDGET_MONTH` 에 **월합≠연총액 + 차수 합산** 병기 | 소비 안내 |
> | ⑦ | `EXPENSE_RESOLUTION` **source 선언**(합산 금지 경고 동봉) | build 되지 않으므로 무해 |
> | ⑧ | `scripts/dbt_schema_lint.py` **신설** | `R4-1` 로 `dbt parse` 를 못 도니 **YAML 구문·구조**라도 검증한다(2,893단정 PASS) |
> | ⑨ | `index_row_gate`·`test_generators` 를 **`resolve_label()` 경유로 통일** | 라벨 해소 **재구현 2곳** 제거 |
> | ⑩ | `test_snapshot_util` **축9 신설** | ⑨ 의 재발을 기계로 막는다(9축 48단정) |
>
> 🔴 **⑤ 3종은 `severity: warn`** 이다 — 차수를 키에 넣을지는 **설계 결정(`DEC-44`)** 이라 이 세션이
> 정할 수 없다. ⇒ **`error` 승격이 `DEC-44` 종결 조건**이고 그 사실을 3곳에 적었다.
> ⚠️ **`dbt parse`·`build` 는 실행하지 않았다**(`R4-1`) — 사용자 소관이다.
>
> ##### 5. 🔴🔴 사고 — `--rebalance` 가 중단되고 삭제가 불가능해졌다 (복구 완료)
>
> 승인받아 `99_NEXT --rebalance --fill 0.7` 을 돌렸고 **조각 삭제 단계에서 중단**됐다:
> `OSError: [Errno 5] Input/output error` (`split_doc.py:956` `os.remove`).
> ⇒ 그 시점 논리 문서 = 001~020(전문) + 021(옛 꼬리) = **내용 중복 상태**.
>
> 🔴 **삭제가 물리적으로 불가능했다** — POSIX `os.remove` 는 `EIO`,
> `cortex ws rm` 도 **계정 정지 오류**로 거부. 반면 **쓰기는 동작**했다.
> ⇒ 🟢 **021 본문을 비워** `collect_bodies` 가 아무것도 이어붙이지 않게 했다(경위를 그 파일 머리에 기록).
> ⇒ 🟢 **조각 015·016 이 개행 없이 끝나** concat 시 앞뒤 줄이 붙는 것을 발견해 **개행 2개 복원**.
>
> **복구 검증** = 재균형 전 스냅샷(`_archive/99_NEXT_SESSION.md.O114-B-prerebalance` 330,715 B) 대비
> ㉠ **공백 제외 문자 완전 동일**(157,309 == 157,309) ㉡ **비어있지 않은 줄 집합 차이 0**
> ⇒ 🟢 **내용 유실 0**(잔여 17자는 조각 경계 빈 줄).
> 🔴 **원장 재균형은 하지 않았다** — 승인은 있으나 **이 환경에서 실행이 안전하지 않다**:
> 조각 수를 줄이면 **잉여 조각을 삭제할 수 없어** 같은 중복 상태가 또 생긴다.
> ⇒ `doc_type_gate` 여유 부족 1건은 **열린 채로 두고 이유를 적었다**(무시가 아니다 · `R3-9 ㉥`).
>
> ##### 6. 🟢 그 사고가 게이트 결함을 하나 드러냈다 — `index_row_gate` 분모
>
> 복구 직후 `index_row_gate` 가 **「행 유실 9건」**(`착수 순서 ¦ ⑩⑬⑮㉒㉓㉔㉕㉖㉗`)을 냈다.
> 🔴 **문서를 고치기 전에 분모를 의심했다**(`▣TTT3`) — 결과:
> **조각 본문 기준 30행 ↔ `logical_text()` 기준 21행** ⇒ **문서는 온전하고 게이트가 9행을 탈락**시켰다.
> 원인 = `logical_text` 가 `'\n'.join(parts)` 로 이어 붙여 **조각 경계마다 빈 줄**을 만들고,
> 마크다운에서 **빈 줄이 표를 끊어** 뒤쪽 행이 새 표의 헤더·구분행으로 소비됐다.
> ⚠️ 그 함수의 docstring 은 **이미 같은 사고(35건 오탐)를 경고**하고 있었다 ⇒ `R1-6-8` **네 번째 재발**이고
> O112 가 `session_brief` 에서 고친 「조각 머리말이 `inside` 를 끈다」와 **같은 축**이다.
> 🟢 시정 = 구분자 없이 잇고 **각 조각이 개행 하나로 끝나도록만** 보정(둘 다 사고가 되는 양끝을 명시).
> 🟢 회귀 = `test_index_row_gate` **축6 신설**(오염 기반 — 옛 방식이 5행 → **2행**으로 탈락함을 실증 · 6축 15단정).
> ⇒ 시정 후 **행 유실 0 · 513행 복원.**
>
> ##### 7. 🔴 O114-B 자기결함
>
> `E1` 🔴🔴 **검토 범위를 스스로 좁혔고 그 사실을 먼저 말하지 않았다.** 사용자는 「추가되고 변경된
>   데이터」 전체를 물었는데 나는 **O113 델타(ERP·CRM)만** 검토했다. 같은 적재의
>   `SILVER.BIGQUERY_REFINED_DATA` **285,387,172행** · `ML` **1,045,732행** · `AGENCY` **255,434행**
>   축을 다루지 않았고, **GA4 원천은 `severity` 기본값(error) `not_null` 3개**를 가지고 있어
>   **build 차단 위험을 첫 보고에서 놓쳤다.** 🟢 뒤늦게 착수했으나 **컴퓨트 정지로 측정 불가**
>   ⇒ `▣UUU3` 재측정 대기 ㉠~㉥ 으로 남겼다. 🔴 **범위를 좁힐 때는 좁힌 사실과 근거를 먼저 적어라.**
> `E2` 🔴 **오류 메시지를 재조회 1회로 뒤집고 사용자에게 「일시적 오류」라고 말했다.**
>   컴퓨트 정지 오류 뒤 `COUNT(*)`·`CURRENT_TIMESTAMP()` 가 성공하자 그렇게 판정했다 —
>   두 쿼리는 **메타데이터만** 읽으므로 컴퓨트를 증명하지 않는다. `SUM()` 으로 재확인해 **정정**했다.
>   🔴 `▣TTT3`(「0건을 받으면 판정식을 먼저 의심하라」)의 **역방향** = **성공 신호도 의심해야 한다.**
>   🟢 처방 = 라이브 생사 판정은 **스캔 강제 쿼리**로 한다(`SELECT SUM(<숫자컬럼>) FROM …`).
> `E3` 🟠 **`R1-3-7-c` 를 어겼다** — 근거를 파일에 쓰기 **전에** 판정을 응답으로 냈다.
>   🟢 다행히 **컴퓨트 정지 전에** 기록해 수치가 보전됐다 ⇒ 🔴 **그 조문의 값이 실증됐다**
>   (정지 후에는 어떤 수치도 다시 얻을 수 없었다).
> `E4` 🟠 **복구 검증에 집합 비교를 써서 한 번 헛다리를 짚었다** — `set(lines)` 는 **중복 수 변화를
>   못 본다.** 다행히 같은 시점 **문자열 완전 동일** 단정이 있어 결론은 옳았다.
>   🟢 교훈 = **집합 대조와 문자열 대조는 다른 축이다**(집합만 쓰면 중복 손실이 안 보인다).
> `E5` 🟠 **`--rebalance` 를 실행한 판단 자체** — 승인은 있었으나 **삭제가 필요한 연산**을
>   **삭제가 되는지 확인하지 않고** 돌렸다. 🟢 처방 = 유지 연산 전에 **삭제 가능성을 먼저 시험하라**
>   (이 환경은 쓰기는 되고 삭제는 안 되는 상태였다).
> ⚠️ **`R4-4-4` 롤백 판단용 기록** = O114 계열은 `R4-4-2` ㉡ 분기로 진행했고 **확정위반 0**이다.
>
> ##### 8. 갱신한 정본
>
> | 무엇 | 어디 |
> |---|---|
> | `DEC-44` **신설** | `30_설계_의사결정-012.md` §30(선택지 4안 · 종결 조건 · 🔴 결정 대기) |
> | `99_NEXT §0-UUU` | `▣UUU1`~`▣UUU3` **stale 시정**(적재 완료 반영 · 「다음 세션이 적재하라」 제거) + `▣UUU6`(DEC-44) · `▣UUU7`(지출결의) · `▣UUU8`(자기결함) **신설** |
> | `99_NEXT §0-TTT` | `▣TTT4 ㉢`(행수) · `㉥`(스테이지) **종결 표기** — 지우지 않고 취소선 |
> | `50_handoff/01` §6.2 | **C 적재 후 실측 표 신설**(브론즈 52/116,152,387 · ML 16/1,045,732 · SILVER 1/285,387,172 · 총 69/402,585,291) · 🔴 08-12 수치는 **고치지 않았다** |
> | dbt | `_sources.yml`·`_silver_bridge_schema.yml`·`_gold_ready_schema.yml`·`_wide_schema.yml` + `tests/warn_erp_budget_*.sql` 3종 |
> | 도구 | `index_row_gate`(분모 시정 + 라벨 통일) · `test_generators`(라벨) · `test_snapshot_util`(축9) · `test_index_row_gate`(축6) · 신설 `dbt_schema_lint.py` |
> | 근거 파일 | `_o114b_silver_gold_impact.md`(190줄 · 실측 원문) |
>
> ##### 9. 🔴 다음 세션이 가장 먼저 할 것
>
> 1. 🔴 **컴퓨트 복구 확인** = `SELECT SUM(<숫자컬럼>) FROM <테이블>`(메타데이터 쿼리로 판정하지 마라).
> 2. 🔴 **GA4 `not_null` 3종 실측**(`▣UUU3` 재측정 대기 ㉠㉡) — **build 차단 위험**이 여기 있다.
> 3. 🔴 **`99_NEXT_SESSION-021.md` 삭제** 후 `--republish`(본문이 비어 있으니 내용은 변하지 않는다).
>    🔴 **`--rebalance` 를 다시 돌리지 마라** — 001~020 은 이미 재배치가 끝났다.
> 4. 🔴 **`DEC-44` 30-B 회신** 확보 → 1안 선택 → 배선 → **warn 3종을 error 로 승격**.
> 5. 🟠 원장 `doc_type_gate` 여유 부족 1건 — **삭제가 가능해진 뒤에** 재균형하라.
