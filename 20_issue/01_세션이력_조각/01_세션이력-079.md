<!-- SPLIT-CHUNK 01_세션이력.md | 079/079 | 허브 = 01_세션이력.md | 원문 1~116행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-22 O178-B] `GA4` → `BIGQUERY` 명칭 통일 — 식별자 개명 + `DW_SOURCE_SYSTEM` 계층 불일치 시정 + 배포 런북 확인 단계 신설
>
> 🔴 **이 항목이 정본이다** — 원장 §1 `O178-B` 행은 색인이다. 사용자 결정 **6건**.
>
> ### ▣1 🟢 배포 런북에 「재배포 후 BIGQUERY 일자 수 확인」 신설 (`O178-A-2 #1` 종결)
>
> `10_dbt_pipeline/deploy_dbt_project.sql` **Step 3-3** 신설(사고 경위·판정식·복구 5단계 포함).
> 🔴 왜 필수인가 = `O178` 이 규명한 **배포 순서 사고**의 유일한 방어다. 증분화 이후
> **재배포로 테이블이 비면 그 빈 상태가 영구히 남는다**(창에 원천이 없으면 SUCCESS + 0행).
> 두 쿼리를 **라이브 실증**했다 = ㉠ 원천↔하류 **일자 수** 4계층 대조(전부 33일 일치)
> ㉡ 감시 테이블 3종 0행. 🔴 행수가 아니라 **일자 수**를 본다(누락의 단위가 일자다).
> 🟢 부수 소득 = **`TASK_DW_*` 파이프라인 Task 는 존재하지 않는다**(`SHOW TASKS` 0건 ·
> `TASK_HISTORY` 26종 전건이 Snowflake 시스템·Trust Center Task) ⇒ `O178-A` 가 「미확인」으로
> 넘긴 항목이 해소됐고 사고 위험은 **잠재**이지 활성이 아니다.
>
> ### ▣2 🔴 명칭 통일 — 「일괄 치환하면 안 되는 이유」가 실제로 3곳에서 드러났다
>
> 사용자 결정 = **`bigquery_refined_data` 유래 데이터·작업은 전부 `BIGQUERY`**.
> 🔴 그런데 기계적 치환은 **참인 문장을 거짓으로 만든다.** 실제로 유지해야 했던 것 =
>
> | 유지 대상 | 왜 |
> |---|---|
> | bare `GA4` **150건**(산문) | 「GA4 는 UTC/로컬 경계에서 오늘+1 이 섞인다」류 = **원천 시스템 성질 서술**. BIGQUERY 로 바꾸면 거짓이 된다 |
> | `BRONZE_GA4` **23건** + `GA4_USER_DEMOGRAPHIC`(45,569행) | **외부 적재기 소관** — 우리가 바꾸면 적재가 깨진다(사용자 결정 ④ 현황 유지) |
> | `SILVER.BIGQUERY_REFINED_DATA` DDL | **현업 제공 · 불변**(사용자 확정) — `USER_PSEUDO_ID` 등 3컬럼 COMMENT 의 「GA4가 부여한」은 사실이다 |
> | `ga4_union_shards.sql` | **폐기(DEPRECATED) · 호출처 0 · 본문이 명시적 역사 기록** |
> | `_wide_schema.yml` 「원천 트랙이 GA4 다 … `GA4_USER` 정제 예정」 | 🔴 이 트랙은 `bigquery_refined_data` 가 **아니라** `BRONZE_GA4.GA4_USER_DEMOGRAPHIC` 계보다 ⇒ 결정 ④ 해당 |
> | `06_DDL.sql` `[주의: GA4 이벤트명 …]` | GA4 가 **정의한** 이벤트명(시스템 서술) |
> | `GA4_EVENT`·`GA4_DEVICE`·`GA4_BASIC` 등 | **O130 이전 옛 모델명을 기록한 역사** — 고치면 그 세션이 무엇을 개명했는지 추적 불가 |
>
> 🟢🟢 **판정식 = 개명은 「토큰 치환」이 아니라 「계보 판정」이다.** 같은 문자열 `GA4` 가
> ㉠ 개명 대상 식별자 ㉡ 원천 시스템 서술 ㉢ 다른 계보의 실물 이름 ㉣ 역사 기록으로 **4가지**였다.
> 🔴 그래서 `_o130_ga4_rename.py`(O130 의 개명기)가 **절반만 개명한 상태**로 남았던 것이다 —
> 모델명은 `BIGQUERY_*` 로 갔는데 매크로·var·테스트가 `ga4_` 로 남았다.
>
> **집행분 = 18파일 / 103건 치환 + 파일 7개 개명**(매크로 2 · 테스트 5):
> `ga4_lookback_days`→`bigquery_lookback_days` · `ga4_dt_ranges`→`bigquery_dt_ranges` ·
> `ga4_load_window`·`ga4_range_predicate`(+`_text`/`_sk`)·`ga4_range_purge` → `bigquery_*` ·
> `warn_ga4_load_gap`·`warn_ga4_null_user_pseudo_id`(+`_rows`)·`warn_ga4_id_scheme_unclassified`·
> `assert_ga4_pk_unique` → `*_bigquery_*`.
> ✅ 대사 = **TOTAL 564 불변**(개명뿐이므로 노드가 하나도 늘거나 줄지 않는다) · `OPS` 9→12 ·
> 신설 3종 0행 → 고아 `WARN_GA4_*` **3테이블 DROP**(승인 · 전부 0행) → `OPS` 9 복귀.
>
> ### ▣3 🔴🔴 최대 발견 — `DW_SOURCE_SYSTEM` 은 주석이 아니라 **데이터 불일치**였다
>
> 사용자가 정의를 알려줬다 = **원천 시스템 = 파이프라인 이전 단계**
> (`BRONZE_CRM`→`CRM` · `BRONZE_AGENCY`→`AGENCY` · `BRONZE_BIGQUERY`→**`BIGQUERY`**).
> 그 정의로 라이브를 재니 **같은 계보가 계층마다 다른 값**을 쓰고 있었다:
>
> | 계층 | 개명 전 값 |
> |---|---|
> | SILVER 6테이블(`BIGQUERY_BASIC`·`EVENT`·`DEVICE`·`EVENT_DIM`·`IDENTITY`·`TRAFFIC_SOURCE`) | 🔴 `GA4` |
> | SILVER `IDENTITY_MEMBER_XREF` | 🔴 `GA4+CRM` |
> | GOLD `DIM_BIGQUERY_EVENT`·`DIM_BIGQUERY_SOURCE`·`DIM_DEVICE`·`FACT_BIGQUERY_BEHAVIOR` | 🟢 **이미 `BIGQUERY`** |
>
> ⇒ 🟢 **명칭 선호 문제가 아니라 실재하는 결함이었다.** 모델 리터럴 **7곳** + 값 열거 주석
> `(GA4/AGENCY/GADS)` → `(BIGQUERY/AGENCY/GADS)` 를 **함께** 바꿨다(같은 것을 다르게 두지 않는다).
> ✅ 종료 실측 = SILVER 7객체 전건 **`BIGQUERY`**(XREF 는 `BIGQUERY+CRM`) · `GA4` **0행**.
>
> 🔴🔴 **그리고 여기에 함정이 있었다 — 착수 전에 잡았다.**
> `RANGED_MODELS = ['BIGQUERY_EVENT','BIGQUERY_BASIC']` 은 **롤링 윈도우**이고 창이 비어 있었다
> (원천 최대 `2026-09-14` < `오늘-3`) ⇒ 그냥 build 하면 `DW_SOURCE_SYSTEM='GA4'` 가
> **10,332,737행 전건에 그대로 남는다.** 에러 0 · 무증상.
> 🟢🟢 **판정식 = 「리터럴 값을 바꾸는 변경」에는 「전량 백필 의무」가 붙는다.**
> `O177-A-2 #4`(배선 후 백필 의무)와 **같은 class** 이고, 그쪽은 컬럼 배선, 이쪽은 상수 값이다
> ⇒ 🔴 **증분 모델에서 「과거를 다시 쓰지 않는 변경」은 절반만 반영된다.**
>
> ### ▣4 🟢 테이블레벨 COMMENT — DDL 재실행 대신 메타데이터만 전파했다
>
> `06_DDL.sql` 의 `[원천: GA4 → …]` **5건**(`DIM_MEMBER_IDENTITY`·`DIM_BIGQUERY_SOURCE`·
> `DIM_BIGQUERY_EVENT`·`DIM_DEVICE`·`FACT_BIGQUERY_BEHAVIOR`)을 `BIGQUERY` 로 고쳐
> `comment_drift_gate` 테이블레벨 축이 **5건 불일치**로 정확히 울렸다(대상 5테이블 명시).
> 🔴 **DDL 재실행으로 반영하지 않았다** — `CREATE OR REPLACE` 는 데이터를 파괴하고 그것이
> 바로 `O178 ▣2` 사고의 기제다 ⇒ `ALTER TABLE … SET COMMENT`(**메타데이터 전용**) 5건으로 전파.
> ✅ 드리프트 **0 복귀**(테이블레벨 37/37).
>
> ### ▣5 🟢🟢 사용자가 DDL 을 전체 재실행했고 — **GRANT 를 잃지 않았다**
>
> 사용자가 `06_DDL.sql` + `08_SILVER_테이블DDL` 을 **전체 재실행**(`CREATE OR REPLACE`)해
> SILVER/GOLD 를 **틀만 남긴 뒤** 전량 build 했다(슬롯이 열려 있어 창 밖 전건이 함께 재적재됐다).
> 🔴 나는 「뷰는 `CREATE OR REPLACE` 로 GRANT 를 잃는다」는 기존 규약 때문에 손실을 의심했다.
> 🟢🟢 **실측 = 손실 0.** 스키마에 **FUTURE GRANTS** 가 걸려 있어 재생성 테이블이 자동으로
> 권한을 받는다(GOLD **35건** 등재 · `FACT_BIGQUERY_BEHAVIOR` 20 grant · OWNERSHIP `GN_DW_ADMIN` ·
> 5역할 전건 · 재부여 시각 `23:03:08`). SV 7 grant 도 `created_on` 불변(객체 미교체).
> 🟢 **판정식 = 「CREATE OR REPLACE 는 GRANT 를 잃는다」는 FUTURE GRANTS 가 없을 때의 규약이다.**
> 🔴 단 **뷰·SV 는 별도 축**이다 — 이번엔 무사했으나 같은 근거로 안전하다고 일반화하지 마라.
>
> ### ▣6 🔴 환경 함정 — 마운트가 세션 중에 재생성됐다
>
> `cd /workspace/10_dbt_pipeline` 가 **`No such file or directory`** 를 냈고 python 은
> `/snowflake/stages/user__public_default_/scripts/…` 를 찾았다. 🔴 파일이 사라진 것이 아니라
> **심링크가 `05:35` 에 재생성**돼 셸의 `cwd` 가 낡은 inode 를 잡고 있었다.
> 🟢 처방 = **스테이지 실경로로 `cd` 를 다시 잡는다**(`/snowflake/stages/user__public__snowflake_files_`).
> 🔴 판정식 = **「파일이 없다」를 믿기 전에 마운트를 재라**(`J1` 계열 · 분모부터 의심).
> · 🔴 `edit` 툴의 `replace_all` 이 동일 장문 줄 2곳에서 듣지 않았다 ⇒ python 줄 인덱스 치환.
> · 🔴 `grep --include` 를 또 썼다(BusyBox 미지원) — `O176-A-4` 에 적힌 함정 **2회 답습**.
>
> ### ▣7 📏 종료 기준선 (2026-09-22 · 계정 `LK96056` · 🔴 인용하지 말고 재라)
>
> · dbt build **PASS 527 / WARN 37 / ERROR 0 / TOTAL 564**(개명 전후 불변 · 사용자 3회 실행)
> · `DW_SOURCE_SYSTEM` = SILVER 7객체 **BIGQUERY**(XREF `BIGQUERY+CRM`) · GOLD **BIGQUERY** · **`GA4` 0행**
> · `BIGQUERY_BASIC`·`EVENT` **10,332,737 / 33일** · `FACT_BIGQUERY_BEHAVIOR` **1,620,857**
> · `FACT_MEMBER_EVENT` **4,716,360** · `FACT_BUDGET` **1,332** / `_YEARLY` **111** · `FACT_MESSAGE_DISPATCH` **41,969,590**
> · `DIM_DATE` **73,414 + Unknown 1** · `DIM_MONTH` **2,413** · `WIDE_*` **14뷰**
> · `OPS` **9테이블** — `WARN_BIGQUERY_LOAD_GAP`·`_NULL_USER_PSEUDO_ID`·`_ROWS`·
>   `WARN_GOLD_FACT_BIGQUERY_DATE_SK_ZERO`·`WARN_GOLD_VIEW_COMMENT_COVERAGE` **전부 0행** ·
>   🟢 `WARN_GA4_*` **3테이블 DROP 완료**(고아 · 승인분)
> · `comment_drift_gate` **rc=0** = GOLD 테이블 746/746 · SILVER 892/892 · GOLD 뷰 **574/574** ·
>   테이블레벨 GOLD 37/37 · SILVER 47/47 · 금지문안 0
> · `dbt_schema_lint` **3,228 단정 rc=0** · `line_len` PASS · `doc_coord_gate` 깨진 좌표 **0**
> · GRANT = GOLD FUTURE GRANTS **35** · `FACT_BIGQUERY_BEHAVIOR` **20** · `SV_SERVICE` **7**(전건 보존)
> · 🔴 **배포된 DBT PROJECT 버전에는 백필 슬롯이 아직 열려 있다** — 파일은 잠갔으나
>   `ADD VERSION` 재배포 **1회가 미이행**이다(`▣ O178-B-2 #1`)
>
> _Co-authored with CoCo_

> #### 🟢 [2026-09-22 O178-C] 문서20·문서50 열린 항목 전건 실측 최신화 — 내 오기 1건 자체 적발·정정 + `pre_hook` 이중 실행 단서 발견
>
> 🔴 **이 항목이 정본이다** — 원장 §1 `O178-C` 행은 색인이다. 사용자 지시 =
> *「열린 항목을 추려 실 적재 데이터로 최신화하고, 남은 작업을 복붙 가능한 프롬프트 블록으로」*.
>
> ### ▣1 🔴 분모 판정식이 두 번 틀렸다 — 둘 다 착수 전에 잡았다
>
> ㉠ `20_현업확인_요청_조각` 에 「열린 절」 추출을 돌려 **0건**을 받았다. 🔴 그 문서군은
>    절 제목에 **상태 이모지가 없는 형식**(`## A. GOLD 지표정의 …`)이라 이모지 선두 판정식이
>    구조적으로 0건을 낸다 ⇒ `J1`(0건은 「없다」가 아니라 「내 판정식이 못 본다」).
> ㉡ 문서50 에 같은 판정식을 돌려 **33건**을 받았으나 브리핑은 **6건**이었다. 🔴 내가
>    `###`·`####` 하위 제목(자기검토 절 등)까지 세었기 때문이다 ⇒ **브리핑과 같은 `##` 단위**로 맞췄다.
> 🟢 정정 후 = 문서50 열린 `##` 절 **9건** · 문서20 은 **절이 아니라 하위 항목 단위**(§M-2·§N-13 등)로 열려 있다.
> 🟢 **판정식 = 「열린 것」을 세기 전에 「그 문서가 열림을 어떻게 표기하는가」를 먼저 확인하라.**
>
> ### ▣2 🔴🔴 자체 적발 — 내가 이번 세션에 발행한 수치가 틀렸다
>
> `O178-A`·`O178-B` 인수인계와 이력 `§O178` 에 `FACT_EVENT_ATTENDANCE.DATE_SK=0` 을
> **162,888** 로 적고 *「범위밖이 아니라 양쪽 원천 NULL」*이라 판정했다. **재현되지 않는다.**
> · 전량 재적재 후 실측 = **50** 이고 이는 문서20 §C 의 **원 기재(`FEP.DATE_SK 50→0`)와 일치**한다.
> · 총행 **1,258,775** = 원천 `CRM_EVENT_PARTICIPATION` 과 **1:1**(정상).
> 🔴 **왜 놓쳤나** = `O178` 종료 시 `R2-8-4` 재조회를 **7축만** 했고 이 팩트가 그 안에 없었다.
> 🟢 **판정식 = 「닫기 직전 재조회」의 분모는 「내가 발행한 수치 전건」이어야 한다** — 축을 고르면
> 고르지 않은 축이 그대로 인수인계에 실린다. 정정 4곳(인수인계 2 · `O178-B` 1 · 이력 1) 병기 완료
> (🔴 삭제하지 않고 「종전 기재」로 남겼다 — `R1-3-7-c` 주변 3줄 보존).
>
> 🔴 **원인 미규명 · 단서 1건** = GOLD 팩트 **5종**(`FACT_BIGQUERY_BEHAVIOR`·`FACT_EVENT_ATTENDANCE`·
> `FACT_MEMBER_DEV_ACHIEVEMENT`·`FACT_MEMBER_EVENT`·`FACT_MESSAGE_DISPATCH`)에 **모델 리터럴
> `pre_hook='TRUNCATE TABLE IF EXISTS {{ this }}'`** 가 남아 있고 `gold_fact_purge` 의 else 분기도
> `TRUNCATE` 라 **hook 이 이중 실행**된다. 🔴 `dbt_project.yml:239-244` 가 **바로 그 누적 구조를
> 경고**하고 있고, `O177` 이 분기 매크로를 신설한 목적이 그것이었는데 **모델 쪽 리터럴은 남았다**
> ⇒ 「절반만 적용된 처방」이다(`O178-B` 의 O130 절반 개명과 같은 class).
>
> ### ▣3 🟢 문서20 이슈 A~E 전건 실측 최신화 (정본 = `20_현업확인_요청-001.md`)
>
> | 이슈 | 종전 기재 | O178-C 실측 | 판정 |
> |---|---|---|---|
> | `A` 회비월 | `MK=0` 141 · 미래월 1,211행/205명 · 정상 40,053,531 | **138 / 138명** · **1,185행 / 223명** · **41,507,519 / 1,782,734명** (합 **41,508,842** 검증 통과) | 🟠 잔여 2건 |
> | `B` 마스터 부재 | distinct 9,248 (8,803+206+239) | **8,491** (**8,112**+187+192) · 고아 팩트 FMD **34,246**·FEA **10,048**·FME **272**·FMM **199** · NULL **746**/**5** | 🟠 줄었으나 미해소 |
> | `C` 캘린더 범위밖 | ~140행(1910 이전 88 + 2030 이후 2) | FME **90** · FEA **50** · 원천 1945미만 **88**(전건 `1900-01-01`) + 2145초과 **2**(전건 `9999-12-31`) · `CRM_MEMBER_SPONSOR_SPAN` **2** · `CRM_EVENT_PARTICIPATION` **0** | 🟢 **질문 2·3 종결** |
> | `D` 100% NULL | 3건 회신 대기 | `CONV_CALL_CNT` **0/39,918** · `TOT_CLICK_CNT` **0/1,719,404** · `CRM_BIZ_TARGET` **0행** | 🔴 재확인 |
> | `E` 참조무결성 | 263,611/1,134,126(23.2%) · 11,313 · 9 | **281,701/1,258,775(22.38%)** · **11,506** · **21** | 🔴 절대 증가 |
>
> 🟢🟢 **이슈 C 는 이번 세션(O178)이 실제로 닫았다** — 질문 3 *「유효하면 캘린더 범위 확장할까」*를
> 우리가 집행했고(`1991~2035` → **`1945~2145`**), 질문 2 *「2030년 이후 유효한가」*는 범위에
> 들어와 **소멸**했다. 🔴 그리고 **남은 90행은 「미래 일정」이 아니라 센티넬 쓰레기**임을 실측으로
> 확정했다(`1900-01-01`·`9999-12-31` 두 값뿐이고 2145 로 넓혀도 그대로 범위 밖) ⇒
> **캘린더 확장으로는 해결되지 않는다.** 🟢 제목의 「(1991~2035)」가 stale 이 된 것도 함께 표기했다.
> 🔴 동반 파생 = 이슈 A 의 *「`cal_end`=2035 범위 내이므로 클램프 통과」* 라는 근거 문장이
> **연도가 바뀌어** 성립 범위가 달라졌다 ⇒ **무효 미래월을 클램프에 의존해 막을 수 없다**(업무 규칙 필요).
> 🟢 **판정식 = 내 설계 변경이 다른 문서의 「근거 연도」를 조용히 바꾼다** — 인용처를 전수 세라.
>
> 🔴 **이슈 E 가 중요하다** — 비율은 23.2% → 22.38% 로 내렸으나 **절대 건수는 3축 전부 증가**했다
> (분모가 더 빨리 늘었다). 🟢 **판정식 = 비율 개선을 해소로 읽지 마라**(`O176` 의 「변동이 설계인
> 값 ↔ 사고인 값」 축과 같다). `CRM_SEND_RESULT` 고아는 **9 → 21 = 2.3배**다.
>
> ### ▣4 🟢 산출물 — 다음 세션 프롬프트 블록
>
> 사용자 지시대로 **복붙 가능한 단일 블록**을 발행했다(9절 구성 = 착수 전 필수 5축 · 정지점 P0 ·
> 실측 조사 2 · 배선·설계 3 · 현업 회신 대기(실측 병기) · 경량 정정 4 · 규약 · 환경 함정 · 기준선).
> 🔴 블록의 「정본 인수인계」 1행은 이 항목 발행으로 **`O178-B` → `O178-C`** 로 교체됐다.
>
> ### ▣5 🔴 이번 단위의 확정위반·약점
>
> | # | 항목 | 비고 |
> |---|---|---|
> | ① | 열린 절 추출 판정식 오류 **2건**(이모지 형식 · 제목 레벨) | 🟢 자체 적발 · 착수 전 시정 |
> | ② | `O178` 종료 재조회 분모를 **축 선택**으로 좁혔다 | 🔴 그 결과 `162,888` 오기가 인수인계에 실렸다(▣2) |
> | ③ | BusyBox `grep` 정규식 `\|` 미지원으로 1회 실패 | 🟢 Grep 툴로 전환 |
>
> ### ▣6 📏 종료 기준선 (2026-09-22 · 계정 `LK96056` · 🔴 인용하지 말고 재라)
>
> · dbt build **527 / 37 / 0 / 564** · 음성 테스트 **43종 rc=0** · `gate_census --final` rc=0
> · `FACT_EVENT_ATTENDANCE` **1,258,775** · `DATE_SK=0` **50** · `FACT_MEMBER_MONTHLY` **41,508,842**
> · `FACT_MEMBER_EVENT` **4,716,360**(`DATE_SK=0` **90**) · `FACT_MESSAGE_DISPATCH` **41,969,590**
> · `BIGQUERY_BASIC`·`EVENT` **10,332,737 / 33일** · `FACT_BIGQUERY_BEHAVIOR` **1,620,857**
> · `DIM_DATE` **73,414 + 1**(1945~2145) · `DIM_MONTH` **2,413** · `DW_SOURCE_SYSTEM` `GA4` **0행**
> · `comment_drift_gate` rc=0 · `doc_heading_gate` rc=0 · `doc_coord_gate` 깨진 좌표 **0** · stale **0**
> · 🔴 **배포 버전에 백필 슬롯이 열려 있다** — `ADD VERSION` 1회 미이행(다음 세션 P0)
>
> _Co-authored with CoCo_
