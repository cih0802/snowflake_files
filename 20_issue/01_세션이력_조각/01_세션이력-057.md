<!-- SPLIT-CHUNK 01_세션이력.md | 057/057 | 허브 = 01_세션이력.md | 원문 1~81행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-01 O129] 영구 NULL 컬럼 컬럼명 단위 등재 + yml 거짓 문안 2건 정정 + Agent 설계 stale 해소
>
> **지시(사용자 3건)** = ㉠ *"숫자표기는 계속 수정하게 되니 매번 수정하면 불필요한 자원이 소모된다.
> 검증용이 아닌 표기는 그냥 냅둬"* ㉡ *"agent 설계 문서나 yaml 문서의 stale 건은 해소해둬"*
> ㉢ *"dbt 모델들의 설계 중 null 로 채운 컬럼들에 대한 정보가 issue 폴더 문서에 제대로 적혀있는지
> 검토하고, null 컬럼에 대해 모델과 issue 문서 전부 최신화"*. `R4-4-2 ㉡`(지시 동봉) 적용.
>
> **① 🔴🔴 최대 발견 = `_wide_schema.yml` 의 「차원 자체는 채워져 있다」가 2건 거짓이었다.**
> `O51-F` 오버레이가 같은 문안을 5개 컬럼에 도포했고(`AD_PLATFORM`·`AD_PLATFORM_TYPE`·`AD_CREATIVE`·
> `AD_CREATIVE_TYPE`·`AD_TARGET_GROUP`), 라이브 대조 결과 **`AD_PLATFORM_TYPE`·`AD_TARGET_GROUP` 2건은
> 차원 컬럼 자체가 전건 NULL** 이었다. 즉 원인이 **두 개**(팩트 FK 센티넬 + 차원 컬럼 공백)인데
> 문안은 후자를 **부정**하고 있었다 ⇒ 🔴 **「FK 를 고치면 채워진다」는 오답을 유도하는 문안**이다.
> · 🟢 이것은 O96 이 *"나머지 도포본도 컬럼별 재검증 대상"* 이라고 남긴 그 숙제이고, O129 가 집행했다.
> · 🔴 **판정식 = 도포된 문안은 「도포 대상 전건」을 재검증해야 한다** — 1건이 참이면 나머지도 참이라는
>   추론이 성립하지 않는다(도포는 근거가 아니라 복사다).
>
> **② 🟢 `§7-C` 신설 = 영구 NULL 컬럼을 컬럼명 단위로 고정**(문서30 §7-C·§7-C-1·§7-C-2).
> 종전 §7-B(O96)는 **E군을 건수로만** 적어(*"`FMM` degen 10 · `FSE` degen 3 · `DIM_AD_CREATIVE` 3 · SILVER 6 등"*)
> **어느 컬럼인지 알 수 없었다** — 실증 = `20_issue/` 전체에서 `PLATFORM_TYPE`·`TARGET_GROUP` **0건** ·
> `매체유형|타겟그룹|잠재고객` **0건**. ⇒ 🔴 **건수는 판정의 대체물이 아니다**(컬럼명이 없으면 재스캔 없이
> 판정도 시정도 불가). 🔴 지시 ㉠ 에 따라 **건수·행수는 적지 않고 「재는 방법」만** 남겼다(`R3-9 ㉦` 와 동방향).
>
> **③ 🔴🔴 `DIM_AD_CREATIVE` 3컬럼 재판정 — 한 문장에 뭉친 사유가 컬럼마다 달랐다.**
> 종전 주석 *"원천 부재 → NULL: `PLATFORM_TYPE`·`RT_TYPE`·`TARGET_GROUP`(AGENCY 원천 미보유)"* 는
> `R2-7-2`(한 문장을 여러 컬럼에 복사 금지) 위반이었고 실제 판정은 셋이 달랐다:
> · `RT_TYPE` = 🔴🔴 **거짓**. 원천 `REBRDC_AD_CMPGN_DTLS.RE_BRDC_TY_NM` 실재 + **이미 두 곳 배선**
>   (① 같은 차원의 `AD_TYPE` ② `FACT_AD_BROADCAST.RT_TYPE`·`DEC-8`) ⇒ `DURATION_SEC` 와 **동형의
>   오배치 중복축**이고 `DEC-30` 이 이 컬럼을 빠뜨렸다. **`P14` 3회차.**
>   🟢 처분 판단 근거 보강 = `AD_RT_TYPE`·`ac.RT_TYPE` **WIDE 노출 0건** ⇒ §7-B 의 뷰 무효화 순서 제약 불필요.
> · `TARGET_GROUP` = 🟠 참이나 불완전 — 정본 트랙이 **GA4**(원천표기 GA → `GA4_USER` phase-2).
> · `PLATFORM_TYPE` = 🟢 참 — BRONZE AGENCY 3테이블에 매체유형 축 없음 · 인접 유형축 3종은 전부 타 목적지 배선.
> ⇒ 🔴 **판정식 = 「같은 사유」로 묶인 컬럼군은 묶음 자체를 의심하라.** 이 건은 묶음 3 중 1 이 거짓이었다.
>
> **④ 🟢 Agent 설계 stale 해소 = 초수(요건 `#22`) 판정이 낡아 소비를 막고 있었다.**
> `30_마케팅_AGENT_설계.md:191` 이 *"O29 결함(HH:MM:SS 무성 소실 + 단위 오류)으로 SV 미노출"* 이라 적었으나
> 라이브 실측에서 **두 결함이 모두 해소**돼 있었다(`DEC-30` 파싱 배선·적재 완료 · 단위 정상).
> 🔴 그런데 **미노출 자체는 사실**이다(현행 `05_7_SV_DDL_AD.sql` 에 `DURATION_SEC` 0건 · archive 백업에만 잔존)
> ⇒ 🔴 **잔여 사유가 「결함」에서 「커버리지」로 바뀌었다**(숫자표기분 µs 해석 미확정 · `P18` DoD ③ 경계 아래).
> ⇒ 판정식 = **「막은 이유가 사라졌는가」와 「지금 열려 있는가」는 다른 질문**이다. 둘을 함께 적었다.
> 🟢 부수 확정 = 요건 `#22` 는 **드랍되지 않았다** — 정본 소재지 `FACT_AD_BROADCAST.DURATION_SEC`(방송 grain)가
>   요건 문구 *"영상광고 매체에 한하며"* 와 일치한다. 드랍된 것은 소재차원 중복분뿐이다.
>
> **⑤ 🟢 B군(원천 전건 공백) 확정 + `WIDE_AD_COMBINED` 문안 2건 보강.**
> `CONV_CALL_CNT`·`MEDIA_POTENTIAL_CUST_CNT` 는 BRONZE 에 **컬럼은 실재하고 값이 전건 공백**이다(대행사 미보고).
> 🔴 `WIDE_AD_BROADCAST`·`WIDE_AD_DIGITAL` 문안에는 그 사실이 있는데 **`WIDE_AD_COMBINED` 문안에는 없어**
> *"디지털행/방송행은 NULL"* 만 적혀 있었다 ⇒ **스코프를 좁히면 값이 나온다는 오독**을 유도한다(도포 누락 방향).
> ⇒ 두 문안에 *"스코프를 좁혀도 값은 나오지 않는다"* 를 병기했다. ⚠️ 인입콜(`REBRDC.INBOUND_CALL_CNT`)은
> 채워져 있어 혼동 금지 문구를 함께 넣었다.
>
> **⑥ 🟠 착수표 등재가 막혔다 — 매달린 참조를 만들지 않고 문안으로 대체했다.**
> `99_NEXT_SESSION-020`(착수 순서 표)이 **상한 근접으로 행 추가 불가**(O127-B 경고의 실물).
> 🔴 초판은 `㊶`·`㊷`·`㊸` 를 「등재했다」고 적었으나 **실제로 등재하지 못했다** ⇒ 자력 적발해 세 곳(문서30 §7-C-1·
> §7-C-2 · Agent 설계 `:191`)과 모델 주석 1곳의 참조를 **자체 완결 문안으로 교체**하고, 정본에 **막힌 사실과
> 해소 조건(용량 회복 승인)** 을 적었다. ⇒ 🔴 **판정식 = 「등재했다」는 그 표를 고친 뒤에만 쓸 수 있다**
> (`R3-9` 계열 · O110 이 신설한 「닫았다고 쓰기 전에 정본 행을 고쳤는지 확인」의 착수표 방향 판본).
>
> **⑦ 🟠 도구 간 판정 불일치 1건 적발(미해소 · 보고만).**
> `99_NEXT_SESSION-020` 여유를 **`doc_type_gate` 는 6,892 B** 로, **`wc -c` 실측은 34 B** 로 낸다
> (`doc_census`·`session_brief` 는 실측과 같은 축). ⇒ `R3-9 ㉡`(같은 것을 다르게 재는 지점)의 실물이며
> **분모·경로 기대값 차이**가 의심된다. 🔴 이 세션은 **실측(34 B)을 믿고 행 추가를 포기**했다.
>
> **범위 밖으로 남긴 것(명시)** = ㉠ `RT_TYPE` DROP 집행(DDL·모델·라이브 · `R4-4-3` 승인 대상)
> ㉡ ❌ 표시 컬럼군의 COMMENT 사유 기재 + 라이브 `ALTER … COMMENT` 전파(사유 미확보분은 `R2-7-1` 창작 금지)
> ㉢ `FACT_BUDGET.PLAN_BUDGET_YEAR`·`FACT_EVENT_PARTICIPATION.INCREASE_FLAG` = §7-B A군 **드랍 미집행**(라이브 실재 확인)
> ㉣ 착수표 3건 등재(용량 회복 후) ㉤ ⑦ 의 도구 불일치 규명.
>
> **검증** = 게이트 6종 전건 **rc=0**(`doc_census`·`doc_type_gate`·`clause_order_gate`·`index_row_gate`·
> `doc_heading_gate`·`doc_coord_gate` · 종료코드는 파이프 없이 리다이렉트로 받았다) ·
> `line_len` 7파일 **초과 0** · `split_doc --republish --expect` 토큰 신선도·영속성 **전건 통과**(원장 1 · 문서30 2회) ·
> YAML 문법 `yaml.safe_load` OK. 🔴 **`dbt parse`/`compile`/`build` 는 실행하지 않았다**(`R4-1` 사용자 소관) —
> 모델 변경은 **주석·산출 순서 불변**이므로 컴파일 영향은 없다고 판단하나 **실증은 사용자 실행이다.**
>
> **독해 기록(`R1-3-7-b`)** = `read` 미반환 **0건** · 재호출 **0건**.
> · `00_guides/00_작업지침_세션운영규칙.md` 전량(316줄) 도착 · 고유 토큰 = `R4-4-4` · `BusyBox awk` · `R2-8-4-d`
> · `20_issue/00_BRIEF.md` 전량 도착 · 고유 토큰 = `doc_coord_gate` 경고 1건 · `31_코드군_매핑등재부` stale
> · `30_설계_의사결정-002.md` offset 40 limit 120 도착 · 고유 토큰 = `O96_NULL_SCAN` · `PRV-2` · `DEC-14-A`
> · `06_DDL.sql` offset 405 limit 50 도착 · 고유 토큰 = `DIM 7: DIM_AD_CREATIVE` · `함수종속 53.7%`
> · `_wide_schema.yml` offset 660·788·1254 도착 · 고유 토큰 = `AD_TARGET_GROUP` · `CRM_DEV_CNT` · `BRDC_CPC_SRC`
>
> **원고** = `_o129_entry.md`(루트 · 이관 후 `_archive/` 정리 대상).

> #### 🟢 [2026-09-01 O129-B] O129 자기검토: 절차 위반 2 · 판정 오류 3 · 도구 결함 1 시정 + 승인 집행 3건
>
> **지시** = *"이슈문서들을 읽고 이번 세션에서 진행한 작업을 비판적으로 검토하고, 승인이 필요한 작업은
> 전부 승인하니, 작업과 문서들을 개선해줘"* ⇒ `R4-4-3` 승인 획득 상태에서 자기검토 + 집행.
>
> ### A. 🔴🔴 절차 위반 2건 (내가 인용한 조문을 내가 어겼다)
>
> **A1 `P52` 미이행 — 「중복축」을 판정하면서 대체축 도달을 재지 않았다.**
> O129 는 `DIM_AD_CREATIVE.RT_TYPE` 을 *"오배치 중복축"* 으로 판정했는데, `P52` 는 **정확히 그 실패에서
> 나온 조문**이다(*"중복축 판정은 「존재」가 아니라 「도달」로 한다 — 도달 불가면 그것은 대체재가 아니라
> 또 하나의 결손이다"*). ⇒ 집행 전에 재측정했다: 대체축 `FACT_AD_BROADCAST.RT_TYPE` 은 **도달 가능**하고
> 값이 실재한다(위성은 `AD_PERF_DK` 로 코어와 1:1 이라 센티넬 문제가 없다).
> 🟢 **판정은 유지됐고 오히려 강해졌다** — `DIM_AD_CREATIVE` 자체가 **도달 불가 차원**이므로 이 차원의
> `RT_TYPE` 은 채워도 조회할 수 없다. 즉 `P52` 가 경고한 상황의 **역방향**이다.
> 🔴 **판정식 = 결론이 맞았다는 것이 절차를 건너뛴 것을 정당화하지 않는다.**
>
> **A2 blast radius 분모 누락 — WIDE 만 보고 「영향 범위 작음」을 발행했다.**
> O129 는 *"WIDE 노출 0건"* 을 근거로 삼았는데 **SV DDL 을 보지 않았다.** 실제로 `05_7_SV_DDL_AD.sql:115`
> 에 `ad.RT_TYPE` 이 있었다. 확인 결과 `ad AS GN_DW.GOLD.WIDE_AD_COMBINED` 이고 그 `RT_TYPE` 은
> **위성(`brc.`) 계열**이라 무관했다 — 🔴 **그러나 그것은 운이고 절차가 아니다.**
> ⇒ 소비처 점검 분모를 명문화했다 = **base 테이블 · WIDE 뷰 · SV DDL · Agent spec · 산출물 생성기**.
>
> ### B. 🔴 판정 오류 3건
>
> **B1 「판정 불가」를 재검증 없이 승계했다**(`O111 ㉠` 의 실물). §7-B 의 E군을 그대로 옮겼는데
> 그중 **2건은 원천 1회 조회로 판정 가능**했다: `SILVER.CRM_MARKETING_CAMPAIGN.RM` ←
> `BRONZE_CRM.TM_CM_MKTNG_CMPGN_MNG.RM` **값 전건 공백** · `SILVER.CRM_SEND_RESULT.TOT_CLICK_CNT` ←
> `TD_MS_MSG_AT_LQY_SNDNG.TOT_CLICK_CNT_CTNT` **값 전건 공백** ⇒ **둘 다 B군(원천 전건 공백)으로 재분류**.
> 🔴 **판정식 = 남이 「판정 불가」로 남긴 것은 「아직 아무도 재지 않았다」는 뜻일 수 있다.**
>
> **B2 분류 체계에 축이 없어 6컬럼을 「판정 불가」로 방치했다 ⇒ G군 신설.**
> `FMM` degen `DEV_TYPE`·`NEW_FLAG`·`INCREASE_FLAG`·`REDONATE_FLAG`·`JOIN_DATE`·`STOP_DATE` 는
> **대체처가 실재하고 도달 가능**하다(`FME.DVLP_DIV_CD`·`DVLP_DIV_NM`·`JOIN_DATE`·`STOP_DATE` 실측 채움).
> 🔴 **그런데 A군으로 판정하면 안 된다** — `FMM`(회원×월 스냅샷) ↔ `FME`(사건) **grain 이 다르다**.
> 같은 실수의 선례가 `FACT_BUDGET.PLAN_BUDGET_YEAR`(*"월 팩트에 채우면 SUM 이 12배 부푼다"*)다.
> ⇒ 🟢 **G군 = 「대체처는 실재·도달 가능하나 grain 상이」** 신설 + **A군 3조건**(정본 소재지 실재 + 채움 +
> **grain 일치**) 명문화. 🔴 **판정식 = 분류에 축이 없으면 항목은 「판정 불가」로 밀려난다** —
> 판정이 어려운 게 아니라 **분류 체계가 그 판정을 표현하지 못한 것이다.**
>
> **B3 `RT_TYPE` 의 배선 지점 2곳을 같은 등급으로 적었다.** ① 같은 차원의 `AD_TYPE` 은 **원천 도달 증거일
> 뿐 대체축이 아니다**(3원천 혼합축이라 RT유형만 뽑으려면 `PLATFORM='REBROADCAST'` 스코프 필요) ·
> ② `FACT_AD_BROADCAST.RT_TYPE` 만이 **대체축(정본 소재지)**이다 ⇒ 역할을 구분해 재기술했다.
>
> ### C. 🔴🔴 도구 결함 1건 규명·시정 (O129 가 「미해소」로 넘기려던 것)
>
> `99_NEXT_SESSION-020` 여유를 `doc_type_gate` 는 **6,503 B**, 실측은 **34 B** 로 냈다(`R3-9 ㉡`).
> **원인 = `doc_type_gate.actual_docs()` 가 `EXTRA_DOCS` 조각을 「형제 방식(`<stem>-001.md`)」으로만 찾고
> 「폴더 방식(`<이름>_조각/`)」은 보지 않았다.** `99_NEXT_SESSION` 은 O107 의 `--to-outdir` 로 폴더 방식으로
> 이전됐으므로 **그 순간 분모가 조용히 깨졌다** — 조각 24개가 분모 밖이었고 게이트는 계속 🟢 였다(`R1-6-25`).
> 🔴🔴 **O107 이 남긴 「이전 후 손으로 고칠 분모」 목록에 이 게이트가 빠져 있었다** — **목록 자체가 분모였고
> 그 분모가 불완전했다.** ⇒ 🟢 **판정식 = 「분모를 고쳐라」는 지시에는 「그 지시의 분모도 검사하라」가 포함된다.**
> ✅ 시정 = 폴더 방식 편입 추가 · 음성 테스트 **`scripts/test_doc_type_gate_denominator.py` 신설**
> (오염 축 = 폴더 제거 시 허브 1개로 떨어지는지 · `_조각` 접미 없는 폴더 미편입 · 실물 회귀 축) ·
> 수정 후 `doc_type_gate` ↔ `doc_census` **판정 일치**.
>
> ### D. ✅ 승인 집행 3건
>
> **D1 `DIM_AD_CREATIVE.RT_TYPE` DROP 집행 완료.** `P52`·SV 축 확인 후 **소비처 0** 을 확정하고
> `ALTER TABLE … DROP COLUMN` 으로 집행했다(`CREATE OR REPLACE TABLE` 금지 규약 준수 · 뷰 재생성 불요).
> 🟢 **`P57` 3축 동시 정합** = 정본 DDL · 라이브 · 모델 산출 제거를 같은 세션에 했고, 모델 센티넬 분기의
> **컬럼 수 주석도 함께 고쳤다**(그 줄이 어긋나면 UNION 이 컴파일 실패한다). 검증 = 라이브 컬럼 집합
> 재조회로 `RT_TYPE` **부재 확인** · 잔여 컬럼 수가 모델 산출 수와 정합.
> **D2 `DIM_AD_CREATIVE` 3컬럼 COMMENT 라이브 전파 완료**(`PLATFORM_TYPE`·`AD_TYPE`·`TARGET_GROUP`) —
> `R2-7-2` 이행분. 🔴 나머지 ❌ 행은 **사유 미확보라 창작하지 않았다**(`R2-7-1`).
> **D3 착수표 용량 회복 + 등재 완료.** 닫힌 행 `~~㉜~~` 의 장문 셀을 포인터화(행 키·열 수 보존 ·
> `R2-8-1` 토큰 대조 선행 = 부재 0) 후 **㊶㊷㊸㊹** 등재. 🔴 **긴 표 행이라 `edit` 앵커 대신 줄 인덱스
> 치환 스크립트**를 썼다(`R1-7-8` · O127-B 가 접두 앵커로 행을 쪼갠 선례).
>
> ### E. 검증 · 잔여
>
> 게이트 6종 전건 **rc=0**(종료코드는 파이프 없이 리다이렉트) · `line_len` 초과 **0** ·
> `--expect` 신선도·영속성 전건 통과 · 음성 테스트 신설분 **rc=0**.
> 🔴 **골든 2종 재발행 = 전량 규명 후**: `test_generators` **13건 차이 전량 대응 확인**(desc_cell +2 =
> COMMENT 사유 병기 · entry_key −2 = 취소선 2건 · index_key 소멸 4/신설 2 = 취소선 + 설명 셀 병기) ·
> `doc_heading_gate` **유실 3 ↔ 신설 3 이 1:1**. ⚠️ `test_generators --update-golden` 전에
> `dump_schema.py` 를 돌려 **스키마 지문을 보전**했다(O111-B 가 한 번 지운 사고).
> 🟠 **잔여(내 소관 아님)** = `gate_census` 미분류 **3건**(`_o128_*`)은 **O128 세션 산출물**이라 손대지 않았고,
> 그 때문에 `gate_census`·`test_gate_census` 가 rc=1 이다. 🔴 **이 FAIL 을 「O129-B 가 깼다」로 읽지 마라.**
> 🟢 내 일회성 산출물(`_o129_entry.md`·`_o129b_backlog_edit.py`)은 `_archive/` 로 옮겨 분모를 오염시키지 않았다.
> ⚠️ **`dbt parse`/`build` 실증은 사용자 소관**(`R4-1`) — 모델 변경은 컬럼 1개 산출 제거 + 센티넬 개수 정합이다.
>
> **독해 기록(`R1-3-7-b`)** = `read` 미반환 **0건** · 재호출 **0건**.
> · `30_설계_의사결정-002.md` 반복 독해(등재표·§7-C-1 구간) 도착 · 고유 토큰 = `G군 신설` · `PLAN_BUDGET_YEAR`
> · `05_7_SV_DDL_AD.sql` grep 도착 · 고유 토큰 = `ad AS GN_DW.GOLD.WIDE_AD_COMBINED` · `재방유형`
> · `99_NEXT_SESSION-020.md` offset 124·125·129 도착 · 고유 토큰 = `~~㉜~~` · `㊳` · `_o125e_entry.md`
> · `scripts/doc_type_gate.py` offset 130 limit 32 도착 · 고유 토큰 = `actual_docs` · `EXTRA_DOCS`
>
> **원고** = `_archive/_o129_entry.md`(O129) · `_archive/_o129b_backlog_edit.py`(D3 집행 스크립트).
