<!-- SPLIT-CHUNK 01_세션이력.md | 071/071 | 허브 = 01_세션이력.md | 원문 1~131행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-17 O171] 원천 정의 문서 11/12/16 갱신분 → 인수인계 DDL(문서04) 반영 + 형제 문서 7종 수치 갱신 + 게이트 분모에 GA4·GSC 편입
>
> **지시** = *"'/99_provided_definition/' 폴더 아래의 '/11_bronze_crm_ddl.sql',
> '/12_bronze_agency_ddl.sql', '/16_bronze_gsc_ddl.sql' 파일이 업데이트 되었어.
> 테이블이나 컬럼이 추가되거나 삭제되었을 수 있다. 업데이트된 DDL을 기존의
> '/50_handoff/04_데이터마이그 GN_DW_BRONZE_DDL.sql' 문서에 반영해줘."*
> ⇒ `R4-4-2 ㉡`(지시 동봉) 적용 · 브리핑 선출력 후 즉시 집행 · **확정위반 2건**(아래 ⑨).
>
> ##### 1. 대조 방식 — 손 대조를 하지 않았다
>
> 원천 3종(11번 1,165줄 · 12번 1,041줄 · 16번 197줄)과 문서04(1,734줄)를 손으로 대조하지 않고,
> 괄호 균형 기반 파서로 `{FQN: [(컬럼, 타입)]}` 을 뽑아 **테이블 집합 · 컬럼 순서 · 타입**을 기계 대조했다
> (임시 도구 · 종료 시 삭제). 그 결과를 `scripts/handoff_ddl_gate.py` 가 **독립적으로 재현**했다
> ⇒ 🟢 **두 축이 같은 수를 냈다**(축2 51 · 축3 1).
>
> 실측 = 원천 CRM **50** · AGENCY **4** · GSC **3** ↔ 문서04 CRM 50 · AGENCY 4 · GSC **2**.
> **삭제된 테이블은 0건**이다(사용자 지시의 「삭제되었을 수 있다」는 이번엔 해당 없음).
>
> ##### 2. 반영한 변경 3건
>
> | # | 변경 | 범위 | CSV 위치 적재 영향 |
> |---|---|---|---|
> | ① | `_STDR_YM VARCHAR(6)` **추가**(마지막 컬럼 · `_BATCH_ID` 뒤) | **CRM 50 테이블 전건** | 🔴 **전건 재언로드**(열 +1) |
> | ② | `UPPER_CMPGN_NM` → `CMPGN_UTM_NM`('utm_campaign') **개명** | `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS` **28번째** | 🟢 없다(수·위치 불변) |
> | ③ | `SEARCH_CONSOLE_DATA2`(10컬럼) **신규** | `BRONZE_GSC` | 🟢 없다(GSC 는 API 적재) |
>
> · ① `SND_MEMBER_LIST` = O166-D 가 확인한 **76컬럼** → **77컬럼**. 🔴 이 테이블은 2026-09-15 에
>   `OPEN_DT` 삭제로 77→76 이 되었고 이번에 77 이 되었다 — **열 수가 같아도 구성이 다르다**
>   ⇒ 「열 수가 같으니 그 CSV 를 쓸 수 있다」는 오판이 가능한 자리라 문서04 「적재 시 주의」에 명시했다.
> · ② 🟢 `VIDEO_AD_CMPGN_DTLS.UPPER_CMPGN_NM` 은 **개명 대상이 아니다**(원천에 그대로 있다)
>   ⇒ 전역 치환을 하지 않고 **테이블을 앵커로 스코프**해 고쳤다.
>   🔴 **컬럼명을 참조하는 SILVER/GOLD dbt 모델은 이번 세션이 손대지 않았다**(다음 세션 소관).
> · ③ 컬럼 구성·타입·코멘트가 `SEARCH_CONSOLE_DATA` 와 **동일**하다.
>   🟠 **원천 16번에 이 테이블을 채우는 적재 경로가 없다**(`SP_LOAD_GOOGLE_SEARCH` 는 원본만 적재)
>   ⇒ 용도는 **현업 확인 대상**이다.
>
> ##### 3. 🔴🔴 게이트 사각을 적발했다 — 판정식 J2 5차 재발
>
> `handoff_ddl_gate.TARGETS` 에 **`BRONZE_GA4`·`BRONZE_GSC` 항목이 없었다.**
> ⇒ 축1(테이블 집합)이 **그 두 스키마를 아예 보지 않았고**, 원천 16번의 신규 테이블을
> **「축1 0건」으로 침묵**했다. 착수 시 게이트는 **축1 0건**을 보고했는데 실제로는 신규 1건이 있었다.
> 🟢 **판정식 = 「내가 본 그릇이 그것을 담는 그릇인가」**(`O165 D5` 계열 · O167 이 4차, 이번이 5차).
> 🟢 처방 = 두 스키마를 `TARGETS` 에 편입(`soft=True` — 원천 15/16번은 `SYNC_ERR_INFO`
> 컬럼 COMMENT 가 비어 있고 문서04 가 관례로 보강했으므로 누락을 blocking 으로 걸지 않는다).
> 🟢 **GA4 는 편입 직후 차이 0** 이었다 ⇒ 편입이 오탐을 만들지 않는다는 것을 같은 실행이 실증했다.
>
> ##### 4. 축6(테이블 COMMENT) — 원천 오기를 원천 무변경으로 처리했다
>
> 원천 16번이 GSC **두 테이블의 테이블 COMMENT 를 `'재송출 광고 성과 내역'`** 으로 적고 있다.
> GSC 내용과 어긋나므로 **원천 오기로 보인다**(재송출 광고는 `BRONZE_AGENCY` 소관이다).
> 종전 문서04 는 `SEARCH_CONSOLE_DATA` 에 `'구글 서치 콘솔 검색 데이터'` 를 쓰고 있었다.
> ⇒ 병합 규칙(**원천 무변경** · 선례 = `BRONZE_ERP` 스키마 COMMENT 가 'SMS/알림톡'으로 어긋나도
> 그대로 두고 객체 인덱스에만 실제 성격을 적은 처리)에 맞춰 **양쪽을 원천 문안으로 통일**하고
> 실제 성격은 **주석 + 객체 인덱스**에 적었다 ⇒ 축6 **1 → 0**.
> 🔴 **이 판정은 현업 확인 대상이다** — 원천이 고쳐지면 문서04 문안도 함께 되돌린다.
> 🟢 판정식 = **「문안이 이상하다」와 「문안을 내가 고쳐도 된다」는 다르다.**
>
> ##### 5. 축7 — 형제 문서 7종 38줄을 한 세대 올렸다
>
> 문서04 만 고치면 축7 이 **50건**을 낸다(01·02·02_1·03·05·06·07번이 옛 수치를 인용).
> 🔴 문서04 자체가 *"절반만 고친 문서군은 고치지 않은 것보다 나쁘다"* 고 적고 있다 ⇒ 전건 갱신했다.
>
> · 현행값 = **이관 총계 77 → 78 · 브론즈 60 → 61 · GSC 2 → 3**(CRM 50 은 불변).
> · 🟢 **과거 전이 기록은 값을 고치지 않았다** — 「52 → 56」·「73 → 77」 같은 줄은 역사이므로
>   숫자를 보존하고 **같은 줄에 현행값을 병기**했다(축7 의 면제 근거 = 「그 줄이 독자에게
>   현행값을 함께 알려주는가」이므로 **문면 조작이 아니라 정보 추가**로 통과시킨 것이다).
> · `STALE_TOKENS` 에 옛 세대(`77 테이블`·`= 77`·`브론즈 60`·`60 테이블` …)를 **추가**했다
>   — 🔴 지우지 않았다(지우면 그 세대 표기가 다시 침묵한다 · O113 축).
> · 🔴 **음성 테스트 픽스처가 같이 깨졌다** — `test_handoff_ddl_gate.py` 의 면제 줄이
>   현행값 `77/60` 을 쓰고 있어 기준값을 올린 순간 **그 줄이 위반으로 잡혔다**(단정 2건 실패).
>   🟢 그 픽스처의 주석이 *"「정정」 줄의 현행값도 함께 올려야 면제가 성립한다"* 고 **스스로 예고**하고
>   있었다 ⇒ 78/61 로 올려 **17/17 통과**. 판정식 = **기준값을 올리면 테스트도 같은 세대로 올려라.**
>
> ##### 6. 최종 게이트 상태
>
> | 축 | 착수 | 종료 |
> |---|---|---|
> | 축1 테이블 집합 | 0 (🔴 **GA4/GSC 미검사 상태의 0**) | **0** (분모 편입 후) |
> | 축2 컬럼 이름·순서 | 51 | **0** |
> | 축3 컬럼 타입 | 1 | **0** (사용자 결정 후 · §7) |
> | 축4 DEFAULT | 0 | 0 |
> | 축5 컬럼 COMMENT | 0 | 0 |
> | 축6 테이블 COMMENT | 0 (🔴 미검사) | **0** |
> | 축7 문서 stale | 0 (🔴 옛 세대 기준) | **0** (새 세대 기준 · 50 → 0) |
>
> 그 밖 = `line_len` 🟢 PASS · `index_row_gate` rc=0 · `doc_heading_gate` rc=0(제목 1,734/1,734)
> · `test_handoff_ddl_gate` **17/17**.
>
> ##### 7. 🟢 축3 종결 — 사용자 결정으로 ERP 를 원천에 맞췄다
>
> **`BRONZE_ERP.EXPENSE_RESOLUTION.WRITE_DATE` `VARCHAR(16777216)` → `DATE`** (원천 13번 `:76`).
> · 판단 근거 = **2026-09-17 사용자 결정** = *"'/99_provided_definition/' 폴더 내 문서들이 정본이야.
>   sql 문서는 최신 정본이고 나머지 문서는 stale 한 부분도 있긴 하다."*
> · 🔴 착수 시 이 건을 「판정 대기」로 남긴 이유는 **어느 문서에도 편차의 근거가 없었기** 때문이다
>   (문서04 변경 이력·이슈 원장 전건 grep 0건) ⇒ 근거 없는 편차는 **원천이 이긴다**는 규칙이 필요했고
>   사용자가 그 규칙(=원천 sql 이 정본)을 명시했다.
> · 🟢 **컬럼 수·순서·COMMENT 는 불변**(16컬럼 · 2번째) ⇒ 위치 기반 CSV 적재의 **열 대조는 통과**한다.
> · 🔴 다만 **문자열 → DATE 형변환이 새로 생긴다** ⇒ 문서04 「적재 시 주의」와 07번 갱신 이력에
>   *"GN_CSV_FORMAT_EUCKR2 로 소량 시적재해 거부행 0 을 먼저 확인하고, 날짜 표기가 'YYYY-MM-DD'
>   가 아니면 DATE_FORMAT 을 지정하라"* 를 적었다. 🟢 판정식 = **열 대조 통과가 적재 성공을 뜻하지 않는다**
>   (열 수는 같고 파싱이 깨지는 경로가 있다).
> ⇒ 🟢 **`handoff_ddl_gate` 판정 축 7종 전건 0 · rc=0** (잔여 = 🟠 SILVER COMMENT 양쪽 부재 9건 경고).
>
> ##### 8. 🟠 환경 사고 — 마운트가 세션 중 분리됐다
>
> 작업 후반에 `/workspace` 가 **다른(빈) 워크스페이스로 보였다**(`scripts/`·`50_handoff/` 부재 ·
> `os.listdir('/workspace')` = `['.snowflake','Untitled.sql']` · `/snowflake/stages/…` 자체가 부재).
> 세션 초반에도 같은 계열의 `OSError: Transport endpoint is not connected` 가 1회 있었다.
> 🟢 **파일 소실로 오판하지 않고** `cortex ws ls` 로 스테이지를 조회해 **전건 무손상**을 확인했다
> (문서04 **113,056 B** · 편집 시각 반영 · 형제 7종 전부 반영).
> 🟢 **판정식 = 마운트 부재는 파일 소실이 아니다. 스테이지를 별 축으로 조회해 판정하라.**
> ⚠️ 이 사고로 `rm -f` 1회가 **조용히 실패**했다(에러 없이 파일이 남았다) — 마운트 복구 후
> `os.remove` 로 개별 삭제하고 `os.listdir` 로 잔존 0 을 확인했다(`R1-7-7` 축).
>
> ##### 9. 🔴 자기결함 2건
>
> · 🔴 **라벨 선점 확인 누락**(`R1-4-3`) — `00_BRIEF` §4 에 **`⏳ O170 — 집행중(선점)`** 행이
>   실려 있었는데 그것을 보지 않고 `O170` 을 스냅샷 라벨·코드 주석에 썼다.
>   적발 = `gate_census.py` 가 **`_o170_*` 파일 3건 미분류**를 냈고 그것이 **내 것이 아니었다**.
>   ⇒ `id_collision_gate --next O` = **O171** 확인 후 스냅샷 8건 개명 · 코드 주석 3곳 정정.
>   🟢 판정식 = **브리핑을 읽는 것과 브리핑으로 판정하는 것은 다르다**(§4 를 출력만 하고 쓰지 않았다).
> · 🔴 **원장 행 상한 초과** — 처음 쓴 O171 행이 **2,112자**로 `R1-5-1` 상한(2,000)을 넘겼다.
>   🟢 `line_len.py` 가 즉시 잡아 축약(상세는 이 이력 항목으로 이관)했다 ⇒ 재검 🟢 PASS.
>   판정식 = **원장 행은 색인이고 이력이 정본이다**(길어지면 원장이 아니라 이력에 쓴다).
>
> ##### 11. 🔴🔴 [O171-C 사용자 적발] 08 SILVER DDL 의 COMMENT 규약을 내가 깼다
>
> 사용자 지적 = *"컬럼 코멘트가 방금 작업으로 우리가 이전에 세웠던 규약을 벗어난 것 같다."*
> 🔴 **규약 정본 = 그 파일 `:33`** — *"본 파일의 범위 = **구조 계약(타입·PK·COMMENT)만**.
> 설계근거·실측이력·리뷰기록은 이관됨"*(AGENCY → 06번 §6) + `:39` 이슈·결정 ID 는 원장 소관.
> ⇒ 🟢 **판정식 = 이력은 `COMMENT '…'` 문자열이 아니라 `--` 블록(`[컬럼별 설계 및 실측 이력]`)에 쓴다.**
>
> **내 위반 2건**(선례 0 · 확정) = ㉠ `AGENCY_AD_PERFORMANCE.UPPER_CAMPAIGN_NM` COMMENT 에
> 「DIGITAL=NULL(원천 개념 소멸 · 2026-09-17 O171) · REBROADCAST=NULL(원천 부재)」 ㉡
> `AGENCY_AD_ROW_DGT.CMPGN_UTM_NM` COMMENT 에 「2026-09-17 원천 12번 개명 = 구 UPPER_CMPGN_NM」.
> ⇒ 이웃 형식(`'디바이스 (DGT만)'`·`'CM위치 (VIDEO)'`)에 맞춰 `'상위 캠페인명 (VIDEO 전용)'` ·
> `'[원천보존] utm_campaign'` 으로 되돌리고 **이력·판단근거는 블록 주석으로 이관**(삭제 0).
> 🔴 **내가 제시한 `ALTER … MODIFY COLUMN COMMENT` 문도 같이 고쳤다** — 그 문안을 그대로 실행하면
> **비규약 COMMENT 가 라이브에 심긴다**(라이브는 AI 컨텍스트 표면 · O167 「라이브 COMMENT 거짓 문안」 축).
>
> **전 문서 감사**(COMMENT 문자열 **940개** · `--` 줄 제외) 후 사용자 지시로 **축 A·C 만** 정리(축 B 보류):
>
> | 축 | 착수 | 종료 | 처리 |
> |---|---|---|---|
> | A 날짜 | 3 | **0** | 내 2건 + `:484`(O162 `2026-09-16 신규`) ⇒ 날짜를 블록 주석으로 이관 |
> | B 세션·결정ID | 9 | **8** | 🟠 **보류**(사용자 지시) — `Q15`·`Q16`·`Q10`·`O145-8`·`O28`·`O14`·`DEC-44`·`E-6` |
> | C 실측수치 | 7 | **0** | 「8종」·「6종」은 블록에 신규 등재 · 나머지 5건은 **블록에 이미 중복 보유**(`:1133`·`:1488`·`:1489`·`:1517`·`:1519`)라 COMMENT 만 정리 |
> | D 이력서술 | 2 | **0** | 둘 다 내 것 |
>
> 🟢 **실측은 한 건도 잃지 않았다** — COMMENT 에서 뺀 수치는 전부 블록 주석에 실재한다(`grep` 8건 확인).
> 🟢 **코드값은 남겼다**(`값 = PC / M`) — `R2-6` 은 *"코드 COMMENT 에는 **코드값만**"* 이므로 열거는 규약 안이다.
> 🔴 **내 초판 판정식이 축 C 를 49건으로 냈다** — 대부분 Grain 표기 `(1행=1회원)` 오탐이었다.
> ⇒ 🟢 **`1행=` 은 실측이 아니라 Grain 계약 문법**이므로 분모에서 빼고 **7건**으로 정정했다
> (판정식 J2 「내가 본 그릇」의 **오탐 방향** 판본 — 분모가 넓으면 규약 위반이 아닌 것까지 위반이 된다).
> 📏 검증 = 스냅샷 대비 변경 **20줄**(의도분 전부) · 축A·C **0건** 재측정 · 게이트 전종 rc=0.
>
> ##### 12. 🔴 다음 세션이 할 일 (11 이후 추가분)
>
> · 🟠 **축 B 8건은 열려 있다** — 짧은 ID 포인터라 `:39`(원장이 정본)와 양립 가능하다고 보아 보류했다.
>   정리하려면 **사용자 지시가 선행**한다(남의 세션 결정이 걸려 있다).
> · 🔴 **`ALTER` + `dbt build` 는 사용자 소관**이다 — 문안은 `99_NEXT` §0-WWWW/O171 ▣ O171-3 에 있고
>   **08 DDL 의 COMMENT 와 바이트 동일**해야 한다.
>
> ① 🟢 ~~**`CMPGN_UTM_NM` 개명의 SILVER/GOLD 영향 분석**~~ ➔ **[O171-B 종결]** — dbt build 가 대신
>    적발했다(`AGENCY_AD_ROW_DGT` · `invalid identifier`). 실측 = `UPPER_CAMPAIGN_NM` 은 **SILVER 종단**
>    (조인 0 · GOLD 승격 0 · SV/Agent 0) ⇒ 모델 4곳 시정. 🔴 **`VIDEO_AD_CMPGN_DTLS` 의 동명 컬럼은
>    살아 있으므로 전역 치환은 오류다** — 이 금지는 유효하다.
> ② 🔴 **`_STDR_YM` 의 SILVER 취급 결정** — CRM 50 테이블 전건에 생긴 컬럼이다(마감 기준년월).
>    증분 적재 키로 쓸 수 있는지, `_LOAD_DT` 와 역할이 겹치는지 설계 판정이 필요하다.
>    🔴 **①이 build 로 적발된 선례를 보라** — 미루면 CRM 계열 build 가 대신 적발한다.
> ③ 🟠 **현업 확인 3건** = GSC 테이블 COMMENT 오기 · `SEARCH_CONSOLE_DATA2` 용도 ·
>    `_STDR_YM` 한글 코멘트 문안(명명 규칙으로 부여했다).
> ④ 🔴 **CRM CSV 전건 재언로드**(열 +1) — 07번 A.1 (4) 대조를 다시 통과시킨 뒤 적재한다.
> ⑤ 🟢 ~~**축3 ERP `WRITE_DATE` 판정**~~ ➔ **[종결]** 사용자 결정으로 원천(`DATE`)에 맞췄다 · 위 §7.
> ⑥ dbt 는 **에이전트가 실행하지 않는다**(`R4-1`) — 모델 4곳을 고쳤고 **build 는 사용자 소관**이다.
>
> ##### 13. 🔴🔴 [O171-D] dbt build ERROR 1건 — AD_DATE 불변식 붕괴(원천 회귀) · 폴백으로 해소
>
> 사용자 build 결과 = `PASS=499 WARN=37 **ERROR=1** SKIP=24 TOTAL=561`.
> 에러 = `not_null_AGENCY_AD_PERFORMANCE_AD_DATE` **Got 194,058 results**.
> 🔴 **내 개명 작업이 원인이 아니다** — NULL 은 전량 DIGITAL 이고 뿌리는 BRONZE 원천이다.
>
> | 축 | 승격 당시(순서9-D) | 2026-09-17 실측 |
> |---|---|---|
> | `DGT_AD_CMPGN_DTLS` 행 | 235,572 | **203,138**(−32,434) |
> | `DATE` NULL | **0** | **194,058 (95.53%)** |
> | `DATE` 실값 범위 | 전 기간 | **2026-06-01 ~ 2026-08-20**(9,080건) |
>
> 🔴 이 테스트는 문서50 `-015:6` 이 *"실측 널 **0/235,572** → severity 제거·error 승격(구조 불변식)"*
> 으로 기록한 것이다 ⇒ **테스트 오작동이 아니라 불변식이 실제로 깨진 것**이고 정확히 잡았다.
> 🟢 **판정식 = 「테스트가 빨간불이면 테스트를 의심하기 전에 그 테스트가 언제 왜 승격됐는지 읽어라.」**
> 원인 = 개명을 가져온 **그 원천 재적재**(현업 GSheet)가 시간축 방식을 함께 바꿨다.
>
> **사용자 결정** = *"2026년 6월 이전은 픽스, 이후는 변경식으로 간다고 고려하면서 2번(폴백)으로 가자."*
> ⇒ 코어 `AGENCY_AD_PERFORMANCE` DIGITAL 분기를
> `COALESCE("DATE", TRY_TO_DATE("YEAR"||LPAD("MONTH",2,'0')||LPAD("DAY",2,'0'),'YYYYMMDD'))` 로 폴백.
> 🔴 `AD_YEAR`·`AD_MONTH` 도 **같은 파생값에서** 뽑도록 함께 고쳤다 — 안 고치면 그 둘만 NULL 로 남는다
> (🟢 판정식 = **폴백은 그 컬럼 하나로 끝나지 않는다. 그 컬럼에서 파생된 것을 전수 따라가라.**)
>
> 🟢 **폴백 신뢰 근거(라이브 실측)** = ㉠ `DATE` 가 있는 9,080행에서 파생값 **9,080/9,080 일치 · 불일치 0**
> ㉡ 파싱 실패 **0/203,138** ㉢ 경계가 깨끗하다 — NULL 행 파생 최대값 **2026-05-31** · 6월 이후 **0건**
> ⇒ 사용자의 「6월 전후」 가설이 **실측으로 확증**됐다 ㉣ 폴백 적용 시 NULL **0/203,138**(빌드 전 dry-run).
> 🔴 `DATE_FROM_PARTS` 를 쓰지 않았다 — 불량 월/일을 **조용히 롤오버**한다. `TRY_TO_DATE` 는 NULL 을 낸다.
> 🔴 **staging 은 손대지 않았다**(원천 무손실 규약) — 파싱은 코어에서만 한다.
> 🔴 **severity 강등은 하지 않았다** — 관례가 *"위반 0 을 먼저 실측한 뒤 올린다"* 이고 게이트를 FAIL 로
> 두고 닫는 것은 O164 `D2` 로 이미 결함 등재돼 있다.
>
> 🟢 **08 DDL 금지 규약을 개정했다**(`:965~974`) — 종전 *"텍스트 시간축 숫자 파싱 금지 · DATE 에서 파생"* 은
> 값이 `'2025년'`·`'03월'` 형태일 때의 처방인데 **DGT 실측 값은 `'2025'`·`'3'`·`'6'`** 이고
> 그 규약이 신뢰하라고 한 `DATE` 쪽이 비었다 ⇒ **DGT 만 예외**로 명시(REBRDC·VIDEO 는 널 0 이라 금지 유지).
> 🟢 판정식 = **금지 규약도 전제가 깨지면 개정 대상이다. 단 개정 근거를 같은 자리에 병기한다.**
>
> ⚠️ **WARN 37건은 이번 변경과 무관**하다(`FACT_MESSAGE_DISPATCH.MEMBER_DK` 고아 34,246 ·
> `FACT_MEMBER_MONTHLY` 199 · `FACT_MEMBER_COHORT` 16 등 = 문서50 `BLOCKING-2`·`BLOCKING-5` 계열 기존 항목).
> 🟢 **내 개명 변경 검증 통과** = DIGITAL `UPPER_CAMPAIGN_NM` NONNULL **0** · VIDEO **37,674**.
> 🟢 라이브 구조 대조 = `CMPGN_UTM_NM` **32번째** · `READ_CNT` 33번째 · `AGENCY_AD_ROW_VIDEO.UPPER_CMPGN_NM`
> **23번째 존치** · `BIGQUERY_REFINED_DATA` **10,770,355행 보존**(08 에서 `CREATE` 주석 처리).
> 🟢 **내가 제시한 `ALTER` 3문은 무효화됐다** — 사용자가 08 DDL 을 통째로 실행해 라이브가 이미 목표 구조다.
>
> ##### 14. 🔴 재빌드 후 확인할 것
>
> · `SELECT SOURCE_SYSTEM, COUNT(*), COUNT(AD_DATE), COUNT(UPPER_CAMPAIGN_NM)` on `SILVER.AGENCY_AD_PERFORMANCE`
>   ⇒ 기대 = **AD_DATE NULL 0 전건** · DIGITAL `UPPER_CAMPAIGN_NM` **0** · VIDEO **37,674**.
> · 🟠 **원천 회귀 자체는 열려 있다** — 행 수가 235,572 → 203,138 로 **32,434 줄었다**. 폴백은 날짜만
>   메웠고 **행 감소는 설명되지 않았다** ⇒ 🔴 **현업 확인 대상**(GSheet 재적재 범위·삭제 여부).
>   🟢 판정식 = **한 증상을 고쳤다고 그 원천 회귀가 규명된 것은 아니다.**
