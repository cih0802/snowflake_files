<!-- SPLIT-CHUNK 01_세션이력.md | 023/025 | 허브 = 01_세션이력.md | 원문 5942~6189행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> ✅ **O33 — SV 「비활성」 선언 전수조사 + 확장**
> 소비 질문("BRONZE 코드로 GOLD 를 끌어낼 수 있나")을 검증하다 **SV 가 GOLD 보다 좁다**는 격차를 찾았다.
> **비활성 선언 5건이 stale**(배선은 끝났는데 "적재 대기"로 남아 Agent 가 "불가"로 답하던 상태):
> ① FMM 사유별(센티넬 92.1%) · ② FMM 지역/연령대(**3.7%**) · ③ FME 캠페인별(22.4%·고아 0%) ·
> ④ FME 사유별(77.6%) · ⑤ FEP 모집인원(**고아 86.6%**).
> 나머지 비활성 선언(캠페인/후원사업/조직별·수신·성공·실패·오픈·+5일 코호트·연 편성예산·신규기존·
> 활동/누계 카운트)은 **전부 정확**했다(해당 컬럼 distinct=1 또는 fill=0 실측).
> ✅ **확장 배포**: `SV_MEMBER_MONTHLY` +지역·연령대 / `SV_MEMBER_EVENT` +캠페인 5축
> (`DIM_CAMPAIGN` PK 유일 → fan-out 0 확인 후 논리테이블 신설). SV COMMENT 의 거짓 비활성 문구 정정.
> 스모크 6/6 통과 — 지역별·연령대별 납입회비, 캠페인·브랜드별 개발건이 실제로 답을 낸다.
> 🔴 **개발/중단 비대칭을 COMMENT 에 명시**: 캠페인은 **개발(DEV) 전용**이다 — 중단(STOP)행은
> 센티넬 **100%**(개발행은 0.0%)라 "캠페인별 중단건"은 `(미매핑)` 한 덩어리로 나온다(스모크로 실증).
> ⬜ **잔여 결정 2건**: ①④ 사유별 노출 여부(센티넬 92.1%/77.6% — 미납/중단 행 한정 노출이 맞는가) ·
> ⑤ 모집인원(고아 86.6% → 보류 권고).
>
> 🔴 **O34 신규 — `DIM_MEMBER.AGE`(연령대) 값 신뢰 보류 (현업 확인 필요)**
> SV 에 연령대를 노출하고 스모크를 돌리다 발견했다. 라벨 매핑은 **코드사전 CM014 와 정확히 일치**
> (우리 쪽 버그 아님)하나, **최다 구간이 ''10대 미만''**이고 ''10대''까지 합치면 회원의 상당 비중이다.
> 회비 납입 주체의 연령분포로는 비정상이며 **원천 기본값이 첫 코드(1)로 유입된 패턴**이 의심된다.
> ⚠️ `AGE=10`→단체 · `AGE=11`→기업 이 성별(`SEX`)과 일치하므로 **회원 속성인 것은 맞다**(결연아동 연령 가설은 기각).
> ✅ 조치: SV dimension COMMENT 에 *"연령대별 순위·최대 연령층 결론 금지 — 확인 전까지 분포 관찰 전용"*
> 가드 배포. 🔴 이 가드 없이는 Agent 가 *"10대 미만이 최대 후원 연령층"* 이라고 확신하며 답한다.
>
> ✅ **소비 가능성 검증 결론** — `DIM_CODE` 신설 **불요**(권고 철회).
> GOLD 코드축 41곳을 전수 대조하니 **라벨 페어링 결함 0**(`JOIN_PATH_CD`→`MEMBER_ENROLL_PATH_NAME` 포함).
> 코드사전이 없어도 라벨로 조회된다. SILVER 에만 있는 코드축 56종은 GOLD 에 컬럼 자체가 없어 사전이 있어도 못 뽑는다.
> ⚠️ 단 역할 차이는 남는다: `GN_DW_ANALYST`=GOLD·SERVING·**SILVER** / `GN_DW_VIEWER`=GOLD·SERVING 만.
>
> **▶ 🔴🔴 최신 상태 [2026-08-04 재구축 준비] — 정본 = `02_GN_DW_building/06_RUNBOOK.md` §11**
> 사용자 결정: **전수 재구축(①→④) 전제** → 물리 ALTER 는 생략하고 **정본 문서만** 정비한다.
> ✅ **완료** ⓵ `O28_O29_COMMENT_GUARD.sql` **사문화 2건 분리** → `03_top-down_gold/_archive/O28_O29_사문화_COMMENT_2건_20260804.sql`
> (`FEP.RECRUIT_CNT`·`DIM_AD_CREATIVE.DURATION_SEC` = DEC-30 이 DROP 한 컬럼. 남겨두면 `COMMENT ON COLUMN`
> 실패로 스크립트 전체가 재실행 불가였다. 뷰측 파생 1건도 절 단위 제거 — `ALTER VIEW` 16→15절, 구문 검증 완료)
> ⓶ **런북 §11 「전체 재구축 순서」 신설** + **§3.3 위험 지시 교정**.
> 🔴 §3.3 은 종전 *"06_DDL.sql 재실행 후 dbt build"* 라고만 적혀 **파괴 경고가 없었다** —
> **런북이 O30 사고를 지시한 문서**였다. 평상시 절차(ALTER + 정본 동기화 + 모델 정합 + build)로 교체했다.
>
> ✅ **O31 해소 + 🔴 내 오판정 정정 (런북 §11.3-B)**
> 나는 O31 을 *"SERVING helper 뷰 2종의 실행 정본 유실(BLOCKER)"* 로 등재했으나 **틀렸다.**
> 정본은 **`02_GN_DW_building/08_After_Deploy_DBT.sql` §G** 에 있었다. 오판 경로 =
> `02_SERVING_setup.sql` 스텁이 *"07_ENVIRONMENT_RBAC_setup.sql 로 이관"* 이라고 적어서
> **07 과 `_archive` 만 확인하고 단정**했다 — 스텁의 이관 안내가 **틀린 파일을 가리키고 있었고
> 나는 그것을 검증 없이 따라갔다**(사용자가 08 을 지목해 바로잡혔다).
> 🔴 **실제 결함은 다른 것이었다**: `08` §G.2 의 `SERVING.DIM_MEMBER_CURRENT` 생성문이
> O27 이 DROP 한 `NEW_EXISTING_FLAG`·`LAST_CAMPAIGN`·`CURRENT_SPONSORSHIP` 을 SELECT 해
> **`invalid identifier` 로 실패**한다(동일 SELECT 컴파일로 실증). → ✅ 3컬럼 제거·컴파일 검증 통과.
> SV 는 이 3컬럼을 참조하지 않아(`05_SV_DDL.sql` 실측 0건) 소비 영향 0.
> 📌 **O27 이 번진 산출물은 4개였다** — `06_DDL.sql`·물리 `GOLD.DIM_MEMBER`·dbt 모델 ·
> **`08_After_Deploy_DBT.sql`(SERVING 소비 뷰)**. 구조 변경은 **소비 뷰까지 역추적**해야 한다 → **P59**.
> ⬜ 잔여(BLOCKER 아님): `DIM_MEMBER_CURRENT` 가 **SERVING(SV 전용 19컬럼) / GOLD(분석가용 20컬럼)**
> 2판 공존 — 소비자가 달라 의도적 분리로 설명되나 미문서였다(양쪽에 역할 명기 완료).
> `DIM_MEMBER_CURRENT.sql` 헤더의 *"전건 NULL 7컬럼 미노출"* 은 **이제 없는 3컬럼을 열거**한다(P33 ③ 회수 대상).
>
> ✅ **재구축 순서 확정 (런북 §11.2·§11.2-B)** — 🔴 **①②(`06_DDL`·`08_SILVER`) 재실행 불요**:
> **정본 DDL ↔ 물리 66테이블 전수 대조 불일치 0**(2026-08-04). 재실행하면 데이터만 날아간다.
> 최소 경로 = **1) `deploy_dbt_project.sql`**(🔴 `SHOW DBT PROJECTS` = **0행** — 재구축이 DBT PROJECT 를
> 삭제했다. `08` 5~7행 GRANT 의 전제조건) → **2) dbt build** → **3) `08_After_Deploy_DBT.sql`** →
> **4) `05_SV_DDL.sql`** → 5) `13_SV_AD` → 6) `09_1_AGENT`.
>
> 🆕 **P59 (구조 변경은 소비 계층까지 역추적해야 완결된다)**: O27 은 정본 DDL·물리·dbt 모델 3곳을
> 맞추고 "완료"로 봤지만, **소비측 뷰(`08_After_Deploy_DBT.sql` §G.2)** 가 DROP 된 컬럼을 계속
> SELECT 하고 있었다. 이 결함은 **dbt build 로는 절대 드러나지 않는다**(그 파일은 dbt 밖이다) —
> SV 배포 단계에서야 터진다. → 컬럼 DROP·개명 시 **`grep -r <컬럼명>` 을 워크스페이스 전역**에
> 돌려 dbt 밖 산출물(setup SQL·SV DDL·Agent spec)까지 회수 목록에 넣는다.
>
> ⬜ **미결 O32 — COMMENT 실측수치 제거 96건 수동 대기**
> 사용자 규칙: *"COMMENT 에서 실측 수치는 코드값 외엔 삭제"*(데이터 계속 입고 → 수치는 반드시 낡는다).
> 실측 대상 = COMMENT **1,686개 중 132개**가 수치 보유(06_DDL 67 · 08_SILVER 30 · dbt models 35).
> 그중 **자동 처리 가능 5건 · 수동 재작성 96건**.
> 🔴 **자동 정규식 방식은 2회 시도 후 폐기했다** — 절 내부 수치만 빼면 한국어 문장과 **코드값이 깨진다**
> (실증: `MM018 1개인·2기업·3단체` → `인·2기업·3단체` · `정본 5종을 3종으로 축약` → `정본 을 으로 축약`).
> 좁은 토큰 삭제도 잔해가 남았다(`(69부서/·131부서/=)` · `값..`). → **96건은 손으로 써야 한다**(P58).
> ⚠️ 그중 **10건은 내가 만든 것**이다 — O27 §4 를 verbatim 적용하면서 `DIM_MEMBER` 에 `7,925,716`·
> `96.91%` 등을 주입했다(§19-E ⑤).
>
> 🆕 **P58 (COMMENT 의 실측 수치는 자동으로 지울 수 없다 — 애초에 넣지 마라)**: 한국어 COMMENT 에서
> 수치만 정규식으로 제거하면 **코드값(`1개인`)과 문장 구조가 함께 깨진다**. 수치는 절(clause)과 엉켜
> 있어 절 단위 삭제도 가드를 함께 날린다. → ① COMMENT 에는 **코드값·가드·정소재지만** 쓰고
> 실측치는 **문서(문서10)에만** 둔다 ② 이미 들어간 것은 **수동 재작성만이 안전**하다
> ③ `ALTER … COMMENT` 를 verbatim 재사용할 때 **수치 포함 여부를 먼저 확인**한다.
>
> **▶ 🔴🔴 최신 상태 [2026-08-04 재구축 드리프트] — 정본 = 문서10 §19 · 복구 스크립트 = `03_top-down_gold/O30_REBUILD_DRIFT_REPAIR.sql`**
> **O30 = 전체 환경 재구축이 「정본 DDL 에 접히지 않은 구조 변경」을 되돌렸다.** 실측 타임라인: 2026-08-03 **20:55** DB·스키마 재생성 → **20:59~21:00** BRONZE 51테이블 `CREATE OR REPLACE`+재적재 (112,512,201행) → **21:36~21:37** SILVER 38 재생성(`08_SILVER_테이블DDL`) → **21:37~21:38** GOLD 28 재생성(`06_DDL.sql`) → **21:40~21:41** `dbt build` **ERROR 3 · SKIP 68**.
> 🔴 **소실 = 정확히 2개 변경집합**(전수 대조·추론 0): **O26 SILVER 개명 2컬럼**(`PRCS_STAT_CD` 부활) · **O27 `DIM_MEMBER` 4추가/3삭제+COMMENT 8**. 🟢 **무사** = DEC-30 구조변경 · O25 SILVER 38컬럼 · O28/O29 COMMENT 가드 21컬럼 — 전부 정본 DDL 에 접혀 있었다. **COMMENT 손실 0**(SILVER 696/696 · GOLD 670/670).
> 🔴 **`O27_DIM_MEMBER_ALTER.sql` §5 가 "정본 DDL 도 같은 세션에 고쳐라"라고 이미 경고**했고 그 단계가 실행되지 않았다 → **P30 3번째 사례**.
> 🔴 **파급이 ERROR 3 보다 22배 넓다**: SKIP 68(모델 9+테스트 59) · **빈 테이블 9종** · **GOLD 뷰 5종 미존재**(`DIM_MEMBER_CURRENT`+WIDE 4 — 뷰는 dbt 소유라 모델 SKIP 시 생성 안 됨) · 🔴 **SERVING 스키마 객체 0건**(SV 6종·`FACT_AD_COMBINED`·`DIM_MONTH` 전멸) = 소비 계층 전체 중단.
> 🔴 **무증상 폐기 1건 발견** — `DIM_AD_CREATIVE.DURATION_SEC`: DEC-30 이 DDL 에서 제거했는데 모델이 계속 산출해 **2회 빌드 동안 에러 없이 버려졌다**. dbt incremental 은 *대상 테이블에 없는 산출 컬럼을 조용히 폐기*한다 → **P57**(드리프트 방향이 증상을 결정한다: 물리에만 있으면 에러, 모델에만 있으면 무증상).
> ✅ **복구 완료(2026-08-04)**: 물리 ALTER(SILVER 개명 2+COMMENT · `DIM_MEMBER` ADD 4/DROP 3/COMMENT 8) + **정본 DDL 동기화**(`06_DDL.sql` DIM_MEMBER 블록 30컬럼 · `08_SILVER_테이블DDL` 2행) + `DIM_AD_CREATIVE` 모델 `DURATION_SEC` 산출 제거. 검증 = 모델산출↔물리 컬럼 **양방향 차집합 0** · 정본 DDL 로 임시테이블 생성 대조 **30==30**.
> ⬜ **잔여(사용자 실행)**: ① `dbt build` ② O27 §6 검증 재실행(⚠️기대값은 재구축 이전 측정치라 재측정 필요) ③ `05_SV_DDL.sql` 전체 실행(SERVING 복구).
>
> **▶ 🔴 최신 상태 [2026-08-04 종반] — 정본 = 문서10 §18 · 문서30 DEC-31**: 2차 `dbt build` 완료(`PASS=329 WARN=26 ERROR=0 TOTAL=355`·기대치 전항목 일치). **센티넬 전수감사** = 100% 센티넬 FK **13건/7팩트** · **도달 불가 차원 2건**(`DIM_PAYMENT` 7행 · `DIM_AD_CREATIVE` 8,716행) → **P52**. **GOLD 미주입 재계수 = 106/371(28.6%)**(ALL_NULL 44+ALL_ZERO 62) · 0행 테이블 `FACT_TARGET_BIZ` 8건 분리 시 **98/363(27.0%)** → **P53**. ⚠️ **종전 `125/454` 는 분모 구성 불명으로 인용 금지**. **GA 데이터검토 종결**(사용자 결정: GA 는 검토만·설계/구현 phase-2) = 가산 3종 배수 **정확히 1.0000** · `SESSION_CNT` **3.1508** · `ENGAGED_SESSIONS` **4.2959** · `PAGE_PATH` 정본 **#105 미충족**(쿼리문자열 제거 → #122 결연아동코드 파생 불가) → **P54** · `PAGE_LOCATION` `MAX()` 대표로 URL **88.9% 소실** → **P55** · `FACT_GA_BEHAVIOR.CAMPAIGN_SK` **상수 0** 이라 WIDE 캠페인 3컬럼 전건 `(미매핑)` · **URL `memnum` identity 결선 여력 +77.1%**(충돌 4). **COMMENT 가드 21컬럼 배포**(산식·구조 변경 0 · 검증 21/21). **귀속처 14건 = 11 귀속확정 + 3 잔여**(잔여 3건은 원인이 하나 — GA 원자 grain 팩트 부재). 🔴 **최종검토에서 내 오판정 1건 자체정정** — `PAGE_REFERRER` 를 「`DIM_GA_PAGE` 역할수행」→ **`DIM_GA_REFERRER` 신설**로 철회(host 206종 중 자사 11종·path 일치 51.1%·외부 25.3%) → **P56**. 자가검토 결함 **10건** 전수 기록(§18-G).
>
> **▶ SILVER→GOLD 컬럼 보존율 최초 측정 [O27 2026-08-03]**: 보존율 **54.7%**(199/364, 본 세션 실측). 탈락 165 = A **12** · B 재설계대기 **6** · C 1 · D **12** · **E 미판정 134** (⚠️초판 15/6/1/13/130 → 자기검토 판정오류 6건 정정, 10 §14-G). ~~GOLD 물리 미주입 **125/454(27.5%)**~~ → **2026-08-04 재계수 106/371(28.6%)** 로 대체·종전 수치 인용 금지(§18-C). 🔴 **DEC-27 §17-C `AGE_BAND` 판정 정정** — `AGE` 는 raw 나이가 아니라 **CM014 12종 코드**여서 구간 창작이 불요하다(독립 교차검증 `AGE=10↔SEX=6`·`AGE=11↔SEX=7` 각 100%). 🔴 `DIM_SERVICE.SEND_TYPE_L/M/S` 는 **정본 지표 #133~135** 이라 DROP 불가하나 현 grain(10→74)이 담을 수 없다 → **차원 재설계 결정 대기**(초판 DROP 판정 자기검토로 철회). 신규 교훈 **P34~P40** = 문서10 §14-E. 정본 = **10 §14** · **30 DEC-28** · 판정표 `30_output_share/08_SILVER→GOLD_보존율.csv`.

> #### 🟢 [2026-08-14 O75] 데이터 마이그레이션 문서군에 `GN_DW.ML` 예측결과 16종 편입 — 문서·스크립트 6종 수정 + 06번 신설
>
> 🔒 라벨 = **O75**(게이트 「다음 **O80**」 ↔ 실사용 최대 **O74** · `P222` **12연속** ⇒ 실사용 기준 +1).
> 🟢 착수 시 인자 없는 `id_collision_gate.py` 선실행(동일 문서 내 정의 중복 **0**) + `--next O` 병행.
> 트리거 = 사용자 지시 「`20_ML_ddl.sql` 이 있어 ML 스키마 구성은 가능하지만 **데이터가 없어 후속작업이 제한**된다 ⇒
> 마이그레이션 문서들을 `GN_DW.ML` 스키마도 포함하도록 수정」.
> 🔴 **범위 결정 = 사용자 확정**: ML BASE TABLE 49 중 **예측결과 `ML_RST_DATA_*` 16종만** 이관.
> 학습 `ML_TRAIN_DATA_*` 20 · 원천 스냅샷 12 · 로그 1 · 뷰 4 · 프로시저 14 · 모델 14 는 **제외**(사용자 지시 「학습용은 Agent 미사용」).
>
> ##### 🟢 산출물 (신설 1 · 수정 5)
> - **신설** `50_handoff/06_데이터마이그 GN_DW_ML_DDL_20260814.sql`(355줄) — ML 스키마 + 결과 16종 DDL.
>   `20_ML_ddl.sql` 에서 **무변경 발췌**(python 추출 · 컬럼 순서·타입·COMMENT 보존). 04번(브론즈 DDL)과 대칭 역할.
> - **수정** `01_데이터마이그레이션 20260730.md` — §3.1-B(ML 부여) · §5.1-B(ML DDL) · §5.6(ML 적재) · **§11 신설**(범위·전제·미해소) +
>   파일맵·식별자·§4.2·§4.4·§5.3·§5.3.1·§6.1~6.3·§7·§8·§9·§10 갱신.
> - **수정** `02_데이터마이그 A_PRODUCER.sql` — **2-B 구간 신설**(ML 16종 테이블 단위 부여) · 스냅샷 쿼리 ML 필터 · teardown REVOKE.
> - **수정** `03_데이터마이그 B_BROKER.sql` — 3.1 집계·**3.1-B(ML 16 확인)** · 언로드 커서에 `OR table_schema='ML'`.
> - **수정** `05_데이터마이그 C_CONSUMER.sql` — **A.5-B 구간 신설**(ML 적재) · A.1 (4) 헤더대조에 ML 포함 · A.6 (1)(2)(4)·COPY_HISTORY 필터 확장.
> - **수정** `02_1_A DB정보.sql` — **A7·A8 블록 신설**(ML 실측·부여확인 쿼리 · 결과는 **미실측 플레이스홀더**).
>
> ##### 🔴 설계 판단 3건 (근거 있음)
> - **① 스키마 통짜 GRANT 금지** — `GRANT SELECT ON ALL TABLES IN SCHEMA GN_DW.ML` 은 학습·스냅샷 **33종을 함께 열고**,
>   B 언로드 루프가 그것을 그대로 가져가 C 에 적재한다. **오류가 나지 않아 적재 후에야 발견된다.**
>   ⇒ 테이블 단위 부여 16건 + 3.3 「ML 16 확인」 게이트. 🟢 부수효과 = 공유 DB `INFORMATION_SCHEMA` 에 16종만 보여
>   **언로드 화이트리스트를 두 번 관리하지 않는다**(단일 지점 통제).
> - **② VARIANT 4종을 스키마 일괄 프로시저에서 제외** — `PREDICTION VARIANT` 보유 4종을 일반 COPY 하면 JSON 이 **문자열**이 되고,
>   `PREDICTION:probability:"1"` 탐색이 조용히 실패해 **SERVING 뷰에서 NULL** 이 된다(GA4 함정의 재발 · 8장-4).
>   ⇒ 전용 프로시저 `LOAD_ML_RESULT_TABLES()`(12종 · **테이블명 기준 제외**) + 4종 개별 `TRY_PARSE_JSON` COPY + 파싱 검증(A.5-B.4 (3)).
> - **③ CSV 경로 유지**(Parquet 분리 안 함) — VARIANT 가 **테이블당 1개이고 전부 마지막 컬럼**이라 `$n` 매핑이 단순하고,
>   GA4 에서 이미 검증된 경로다. 포맷을 둘로 나누면 스테이지·파일포맷·검증이 이중화된다. ⚠️ 대가 = 변환 누락 시 무증상.
>
> ##### 🟢 실측 (`R2-3`)
> - `PREDICTION` 컬럼 위치 = **python 으로 DDL 파싱해 확정**(문서 기재를 근거로 쓰지 않았다):
>   `SPNSR_CHURN_12M` 18컬럼 **$18** · `MBER_CHURN_12M` 18컬럼 **$18** · `MBER_INC_12M` 21컬럼 **$21** · `LOYAL_MBER` 22컬럼 **$22**.
> - ML 객체 구성 = BASE TABLE **49**(결과 16 + 학습 20 + 스냅샷 12 + 로그 1) + VIEW 4 + PROCEDURE **14** + 모델 **14**.
>   🔴 모델 14종은 **프로시저 본문 안에서 `CREATE`** 된다 ⇒ 독립 DDL 이 아니고 학습 데이터 없이는 생성 불가
>   ⇒ **C 계정은 예측 「소비 환경」이고 「실행 환경」이 아니다**(06번·01번 §11-1 에 명시).
> - 🔴 **라이브 계정 실측 = ML 스키마 부재** — `GN_DW.INFORMATION_SCHEMA.SCHEMATA` 에 `ML` 없음
>   (실재 = BRONZE 4 · SILVER · GOLD · OPS · SERVING · SECURITY · PUBLIC) ⇒ 사용자 전제(「데이터가 없다」)를 실측으로 확인.
>
> ##### 🔴 신규 발견 — O74 배포 기재와 라이브가 불일치 (판정 보류)
> `20_ML_SV_설계.md §7` 은 **SERVING 뷰 7 + SV 7 + Agent 도구 7 배포 완료**로 기재하지만 현재 계정(`DV07626`)에 없다.
> 실측 = `SHOW VIEWS IN SCHEMA GN_DW.SERVING` **0건** · `SHOW SEMANTIC VIEWS` **9종**(`SV_AD`·`SV_BUDGET` 등)에 `SV_ML_*` **없음** ·
> `ACCOUNT_USAGE.SCHEMATA` 에 `GN_DW.ML` 이력 **없음** · `QUERY_HISTORY` 10일 내 `GN_DW.ML.` 참조 **0건**.
> ⚠️ **원인은 판정하지 않는다** — 다른 계정 배포인지 이후 소실인지 근거가 없다(`P169` 계열 · 추정 금지).
> 🟢 절차 영향만 확정 = **ML 적재 후 21·22번 + Agent spec 재배포 필요**(01번 §10 **10-B 단계** 신설 · §11-4 등재).
> 🔴 **50_handoff C 계정 식별자도 stale 이었다** — 문서 기재 `zx03676` ↔ 실측 `DV07626`. 취소선 병기로 정정(§2·§2.1 · 05번 헤더).
>
> ##### ⬜ 미실측으로 남긴 것 (01번 §11-5 에 표로 등재)
> - ① A 계정 ML 16종 테이블별 행수·용량 → `02_1_A DB정보.sql` **A7**
> - ② B 언로드 ML 폴더·파일·바이트 → 03번 6단계 + 05번 A.1 (1)
> - ③ C 적재 후 ML 합계 행수 → 05번 A.6 (1)
> 🔴 **세 곳을 함께 채운다** — 한 곳만 채우면 대조 기준이 갈려 검증이 무력화된다(브론즈에서 겪은 유형).
> ⚠️ 참고값 **1,045,732행**은 **A 실측이 아니다**(원천 계정 라이브 O74 · 정본 `20_ML_SV_설계.md §0-A`).
> ML 은 기준월 단위 `DELETE`+`INSERT` 누적이라 **기준월이 늘면 행수가 증가**한다 ⇒ 이관 시점 실측만 대조 기준.
>
> ##### 🟢 게이트
> - `doc_line_length_gate.py` **PASS**(2,000자 초과 0 · 표 열수 위반 0) · 50_handoff 12파일 자체검사도 초과 **0**.
> - `cortex ws ls` 로 스테이지 실체 확인 — 수정 5 + 신설 1 전건 반영(`R2-5`).
> - `id_collision_gate.py` 인자 없이 선실행 **통과**(정의 중복 0).
>
> ##### ⚠️ 이관으로 해소되지 않는 것 (01번 §11-3)
> O74 가 적발한 ML 결함(회원 중복 18.02% · `12M`↔6M · 모집단 10.10% · `'UNKNOWN'` · 후원사업 합 125%)은
> **데이터를 옮겨도 그대로 따라온다.** ⇒ 이관 검증에서 「적재 오류」로 오진하지 않도록 §9·§11-3 에 경고를 박았다.

> #### 🟢 [2026-08-14 O76] 마케팅용 Cortex Agent 설계 신설 — 신설 SV 0 · `09` 판정 6건 실측 반증 (**미배포**)
>
> 🔒 라벨 = **O76**(게이트 「다음 **O81**」 ↔ 실사용 최대 **O75** · `P222` **13연속** ⇒ 실사용 기준 +1 · `R1-4-3`).
> 🟢 착수 시 인자 없는 `id_collision_gate.py` 선실행(동일 문서 내 정의 중복 **0**) + `--next O` 병행 · 원장 §1 에 **선점 등재 후 착수**.
> 트리거 = 사용자 지시 「회원·overall Agent 만 만들어졌으니 슬슬 **마케팅용 Agent** 도 만들자.
> 요건 정본은 `99_provided_definition/04_마케팅_보고서필드 인벤토리.md` 이고
> 기존 SV·Agent 문서를 참고해 `05_SV-Agent_ai/01_SV-Agent 작업계획.md` 부터 순서대로 검토하며 진행하라」.
>
> ##### 🟢 사용자 결정 4건 (질문으로 확보)
> 1. 라우팅 범위 = **요건 5섹션 전량 = 6 SV**(회원 도메인 SV 3종을 `AGENT_MEMBER` 와 공유)
> 2. 이번 세션 = **설계 문서까지**(배포 전 정지 · `CREATE AGENT` 는 승인 후)
> 3. `analyst_ad` = **양쪽 보유**(중복 허용) ⇒ **`AGENT_OVERALL` 무변경**
> 4. ML 도구 드리프트 = **미착수·미등재**(사용자가 직접 배포 예정 · 정본이 맞다는 전제)
>
> ##### 🟢 산출물 (신설 2 · 수정 4)
> - **신설** `05_SV-Agent_ai/30_마케팅_AGENT_설계.md`(357줄 · 설계 정본)
> - **신설** `cortex_project/agents/AGENT_MARKETING/agent_spec.yaml`(227줄 · 스펙 정본 · **미배포**)
>   · 검증 = `yaml.safe_load` 통과 · 도구 **6 = 리소스 6** · 참조 SV **6종 전부 라이브 실재** · 2,000자 초과 0
> - **수정** `01_SV-Agent 작업계획.md` — §1.2 마케팅 Agent 행 재작성 + 유예 사유 stale 정정 + 진행상태표 **§11 신설**
> - **수정** `20_issue/00_INDEX_이슈원장.md` §1 — 선점 행 → 결과 행 갱신
> - **수정** `05_SV-Agent_ai/08_AGENT_spec.md` — **표 열수 위반 10건 교정**(아래 게이트 항목)
> - **수정** `scripts/doc_line_length_gate.py` — 분모 glob **2패턴 추가**(`P229` 청산)
>
> ##### 🟢🟢 핵심 결론 — **신설 SV 는 필요 없었다**
> 마케팅 요건 5섹션을 답할 SV 가 **이미 6종 배포돼 있다**(2026-08-14 `SHOW SEMANTIC VIEWS` 실측 9종):
> `SV_DEV_ACHIEVEMENT`(①개발현황) · `SV_BUDGET`(②집행예산) · `SV_AD`(③매체별 개발효율) ·
> `SV_MEMBER_EVENT`(④전환회원) · `SV_MEMBER_COHORT`(⑤-1 활동기간) · `SV_MEMBER_FEE`(⑤-2 납입회비).
> ⇒ 작업의 실체는 **SV 신설이 아니라 Agent 신설 + 도구 라우팅 + 요건 재배속**이었다.
> 🔴 `01_작업계획` §1.2 의 「마케팅 Agent = bronze 원천 미완으로 Phase-2 유예」는 **절반이 stale** —
> 「`SV_AD` 미배포·스캐폴드」는 2026-07-28 배포 + O45 마케팅캠페인 축 활성으로 해소됐고,
> 잔여는 **GA 계열 3필드(`G-5`) + 소재·캠페인유형 축(`Q10`)** 으로 좁혀졌다. 그 둘은 **Agent 개설을 막지 않는다.**
>
> ##### 🔴🔴 신규 발견 — `09_보고서필드_조립가능성` 마케팅 판정 **6건이 SV 층 실측과 어긋난다**(M1~M6)
> **원인** = `09` 는 섹션마다 앵커 팩트 1개를 골라 도달 가능성으로 판정하는데
> 마케팅 섹션 **4·5 의 앵커가 `FACT_MEMBER_MONTHLY`** 이고 요건 개념은 **FME·FMC·FMF·위성 pre-join** 에 있다.
> ⇒ 판정이 「데이터 부재」가 아니라 **「그 앵커에서 도달 못 함」**을 뜻한다 = **「섹션당 앵커 1개」 설계 한계 + `앵커_경합` 판정 미부여**.
> 🔴 **[O76-B A5 정정] 최초 이 줄은 「`O47-B` 앵커 임의성의 재현」이라 적었다 — 오귀속이다**(`O47-B` = 앵커 동점 비결정성 버그 · 골든 `T1` 로 해소).
>
> | # | 필드 | `09` 판정 | 2026-08-14 실측 |
> |---|---|---|---|
> | M1 | 광고유형 | 도달불가(소재 FK 센티넬) | `AD_TYPE_NM` **디지털 205,059/205,059 전건** |
> | M2 | CM위치 | 도달불가 | `CM_POSITION` **방송 32,840/36,416 = 90.2%** |
> | M3 | 광고비 | 값없음(`FACT_BUDGET.AD_COST`) | 비영 **디지털 193,959 · 방송 35,046 · 재방송 2,007** — 예산(ERP)이 아니라 **대행사(AGENCY)** 원천 |
> | M4 | 개발구분 | 값없음(`FMM.DEV_TYPE` 전건 NULL) | `FME.DVLP_DIV_NM` **3,594,843 전건** · `FMC.ACQ_DVLP_DIV_CD` **1,585,949 전건** |
> | M5 | 가입캠페인중단일 | 값없음(`FMM.STOP_DATE`) | `FMC.FIRST_STOP_DATE_SK` **901,577/1,585,949 = 56.85%**(미중단 회원은 값 없음이 **정상**) |
> | M6 | 성별·연령대 | 조립가능(`DIM_MEMBER` 경유) | 사건·획득 시점 축 **전건 채움** ⇒ SCD2 **4.49배 팬아웃** 회피(`P151`) |
>
> 🔴 **`09` 산출물 자체는 고치지 않았다** — 앵커 로직 변경은 507행 전량·골든 재승인에 영향하므로 **별건**이고,
> 이 세션 문서(`30` §2)가 마케팅 축 **정정 등재부**다. 🔴 배속 판정 **가용 41 · 부분 11 · 불가 21** 은
> `09` 분포(조립가능 30·SV파생 11·도달불가 11·원천부재 10·값없음 10·요구grain부정합 1)와
> **분모는 같고 기준이 다르다** ⇒ 개선·악화로 읽지 말 것.
> 🟢 「판정불가(SV파생)」 11건은 **불가가 아니다** — 누계·달성율·집행율은 SV metric 파생이라 물리 컬럼 스캔이 판정할 수 없었던 것이다(`P7`).
>
> ##### 🔴 설계에 박은 마케팅 고유 가드
> - **「개발단가」가 두 개다** — 대행사 리포트 기준(공7 디지털·공8 재방송)은 산출되고,
>   **광고비 ÷ CRM 개발실적은 cross-fact 라 산출 불가**다. 두 정의를 섞지 않고 기준을 밝힌다.
> - **cross-fact 금지가 이 도메인에서 가장 자주 깨진다**(원천 4계열) — 예산(ERP) ↔ 광고비(AGENCY) ·
>   광고비 ↔ 개발실적(CRM) · 목표(CRM) ↔ 광고 성과는 전부 금지. 표를 분리하고 각 표에 원천을 각주로 단다.
> - **캠페인별 LTV 는 한 표로 못 답한다** — 활동기간(회원 1행) ↔ 납입회비(회원×회비월×사업) grain 상이
>   ⇒ 표를 분리해 캠페인 축에서 병기하고, **활동기간 × 납입회비를 곱해 단일 LTV 를 만들지 않는다**(정본에 산식이 없다).
> - **「캠페인 유형별」을 뭉뚱그려 불가로 답하면 틀린다** — 광고 효율은 불가지만 **개발 실적은 산출된다**.
>
> ##### 🔴 착수 중 자기적발 2건
> 1. **SQL 식별자를 추측해 컴파일 3회 실패**(`SEX_NM_AT_EVENT` 등) — 인수인계 §7 이 *"SQL 식별자를 추측하지 말 것 —
>    O62 가 5회 컴파일 실패했다"* 로 **이미 경고한 함정을 그대로 재현**했다. ⇒ `INFORMATION_SCHEMA.COLUMNS` 로
>    컬럼 목록을 먼저 확보한 뒤 1회에 성공했다. 🔴 **읽은 인수인계가 재발을 막지 못했다**(O62 와 같은 구조).
> 2. **`P229` 재현** — 신설 2파일이 `doc_line_length_gate` 분모 **밖**이었다(Agent 스펙이 파일명 2건으로만 등재).
>    ⇒ O71-C 규약대로 **glob 승격**(`cortex_project/agents/*/agent_spec.yaml` · `05_SV-Agent_ai/*.md`).
>
> ##### 🟢 분모 확장이 기존 결함을 드러냈다 (`P187` 재현)
> 확장 직후 게이트가 **FAIL** 했는데 원인은 내 신설 파일이 아니라 **`08_AGENT_spec.md` 의 표 열수 위반 10건**이었다
> (§5.1 6행 · §5.2 4행 — 행이 **5열**인데 헤더가 **4열**이라 렌더러가 **정정 근거 칸을 잘라 버리고 있었다**).
> ⇒ **미독 편집 금지**(§O68-B `A1`)에 따라 `08` **415줄을 전량 독해**(140줄 × 3회 · 미반환 **0**)한 뒤
> 헤더에 5번째 열을 추가하고 4열 행에 빈 칸을 채웠다 — **값·판정 무변경**.
> 🔴 넓히지 않았으면 이 결함은 계속 조용했다.
>
> ##### 🟢 게이트
> - `doc_line_length_gate.py` **PASS**(2,000자 초과 0 · 표 열수 0 · 대상 **193 → 209파일**) ·
>   자기검사 **7/7** · **분모 확장 자기검사 10/10**(glob 8패턴 전건 ≥1건 · 제외 잔존 0 · 중복 4 관측)
> - `id_collision_gate.py` 인자 없이 실행 **통과**(동일 문서 내 정의 중복 **0**)
> - `doc_heading_gate.py` **통과 — 제목 유실 0**(현재 762 · 골든 741 · 신설만)
> - 🔴 **`P` 계열 신규 발급 0건** — 이 세션 발견(앵커가 판정을 만든다)은 **O47-B 의 재현**이라 새 번호를 만들지 않았다.
>
> ##### ⏸ 잔여
> - ⛔ **미배포**(사용자 결정) — 배포 절차는 `30` §6 · `cortex_agent_save`/`deploy` **사용 금지**(live 버전 충돌)
> - 🔴 **NL 스모크 미실행** — 6종 라우팅이 실제로 갈리는지 미검증(트라이얼 `DATA_AGENT_RUN` 차단 · 사용자 CoWork UI)
> - 🟠 **VQR·평가셋 미작성** — 마케팅 골든 질문이 `06`·`07` 에 없다
> - 🟠 **도구 설명 3중 동기화 비용** — 같은 SV 도구를 Agent 3종이 보유(중복 허용 결정의 알려진 대가)
> - 🟠 **`09` 앵커 로직**(별건 · `30` §7 ①)
