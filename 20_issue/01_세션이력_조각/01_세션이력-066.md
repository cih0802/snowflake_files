<!-- SPLIT-CHUNK 01_세션이력.md | 066/066 | 허브 = 01_세션이력.md | 원문 1~29행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-14 O159-B] 30_output_share 4대 정본에 MSTR 해결 불가 사유 및 실측 쿼리 전면 반영

> **지시** = `30_output_share/15_문서 통합.md`, `17_현업의사결정 요청.md` 및 후속 문서들에
> MSTR 로직이 있음에도 해결 불가능한 사항과 이유를 업데이트.
> **적용 분기** = `R4-4-2 ㉡`(지시 동봉) · 정본 4종 갱신 완결.

> **① `30_output_share/17_현업의사결정 요청.md` 갱신 (열린 의사결정 정본)**
> - **§1-3 [L-2]**: MSTR-4 (월마감 스냅샷 vs ODS 재계산 시 동시중단 다중사업 1.56배 팬아웃 차단 한계) 검토 주석 보강.
> - **§1-4 [MSTR-2] 신설**: 증액·감액 (건) 지표의 MSTR 1만 원 환산(`SPNSR_AMT / 10000.0`) 적용 축 확정 요청 및 실증 쿼리 신설 (`O24 미확정` 연계 · `DEV_CNT` 229.1만 건 모수 체계 변경 충돌 명시).
> - **§2-7 [MSTR-1] 신설**: 법인구분 `CPR_DIV_CD` 미정의 코드 `A`(21,167명, 1.20%) 업무 정의 및 `GOLD.DIM_MEMBER`/`DIM_SPONSORSHIP` 노출 기준 확정 요청 및 실증 쿼리 신설.
> - **§3-2 [F-1, F-2 / MSTR-3] 갱신**: MSTR 5단계 부서 계층(`D_DEPT_CD`) ↔ 현행 DW 4단(`DIM_ORG`) 및 `CONF-4`/`ORG-H` 거버넌스 충돌 사유 보강.

> **② `30_output_share/15_문서 통합.md` 갱신 (통합 검증 정본)**
> - §1 `[L-1, L-2]`, §1 `[MSTR-2]` 신설, §2 `[MSTR-1]` 신설, §4 `[F-1, F-2 / MSTR-3]` 갱신.
> - §8-2 및 §8-4 파생 무결성 표에 MSTR 4대 항목 파생 등재 완결.

> **③ `30_output_share/20_현업의사결정_요청_주니어_해설서.md` 갱신 (해설서)**
> - §0-1 과제 진행 현황 갱신 (대기 14건 / 협의 2건 / 완료 8건).
> - §1-3 MSTR-4 해설, §1-4 MSTR-2 해설, §2-8 MSTR-1 해설, §3-2 MSTR-3 5단 계층 해설 추가.

> **④ `30_output_share/22_요청서 통합 및 매핑.md` 갱신 (매핑표)**
> - §1 역할별 요약 및 §2 프로세스 순 25개 미해결 과제 상세 매핑표에 MSTR-1~4 전수 매핑 반영.

> **⑤ 품질 및 가드 검증**
> - 수정된 4파일 모두 `line_len.py` 2000자 가드 `🟢 PASS` (초과 줄 0).
> - 원장 §1 대시보드 O159 행 갱신 및 허브 재발행 완료.

_Co-authored with CoCo_

> #### 🟢 [2026-09-14 O160] 착수표 ⑭(MSTR-4) STOP 후원사업 분해 실측 및 단일사업(92.45%) 1차 배선 + dbt build 완결 + 현업 질의 §N-13 상정
>
> - **작업 배경**: 착수표 ⑭ / MSTR-4 (`FME.SPONSORSHIP_SK(STOP)`) 동시중단 다중사업 귀속 규칙 미확정으로 인해 STOP 행(1,038,262행) 전체가 `SPONSORSHIP_SK = 0` 센티넬로 차단되어 있던 이슈를 원천 데이터 실측 및 분해 접근으로 해결 검토.
> - **원천 데이터 실측 및 수치 교정**:
>   - 착수표 ⑭ 기재(팬아웃 1.56배)는 날짜 일치 없는 회원단위 조인의 과대표기(1.6743배)였으며, 중단일(`SPNSR_DSCNTC_DE`) 일치 정상 조인은 **1.1020배**임을 실측 규명.
>   - FME STOP(1,038,262행) 중 **단일사업 중단(92.45%, 959,872행)**은 귀속 룰 없이도 고유하게 1:1 확정됨을 발견.
>   - 잔여 다중사업 건은 **행수 일치(6.04%, 62,725행)**, **사업 초과(1.25%, 12,935행)**, **원천 미매칭(0.26%, 2,730행)**으로 정밀 분해됨.
> - **방안 A(대표 1건) vs 방안 B(DVLP5 전개) 검토 및 권고**:
>   - 방안 A: 대표 1건 선정 시 다중사업 키에서 금액 신호 57.66% 폐기 및 사업 단위 사건 18,796건 소멸 발생.
>   - 방안 B: `DVLP_DIV_CD='5'` 사용 시 중단이력 키 49,284건(5.06%) 결손, `STOP_CNT` 기준선 변경(−27,582행), 중단사유/경로 축 부재 확인.
>   - 결론: 92.45% 단일사업을 우선 배선하고, 잔여 7.29%만 현업 질의로 분해 상정하는 분해 착수 채택.
> - **dbt 모델 1차 배선 (`FACT_MEMBER_EVENT.sql`)**:
>   - `stop_single_biz` CTE 신설: `CRM_MEMBER_SPONSOR_SPAN`에서 `SPNSR_DSCNTC_YN='Y'`인 단일 후원사업 건을 추출하여 `STOP` 브랜치에 조인, 959,872행(92.45%) 1:1 배선 완결.
> - **라이브 dbt build 및 실측 검증**:
>   - `dbt build --select FACT_MEMBER_EVENT` (PASS=27 WARN=1 ERROR=0) 정상 완결.
>   - 라이브 DB 실측 결과: `TOTAL_ROWS=4,633,105`, `SPONSORSHIP_SK 채움률=98.31%`(4,554,715행 / 4,633,105행), `DEV_CNT=2,291,878`, `STOP_CNT=1,038,262`로 팬아웃 0 및 기준선 100% 보존 확인.
> - **현업 질의(문서20 §N-13) 상정 및 문서 갱신**:
>   - `20_issue/20_현업확인_요청.md` **§N-13** 등재 및 허브 발행 완료.
>   - `20_issue/50_dbt_파이프라인_미결조치.md` §BLOCKING-5 표 정정 및 `99_NEXT_SESSION.md` 착수표 ⑭ 1차 부분배선 갱신.
> - **비판적 자기검토**:
>   - 지침 준수: R1-3-7-c에 따라 판정 전 근거 파일(`tmp/_o160_stop_sponsorship_evidence.md`)을 먼저 작성 후 판정.
>   - R4-1 정지점 준수: dbt build 명령을 에이전트가 직접 실행하지 않고 사용자에게 제시하여 실행 완료.
>   - 불변식 검증: 핵심 게이트 6종 및 단위 테스트 21종 전건 PASS 확인.

> #### 🟢 [2026-09-15 O161] 원천 정의 11/12/18 재대조 → 인수인계 BRONZE DDL 개편 반영 + 문서군 수치 2세대 stale 일괄 정정 + 04·06번 파일명 날짜 제거
>
> **지시** — `99_provided_definition/` 의 `11_bronze_crm_ddl.sql`·`12_bronze_agency_ddl.sql`·
> `18_silver_bigquery_refined.sql` 3종이 갱신되었으니 읽고, 마이그레이션용 DDL
> (`50_handoff/04_…BRONZE_DDL`·`06_…SILVER_DDL`)을 갱신하라. 갱신했으면 파일명에 날짜를 붙이라.
> CRM 신규 테이블의 컬럼 코멘트는 동봉된 현업 메시지 이력 2건(a·b)을 근거로 삼으라.
> ⇒ `R4-4-2 ㉡` 적용(브리핑 출력 후 되묻지 않고 즉시 착수). 파괴 작업(파일 삭제)은 별도 승인을 받았다.
>
> **① 독해 기록 (`R1-3-7-b`)** — 미반환 0건 · 재호출 0건.
> · `99_provided_definition/11_bronze_crm_ddl.sql` (1,116줄 · offset 0/300/600/900 4회) — 도착 O ·
>   고유 토큰 = `SND_MEMBER_MAIL_LINK_LOG`(81행) · `TC_MKTNG_DTL_CD`(195행) · `MKTG_CHANNEL`(490행).
> · `99_provided_definition/12_bronze_agency_ddl.sql` (1,029줄 · 4회) — 도착 O ·
>   고유 토큰 = `AGENCY_TASK_MASTER`(984행) · `GRP_DIV_NM`(40행).
> · `99_provided_definition/18_silver_bigquery_refined.sql` (187줄 · 1회) — 도착 O ·
>   고유 토큰 = `SP_LOAD_GOOGLE_SEARCH` · `sc-domain:goodneighbors.kr` ⇒ **내용이 GSC DDL 이다**.
> · `50_handoff/04_…BRONZE_DDL_20260730.sql` (1,609줄 · 6회) — 도착 O · 고유 토큰 = `OPEN_DT`(311행).
> · `50_handoff/06_…SILVER_DDL_20260820.sql` (237줄 · 1회) — 도착 O · 고유 토큰 = `STSLC_MC_SOURCE_PLATFORM`.
> · `00_guides/00_작업지침_세션운영규칙.md` (356줄 · 1회) — 도착 O · 고유 토큰 = `R0-8-4`.
>
> **② BRONZE_CRM 개편 반영 (원천 11번 정본)** — 게이트가 차이를 전건 열거했고 그대로 반영했다.
> · **신규 4종** = `SND_MEMBER_MAIL_LINK_LOG`(13컬럼) · `SND_MEMBER_OPEN_LOG`(8) ·
>   `TC_MKTNG_DTL_CD`(16) · `TM_MM_FDRM_MBER_DT_DTLS`(5).
> · **삭제 2종** = `TM_CM_MKTNG_CMPGN_MNG`(10) · `TM_CM_MKTNG_UTM`(12) ⇒ `TC_MKTNG_DTL_CD` 로 **통합**.
>   🔴 `TC_MKTNG_DTL_CD` 는 기존 코드 테이블(`TC_CMMN_CD`/`TC_CMMN_DTL_CD`)과 **연관성이 없다**
>   (현업 메시지 명시) — 컬럼명이 유사하지만 조인 대상이 아니다. `CD_ID` 3종 = `C001` 마케팅캠페인명 ·
>   `C002` 채널 · `U001` UTM. ⇒ 이 2종을 참조하는 SILVER/GOLD dbt 모델은 재작성 대상이다.
> · **컬럼 추가** = `TM_CM_CMPGN_MNG.MKTG_CHANNEL`(36 → 37컬럼 · 캠페인채널).
> · **컬럼 삭제** = `SND_MEMBER_LIST.OPEN_DT`(77 → 76컬럼) ⇒ 오픈 이력이 위 로그 2종으로 **확장**됐다.
>   🔴 이전 판 CSV 는 열이 1개 많다 ⇒ 열 수 불일치로 적재가 실패한다. **재언로드가 정답**이다.
> · 🟠 **누락분 2종 보완** = `TM_PM_INSTT_ACNUT`(16) · `TM_PM_SETLE_CMPNY_ACNT`(14).
>   이 2종은 **이번 개편으로 신설된 것이 아니라** 원천에 계속 있었는데 인수인계 판이 빠뜨린 것이고
>   `handoff_ddl_gate` **축1** 이 적발했다 ⇒ 신규분과 **구분해** 기재했다(사람이 오독하지 않도록).
> · **컬럼 코멘트 근거** — `컬럼정의서 20260714.csv` 에 신규 6테이블이 **전건 0건**(실측)이므로
>   현업 메시지 문안 + 명명 규칙으로 부여하고 「현업 확인 대상」을 명시했다(`병합 규칙` 3항).
>   ⚠️ `TC_MKTNG_DTL_CD` 의 감사컬럼은 타 CRM 테이블과 이름이 다르다(`FRST_RGSTR_DT`/`LAST_UPDT_ID`)
>   — 원천 그대로 두었다. 고치면 위치 기반 적재 대조가 깨진다.
> · **AGENCY(12번)·GA4·GSC** = 기계 대조 차이 **0건** ⇒ 손대지 않았다.
>
> **③ 게이트 판정 (`handoff_ddl_gate.py`)**
> · 착수 시점 = 축1 **9건** · 축2 **2건** · 축7 **5건** (총 16건 FAIL).
> · 반영 후 = **BRONZE_CRM 원천 50 ↔ 인수인계 50 · 축1~축6 전건 0건**.
> · 잔여 = **SILVER 축1 1건**(아래 ⑥) — 이것은 인수인계 파일의 결함이 아니다.
>
> **④ 문서군 수치 2세대 stale 일괄 정정 (사용자 결정)**
> · 발견 = `01`·`02`·`02_1`·`03`·`05`·`07`번이 「브론즈 52 · 총계 69」에 멈춰 있었다.
>   2026-09-01 GA4/GSC 복귀(→ 56/73)와 2026-09-15 CRM 개편(→ 60/77)이 **둘 다** 미반영이었다
>   ⇒ 04번만 갱신돼 있었고 나머지가 2세대 뒤였다(O113 이 경고한 「절반만 고친 문서군」의 재발).
> · 조치 = **현행 = 브론즈 60 · 총계 77 · CRM 50** 으로 전건 정정. 축7 **65건 → 0건**.
> · 게이트 `STALE_TOKENS` 기준값도 2세대 올렸다(69→77 · 52→60 · CRM 45/46→50) — 🔴 **옛 값을 지우지
>   않고 새 세대를 추가**했다(지우면 그 세대 표기가 다시 침묵한다). 파일명 토큰 2종도 신설.
> · 🟢 **음성 테스트 픽스처를 함께 고쳤다**(`test_handoff_ddl_gate.py` 축11/12 · 3건 → **4건** 기준).
>   🔴 이것이 이번 세션의 교훈이다 — 기준값을 올리면 **그 기준값을 쓰는 픽스처가 조용히 깨진다.**
>   실제로 게이트만 고친 직후 그 테스트가 `rc=1` 로 실패했고, 파이프 없이 종료코드를 받아 적발했다(`R0-8-2`).
> · 관례 = 과거 세대 문장은 **지우지 않고** 같은 줄에 현행값을 병기해 면제시켰다
>   (게이트 판정식 = 「그 줄이 독자에게 현행값을 함께 알려주는가」).
>
> **⑤ 파일명에서 날짜 제거 (사용자 결정 · 종전 관례 폐지)**
> · 종전 지시는 「갱신했으면 파일명 뒤 날짜를 갱신(`*_20260915`)」이었으나, 사용자가 **개명 자체가
>   불필요한 작업을 만든다**고 판단해 **파일명에서 날짜를 뗀다**로 전환했다.
> · 결과 = `04_데이터마이그 GN_DW_BRONZE_DDL.sql` · `06_데이터마이그 GN_DW_SILVER_DDL.sql`.
>   날짜는 각 파일 머리의 **「최근 갱신일」** 에만 적고, 그 이유를 파일 안에 남겼다.
> · 절차(`R1-7-1`·`R1-7-6/7` 준수) = ㉠ 새 이름으로 **바이트 동일** 사본 생성(SHA256 대조 True)
>   ㉡ 참조 6곳 + 게이트 경로 정정 ㉢ **그 뒤에** 구 파일 2개 개별 삭제 ㉣ `cortex ws ls` 로
>   스테이지 실체 확인(구 이름 live 부재 · 새 이름 실재). `_archive/` 의 옛 사본은 그대로 보존된다.
>
> **⑥ 🔴 미해소 — 원천 문서 소실 1건 (현업 요청 대상)**
> · `99_provided_definition/18_silver_bigquery_refined.sql` 의 내용이 `16_bronze_gsc_ddl.sql`
>   (BRONZE_GSC DDL)과 **바이트 동일**로 교체되었다(`diff` rc=0 실측).
> · ⇒ `99_provided_definition/*.sql` 전건에 `BIGQUERY_REFINED_DATA` 가 **0건**(`grep` rc=1)
>   ⇒ SILVER 정의의 원천이 워크스페이스에 **없다**.
> · 판단 = 06번 **DDL 본문 118컬럼은 한 줄도 고치지 않았다.** 고칠 근거가 사라졌기 때문이다
>   (`R2-8-4-c` 「대조 불가면 판정을 보류하고 수치를 창작하지 않는다」).
>   마지막 유효 판정은 2026-08-29 대조(차이 0)이며 그 사실을 06번에 명시했다.
> · 게이트 SILVER 축1 1건은 **이 소실의 신호**다 — 🔴 파일을 고쳐 이 경고를 없애지 마라.
> · 조치 = 현업에 **18번(SILVER 정의) 재공유 요청**. 받으면 다시 기계 대조한다.
>   대체 기준 = `02번 6.2` 의 `GET_DDL('TABLE','GN_DW.SILVER.BIGQUERY_REFINED_DATA',TRUE)`.
>
> **⑦ 검증 (전건 종료코드로 확인 · `R0-8-2`)**
> · `handoff_ddl_gate.py` = 축1 1건(⑥ · 의도된 신호) · 축2~축7 **0건**.
> · `line_len.py` 9파일 = 🟢 PASS(상한 초과 0줄).
> · 음성 테스트 = `test_handoff_ddl_gate.py` **단정 17 · 통과 17 · 실패 0**.
> · `scripts/test_*.py` 전건 루프 = 실패 **1건** = `test_verify_wide_doc.py` (`rc=1`).
>   🟠 이 실패는 **본 세션과 무관한 선행 결함**이다 — `verify_wide_doc.py` 가 라이브 뷰 ↔ dbt ↔ 문서를
>   대조하는데 이 계정에 라이브 객체가 없어 `View names mismatch!` 로 죽는다(WIDE·GOLD 소관).
>   🔴 내가 만든 것이 아니므로 고치지 않고 **다음 세션 확인 항목으로 남긴다**.
> · 워크스페이스 게이트 5종(`doc_type`·`clause_order`·`index_row`·`doc_heading`·`doc_coord`) = 전건 `rc=0`.
> · `cortex ws ls` = 04·06번 새 이름 실재 · 구 이름 live 부재 확인.
>
> **⑧ 확정위반 (`R4-4-4` 롤백 판단 근거)** — **0건**.
> 브리핑을 먼저 출력했고(`R4-4-1`), 파괴 작업 전에 별도 승인을 받았다(`R4-4-3`).
> ⚠️ 판정약점 1건 = `TM_PM_INSTT_ACNUT`·`TM_PM_SETLE_CMPNY_ACNT` 가 **언제부터** 빠져 있었는지는
> 확정하지 못했다(`_archive/` 에 11번의 옛 스냅샷이 없다) ⇒ 「개편 신규가 아니다」까지만 단정했다.

> #### 🟢 [2026-09-16 O162] CRM 50 개편 반영 SILVER·GOLD 설계 개정 + SILVER/GOLD DDL 반영 + dbt 모델 7종 갱신/신설 + 검증 완결
>
> - **배경 및 요구사항**:
>   - CRM 원천 50테이블 개편(신규 4: `TC_MKTNG_DTL_CD`·`SND_MEMBER_OPEN_LOG`·`SND_MEMBER_MAIL_LINK_LOG`·`TM_MM_FDRM_MBER_DT_DTLS`, 삭제 2: `TM_CM_MKTNG_CMPGN_MNG`·`TM_CM_MKTNG_UTM`, 컬럼 ±1: `TM_CM_CMPGN_MNG.MKTG_CHANNEL` 추가 / `SND_MEMBER_LIST.OPEN_DT` 삭제)에 따른 상위 계층 반영.
>   - 직전 O161 이 신설한 열린 작업인 「dbt 영향 분모 실측 및 모델 재작성」을 완결.
>
> - **수행 내역**:
>   1. **설계 문서 2종 개정**:
>      - `04_silver_design/03_SILVER_작업계획_CRM전용 20260714.md` §7 신설 (BRONZE 50테이블 개편 전량 반영, 코드 계통 분리·오픈로그 fan-out 위험·전환테이블 커버리지·현업확인 M-1~M-6).
>      - `03_top-down_gold/03_테이블 설계.md` §7 신설 (마케팅 코드 계통 교체, `DIM_CAMPAIGN` 채널축 신설, FSE 오픈로그 축약 적재 연계).
>   2. **SILVER DDL 갱신 (`04_silver_design/08_SILVER_테이블DDL_20260714.sql`)**:
>      - CRM 22 → **26테이블** (전체 SILVER **44테이블**)로 확장.
>      - `CRM_CAMPAIGN` 에 `MKTG_CHANNEL` / `MKTG_CHANNEL_NM` 추가.
>      - `CRM_SEND_MEMBER` 의 `OPEN_DT` 코멘트에 `SND_MEMBER_OPEN_LOG` 축약 적재 명시.
>      - 신규 DDL 4종 추가: `CRM_MKTNG_CODE` (마케팅 통합 코드), `CRM_SEND_MEMBER_OPEN_LOG` (메일 오픈 로그), `CRM_SEND_MEMBER_LINK_LOG` (메일 링크 클릭 로그), `CRM_MEMBER_CONVERT_HIST` (일시→정기 전환 매핑).
>      - `CRM_MARKETING_CAMPAIGN` 주석을 `TC_MKTNG_DTL_CD` C001 호환 정제로 갱신.
>   3. **GOLD DDL 갱신 (`03_top-down_gold/06_DDL.sql`)**:
>      - `DIM_CAMPAIGN` 에 `MKTG_CHANNEL NUMBER(38,0)` 및 `MKTG_CHANNEL_NM VARCHAR` 추가.
>      - `FACT_MESSAGE_DISPATCH` 의 `OPEN_MEMBERS` 코멘트에 오픈로그 축약 반영 명시.
>   4. **dbt 모델 7종 갱신 및 신설 (`10_dbt_pipeline/models/`)**:
>      - `models/silver/_sources.yml`: 신규 4원천 선언, 삭제 2테이블 정리.
>      - `models/silver/crm/CRM_CAMPAIGN.sql`: `TC_MKTNG_DTL_CD` 조인으로 C001, C002(채널), U001(UTM) 라벨 연결.
>      - `models/silver/crm/CRM_MARKETING_CAMPAIGN.sql`: `TC_MKTNG_DTL_CD` 의 `CD_ID='C001'` 기반 모델로 재정의.
>      - `models/silver/crm/CRM_SEND_MEMBER.sql`: `SND_MEMBER_OPEN_LOG` 에서 `MIN(OPEN_DT)` 집계 조인으로 `OPEN_DT` 보존.
>      - `models/silver/crm/CRM_MEMBER_DEV.sql`: `MKTG_CHANNEL`, `MKTG_CHANNEL_NM` 추가.
>      - 신규 Silver 모델 4종 작성: `CRM_MKTNG_CODE.sql`, `CRM_SEND_MEMBER_OPEN_LOG.sql`, `CRM_SEND_MEMBER_LINK_LOG.sql`, `CRM_MEMBER_CONVERT_HIST.sql`.
>      - `models/gold/dim/DIM_CAMPAIGN.sql`: `MKTG_CHANNEL`, `MKTG_CHANNEL_NM` 컬럼 및 SK=0 NULL 추가.
>      - `_crm_schema.yml`, `_gold_ready_schema.yml`: 신규 4모델 스키마/테스트 및 `MKTG_CHANNEL_NM` 테스트 등록.
>   5. **검증 및 사용자 dbt compile 성공**:
>      - 사용자 실행: `dbt compile --target dev --select models/silver/crm`, `dbt compile --target dev --select models/gold` (98 models, 359 tests) 정상 완료.
>      - `line_len.py` 10파일 PASS, 문서 게이트 4종 rc=0, 음성 테스트 전건 rc=0 (선행결함 1건 제외).

> #### 🟢 [2026-09-17 O163] DDL↔라이브 드리프트 37건 전수 규명 + `DEC-50` SILVER 미집행 8건 개명 + 라이브 복원(드리프트 0 · FK 56) 완결
>
> - **착수 배경**: 사용자 dbt build 가 `16 model(s) failed out of 560` 로 실패했고 첫 에러가
>   `003001 (42501) … must have MODIFY granted on TABLE` 였다. 사용자 가설 = *"O160 이후 요건
>   (CRM 7~8월 백업 신규코드 적재)으로 파이프라인을 수정했는데, 요건에 해당되지 않는 테이블이
>   stale한데 그냥 넘어가서 ddl과 dbt가 문제가 생긴 것 같다"*.
>
> 1. 🔴 **「권한 문제」가 아니었다 — `GRANT MODIFY ON TABLE` 은 존재하지 않는 권한이다.**
>    - `GRANT MODIFY ON {ALL|FUTURE} TABLES` 4건 전부 `Invalid object type 'TABLE' for privilege 'MODIFY'` 로 실패.
>      ⇒ 이 메시지는 **OWNERSHIP 요구의 표현**이고 권한 부여로 해결할 수 있는 종류가 아니다.
>    - 발화 문장 실측 = `alter  table … add column` (**공백 2칸**). 🔴 `grep "alter table"` 로는
>      한 번 놓쳤다 — dbt 가 두 칸으로 발행한다. 로그 조사 시 주의.
>    - 원인 경로 = `+on_schema_change: append_new_columns` 의 **ADD COLUMN**. 가드 매크로
>      `gn_no_structural_alter` 는 **타입 확장만** 막고 그 헤더 22~24행이 컬럼 추가 경로를
>      의도적으로 열어 두었다 ⇒ O95(P82 방어)와 O121(구조 보호)이 **서로 충돌**하며, 그 충돌은
>      DDL 과 모델의 컬럼 집합이 일치할 때만 잠재 상태로 남는다.
>
> 2. 🟢 **어긋난 축 규명 = 「모델↔DDL」이 아니라 「DDL 파일↔라이브」였다.**
>    - 신설 도구 `scripts/o163_ddl_live_drift.py` 로 전수 실측 = **컬럼 드리프트 37건 · MISSING_TABLE 0건**.
>    - 15모델 전건에서 **DDL 파일과 dbt 모델의 컬럼 집합이 완전 일치**하고 라이브만 낡음.
>      ⇒ 라이브는 이력 `01_세션이력-059.md:119` 기재대로 **계정 재구축(2026-09-07) 후
>      O139~O142 가 손댄 것만 재생성**된 부분 복구 상태였다.
>    - 🟢 **요건 반영분은 이미 라이브에 있었다**(사용자 가설 확증): `CRM_MKTNG_CODE` ·
>      `CRM_SEND_MEMBER_OPEN_LOG` · `CRM_SEND_MEMBER_LINK_LOG` · `CRM_MEMBER_CONVERT_HIST` 4종 실재 ·
>      `MKTG_CHANNEL` 4곳 배선(`CRM_CAMPAIGN`·`CRM_MEMBER_DEV`·`DIM_CAMPAIGN`·`FACT_MEMBER_EVENT`).
>      CRM 계열 드리프트는 `CRM_EVENT_PARTICIPATION.EVENT_SOURCE` **1건**뿐이고 깨진 15개는
>      예산·광고·행사·발송·후원 계열 = **전부 요건 무관 테이블**이었다.
>
> 3. 🔴 **자기판정 2건 철회(사용자 지적으로 반증)**.
>    - 초판은 `FACT_AD_DIGITAL` 7건·`FACT_AD_PERFORMANCE` 3건을 「라이브 최신 · DDL stale」로
>      판정했다. 사용자 지적(*"O133 개명이 GA→AGENCY일텐데 그럼 ddl이 맞는것 아닌가"*)을 받아
>      모델을 실독하니 **DDL 이 맞았다**: `FACT_AD_DIGITAL.sql:17~24` 가
>      `PAGE_TYPE_NM as PAGE_TYPE` 형태로 **SILVER=원천명(P규약) / GOLD=의미명** 2층 구조를 구현한다
>      ⇒ O133 이 `06_DDL.sql` 을 고치지 않은 것은 누락이 아니다. **역방향 드리프트 0건**.
>    - 🔴 개명 2건을 한 묶음으로 뭉갠 것도 정정했다: O133 = `DEC-46` 원천명 복원(GA 무관) ·
>      **O142 = `DEC-51` GA→AGENCY**. 사용자 규칙(*"ga 명칭은 ga4스키마 테이블만"*)은 후자의 근거와
>      정확히 일치하고, 라이브가 `GA_CONV_*` 인 이유는 「SV 때문에 취소」가 아니라 **계정 재구축 소실**이었다.
>    - 🔴 「보류 3건은 build 성공」이라던 초판 근거도 **오류**였다 — 실측하니 그 6모델은 **전부 SKIP**
>      (상류 실패)이라 모델↔라이브 대조가 애초에 수행되지 않았다. 모델 실독으로 재판정 =
>      `FACT_MEMBER_COHORT` 34/34 · `DIM_MEMBER_ACQUISITION` 21/21 · `FACT_EVENT_ATTENDANCE` 16/16 ·
>      `FACT_AD_DIGITAL` 7/7 · `FACT_AD_PERFORMANCE` 3/3 **전건 모델 실재** ⇒ 라이브만 낡음.
>
> 4. 🟢 **`DEC-50` SILVER 미집행 8건 개명(사용자 지적)**.
>    - 실측 = `06_DDL.sql` 의 `GA_*` 는 **0건**(GOLD 집행 완료 · `DIM_MEMBER_IDENTITY` =
>      `BIGQUERY_MEMBER_ID`)인데 SILVER 는 미집행이었다. 근거 = `_sources.yml` 활성 source 4종
>      (`SILVER`·`BRONZE_CRM`·`BRONZE_ERP`·`BRONZE_AGENCY`)에 **`BRONZE_GA4` 배선 0건**(29행은 주석)
>      ⇒ `GA_` 를 정당화할 계통이 파이프라인에 없다. 라이브 `BRONZE_GA4` 2테이블은 미연결.
>    - 개명 = `GA_SESSION_ID/_NUMBER/_KEY` → `BIGQUERY_SESSION_*`(`BIGQUERY_BASIC`·`BIGQUERY_EVENT`) ·
>      `GA_MEMBER_ID` → `BIGQUERY_MEMBER_ID`(`BIGQUERY_IDENTITY`·`IDENTITY_MEMBER_XREF`).
>    - 반영 = dbt 모델 6개 + DDL 2개(08·09) + 생성기 3개 + 가드 매크로 목록.
>      🔴🔴 **최대 위험 = `EP_GA_SESSION_ID`·`EP_GA_SESSION_NUMBER`** 는
>      `BIGQUERY_REFINED_DATA` 의 **외부 Python 적재 원천 컬럼**(개명표 §1 `EXTERNAL_PYTHON`)인데
>      개명 대상을 **부분문자열로 포함**한다 ⇒ negative lookbehind `(?<!EP_)` 로 보호하고
>      치환 후 **EP_ 개수 불변**을 단정했다(도구 = `scripts/o163_dec50_rename.py`).
>    - 🟢 **유지 판정 2축**: `AGENCY_AD_ROW_DGT.GA_AD_COST`·`GA_CONV_MBER_CNT` 는
>      `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS` 에 **원천 실재**(P규약 · `DEC-51` 이 GOLD 노출을
>      `AGENCY_CONV_*` 로 이미 분리) · `GAC_*` 는 Google Ads Campaign 축(별개 · 백로그).
>    - 🟢 역사 기록은 **원문 보존**했다(08 DDL 1271·1293~1295) — 08 DDL 1343~1351 에
>      「개명 누락으로 읽지 마라」 배너를 남겼다(`R2-8-3`).
>
> 5. 🔴🔴 **도구 자기결함 2건을 스스로 적발했다 — 둘 다 데이터 손실로 이어질 수 있었다.**
>    - ㉠ **주석 CREATE 오인**: 초판 `CREATE_RE` 가 `-- CREATE OR REPLACE TABLE …` 까지 선언으로
>      세어 `SILVER.BIGQUERY_REFINED_DATA`(08 DDL 1283행 **커밋아웃**)를 복원 대상에 넣었다.
>      🔴 그대로 실행하면 **외부 Python 적재 11,600,680행이 영구 소실**됐다(dbt 가 재적재하지 않는다).
>      ⇒ 판정식 = CREATE 매칭이 속한 줄 선두가 `--` 면 선언이 아니다. 시정 후 DDL 선언 85→**84** ·
>      드리프트 37→**36** · 그 테이블은 `LIVE_ONLY_TABLE` 로 정확히 재분류됐다.
>    - ㉡ **SQL 키워드 오탐**: `FACT_MEMBER_SPONSORSHIP_SPAN` 에서 `BEGIN`·`CASE`·`AND` 를
>      컬럼으로 잡아 없는 드리프트를 만들었다(DDL 본문 CASE 식의 들여쓰기가 컬럼 정의와 같다).
>      `NOT_COL` 에 키워드 보강 ⇒ DDL_ONLY 28→**25**.
>
> 6. 🟢 **라이브 복원 집행(사용자 승인 · `R4-4-3`)** — DDL 파일 전수 실행.
>    - 선행 실측으로 3대 우려를 해소했다: **GRANT** = FUTURE GRANTS(SILVER 13 + GOLD 41 ·
>      ADMIN·ANALYST·ENGINEER·SERVICE·VIEWER)로 자동 복구 · **FK** = DDL 이 별도
>      `ALTER … FOREIGN KEY … NOT ENFORCED` 로 부여 · **데이터** = 대상 36개 전부 dbt 모델 실재(부재 0).
>      ⇒ 개명표 §5 의 「`CREATE OR REPLACE` 금지」는 **개명 맥락**의 지침이고, 낡은 세대를 정본으로
>      복원하는 이 작업에는 위 3축이 정비돼 있어 성립한다.
>    - 실행 경로 = `snow sql -f`. 🔴 `EXECUTE IMMEDIATE FROM` 은 스크립트 안의 `USE ROLE` 을
>      거부하고(65행 `unexpected 'USE'`), `snow sql -f` 는 **선행 블록주석의 세미콜론**에서 깨져
>      첫 문장이 `Empty SQL statement` 가 된다(3~64행 `/*…*/` 중첩) ⇒ 주석 블록을 제외한
>      실행본(`tmp/_o163_exec_silver.sql`·`_gold.sql`)을 만들어 실행했다.
>    - 🟢 **O142 사고 회피**: 파일 내 `USE ROLE GN_DW_ADMIN` 으로 실행해 **OWNERSHIP =
>      `GN_DW_ADMIN`** 을 확보했다(O142 는 `SV_AD` 를 `ACCOUNTADMIN` 으로 만들어 소비 3역할이
>      읽지 못했다 · `01_세션이력-059.md:257`).
>    - 실측 결과 = **드리프트 36 → 0건** · **GOLD FK 20 → 56건** · SILVER 48 · GOLD 37 ·
>      `BIGQUERY_REFINED_DATA` **11,600,680행 보존** · `FACT_MEMBER_FEE` GRANT 26건 자동 복구 ·
>      `table_ddl_column_gate` **84/84 집합 일치**(blocking 0 · 순서 드리프트 0).
>
> 7. 🔴 **자기결함(순서 오류) — 산출물 4종을 데이터 미적재 상태에서 재생성해 덮었다.**
>    - 라이브 복원 직후(dbt build **전**) `census_columns` + 생성기 4종을 돌렸는데,
>      생성기가 라이브 **데이터**를 읽어 판정하므로 결과가 무의미해졌다:
>      `04.rows` 563→246 · `05.WAIT` 27→89 · `09.조립가능` 267→**12** · `09.값없음` 52→**341**.
>    - 🟢 **골든은 갱신하지 않았다** — 빈 상태를 기준선으로 만들면 그 골든이 거짓이 된다
>      (`R1-7-4` 「FAIL 을 `--update-golden` 으로 덮지 마라」).
>    - 🔴 `_archive` 에 그 4종 스냅샷이 없어 되돌릴 수 없다 ⇒ **복구 경로 = dbt build 후 재생성**
>      이고 이를 인수인계 필수 항목으로 올렸다. 교훈 = **산출물 재생성은 적재 이후다.**
>
> 8. 🟢 **게이트·테스트**: 게이트 6종 전건 rc=0 · `table_ddl_column_gate` 84/84 ·
>    음성 테스트 33종 중 실패 3 → `test_gate_census` 는 신설 도구 2건 등재로 **11/11 해소**
>    (`o163_ddl_live_drift` = `NEEDS_ARGS` · `o163_dec50_rename` = `MUTATES`) ·
>    `test_generators` 는 위 7항 사유로 **의도적 미해소** · `test_verify_wide_doc` 는
>    인수인계 §0-JJJJ ▣JJJJ2 에 이미 등재된 **선행결함**(라이브 뷰 부재).
>
> - **read 미반환**: 0건 · 재호출 0건.
> - **`R4-4-2 ㉡` 적용**(지시 동봉) · 확정위반 = 7항 1건(산출물 재생성 순서).
