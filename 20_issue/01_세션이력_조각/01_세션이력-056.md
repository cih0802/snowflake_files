<!-- SPLIT-CHUNK 01_세션이력.md | 056/056 | 허브 = 01_세션이력.md | 원문 1~175행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-31 O126] GOLD ERD 생성기 신설 + 자기검토 — ERD 가 fan-out 차단 차원을 고립 섬으로 그렸다
>
> **요청** = 「dbt 기반으로 GOLD 스키마 테이블별 ERD 문서를 만들고, 파이프라인 수정 시 재사용 가능하게」.
> 이어서 **「이슈문서를 읽고 이번 세션 작업을 비판적으로 검토하고 개선하라 · 승인 필요 작업 전부 승인」**.
>
> ---
>
> **① 🔴 착수 절차 위반 — 브리핑 없이 시작했다 (`A5` 확정위반 · `R4-4-1`)**
>
> · 초판 작업은 `init_ihcho` 미실행 · `00_BRIEF.md` 미독 · 라벨 선점 없음으로 진행됐다.
> · 검토 지시를 받은 뒤에야 지침을 로드하고 `O126` 을 선점했다(`id_collision_gate --next O` = 126).
> · 🔴 **이 위반이 ②의 원인과 무관하지 않다** — 인수인계 `§0-CCCC` `CCCC1` 이
>   「**「객체가 다 있다」와 「설계대로 작동한다」는 다른 판정이다**」를 말하고 있었고,
>   초판이 정확히 그 함정에 빠졌다. **브리핑을 읽었으면 피할 수 있었다.**
> · 🟠 `R4-4-4` 롤백 근거 기록: 이 세션은 `R4-4-2 ㉡`(지시 동봉 즉시 수행) 분기로 진행했고
>   **확정위반 4건**(브리핑 미출력 · 라벨 선점 누락 · 사후 검증 2종 · `edit` 앵커 사고)이 났다.
>
> ---
>
> **② 🔴🔴 초판 최악 결함 — DIM_MONTH 를 관계선 0개의 고립 섬으로 그렸다**
>
> · **초판 설계** = dbt `_gold_ready_schema.yml` 의 `relationships` 테스트(45) +
>   물리 FK `SHOW IMPORTED KEYS`(56) 를 **합집합**(63)해 ERD 를 그렸다.
>   두 소스가 서로를 포함하지 않는다는 것까지는 실측으로 맞게 잡았다
>   (YAML만 7 = `MEMBER_DK → DIM_MEMBER` SCD2 비유일 / 물리만 18 = yml 테스트 미작성).
> · 🔴🔴 **그러나 합집합도 전량이 아니었다.** 실측 결과 **월 conform 축 7건이
>   dbt relationships 에도, 물리 FK 에도 없다**:
>   `FACT_BUDGET·FACT_MEMBER_FEE·FACT_MEMBER_MONTHLY·FACT_TARGET_BIZ·FACT_TARGET_DEV.MONTH_KEY`
>   + `FACT_MEMBER_SPONSOR_BIZ.START_MONTH_KEY·DSCNTC_MONTH_KEY`.
>   원인 = `06_DDL.sql` [관계 제약] 이 월 conform 키를 **비유일 참조로 보아 선언 보류**했고
>   yml 에도 테스트가 없다 ⇒ **두 소스 어디에도 흔적이 없다.**
> · ⚠️ **왜 이것이 이 스키마에서 가능한 최악의 오도인가** — `DIM_MONTH` 는 **fan-out 차단 차원**이다.
>   `DIM_MONTH.MONTH_KEY` COMMENT 가 🔴🔴 로 「월 팩트는 DIM_DATE 를 직접 조인하지 말고 이 차원을 쓴다 —
>   DIM_DATE 는 일 grain 이라 월팩트와 조인하면 월당 일수만큼 행이 증폭되고 금액·건수가 그 배수로
>   과대해진다」고 경고한다. 그런데 **초판 ERD 는 DIM_MONTH 를 아무것과도 연결하지 않은 채
>   `FACT_MEMBER_FEE → DIM_DATE`(`LAST_PAY_DATE_SK` 등)만 그렸다** ⇒ 독자가 ERD 를 보고
>   **설계가 막으려던 바로 그 조인을 하도록 유도**했다.
> · 🔴 **실측 근거**(초판 산출물 파싱): `DIM_MONTH` 섹션 역참조 팩트 **0** · 섹션 관계선 **0** ·
>   전체 ERD 의 `DIM_MONTH` 관계선 **0**.
> · 🔴 **판정식 = 「Mermaid 문법 38/38 유효」와 「관계가 설계를 옳게 전달한다」는 다른 판정이다.**
>   초판은 문법 검사를 통과했고 FK 병합 산술도 맞았다. **틀린 것은 「무엇을 세는가」였다.**
>   이것이 `R3-9`(게이트가 보지 않는 축)의 실물이고 `CCCC1` 의 재현이다.
>
> ---
>
> **③ 🟢 시정 — 3차 소스(논리 관계) 도입**
>
> · `scripts/gold_erd_coverage_gate.py` **신설**. 판정식 = FACT 의 키 형태 컬럼(`_SK`/`_KEY`/`_DK`)
>   중 병합 FK·물리 PK 어디에도 없는 것 = **「고립」**이고, **고립 1건마다 분류·사유를 등재해야 통과**한다.
>   실측 = FACT 키 컬럼 **71** · 병합 FK 커버 **64** · 고립 **7** · 미분류 **0**.
> · 🔴 **「고립 0」을 요구하지 않는다** — degenerate key 는 관계가 없는 것이 설계다.
>   요구하는 것은 **「모르는 고립 0」**이다. 분류 = `DEGEN`(관계 없음이 설계) /
>   `CONFORM`(FK 선언 불가 · **ERD 에 논리 관계로 표기해야 한다**) / `SELFREF`.
> · `gen_gold_erd.py` 가 게이트를 `import` 해 `LOGICAL_FK` 를 읽어 **3차 소스로 병합**한다.
>   결과 = 선언 FK **63** + 논리 **7** = **총 관계 70**.
> · 표기 = Mermaid **점선**(`||..o{`) + `logical` 배지. 🔴 문서에 명시했다 —
>   **「점선은 약한 관계가 아니라 FK 로 선언되어 있지 않다는 뜻이고, 조인 경로로는 실선과 동등하게
>   유효하다 — 월 팩트는 반드시 DIM_MONTH 를 쓴다」.**
> · 🔴 **미분류 고립이 있으면 생성 자체를 `return 1` 로 차단**한다(경고로 흘리지 않는다).
>   경고였다면 초판과 같은 산출물이 계속 나온다.
> · 🔴 **규칙은 게이트 1곳에만 둔다**(`R3-9 ㉡` 이중정의 금지) — 생성기에 `LOGICAL_FK`·`KNOWN_ORPHANS`
>   정의가 없음을 음성 테스트 축5 가 단정한다.
>
> ---
>
> **④ 🔴 초판 `--yaml-only` 가 불완전 산출물을 전량판 경로에 덮었다**
>
> · 초판은 두 모드가 **같은 `OUT_HTML`** 에 썼다 ⇒ 비용 절감 모드를 한 번 돌리면
>   전량판이 **경고 없이 사라진다**. 이 워크스페이스가 4번 당한 **「조용한 소실」** 유형이다.
> · 🟢 시정 = `OUT_HTML_YAML`(`GOLD_ERD_테이블별_YAML전용.html`) 로 **경로 분리** ·
>   그 판본 상단에 **빠진 것 3종을 열거한 🔴🔴 경고 블록**(특히 「DIM_MONTH 가 고립 섬으로 그려진다」).
> · 실측 = 전량판 점선 **21**(전체+테이블별 합산) vs YAML전용판 점선 **0** ⇒ 경고가 사실과 일치한다.
>
> ---
>
> **⑤ 🔴 정본 stale 3곳 적발·시정 — `03_top-down_gold/06_DDL.sql` 헤더**
>
> | 위치 | 종전 기재 | 실측 |
> |---|---|---|
> | 표제(5행) | 「35개: DIM 20 + FACT 15」 | **파일 선언 37 = DIM 20 + FACT 17** = 라이브 BASE TABLE 37 |
> | 실측대조(13행) | 「선언 FOREIGN KEY 50 = 라이브 50」 | **파일 실행 FK 56 = 라이브 56** |
> | 실행규칙 1(48행) | 「DIM 20개를 … FACT 15개를 생성한다 … O53 로 20/15」 | **FACT 17** |
> | 1행 요약 | 「전체 DDL(35개 테이블)」 | 동일 사유 |
>
> · 🟢 **파일 본문은 정상이었다 — 틀린 것은 헤더 산문뿐이다.** 그러나 헤더는 인수인계에서 먼저 읽히므로
>   본문 신뢰도와 무관하게 오도한다.
> · 🟢 **시정 방식 = 수를 지우고 「재는 방법」으로 대체**(`R3-9 ㉦` 「내가 만든 수를 문서에 박아 넣지 마라」).
>   `doc_census.py` 의 분모는 `20_issue/` 문서군이라 **`03_top-down_gold/` 는 감시 밖**이었다
>   ⇒ 워크스페이스가 이미 확립한 원칙을 **게이트가 보지 않는 파일에 일반화**했다.
> · 🔴 **함정 1건 함께 명문화**: `grep -c 'FOREIGN KEY'` 는 헤더 산문·주석·집계 SQL 을 세어 **60** 을 낸다.
>   실행 FK 는 `grep -cE '^\s+FOREIGN KEY'` = **56**. 차이 4건이 전부 비실행 줄이었다
>   ⇒ **판정식이 세는 것과 사람이 읽는 뜻이 다른 지점**(`O111 ㉢`).
>
> ---
>
> **⑥ 🟢 음성 테스트 2종 신설 (`R3-2` · `R3-9 ⑥`)**
>
> · `scripts/test_gold_erd.py` — **오염 기반**. 논리 관계를 뺀 픽스처로 **초판 결함(고립)을 재현**하고,
>   병합 후 점선이 생기는지를 **양축 단정**한다. 축 = rel_op 출처 분기 · 오염 재현 · 복구 ·
>   blocking 여부 · 규칙 단일 정본 · 출력 경로 분리 · CONFORM↔LOGICAL_FK 대응 · Mermaid 타입 정규화 ·
>   **산출물 회귀 가드**. 🔴 축·단정 수는 테스트가 스스로 세어 출력한다(문서에 적지 않는다).
> · `scripts/test_gold_erd_mermaid.py` — 문법 축 **분리**. 🔴 **일부러 나눴다** —
>   문법 유효는 의미 정합을 보증하지 않는다는 것이 이 세션의 교훈이다(`R3-9 ㉡` 「같은 것을 다르게 재라」).
> · 종료코드는 **리다이렉트로 받았다**(`>/tmp/…out; echo rc=$?`) — 파이프 뒤 `$?` 금지(`O120`).
>
> ---
>
> **⑦ 🔴 `edit` 앵커 사고 1건 (`R1-7-8`) — 즉시 복구**
>
> · `rel_op` 를 추가하며 앵커를 `def eff_pk(info):` 한 줄로 잡아 **그 정의줄을 덮었다**
>   (docstring·return 2줄이 고아가 됐다). 다음 편집에서 즉시 복구하고 `py_compile` 로 확인했다.
> · 🔴 **조문이 말한 그대로다** — 「앵커는 직전 줄까지 포함하라」. 짧은 앵커가 실제로 정의줄을 지운다.
>
> ---
>
> **⑧ 비용 판정 (사용자 원 질문에 대한 답)**
>
> | 소스 | 컴퓨트 | FK 커버리지 |
> |---|---|---|
> | dbt YAML 파싱 | **$0** | 45 / 70 — 🔴 불완전 |
> | + 물리 FK·컬럼 메타 | XS ~2초 | 63 / 70 — 🔴 여전히 불완전 |
> | + 논리 관계(사람 판정) | 동일 | **70 / 70** |
>
> · 🟢 **「dbt 기반이 가장 저렴하다」는 맞지만, 저렴한 소스만으로는 ERD 가 틀린다.**
>   ⇒ 판정식 = **「비용이 얼마인가」가 아니라 「어느 축이 빠지는가」**다.
> · 🟢 GOLD 기반 선택은 타당하다(스타스키마라 FK 가 명시적 · SILVER 는 SQL 파싱이 추가로 필요).
> · 재사용 절차(산출물 상단에 명시) = ① 커버리지 게이트 → ② 생성기 → ③ 음성 테스트.
>   🔴 **① 을 먼저 통과시켜야 한다** — 새 키 컬럼이 생기면 FAIL 로 사람 판정을 부른다.
>
> ---
>
> **⑨ 검증 실행 결과 (전부 종료코드 기준)**
>
> · `gold_erd_coverage_gate.py` rc=0 — 고립 7 · 미분류 **0**
> · `gen_gold_erd.py` rc=0 — 관계 **70**(선언 63 + 논리 7) · BASE TABLE 행 146,457,668
> · `gen_gold_erd.py --yaml-only` rc=0 — 별도 경로 · 점선 0(경고와 일치)
> · `test_gold_erd.py` rc=0 — 단정 **25 / 통과 25 / 실패 0**
> · `test_gold_erd_mermaid.py` rc=0 — 전량판 블록 38 · 실선 204 · 점선 21 · 위반 **0**
> · `line_len.py` rc=0(신규·수정 6파일) · `py_compile` OK · `index_row_gate` rc=0 ·
>   `doc_heading_gate` rc=0 · `doc_type_gate` rc=0
> · `cortex ws ls` — 신규 4스크립트 + 산출물 2건 스테이지 실재 확인(`R2-5`).
>   ⚠️ `ws ls` 는 **FQN 필수**다(`USER$.PUBLIC."snowflake_files":/scripts/`) — 상대경로는 rc=1.
> · `read` 미반환 **0건** · 재호출 **0건**(`R1-3-7-a`).
>
> **독해 좌표·고유 토큰**(`R1-3-7-b`)
> · `00_guides/00_작업지침_세션운영규칙.md` 1~316 전량 · 도착 O ·
>   토큰 = 「R0-7 워크스페이스 파서 제약」·「R2-8-4-d 역방향도 성립한다」·「R4-4-4 🟠 롤백 조건」
> · `20_issue/00_BRIEF.md` 1~143 전량 · 도착 O ·
>   토큰 = 「0-CCCC」·「열린 착수표 13」·「전 문서군 = 2,311,242 자」
> · `03_top-down_gold/06_DDL.sql` 1~100 + 헤더 구간 재독 · 도착 O ·
>   토큰 = 「선언 FOREIGN KEY 50 = 라이브 50」·「SK=0 '(미매핑)'」·「부분 실행 규칙(O53)」
> · `10_dbt_pipeline/models/gold/_gold_ready_schema.yml` 917줄 · 🔴 **1회 미반환**(Output too large)
>   → 캐시 경로 분할 재독 2회로 전량 확보 · 토큰 = 「FACT_TARGET_BIZ」·「EVENT_BK || '|' || MEMBER_DK」
>   ⇒ 판정식 충족(재호출 2 ≥ 미반환 1).
>
> ---
>
> **⑩ 🟠 잔여 · 다음 세션이 판정할 것**
>
> · **㉞ `_gold_ready_schema.yml` 에 `relationships` 테스트 18건 미작성** — 물리 FK 에만 있는 축이다.
>   추가하면 dbt 가 참조 정합을 실제로 검증한다(현재는 ERD 표기만 되고 **검증은 안 된다**).
>   🔴 추가 전에 **위반 행 수를 먼저 실측**하라 — `FACT_TARGET_BIZ` 는 0행이라 공허하게 통과하고,
>   이름 기반 조인 축(`ORG`)은 미매칭이 예상된다(문서50 §O116 WARN 계열과 같은 성격).
> · **㉟ FACT 11개가 물리 PK 미선언** — DIM 20/20 은 선언됐고 FACT 는 6/17 뿐이다.
>   `06_DDL.sql` 실행규칙 8 「FACT PK/UNIQUE 는 grain 미확정으로 보류」와 정합하므로 **결함이 아니다**.
>   🔴 다만 ERD 는 물리 PK 부재 시 **dbt 추론 PK(unique+not_null)로 대체 표기**한다 ⇒
>   그 표기가 「grain 확정」으로 읽힐 위험을 다음 세션이 판정하라(현재는 `pk_note` 로 불일치만 경고한다).
> · **㊱ 산출물이 Mermaid CDN 에 의존한다** — 오프라인에서는 렌더되지 않는다.
>   🟢 폴백을 넣어 **다이어그램 소스가 텍스트로 읽히게** 했고 실패 알림도 표시한다.
>   🔴 그러나 `gen_arch_map.py` 가 확립한 「인라인 · 외부 fetch 없음」 규약에는 **미달**이다 ⇒
>   현업 배포 전에 mermaid 를 vendoring 할지 판정하라.
> · **㊲ 커버리지 게이트가 FACT 만 본다** — DIM → DIM 참조(`DIM_CAMPAIGN.ORG_SK`·`MKTG_CAMPAIGN_SK`)는
>   물리 FK 로 잡혀 ERD 에 있으나, **고립 판정 분모에는 없다** ⇒ DIM 측 고립은 검사되지 않는다.
>   🔴 **「0건」이 「없다」가 아니라 「내 판정식이 못 본다」일 수 있다**(`O111 ㉠`)의 실물 후보다.
>
> **⑪ 🔴🔴 [2026-08-31 O128 소급 등재] O126 이 코드에 넣고 정본에 등재하지 않은 개정 2건 + 자기신고 2건**
>
> 🔴 **왜 소급 등재인가** — O126 은 아래를 **코드에만** 넣고 원장·이력에 한 줄도 적지 않았다. O128 착수
> 실측 = `grep -c _rolling 20_issue/00_INDEX_이슈원장_조각/*.md` = **11조각 전건 0** · `grep -rln _rolling
> 20_issue/01_세션이력_조각/` = **0건** · 반면 코드에는 실재(`gen_gold_erd.py`·`snapshot_util.py`·
> `test_gold_erd.py`) ⇒ **정본이 코드보다 낡았다.** 🔴 판정식 = **「코드를 고쳤다」와 「정본에 등재했다」는 다른 판정이다**(`R3-9 ㉠` 의 코드판).
> · **개정 ② = 생성 시각을 제외한 내용 비교로 「실질 변경 시에만」 회전한다.** 근거 = HTML 이 자기 생성
>   시각을 본문에 박으므로 `snapshot_util` 의 **「바이트 동일 재사용」 멱등이 이 산출물에 원리적으로
>   듣지 않았다** ⇒ 돌릴 때마다 새 판본이 쌓였다. 실측 = 검증용 3판본의 차이가 **생성 시각 한 줄뿐**
>   이었다(diff 다른 줄 2 = 그 줄의 -/+). 🟢 구현 = `VOLATILE_RE`(`생성 YYYY-MM-DD HH:MM` 만 지운다) +
>   `normalize_for_compare()` · 같으면 `rotate_previous()` 가 `("unchanged", None, date, axis)` 를 돌려준다.
>   🔴 **계정·DIM/FACT 수는 지우지 않는다** — 넓게 지우면 진짜 변경을 놓친다(`P130` 의 반대 실수).
>   🔴 **새 HTML 은 그래도 항상 쓴다** — 생성 시각이 갱신되어야 산출물 상단 자기표기로 「방금 생성됐나」를 판정할 수 있다 ⇒ 판정식 = **「돌린 횟수」가 아니라 「실제로 바뀐 횟수」만큼 쌓인다.**
> · **개정 ① = 롤링 백업을 `30_output_share/_archive/_rolling/` 으로 격리했다**(`ARCHIVE_DIR`). `_archive/`
>   최상위의 **날짜 폴더**(수동 이관 · 특정일 배포 세트 전체 보존)와 **층을 분리**했다 — 날짜 폴더 =
>   **릴리스 태그(영구)** · 롤링 = **undo 버퍼(임시)** 로 **다른 질문에 답한다** ⇒ 같은 층에 섞으면 「어느
>   것이 이력의 정본이냐」가 모호해진다(`R3-9 ㉡`). 층을 나누면 중복이 아니다.
> · **자기신고 A = 음성 테스트가 실 `_archive/_rolling/` 을 오염시켰다**(`OUT_20260830.html`). 원인 =
>   `rotate_previous()` 에 **`archive` 주입 지점이 없어** 테스트가 임시 디렉터리를 만들어 놓고도 보관본이
>   모듈 상수 경로로 갔다. 🔴🔴 **테스트는 전건 통과했고 오염은 판정에 나타나지 않았다** ⇒ 🟢 처방 =
>   인자로 받게 고치고 **축12-g**(보관본이 임시 경로 안에 있다) · **축13-d**(실 `_rolling/` 에 테스트 잔재
>   `OUT_` 이 없다)를 신설해 **기계로 검출**하게 했다. 🟢 `snapshot_util` docstring 이 이미 「`archive=` 를
>   넘겨라」로 경고하고 있었다 ⇒ 🔴 판정식 = **경고를 읽는 지점과 경고를 받아야 할 함수가 다르면 그
>   경고는 듣지 않는다.**
> · **자기신고 B = `edit` 앵커가 짧아 `test_gold_erd.py` 축11 을 지웠다**(`R1-7-8` 재발 · 2회). 축12·축13 을
>   삽입할 때 앵커가 접두뿐이어서 축11 블록이 덮였다 ⇒ 복구 후 `test_gold_erd.py:217` 주석에 남겼다.
>   🔴 **`R1-7-8` 은 표 행뿐 아니라 코드 블록에도 적용된다.**
> · **신설 = `snapshot_util.dated_snapshot_name()` + `snapshot(name=…, keep_ext=…)`.** 이유 = 기본
>   `snapshot_name()` 은 `foo.html.O126-prerun` 을 만들어 **`.html` 이 뒤로 밀려 브라우저가 열지 못한다**.
>   문서 되돌리기는 무해하지만 **배포 산출물의 과거 판본은 열려야 값이 있다** ⇒ `foo_20260830.html` ·
>   충돌 접미도 확장자 앞(`foo_20260830_2.html`). 🔴 **두 이름 규칙 모두 `snapshot_util` 안에 둔다** —
>   호출자가 경로를 조립하면 `R1-7-10` 우회다. 🟢 **기본값 불변**(`name=None` · `keep_ext=False`) ⇒ 기존
>   호출자 동작 불변 · 🟢 `test_snapshot_util.py` 축7(도구별 헬퍼 경유) 분모에 `gen_gold_erd.py` 편입.

> #### 🟠 [2026-08-31 O127 · O127-B] 미해결 이슈 순차 종결 3건(`DEC-44`·`DEC-46 ㉛`·`DEC-47 ⑫`) + 자기검토 3결함 시정
>
> **지시** = 「미해결 이슈를 기존 가이드·해결방침(dec 등)을 고려해, 권장사항과 이전 유사결정을 보여주며
> 사용자 응답으로 하나하나 해결하자」 → 후속 지시 = 「비판적 검토 · 승인 전건 · 문서와 작업을 개선」.
> 🔴 **`R4-4-2 ㉡` 적용**(지시 동봉 ⇒ 브리핑 후 즉시 수행) · 확정위반 자기신고는 아래 ⑦.
>
> **① 🔴 라벨 경합이 실현됐다 — 게이트의 「다음 발급」을 믿을 수 없었다.**
> `id_collision_gate --next O` = 「정의 125 · 참조 125 ⇒ O126」이었으나 **원장 §1 에 `O126` 행이 실재**했고
> 그 행 자신이 **「라벨 선점 누락」을 자기신고**하고 있었다(`R1-4-3` 이 경고한 경합의 실물).
> ⇒ `O127` 전수 grep **0건**을 확인하고 **원장 §1 에 선점 등재 후** 착수했다.
>
> **② 🟢 `DEC-44` 종결(사용자 확정 3건) — 문서30 §30-H(처방) · §30-I(집행 8단계) 신설.**
> · 회계 의미 = **「재작성」**(추경이 그 해 최종액 ⇒ 차수 합산은 이중계상). 근거는 독립 두 축 =
>   §30-F ②(금액 다중집합 상이 0) + §30-G ㉠(항목×연도 24키 중 집행 상이 0).
>   🔴 **현업 문서로 확인된 것이 아니라 실측으로 확정**했음을 §30-H ① 에 명시했다.
> · 배선 = **㉡ 차수를 팩트 컬럼**(`BUDGET_ITEM_DK` MD5 10컬럼 산식 **불변** ⇒ SK 안정) ·
>   ㉠ 기각(SK 전면 변경) · ㉣ 기각(수치가 틀린 채 잔존) · ㉢ 는 미래 확장 여지로만 남겼다.
> · 행 선택 = **「연도별 최신 차수만」이 기본**. 🟢 선례 = `DEC-24`(대표 미납사유 = **최종 차수의 사유** ·
>   「마지막 시도가 최종 상태를 설명한다」) · 🟢 반대 선례 = `DEC-41`(실측이 지지하지 않으면 대표를
>   만들지 않는다) — DEC-44 는 **차수 간 집행 완전 동일**이라는 직접 실측이 있어 그 축에 걸리지 않는다.
> · 🔴 **배선·`severity` 승격·SV 재배포는 미실행** ⇒ `SV_BUDGET` 과대 발행은 **존속**한다(§30-G ㉡).
> · 🟢 §30-E 종전 종결 조건(「30-B 회신 확보」)은 **승계 표기**로 닫았다 — 두 절이 서로 다른 종결
>   조건을 말하는 **이중정의**(`R3-9 ㉡`)를 자기검토에서 적발해 시정한 것이다.
>
> **③ 🟢 `㉛`(`DEC-46`) 종결 — 문서30 §32-G 신설 + 개명표 `0-A ㉤㉥` 등재.**
> · ㉠ 판정식 정밀화 **채택** = L 조건을 「`union all` 이 있다」 → **「그 `union all` 로 원천명이
>   유일하지 않게 된다」**로 좁혔다. 🟢 근거 = **코드베이스 자신의 관례**(`CRM_MEMBER` 4컬럼이 이미
>   그 처리를 받고 원천명 유지) ⇒ `0-A ㉡` 와 같은 형태. 🔴 **개명 확정 12건은 불변**이다.
> · ㉡ `CRM_MEMBER.JOIN_DT` = 🟠 **보류 · 현업 질의 승격**(문서20 **§N-10** 신설) — 규약으로는 P 지만
>   「최초등록일 = 가입일」이 업무상 같다는 확인이 없다.
> · ㉢ `EFFECTIVE_FROM` = ✅ **L 유지** + **쌍 규약 예외 조문 신설**(짝의 한쪽이 L 이면 쌍 전체를 L).
>   🔴 이 예외는 분류 X(변별 토큰)와 **다른 축**임을 명시했다(합치면 판정이 흐려진다).
>
> **④ 🟢 `⑫` 부분 확정 = `DEC-47` 신설(문서30 §33) · 라벨은 게이트 실측(정의 36 · 참조 46 ⇒ 47) + grep 0건.**
> · ㉠ **「활동(건) = 미중단 약정금액 ÷ 10,000」 확정** — 🟢 근거 = `CONF-2` 규약의 재사용
>   (정본 공#38·#151 이 이미 금액÷10,000) · 🔴 「행수로 세지 마라」를 처방에 못박았다.
> · ㉡ `CONF-3` 판정 방향(중단 우세 ↔ 재후원 우세) = 🟠 **문서20 §N-11 로 승격** ·
>   🔴 **「후원 단위 플래그 우회」를 에이전트가 창작하지 않았다**(`DEC-41` 축).
> · ㉢ 센티넬·이상치 = **`R2-7` 그대로**(시작일 93·중단일 2 → NULL + **컬럼별** COMMENT · 음수 금액은
>   `severity: warn` 관측 축으로 분리). 🔴 O72-C 의 **차 2 를 필터로 0 으로 만들지 마라**를 명문화했다.
> · 🔴 **배선 금지는 유지**된다(㉡ 회신 전) ⇒ 착수표 `⑫` 는 열린 채 부분 확정만 병기했다.
>
> **⑤ 🔴🔴 [O127-B 자기검토 최대 결함] 채택된 규칙과 다른 축의 값을 회귀 목표로 옮겨 적었다.**
> §30-I 4 초판에 `PLAN` 기대값을 **62,100,245,921** 로 적었는데 그것은 §30-F ③ 의 **「연사업만」** 열이고,
> 사용자가 채택한 규칙은 **연도별 최신 차수**다. §30-F ① 표로 검산 =
> `PLAN` **65,202,608,326**(2024 추경 20,494,617,158 + 2025 추경 23,490,801,247 + 2026 연사업 21,217,189,921) ·
> `EXEC` **55,094,546,653**(두 규칙에서 **동일** — 집행액이 차수 간 완전 중복이므로).
> ⇒ 🔴🔴 **판정식 신설 = 두 규칙을 가르는 축이 아닌 지표로 회귀 검증하지 마라.** `EXEC` 만 맞은 것은
> 「규칙이 옳게 배선됐다」의 증거가 아니고, 이 결정에서 **가르는 축은 `PLAN` 하나뿐**이다.
> 🟢 시정 = §30-I 4(취소선 + 산식 병기) · §30-H ③ 병기 · §30-E 포인터 · 착수표 `㊴` 에 기대값 명시.
> ⚠️ 함께 = **`EXEC_RATE` 분모도 바뀐다**(현행 집행율과 직접 비교 금지).
>
> **⑥ 🔴🔴 [O127-B] 허위 게이트 보고 1건 정정 — 착수 브리핑의 `gate_census` rc=1 은 실재하지 않았다.**
> 착수 시 「미분류 1건 = `gold_erd_coverage_gate`」로 보고했으나, 그 등재는 **파일에 이미 있었다**
> (`gate_census.py` 66~67행 · 두 줄 묵시 연결). 재실측 = **rc=0 · 미분류 0 · 음성 테스트 25종 전건 rc=0**.
> 원인은 **낡은 마운트 뷰**(`OPS-3`)이고, 같은 세션에서 **원장 조각 바이트 판독이 34,412 ↔ 34,247 로 갈리고**
> **`O126` 행 본문 자체가 판독 시점마다 달랐다**(항목 ⑧⑨⑩ 이 뒤늦게 나타났다).
> ⇒ 🔴 **판정식 = 착수 게이트 결과도 낡을 수 있다 — FAIL 을 사용자에게 발행하기 전에 1회 재실행하라**
> (`R1-7-3` 「손상 판정 전 재읽기」의 게이트 판정판).
>
> **⑦ 🔴 이 세션의 자기결함 · 확정위반**
> · 🔴 **원장 조각 꼬리 절단 1건** — 마지막 표 행이 UTF-8 중간에서 끊겼다(`line_len` 이 적발).
>   `R1-7-3` 대로 해시를 4회 대조해 안정화를 먼저 확인하고, **스냅샷의 4열 원문**으로 꼬리만 복원했다(중복 0).
> · 🔴 **표 행 파괴 1건**(`R1-7-8` 재발) — `㊳` 행의 **접두만 앵커로 써서** 행이 두 줄로 쪼개졌다.
>   즉시 자력 적발·복구(행 1건 · 열 수 불변) ⇒ 🔴 **긴 표 행은 접두 앵커를 쓰지 마라**를 다시 확인했다.
> · 🔴 **`99_NEXT-020` 조각이 40KB 상한을 3회 초과**했다(내 추가분 때문). 매번 **포인터화로 회피**하고
>   재균형·은퇴는 **실행하지 않았다**. 🔴🔴 **은퇴 dry-run 실측 = 이관 557 B / 절감 −655 B**(포인터가 더 크다)
>   ⇒ **승인이 있어도 실측이 반대면 멈춘다**(`R1-6-24` 경고의 실물).
> · 🟠 **잔여 위험 = 그 조각 여유 34 B** · `doc_type_gate` 는 `99_NEXT` 여유 경고(🟠)를 유지한다.
> · 🟢 **[적발] `O126` 행이 2,025자로 `R1-5-1` 상한 초과** 상태였다 ⇒ 이력 포인터로 축약(실측치는 §O126 ⑤).
>
> **⑧ 🟢 구조 결함 시정 = 「결정은 닫고 착수 경로는 안 남긴다」**
> 착수표에 후속 항목이 없으면 다음 세션은 문서30 을 열지 않는다 ⇒ **㊴**(`DEC-44` 배선 · 기대값·정지점 포함) ·
> **㊵**(현업 회신 2건 반영처와 반영 규칙) 신설. 🔴 `㊶`(게이트 FAIL) 는 **허위였으므로 신설 후 즉시 삭제**했다.
>
> **⑨ 🟢 검증 실측**(파이프 없이 종료코드 수신 · O120 판정식)
> `index_row_gate` · `doc_heading_gate` · `clause_order_gate` · `doc_census --strict` · `doc_coord_gate` ·
> `doc_type_gate` · `id_collision_gate` · `gate_census` · `gate_census --run-tests` = **전건 rc=0** ·
> `line_len` **PASS**(상한 초과 0) · `split_doc --republish --expect` **전건 PASS**(유실 0) ·
> 골든 발행 2회(`--reason` 기재 · 실유실 0 확인 후) · `cortex ws ls` 로 스테이지 반영 확인.
> `read` 미반환 **0건** · 재호출 **0건**.
>
> **⑩ 🔴 미실행(의도적)** = `DEC-44` 배선·`severity` 승격·SV 재배포 · 개명 12건 · as-of 배선 ·
> dbt 명령 전건(`R4-1` 정지점 — 명령문만 §30-I 7 에 제시) · 라이브 DDL/DML **0**.
>
> **⑪ 🟠 남은 미해결 이슈**(사용자 요청 목록 기준) = `⑭`(FME 귀속 규칙) · `DEC-45`(다중값 모델링) ·
> `②`(NL 스모크) · `④⑤⑦⑪⑰⑱` · `㉙` · `㉚` · `㉝` · **20_issue 일자 최신화** · `99_NEXT` 여유.
