<!-- SPLIT-CHUNK 01_세션이력.md | 026/026 | 허브 = 01_세션이력.md | 원문 1~39행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-18 O83-F] 스킬 동기화 + 임계값 근거 도출(C2 해소) + 목표 전수 재점검
> 사용자 지시 = **「스킬도 이번 지침에 맞게 수정 + 목표 전부 달성했는지 · 남은 작업 마저」**.
>
> ##### 🟢 A. `init_ihcho` 스킬 동기화 (stale 6건 시정)
> 스킬은 **세션 최초에 반드시 실행**되는데 이 세션의 변경을 하나도 반영하지 않아 **다음 세션을 오도**했다:
> ① 지침 실측 「300줄/24.6KB」 → **267줄/14,249자/26,083B** ② `R1-6` 이 `00_guides/01_문서분할_규약.md`
> 로 분리된 사실 누락 ③ 인덱스 「분할 금지·346줄」 → **허브+형제 조각 5** ④ 조각 수 전건 stale
> (`10`=14→**20** · `02`=3→**4** · `20`=5→**7** · `30`=5→**7** · `50`=9→**12** · `99_NEXT`=4→**6**)
> ⑤ 이력 「분할 금지·6,400줄」 → **허브(포인터) + 폴더 조각 25** 이고 `grep` 대상이 **폴더**로 바뀐 사실 누락
> ⑥ 유형 등재표·`doc_type_gate`·`--rebalance`/`--rollover`/`--republish`/`retire_rows` 전부 미기재.
> 추가 반영 = **`rm -rf` 파괴성**(`R1-7-7`) · **`--next O` 를 그대로 믿지 말 것**(`P222` 참조 축 과대 ·
> 같은 세션 후속은 접미 `-B`·`-C` 로 이을 것). 실측 = 스킬 **180줄 / 16,536B** · 줄길이 PASS.
> 🔴 교훈: **스킬은 지침의 사본이 아니라 「진입 경로」다.** 지침만 고치고 스킬을 두면 다음 세션이
> **틀린 좌표로 착수**한다 — 이 세션도 스킬의 stale 수치(300줄)를 그대로 인용했었다.
>
> ##### 🟢 B. C2 해소 — 임계값을 실측으로 역산했다
> 자기검토(O83-E)에서 *"`fill 0.7`·`MIN_FREE 20%`·`HEADER_BYTES 400` 이 전부 관례값이고
> 세션당 증가율로 역산한 값이 하나도 없다"* 를 약점으로 등재했다. 그것을 측정했다:
> · 분모 = 이력 항목(작업 단위) **61건** / 6일(08-10~08-18) · 인덱스 조각 총량 **124,698B**
>   ⇒ 단위당 평균 **2,044B**(분할 이전 내용이 섞여 **상한선 추정**).
> · 직접 관측 = 라벨 행 **1,244~1,374B** · §0 표 3행 **약 1,300B** ⇒ 단위당 **1,200~1,400B**.
> · 환산 = `fill 0.7`(여유 12,288B) → **8~10 단위 흡수** · `MIN_FREE 20%`(8,192B) → **약 6 단위** 남을 때 경고.
> 🟢 **두 값은 바꾸지 않았다 — 관례가 아니라 근거를 갖게 됐다.** 이것이 「임계값을 실측으로 정하라」의 이행이다.
> ⚠️ 한계 = 61건은 **6일치**이고 세션 밀도 균일성은 미확인. 은퇴 이관을 정기화하면 증가율이 바뀌므로 **재도출 필요**.
> ⚠️ `HEADER_BYTES 400` 은 여전히 관례값이다(머리말 실측 약 300B + 마진) — **미해소**.
>
> ##### 🟢 C. 목표 전수 재점검 (실측)
> ① `20_issue` **95파일 · 상한 초과 0** ② 인덱스 조각 5 · 이력 폴더 조각 25
> ③ 유형 등재 **갱신형 12 · append형 2 · 정적 2** = 16문서 전건 ④ 상시 독해 3문서 전건 여유 **14.8KB↑**
> ⑤ 신설 도구 5종 실재 ⑥ 게이트 6종 + 허브 9종 전수 PASS.
>
> ##### ⏸ 잔여 (실행 가능한 것은 없다 — 성격 구분)
> * 🔴 **원리적 한계**: ㉠(`edited out`)은 크기와 무관해 상한으로 막을 수 없다. `R1-3-7` 도착 확인만이 방법이다.
> * 🟠 **트레이드오프(의도적)**: 파일 52→95개(+83%) — 조각당 1회 반환을 사면서 전량 독해 호출 수를 냈다.
> * 🟠 **의도적 보류**: 인덱스 363행 결손(verbatim 부재) · 은퇴 후보 잔존(🔴 미해결 항목이라 대상 아님) ·
>   이력 비꼬리 조각 여유 2,735B(append형이라 꼬리만 쓰면 무해 · 편집 시 재균형).
> * 🟠 **미해소**: `HEADER_BYTES 400` 근거 · 임계값 분모 6일치 한계 ·
>   🔴 **O83-E C3(줄 수 축 완화)은 내 이해가 걸린 규칙 변경이므로 다음 세션 재검토 대상**으로 남긴다.

> #### 🔴 [2026-08-18 O83-G] 스킬 「생성」 축 공백 시정 — 업데이트는 됐고 생성은 안 됐다
> 사용자 질문 = **「스킬 실행 시 issue 문서 생성·업데이트도 이번 세션 규칙대로 되는가」**.
> ⇒ 실측 답 = **업데이트는 된다. 생성은 안 된다.** 구멍 4건을 확인하고 메웠다.
>
> ##### 🔴 구멍 4건 (전부 실측 확인)
> * **G1 「허브 편집 금지」 명문이 없었다 — 가장 위험한 축.** 허브는 `build_hub` 결과를
>   `write_text(src, …)` 로 **통째로 덮어쓴다**(코드 **3곳** = build·rebalance·rollover).
>   ⇒ 허브에 손으로 쓴 문장은 **경고 없이 사라진다.** 지침·스펙·스킬 어디에도 금지가 없어서
>   다음 세션이 허브에 내용을 쓰면 **조용한 소실**이 난다(이 워크스페이스가 4번 당한 유형).
>   🟢 3중 집행 = ① `build_hub` 가 **모든 허브 머리말에 자기경고**를 넣는다(9허브 재발행 전파 완료)
>   ② 스펙 `R1-6-18` 조문화 ③ 스킬 상시 준수에 명시.
> * **G2 신규 문서 생성 절차 0건** — `grep "신규 문서\|문서 신설"` 이 지침·스펙 **양쪽 0건**이었다.
>   `R1-6-14`~`R1-6-17` 은 전부 **「이미 있는 문서를 유지하는」** 규약이고 생성 축이 비어 있었다.
>   🔴 실질 위험: `20_issue/` 에 문서를 만들면 `doc_type_gate` 축1 이 **즉시 FAIL** 하는데
>   그 이유와 순서(등재표 선등재)를 아무도 안내하지 않았다 ⇒ 다음 세션이 FAIL 앞에서 추측한다.
>   🟢 `R1-6-18` 신설 = ①등재표 선등재 ②`write`+LLM-METADATA 헤더 ③**상한 이내면 분할 금지**
>   ④폴더 밖이면 게이트 분모 3곳 손 편입 ⑤게이트 확인. + **새 문서보다 기존 문서 절 추가를 먼저 검토**.
> * **G3 세션 시작에 게이트를 볼 트리거가 없었다.** `doc_type_gate` 를 만들어 놓고
>   **Step 3 브리핑에 그 축이 없었다** ⇒ 「경고 0 이 목표」인데 아무도 보지 않는 상태였다.
>   🟢 브리핑에 `문서 상태: 유형 미선언 n · 상한 초과 n · 여유 부족 n` 행 추가 + **FAIL 시 사용자 대기**
>   (재균형·은퇴는 파괴 가능성이 있어 임의 실행 금지).
> * **G4 스킬의 조문 범위 표기가 `R1-6-16` 까지**여서 `R1-6-17`·`R1-6-18` 이 빠졌다 → `R1-6-18` 로 갱신.
>
> ##### 🟢 검증
> 허브 9종 재발행 전건 PASS(본문 바이트 동일) · 줄길이 8파일 PASS · 게이트 6종 전수 PASS.
>
> ##### 🔴 반복 결함 1건 — 같은 실수를 4번째
> `edit` 앵커를 좁게 잡아 **절/조문 제목줄을 덮어쓴 사고가 이 세션에서 4회**(`R1-6-13` 2회 ·
> 좌표 환산표 1회 · `R1-6-17` 1회) 발생했다. 전부 즉시 복원했지만 **탐지가 「눈으로 보기」에 의존**했다.
> ⇒ 다음 세션 조치 후보: 앵커에 **직전 줄까지 포함**하거나, 편집 후 `doc_heading_gate` 를 매번 돌린다
> (제목 게이트가 이 축을 잡는데 나는 **작업 끝에만** 돌렸다 — 게이트가 있는데 늦게 쓴 것이 문제였다).
>
> ##### ⏸ 잔여
> * 🟠 `HEADER_BYTES 400` 근거 미도출 · 임계값 분모 6일치 한계.
> * 🔴 **O83-E C3(줄 수 축 완화)은 여전히 다음 세션 재검토 대상**이다(내 이해가 걸린 규칙 변경).
> * 🟠 앵커 덮어쓰기 4회에 대한 **구조적 방지책 미구현**(위 후보만 등재).

> #### 🔴 [2026-08-18 O83-H] R4-2 미이행 시정(대시보드 stale) + 잔여 2건 해소 + 앵커 결함 5번째 재발
> 사용자 질문 = **「프롬프트로 요청한 작업 중 할 수 있는건 다 한거지?」** ⇒ 점검해 **미이행 1건**을 찾았다.
>
> ##### 🔴 A. `R4-2` 미이행 — 원장 대시보드가 stale 이었다 (시정)
> O83~O83-G 를 **이력에만** 등재하고 **원장 §1 대시보드 3행을 「진행 중」 그대로** 뒀다.
> `R4-2`(큰 작업 종료 시 `20_issue/` 갱신)의 정면 미이행이고, 다음 세션이 원장을 읽으면
> **끝난 작업을 진행 중으로 오독**한다. ⇒ 3행 전건을 🟢 완료로 갱신(행 키 보존 · `R1-7-4`).
> 🔴 **교훈: 이력 등재는 원장 갱신의 대체물이 아니다.** 이력은 서술이고 원장은 **상태**다.
> (이것은 `P107`「작업 완료의 정본은 이슈원장이다」의 재발이다 — 조문이 있는데 또 놓쳤다.)
>
> ##### 🔴🔴 B. 앵커 덮어쓰기 — 5번째 재발, 그리고 그 조문을 쓰면서 재발했다
> 대시보드 갱신 시 `edit` 앵커를 **행 접두만** 잡아 ① 신규 행 뒤에 **구 O83-B 셀 꼬리가 붙고**
> ② 구 O83 행이 **중복**으로 남았다(python 으로 줄 인덱스 지정 치환해 복구 · 행 게이트 유실 0·중복 0 확인).
> 🔴 더 나쁜 것: 이 결함을 막는 **`R1-7-8` 을 삽입하면서 `R1-7-7` 제목줄을 또 덮어썼다** ⇒ **5회**.
> **조문을 쓰는 행위 자체가 그 조문의 반례였다.**
> 🟢 `R1-7-8` 신설 = ① 앵커에 **직전 줄까지** 포함 ② **긴 표 행은 행 전체**를 앵커로(접두만 넣으면 꼬리가 붙는다)
> · 너무 길면 `edit` 대신 **python 줄 인덱스 치환** ③ **편집 직후 그 자리에서** `doc_heading_gate`·`index_row_gate` 실행.
> 🔴 핵심 = **게이트가 없어서가 아니라 늦게 써서 놓쳤다.** 「게이트를 언제 돌리는지가 게이트의 성능이다.」
>
> ##### 🟢 C. 잔여 2건 해소
> * **`HEADER_BYTES` 근거 도출** — 조각 머리말 **89개 전수 측정** = min 332 · median 349 · **max 381B**.
>   종전 400 은 max 대비 마진이 **19B 뿐**이라 파일명이 길거나 행번호가 5자리면 **과소 예약**이 된다(잠복 결함)
>   ⇒ **448**(max+67)로 교정. 올리는 방향이라 안전측이고, 반영 후 게이트 **상한 초과 0 · 여유 부족 0** 유지.
> * **앵커 방지책** = 위 `R1-7-8` 로 조문화(종전 「후보만 등재」 상태 해소).
>
> ##### 🟢 D. 검증
> 제목 유실 0 · 행 유실 0·중복 0 · 줄길이 7파일 PASS · 유형 게이트 `--strict` PASS(16문서).
>
> ##### ⏸ 잔여 (실행 가능한 것 없음 — 성격별)
> * 🔴 **원리적**: ㉠ 는 크기 무관이라 상한으로 못 막는다(`R1-3-7` 도착 확인만이 방법).
> * 🔴 **의도적 이월**: **O83-E C3(줄 수 축 완화)** — 내 이해가 걸린 규칙 변경이라 **자기 재검토가 부적절**하다.
>   다음 세션이 독립적으로 판정할 항목으로 남긴다.
> * 🟠 **의도적**: 파일 52→95(트레이드오프) · 363행 결손 보류(verbatim 부재) · 은퇴 후보 잔존(미해결 항목) ·
>   이력 비꼬리 조각 여유 2,735B(append형) · 임계값 분모 6일치 한계(시간이 더 필요).
> * 🟠 **미해소**: 앵커 결함의 **구조적** 방지(현재는 조문+게이트 조기 실행이고 도구 강제는 아니다).
>
> ##### 🔴 E. 자기검증 — 「수정 문서만 지침 대조」에서 **결함 4건 적발**(사용자 지시 · 전건 시정)
> 위 D 의 「검증 PASS」는 **기계 게이트만** 돌린 것이었다. 지침 축별 내용 대조를 하니 4건이 나왔다.
> 🔴 **게이트 전수 PASS 는 「지침 준수」의 증거가 아니다** — 게이트는 자기가 보는 축만 본다.
> * 🔴 **① 대시보드 stale 재발(`R4-2`)** — 원장 §1 에 `⏸ 잔여 = HEADER_BYTES 400 근거` 가 남아 있었다.
>   위 C 에서 448 로 교정하고 **이력에만 적었다**. 같은 세션 안에서 「이력 등재는 원장 갱신의
>   대체물이 아니다」를 **다시 위반**했다(`P107` 3차). ⇒ 셀 갱신(행 키 보존 · 줄 인덱스 치환).
> * 🔴 **② 조문 번호 순서 역전 2건** — `R1-7-8` 이 `R1-7-7` 앞 · `R1-6-18` 이 `R1-6-17` 앞.
>   규약 전체가 `1,2 → 10~18 → 3~9` 였다(신설 조문을 `R1-6-2` 뒤에 계속 끼워넣은 결과).
>   🔴 **앵커 사고의 잔재가 「위치」로 남아 있었다** — 제목은 복원했지만 순서는 복원하지 않았다.
>   ⇒ 지침 `R1-7-8` 을 `R1-7-7` 뒤로 이동 · 규약 18조문 **전건 번호순 정렬**(줄 다중집합 완전
>   일치로 무변경 이관 실증). 조문화 = **조문은 번호 순서 말미에 덧붙인다**(중간 삽입은 신호다).
> * 🔴 **③ `doc_heading_gate` 골든 미등재 = 게이트 침묵** — `01_문서분할_규약.md` 가 `DOCS` 분모
>   에는 있는데 골든이 없어 **그 문서의 제목 유실은 실질 미검사**였다(출력은 ⚪ 로 조용히 흘렀다).
>   `R1-6-18` ④ 가 「분모 추가」만 요구하고 **골든 발행을 빼놓은** 것이 근인이다.
>   ⇒ 유실 0 확인 후 사유 기재 발행(`--update-golden --reason`) ⇒ 현재 815 = 골든 815 · 미등재 0.
> * 🔴 **④ `HEADER_BYTES` 교정이 `split_doc.py` 리포트 축에 미전파(`R1-6-17` 3차 누락)** —
>   dry-run 판정이 `b - a` · `nbytes(t)` 로 **머리말을 빼고** 재어, 파일이 상한을 넘어도 🟢 로
>   표시했다(`R1-6-11` 위반 지점). 배치는 `fits()` 가 선반영하므로 안전했고 ⇒ **표시상 과소보고**
>   였다. `--verify` 게이트6 은 실제 파일을 읽어 정확했다 — **두 축의 판정 기준이 어긋나 있었다.**
>   ⇒ `f_lines`·`f_bytes`(머리말 포함)로 통일 · 실증 = 이력 dry-run `105줄 8.7KB`(본문 102줄 8,464B).
> * 🔴 **⑤ 이 절을 쓰다가 백틱 사고 1건** — 위 본문을 `bash -c "python3 -c '...'"` 로 넣자
>   **백틱 구간이 bash 명령치환으로 해석돼 전부 삭제**된 채 기록됐다(23줄). 되돌림 · 손실 0 ·
>   스크립트 파일 경유로 재작성. 🟢 조문화 = **백틱·`$`·`!` 를 포함한 본문은 셸을 경유시키지 않고
>   파일로 쓴다**(`R1-7-9`). 🔴 `R1-7-8` 이 「앵커」만 다뤘고 **전달 경로**는 비어 있었다.
> 🟢 **일반화**: 파라미터를 고치면 **그 값을 읽는 지점**뿐 아니라 **같은 것을 다르게 재는 지점**도
> 찾아야 한다. `R1-6-17` 이 3차 재발한 이유는 「보는 지점」을 **읽기 지점**으로만 좁게 봤기 때문이다.
>
> ##### 🟢 F. 후속 — 「게이트가 보지 않는 축」을 게이트로 만들었다 (사용자 지시)
> E 절의 결론이 「게이트는 자기가 보는 축만 본다」였으므로, **보지 않던 축 2개를 게이트화**했다.
> * 🟢 **`scripts/clause_order_gate.py` 신설** — 축1 **역전**(blocking) · 축2 **중복 정의**
>   (blocking) · 축3 **결번**(🟠 경고 · 은퇴·철회일 수 있다). 접미 조문(`R1-3-7-a`)은 부모와
>   같은 자리로 취급해 **중복 오탐을 피한다**. 분모 = 조문을 **정의**하는 문서 2종
>   (`R1-6-N` 을 참조만 하는 문서는 분모가 아니다 — 백틱 인용은 정규식에 걸리지 않는다).
>   🔴 **self-check 5축**(역전·중복·접미·결번·정규식 사망)을 넣었다 — `R3` 2번 「게이트를 실패
>   케이스로 음성 테스트했는가」의 이행이고, ①은 `R1-7-8`↔`R1-7-7` **실제 사례**를 쓴다.
>   실측 = 조문 **75개 / 계열 12종 · 역전 0 · 중복 0 · 결번 0**.
> * 🟢 **`doc_heading_gate` 골든 미등재 ⚪ → 🔴 FAIL 격상** — 종전에는 `added`(정보)로 흘려
>   분모에 있고 골든에 없는 문서가 **조용히 미검사**로 남았다. 격상 실증 = 골든에서 1건을 뺀
>   상태로 `judge` 를 돌려 **FAIL 1건 검출** 확인(`added` 에는 더 이상 남지 않는다).
>   🔴 근인 일반화 = **분모 편입과 기준선 발행은 별개의 두 동작**이고, 하나만 하면 게이트가
>   **0 을 내며 침묵**한다. O83-E 의 「분모를 넓혔는데 침묵」과 같은 뿌리다.
> * 🟢 **지침 `R3` 단계 종료 게이트 8종 → 9종** — 9 = **게이트가 보지 않는 축**을 자문하는 항목
>   (㉠ 원장 §1 stale · ㉡ 파라미터를 **다르게 재는** 지점 · ㉢ 기준선 발행 · ㉣ 조문 순서).
>   🔴 **8종 → 9종 변경을 참조하는 3개 지점을 전수 조사해 전건 교정**했다(지침 1 · 스킬 2).
>   이것이 `R1-6-17` 이 3차 재발한 경로의 예방 이행이다 — **수를 바꾸면 그 수를 적은 곳을 찾는다.**
> * 🟢 **`init_ihcho` 스킬 갱신** — stale **6건** 교정: 지침 실측 267줄/26,083B → **298줄/29,860B** ·
>   규약 157줄/17,131B → **191줄/20,975B** · `R1-6-17` → **`R1-6-18`** · 이력 조각 25 → **26** ·
>   이력 허브 95줄/7,829B → **102줄/8,464B** · 원장 조각 최대 29,163 → **29,677B**.
>   🔴 **더 중요한 것은 재발원 제거다** — 하드코딩된 조각 번호(`-025`)와 조각 수 목록을
>   **실측 명령으로 대체**했다(`ls -1 … | tail -1`). 세션마다 늘어나는 값을 문서에 적으면
>   **그 줄은 반드시 stale 이 된다** ⇒ 값이 아니라 **재는 방법**을 적는다.
>   `R1-7-8`·`R1-7-9` 요약과 브리핑 게이트 2종도 스킬에 반영했다.
> 🟢 **일반화**: 결함을 고치는 것과 **그 결함을 잡는 축을 만드는 것**은 다른 작업이다.
> O83-H 는 앞의 것을 4건 했고 이 절에서 뒤의 것을 했다 — **재발을 막는 쪽이 뒤에 있다.**
>
> ##### 🔴 G. 확장 점검에서 stale 8행 추가 적발 (사용자 지시 「issue 폴더·스킬에도 적용」)
> `R3` 9번 ㉡(「내가 바꾼 수를 다르게 재는 지점」)을 실제로 이행하려고 **게이트 수 언급을
> 전수 조사**하다가, 원장 **§0 「조각 분할 문서」 표가 8행 전건 과소**인 것을 발견했다.
> * 🔴 **실측 대비**: `10_진단` 14→**20** · `02` 3→**4** · `20_현업` 5→**7** · `30_설계` 5→**7** ·
>   `50_dbt` 9→**12** · `99_NEXT` 4→**6** · `00_INDEX` 4→**5** · `01_세션이력` 25→**26**.
>   「최대 조각」 열도 같이 어긋나 있었다(예: `10_진단` 292줄/37.7KB → 실측 189줄/26.3KB).
>   ⇒ 17개 셀을 실측값으로 치환(**행 키·열 수 보존 · 행 수 148 불변**).
> * 🔴 **근인은 ①과 동일하다** — O83-D 재균형과 O83·O83-H 롤오버가 조각을 늘렸는데
>   **표는 분할 당시 값에 고정**돼 있었다. `doc_type_gate` 는 상한·여유를 보고 **이 표를 보지
>   않는다** ⇒ 게이트 밖의 축이었다. 🔴 **stale 은 1건이 아니라 「부류」다** — ① 을 고칠 때
>   같은 부류를 함께 찾지 않아 **같은 세션에서 8행이 더 나왔다.**
> * 🟢 **재발원 제거** — 표 위에 「여기서 읽지 말고 `doc_type_gate.py` 로 재라」를 명시하고,
>   「원문 줄/바이트」 열은 **분할 당시 값으로 의도적 고정**임을 구분해 적었다.
>   규약 `R1-6-2` ㉣ 의 조각 수 기재도 같은 처방으로 교정했다(스킬에 적용한 것과 같은 원리).
> * 🔴 **이 절을 쓰다가 삽입 위치 오류 1건** — 재측정 주석을 표 위에 넣으려 했는데 앵커 탐색이
>   **다른 표(문서맵)의 구분선**을 잡아 19행에 붙었다. 되읽어 발견 → 절 제목 직후로 이동
>   (줄 다중집합 완전 일치로 무변경 실증). 🟢 교훈 = **자동 앵커 탐색도 앵커 사고를 낸다**
>   ⇒ `R1-7-8` 의 「편집 직후 확인」은 게이트뿐 아니라 **눈으로 위치 확인**까지 포함해야 한다.
> 🟢 **일반화**: 「게이트가 보지 않는 축」을 게이트로 만드는 작업(F)과, **그 축에 이미 쌓인**
> 부채를 청산하는 작업(G)은 다르다. 게이트를 만들면 **앞으로**를 막고, 과거는 남는다
> ⇒ 게이트 신설 시 **그 게이트로 현재 상태를 전수 스캔**해 기존 위반을 함께 청산한다.
