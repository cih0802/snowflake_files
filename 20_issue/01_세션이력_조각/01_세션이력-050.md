<!-- SPLIT-CHUNK 01_세션이력.md | 050/050 | 허브 = 01_세션이력.md | 원문 1~118행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-30 O121-B] 자기검토: 게이트 분모를 손으로 골랐다(5세션 연속 축) ⇒ 분모를 기계화했다
>
> **지시** = 「이번 세션 작업을 비판적으로 검토 · 승인 필요 작업 전건 승인 · 작업과 문서 개선」.
> 🔴 `R1-4-3` 대로 새 번호를 발급하지 않고 **접미 `-B`** 로 이었다(게이트의 「다음 발급」은 착수 시 실측했다).
> 🔴🔴 **[자기결함 · 즉시 정정] 초판은 그 실측값을 이 문장에 숫자로 적었다** ⇒ 그것이 `id_collision_gate`
> 의 **참조 축을 부풀려** 다음 발급이 한 칸 밀려 보이게 만들었다(`P222` 실물 재현).
> ⇒ 🔴 **판정식 = 이력·원장에 「다음 라벨 예고 숫자」를 쓰지 마라.** 다음 세션은 **정의 축 최대값**을
> 기준으로 판단하라(정의 최대 = 이 항목의 라벨이며, 참조 최대는 예고 문장에 오염된다).
>
> ### 1. 🔴🔴 최대 자기결함 — O121 이 게이트 분모를 손으로 골랐다
>
> O121 은 「게이트 문서 7종 + **도메인 8종** 전건 rc=0」을 보고했다. 🔴 그 8종은 **내가 손으로 고른 것**이다.
> 기계로 재니 무인자 판정 게이트(`JUDGE`)는 **24종**이고, 그중 **2종을 아예 돌리지 않았다**:
> · `ws_stage_verify` rc=1 · `table_ddl_column_gate` rc=1 — 둘 다 O121 보고에 없다.
> 🔴 이 축은 **O118-B → O119 → O119-B → O120 → O121 로 5세션 연속** 결함이 났다.
> O119 가 「`ls scripts/` 로 다시 세라」를 신설했고 O119-B 가 **그것을 신설하고 스스로 어겼다**.
> ⇒ 🔴 **판정식 = 「사람에게 세라고 지시하는 것」이 5번 실패한 처방이다. 세는 일을 기계에 넘겨야 한다.**
>
> 🟢 **처방 = `scripts/gate_census.py` 신설**(음성 테스트 `test_gate_census.py` **11/11**)
> · 분모는 **`ls scripts/*.py` 실측**이고 문서나 등재표가 아니다.
> · 6분류 **명시 등재** = `JUDGE`(무인자 판정) · `OBSERVE`(관측 · 판정 아님) · `TEST` ·
>   `NEEDS_ARGS` · `GEN` · `MUTATES`. 🔴 **미분류는 FAIL** — `doc_type_gate` 의 검증된 패턴이다
>   (새 스크립트를 만들면 게이트가 **막는다** ⇒ 분모 미달이 원리적으로 남지 않는다).
> · 등재에는 **판정 축 문구가 필수**다(축이 없으면 다음 세션이 무엇을 재는지 모른다 · 테스트 축⑦).
> · `--run` 은 **`JUDGE` 만** 실행한다. 🔴🔴 **「전부 무인자로 돌려보기」는 답이 아니다** —
>   `MUTATES` 26종에는 `deploy_*`·`retire_*`·`split_doc`·`patch_*` 가 있어 라이브·문서를 바꾼다(`R4-4-3`).
>   그 교집합이 공집합임을 테스트 축⑤가 단정한다.
>
> ### 2. 🔴🔴 그 도구의 **초판이 같은 결함을 냈다** — 휴리스틱은 분모를 반대로 깨뜨린다
>
> 초판은 파일 내용에 `CREATE OR REPLACE`·`GRANT` 토큰이 있으면 「라이브를 바꾼다」로 분류했다.
> 🔴 그 결과 **`audit_ddl_rule7`·`sv_unit_gate`·`table_ddl_column_gate` 를 「실행 금지」로 오분류**했다 —
> 이들은 **DDL 파일을 입력으로 읽는 판정 게이트**다(실측 = `execute(` 호출은 읽기뿐).
> ⇒ 그대로 뒀다면 이 도구가 **다음 세션에게 3종을 빼라고 지시**했을 것이다(분모 미달의 자동화).
> 🟢 휴리스틱을 버리고 명시 등재로 바꿨고, **테스트 축⑥이 이 3종의 `JUDGE` 소속을 못박는다.**
> 🔴 **판정식(신설) = 위험 분류를 「내용 추측」으로 하지 마라 — 게이트는 위험한 것을 입력으로 읽는다.**
>
> ### 3. 🟢 「사용법을 rc=1 로 낸다」는 규약 위반이 남아 있었다 — 축으로 집행했다
>
> `ws_stage_verify` rc=1 은 **위반이 아니었다** — 무인자에서 `sys.exit(__doc__)`(= rc 1)로 사용법을 냈다.
> 🔴 O120 이 `o54_sv_value_gate` 에서 **똑같은 것을 고쳤는데** 형제에는 남아 있었고, 그 결과
> O121 의 순회가 그것을 **위반으로 거짓 계상**할 수 있었다(실제로 O121-B 초측에서 그렇게 셌다).
> 🟢 시정 = `print(__doc__); sys.exit(2)` · 그리고 **스크립트 하나를 고치는 것으로는 재발하므로**
> `gate_census --run` 에 **「`NEEDS_ARGS` 무인자 실행은 rc=2 여야 한다」** 축을 넣었다(실측 3/3 rc=2).
> 🔴 **판정식 = 규약을 어긴 스크립트를 찾으면 규약 자체를 축으로 만들어라.**
>
> ### 4. 🟢 영구 빨강 게이트 2건 중 1건 해소 — `table_ddl_column_gate`
>
> O120 은 이 게이트의 「2/7 불일치」를 **오탐으로 확정**하고 *"경고 문안 자체가 사실과 다르다"* 고 적었다.
> 🔴 **그런데 문안을 고치지 않았다**(`P105` 마커만 남기기) ⇒ 게이트가 **영구 빨강**이 됐고,
> 영구 빨강 게이트는 무력화된다(`O119-B` 원칙).
> 🟢 시정 = **두 축을 분리**했다:
> · **집합 불일치(누락·초과) = 🔴 blocking** — 모델이 컬럼을 안 채우거나 물리가 정본을 앞서 나갔다(실데이터 위험).
> · **순서 드리프트 = 🟠 advisory(문서 축)** — dbt insert 가 **양측 이름 지정**이라 위치 오삽입이
>   원리적으로 불가하다(O120 실측 `logs/dbt.log:314-316`).
> 🔴 강등이 「검사 끄기」가 아님을 **severity 자기검사 4축**으로 못박았다(누락·초과는 blocking 유지).
> 🟢 결과 = **rc=1 → rc=0** · 집합 일치 7/7 · advisory 2건은 문안과 함께 계속 보인다.
>
> ### 5. 🟢 O121 이 마커로만 남긴 축을 실제로 구현 — `comment_drift_gate` 테이블레벨
>
> O121 은 「테이블레벨 COMMENT 가 어느 드리프트 축에도 없다」를 적발하고 **🟠 미시정으로 등재만** 했다.
> 🔴 그것은 `P105` 위반이고, 그 상태로는 **다음 세션이 파일만 고쳤을 때 게이트가 🟢 를 내며 드리프트가 생긴다.**
> 🟢 구현 = `parse_ddl_table_level()` + `live_table_level()` + 판정 2축(GOLD·SILVER) ·
> 음성 테스트 `test_comment_drift_table_level.py` **10/10**.
>
> 🔴🔴 **그리고 신설 직후 내 축이 거짓 드리프트 1건을 발행했다.** 초판 정규식이 `'(.*)'\s*;` + `re.S` 여서
> **파일 마지막 테이블에서 탐욕 매칭이 파일 끝까지 삼켰다**(실측 = 값 길이 **21,453자** ↔ 라이브 354자)
> ⇒ `GOLD.FACT_MEMBER_SPONSOR_BIZ` 를 「불일치」로 보고했다.
> 🟢 SQL 문자열 본문 패턴 `(?:[^']|'')*` 로 시정 · 회귀는 **테스트 축②(파일 마지막 테이블)**와
> **축⑨(DOTALL 부재 직접 단정)**이 막는다. 시정 후 **GOLD 37/37 · SILVER 42/42 · 드리프트 0**.
> 🔴 **판정식 = 내가 방금 만든 축의 첫 FAIL 은 「발견」보다 「내 파서 결함」일 확률이 높다 —
>   생산물을 고치기 전에 값의 길이·경계를 재라**(`▣ZZZ6 ㉡` 의 신설 축 판본).
>
> 🟢 **부수 = 분모 차이 1건을 규명해 게이트에 인쇄했다.** 이 축은 SILVER **42**, `audit_ddl_rule7` 은
> 블록 **43** 으로 다르고 **둘 다 옳다** — 차이는 `SILVER.BIGQUERY_REFINED_DATA` 이며 파일에서
> **주석 처리된 선언**이다(외부 Python 적재 · `DEC-37`). 테스트 축⑦이 그 동작을 단정한다.
>
> ### 6. 🟢 O121 이 만든 게이트의 자기결함 2건 시정
>
> ㉠ 🔴 **내가 넣은 `NEG_RE` 가 과대 면제였다.** 맨 `없다|없고|부재` 를 토큰으로 가져서
>   「`GOLD.FACT_X` 로 답한다. 캠페인 축이 **없다**」처럼 **객체와 무관한 부정어**로도 면제가 성립했다
>   ⇒ 무력화 경로다. 🟢 부정어를 **객체 어휘(테이블·스키마·객체·뷰·샤드)와 짝지어야만** 인정하도록 좁혔고,
>   실제 시정 문안 2종은 **여전히 면제**된다(실측 = 부정서술 면제 3건 유지 · rc=0).
>   🔴 **테스트 축③-B 가 양방향을 단정한다** — 무관 부정어는 FAIL · 객체 부정어는 PASS.
> ㉡ 🔴 **O121 은 advisory 6건을 「라이브 미포함 6건」으로만 보고하고 규명하지 않았다.**
>   이 세션의 주제가 「advisory 를 안 보면 진짜가 숨는다」인데 스스로 그것을 남겼다.
>   🟢 **전건 규명 = 6건 모두 오탐**(컬럼명 4 = `SPNSR_BSNS_NO`·`EXEC_BUDGET_ERP`·`AD_SOURCE_TYPE` /
>   코드값 2 = `UCMPGN_AVG_MEMBER`·`CHANNEL_NEW_SPNSR` · 진짜 위반 **0**) ⇒ 사유와 함께 등재해
>   **미규명 0** 으로 내렸다. 🔴 등재는 잡음 끄기가 아니라 **신규 신호를 보이게 하는 것**이며,
>   등재를 비우면 다시 올라온다는 것을 **테스트 축⑬**이 단정한다.
> 🟢 그 게이트 음성 테스트 = **12 → 15축**(면제 **4종 전부 양방향**).
>
> ### 7. 🔴 O121 이 문서에 박은 수치 — 재는 방법을 함께 남긴다
>
> O121 은 「3스펙 문자열 표면 **201개**」·「SILVER 테이블레벨 블록 **39→43** · GOLD **35→37**」을
> 문서에 적었다. 🔴 이 수치들은 **재적재·문서 수정으로 변한다**(`R3-9 ㉦`).
> 🟢 재는 방법을 여기 남긴다(값이 아니라 방법이 정본이다):
> · 표면 수 = `python3 scripts/agent_object_ref_gate.py` 의 **축⑤ 표면 인벤토리**.
> · 테이블레벨 블록·일치 수 = `python3 scripts/comment_drift_gate.py` 의 **[테이블레벨 COMMENT]** 절.
> · 게이트·테스트 분모 = `python3 scripts/gate_census.py`(문서에서 읽지 마라).
>
> ### 8. 🟠 승인 집행 · 잔여
>
> · 🟢 **승인 집행** = 오배치 `20_issue/_o121_entry.md` 삭제(루트 사본 SHA256 동일 확인 후) ·
>   임시 도구 `scripts/_o121_tx.py`·`_o121_trim.py`·`_o121b_probe.py`·`_o121b_probe2.py` 삭제
>   (전건 `os.listdir` 로 삭제 전·후 접두 나열 · `R1-7-7`).
> · 🟢 **전건 재측정** = `gate_census --run --run-tests` = `JUDGE` **24** + `TEST` **21** 중 **rc=0 44건** ·
>   잔여 rc=1 = **`merge_check` 1건뿐**이고 이것은 **정당한 신호**다(근거철 **6건**이 정본에 미이관).
>   🔴 그 6건은 지우면 내용이 사라진다 ⇒ **먼저 정본으로 이관**해야 한다(`R2-8-1`).
> · 🔴 **`DEC-44` 무영향** — 예산 `metric`·`where`·차수 필터·`severity` **0곳** · Agent 배포 미실행 ·
>   이 세션의 **라이브 변경 0건**(O121 의 `SET COMMENT` 1건은 그 세션 소관).
> · 🔴🔴 **다음 세션에 넘기는 신규 blocking-class 위험 1건** = `retire_rows.py` 는 목적지(`90`)가
>   **허브가 된 뒤에도 허브에 직접 append** 한다(지침이 「미시정」으로 표기). 🔴 허브는 자동 생성물이므로
>   그 append 는 **다음 `--republish` 에서 조용히 사라진다** — 이 워크스페이스가 4번 당한 유형이다.
>   ⇒ **은퇴 도구를 쓰기 전에 이것을 고쳐라**(목적지를 마지막 조각으로) · 음성 테스트 동반.
> · 🟠 승계(성격 불변) = 규칙7 조사 경계 공백 · `04_SV_설계.md` 구조 절 결손 · 열거누락 16(advisory) ·
>   DDL↔라이브 미독 `05_4`·`05_5`·`05_8`·`05_9` · `99_NEXT` 허브 용량(🟢 `retire_sections` 경로 유효) ·
>   ~~`retire_sections` 수리~~ 🔴 **[O121 재현 불가 · 사용자 판단 대기]**.
> · 🔴 **종료코드는 전부 리다이렉트로 받았다**(`| tail` 뒤 `$?` 를 읽은 곳 **0회**) · `read` 미반환 **0**.
