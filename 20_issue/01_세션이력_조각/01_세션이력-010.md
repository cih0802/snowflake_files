<!-- SPLIT-CHUNK 01_세션이력.md | 010/025 | 허브 = 01_세션이력.md | 원문 2245~2495행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-11 O59-R] **DEC-35 4단계 — SV 라벨축 노출 + SV COMMENT 부채 12건 종결**
>
> **트리거** = 착수 순서 6번(사용자 결정 = 「라벨축 + 부채 12건 동시」). 정본 명세 = 문서30 **§23-C(DEC-35 R1~R5)** · **§23-D 4단계** · 등재부 `31_코드군_매핑등재부.md`.
>
> ##### ① 🔴🔴 착수 점검에서 게이트가 **거짓 정보**를 내고 있었다 — P149 재발
> `sv_code_label_gate` 가 *"SV_SERVICE.SEND_STATUS: 라벨 컬럼 부재 확인됨"* 을 3건 보고했는데
> **라이브 GOLD 에는 라벨 컬럼이 실재**했다(`INFORMATION_SCHEMA.COLUMNS` 실측: FSE +5 · DIM_EVENT +2 · FEP +6).
> 원인 = `LABEL_PAIRS` 가 `SEND_STATUS`·`EVENT_CATEGORY`·`PART_STATUS` 를 `None`(= 「짝이 없음을 **확인했다**」)로
> 단정해 두었고, **O59-N 이 컬럼을 만들면서 같은 커밋에서 등재부를 갱신하지 않았다.**
> 🔴 단순 오보가 아니다 — 등재부가 stale 이면 검사 ②(라벨축 미노출)가 **대상에서 빠져 침묵**한다(미탐).
> ⇒ 6종을 실명으로 교체(`SEND_STATUS_NAME`·`SEND_RESULT_NAME`·`EVENT_CATEGORY_NAME`·`PART_STATUS_NAME`·
> `PART_PATH_NAME`·`PART_CHANNEL_NAME`) · `SUBTYPE` 만 `None` 유지(등급 D · 라벨 창작 금지).
>
> ##### ② 라벨축 노출 — 차원 **160 → 171**(+11)
> | SV | 신설 차원 | 비고 |
> |---|---|---|
> | `SV_SERVICE` | `SEND_STATUS_NAME` · `SEND_RESULT_CD` · `SEND_RESULT_GROUP` · `SEND_RESULT_NAME` (4) | 축A 라벨 + **축B 신규 노출**(§23-G) |
> | `SV_EVENT_PARTICIPATION` | `EVENT_CATEGORY_NAME` · `PART_STATUS`/`_NAME` · `PART_PATH`/`_NAME` · `PART_CHANNEL`/`_NAME` (7) | 참여 3축은 **코드축조차 미노출**이었다(설계 §4.3 미이행) |
>
> - **동의어 재배치(R1)**: `SEND_STATUS` 의 `'발송상태'` → 라벨축으로 이전하고 코드축엔 `'발송상태코드'` 만 남겼다.
> - **의도적 미노출 2종**: `SEND_STATUS_GROUP`(distinct 1 = 정보량 0) · `EVENT_CATEGORY_GROUP`(`EVENT_KIND` 와 1:1 중복)
>   ⇒ 판별자 역할을 이미 `CHANNEL`·`EVENT_KIND_NAME` 이 하고 있어 축을 늘리지 않았다.
>
> ##### ③ 🔴🔴 실측이 새 결함을 드러냈다 — **라벨공간이 원천별로 갈린다**
> 라벨을 노출하면 끝이 아니었다. 두 축에서 **같은 의미가 두 라벨로 갈라져 나온다**:
> - `PART_PATH_NAME` = `homepage`(일반행사 MS303 · 970,307행) **와** `홈페이지`(캠페인행사 MS004 · 29,522행)가 공존
> - `PART_STATUS_NAME` = 영문 계열(`Success`·`n_step_right/fail` · MS304) **와** 한글 계열(`신청`·`참여`·`불참` · MS006)
>
> ⇒ 「참여경로별」·「참여상태별」 질문이 **한 의미를 두 행으로 갈라** 반환한다.
> 🔴 **통합 라벨을 만들지 않았다** — 사전에 없는 문자열을 만드는 것은 **라벨 창작**(DEC-17-B)이다.
> **사용자 결정 = 「노출 + 혼재 경고 동반」** ⇒ 차원 COMMENT · SV COMMENT · AI_SQL · Agent 도구 description
> 4계층에 「`EVENT_KIND_NAME` 동반 필수 · 통합 금지 · 합치면 합친 사실을 명시」를 심었다.
> 🔴 부수: **두 원천은 「참여」의 정의 자체가 다르다**(다단계 통과 여부 vs 신청/참여/불참) ⇒ 합산 참여율 금지.
>
> ##### ④ 부채 12건 — 🔴 **전부 지우면 안 됐다. 2건은 실측치가 아니었다**(O59-E `10,000` 유형 재발)
> §O56-D 가 「12건 정리」로 남긴 것을 **토큰별로 판정**했다:
> | 판정 | 건수 | 처리 | 대상 |
> |---|---:|---|---|
> | **실측치** | **10** | 문안을 **§참조로 치환** | `8.32%p` · `85.65%`·`14.29%` · `18.5%`(4곳) · `891,959,790,888`·`1,056,821,121,099`(각 2곳) · `99.99%` · `1,010,680`·`1,038,262` · `56.86%`·`2,291,878`·`3,594,843` · `13.747454%` |
> | **규약상수** | **2** | 문안 **유지** + `NUM_EXEMPT` 등재 | `1,000`(소표본 1위 방지 **하한 임계 예시**) · `100배`(**퍼센트↔분수 환산 계수**) |
>
> 🔴 지웠다면 `1,000` 은 **P76/P77 소표본 가드 지침**을, `100배` 는 **단위 규약 서술**을 COMMENT 에서 파괴했다.
> ✅ **`CMT_BASELINE` 을 비웠다**(12 → 0) — 항목을 남기면 그 조합이 영구 면제돼 부채가 고착된다.
> ⇒ 이후로는 **모든 검출이 실패**다. 🆕 **P208**.
>
> ##### ⑤ 판정 (P33 — 문장이 아니라 실행)
> | 항목 | 결과 |
> |---|---|
> | 재배포 | SV **5종** `CREATE OR ALTER`(`05_1`·`05_2`·`05_4`·`05_5`·`05_9`) · ERR 0 |
> | `sv_code_label_gate` | **위반 0**(코드값부재 0 · **종수 0** · 열거누락 0 · **라벨축 0** · 폐기리터럴 0) · 정보 **3 → 1**(`SUBTYPE` 만 · 의도) · 차원 **171** |
> | `sv_unit_gate` | 위반 0 · GRANT 누락 0 · 폐기식 0 · **COMMENT 수치 신규 0 · 기지 부채 0**(12→0) · 의미 예외 21 |
> | 값 불변 | `SV_SERVICE` 38,470,780 / 1,031,971 · `SV_EVENT_PARTICIPATION` 1,134,126 / 1,134,126 / 407,223 = **재배포 전후 동일** |
> | 🟢 **fan-out 0 실증**(P90) | 신설 라벨 **5축 동시 분해**(`EVENT_KIND_NAME`+`EVENT_CATEGORY_NAME`+`PART_STATUS_NAME`+`PART_PATH_NAME`+`PART_CHANNEL_NAME`) 합계 = 총계 **정확 일치** · `SV_SERVICE` 도 축A·축B 동시 분해에서 일치 |
> | 자기검사 | `sv_code_label_gate --self-check` 양성 4/4 + ④ 1/1 · 음성 9/9 오탐 0 |
> | 회귀 | `test_generators` **18/18** · `audit_ddl_rule7` 4표면 **위반 0** · `table_ddl_column_gate` **7/7** |
> | 열거 정확성 | 신설 저카디널리티 축 **전량 열거**(종수 선언이 라이브 실측과 일치 = 게이트 ① 통과) |
>
> ##### ⑥ 🔴 내가 만든 결함 1건 — 자기적발·교정
> `EVENT_CATEGORY_NAME` 나열에서 실제 라벨 `I'm your pen` 을 **`I·m your pen` 으로 변형해 적었다**
> (아포스트로피가 SQL 문자열·게이트 열거 정규식과 충돌하는 것을 회피하려다 값을 바꿨다).
> 🔴 **라벨값 변형은 이 세션이 잡으려는 바로 그 결함**이다 — Analyst 가 그 문자열로 필터하면 0행 무증상 오답이다.
> ⇒ `I''m your pen` 으로 교정. 부수로 **이 축은 따옴표 열거 규약을 쓸 수 없다**는 것이 드러났다
> (값에 `'` 가 있으면 게이트 `ITEM` 정규식이 첫 따옴표에서 끊긴다) ⇒ **「종수 선언 + 따옴표 없는 나열」** 로 적었고,
> 종수는 게이트 ①(선언 ↔ 실측 종수 정합)이 계속 검증한다.
>
> ##### ⑦ 회수한 거짓 서술
> - `05_4` `SEND_STATUS` · `05_5` `EVENT_CATEGORY` 의 *"이 SV 에는 라벨축이 아직 노출되지 않았다(4단계 예정) —
>   없는 차원명을 생성하지 말 것 · WIDE 직접조회로 안내"* ⇒ **집행으로 거짓이 됐다**. 전수 grep 으로 `05_*` 잔존 0 확인.
> - `05_4` SV COMMENT 의 *"비활성: 수신/성공/실패/오픈"* ⇒ **measure 는 여전히 미적재이나 축B 라벨로 도달 결과
>   분해는 산출된다**로 정정(P61 — 부정형 서술이 남으면 Agent 가 "불가"로 답한다).
> - `05_4` `SUBTYPE` 의 *"라벨 사전이 아직 배선되지 않았다"* ⇒ **참이지만 불완전**했다. 등재부 §5 가 요구한
>   **「채널별 다체계」 명시가 미이행**이었다(EMAIL 0·1·2 / MSG_AT 0·1 / PSTMTR 1·2·3 / SND NULL) ⇒ 추가.
>
> ##### ⑧ Agent 계층 반영 (4단계 명세 = SV + `08_AGENT_spec.md`)
> 🔴 **문서만 고치면 효과가 없다** — 정본은 `cortex_project/agents/<AGENT>/agent_spec.yaml` 이다(인수인계 §5).
> ⇒ **3파일** 갱신: `08_AGENT_spec.md`(설계 문서) + `AGENT_MEMBER/agent_spec.yaml`(`analyst_service`·
> `analyst_event_participation`) + `AGENT_OVERALL/agent_spec.yaml`(`analyst_service`).
> 🟠 **배포는 남았다** — Agent 스펙 반영은 `ALTER WORKSPACE … COMMIT` → `09_2_AGENT_버전업.sql` →
> `SHOW VERSIONS IN AGENT` 로 `is_default` **실측**(P66)이 필요하다. **파일 편집 ≠ 배포**(P33).
>
> ##### ⑨ 🆕 교훈
> - **P208** 🔴 **부채 대장을 비우기 전에 토큰별로 「실측치인가 정의인가」를 판정한다.** 「기지 부채 N건」은
>   **작업 지시가 아니라 후보 목록**이다 — O59-E 가 `10,000` 4건을 회수했는데도 같은 유형이 2건 더 남아 있었고,
>   목록을 그대로 집행하면 **지침·단위 규약을 지우는 작업**이 된다. 대장은 **정리의 마지막 단계에** 비운다.
> - **P209** 🔴 **라벨을 붙였다고 라벨축이 쓸 수 있는 것은 아니다.** 다원천 UNION 도메인은 **라벨공간도 갈린다** —
>   사전이 원천별로 다른 문자열을 등재해 두면 같은 의미가 두 행으로 나온다. 통합은 라벨 창작이므로
>   **구조로 못 고친다** ⇒ 판별자 동반을 **소비 4계층 전부**(차원 COMMENT·SV COMMENT·AI_SQL·Agent description)에
>   심고 현업 통합 표기를 질의한다. 「라벨 배선 완료」를 「라벨로 답할 수 있다」로 읽지 말 것.
> - **P210** 🔴 **문안 규약과 충돌하는 값을 만나면 값을 바꾸지 말고 표기 방식을 바꾼다.** 아포스트로피를 포함한
>   라벨을 열거 규약에 맞추려고 문자를 치환하는 순간 **COMMENT 가 존재하지 않는 값을 가리킨다**(0행 무증상 오답).
>   ⇒ 규약을 벗어나되 **게이트가 검증할 수 있는 형태**(종수 선언)를 남긴다.
>
> ##### ⑪ 🟢 **build 판정 (2026-08-11 08:34 UTC · 사용자 실행)** — `PASS=386 WARN=32 ERROR=0 TOTAL=418`
> 🟢 **P206 준수 — artifact 를 실행과 먼저 대조했다**: `run_results.json` `generated_at` **08:34:23.382986Z** 이
> 로그 종료(01:34:23.687 로컬)와 **초·소수점까지 일치**하고 418/386/32/0 도 동일 ⇒ stale 아님.
> (O59-Q 규명이 막힌 원인이 stale 판 기준선이었다 · P206.)
>
> - 🟢 **WARN 33 재발 없음** — `warn_gold_view_comment_coverage` = **`pass` · `failures=0`**.
> - 🟢 **WARN 32 전건 기지 유형 확인**. 해시 `unique_id` 는 `manifest.json` 으로 실명 해소했다(메시지만으로는 대상 불명).
>   구성 = `relationships` 21 · `not_null` 9 · `accepted_values` 1 · singular 1 — 전부 이슈 A~E·고아·사전미등재 계열.
>   🟢 **부수 독립 확인 2건**(이번 세션 SV 문안의 근거가 dbt 표면에서 재확인됐다):
>   · `not_null_CRM_SEND_MEMBER_SEND_RESULT_NAME` = **1,401,419** = 문서20 **§M-5** 사전 미등재 17종 규모와 정확히 일치
>     ⇒ `05_4` `SEND_RESULT_NAME` 문안의 「사전에 라벨이 없는 행은 NULL」이 맞다.
>   · `not_null_CRM_EVENT_PARTICIPATION_PARTCPT_{STAT,PATH,CHNNL}_NM` = **각 2건** = 내가 실측한 오염값 `)` 2행과 일치
>     ⇒ 「라벨 NULL 이 GROUP NULL 보다 축마다 2 많다」는 관측이 독립 표면에서 재현됐다.
>   · `accepted_values_FACT_EVENT_PARTICIPATION_PART_STATUS` = **1건** = 오염값 `)` 1종(yml 은 미등재로 두고 warn 유지).
>     🟢 두 표면의 처방이 갈리지만 **모순이 아니다** — yml 은 검출을 유지하고 SV COMMENT 는 Analyst 에게 존재+오염을 알린다.
>
> ##### ⑫ 🟢 `store_failures` **효과 확인 — 설정이 아니라 물리로 확인했다**(P33 · O59-Q 잔여 해소)
> | 확인 항목 | 결과 |
> |---|---|
> | 컴파일 반영 | `manifest.json` `config.store_failures = True` · `config.schema = OPS` |
> | **물리 실재** | `GN_DW.OPS.WARN_GOLD_VIEW_COMMENT_COVERAGE` = **BASE TABLE** |
> | **이번 실행이 썼다** | `CREATED` 01:34:21.522 · `LAST_ALTERED` 01:34:23.209 = 이번 build 구간(매 실행 재생성) |
> | 행수 정합 | **0행** ↔ 테스트 `pass`·`failures=0` 과 일치 |
> | **열 구성** | `TABLE_NAME` · `VIEW_COMMENT_STATUS` · `COLS` · `COLS_NO_COMMENT` ⇒ **P207 이 요구한 3개 전부 + 분모(`COLS`)** |
>
> 🔴 **단 미실증 1건을 남긴다 — 「실패 행이 실제로 저장되는」 동작은 확인하지 못했다.**
> 이 테스트가 PASS 라 저장할 행이 없었고, **프로젝트에 `store_failures` 를 가진 테스트가 이 1개뿐**이라
> 형제 실증도 없다. 열 구성이 테스트 쿼리의 projection 이므로 **구조적으로는** 담기지만, 실행으로 본 것은 아니다.
> ⚠️ 「테이블이 생겼다」를 「실패를 남긴다」로 읽지 않는다(P33 의 한 단계 더 안쪽).
>
> ##### ⑬ 🔴🔴 **새 실측 — 경합 창은 우연이 아니라 구조다**(O59-Q 정황의 정밀화)
> O59-Q 는 *"마지막 뷰 생성과 테스트 START 가 **같은 초**"* 라는 **초 단위 정황**만 남겼다. ms 로 다시 쟀다:
> ```
> WIDE_MEMBER_MONTHLY  execute 완료 : 2026-08-11T08:34:21.063767Z
> warn_gold_view_comment_coverage START: 2026-08-11T08:34:21.082295Z   ⇒ 간격 18.5ms
> ```
> 🔴 **이번 실행에도 창이 있었다 — 오히려 더 좁다.** 즉 창은 그 build 만의 사정이 아니라
> **`WIDE_MEMBER_MONTHLY` 가 항상 마지막 모델이고 게이트가 곧바로 뒤에 오는 구조**에서 **매 build 재현**된다.
> ⇒ 같은(더 좁은) 창에서 **2회 중 1회만 재현** = 경합 가설은 **확증도 반증도 되지 않았고 간헐성만 확인**됐다.
> 🔴 **이것을 「재발 없음 = 해결」로 읽으면 안 된다** — 창이 상존하므로 다음 build 에서도 발생 가능하다.
> 처방(`store_failures`)이 여전히 옳은 이유가 여기 있다: 원인을 못 정한 채 **다음 발생을 특정 가능하게** 해 둔 것이다.
> 🆕 **P211**: **「같은 초」는 정황이고 「창의 폭」은 실측이다.** 경합을 논하려면 ms 단위로 창을 재고
> **그 창이 1회성인지 구조적으로 상존하는지**를 가른다. 초 단위로 보면 1회 사건처럼 보여 「우연」으로 닫히고,
> ms 로 보면 **매번 열리는 창**이 드러나 처방의 성격이 바뀐다(사후 규명 → 상존 위험 관리).
>
> ##### ⑭ 🟠 **P207 이 1개 테스트에만 적용됐다**(신규 발견)
> `store_failures` 를 가진 테스트는 **1개**다. 나머지 **WARN 32건은 여전히 「Got N results」만** 남긴다 —
> 실명은 `manifest` 대조로 알 수 있어도 **어느 행이 걸렸는지는 모른다**. P207 이 말한 결함이 32곳에 남아 있다.
> 🔴 이것이 ⑫의 미실증과 **같은 조치로 동시에 풀린다**: 소수 행 WARN(예: `PARTCPT_*_NM` 각 2건 ·
> `accepted_values PART_STATUS` 1건)에 `store_failures` 를 확장하면 ㉠ 저장 기제가 **실패 행으로 실증**되고
> ㉡ P207 이 실제로 이행된다. ⇒ 다음 build 대기 항목으로 등재(사용자 승인 필요 · R4-1).
>
> ##### ⑮ 🟢 build 후 SV 무결성 재확인
> build 가 GOLD 를 재적재한 **뒤에** 다시 쟀다: `sv_code_label_gate` 위반 **0**(차원 171 · 정보 1) ·
> `sv_unit_gate` 위반 **0** · 기지 부채 **0** · GRANT 누락 **0** ·
> 값 **불변**(38,470,780 / 1,031,971 · 1,134,126 / 1,134,126 / 407,223) ·
> **라벨 5축 동시 분해 합계 = 총계 일치**(fan-out 0 유지).
>
> ##### ⑩ ⬜ O59-R 이 남긴 것
> - 🟠 **Agent 버전업 미배포**(위 ⑧) — 파일 3종은 갱신됐고 `COMMIT` + `09_2` 실행이 남았다.
> - 🟠 **문서20 §M-1 확장 필요** — 종전 질의는 「`MS304` 영문 라벨의 한글 표기」였으나 실측으로 범위가 넓어졌다:
>   **참여경로·참여상태의 원천별 라벨 분화**(`homepage`/`홈페이지` · 영문/한글 계열)를 현업이 어떻게 통합해
>   부르는지 확인해야 라벨축 단독 응답이 가능하다. ⇒ 🟢 **§M-1-B 로 등재 완료**(회신 대기).
> - 🟢 **`store_failures` 효과 확인 — 완료**(위 ⑫) · **WARN 33 재발 없음**(위 ⑪).
> - 🟠 **[신규] `store_failures` 확장 + 저장 기제 실패행 실증** — 위 ⑭ · **다음 build 대기**.
> - 🟠 **[상존 위험] 게이트 경합 창 18.5ms** — 위 ⑬. 구조적으로 매 build 열린다. 재발 시 ⑫의 테이블에서 특정한다.

>
> #### 🟡 [2026-08-11 O59-Q] **DEC-35 3단계 — 라벨을 소비계층까지 전파** · ⛔ `dbt build` 대기
>
> **트리거** = 착수 순서 5번. 🔴 **초안의 「개명/분리」는 집행하지 않는다** — §23-F 결정 1 이 **(c) 확장판**
> (코드 raw + 코드군 + 라벨 3컬럼 세트)을 택했으므로 개명은 대체됐다. 남은 3단계 실질 = **소비처 전파 + 거짓 서술 회수 + 게이트**.
>
> ##### ① 🔴🔴 실측이 공백을 드러냈다 — **WIDE 가 라벨을 하나도 노출하지 않고 있었다**
> O59-N 이 SILVER·GOLD 에 26컬럼을 붙였지만 **소비뷰는 코드축만** 갖고 있었다(라이브 스캔):
> · `WIDE_SERVICE_EVENT` = `SEND_STATUS` + `SEND_STATUS2`(전건 NULL) 뿐 — 축A 라벨·축B 3컬럼 **전부 없음**
> · `WIDE_EVENT_PARTICIPATION` = `PART_STATUS`·`PART_PATH`·`PART_CHANNEL`·`EVENT_CATEGORY` 코드축 뿐 — `*_GROUP`·`*_NAME` **전부 없음**
> ⇒ **DEC-25 15-D(코드+라벨 병기)가 WIDE 층에서 미충족**이었다. 라벨을 GOLD 에만 붙이면 **소비계층은 라벨을 볼 수 없다** —
>   즉 1·2단계의 목적(현업이 라벨로 이해)이 달성되지 않은 상태였다.
> 🆕 **P204: 계층 파이프라인에서 「컬럼을 만들었다」는 최종 소비면에 도달했다는 뜻이 아니다.** 각 단계는 자기 층만 바꾼다 —
>   전파는 **층별로 따로 실측**해야 하고, 상류 완료 보고를 하류 존재의 근거로 쓰면 안 된다(P202 의 표면 논리와 같은 구조).
>
> ##### ② 🟢 조치 — WIDE 2뷰에 코드+라벨 병기 (모델 + yml 동시)
> · `WIDE_SERVICE_EVENT` +5(`SEND_STATUS_GROUP`·`SEND_STATUS_NAME`·`SEND_RESULT_CD`·`SEND_RESULT_GROUP`·`SEND_RESULT_NAME`)
> · `WIDE_EVENT_PARTICIPATION` +9(참여 3축 GROUP/NAME 6 + `EVENT_KIND_NAME` + `EVENT_CATEGORY_GROUP`/`_NAME`)
> · 🔴 **`EVENT_KIND_NAME` 이 WIDE 에 없던 것은 O58-C 와 같은 유형**이다 — 라벨이 옆에 있는데 코드축만 노출.
> · 사전 검증(R2-1 · build 전): 모델↔yml 컬럼 **순서·개수 62/62 정확 일치**(두 뷰 모두) — 불일치는 build ERROR 다.
>   ⚠️ `dbt parse` 는 프로필 자격증명 문제로 막혀(`'database' is a required property`) 자체 파서로 대조했다. yml 결함이 아니다.
> · 🔴 편집 중 **내가 `- name: PART_EVENT_BK` 항목명을 지웠다**(description 만 남아 yml 이 깨진 상태) — 즉시 복구했다.
>   교훈: 앵커에 인접 항목을 포함시켜 치환하면 그 항목이 소실된다.
>
> ##### ③ 🔴🔴 거짓이 된 서술 회수 (DEC-25 DoD) — **SV COMMENT 는 LLM 입력이라 가장 위험하다**
> | 위치 | 거짓 내용 | 조치 |
> |---|---|---|
> | `05_4_SV_DDL_SERVICE.sql:61` `fse.SEND_STATUS` | *"라벨 미배선 … 성공률·실패율은 **산출 불가**로 답하라"* | 폐기. 축A·축B 라벨 배선 사실 + **MSG_AT 한정 판정 가능** + EMAIL·SND 는 의도적 NULL 명시 |
> | `05_5_SV_DDL_EVENT_PARTICIPATION.sql:62` `event.EVENT_CATEGORY` | *"라벨 미배선"* + 🔴 **두 계열을 거꾸로 서술**(일반행사=소수·캠페인=백단위라 했으나 실측은 **반대**) | 둘 다 정정 — EVENT=100~500(MS286) · CRMN=1~16(MS002) |
> | `_wide_schema.yml` `EVENT_KIND` | *"온라인/오프라인"* — 🔴 **거짓**(실제 = `EVENT`/`CRMN` 원천 판별자) | 정정 + 판별자 역할 명시 |
> 🟢 **`DIM_SERVICE.SUBTYPE` 의 「라벨 미배선」은 회수하지 않았다** — 등급 D 라 **여전히 참**이다(라벨 창작 금지 유지).
>
> ##### ④ 🔴 자기적발 — **내가 회수 과정에서 새 거짓을 만들었다**
> SV COMMENT 에 *"라벨축으로 답한다"* 고 썼는데 **그 SV 에는 라벨축이 노출돼 있지 않다**(라벨축 노출은 4단계다).
> Agent 가 **존재하지 않는 차원명을 생성**하게 만드는 문안이고, 이것은 O58-C 사고의 거울상이다.
> 🔴 **게이트가 지목해 줬다** — `sv_code_label_gate` 정보 3건이 *"라벨 컬럼 부재 확인됨 — DEC-35 1~2단계 대상"* 이라고
> 정확히 그 축들을 가리켰고, 그 출력을 읽고 잡았다. ⇒ 두 문안을 **「이 SV 에는 아직 라벨축이 없다 · 없는 차원명 생성 금지 ·
> 라벨 집계는 WIDE 직접조회로 안내」** 로 교정했다.
> 🆕 **P205: 「거짓 서술을 회수한다」와 「기능을 구현한다」는 다른 작업이다.** 회수 문안이 아직 없는 기능을 있다고 말하면
>   회수가 **새 거짓을 만든다** — 회수 시점의 **실제 노출 상태**를 기준으로 쓴다.
>
> ##### ⑤ 🟢 판정 (물리)
> · SV 2종 **`CREATE OR ALTER` 재배포**(DDL 8 + 7 실행) · `sv_unit_gate` 통과(GRANT 누락 0 · COMMENT 수치 신규 유입 0 ·
>   폐기식 노출 0) · `sv_code_label_gate` 위반 0.
> · **값 불변 실측**: `SV_SERVICE.TOTAL_SEND_MEMBERS` **38,470,780** · `SV_EVENT_PARTICIPATION.TOTAL_PARTICIPANTS` **1,134,126**
>   = GOLD 팩트 행수와 정확 일치.
>
> ##### ⑥ 🟢 **build 후 물리 판정 완료** (2026-08-11 사용자 실행 · 보고 `PASS=385 WARN=33 ERROR=0 SKIP=0 TOTAL=418`)
> · **신규 14컬럼 전건 실재** — `WIDE_SERVICE_EVENT` **62컬럼**(신규 5/5) · `WIDE_EVENT_PARTICIPATION` **62컬럼**(신규 9/9).
> · **뷰 컬럼 COMMENT 커버리지 = 560/560 · 결손 0**(⚠️ **컬럼레벨만** 센 값이다 — 뷰레벨 COMMENT 는 별 표면이고
>   그것을 빠뜨린 것이 ⑦ ㉡ 의 결함이다) · yml↔라이브 문안 대조 **62/62 불일치 0**(두 뷰 모두)
>   🟢 **게이트 정본 SQL 로 재판정 = 0행 PASS**(뷰레벨 + 컬럼레벨 동시 검사) ⇒ 소비면까지 라벨이 도달했다(P204 충족).
> · **채움이 GOLD 팩트와 전건 일치**(분모 명시): WSE 38,470,780 → 축A 20,284,338 · 축B 코드 26,255,375 / 라벨 24,853,956 ·
>   WEP 1,134,126 → 상태 1,122,886/1,122,884 · 경로라벨 1,124,439 · 채널라벨 981,920 · `EVENT_KIND_NAME` **전건**.
>   🟢 **`EVENT_CATEGORY_NAME` 870,515 는 결손이 아니다** — `1,134,126 − 고아 263,611 = 870,515` **정확 일치**
>   (센티넬 `EVENT_SK=0` 행은 코드가 없어 조인 대상이 아니다) ⇒ 라벨 결손 **0**.
> · 게이트 전건: `audit_ddl_rule7` **4표면 전부 0**(컬럼 632/632 · SILVER 신규 유입 0 · GOLD 테이블 35/35 · SILVER 테이블 39/39) ·
>   `table_ddl_column_gate` 7/7 · `sv_code_label_gate` 위반 0.
>
> ##### ⑦ 🟢 **WARN 32 → 33 규명 완료 — 사용자가 build 로그 전문을 주자 즉시 특정됐다**
> `TOTAL` 은 418 불변이고 **PASS 1건이 WARN 으로 이동**했다. 33번째는 **`warn_gold_view_comment_coverage`**
> (로그 `418 of 418` · 07:56:22 · `Got 1 result, configured to warn if != 0`)다.
> 🔴 **내 규명이 한 차례 막혔던 이유 2건 — 둘 다 내 쪽 결함이다.**
> · ㉠ **artifact 를 실행과 대조하지 않았다**: 스테이지 `run_results.json` 은 **07:38:57 판**(386/32)이고
>   사용자 build 는 **07:56:22** 였다. 그 stale 판으로 warn 목록을 뽑아 「32건」 기준선을 세웠으니 33번째가 보일 수 없었다.
>   🆕 **P206: dbt 요약 수치와 artifact `generated_at` 을 항상 대조한다** — 다르면 그 artifact 로 회귀를 판정할 수 없다.
> · ㉡ 🔴🔴 **내 라이브 재평가가 이 테스트를 분모에서 빠뜨렸다**: 나는 「warn 등급 통과 테스트 26건 전량 평가 = 위반 0」
>   이라고 보고했는데, **커버리지 검증을 컬럼레벨(`INFORMATION_SCHEMA.COLUMNS`)로만** 쳤다. 이 테스트는
>   **뷰레벨 COMMENT(`INFORMATION_SCHEMA.VIEWS.COMMENT`)도** 검사한다 ⇒ 내가 「560/560 결손 0」이라 한 스캔은
>   **다른 것을 센 값**이었다. **P202 를 세운 세션에서 내가 P202 를 어겼다**(표면을 분모에서 누락).
>   ⚠️ 「형제 표면도 측정했다」며 쓴 `INFORMATION_SCHEMA.TABLES` 조회도 **`VIEWS` 가 아니다** — 같은 누락이다.
> ##### 🟠 원인은 특정하지 못했다 — 정황만 남긴다
> 테스트의 **정확한 SQL 을 그대로 라이브 재실행 = 0행(PASS)**. 즉 지금은 위반이 없다.
> 정황: 마지막 뷰 `WIDE_MEMBER_MONTHLY` 생성 완료(**07:56:20**)와 이 테스트 START(**07:56:20**)가 **같은 초**다.
> `depends_on` 14종은 **실행 순서**를 보장하지만 `INFORMATION_SCHEMA` **메타데이터 가시성 지연**은 막지 못한다
> ⇒ 「생성 직후 조회」 경합이 유력하나 **재현 근거가 1회뿐이라 단정하지 않는다**.
> ##### 🟢 처방 — 문장이 아니라 게이트로 고정했다
> `warn_gold_view_comment_coverage` 에 **`store_failures=true`** 를 켰다. dbt 요약·로그는 **행 수만** 남기므로
> 시점 의존 조건은 재실행하면 사라져 **영구 규명 불가**가 된다. 다음 발생 시 실패 행 테이블에서
> `TABLE_NAME`·`VIEW_COMMENT_STATUS`·`COLS_NO_COMMENT` 를 그대로 읽으면 된다.
> 🆕 **P207: warn 게이트는 「몇 건」이 아니라 「무엇」을 남겨야 한다.** 관측 게이트의 산출물이 카운트뿐이면
>   그 관측은 **재현 불가능한 알림**이다 — 특히 경합·시점 의존 조건에서.
>
> ##### ⑧ 🟠 잔여
> · 🟠 **`store_failures` 적용 확인은 다음 build 에서** 한다(설정을 넣은 것 ≠ 효과가 있는 것 · P33) —
>   실패 행 테이블이 생성되는지, 그리고 WARN 33 이 재발하는지 함께 본다.
> · 🟠 4단계 = **SV 라벨축 노출**(`sv_code_label_gate` 정보 3건이 대상) + SV COMMENT 부채 12건.
> · 🟠 **문서 기재 「GOLD 뷰 559/559」와 실측이 어긋난다** — 현재 라이브 **560**(내 +14 반영) ⇒ 변경 전은 546 이어야 하는데
>   문서는 559 라고 적고 있었다. 변경 전 스냅샷을 뜨지 않아(P147) 지금 재구성할 수 없다 ⇒ **다음 세션에 뷰 컬럼 수를 재실측**해
>   문서를 정정할 것(둘 중 어느 쪽이 stale 인지 단정하지 않는다).
>
>