<!-- SPLIT-CHUNK 01_세션이력.md | 049/049 | 허브 = 01_세션이력.md | 원문 1~176행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-29 O120] dbt→SV 계층 완결성 전수 대조 + 자기검토: 내가 종료코드를 거짓 발행했다
>
> **지시** = ㉠ 「dbt 파이프라인과 SV 까지 이어지는 데이터 흐름에 BRONZE·ML·`SILVER.BIGQUERY_REFINED_DATA`
> 기반 요소가 누락 없이 다 됐는가 · Agent 수정만 남았는가」 ㉡ 이어서 **「비판적 검토 + 전건 승인 + 개선」**.
>
> ---
>
> ### 1. 답 — 「Agent 수정만 남았다」는 성립하지 않는다
>
> 라이브 전수 실측 결과 **BRONZE·ML 축은 누락이 없고, bigquery_refined 축은 GOLD 에서 끊긴다.**
>
> | 축 | 경로 | 판정 |
> |---|---|---|
> | CRM | BRONZE 46 → SILVER 25 → GOLD 팩트+WIDE → SV 10종 | 🟢 완결 |
> | ERP(예산) | BRONZE 2 → SILVER 3 → `FACT_BUDGET`(+`_YEARLY`) → `SV_BUDGET` | 🟠 `DEC-44` 차단 |
> | AGENCY(광고) | BRONZE 4 → SILVER 8 → 팩트 4 + WIDE → `SV_AD` | 🟢 완결 |
> | **bigquery_refined** | `BRONZE_BIGQUERY` **0테이블**(우회) → `SILVER.BIGQUERY_REFINED_DATA` → `GA4_BASIC` → GA4 5종 → `FACT_GA_BEHAVIOR`·`WIDE_GA_BEHAVIOR` | 🔴 **SV 층 부재** |
> | ML | `ML` 16테이블 → SERVING 뷰 7종 → ML SV 7종 | 🟢 완결(스모크 7/7) |
>
> 🟢 **`BRONZE_BIGQUERY` 0테이블은 결함이 아니라 설계다** — 외부 Python 이 `SILVER.BIGQUERY_REFINED_DATA` 에
> 직접 적재하고 dbt 는 `source('silver_external', …)` 로 읽어 `GA4_BASIC` 에서 재파생한다
> (`DEC-37`·`DEC-38` · 근거 = `10_dbt_pipeline/models/silver/_sources.yml`·`ga4/GA4_BASIC.sql`).
> 🔴 **그러나 그 축은 SV 층에 도달하지 않는다** — 라이브 SV 17종에 GA base 가 **0** 이므로
> Agent 는 GA 를 의미계층으로 답할 수 없고, 현 처방은 `SV_SERVICE` COMMENT 가 이미 발행한
> **`WIDE_GA_BEHAVIOR` 직접 조회 안내**다.
>
> 🟢 **SV 없는 나머지 GOLD 팩트 3종은 전부 설명된다**(누락 아님):
> · `FACT_TARGET_BIZ` **0행** = `CRM_BIZ_TARGET` 이 `WHERE 1=0` **스키마-only 빈 모델**(원천 입고 대기 E-6)
>   ⇒ 🔴 초판은 이것을 **근거 없이** 「적재 대기 = 정상」이라 단정했고, 자기검토에서 모델 원문으로 확증했다.
> · `FACT_BUDGET_YEARLY` = `DEC-44` 축(`SV_BUDGET` base 는 월 팩트)
> · `FACT_AD_BROADCAST_CASE` = `WIDE_AD_COMBINED` 가 **의도적 제외**(1:N 위성 · 사례 수만큼 광고비 복제)
>
> ---
>
> ### 2. 🔴🔴 최대 자기결함 — 게이트 종료코드를 `tail` 로 파이프해 읽었다
>
> 초판은 도메인 게이트를 `for g in …; do python3 scripts/$g.py | tail -6; echo "-- exit=$?"; done` 으로
> 돌리고 **그 `exit=0` 을 게이트 판정으로 보고**했다. 🔴 **그것은 `tail` 의 종료코드다.**
> 파이프 없이 재측정하니 **16종 중 rc=1 이 4건**이었다:
>
> | 스크립트 | rc | 성격 |
> |---|---|---|
> | `audit_ddl_rule7` | 1 | 규칙7 위반(오탐 3 + 진짜 1) — **주제의 정면** |
> | `table_ddl_column_gate` | 1 | DDL↔모델 순서 드리프트 |
> | `merge_check` | 1 | 임시파일 6건 미이관(승계) |
> | `o54_sv_value_gate` | 1 | **크래시**(`IndexError`) — 애초에 무인자 게이트가 아니다 |
>
> ⇒ 🔴 **판정식 = 종료코드는 파이프 없이 재라.** 초판이 발행한 「도메인 게이트 14종 exit 0」은
> **분모(14≠16)와 판정(전건 0≠4건 rc=1) 둘 다 거짓**이었다. 원장 §1 행을 그 자리에서 정정했다.
> 🔴 이것은 `R3-9`(게이트가 보지 않는 축)의 변종이다 — **게이트를 돌렸지만 결과를 읽는 계측이 깨져 있었다.**
>
> ---
>
> ### 3. 🔴🔴 두 번째·세 번째 자기결함 — 분모 미달(4세션 연속) 그리고 그 뒤의 오단정
>
> **㉠ 분모 미달** = `ls scripts/` 를 분모로 삼았다면서 SV 축 도구 **`sv_dim_cardinality` 를 1차에 안 돌렸다.**
> O118-B·O119·O119-B 에 이어 **4세션 연속** 같은 축에서 결함이 났다.
>
> **㉡ 그런데 그것을 돌린 뒤 더 나쁜 판정을 했다** — 이 도구가 「열거 없음 25(임계 5)·52(임계 20)」를
> 내는데 `sv_code_label_gate` 는 「열거누락 16」이므로 **「같은 규칙을 두 게이트가 다르게 잰다
> (`▣ZZZ6 ㉤`) 재발」로 사용자에게 보고했다.** 🔴 **틀린 판정이었다.**
> 판정식을 재현하니 두 수는 **분모가 다르고 둘 다 옳다**:
> · 게이트 = `is_enum_target()` 이 **VARCHAR/CHAR/TEXT/STRING 만** 열거 의무 대상으로 본다
>   (NUMBER 는 자기설명적 · DATE 는 자명 · `ENUM_SKIP` 수동 제외) — 근거 = `sv_code_label_gate.py:139-162`
> · 도구 = 임계 이하 **전 타입** 차원을 센다(관측 축이고 판정이 아니다)
> ⇒ 차이는 BOOLEAN(`HAS_*` 플래그)·NUMBER(피처값)·DATE 이며 **열거 의무가 없는 것들**이다.
>
> 🔴 **신설 판정식 = 「불일치」도 오탐일 수 있다.** `▣ZZZ6 ㉡` 는 「FAIL 도 오탐일 수 있다」였고
> 이것은 그 **역방향**이다 — 두 수가 다르면 먼저 **두 판정식의 자격 조건**을 읽어라.
> 🟢 **재발 방지를 코드에 심었다** = `sv_dim_cardinality` 출력 말미에
> 「이 표는 관측 축이고 판정 정본은 `sv_code_label_gate` 다 · 이 수를 게이트 위반 건수로 인용하지 마라」를
> 출력하게 했다(주석에 O120 오독 경위 포함).
>
> ---
>
> ### 4. 🟢 실질 개선 — ML SV 7종 스모크(사상 최초) · 게이트 오탐 3건 종결
>
> **㉠ ML 축을 파일이 아니라 라이브로 실증했다.** 초판은 `21_ML_SERVING_뷰_DDL.sql` 을 grep 해
> 「16테이블 전건 편입」을 단정했는데 그것은 **파일 주장**이다(`▣ZZZ6 ㉣`). `INFORMATION_SCHEMA.VIEWS`
> 의 라이브 뷰 정의로 대조해 **16/16 일치**를 확인했다(`CMPGN_LTV`·`UCMPGN_LTV` 는 forecast·score 2뷰가 공유).
>
> **㉡ ML SV 7종 스모크 = 7/7 PASS · fan-out 0.** 🔴 **O118-B 가 배포한 ML 축은 그때까지 한 번도
> 스모크되지 않았다** — O119-B 의 「6/6」은 **실적 SV 재배포분**이었다(§0.1 DoD 공백).
> 판정은 행수 교차검증 + 의미 불변식으로 세웠다:
> · `SV_ML_MEMBER_RISK.PREDICTED_MEMBERS` == base 뷰 `COUNT(DISTINCT MBER_NO)`
> · `SV_ML_SPONSOR_RISK` 후원건수·회원수 **둘 다** base 와 일치(grain 이 후원건이므로 양축 필요)
> · `SV_ML_DVLP_FORECAST` 계열유형 5종의 **계열수 합 × 예측월 = base 행수**
> · `SV_ML_LTV_FORECAST`·`LTV_SCORE` 유형 2종 각 50계열 · `SV_ML_FEE_FORECAST` 카테고리 × 예측월 = base
> · `SV_ML_FEATURE_IMPORTANCE` = **분석유형별 기여도 합이 정확히 1** ⇒ COMMENT 의 주장이 라이브에서 성립
> 🔴 **수치는 이력에도 최소만 적는다** — 재현은 조회로 한다(`R2-8-4`).
>
> **㉢ `audit_ddl_rule7` 오탐 3건 종결 + 음성 테스트 신설.**
> · 서수형 라벨 접미 2건 = `CMPGN_TYPE1_NM`·`CMPGN_TYPE2_NM` COMMENT 의 「캠페인 **유형1명**」이
>   소규모 패턴 `[0-9]+\s*(?:행|건|명|원)` 에 「1명(사람)」으로 잡혔다 ⇒ `WHITE` 에 `유형[0-9]+|타입[0-9]+`
>   추가(기존 주석의 「타입은 수치가 아니다」 원칙 그대로).
> · 월↔연 환산 상수 1건 = `FACT_BUDGET_YEARLY` COMMENT 「월 grain 에 연 총액을 담으면 **SUM 이 12배**」는
>   **달력 상수**이고 적재량과 무관하다 ⇒ `SUM\s*이\s*\*{0,2}12배` 만 면제(맨 「12배」는 여전히 잡힌다).
> · 결과 = **신규 유입 3→0 · 테이블레벨 위반 2→1 · GOLD 컬럼 위반 12→5.**
> · 🟢 **`scripts/test_audit_ddl_rule7.py` 신설 = 28단정 5축** — 축1 오탐 제거 · 축2 무력화 아님 ·
>   **축2-B 선재 공백 명시** · 축2-C 상수 면제와 무력화 아님 · 축3 `NUM_EXEMPT` 회귀 · 축4 `WHITE` 회귀.
> 🔴 **잔여 1건은 진짜 위반** = `SILVER.GA4_EVENT` 테이블 COMMENT 의 「실측 9.6%」
> ⇒ `P107` 대로 **문서10 §26-B 로 수치를 옮긴 뒤** 제거해야 하고, 파일만 고치면 `comment_drift_gate` 가
> 드리프트로 잡으므로 **라이브 COMMENT 도 함께** 고쳐야 한다(미실행 · 다음 세션).
>
> **㉣ `o54_sv_value_gate` 무인자 크래시 시정** — 사용법 출력 + **exit 2**(0=통과·1=위반과 구별).
> 부수로 **잠재 argv 버그**도 고쳤다: 비교 경로가 `diff(argv[2], argv[3])` 여서 2인자 호출이
> 성립하지 않았다(호출자 0건이라 무영향 확인 후 `argv[1], argv[2]` 로 교정).
>
> ---
>
> ### 5. 🟢 게이트 FAIL 2건은 오탐임을 재현으로 확정했다 (`▣ZZZ6 ㉡`)
>
> **㉠ `table_ddl_column_gate` 「2/7 불일치 — `dbt build` 하지 말 것」 = 오탐.**
> 그 경고문은 *"append 는 위치로 INSERT 한다 ⇒ 값이 다른 컬럼에 들어간다"* 인데,
> dbt 가 **실제로 실행한 SQL** 은 `insert into T ("A","B",…) (select "A","B",… from T__dbt_tmp)` 로
> **양측 모두 이름 지정**이다(`10_dbt_pipeline/logs/dbt.log:314-316`) ⇒ 위치 오삽입은 **원리적으로 불가**.
> 🟢 남는 것은 DDL 파일 ↔ 모델 SELECT 의 **순서 드리프트**(문서 축)이고 라이브는 DDL 파일 순서와 같다.
> 🔴 **게이트 경고 문안 자체가 사실과 다르므로** 다음 세션이 이 문안을 근거로 모델을 고치면 안 된다.
>
> **㉡ `audit_ddl_rule7` 「신규 유입 3건」 중 2건 = 오탐**(위 4-㉢).
>
> ---
>
> ### 6. 🔴 내 테스트가 선재 검출 공백을 찾았고, 고치지 않기로 판단했다
>
> 축2 를 쓰다가 「유형 3건**이** 실재한다」가 **검출되지 않는 것**을 발견했다.
> 원인 = 소규모 패턴이 `…(?:행|건|명|원)\b` 로 끝나는데 **한글 조사(이·은·는·을·의·에)가 `\w` 에 속해**
> 단위어 뒤에 조사가 붙으면 `\b` 가 성립하지 않는다 ⇒ 「3건이」·「504행에」·「28건이」를 놓친다.
> 🔴 **내 편집과 무관한 선재 결함**이다.
>
> **고치기 전에 영향을 실측했다**(`scripts/_o120_probe_boundary.py` · 임시 · 판정 후 삭제):
> 정본 DDL COMMENT 2,741건에서 `\b` → `(?![0-9])` 로 바꾸면 검출 **6 → 16(+10)** 이고 그 10곳은
> · **진짜 위반 5** = `499행`(smart tv) · `3건`(검출된 3건) · `8행`(미매칭) · `28건`(공동후원 쌍) · `6건`
> · **신규 유형 오탐 5** = `1행사`(1행+사) · `3원천`(3원+천) · `2-2 원천`(절 참조) · `1명당`(grain 정의)
> ⇒ **신규 예외 4종이 필요**하다.
>
> 🔴 **그래서 넓히지 않았다** — `▣ZZZ6 ㉦`(예외를 넓힐 때는 무력화 아님을 음성 테스트로 함께 단정하라)를
> 이 세션 예산 안에서 4종 전부에 대해 이행할 수 없었다. 🟢 **대신 공백을 비가시화하지 않게 했다**:
> **테스트 축2-B 가 「현재 이렇게 동작한다」를 명시 단정**하고, 주석에 영향 실측·필요 예외·미시정 이유를
> 적었다 ⇒ **고치는 세션은 이 축이 FAIL 하므로 반드시 그 주석과 함께 갱신하게 된다.**
>
> ---
>
> ### 7. 🟢 문서 개선 — GA 유예 근거가 stale 이었다
>
> 초판이 「`SV_GA` 미배포는 결손 아님(§7 Phase-2 선언)」을 인용했는데, **그 §7 의 근거를 재보니 절반이 거짓**이었다.
> · ~~「GA4 **1일 샤드만**」~~ ⇒ 🔴 **거짓** — 라이브 `SILVER.GA4_EVENT` 는 **다수일**을 담고 있다
>   ⇒ 「샤드가 하루뿐이라 SV 를 못 만든다」는 논거가 **소멸**했다.
> · 🟢 「identity 커버리지가 낮다」는 **유효**하나 **수치도 stale** 이었다(재측정치는 조회로 확인).
> · 🔴 단 「전기간」은 아니므로 **Phase-2 유예 자체는 성립할 수 있다** — 문제는 **근거가 틀렸다는 것**이다.
>
> 고친 곳(전부 **취소선 + 실측 병기** · O112 §C24 대로 삭제하지 않았다):
> · `04_SV_설계.md` **§7 머리말** — 취소선 + 실측 반박 + **재측정 쿼리 3종 삽입**(수치를 문서에서 읽지 않게)
>   + **「SV_GA 신설 여부 = 사람 결정」 상신**
> · **§7.5 Instruction 재작성** — 초안 문안이 ㉠ 「1일 샤드만」 거짓 ㉡ **수치를 SV COMMENT 에 박아 규칙7 위반**
>   이었다 ⇒ **수치 없는 대체 문안**을 제시(그대로 배포하면 `sv_unit_gate` 가 즉시 잡는다)
> · **§0.9-B 하단** — 「§7 기재는 유효하다」에 취소선 + 반박 · **ML SV 스모크 7/7 결과 추가**
> · **§0.9-C 축③ 표의 `SV_GA` 행** — 🟢→🟠 · 「입고 대기로 설명하지 마라」 명시
>
> ---
>
> ### 8. 부수 확인 · 잔여
>
> 🔴 **계정 기재 stale**(`P169` 계열) — 문서 `NX55103` ↔ 라이브 `CURRENT_ACCOUNT()` = **`YQ47212`**.
> 🟢 단 **배포 상태는 연속**이다: SV `created_on` 이 O118 기록과 일치하고 ML SV 는 O118-B 기록과 일치한다
> ⇒ 이번은 「상태 무효」가 아니라 **계정 식별자 기재만 낡은 경우**다(그 구분을 원장 행에 적었다).
>
> 🟠 **잔여**(성격 불변) = `DEC-44` 회신 · **GA SV 신설 결정(신규 상신)** · `04_SV_설계.md` 하위 4 SV + ML 7종
> 구조 절 결손 · `sv_code_label_gate` 열거누락 16(advisory) · DDL↔라이브 대조 미독 `05_4`·`05_5`·`05_8`·`05_9` ·
> §9 Cortex Search 카디널리티 재검증 · Agent 2종 배포(`R4-4-3`) · `audit_ddl_rule7` 진짜 위반 1건(`P107` 이관) ·
> 규칙7 조사 경계 공백(위 6) · `merge_check` 임시파일 6건 · `99_NEXT` 허브 용량(은퇴 필요).
>
> 🟢 **절차** = 라벨 `id_collision_gate --next O` 실측 후 원장 §1 **선점 등재**(O112~O120 **10세션 연속**) ·
> 원장 행 **1,899자**(상한 이내) · `--expect` 신선도 토큰 부착 재발행 · `read` 미반환 **0** / 재호출 **0** ·
> 🔴 **자기결함 = 위 2·3(계측 파이프 · 분모 미달 · 오단정) 3건**이며 전부 **사용자 보고 후 자력 정정**했다.

> #### 🟢 [2026-08-30 O121] 인수인계 4건 기록 + Agent 스펙 객체명 실재 게이트 축 신설 — 그리고 전 세션 시정이 분모 미달이었다
>
> **지시** = ㉠ O120-B 가 손으로 찾은 **4건**(Agent 스펙이 부재 객체를 원천으로 발행)을
> **원장·이력에 기록** ㉡ **`description` ↔ 라이브 객체명 게이트 축 신설**(공백)
> ㉢ 이어서 §3 ①~⑦(retire_sections 수리 · `audit_ddl_rule7` 진짜 위반 1건 · 규칙7 조사 경계 등).
>
> ### 1. 🔴🔴 최대 발견 — 「4건 모두 고쳤다」가 부분만 참이었다 (잔여 1건 실측 적발)
>
> 인수인계는 「4건 모두 실제 경로로 고쳤고 게이트 4종 전건 rc=0」이라고 적었다. 🔴 **전문은 정본이 아니므로**
> (`R1-3-7` 대체 근거 금지) 라이브와 파일로 **직접 재측정**했고 그 결과가 인수인계와 달랐다:
>
> | 참조 | 라이브 실측 | O121 착수 시점 상태 |
> |---|---|---|
> | `GN_DW.BRONZE_GA4` ×2 (`AGENT_MARKETING`) | `SCHEMATA` 에 **부재** | 🟢 **부정 서술로 전환됨** — 위반 아님 |
> | `GOLD.FACT_AD_COMBINED` ×2 | 부재 · 실제 base = `GOLD.WIDE_AD_COMBINED`(VIEW) | 🟢 잔량 **0** |
> | `BRONZE_BIGQUERY.events_YYYYMMDD` | 그 스키마 객체 **0** | 🔴 **`AGENT_OVERALL` 에 1건 잔존** |
>
> 🔴🔴 **잔존 1건의 위치가 원인을 말해 준다** — `tool_spec.description`(고쳐짐)이 아니라
> **`instructions.system`** 에 있었다 ⇒ **전 세션 분모가 「description」이었고 스펙에는 그보다 넓은 표면이 있다**
> (실측 = 3스펙 합계 문자열 표면 **201개** · 200자 이상 대형 표면이 `instructions.system`·`orchestration`·
> `response`·`tool_spec.description`). 🟢 시정 = 그 참조를 실제 경로(외부 Python →
> `GN_DW.SILVER.BIGQUERY_REFINED_DATA`)로 교체 · YAML 파싱 OK · 도구 수 **7/11/8 불변** · `line_len` PASS.
> 🔴 **판정식(신설) = 「고쳤다」의 분모도 게이트로 재라.** 손으로 고친 세션은 자기가 본 표면만 고치고
> 「전건」이라고 쓴다 — O119-B 의 「형제 표면이 또 있는지 물어라」가 **한 겹 더** 필요했다.
>
> ### 2. 🟢 게이트 축 신설 — `scripts/agent_object_ref_gate.py` (판정 5축) + 음성 테스트 12/12
>
> 인수인계가 지목한 공백을 메웠다. 🔴 **분모는 `description` 이 아니라 스펙 파일 전문**이다.
>
> · 축 = **① 🔴 스키마 실재** · **② 🔴 객체 실재**(같은 이름이 다른 스키마에 있으면 **「스키마가 틀렸다」로
>   소재를 알린다**) · **③ 🟠** 빈 스키마를 부정 서술 없이 원천 발행 · **④ 🟠** 미수식 UPPER_SNAKE 토큰
>   (라이브 미포함 **6건**) · **⑤ ⚪ 관측** 표면 인벤토리. ①② blocking · ③④ advisory · ⑤ 판정 아님.
> · 🔴 ① 의 스키마 후보를 **라이브에서 뽑지 않고 「모양」으로** 잡았다 — 라이브를 분모로 쓰면 `BRONZE_GA4` 가
>   애초에 후보에 안 들어와 **0건으로 침묵**한다(`§C24 ㉠`).
> · 🔴 ② 에서 **`SV_*` 를 제외**했다 — SV 는 `INFORMATION_SCHEMA.TABLES` 에 없어(O118 판정식) 전건 거짓 위반이
>   된다. SV 는 `sv_identifier_gate` ① 소관 ⇒ **같은 것을 두 곳에서 다르게 재지 않는다**(`R3-9 ㉡` 선제 준수 ·
>   그 사유를 게이트 출력에 인쇄해 다음 세션이 「불일치」로 오단정하지 않게 했다).
> · 🔴 ③④ 를 blocking 으로 두면 컬럼명·코드값·지표명이 쏟아진다(`sv_identifier_gate` ③ 과 같은 절충).
> · 🟢 **⑤ 가 이번 결함의 재발 방지 장치다** — **분모를 숫자로 보여준다** ⇒ 「어느 표면을 봤는가」를 눈으로 센다.
> · 🟢 실행 = **rc=0**(① 검사 70 · ② 검사 20 · SV 제외 26 · 부정서술 면제 각 3).
>   🔴 **그 PASS 를 근거로 「없다」고 쓰지 않았다** — 이 게이트는 **처음 돌린 순간 PASS** 였고 그때 실제 오기가
>   1건 있었다 ⇒ 「0건」이 「없다」인지 「판정식이 못 본다」인지 PASS 로는 구별되지 않는다(`§C24 ㉠`).
> · 🟢 **음성 테스트 `test_agent_object_ref_gate.py` = 12/12** · 면제 **3종 전부 양방향**(`▣ZZZ6 ㉦`) =
>   부정 서술 · `SV_*` 제외(같은 스키마의 비-SV 오기는 FAIL) · `KNOWN_ABSENT` — **셋 다 빼면 FAIL**.
> · 🔴🔴 **축⑥ 이 이번 실사고를 그대로 재현한다** = 같은 오기를 `instructions.system` 에 둔 경우와
>   `tool_spec.description` 에 둔 경우를 **양쪽 다 검출**하는지 단정 ⇒ **분모가 다시 좁아지면 FAIL 한다.**
> · 축⑪ **중복 계상 금지**(①②가 두 번 세지 않는지) · 축① 과탐 아님 · 축⑨⑩ advisory 종료코드 무영향 ·
>   축⑫ 분모 가시화 생존.
>
> ### 3-B. 🟢 `audit_ddl_rule7` 진짜 위반 1건 종결 (게이트 rc=1 → rc=0) — 사용자 지정 후속
>
> 대상 = `SILVER.GA4_EVENT` **테이블레벨** COMMENT 의 「(2025-06 실측 **9.6%**)」 = 원천 PK 중복률이며
> **재적재로 바뀌는 실측치**다(진짜 규칙7 위반 · 오탐 아님).
>
> · 🔴 **착수 시점 파일↔라이브는 문안 완전 일치**였다 ⇒ 드리프트가 아니라 **양쪽이 같이 위반 중**이었다.
> · 🟢 `P107` 순서대로 **지우기 전에** 문서10 **§26-B 표 #17** 로 verbatim 이관(그 표의 관례에 맞춤) ·
>   판정 스냅샷 병기(SILVER 테이블레벨 블록 **39→43** · GOLD **35→37 · 위반 0** — O59-P 판정은 그 시점 값이다).
> · 🟢 정본 DDL `04_silver_design/08_SILVER_테이블DDL_20260714.sql` **L1180** 수치 제거 +
>   **라이브 `ALTER TABLE … SET COMMENT` 동시 시정** ⇒ 문안은 「재는 방법」으로 대체했다(수치 대신 조회 지시).
> · 🟢 **판정** = `audit_ddl_rule7` **rc=1 → rc=0**(테이블레벨 **1→0** · SILVER 정본 DDL 신규 유입 **0** 유지) ·
>   `comment_drift_gate` rc=0 · `test_audit_ddl_rule7` 회귀 **rc=0** · 파일↔라이브 **SHA256 일치** · 「9.6」 잔량 **0**.
>
> 🔴🔴 **부수 적발 — 인수인계의 근거가 틀렸다(결론은 옳았다).** 인수인계는 「파일만 고치면
> `comment_drift_gate` 가 드리프트로 잡는다」를 근거로 「둘을 함께 고쳐라」를 지시했다. 그 게이트를 돌려 출력을
> 읽으니 **분모가 전부 컬럼 COMMENT** 다(GOLD 테이블 **743** · SILVER 테이블 **830** · GOLD 뷰 **575**) ⇒
> **테이블레벨 COMMENT 는 어느 드리프트 축에도 없다** — 파일만 고쳤다면 게이트는 **🟢 를 내면서** 드리프트가
> 생겼을 것이다. 🟢 O121 은 라이브를 함께 고치고 **SHA256 으로 직접 대조**해 게이트에 의존하지 않았다.
> 🔴 **판정식 = 「그 게이트가 잡아 준다」를 근거로 삼기 전에 그 게이트의 분모를 출력에서 읽어라** — 이 세션에
> **같은 유형의 분모 공백이 3건** 나왔다(Agent 스펙 표면 · 드리프트 테이블레벨 · O59-P 판정 시점).
> 🟠 **미시정 = 그 게이트에 테이블레벨 축 추가**(음성 테스트 동반) · ⚠️ 파생 산출물
> `30_output_share/20260826_아키텍처 지도 요약본.html` 은 옛 문안 보유(생성기 `gen_arch_map.py` 재실행으로
> 갱신 · **손으로 고치지 말 것**).
>
> ### 3-C. 🔴🔴 인수인계 최우선 항목의 전제가 재현되지 않았다 — `retire_sections` 는 고장이 아니다
>
> 인수인계 §3 ① 은 **최우선**으로 「`test_retire_sections.py` = **rc=1**(축2-d/e/f/g·축6-a~e·축8-d·축5-b) ·
> `--apply --force` 를 줘도 본문이 원본에 남고 쓰기 단계가 중단된다」를 지시했다.
>
> 🔴 **재현되지 않는다**(전부 파이프 없이 종료코드 수신) = ㉠ `test_retire_sections.py` **rc=0** ·
> 출력 **「단정 62건 통과 · 실패 0건」 · 「10축 통과」**(무라벨·`SESSION_LABEL=O121` **둘 다**)
> ㉡ 음성 테스트 **19종 전건 순회 rc≠0 = 0건** ㉢ `retire_sections.py --src 99_NEXT_SESSION.md --list
> --closed-only` **rc=0** 으로 정상 동작 + **열림 신호 판정 출력**(`R1-6-24` 가드 생존) · 무인자는 **exit 2**.
>
> 🔴 **그래서 도구를 고치지 않았다** — 재현되지 않는 실패를 「수리」하면 **동작하는 코드를 바꾸는 것**이고,
> 이 워크스페이스는 그 반대 방향으로 이미 손실을 봤다. **승인받은 지시라도 전제가 틀리면 되물어라**(`▣ZZZ6`)
> ⇒ **사용자 판단 대기**. 🟠 다음 세션이 볼 것 = O120 이 rc=1 을 본 조건(작업 디렉터리 · `_archive` 슬롯 ·
> 미커밋 상태). 🔴 **그 조건을 특정하지 못하면 「수리 완료」로 닫지 마라** — 지금은 「**재현 불가**」이고
> 「**정상 확인**」과 다른 상태다. 🟢 부수 효과 = 허브 용량 처방(절 은퇴)이 **열려 있다**(적용은 `R4-4-3` 승인 ·
> `--list` 는 허브에서 본문을 0 B 로 보므로 **`--src` 로 조각을 지정**해야 실제 후보가 나온다).
>
> ### 4. 🔴 착수 절차 · 승계 정정
>
> · 라벨 = `id_collision_gate --next O` 실측(정의·참조 최대 **120**) ⇒ `O121` · 원장 §1 **선점 등재**
>   (O112~O121 **11세션 연속**) · `--expect` 신선도 단정 · 행키·줄길이·유형·제목·좌표·census 게이트 rc=0.
> · 🔴 **브리핑 §2 가 뽑은 현행 인수인계 절은 `§0-ZZZ`(O119 시점)로 stale 이었다** — O120 이 허브 여유 부족으로
>   **§0-AAA 를 쓰지 못했기** 때문이다(🔴 O120 은 그 원인을 `retire_sections` **고장**으로 적었으나 위 3-C 대로
>   **재현되지 않는다**). 사용자 지시대로 **정본을 이력 §O120 으로 잡고** 전량 독해했다(178줄).
> · 음성 테스트 분모 = `ls scripts/test_*.py` 실측 **착수 18종 → 종료 19종** · 🔴 **종료코드는 리다이렉트로
>   받았다**(이 세션에 `| tail` 뒤 `$?` 를 읽은 곳은 **0회** · O120 처방 준수) · `read` 미반환 **0** / 재호출 **0**.
> · 🟠 원장 행이 **2,336자로 상한을 넘어 두 번 압축**했다(→ **1,971자**) · 이력 조각이 **305→300줄 상한**에 걸려
>   `--rebalance` 대신 **내 항목을 압축**해 피했다(O111 선례 · `R4-4-3` 승인 회피).
>
> ---
>
> ### 5. 🟠 잔여 · 다음 세션
>
> · 🔴 **`DEC-44` 무영향** — 예산 `metric`·`where`·차수 필터·`severity` 를 **하나도 만지지 않았다**.
>   `AGENT_MARKETING.analyst_budget` ↔ `SV_BUDGET`(+79.7% 과대) 배선은 **그대로**이며 사람 결정 대기다.
> · 🔴 **Agent 배포 미실행**(`R4-4-3` 별도 승인 · 선행조건 = `DEC-44`) · 계정 Agent **0** 불변.
> · 🟠 미착수 승계 = ~~`audit_ddl_rule7` 진짜 위반 1건~~ 🟢 **[O121 종결 · 3-B]** ·
>   ~~`retire_sections.py` 수리~~ 🔴 **[O121 재현 불가 · 3-C · 사용자 판단 대기]** ·
>   규칙7 조사 경계 공백(신규 예외 4종 + 음성 테스트) · `04_SV_설계.md` 구조 절 결손(4 SV + ML 7종) ·
>   열거누락 16(advisory) · DDL↔라이브 미독 `05_4`·`05_5`·`05_8`·`05_9` · `merge_check` 임시파일 6건 ·
>   `99_NEXT` 허브 용량(🟢 3-C 대로 절 은퇴 경로가 열려 있다).
> · 🆕 🟠 **O121 신규 등재 = `comment_drift_gate` 에 테이블레벨 COMMENT 축이 없다**(3-B 부수 적발) —
>   음성 테스트를 동반해 추가할 것. 🔴 그때까지 **테이블 COMMENT 를 고치면 라이브를 손으로 대조하라.**
> · 🟢 임시 도구 `scripts/_o121_tx.py` **승인 삭제**(잔량 0) · 오배치 `20_issue/_o121_entry.md` 도 루트 사본
>   (SHA256 동일) 확보 후 **승인 삭제**(`doc_type_gate` FAIL→PASS).
