<!-- SPLIT-CHUNK 01_세션이력.md | 011/025 | 허브 = 01_세션이력.md | 원문 2496~2783행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-11 O59-P] **build 후 물리 판정 — O59-N ⑥ 전건 재현 · 문서 stale 3건 회수**
>
> **트리거** = 사용자 `dbt build` 실행(`PASS=386 WARN=32 ERROR=0 SKIP=0 TOTAL=418` · `run_results.json` `generated_at` 2026-08-11T06:52:25Z).
> 🔴 **O59-N ⑥ 에 같은 수치가 이미 적혀 있었지만 그것을 근거로 쓰지 않고 전부 재스캔했다**(P33 — 문서는 완료의 근거가 아니다).
>
> ##### ① 🟢 인수인계의 「깨진 항목」은 실재하지 않았다 — 문서만 stale 이었다
> 인수인계 `99_NEXT_SESSION.md:468` 이 *"`SV_DEV_ACHIEVEMENT` 는 현재 깨져 있다 — `CREATE OR ALTER` 재배포가 즉시 필요"* 라고
> 적고 있었다. **라이브 확인 결과 거짓이다.**
> · SV 실존(`SHOW SEMANTIC VIEWS IN GN_DW.SERVING` = **9종** · `created_on` 2026-08-10 17:19:14)
> · base = `GOLD.FACT_DEV_ACHIEVEMENT` · 스모크 `TOTAL_GOAL_CNT` **4,622,103** / `TOTAL_ACTUAL_CNT` **2,291,878** = **O54 ② 기대치 정확 일치**
> 🔴 **같은 표 칸의 ① 도 거짓이었다** — *"SV 가 여전히 `SERVING.DIM_MONTH`·`DIM_MEMBER_CURRENT`·`FACT_AD_COMBINED` 를 참조"*
> ⇒ `INFORMATION_SCHEMA.SEMANTIC_TABLES` 전수 = **논리테이블 32/32 전건 `BASE_TABLE_SCHEMA=GOLD` · `SERVING` 참조 0**(O54 ③ 이 이미 끝냈다).
> ⇒ 회수 3곳(`99_NEXT_SESSION.md` **468**(2건) · **521**(SV 7종→9종) · **522**(「SV 미배포 2종」)).
> 🆕 **P201: 인수인계의 「깨져 있다·미배포」류 부정 단정도 실측 대상이다.** 완료 기록은 P137 로 검증하는데
>   **미완 기록은 검증 없이 신뢰**하고 있었다 — 방향만 반대인 같은 결함이고, 이쪽은 **하지 않아도 되는 재배포를 하게 만든다**
>   (이번에는 `CREATE OR ALTER` 를 헛으로 실행할 뻔했다). ⇒ 착수 전 첫 동작은 **대상의 라이브 상태 확인**이다.
>
> ##### ② 🟢 build 가 라이브 COMMENT 를 덮지 않았다 (P199 유지 확인)
> O59-M 이 라이브 48컬럼을 `ALTER … COMMENT` 로 동기화해 뒀으므로 **build 가 되돌렸는지**가 실질 위험이었다.
> · GOLD 기본테이블 컬럼 COMMENT **632/632 채움 · 결손 0**
> · **파일↔라이브 문안 대조 = 632/632 완전 일치**(파일에만 0 · 라이브에만 0 · 문안 불일치 0)
> 🟠 이 대조는 **전용 게이트가 없어 애드혹**이었다(O59-M 도 그랬다) ⇒ 게이트화 후보로 등재.
>
> ##### ③ 🟢 신설 26컬럼 — 실존·배치규약·채움 전건 재현
> · 물리 실존 = SILVER 13(`CRM_SEND_MEMBER` 5 · `CRM_EVENT_PARTICIPATION` 6 · `CRM_EVENT` 2) + GOLD 13(`FACT_SERVICE_EVENT` 5 · `FACT_EVENT_PARTICIPATION` 6 · `DIM_EVENT` 2)
> · **전건 감사컬럼(`DW_*`) 뒤**에 위치 = `06_DDL.sql:298` 규약 준수(O59-N ③ 재배치가 물리에 반영됨)
> · 채움(분모 명시 · P128) = `CRM_SEND_MEMBER` 38,471,525 → 축A 20,285,072 · 축B 코드 26,256,109 / 라벨 24,854,690 ·
>   `CRM_EVENT_PARTICIPATION` 1,134,126 → 상태 1,122,886/1,122,884 · 경로 1,124,441/1,124,439 · 채널 라벨 981,920 ·
>   `CRM_EVENT` 3,786 전건 · `FSE` 38,470,780(= SILVER − 회원번호 NULL 745) · `FEP` 1,134,126(**팬아웃 0**) · `DIM_EVENT` 3,787(= 3,786 + 센티넬 1)
> ⇒ O59-N ⑥ 기록과 **전건 일치**.
>
> ##### ④ 🟢 §0-Z 재검 쿼리 = 기대 6행 정확 일치
> 참여 3축 「코드 있고 라벨 없음」 = **6행**이고 **전부 O28 오염 행**(`0526424`/`EVENT_118` 3행 · `1377663`/`EVENT_153` 3행 ·
> `)` 가 행마다 다른 축으로 이동). 그 외 미매칭 **0** ⇒ 라벨 NULL 관측축이 build 후에도 유효하다.
>
> ##### ⑤ 🟢 게이트·회귀
> · `audit_ddl_rule7` = GOLD 선언 632 = 파싱 **632/632** · 위반 **0** / SILVER 파싱 717 · 신규 유입 **0**(기지 부채 13 유지)
> · `table_ddl_column_gate` **7/7**(누락 0 · 초과 0 · 순서 일치)
> · WARN 32 = 기존 28 + **신규 4**(`PARTCPT_STAT_NM`·`PARTCPT_PATH_NM`·`PARTCPT_CHNNL_NM`·`SEND_RESULT_NAME` = 의도된 라벨 관측) ·
>   **종전 통과 테스트가 warn 으로 바뀐 것 0 = 회귀 0** · ERROR 0
>
> ##### ⑥ 🔴🔴 `SEND_STATUS2` — **내가 「유령 컬럼」이라 판정했고 그것이 틀렸다**(같은 세션 자기정정)
> 실측은 맞았다: `FACT_SERVICE_EVENT.SEND_STATUS2` = **38,470,780행 전건 NULL · distinct 0**(배선이 `CAST(NULL AS VARCHAR)`).
> 🔴 **틀린 것은 판정이다.** 나는 「DEC-35 §23-G 형상에 없다 ⇒ 유령 컬럼 ⇒ **제거 권고**」라고 적었다.
> 사용자가 *"설계와 이슈문서를 읽고 판단하라"* 고 해서 정본을 읽었고 **결론이 뒤집혔다**:
> · `99_provided_definition/05_회원_보고서필드 인벤토리.md` **§3-1**(서비스효과성분석보고 · 알림톡/이메일)이
>   **「발송상태1」과 「발송상태2」를 둘 다 요구**한다(line 349·350) · `06_지표용어사전:377` 에도 등재 ·
>   구 컬럼 인벤토리는 **`발송상태2(05 3-1; SEND_STATUS=발송상태1)`** 로 **대응까지 명시**해 뒀다.
> ⇒ **유령이 아니라 정본 요구 필드의 미적재다.** 제거했다면 **현업 요구를 우리가 지우는 것**이었다.
> 🔴 **근거 자체가 범주 오류였다** — §23-G 는 **코드→라벨 축 설계**이고 이 컬럼의 소관이 아니다.
>   「그 설계의 형상표에 없다」를 「불필요하다」로 읽었다.
> 🆕 **P203: 컬럼을 폐기 후보로 올리기 전에 정본 요구 인벤토리를 먼저 본다.** 설계 문서는 **그 설계의 범위**만 덮는다 —
>   범위 밖이라는 사실을 불필요의 증거로 오독하면 현업 요구가 우리 손에 지워진다. 「전건 NULL」은 **미적재의 증거이고 불필요의 증거가 아니다**(P21 과 같은 구조).
> **후보를 하나까지 좁혔다**(원천 상태성 컬럼 개수 실측): MSG_AT 2(`TRNSMS_STAT_CD`+`TRNSMS_FAILR_CD_ID`) ·
>   SND 2(`SND_YN`+`CALL_STATUS`) · EMAIL 1(`SNDNG_RST_CD`) · PSTMTR 0. 발송상태1 = 축A(`MS282`)로 이미 배선됐으므로
>   **남은 후보는 축B**(`SEND_RESULT_CD` · O59-N 이 이미 적재)뿐이다.
> 🔴 **그래도 단정하지 않는다** — 축B 사전 라벨은 「상태」가 아니라 **실패 사유**다(`TIMEOUT`·`POWER OFF`·`음영지역`·
>   `블랙리스트에 의한 차단`·`잘못된 전화번호`). 의미가 겹치지 않으므로 배선하면 **라벨 창작과 같은 유형의 오답**이다.
> ⇒ 문서20 **§M-6 신설**(택1 질의: ①축B와 동일 ②제3의 컬럼 ③보고서 폐기) · 회신까지 **전건 NULL 유지** · 문서50 §O59-P-1 갱신.
> 🟠 소비처 실측: **`WIDE_SERVICE_EVENT` 가 이 컬럼을 노출**한다(`_wide_schema.yml:415`) ⇒ 제거 처분이면 뷰도 함께다.
>
> ##### ⑦ 🟢 문안 오타 교정 — 🔴 **「1건」이라 적었는데 4건이었다**(P187 재현)
> `등급 C→B))` 닫는 괄호 중복. 처음 `FSE.SEND_STATUS_GROUP` 하나만 보고 「1건」이라 적었고,
> **전수 검색(P62-B)하니 4건**이었다 — GOLD 2(`FSE.SEND_STATUS_GROUP`·`FEP.PART_PATH_GROUP`) + SILVER 2
> (`CRM_SEND_MEMBER.SEND_STATUS_GROUP`·`CRM_EVENT_PARTICIPATION.PARTCPT_PATH_GROUP`).
> ⇒ **들여다본 컬럼 수만큼만 보였다** = P187 과 정확히 같은 실패다. 규모를 적기 전에 전수 검색한다.
> ✅ 파일 4건 + **라이브 4건 `ALTER … MODIFY COLUMN COMMENT` 동시 교정**(P199) ⇒ 재대조 GOLD **632/632 불일치 0** · SILVER 2건 OK.
>
> ##### ⑧ 🔴🔴 신규 적발 — **규칙7 게이트 분모에 「테이블레벨 COMMENT」가 없다**
> 오타를 고치다 `FEP` 테이블 COMMENT 에 `1,134,126행(2026-08-04 실측)` 이 박혀 있는 것이 눈에 들어왔다.
> `audit_ddl_rule7` 의 매칭 정규식은 **컬럼 선언 라인**(`\s+COL TYPE … COMMENT '…'`)만 잡고
> 테이블 COMMENT(`) COMMENT = '…';`)는 **애초에 분모에 없다.** 그래서 O59-M 이 「위반 0」이라 판정한 상태에서도
> 이 표면은 한 번도 검사된 적이 없다.
> 🔴 **P197 이 지적한 「분모 공백」의 재현이다** — 그때는 블록 경계가 원인이었고 이번엔 **표면 자체가 누락**이다.
>   O59-M 이 커버리지 자기판정(선언 632 = 파싱 632)을 넣었지만 그 분모도 **컬럼 기준**이라 이 공백을 못 잡는다.
> **실측 규모**(테이블레벨 COMMENT 전수 · GOLD 35 · SILVER 39):
> | 표면 | 대상 | 위반 | 진짜 | 오탐 |
> |---|---:|---:|---:|---:|
> | GOLD `06_DDL.sql` | 35 | 9 | **8** | 1(`DIM_MEMBER_CURRENT` 「회원 1명 = 1행」 = grain 정의) |
> | SILVER 정본 DDL | 39 | 8 | **8** | 0 |
> · 진짜 16건 = 전부 **행수·커버리지%·배수·금액**이다. 무거운 것: `DIM_MARKETING_CAMPAIGN`(323행·89.7%·218,402/243,545·73,842원·**181.6배**·39,669,103행) ·
>   `DIM_SEND_TYPE`(21.58%·8,300,272/38,470,780·0.106%) · `FACT_MEMBER_FEE`(40,262,076행·891,959,790,888) · `FEP`(1,134,126행) · 광고 계열 6종(행수).
> 🟢 **[같은 세션 집행 완료 · 문서50 §O59-P-2 = DONE]**
> · 파일 치환 **16/16**(손 재작성 P58) + **문서10 §26-B 신설**(16건 원문 verbatim · P107) +
>   **라이브 16/16 `ALTER TABLE … SET COMMENT`**(P199) ⇒ 파일↔라이브 대상 16건 **불일치 0**.
> · 🔴 유일 사본 위험이 실제로 있었다 — `235,572`·`197,686`·`35,822`·`2,064`·`37,886`·`5,327`·`40,262,076`·
>   `1,056,821,121,099`·`+18.5%`·`99.42%`·`73,842원`·`65조합` 은 **§26 에 없던 값**이다.
> · 게이트에 **표면 3·4** 신설(GOLD·SILVER 테이블레벨) + **표면별 분모 자기판정**(블록 수 대비 파싱 수 ·
>   COMMENT 미작성 테이블도 추적) ⇒ 판정 = 컬럼 632/632 위반 0 · SILVER 컬럼 신규 유입 0 ·
>   **GOLD 테이블 35/35 위반 0** · **SILVER 테이블 39/39 위반 0** · exit 0.
> · 자기검사 = **양성 16/16**(치환 전 원문을 그대로 먹여 재현) · **오탐 면제 1/1** ·
>   `sv_unit_gate --self-check` 8/8·13/13·2/2(공유 예외 수정 회귀 0) ·
>   회귀 `table_ddl_column_gate` 7/7 · `sv_code_label_gate` 통과 · `test_generators` **18/18**.
> · 🔴 **오탐 1건의 원인은 어순이었다** — 공유 `NUM_EXEMPT` 첫 항목이 `\d+행 = …`(행이 앞)만 잡아
>   *"회원 1명 = 1행"* 을 놓쳤다. 같은 grain 정의를 어순으로 다르게 판정하던 것이므로 **대칭형 패턴 추가**(P182 계열).
> · 🔴 **부수 적발 — 게이트가 테이블명을 틀리게 귀속했다**: SILVER #16 을 `IDENTITY_MEMBER_XREF` 로 보고했으나
>   실제 소유는 **`CRM_MARKETING_CAMPAIGN`** 이었다(원인 = `blocks()` 시작 패턴이 `CREATE OR REPLACE TABLE` 만이라
>   `CREATE TABLE IF NOT EXISTS …` 가 앞 블록에 흡수). **위치는 맞고 이름이 틀린** 유형이며 이름을 믿고 파일을
>   열면 다른 테이블을 고친다 ⇒ 두 형태를 모두 블록 시작으로 잡도록 교정.
> ⚠️ **남은 형제 표면(미측정)**: GOLD 뷰 COMMENT(559) · SV COMMENT · dbt yml `columns[]` — O59-M 의 「yml 56건 전량 오탐」
>   판정에는 **테이블/뷰레벨 문안이 범위에 없었다**.
>
> ##### ⑨ ⛔ 다음
> · **O59-P-2** = 🟢 **집행 완료**(위 ⑧).
> · **O59-P-1** = 🔴 서술이 **부분만** 있었다 — 「요구 필드다」는 확정, 「어느 원천 컬럼인가」는 미서술 ⇒ 문서20 §M-6 현업 질의.
> · 로드맵 = DEC-35 착수 순서 **5번**(3단계 다체계 해소 · (c) 확장판 §23-G). 4번은 build 판정까지 종결.
>
>
> #### 🟢🟢 [2026-08-11 O59-O] **O28 종결 — 운영서버 대조로 「원천 유래」 확정** · 적재 완전성 100% · 등급 C 3건 확정
>
> **트리거** = 사용자가 운영 CRM 에서 §0-Z A-1~A-5 를 실측해 왔다. **BRONZE 로는 불가능했던 판정**이 끝났다(§O59-G ②).
>
> ##### ① 🔴🔴🔴 **B-1 판정 = 원천 유래 확정** — 적재 결함이 아니다
> A-1 원본에 `)` 가 **그대로 있다**(`H_STAT=29`·`H_PATH=29`·`H_CHNNL=29` = ASCII `)` 확증 · HEX 대조).
> 🔴 **그리고 주변 행이 정체를 못 박았다** — 같은 회원·같은 초 단위 시각대에 이런 `MBER_NO` 가 줄지어 있다:
> `bxss.me`(Acunetix **Blind XSS 도메인**) · `../0526424`(**path traversal**) · `1&#39;"`(SQLi/XSS 인젝션) ·
> `@@XAsBY`·`@@u1DS8`·`@@CJb1M`(SQL 변수 프로빙) · `&lt;!--`(HTML 주입) · `&#39;"()` · `0526424*1` · 무작위 8자 토큰.
> ⇒ **§O59-H 의 「웹 취약점 스캐너 트래픽」 결론이 원천에서 직접 재확증됐다**(그때는 BRONZE 정황이었다).
> ⇒ 문서20 §I-2 #4 의 **「삭제 요청」 유지**가 옳다. 적재 경로·재적재·전 테이블 재검증은 **불요**(가설 기각).
>
> ##### ② 🟢 **B-2** — `MBER_NO=')'` 2행도 **원천 실재**(행사 118·153 · `FRST_RGSTR_ID='HomePage'`)
> ⇒ 우리가 만든 행이 아니다. GOLD 고아 2행은 **우리가 지우지 않는다** — 원천 정리(현업) 대상이고 관측만 유지한다.
>
> ##### ③ 🟢 **B-3 검사축 완전 정합** — 원천 A-3 = `STAT ) 2` · `PATH ) 2` · `CHNNL ) 2` = **총 6행**
> 우리 GOLD 라벨 NULL **6행**과 **정확히 일치**. ⇒ 오염은 넓게 퍼져 있지 않고(적재 아티팩트 아님),
> **라벨 NULL 이 오염의 완전한 관측축**임이 원천 대조로 확증됐다.
>
> ##### ④ 🟢🟢 **B-4 적재 완전성 — 처음으로 원천과 대조했고 100% 다**
> | 대상 | 운영 원천 | 우리 BRONZE | 판정 |
> |---|---|---|---|
> | `TD_MS_EVENT_PRTCPNT_DTL` | **983,971** | **983,971** | 🟢 정확 일치 |
> | `TD_MS_CRMN_PRTCPNT` | **152,132** | **152,132** | 🟢 정확 일치 |
> | `TM_MS_EVENT` | **376** | **376** | 🟢 정확 일치 |
> ⇒ **적재 누락·중복 0.** SILVER 와의 차(EVENT 1,972 · CRMN 5)는 **전부 `MBER_NO` NULL/빈값 행**이고
> 모델 WHERE 가 의도적으로 제외한 것이다(dedup 손실 **0** — PK 중복 없음).
> 🟠 다만 **정보 손실 규모를 기록한다**: 회원번호 없는 참여 1,977행은 GOLD 에 없다(스캐너분 + 비로그인 참여 혼재).
>
> ##### ⑤ 🟢🟢 **B-5 등급 C 3건 전부 확정 — 롤백 미발동**
> 운영 사전이 우리 추정과 **전건 일치**했다: `MS282`(2 발송완료·3 에러·4 예약취소) · `MS303`(webmail·homepage·출석·
> 포토·텍스트·독자의견·독자이벤트) · `MS004`(홈페이지·상담센터·초대·이메일·모바일웹·APP).
> ⇒ **등급 C → B 승격** · 문서30 §23-J 결정 2 의 **롤백 조건 미발동** · 조건부 COMMENT 5곳을 **확정 문안으로 교체**
> (정본 DDL + 라이브 동시 · 불일치 0) · 문서20 §M-3 **닫음**.
> 🔴 **부수 발견 — 판별축의 필요성이 사전으로 증명됐다**: `MS304`의 `200`=`4_step_fail` 인데 `MS303`의 `200`=`homepage` 다.
> **같은 값이 축마다 다른 뜻**이므로 코드 단독 필터·GROUP BY 는 조용히 틀린다 ⇒ `*_GROUP` 신설이 옳았다.
> 🟢 `MS006`의 `6 대기(결제)`가 **`USE_YN='N'` 폐기코드**임도 확인 — 우리 「USE_YN 무필터 조인」 규약이 옳았다(필터하면 사라진다).
>
> ##### ⑥ 🔴 **B-6 미해소 — 축B 미등재 17종은 운영 사전에도 없다**
> 우리 스냅샷 stale **아님**. 후보가 좁혀졌다: ⓐ **사전이 통신사 코드 증설을 못 따라갔다**(`7319`·`7320`·`7321` 이
> `MS057` 번호대인데 사전은 `7318`·`7322` 만 있다 · `9034` 도 `MS056` 이 `9031` 까지) ⓑ **SND 한 자리 코드는 같은 계열**이다
> — `MS056` 에 단문자 코드가 실재하고(`1`=TIMEOUT·`2`=잘못된 전화번호·`C`·`n`·`x`·`g`… ) 실제로 `1`·`2` 는 라벨이 붙었으며
> **`0`·`3`~`9` 만 없다**. ⇒ 문서20 §M-5 를 **「사전 갱신 요청」으로 전환**(추정 라벨 생성 금지 유지).
>
> ##### ⑦ ⛔ 다음
> - 🟠 문서20 잔여 = **§M-1 ②**(MS304 한글 표기) · **§M-4**(EMAIL·SND 라벨) · **§M-5**(통신사 코드 사전 갱신).
> - 🟠 **회원번호 NULL 1,977행 처분** 판단 필요(현재 GOLD 미포함) — 스캐너분과 비로그인 정상 참여를 분리할지.
> - 🟢 **오염 판정이 끝났으므로 로드맵 6단계 後半(SV·Agent) 착수 차단이 해제됐다**(§0-Z C 의 선행 의존 해소).
>
>
> #### 🟢 [2026-08-11 O59-N] **DEC-35 1·2단계 실행** — 코드→라벨 26컬럼 신설(SILVER 13 + GOLD 13) · build 대기
>
> **트리거** = 착수 순서 4번. 사용자 결정 4건 확보 후 착수(문서30 **§23-J** 에 등재 · 롤백 조건 포함).
>
> ##### ① 🟢 사전 검증이 설계를 바꿨다 (R2-1)
> · 사전 (CD_ID, DTL_CD_ID) **대상 14그룹 전건 유일** ⇒ 팬아웃 안전 확정.
> · 🟢 **축B 4그룹 코드값 교차중복 0**(실측) ⇒ 코드군을 **리터럴이 아니라 조인 결과에서** 얻게 설계했다(P31 준수).
>   이 사실을 재기 전에는 「메시지 타입 판별자가 필요하다」고 보고 있었다 — 실측이 판별자 요구를 없앴다.
> · 조인 커버리지 사전 실측(분모 명시): MSG_AT 축A **20,285,072/20,285,072** · 참여 CRMN 상태·경로 100% ·
>   행사구분 EVENT 376/376 · CRMN 3,410/3,410 · 축B MSG_AT 17,934,911/18,440,452 · SND 6,919,779/7,815,657.
> · 🔴 **미매칭 distinct 1 = O28 `)` 오염값** — 라벨 NULL 로 떨어진다(라벨 부여 금지 · DEC-17-B).
>
> ##### ② 🔴 인수인계·정본과 어긋난 것 2건을 실측으로 잡았다
> · **EMAIL·SND 라벨**: 문서31 은 *"라벨은 A 근거로 만들고 코드군은 비워 둔다"* 였는데, 코드군이 특정되지 않으면
>   **`CRM_CODE` 에서 가져올 문자열이 아예 없다** ⇒ 라벨 생성은 곧 창작이다. 사용자 결정으로 **철회**하고 §M-4 신설.
> · **O53 4블록 상류**: §O59-L ⑤ 는 *"yml 수정 후 생성기 재실행"* 이라 적었으나 그 4블록은 yml 에 `columns[]` 가 없다
>   (§O59-M ③ · 🆕 P198).
>
> ##### ③ 🔴🔴 사용자 지적으로 **절차·규약 위반 2건 교정** (이 세션 최대 결함)
> · **절차 역순**: `dbt_project.yml:71` 이 **①DDL→②ALTER→③스캔→④모델**을 규정하는데 SILVER 에서 **④부터** 했다.
>   그대로 build 하면 `on_schema_change: append_new_columns` 가 **dbt 자동 ALTER** 로 물리를 만들어
>   **정본보다 물리가 앞서 나가는 드리프트**(O42-C 감시항목)가 됐을 것이다.
> · **컬럼 위치 규약**: `06_DDL.sql:298` 이 *"신규 컬럼은 감사컬럼 뒤(맨 끝)"* 를 못박아 뒀는데 raw 컬럼 옆에 끼워 넣었다
>   ⇒ 재구축 시 라이브와 순서가 갈라지는 바로 그 오류다. SILVER·GOLD 모두 맨 끝으로 재배치 + 모델 SELECT 순서 동기화.
> ⇒ 🆕 **P200: 「정본 파일 수정」과 「물리 반영」은 순서가 규정돼 있다 — 모델을 먼저 고치면 자동 스키마 변경이
>   정본을 앞질러 간다.** 규정 순서를 파일 주석이 아니라 **착수 체크리스트로** 읽을 것.
>
> ##### ④ 🟢 실행 결과 (절차 ①~④ · ⑤build 는 사용자 · ⑥은 build 후)
> · SILVER 13컬럼(`CRM_SEND_MEMBER` 5 · `CRM_EVENT_PARTICIPATION` 6 · `CRM_EVENT` 2) · GOLD 13컬럼
>   (`FACT_SERVICE_EVENT` 5 · `FACT_EVENT_PARTICIPATION` 6 · `DIM_EVENT` 2).
> · ALTER **13/13 + 13/13 성공** · 정본↔물리 **순서 전건 일치 · COMMENT 불일치 0**(SILVER 6건 발견 후 동기화).
> · `06_DDL` 619 → **632컬럼** · 규칙7 위반 **0**. GOLD 는 **전파만** 하고 사전 조인은 SILVER 소관으로 고정(단일 정의 지점).
> · 게이트: `table_ddl_column_gate` **7/7** · 자기검사 4/4 · 팬아웃 **0**(3모델 행수 라이브와 동일) · 스테이지 전건.
>
> ##### ⑤ 🔴 자기검토에서 **내 결함 5건** 추가 적발 (사용자 지시)
> | # | 결함 | 조치 |
> |---|---|---|
> | ㉠ | 🔴 **규칙7 위반 재발** — SILVER DDL COMMENT 에 실측 `8/8` 을 넣었다(오늘 ⑩에서 48건 치환한 그 규칙이다) | 파일·라이브 동시 교정 |
> | ㉡ | 🔴 **게이트 분모에 SILVER 가 없었다** — 그래서 ㉠을 아무 게이트도 못 잡았다 | `audit_ddl_rule7` 에 **SILVER 정본 DDL 표면 추가**(파싱 717) · 기지 부채 13 고정 · 신규 유입만 실패 |
> | ㉢ | 🟠 **선행 세션 유래 13건 적발**(SILVER DDL·라이브 동일) | 부채 등재 · 치환은 §26 같은 이관 대장 선행 ⇒ 별건 |
> | ㉣ | 🔴 **`table_ddl_column_gate` 결함 3건** — 대상이 O53 4종뿐(분모) · `s.index(';')` 블록 경계(O59-M 과 동일 유형) · `gold_sk()` 미지원 + 매크로 인자 작은따옴표 미처리 | 전부 교정 후 7/7·자기검사 4/4 |
> | ㉤ | 🟠 **R1-3 부분 읽기 미신고** — `06_DDL`(1,468)·`_crm_schema.yml`(628)·`08_SILVER`(1,010) 전량 미독 · 모델은 `sed` 로 읽었다(R1-3-4 위반) | 신고 · 영향 실측: 실제 오류 1건(㉣ `date_sk`)은 이것 때문에 늦게 드러났다 |
>
> ##### ⑥ 🟢 **build 완료 · 절차 ⑥ 통과** (2026-08-11 사용자 실행)
> `PASS=386 WARN=32 ERROR=0 SKIP=0 TOTAL=418` (success 88 · pass 298 · warn 32).
> · **WARN 32 = 기존 28 + 신규 4** — 신규는 전부 **내가 심은 라벨 관측**(`PARTCPT_STAT_NM`·`PATH_NM`·`CHNNL_NM`·
>   `SEND_RESULT_NAME`)이고 **의도된 warn** 이다. 종전 통과 테스트가 warn 으로 바뀐 것 **0** = 회귀 0.
> · **절차 ⑥ 비NULL 검증 — 26컬럼 전건 실채움**(분모 명시):
>   `CRM_SEND_MEMBER` 38,471,525행 → 축A 20,285,072 · 축B 코드 26,256,109 / 라벨 24,854,690 ·
>   `CRM_EVENT_PARTICIPATION` 1,134,126 → 상태 1,122,886/1,122,884 · 경로 1,124,441/1,124,439 · 채널 981,922/981,920 ·
>   `CRM_EVENT` 3,786 전건 · GOLD `FSE` 38,470,780(= SILVER − 회원번호 NULL 745 · 기존 규약대로) ·
>   `FEP` 1,134,126(팬아웃 0) · `DIM_EVENT` 3,787(= 3,786 + 센티넬 1 · 센티넬 라벨은 NULL 로 의도)
> · 🔴🔴 **§0-Z B-3 검사축이 실증됐다**: 참여 3축에서 「코드 있고 라벨 없음」이 **정확히 6행**이고 **전부 O28 오염 행**이다
>   (`0526424`/`EVENT_118` 3행 · `1377663`/`EVENT_153` 3행 · `)` 가 행마다 다른 축으로 이동 — §O59-G 기록과 일치).
>   그 외 미매칭 **0** ⇒ 이제 오염은 **라벨 NULL 로 자동 관측**된다(종전에는 전용 스캔이 필요했다).
> · 🟠 **신규 발견 — 축B 사전 미등재 17종 · 1,401,419행**(MSG_AT 505,541/5종 · SND 895,878/12종).
>   🔴 SND 에 알림톡 체계(`7319`·`7320`)와 한 자리 코드(`0`·`3`~`9`)가 **함께** 있다 ⇒ **같은 컬럼 2체계 의심 = O28 과 같은 유형**.
>   ⇒ 문서20 **§M-5** 신설(코드·행수 표 + 두 질문). 라벨은 NULL 유지(DEC-17-B).
> · 코드군 분포(조인 결과 취득 실증): `MS057` 18,648,368 · `MS056` 3,874,114 · `MS059` 2,331,373 · `MS058` 101.
>
> ##### ⑦ ⛔ 다음
> - 🔴 **오염 판정** = `99_NEXT_SESSION.md` **§0-Z**(운영서버 반출 목록 A-1~A-6 · 복귀 후 B-1~B-5).
>   🆕 O59-N 덕분에 **오염값이 라벨 NULL 로 드러나는지**를 새 검사축으로 쓸 수 있다(B-3).
> - 🟠 `FACT_SERVICE_EVENT.SEND_STATUS2`(전건 NULL · §23-G 형상에 없는 유령 컬럼) 처분 미결.
>
>
> #### 🟢 [2026-08-11 O59-M] ⑩ **본체 완료** — 규칙7 위반 **48건 치환**(37 은 오측이었다) · 파일↔라이브 619/619 일치
>
> **트리거** = §O59-L ⑤(⑩ 본체). 🔴 **치환부터 하지 않고 게이트 커버리지를 먼저 실측한 것이 결정적이었다.**
>
> ##### ① 🔴🔴 「37건」은 하한이 아니라 **오측**이었다 — 게이트가 테이블 본문을 잘라 읽었다
> `audit_ddl_rule7.blocks()` 가 블록 끝을 `s.index(';', i)` 로 잡았고, **COMMENT 문안 속 `;` 에 걸려**
> 블록이 조용히 잘렸다(실측: `DIM_ORG`·`DIM_MEMBER` 는 **2줄만** 검사됨).
> ⇒ 파싱 컬럼이 **579/619** 였고 미검사 40컬럼 안에 **진짜 위반 11건**(DIM_MEMBER 7 · DIM_ORG 4)이 있었다.
> 🔴 docstring 의 *"파싱 실패분(다중행 COMMENT 등)은 검사되지 않는다 — 하한이다"* 는 **원인을 잘못 지목한 경고**였다
> (실측 다중행 미파싱 = **0**). ⇒ 경계를 「다음 `CREATE` 문까지」로 고치고 **선언=파싱 커버리지 자기판정**을 넣었다
> (분모를 출력만 하지 않고 불일치면 실패 · P106·P138).
> ⇒ 🆕 **P197: 「하한이다」라는 자기 고백은 게이트를 면제하지 않는다 — 분모(대상 총수)를 게이트가 스스로 세게 만든다.**
>   미검사분을 문장으로 인정하면 그 구간은 영구히 안 보인다. 종전 세션이 37 → 39 → 37 로 세 번 세면서도
>   **한 번도 분모를 세지 않았다.**
>
> ##### ② 🔴 지우기 전에 원문을 옮겼다 — COMMENT 가 **유일 사본**인 값이 실재했다
> 문서10 **§26 이관 대장** 신설(48컬럼 · 제거 토큰 **77개** · COMMENT 원문 verbatim).
> 🔴 `97.76%`(`DIM_MEMBER.FIRST_SPONSORSHIP`)·`19.73%`(`LAST_STOP_DATE`)는 **정본 문서에 없고** `_archive` SQL 에만
> 있었다 — 그대로 지웠으면 소실이다(P107). 보존 게이트 = 대장 누락 **0/77**.
>
> ##### ③ 🟢 치환 = **손으로 작성한 48벌**(P58 준수) · 구조 불변 검증
> P58 이 *"안전한 자동 제거는 존재하지 않는다"* 를 이미 규정하므로 문안을 전부 직접 재작성하고
> 스크립트는 **지정 라인의 COMMENT 본문 교체만** 하게 했다. 사전 검증 = 새 문안 47벌 자체가 게이트 통과 · 인용부호 짝 정합.
> 사후 = 변경 라인 **47/47** · **COMMENT 본문 외 변경 0** · 홀수 인용부호 0 · 줄 수 1,444 불변(P141).
> 🔴 **1건은 별 경로였고 인수인계의 처방이 틀렸다** — `DIM_MEMBER_ACQUISITION.FIRST_STOP_REASON_NM` 은 O53 신규 블록
> (손 편집 금지)인데 §O59-L ⑤ 는 *"dbt `schema.yml` 수정 후 생성기 재실행"* 이라 적었다. 실측: 그 4블록은 yml 에
> **`columns[]` 자체가 없다**(문안 정본이 `06_DDL` 로 이동 · `_gold_ready_schema.yml:691` 이 명시).
> 진짜 상류는 **`scripts/o51d_view_comments/desc_dim.py:173`** 이었다 ⇒ **상류와 생성물을 같은 문안으로** 고쳤다.
> ⇒ 🆕 **P198: 「생성물이니 상류를 고쳐라」는 상류가 실제로 그 문안을 갖고 있을 때만 참이다.** 정본 위치가 이동한 뒤에는
>   가리키는 상류가 빈 껍데기일 수 있다 — 고치기 전에 **상류에 그 문안이 있는지 grep 으로 확인**한다.
>
> ##### ④ 🔴🔴 파일만 고치면 소비 계층은 그대로다 — **라이브 48컬럼 동기화**
> 자기검토 ②(P33)로 라이브를 스캔하니 **같은 48컬럼이 전건 stale** 이었다(GOLD BASE TABLE 619 컬럼 기준).
> `persist_docs` 미사용을 확인해 dbt 가 덮지 않음을 확정한 뒤, **교정된 파일에서 `ALTER … COLUMN … COMMENT` 를 기계 생성**해
> 적용(**48/48 성공 · 실패 0** · role `GN_DW_ADMIN`). ⇒ 파일↔라이브 문안 **619/619 완전 일치**(불일치 0).
> ⇒ 🆕 **P199: P57(물리만 고치면 재구축에서 소실)의 역방향도 참이다 — 정본만 고치면 라이브가 stale 이다.**
>   COMMENT 는 Analyst 가 읽는 **소비 인터페이스**이므로 정본·라이브 양방을 같은 소스에서 생성해 맞춘다.
>
> ##### ⑤ 🔴 형제 표면을 찾았다 — dbt yml `columns[]` **56건**(= GOLD 뷰 라이브 COMMENT 원천) · 전량 오탐
> 같은 판정식을 yml 에 돌리자 **565 중 56 위반**이 나왔고 라이브 뷰도 **546 중 56** 으로 정확히 같은 집합이었다.
> 🟢 문맥 분류 결과 **진짜 0 · 오탐 56** — `IS_CURRENT 1건`(45, grain 서술) · `총계 1행`(11, 논리 서술).
> ⇒ 공유 예외(`sv_unit_gate.NUM_EXEMPT`)에 두 문맥을 등재(P182·P195 의 「판정식 1벌」 이행).
> 🔴 **이 표면은 종전에 어느 게이트도 보지 않았다** — `audit_ddl_rule7` 은 `06_DDL` 만, `sv_unit_gate` 는 SV·라이브만 봤다.
>
> ##### ⑥ 🟢 판정 (전부 대상 존재 상태에서 실행 · P106)
> · `audit_ddl_rule7`: 선언 **619 = 파싱 619** · 위반 **0** · 커버리지 자기판정 통과
> · 라이브 GOLD 전 컬럼(테이블+뷰) **1,165** 규칙7 위반 **0** · yml `columns[]` **565** 위반 **0**
> · `gen_o53_gold_ddl.py --self-check` **9/9** · `sv_unit_gate --self-check` **양성 8/8 · 음성 13/13 · 분할금지 2/2**
> · 스테이지 **5/5** 반영(패딩 허용 대조)
>
> ##### ⑦ ⛔ 미결 (승계)
> - 🟠 **SV COMMENT 수치 기지 부채 12건**(`CMT_BASELINE`) — ⑩ 과 별건이며 그대로 남아 있다.
> - 🟠 **`03_top-down_gold/03_테이블 설계.md` 에 같은 수치가 산재**한다 — 설계문서는 수치 보유가 정상이지만
>   §26 과 값이 갈라질 수 있다(현재 대조 미실시). 인용 시 재실측할 것(PROC-3).
> - 🟠 **`06_DDL` 헤더의 실측 대조 기재는 2026-08-06/10 시점**이다 — 라이브 619 컬럼과 오늘 대조했으나 헤더는 미갱신.
> - 🔴 **자기 지적**: 이 항목을 등재하면서 **바로 아래 §O59-L 의 헤더를 지웠다**(즉시 복원). §O59-E ⑥ 이 같은 사고를
>   두 번 기록했는데 **세 번째**다 ⇒ 원인은 `edit` 의 `old_string` 에 **앞 항목 헤더를 포함**시키는 습관이다.
>   재발 방지 = 이력 파일에 새 항목을 넣을 때 **앵커를 앞 항목의 본문 첫 줄이 아니라 빈 줄로** 잡고,
>   등재 후 `grep -c "^> #### " 20_issue/01_세션이력.md` 로 **항목 수 증가(+1)를 확인**한다(2026-08-11 O59-M 시점 **51**).
>   ⚠️ `grep -c "^> #### \["` 는 헤더에 **상태 이모지가 끼어 0 을 반환**한다 — 내가 이 항목에 처음 적은 게이트가 그것이었고
>   실행해 보고 고쳤다(P139: 게이트는 실행해서 값을 확인한 형태로만 심는다).
>

>