<!-- SPLIT-CHUNK 01_세션이력.md | 054/054 | 허브 = 01_세션이력.md | 원문 1~113행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-30 O124-B] O124 자기검토 — 확정위반 2 · 판정약점 2 · 구조결함 1 전건 시정 + 인수 용량 문제 실제 해소
>
> 사용자 지시 = 「이슈문서를 읽고 이번 세션 작업을 **비판적으로 검토** · 승인 필요 작업 **전건 승인** · 작업과 문서를 개선」.
> 🔴 **라벨은 새로 발급하지 않고 접미로 이었다**(`R1-4-3` — 같은 세션의 후속 작업 단위).
> 🟢 이 절의 판정은 전부 **재실측**이다. O124 본문 서술을 근거로 쓰지 않았다(`R1-3-7` 대체 근거 금지).
>
> ##### D1. 🔴🔴 확정위반 — 「파일이 없다」를 **경로 한정 검사**로 단정했다 (내가 이 세션에 계속 인용한 축의 자기위반)
>
> O124 는 원장 §0 등재표에 *"선례 `_o123c_evidence.md` 는 실재하지 않는다"* 를 적고,
> O123-D 의 근거철 인용을 **결함으로 규정**했다. 🔴 **거짓이다.**
>
> | 축 | O124 기재 | 🔴 O124-B 재실측 |
> |---|---|---|
> | 존재 | 「없다」(`ls` 부재) | **실재한다** — 워크스페이스 **루트** `_o123c_evidence.md` |
> | 규모 | — | **11,600 B · 166줄** |
> | 내용 | — | `## E0`~`## E11` **12절** = 인용된 「E0~E11」과 **정확히 일치** |
>
> 🔴 **원인 = `20_issue/` 한 폴더만 보고 「부재」로 결론했다.** 근거철의 실제 배치가 **루트**였다.
> ⇒ 이것은 `P132`(「없다」와 「못 찾았다」를 구분하라)이고 `O111 ㉠`(「0건」은 판정식이 못 본 것일 수 있다)이다.
> 🔴🔴 **가장 무거운 점 = 나는 이 세션에서 그 두 축을 근거로 게이트를 고치고 남을 비판했다** —
> a-5 의 판정식 시정 · ⑨ 의 「20건은 stale」 판정이 전부 그 축이었다. **같은 세션에 같은 실수를 했다.**
> 🟢 **시정 4곳 전건**(취소선을 **되돌렸다** — 유효한 인용을 내가 잘못 지웠으므로):
> 원장 §1 O123-D 행 · 원장 §0 등재표 · 이력 §O123-C · `99_NEXT §0-AAAA ▣AAAA8`.
> 🟠 **다만 배치 관례는 실제로 갈려 있다** — 루트(`_o123c`) ↔ `20_issue/`(`_o124`).
> 🟢 판정 = **`20_issue/` 를 표준으로 삼는다** · 🔴 루트 선례는 **옮기지 않았다**(4곳이 그 경로를 인용한다).
>
> ##### D2. 🔴🔴 구조결함 — 근거철 인용은 **어떤 게이트의 분모에도 없었다**
>
> D1 이 게이트 6종 🟢 상태로 통과한 이유를 실측했다: `doc_coord_gate.COORD` 가 파일명을
> **`[0-9]{2}_` 로 시작하는 것만** 잡는다 ⇒ 접두 `_` 인 근거철은 **원리적으로 분모 밖**이었다(`P106`).
> 🔴 이것은 O124 **자신에게도** 위험했다 — `_o124_evidence.md` 를 원장·이력·`99_NEXT`·게이트 주석
> **5곳**에서 인용하는데 그 파일이 사라지면 **같은 방식으로 조용히** 죽는다.
> 🟢 **시정 = `COORD` 에 `_<라벨>_evidence.md` 관례를 편입**(대안 접미까지 넓히지 않았다) +
> `test_doc_coord_gate.py` **축12 신설**(인식 축 3 + 오탐 축 4 + 「실재 판정 분모는 전역」 축 1).
> 🟢🟢 **그 축이 즉시 값을 했다** — 편입 직후 게이트가 **축1a blocking 1건**을 냈고,
> 그것은 내가 등재표에 **오판의 원인이 된 잘못된 경로를 문자열로 적어 둔 것**이었다 ⇒ 문구 시정 후 rc=0.
> 🔴 **판정식 = 잘못된 좌표는 「설명하려고 적을 때」도 좌표다.** 오류 경로를 본문에 인용하지 마라.
>
> ##### D3. 🔴 판정약점 — 고친 코드는 옳았고 **「무엇을 고쳤는가」의 서술이 틀렸다**
>
> `code_cell()` 주석·착수표 ⑨·이력·원장에 *"`` `{값[:60]}` `` 로 **감싼 값을 절단**"* 이라 적었다.
> 🔴 **방향이 거꾸로다** — 종전 코드는 **자른 뒤 감쌌다**. 그래서 초판 처방 ㉢
> 「자른 뒤에 감싼다(감싼 뒤 자르면 닫는 백틱이 잘려 나간다)」는 **존재하지 않은 버그를 처방으로 제시**한 것이고,
> 실제로 필요·충분한 근본 처방은 **㉠ 값의 백틱 제거** 하나다.
> 🔴 **음성 테스트가 잡을 수 없었다** — 동작은 옳았고 **서술만** 틀렸다.
> 🟢 시정 = 주석·docstring·이력을 정정하고, **메커니즘을 실측으로 못 박았다**:
> `doc_line_length_gate.split_row` 는 백틱마다 `in_code` 를 **토글**하고 `in_code` 동안 `|` 를 세지 않는다
> ⇒ 홀수 백틱이면 `in_code` 가 **행 끝까지 켜진 채 남아** 열수가 4 → 2 로 무너진다.
> 🟢 **축5 신설 = 판정을 그 파서에 직접 물어본다**(자기 계산 파서 대조) — 종전 구현 행이
> **게이트 실측으로 열수 2**(헤더 4)임을 확인했다 ⇒ 「열수 2」가 이제 **자기신고가 아니다**.
> 🔴 **판정식(신설) = 코드가 통과해도 서술은 따로 검증하라 — 테스트는 동작만 본다.**
>
> ##### D4. 🔴 판정약점 — 원장에 **단정 수를 박아** 즉시 stale 이 됐다
>
> 원장 §1 에 *"음성 테스트 2(**110단정**)"* 을 적었다. 🔴 두 가지가 틀렸다:
> ㉠ 110 = 46(**기존 파일 확장**) + 40 + 24(신설 2)인데 「신설 2 = 110」으로 읽힌다(귀속 오류).
> ㉡ O124-B 가 축을 늘리자 그 수가 **그 자리에서 낡았다**(24 → 35).
> 🟢 시정 = **수를 지우고 「2 신설 · 1 확장」 + 「단정 수를 적지 않는다」**로 바꿨다(`R3-9 ㉦`).
> 🔴 이것은 O124 가 같은 세션에 **이미 한 번 걸린 실수**다(TEST 「23→25」 → 실측 22→24).
> ⇒ 🔴 **판정식 = 내가 만든 수는 문서가 아니라 도구가 출력해야 한다**(두 번 걸렸으므로 조문 수준이다).
>
> ##### D5. 🟢🟢 인수 용량 문제를 **실제로 해소했다** — 내 작업이 악화시킨 것을 되돌리고 더 내렸다
>
> 🔴 **먼저 자기신고** = 인수 시점 `99_NEXT_SESSION.md` 허브 여유 **6,994 B** 였고,
> O124 가 인수인계 절을 넣자 **6,468 B** 로 **악화**됐다. O124 는 그 사실을 어디에도 적지 않았다.
> 🟢 승인 전건이 있으므로 O124 가 넘긴 3안 중 **㉮(색인 밀도 인하)** 를 실행했다 — 근거는 실측이다.
>
> **판정식 = 변별 붕괴 0** (「서로 다른 절 제목이 같은 문자열로 잘려 조각을 고를 수 없게 되는가」).
> 실측 = 허브 **11종** · 절+`▣` 제목 **967개** · `TITLE_MAX` **70·60·55·50·45·40·35 전부 붕괴 0**
> (근거 = 이 문서군의 제목은 선두에 변별자 `0-BBBB.`·`▣ ZZZ3`·절 번호를 둔다).
> ⇒ 🟢 **`split_doc.TITLE_MAX` 70 → 50** (🔴 45 이하로 더 내리지 않았다 — 붕괴는 0 이지만 **사람이 읽고 고를** 여유가 사라진다).
>
> | 허브 | 전 | 후 | 차 |
> |---|---:|---:|---:|
> | `99_NEXT_SESSION.md` | 34,492 | **32,230** | **−2,262** |
> | `01_세션이력.md` | 31,815 | 30,398 | −1,417 |
> | `50_dbt_파이프라인_미결조치.md` | 28,159 | 26,289 | −1,870 |
> | `30_설계_의사결정.md` | 20,720 | 19,424 | −1,296 |
> | `10_진단_원인분석.md` | 28,526 | 27,318 | −1,208 |
> | 그 밖 6종 | — | — | −554 · `00_INDEX` 는 **+18**(절단 `…` 3바이트 때문 · 무해) |
>
> 🟢 **결과 = `doc_type_gate` 여유 부족 경고 1건 → 0건** ⇒ 판정이 **🟡 → ✅** 로 올라갔다.
> `99_NEXT` 여유 = **8,730 B**(상한의 21.3% · 축3 기준 20% 통과).
> 🟢 회귀방어 = `test_split_doc_toc.py` **「변별 붕괴」 축 신설** — 실 문서군을 분모로 현재 `TITLE_MAX`
> 에서 붕괴 0 을 강제하고, **음성으로 `TITLE_MAX=4` 를 넣어 그 축이 실제로 깨지는 것**까지 단정한다
> (🔴 이것이 없으면 「붕괴 0」이 축이 죽어서 나온 값인지 구별할 수 없다 · `O111 ㉠`).
> 🔴 **인수 경고는 여전히 유효하다** — `--rebalance` 는 조각을 늘려 색인을 **키운다**. 쓰지 마라.
>
> ##### D6. 🔴🔴 `R1-7-3` 이 나를 막았다 — 건강한 골든을 「손상」으로 오판하기 직전이었다
>
> `doc_heading_gate --update-golden`(rc=0) 직후 재검에서 **`json.decoder.JSONDecodeError:
> Unterminated string starting at line 1980`** 이 났고 `wc -c` 는 **145,165 B** 를 냈다.
> 🔴 나는 「골든 JSON 이 깨졌다」로 판정하고 **꼬리를 손으로 봉합**하려 했다 —
> 그렇게 했으면 **건강한 파일을 실제로 파괴**했을 것이다.
> 🟢 `R1-7-3`(「손상」 판정 전에 2~3회 재읽어 해시 대조)을 이행하니 결과가 갈렸다:
>
> | 축 | 첫 관측 | 재측정 3회 |
> |---|---|---|
> | 바이트 | 145,165 | **146,158**(3회 전부 동일) |
> | SHA256 | — | **3891b9f1f3890971**(3회 동일) |
> | 꼬리 | 문자열 중간에서 끊김 | `"…내역 (2026-08-12 O62)"\n  ]\n }\n}\n` **정상 종결** |
> | `json.load` | FAIL | **OK · `docs` 19종** |
> | `_updates` | — | **62회 보전**(발행 이력 손실 0) |
>
> ⇒ 🔴 **원인 = torn read**(스테이지 마운트가 쓰기 직후 부분 뷰를 줬다 · `OPS-3` 「낡은 뷰」).
> 🔴🔴 **판정식 = 「손상」은 재현되어야 손상이다.** 쓰기 직후의 읽기 1회로 손상을 단정하면
> **복구 시도 자체가 유일한 실제 손상**이 된다. 🟢 게이트 재실행 **3회 전건 rc=0** 으로 종결했다.
> 🟢 이 항목은 `R1-7-3` 이 **가상의 위험이 아니라 실제로 작동하는 조문**임을 이 워크스페이스에서 다시 실증한다.

> ##### D7. 🟠 시정의 부작용 — 원장 재균형이 좌표 2건을 낡게 했다 (파급을 먼저 재고 실행했다)
>
> `TITLE_MAX` 인하로 `99_NEXT` 여유는 회복됐지만 **원장 조각 `-001` 이 새 경고를 냈다**
> (여유 **6,889 B** < 20%) — 🔴 **내가 O124·O124-B·등재표 장문 행 3개를 그 조각에 넣은 결과다.**
> 🟢 ① 먼저 등재표 행의 서사를 **이력 좌표로 압축**했다(912 → 503자 · 정본 중복 제거) ⇒ 여유 7,663 B(부족).
> 🟢 ② 그래서 `--rebalance --fill 0.7` 을 썼다(**갱신형 조각 과충전의 지정 도구** · `R1-6`).
> 🔴 **`▣BBBB2` 의 「`--rebalance` 금지」와 충돌하지 않는다** — 그 금지는 **허브 색인 팽창**에 대한 것이고
> 이번 대상은 **조각 과충전**이다. ⇒ 🔴 **금지 조문을 적용 범위와 함께 읽어라.**
> 🟢 **파급을 먼저 실측**했다(승인이 있어도 blast radius 는 재는 것이 순서다):
> 행번호 포함 원장 조각 인용 **8건** = `00_BRIEF.md` **6건(자동 생성물 ⇒ 비용 0)** + 실인용 **2건**.
> 🟢 결과 = 조각 max **28,207 B** · 최소 여유 **12,753 B** · 유실 **0**(이동 확인 53종 · 행선지 없음 0)
> ⇒ **`doc_type_gate` 여유 부족 0건**(완전 ✅).
> 🟢 실인용 2건 처리:
> · `99_NEXT_SESSION_조각/99_NEXT_SESSION-013.md:183` 「실질 조치 후보」 좌표 = `-003.md:11` → **`-007.md:17`** 로 시정(**살아 있는 지시**라 고쳐야 한다).
> · `_o124_evidence.md:51` 의 `-005.md:9·11·…` 는 **2행씩 밀렸다**(O101 이 `:7` → `:9`).
>   🔴 **고치지 않았다** — 그 파일은 원장 §0 에 **정적**으로 등재돼 있고 「발행 후 갱신하지 않는다」가 존재 조건이다.
>   ⇒ 🟢 **판정 = 근거철의 좌표는 발행 시점의 사실이고, 재균형 후에는 좌표가 아니라 「그 세션이 무엇을 봤는지」로 읽는다**
>   (O123-D 판정식 「서술은 낡지 않지만 좌표는 재균형으로 낡는다」의 정확한 사례다).
>   🟠 **남는 위험** = 다음 세션이 그 좌표를 그대로 열면 다른 행을 본다 ⇒ 근거철 인용 시 **행 번호가 아니라 내용 토큰으로 확인하라.**

> ##### 🟢 O124-B 검증 결과 (전건 리다이렉트로 rc 수신)
>
> `gate_census` **rc=0** · `--run-tests` **rc=0**(TEST 24종 전건 rc=0) ·
> `doc_census --strict` **rc=0** · `doc_type_gate` **rc=0(경고 0)** · `index_row_gate` **rc=0** ·
> `doc_heading_gate` **rc=0** · `doc_coord_gate` **rc=0** · `decision_closure_gate` **미봉합 0**.
> 🟢 `read` 미반환 **0** / 재호출 **0**. 🔴 라이브 DDL/DML **0** · dbt **0**.
> 🟠 **파괴 가능 연산**(사용자 승인 전건) = 허브 **11종 `--republish`** + 원장 **`--rebalance --fill 0.7`**
> ⇒ 전건 「본문 바이트 동일 · 유실 0 · 상한 초과 0」 확인 · 스냅샷 자동(`snapshot_util` 경유 · 라벨 `O124-B`).
>
> ##### 🔴 O124-B 가 하지 않은 것 (범위 명시)
>
> · **b 축(개명)은 여전히 실행하지 않았다** — 권장·차선안 제시 상태이고 사용자 답변 대기다.
> · `09` 재생성 **미실행**(라이브·census 입력 필요) ⇒ 표 열수 잔여 **5건**은 그대로다.
> · `10_원천입고_결손요약` A·B·C 계열 재분류 **미실행**(판정 작업 · `R2-8-4-c`).
> · 착수표 ② ④ ⑤ ⑦ ⑪ ⑫ ⑭ ⑰ ⑱ ㉙ ㉚ ㉛ **열린 상태 유지**.
>
> ##### 🟢 O124-B 신설 판정식 3조 (승계)
>
> ㉠ 🔴🔴 **「없다」는 분모와 함께만 말할 수 있다** — 경로·폴더를 한정한 검사 결과를 「부재」로 쓰지 마라.
>   실물 = 근거철을 `20_issue/` 만 보고 「실재하지 않는다」고 **문서에 발행**했다(실제는 루트).
>   🟢 판정법 = **전역 인덱스**(`basename_index` 형)로 확인하거나, 못 했으면 **「그 경로에 없다」**로 적어라.
> ㉡ **잘못된 좌표는 설명하려고 적을 때도 좌표다** — 오류 경로를 본문에 문자열로 인용하지 마라.
>   실물 = 오판 경위를 적으면서 그 경로를 적었더니 좌표 게이트가 blocking 을 냈다(**옳은 동작**).
> ㉢ **코드가 통과해도 서술은 따로 검증하라** — 테스트는 동작만 보고 「무엇을 고쳤는가」는 보지 않는다.
>   실물 = `code_cell` 의 처방 서술이 방향이 거꾸로였는데 35단정이 전부 🟢 였다.

> #### 🟢 [2026-08-31 O125] SILVER·GOLD·SERVING 실재 검토 — 「구성됐다」와 「작동한다」는 다른 축이다
>
> **지시** = 「현재 수정된 테이블 설계에 맞게 silver·gold·serving 스키마의 테이블·뷰가 생성되어
> 있는지 검토」(브리핑 후 즉시 수행 · `R4-4-2 ㉡`).
> 🟢 착수 게이트는 리다이렉트로 rc 를 받았다(`| tail` 금지 · O120 판정식):
> `gate_census.py` **rc=0** · `session_brief.py --write` **rc=0** · `id_collision_gate --next O` = **O125**.
> 🟢 라벨은 착수 전에 원장 §1 에 **선점 등재**했다(`R1-4-3`).
>
> ### 1. 판정 결과 — 객체 실재는 전건 일치(미생성 0건)
>
> 판정 근거는 **조회 시점 라이브**이고 문서 기재가 아니다(`R2-4`). 재현 = `python3 scripts/o125_layer_census.py`.
>
> | 층 | 설계 분모 | 판정 |
> |---|---|---|
> | SILVER 테이블 | dbt `models/silver` = `08_SILVER_테이블DDL_20260714.sql` 선언 | 🟢 미생성 0 |
> | GOLD 테이블 | dbt `models/gold/dim`+`fact` = `03_top-down_gold/06_DDL.sql` 선언 | 🟢 미생성 0 |
> | GOLD WIDE 뷰 | dbt `models/gold/wide` | 🟢 미생성 0 |
> | SERVING ML 뷰 | `21_ML_SERVING_뷰_DDL.sql` + 배포 러너 | 🟢 미생성 0 |
> | SERVING 시맨틱뷰 | `05_*_SV_DDL*.sql` + `22_ML_SV_DDL.sql` | 🟢 미생성 0 |
>
> 🔴 **개수를 이 항목에 박지 않는다**(`R3-9 ㉦`) — 값은 위 스크립트가 매 실행마다 센다.
>
> ### 2. 🔴 초판이 SERVING 분모를 0 으로 냈다 — 「0건」은 부재가 아니라 판정식 결함이었다
>
> 정본 SV DDL 은 `CREATE OR ALTER SEMANTIC VIEW` 인데 추출기가 `OR REPLACE` 만 봤다.
> 출력은 「설계 0 · 라이브 17 · 미생성 0」으로 **✅ 를 냈다** — 🔴 **분모가 비면 「누락 0」은 자동으로 참**이다.
> 🟢 `O111 ㉠`(0건을 받으면 분모를 먼저 의심하라)을 자기적용해 문법을 넓혀 **17** 을 회복했다.
> 🟢 파서 음성 확인 = 빈 집합 0 · 표본 컬럼수(37·38)가 `table_ddl_column_gate` 실측과 일치.
>
> ### 3. 컬럼 축 확장 — 기존 게이트 분모는 7개뿐이었다
>
> `table_ddl_column_gate` 는 **7개 테이블**만 본다(그 도구의 스코프). 이 세션은 DDL 정본 ↔ 라이브
> **컬럼 이름 집합**을 GOLD 37개·SILVER 42개 전건으로 대조해 **불일치 0** 을 얻었다.
> 🔴 **순서는 판정하지 않았다** — 순서 드리프트 2건(`DIM_MEMBER_ACQUISITION`·`FACT_EVENT_PARTICIPATION`)은
> 그 게이트가 유지 중이고 **적재 위험이 아니다**(dbt insert 는 양측 이름 지정).
>
> ### 4. 🟠 잔여 — 전부 문서 축
>
> - dbt schema yml 미등재 **7모델**(`CRM_MARKETING_CAMPAIGN`·`CRM_MEMBER_SPONSOR_SPAN`·
>   `CRM_RELATION_ACTIVITY`·`DIM_MARKETING_CAMPAIGN`·`FACT_MEMBER_FEE`·`FACT_MEMBER_SPONSOR_BIZ`·
>   `FACT_TARGET_BIZ`) ⇒ 착수표에 신규 등재(`R3-5`).
> - 라이브에만 1건 = `SILVER.BIGQUERY_REFINED_DATA` — 외부 Python 적재이고 `_sources.yml` 에 source 로
>   등재돼 있으며 SILVER DDL 은 **의도적으로 주석 처리**돼 있다 ⇒ 🟢 **결함이 아니다**.
> - 객체는 있으나 **0행 2건** = `GOLD.FACT_TARGET_BIZ` · `SILVER.CRM_BIZ_TARGET`(원천 미입고 `E-6`).
>
> ### 5. 🟢 산출물
>
> - `scripts/o125_layer_census.py` 신설 — `gate_census` **JUDGE** 에 등재(미분류 FAIL 을 즉시 받아 시정).
> - 게이트 rc = `table_ddl_column_gate` 0 · `dbt_schema_lint` 0 · `wide_select_yml_gate` 0 ·
>   `sv_identifier_gate` 0 · `doc_census --strict` 0 · 행키·제목 0 · 음성 테스트 전건 0.
> - `read` 미반환 **0** · 재호출 **0** · dbt 실행 **0** · 파괴 연산 **0**.
>
> 🔴 **이 항목의 한계는 §O125-B 가 지적한다** — 존재 판정을 「설계대로 작동한다」로 읽으면 틀린다.

> #### 🟢 [2026-08-31 O125-B] O125 자기검토 — 존재 판정을 「정상」으로 읽히게 발행한 것이 최대 결함이다
>
> **지시** = 「이슈문서들을 읽고 이번 세션 작업을 비판적으로 검토 · 승인이 필요한 작업은 전부 승인 ·
> 작업과 문서를 개선」. ⇒ `--rollover` 등 파괴 가능 연산은 **이 승인으로** 집행했다(`R4-4-3`).
>
> ### D1 🔴🔴 확정위반 — 「설계에 맞게 생성됐다」는 값 축을 포함하는 말로 읽힌다
>
> O125 는 **객체·컬럼 이름 집합**만 판정하고 보고에는 「전건 일치 ✅」를 앞세웠다.
> 그런데 정본은 그 반대를 말한다 — 문서50 **`BLOCKING-5`**(열림)는 *「행수는 있으나 대부분의 카운트
> measure·차원 FK 가 전건 0/NULL」* 이다. 🔴 **구조가 있다는 판정이 「지표가 나온다」로 읽히면
> 조용한 오답의 입구**가 된다(Cortex Analyst 가 그 위에 서면 0 을 자신 있게 답한다).
> 🟢 **처방 = 도구가 값 축을 함께 재고 「판정하지 않는 축」을 스스로 선언한다**
> (`o125_layer_census.py` 머리말 ㉠~㉥ + 말미 `값 적재 축 [관측]` 절).
> 🟢 재실측(2026-08-31 · 조회 시점):
> `FMM` CAMPAIGN_SK **0**/40,054,883 · PAYMENT_SK **0** · `FSE` CAMPAIGN_SK **0**/38,467,142 ·
> `FEP` CAMPAIGN_SK·SPONSORSHIP_SK **0**/1,134,126 · `FBD` CAMPAIGN_SK·ORG_SK **0**/2,964.
> ⇒ 🔴 **캠페인 축은 5개 팩트에서 지금도 전건 0 이다.**
>
> ### D2 🔴🔴 판정약점 — 배포 상태 정본을 우회했다
>
> SERVING 을 검토하면서 **배포 상태 정본**인 `05_SV-Agent_ai/04_SV_설계.md` **§0.9** 를 읽지 않고
> 파일 파싱으로 자체 분모를 만들었다. 결과는 우연히 일치했지만(17) **경로가 정본을 우회**했고,
> 그 정본이 담은 **라이브 결손**을 보고에서 빠뜨렸다: `SV_BUDGET.TOTAL_EXEC_BUDGET` 이
> **+79.7% 과대값을 발행 중**(§0.9-D · `DEC-44` · 사람 결정 대기)이다.
> ⇒ 🔴 **「SERVING 17/17 ✅」와 「SERVING 이 틀린 값을 발행 중」이 동시에 참**이다.
> 🟢 처방 = 스크립트 헤더가 §0.9 와 `BLOCKING-5` 좌표를 **출력에 싣는다**(다음 세션이 우회하지 못한다).
>
> ### D3 🔴 정본 stale 2셀 적발 — 재실측이 기재를 반박했다(`BBBB4 ㉠` 적용)
>
> `BLOCKING-5` 표의 「전건 0/NULL」 열에 있던 두 셀이 실측과 달랐다:
> - `FMM.REASON_SK` = **3,164,724 / 40,054,883**(7.90%) — 🔴 **이번에 처음 적발**
> - `FME.ORG_SK` = **3,594,835 / 4,633,105**(DEV 한정 · O38 배선분) — 한정 표기 누락
>
> 🔴 그 절 말미의 교훈이 *「부분 적재 축은 반드시 한정 표기를 붙인다」* 인데 **그 절이 자기 교훈을
> 두 셀에서 어겼다.** 🟢 두 셀을 취소선 + 실측 병기로 정정했다(삭제가 아니라 병기 · `§C24` 처방).
>
> ### D4 🟠 확정위반 — 원장 §1 행에 라이브 수치를 박았다
>
> 초판 O125 행은 `42/42`·`37/37`·`17/17`·`743`·`830` 을 본문에 박았다 — `R3-9 ㉦` 위반이고
> **O124-B `D4` 와 같은 결함의 재발**이다(라이브 수치는 다음 적재에서 stale 이 된다).
> 🟢 수치를 지우고 **판정(미생성 0) + 재는 방법(스크립트 경로) + 판정 축 선언**으로 대체했다.
>
> ### D5 🟠 구조결함 — 게이트를 신설하면서 음성 테스트를 같이 만들지 않았다
>
> `gate_census` **JUDGE** 에 등재까지 하고 음성 테스트가 **0** 이었다(`R3-2`·세션 규칙 ⑥ 위반).
> 🟢 `scripts/test_o125_layer_census.py` 신설 — **오염 기반 축**(정본 문법을 변형해 분모가 0 이 되는 것을
> 재현) + 스키마 한정 오탐 축 + 빈 집합 금지 축 + `column_report` 양방향 축 + 재현율 축.
> 🔴 **단정 수를 문서에 적지 않는다** — 테스트가 스스로 세어 출력한다.
>
> ### D6 🟢 자기검토가 확인한 것(반박 아님)
>
> - `SILVER.BIGQUERY_REFINED_DATA` 를 「설계 밖」으로 적지 않고 **source 등재 + DDL 주석**을 근거로
>   결함 아님으로 판정한 것은 유효하다(`_sources.yml` 실재 확인).
> - 「누락/과잉 GOLD 6개」(문서30 §6 · 문서50 🟡)는 **객체 축에서는 닫혀 있다** — 4종 작성완료 ·
>   `FACT_TARGET_BIZ` 0행 스켈레톤 실재 · `DIM_MEMBER_IDENTITY` 활성. 🔴 열려 있는 것은 **measure·FK 배선**이다
>   ⇒ 그 절 제목의 🟡 는 값 축 때문이며 「테이블이 없다」는 뜻이 아니다.
>
> ### 판정식 신설 (승계)
>
> ㉨ 🔴🔴 **실재 판정과 값 판정을 한 문장에 담지 마라** — 「생성됐다」는 「채워졌다」를 함의하지 않는다.
>   도구는 **자기가 판정하지 않는 축을 스스로 선언**해야 하고, 선언이 없으면 사용자는 ✅ 를 전체 판정으로 읽는다.
> ㉩ 🔴 **분모를 파일 파싱으로 만들기 전에 「그 분모의 정본이 이미 있는가」를 물어라** —
>   있으면 그 정본을 인용하고, 만들었으면 **정본 좌표를 출력에 실어라**.
> ㉪ 🔴 **표의 「전건 0」 열은 부분 적재를 표현할 수 없다** — 한정 표기(`(DEV 한정)`)가 없는 셀은
>   재실측 대상이다. 실제로 두 셀이 그렇게 낡아 있었다.
