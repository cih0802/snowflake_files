<!-- SPLIT-CHUNK 01_세션이력.md | 051/051 | 허브 = 01_세션이력.md | 원문 1~164행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-30 O123] BRONZE→SILVER→GOLD 컬럼 명명 규약 — DEC-46 발급(DEC-25 전 계층 확장) + Phase 1 ERP 계보 라벨링 완료
>
> **지시** = O122 계획의 Phase 0~1 착수. `R4-4-2 ㉡` 적용(지시 동봉 ⇒ 브리핑 후 즉시 수행) · 확정위반 **0건**.
>
> ##### ① 착수 직후 게이트 — 🟢 rc 를 리다이렉트로 받았다
>
> 지시가 요구한 대로 파이프 없이 실측했다(`O120` 판정식).
> · `python3 scripts/gate_census.py` → **rc=0**
> · `python3 scripts/gate_census.py --run-tests` → **rc=0**(음성 테스트 **22종 전건 rc=0** · `NEEDS_ARGS` 3종 규약 **rc=2**)
> 🔴 브리핑에 옮겨 적은 값이 아니라 **이 세션이 실제로 돌린 종료코드**다. 세션 말미 재실행도 **rc=0**.
>
> ##### ② 라벨 선점 — 🔴 원장에 먼저 등재한 뒤 착수했다
>
> `id_collision_gate --next O` = **정의 122 · 참조 122 ⇒ O123**(양축 동일이라 `R1-4-2` 과대 위험 없음).
> 🟢 **O122 의 실패를 반복하지 않았다** — 파일명에 먼저 쓰지 않고 **원장 §1 에 한 줄 선점 등재를 먼저** 했다(`R1-4-3`).
>
> ##### ③ 🔴 파괴 가능 연산은 전건 별도 승인을 받았다(`R4-4-3`)
>
> 원장 chunk 여유 **344 B** · `99_NEXT` 여유 **2 B** 여서 선점 등재 자체가 재균형을 요구했다.
> 지시가 동봉됐지만 `R4-4-3` 이 `R4-4-2 ㉡` 보다 강하므로 **승인을 먼저 받았다**(원장 4회 파손 이력).
> 승인 = ㉠ `00_INDEX` + `99_NEXT` 재균형 ㉡ 개명표 신설 문서 분리.
>
> **재균형 실측**(양쪽 `--expect` 신선도·영속성 가드 통과 · 토큰 유실 0):
> · `00_INDEX` = 조각 max 40,616 → **28,243 B**(여유 344 → **12,717 B**)
> · `99_NEXT` = 조각 23 → 24개 · max 40,958 → **27,637 B**(여유 2 → **13,323 B**)
>
> 🔴 **잔여 `doc_census` 🟠 1건은 조각이 아니라 허브다** = `99_NEXT_SESSION.md` **33,652 B**(여유 7,308 B).
> 허브는 자동 생성 목차이고 **조각이 늘면 커지므로 `--rebalance` 로는 줄지 않는다.**
> ⇒ 종전 「여유 2 B」는 **조각 축**이었고 그것은 해소됐다. 🔴 **두 축을 혼동하지 마라**(`R3-9 ㉡` 계열).
>
> ##### ④ Phase 0 — DEC-46 발급 · 3분류 규약 명문화
>
> `id_collision_gate --next DEC` = 정의 36 · 참조 45 ⇒ **DEC-46**. 🔴 참조 축 과대를 의심해 재측정했고
> `DEC-43` 50회 · `DEC-44` 91회 · `DEC-45` 24회 · **`DEC-46` 0회**로 공번을 실증한 뒤 발급했다.
>
> 등재 = `30_설계_의사결정_조각/30_설계_의사결정-012.md` **§32**(절 번호 **순서 말미** · `clause_order_gate` rc=0).
> · **32-A** = 3분류 판정식(P = 로직 0 → 원천명 / L = join·union all·계산·키승격 → 의미명 / X = 동명이의 → 변별 접두)
> · **32-B** = 사용자 확정 4건(GOLD WIDE 93 유지 · 키승격 유지 · SILVER 원천명 복원 · GA4 제외)
> · **32-C** = `ERP_BUDGET` 예외 · **32-D** = `DEC-25` 15-D 승계 · **32-E** = 종결 조건
> 🟢 **`DEC-25`(§15)에 양방향 포인터를 병기**했다 — 「대체가 아니라 확장 · 두 절을 함께 읽어라」(`R3-9 ㉥`).
> 🔴 **신규 규약으로 쓰지 않았다** — 지시가 확정한 대로 **`DEC-25` 의 확장**으로만 등재했다.
>
> ##### ⑤ 🔴 지시가 예고한 ERP_BUDGET 예외를 실측으로 확인하고 사용자 결정을 받았다
>
> `YB`·`CB`·`AB`·`EX` 는 원천명이 아니라 **12-way `UNION ALL` 언피벗의 위치 별칭**임을 실측했다
> (`10_dbt_pipeline/models/silver/erp/ERP_BUDGET.sql` **14-25행** · 원천 = `YEAR_BDGT_AMT_1..12` 계열 · 6-11행).
> ⚠️ 지시의 경로 표기 `models/silver/erp/…` 는 실재하지 않는다 — 실제 경로는 **`10_dbt_pipeline/` 접두**가 붙는다.
> 🟢 **사용자 결정 = 분류 L 로 재판정** ⇒ 현행 4개 이름 유지 · **개명 0건**.
> 🟢 **판정 근거를 더 좁혔다** — 별도 예외 조항이 필요한 것이 아니라 **`union all` 이 있으므로 32-A 의 L 규칙 그 자체**다.
>
> ##### ⑥ Phase 1 — 🟢 분모를 라이브로 확정했다(169컬럼 / 26 계보)
>
> 🔴 **계정이 또 바뀌었다** — O120 기재 `YQ47212` ↔ 실측 **`KA98941`**(`P169` 계열).
> `R3-9 ㉤` 대로 닫기 전에 재조회했고 `GN_DW` 13스키마 · SILVER 948 · BRONZE 1,153 · GOLD 1,320 컬럼이 **실재**했다.
> 🟢 ⇒ **이 계정은 「문서작업 전용」이 아니다.** 🔴 원장 §0 의 `92_실측필요_후속작업` 등재 근거문
> (*"이 계정은 문서작업용이라 라이브 판정이 불가"*)이 **이 계정에서는 stale 이다** ⇒ 다음 세션 과제로 넘긴다.
>
> `scripts/o122_name_drift.sql` **§A** 실측 = **O122 기재값과 정확히 일치**했다:
> `BRONZE_CRM` 302/225/**77** · `BRONZE_AGENCY` 186/111/**75** · `BRONZE_ERP` 25/8/**17** ⇒ 합계 **169**.
> 범위 밖 = `SILVER_BASE`(GA4) 88(결정 ④) · `BRIDGE` 9 · `EXTERNAL_PYTHON` 118(SQL 자체가 제외).
> 🟢 **계보 = 26**(CRM 15 + AGENCY 8 + ERP 3) · 합계 검증 통과.
>
> 🟢 **지시대로 계보 단위로 집계했다** — §B 를 줄 단위로 뽑지 않고 `LISTAGG` 로 **테이블별로 롤업**했다.
>
> ##### ⑦ ERP 3 계보 17컬럼 라벨링 완료 = L 12 + P 5 ⇒ 개명 대상 5건
>
> 🔴 **개명 실행은 0건이다**(선행조건 미충족). 확정된 개명표:
> · `ERP_BUDGET_ITEM.BUDGET_UNIT_NM` → `BDGT_UNIT_NM`
> · `ERP_BUDGET_ITEM.INCOME_EXPENSE_DIV` → `INCOME_EXPS_DIV_NM`
> · `ERP_BUDGET_YEARLY.YEAR_BUDGET_TOT_AMT` → `YEAR_BDGT_TOT_AMT`
> · `ERP_BUDGET_YEARLY.CHN_BUDGET_TOT_AMT` → `CHN_BDGT_TOT_AMT`
> · `ERP_BUDGET_YEARLY.ADJ_BUDGET_TOT_AMT` → `ADJ_BDGT_TOT_AMT`
> 🟢 앞 2건은 **O122 가 「라이브 축이 텍스트 축이 놓쳤다」고 지목한 바로 그 2건**이고, 계보 독해로 **P 위반임이 확정**됐다.
>
> **🟢 최대 실질 발견 = 계보 단위 판정이 실제로 갈렸다.**
> 겉모습이 같은 `BDGT`→`BUDGET` 약어확장인데 `ERP_BUDGET`(월)은 **L(유지)**,
> `ERP_BUDGET_YEARLY`(연)은 **P(개명)** 이다 — 전자는 `UNION ALL` 을 지나고 후자는 **로직 0 순수 투영**이다.
> 🔴 **줄 단위로 봤다면 둘을 같게 처리해 한쪽이 반드시 틀렸을 것이다** ⇒ 지시의 「계보 단위」 요구가 실증됐다.
>
> **🟢 P 판정의 근거를 코드베이스 자신에서 찾았다** — `ERP_BUDGET_ITEM.sql` 10-16행이
> `JANG_NM`·`KWAN_NM`·`HANG_NM`·`MOK_NM`·`DTL_ITEM_NM`·`SUBDTL_ITEM_NM`·`FUND_SOURCE_NM` 에
> **동일한 `NULLIF(TRIM(...))` 를 걸면서 원천명을 유지**한다 ⇒ 「`NULLIF`/`TRIM` 은 로직이 아니다」가 **이 프로젝트의 관례**다.
> 🟢 대조군도 있다 — 같은 자리 4개 금액 중 `EXEC_TOT_AMT` 만 원천과 동명이다(부재 목록에 없다)
> ⇒ 3건은 「의도적 의미명 부여」가 아니라 **일관성 없는 약어확장**이다.
>
> ##### ⑧ 판정 원칙 3건을 사용자 결정으로 확정했다(지시에 없던 쟁점 — 측정이 새로 만들어냈다)
>
> · **㉠ 타입 캐스트(`TRY_TO_NUMBER`·`CAST`)는 로직이다 ⇒ 분류 L.** 계기 = `BUDGET_YEAR` 3건의
>   원천명이 **`YEAR`(SQL 예약어/함수명)** 여서 복원하면 전 소비처에 인용부호가 필요해진다.
>   🟢 L 로 재판정하니 **예약어 문제가 원리적으로 사라졌다**(개명 0건) — 예외 조항을 만들지 않아도 됐다.
> · **㉡ `NULLIF`/`TRIM` 만 거친 컬럼은 로직 0 ⇒ 분류 P**(근거 = ⑦ 의 코드베이스 관례).
> · **㉢ 형제 모델 어휘 불일치는 수용 — 규약 우선.** 개명 후 같은 ERP 원장에서 온 두 모델이
>   `*_BUDGET_AMT`(월 · L)와 `*_BDGT_TOT_AMT`(연 · P)로 갈린다.
>   🔴 **결함이 아니라 승인된 결과다** — 문서 두 곳에 「되돌리지 마라」를 명시했다(다음 세션의 오판 차단).
>
> ##### ⑨ 🔴 조각 상한 초과를 만들었고 승인받아 문서 분리로 해소했다
>
> 32-F 에 라벨링 표를 넣자 `30_설계_의사결정-012.md` 가 **319줄**이 되어 **300줄 상한을 19줄 초과**했고
> `--republish` 가 **rc=1(게이트6)** 로 막았다. 🔴 바이트는 28,008 B 로 여유가 있었다 ⇒ **줄 수 축 단독 초과**다.
> 🟢 **가드가 제대로 작동해 내가 상한을 넘긴 채 넘어가는 것을 막았다**(`R1-6` 불변식).
> 🟢 사용자 결정 = **개명표를 신설 문서로 분리** ⇒ `20_issue/32_컬럼개명표.md` 신설(작업표) ·
> §32-F 는 **포인터로 축약**(297줄로 상한 이내 복귀 · 🔴 사본을 두지 않았다 — 두면 한쪽이 낡는다 · `R3-9 ㉡`).
>
> **`R1-6-18` 순서를 지켰다**: ① 원장 §0 유형 등재표에 **갱신형으로 먼저 등재** ② `write` 툴 + LLM-METADATA 헤더
> ③ 상한 이내이므로 **분할하지 않았다** ④ 분모 3곳 손 편입(`doc_heading_gate.DOCS` · `doc_line_length_gate.CANON` ·
> `doc_census.SINGLES` · `doc_type_gate` 는 `20_issue/*.md` 자동 발견) ⑤ 게이트 확인.
>
> ##### ⑩ 🔴 `R3-9 ㉢` 이 실제로 걸렸다 — 분모 편입과 골든 발행은 별개다
>
> 분모 3곳을 편입한 직후 `doc_heading_gate` 가 **rc=1** 로 막았다:
> *"20_issue/32_컬럼개명표.md: 골든 미등재 — 제목 11개가 유실 검사 대상 밖이다."*
> 🟢 **지침이 예고한 그 상태를 게이트가 침묵이 아니라 FAIL 로 잡았다**(O83-H 가 ⚪→🔴 로 승격시킨 효과).
> 🟢 유실 0 을 먼저 확인(차이가 전부 신설 = 32_컬럼개명표 11 · 30_설계 7 · 01_세션이력 33)한 뒤
> **`--reason` 을 달아** 발행했다(`R1-7-4` · 이력 56회) ⇒ 재검 **rc=0**.
> 🔴 **FAIL 을 골든으로 덮은 것이 아니다** — 사유에 유실 0 근거를 숫자로 적었다.
>
> ##### ⑪ 🔴 다음 세션이 반드시 알아야 할 함정 1건 — `kind_hint` 를 판정으로 쓰지 마라
>
> `o122_name_drift.sql` §B 의 `kind_hint` 는 **`DEC-25` 15-A 가 명시한 동명이의 변별 접두 2건**
> (`CRM_PAYMENT_BILLING.MBRFEE_PRCS_STAT_CD` · `CRM_SEND_REQUEST.PSTMTR_PRCS_STAT_CD`)을
> **`P?(원천명 복원 후보)` 로 표시**한다. 🔴 **힌트를 그대로 따르면 그 두 건은 분류 X 인데 개명되어 규약을 되돌린다.**
> 🟢 두 문서(`32_컬럼개명표.md` §4 · `DEC-46` 32-F)에 이 경고를 명시했다. 그 SQL 자신도 94행에서 *"`kind` 는 힌트다"* 라고 적고 있다.
>
> ##### ⑫ 하지 않은 것 — 🔴 범위를 지켰다
>
> · **`ALTER TABLE … RENAME COLUMN` 0건 · 라이브 DDL/DML 변경 0건.** 라이브는 **메타데이터 조회만** 했다.
> · **`dbt parse`/`compile`/`build` 0건**(`R4-1` 정지점 · 아직 그 단계에 도달하지 않았다).
> · **AGENCY 8 계보 75컬럼 · CRM 15 계보 77컬럼 = 라벨링 미착수**(계보 독해 미실시 ⇒ 라벨을 창작하지 않았다).
> · **`table_ddl_column_gate` 분모 확대 미착수**(개명 실행의 선행조건 · 착수표 항목으로 남긴다).
> · **GA4 계열 미착수**(결정 ④ 로 범위 제외 · O121 잔여와 함께 재개한다).
> · **착수표 ⑫·⑭ 컬럼 미배선**(현업 결정 전 배선 금지).
>
> ##### ⑬ read 미반환 · 독해 기록
>
> **미반환 0건 · 재호출 0건**(판정식 = 재호출 ≥ 미반환 ⇒ 충족).
> 전량 확보한 정본과 고유 토큰(`R1-3-7-b`):
> · `00_guides/00_작업지침_세션운영규칙.md` 1-316 · `R4-4-4` / `㉦㉧ 는 O106` / `BusyBox awk`
> · `20_issue/00_BRIEF.md` 1-142 · `O121-B` / `2,183,273 자`
> · `20_issue/30_설계_의사결정_조각/30_설계_의사결정-005.md` 1-185 · `15-C. 동명이의 해법 선택 규칙 (P32)` / `PSTMTR_PRCS_STAT_CD`
> · `scripts/o122_name_drift.sql` 1-175 · `BRONZE_BIGQUERY 함정` / `EXTERNAL_PYTHON`
> · `10_dbt_pipeline/models/silver/erp/ERP_BUDGET.sql` 1-41 · `BDGT_ACMSLT_LEDGER` / `YB  AS YEAR_BUDGET_AMT`
> · `…/ERP_BUDGET_ITEM.sql` 1-23 · `NULLIF(TRIM(BDGT_UNIT_NM),'')`
> · `…/ERP_BUDGET_YEARLY.sql` 1-29 · `연 총액 4종 (편성·추경·조정·집행)` / `grain 이 다르면 모델을 나눈다`
>
> ##### ⑭ 게이트 종료 상태 (🔴 전부 파이프 없이 rc 수신)
>
> `gate_census.py --run-tests` **rc=0** — 음성 테스트 **22종 전건 rc=0** · `NEEDS_ARGS` 3종 **rc=2**.
> 개별 = `doc_census`·`doc_type_gate`·`index_row_gate`·`doc_heading_gate`·`doc_coord_gate`·
> `clause_order_gate`·`doc_line_length_gate`·`id_collision_gate`·`decision_closure_gate` **전건 rc=0**.
> 스테이지 실체 확인 = `cortex ws ls` 로 `32_컬럼개명표.md` **10,704 B**(블록 반올림) 실재.
> 🔴 **게이트 전수 PASS 는 지침 준수의 증거가 아니다**(`R3-9`) — 아래가 이 세션에 남은 열린 축이다.
>
> ##### ⑮ 🔴 자기검토 — 게이트가 🟢 인 상태로 남는 것
>
> · **A. 라벨링이 3/26 계보에서 멈췄다.** 169컬럼 중 **17컬럼(10.1%)** 만 판정했다. 남은 152컬럼은
>   **판정하지 않았고 힌트도 판정이 아니다** ⇒ 「Phase 1 완료」로 읽으면 거짓이다.
> · **B. 93건·11건·178파일·7/80 은 내가 재측정하지 않았다.** O122 계획의 기재값을 **출처를 명시해** 인용했을 뿐이다
>   (`R3-1`). 🔴 다음 세션은 이 4개 수치를 **인용하지 말고 재라.**
> · **C. `92_실측필요_후속작업` 의 등재 근거가 이 계정에서 stale 이다**(⑥) — 적발만 했고 고치지 않았다.
>   🔴 「적발했다고 쓰는 것」과 「정본을 고치는 것」은 다르다(`P154` 축) ⇒ 착수표에 올린다.
> · **D. `99_NEXT` 허브 여유 7,308 B 는 구조적으로 줄지 않는다**(조각이 늘면 목차가 커진다).
>   지금은 통과하지만 **조각이 더 늘면 허브가 상한에 닿는다** ⇒ 그때는 재균형이 아니라 **목차 형식**을 봐야 한다.
> · **E. ERP 개명 5건은 소비처 전수 grep 을 하지 않았다**(`DEC-25` 15-D 의 요구) ⇒ 개명 실행 전 필수 선행이다.
