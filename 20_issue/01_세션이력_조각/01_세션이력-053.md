<!-- SPLIT-CHUNK 01_세션이력.md | 053/053 | 허브 = 01_세션이력.md | 원문 1~62행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-30 O123-D] O123-C 정본 소급 등재 — 프롬프트 서술을 재실측했더니 4건이 틀렸다
>
> **지시** = 「미해결 이슈 진행 승인 ①사실 재확인 → 근거 파일 기록(`R1-3-7-c`) → ②4곳 등재」.
> 🔴 이 세션은 **새 번호를 발급하지 않았다** — 내용이 O123 계열의 등재 작업이므로 **접미로 이었다**
> (`R1-4-3` · `id_collision_gate --next O` 실측 = `정의 123 · 참조 123 ⇒ O124` 를 **소비하지 않았다**).
>
> ### 1. 🔴🔴 착수 전제 — O123-C 는 정본에 0건이었다
>
> `grep -rl "O123-C" 20_issue/ 99_NEXT_SESSION_조각/` → **rc=1 · 매칭 파일 0개**.
> ⇒ 원장 §1 · 이력 · 인수인계 어디에도 없었고 **인수인계 프롬프트가 유일한 운반체**였다.
> 🔴 이것이 `R4-2`(큰 작업 종료 시 `20_issue/` 갱신)의 실패다 — 작업은 했고 **상태를 남기지 않았다**.
> 🟢 착수 게이트는 프롬프트 요구대로 **리다이렉트로 rc 를 받았다**(`| tail` 금지 · O120 판정식):
> `gate_census.py` **rc=0** · `--run-tests` **rc=0**(TEST **22종 전건 rc=0** · `NEEDS_ARGS` 3종 **rc=2**).
>
> ### 2. 🔴🔴 최대 발견 — 프롬프트 서술 4건이 실측과 달랐다
>
> `R1-3-7`(대체 근거 금지)에 따라 프롬프트를 정본으로 쓰지 않고 파일에서 다시 쟀다. 결과:
>
> | # | 프롬프트 서술 | 실측 | 성격 |
> |---|---|---|---|
> | ㉠ | `20260826_아키텍처 지도.html` | **`20260826_아키텍처 지도 요약본.html`** | 파일명 오기 |
> | ㉡ | 「환경 지뢰 **신규**」 | `/tmp` 소실은 **`-024.md:56` 에 기등재** | **과대 주장** |
> | ㉢ | 「`-013 §7` 에 등재하라」 | §7 좌표는 **`-024.md:4`**(`-013` 은 0건) | **stale 좌표** |
> | ㉣ | 「`--label` 지원 여부를 확인하라」 | **이미 지원한다**(`:746` + CLI + `resolve_label`) | 처방 오설정 |
>
> 🔴 **㉡ 가 가장 위험했다** — 「신규 지뢰」로 등재하면 같은 항목이 두 곳에 생기고
> **한 곳이 반드시 낡는다**(`R3-9 ㉡`). ⇒ 신규인 것은 **그 지뢰의 결과와 처방**이므로
> 기존 항목(`-024.md:56`)에 **보강으로 붙였다**(새 항목 신설 0).
> 🔴 **㉣ 는 처방의 방향을 뒤집었다** — 「도구를 고쳐라」가 아니라 **「절차를 지켜라」**다
> (`export SESSION_LABEL` 미이행이 원인이고, 그 지시는 `§0-AAAA:49` 에 이미 있었다).
> ⇒ 🔴 **판정식 = 인수인계 프롬프트는 「무엇을 확인할지」의 목록이고 「확인된 사실」이 아니다.**
> 4건 중 2건이 좌표·명칭이었다는 점이 중요하다 — **서술은 낡지 않지만 좌표는 재균형으로 낡는다.**
>
> ### 3. 🔴 방법론 자기시정 — 내 키 정의가 「소실 10건」을 만들어냈다
>
> 08 보존율 델타를 검증할 때 키를 **선두 3열**(`SILVER_TABLE`·`COLUMN`·**`STATUS`**)로 잡아
> 「사라진 키 **10** · 신규 **226**」을 얻었고, 프롬프트의 「사라진 0 · 신규 216」과 어긋나
> **프롬프트가 틀렸다고 보고할 뻔했다.** 🔴 원인은 데이터가 아니라 **내 판정식**이었다 —
> `STATUS` 는 **판정값**이라 상태 전이(`REFERENCED` 238→291 · `DROPPED` 143→141)가 **소실로 보인다.**
> 🟢 키를 `(SILVER_TABLE, COLUMN)` 2열로 고치니 **사라진 0 · 신규 216** 으로 프롬프트와 일치했다.
> ⇒ 🟢 **신설 판정식 = 소실·중복 판정의 키에는 식별자만 넣어라. 판정값을 키에 넣으면 전이가 소실로 보인다.**
> 🔴 이것은 `O111 ㉠`(「0건」은 판정식이 못 본 것일 수 있다)의 **역방향 쌍**이다 —
> **「N건 있다」도 판정식이 만들어낸 것일 수 있다.** 🟢 그리고 이 세션은 **보고 전에 스스로 잡았다.**
>
> ### 4. 🟢 등재 4곳 (프롬프트가 운반하던 내용의 정본화)
>
> · **원장 §1** = `O123-C` 행 + 이 행(`O123-D`) 신설 · `--republish --expect` 로 신선도 단정.
> · **이력** = `§O123-C`(작업 서술 · 정정 4건 병기) + `§O123-D`(이 항목) · `--rollover`.
> · **인수인계** = `§0-AAAA` 에 **`▣ AAAA8`** 추가(새 절 신설 0 ⇒ 조각 001 용량 압박 회피).
> · **환경 지뢰** = `-024.md:56` 의 `/tmp` 항목에 **보강 3줄**(골든 지문 결과 + 체인 처방).
> 🟢 **부수 효과 = 다음 세션 프롬프트가 짧아진다** — 「■ 최우선」 블록 전체가
> `정본 = 이력 §O123-C` 한 줄로 대체된다(운반체가 프롬프트에서 정본으로 옮겨졌다).
>
> ### 5. 🔴 이 세션이 하지 않은 것 (범위 명시)
>
> **라이브 DDL/DML 0 · dbt 0 · 개명 실행 0 · 산출물 재생성 0 · 파괴 연산 0.**
> 🔴 착수표 ㉙㉚㉛ 는 **열린 상태를 유지**했다 — 등재는 상태 기록이고 **해소가 아니다**
> (`R3-9 ㉠` 「이력 등재는 원장 갱신의 대체물이 아니다」의 짝 = **등재는 해소의 대체물이 아니다**).
> 🟢 `R4-4-2 ㉡`(지시 동봉) 적용 · **확정위반 0건**(롤백 판단 근거 · `R4-4-4`).
> 🟢 `read` 미반환 **0건** · 재호출 **0건**(판정식 `재호출 ≥ 미반환` 충족).
> 🟢 근거철 = **`_o123c_evidence.md`**(워크스페이스 **루트** · E0~E11 · 판정보다 먼저 기록).
> ⚠️ 🔴 **[O124-B 자기정정] O124 가 이 인용을 「부재」로 잘못 판정했다** — 경위 = §O124-B `D1`.
>
> ### 6. 🔴🔴 [승인 후 자기검토] 등재 자체가 3건의 결함을 남겼고 전건 시정했다
>
> 사용자 지시 = 「이슈문서를 읽고 비판적으로 검토 · 승인 전건 · 작업과 문서 개선」.
>
> **㉠ 🔴🔴 최대 결함 — 내 등재를 브리핑이 보지 못했다.**
> `session_brief.py` 가 `line.startswith('### ')` 로 `▣` 항목을 찾는데 **현행 절 `§0-AAAA` 는
> 전부 `> ### ▣`(blockquote)** 였다 ⇒ **AAAA1~AAAA8 8건이 전건 미추출**이고 브리핑은
> **「인수인계 항목 0」으로 조용히 흘렀다**(실측 = `grep -c "^> ### ▣ AAAA"` **8**).
> 🔴 **즉 등재의 목적(다음 세션이 브리핑 1회로 본다)이 무력화된 상태였다** — 내가 `▣ AAAA8` 을
> 쓴 직후에도 브리핑은 0 을 냈고, **나는 그 0 을 그대로 보고했다.**
> 🔴 `§0-ZZZ`(O119)는 비-quote 라 그때는 동작했다 ⇒ **형식 드리프트가 도구를 조용히 껐다.**
> 🔴 게이트 6종·음성 테스트 22종이 **전건 🟢 인 상태**에서 이 결함이 살아 있었다(`R3-9` 실물).
> 🟢 시정 = `dequote()` 신설(인용 접두 + **선행 공백**까지 정규화) · 「항목 0」이면 브리핑에
> **🔴 경고를 박아 분모를 의심하게 한다**(`O111 ㉠` 제도화) · 음성 테스트 **축5 신설**
> (`test_session_brief.py` 8축 **40단정** rc=0). 실측 = **항목 0 → 8**.
> 🔴 **자기시정 1건 동반** = 초판 `dequote` 가 `\s?` 로 공백 1개만 먹어 `>   ### ▣`(들여쓴 quote)를
> **또 놓쳤다**(테스트가 즉시 rc=1 로 적발). ⇒ 🟢 **음성 테스트가 같은 결함의 재발을 그 자리에서 잡았다.**
> ⇒ 🔴🔴 **판정식 2건 신설**:
>   · **마크다운 구조를 볼 때는 인용 접두를 먼저 정규화하라** — 서식은 의미가 아니다.
>   · **테스트 픽스처는 「정본이 실제로 쓰는 형태」를 넣어라** — 내가 만든 깔끔한 형태만 넣으면
>     그 테스트는 형식 드리프트를 **구조적으로 못 잡는다**(축4 통과 ↔ 실물 0건이 그 증거다).
>
> **㉡ 🔴 내가 용량 회귀를 만들었다.**
> 원장 2행(1,517 + 1,688자)이 `00_INDEX` 조각 여유를 **6,931 → 1,687 B** 로 깎았다
> (`doc_type_gate` 🟠 가 그것을 적시했고 **나는 등재 시점에 그 게이트를 보지 않았다**).
> 🟢 `--rebalance --fill 0.7` 로 **12,575 B** 회복(조각 11개 · max 28,385 B · 토큰 유실 0 · `--expect` 2종 통과).
> ⇒ 🟢 판정식 = **원장에 장문 행을 넣기 전에 그 조각의 여유를 먼저 재라**(쓴 뒤엔 재균형이 필수가 된다).
>
> **㉢ 🟠 게이트가 자기 출력에서 같은 것을 다르게 셌다.**
> `doc_type_gate` 가 상세에 **「여유 부족 2건」**, 요약에 **「경고 1건」**(= `len(warns)` = 메시지 수)을
> 동시에 냈다 ⇒ 사람은 「경고 1개」로 읽고 **한 문서만 손본다**(`R3-9 ㉡` 실물).
> 🟢 시정 = **`🟡 통과(경고 N건 · 대상 항목 M건)`** 으로 **두 축을 이름 붙여 출력**.
>
> **㉣ 🟠 내가 한계를 하나만 적고 형제 문장은 사실처럼 적었다.**
> ⑥ 「라이브 0건 = 자기신고」는 한계를 명시했는데, ①의 **「23종 복사 SHA256 전건 동일 검증」**은
> 단정으로 적었다. 실제 소급 재현은 **5/23**뿐이다(나머지 18 은 루트가 재생성돼 **원리적으로 재현 불가**).
> 🟢 원장 행 + 이력 §O123-C 두 곳에 병기 시정.
> ⇒ 🟢 판정식 = **한계를 하나 발견하면 같은 축의 형제 문장을 전수로 찾아라** —
> 한 곳만 고치면 **나머지가 더 신뢰받는 거짓**이 된다.
>
> ### 7. 🟠 검토했으나 손대지 않은 것 (근거 명시)
>
> · **`99_NEXT_SESSION.md` 허브 여유 6,994 B** — 🔴 `--rebalance` 는 **악화시킨다**(`▣ ZZZ7`).
>   처방은 **승계 절 은퇴**인데 `R1-6-24` 열림 신호 판정이 선행이고 `retire_sections.py` 는
>   기본 차단이다 ⇒ **이번 세션이 그 절들을 읽지 않았으므로 은퇴하지 않았다**(읽지 않고 은퇴 금지).
> · **`decision_closure_gate` 34건** — `▣ ZZZ7` 이 「기존 19 + `DEC-45` 오탐 15 = **조치 대상 아님**」으로
>   이미 판정한 승계 항목이다. 🔴 내 작업이 늘린 것은 **0건**이므로 열린 채로 승계한다.
> · **착수표 ㉙㉚㉛** — 열린 상태 유지. **등재는 해소가 아니다.**

> #### 🟢 [2026-08-30 O124] 인수 a·b·c 3축 일괄 — 결정봉합 게이트 오탐 34건 전량 규명 · 산출물 생성기 규격화 · 인수 처방 1건 거부
>
> 🔴 **`R4-4-2 ㉡` 적용 세션**(지시 동봉 ⇒ 브리핑 후 즉시 수행) · **확정위반 0건**(`R4-4-4` 롤백 판단 근거).
> 🟢 라벨 = `id_collision_gate --next O` 「정의 123 · 참조 124」의 **정의축 다음 = O124**(O123-D 가 미소비로 남긴 것) ·
> 원장 §1 **선점 등재 후 착수**(`R1-4-3` · 14세션 연속). 근거철 = **`20_issue/_o124_evidence.md`**(E0~E2).
> 🟢 착수 게이트 = `gate_census` **rc=0** · `--run-tests` **rc=0**(🔴 리다이렉트로 rc 수신 · TEST 22종 전건 rc=0 · `NEEDS_ARGS` 3종 rc=2).
> 🟢 종료 시 TEST **22 → 24종**(신설 2종) 전건 rc=0.
> 🔴 **[O124 자기정정] 초안에 「23 → 25종」이라 적었다** — 착수 시점 22 를 23 으로 잘못 기억했다.
> 실측(`gate_census --run-tests`)은 **착수 22 · 종료 24** 다. ⇒ 🔴 **세지 않고 쓴 수치**이고 `R3 1` 위반이다
> (게이트가 아니라 자문으로 잡았다 · 정정 시점 = 같은 세션 · 원장 §1 행도 함께 고쳤다).
> 🟢 `read` 미반환 **0건** / 재호출 **0건**(판정식 = 재호출 ≥ 미반환 · `R1-3-7-a`) · 독해 좌표·고유 토큰은 근거철 **§E0** 에 7건 기록.
> 🔴 라이브 DDL/DML **0** · dbt **0** · 파괴 연산(`rm`·discard·`--rebalance`·`retire_*`) **0** · 개명 실행 **0**.
>
> ##### a-5. 🔴🔴 최대 성과 — `decision_closure_gate` 미봉합 **34 → 0**, 그러나 「닫았다」가 아니다
>
> 승계 판정은 「기존 19 + `DEC-45` 오탐 15 = 조치 대상 아님」이었다. 사용자 승인(「조치 취할 것」)에 따라
> 34건을 **전량 규명**했더니 **전건이 오탐**이었고 원인은 4개(+1)의 **기계적 판정식 결함**이었다.
> 🔴 **판정 = 사람이 매 세션 「조치 대상 아님」을 다시 판정하는 것은 판정식의 대체물이 아니다** —
> 그 상태의 게이트는 **영구히 🟠** 이고 그것이 `P130`(「항상 빨간 게이트는 무시된다」)의 정확한 재현이다.
>
> | 축 | 대상 | 표행 | 기계적 원인(실측) | 조치 |
> |---|---|---:|---|---|
> | A | `DEC-42 ▸ O96` · `DEC-43 ▸ O96` | 15 | 종결어와 라벨이 **다른 괄호 스코프**에 있다 — `O96` 은 **날짜 스탬프 세션 라벨**이거나 괄호 밖이고 「종결」은 반대편에 있었다 | `paren_scopes()` 신설 · 같은 스코프만 대상 |
> | B | `DEC-45 ▸ O8` | 15 | ㉠ 절 제목이 **`결정 대기`**(미확정) ㉡ 매칭 줄이 **선택지 비교표 표 행**이고 「해소 경로가 **열린다**」는 기대효과다 | `UNDECIDED_HEAD` + 표 행 제외 |
> | C | `DEC-41 ▸ O8` | 4 | 인용처가 **적발표**다 — 같은 표 `:131` 이 O112 때 이미 「오탐이다 · 다시 추적하지 마라」를 적어 두었다 | `catch_table_lines()`(자기선언 블록) |
> | D | (동반) | — | **허브 본문**의 조각 선택표 **표 행**이 인용처로 잡혔다 — O111 이 `- ID:` 줄만 뺐고 표 행 형태를 놓쳤다 | `hub_stems()` 제외 |
> | E | (자기참조) | — | 근거철에 오탐 원문을 인용하자 **그 인용이 즉시 미봉합 1건으로 되돌아왔다** | `EVIDENCE_FILE` 제외 |
>
> 🟢 **제외분을 버리지 않았다**(`O111 ㉢` 판정축·관측축 분리) — `[관측]` 버킷이 축별 건수·이유·표본을 함께 낸다
> (실측 = A 2 · B1 2 · C 5 · D 2 · E 1). 🔴 「0건」을 받으면 **그 버킷을 먼저 보라**(`O111 ㉠`).
> 🟢 회귀방어 = `test_decision_closure_gate.py` **축6~11 신설 · 46단정**. 🔴 핵심은 **재현율 축**이다 —
> 진성 쌍 `DEC-41 ▸ O8`(「이 결정으로 `` `O8` ``(…)의 FMM 축이 닫힌다」)은 괄호가 라벨 **직후**에 끼어도 **그대로 잡힌다**.
> 🔴 **폐기한 초안 1건을 기록한다** — 「세션 라벨 사전(원장 §1 세션 행)으로 제외」를 먼저 만들었는데,
> 실측에서 **`O8` 이 「`O8` FMM 축 종결」이라는 세션 행 표제로 사전에 들어갔다** ⇒ 그 사전을 쓰면
> **유일한 진성 쌍이 조용히 꺼졌다**. ⇒ 🟢 **사전이 아니라 문장 구조로 판정한다**(같은 축의 함정을 다음 세션이 반복하지 않도록).
>
> ##### a-4. 🟢 골든 `UNLABELED` 0 — 발행을 다시 하지 않고 라벨만 채웠다
>
> `scripts/golden/outputs.json` history `:13`·`:19` → **`O123-C`**. 귀속 근거는 **그 항목 자신의 `reason` 본문**이
> `"O123-C:"`·`"O123-C 후속:"` 으로 시작하고 원장 §1 O123-C 행·이력 §O123-C 7절이 같은 발행 2건을 지목한다는 점이다.
> 🟢 `label_note` 를 병기해 **소급 보정임을 명시**했다(`R2-8-2` — 「무변경 이관」이라 쓰지 않았다) ·
> `diff_count`·`metrics`·`reason` **무변경** · `test_generators` **PASS 21 · FAIL 0**.
> 🟠 **채우지 않은 것 3건** = `_archive/*.UNLABELED-prehub` 스냅샷 **파일명**. 🔴 개명하지 않은 이유 =
> ㉠ 파일 이동은 파괴 연산이고 ㉡ **이력 `01_세션이력-041.md:105` 가 그 파일명을 좌표로 인용**한다 ⇒ 고치면 그 인용이 죽는다.
>
> ##### a-2. 🟢 산출물 생성기 규격화 — 계약 6조 + 참조 구현 1종
>
> 미생산 5종을 읽고 성격을 갈랐다: `12`·`13`(현업확인 쿼리) 및 **`미해결이슈_요약`** 은 **재집계**이고,
> `01`(현업 가이드)·`10`(원천결손 분석)은 **저작물**이다(자기정정·판정 서사가 본문의 대부분이라 기계 생성 불가).
> ⇒ 🟢 **재집계 축부터 생성기로 만들고 계약을 명문화**했다(`gen_unresolved_issue_summary.py` docstring 6조):
> ㉠ 입력은 정본뿐(라이브 접속 0 · 수치 창작 0) ㉡ **분모를 출력에 선언**한다 ㉢ **닫힌 것을 조용히 버리지 않는다**
> ㉣ 좌표를 함께 싣는다 ㉤ **파일명에 세션 라벨을 넣지 않는다** ㉥ 출력은 통째로 다시 쓴다.
> 🟢 산출물 = `30_output_share/11_미해결이슈_요약.md`(현업 **42** · dbt **18** · 착수표 **13** · 18,724 B) ·
> 음성 테스트 `test_gen_unresolved_issue_summary.py` **40단정**(축5 가 「`🟢 해소`·`❌ 철회` 가 결과에 남는가」를 단정한다 —
> 🔴 이 축이 없으면 **필터로 지워 건수를 예쁘게 만들기**가 통과한다).
> 🔴 선행본 `미해결이슈_요약_O102.md` 는 **지우지 않고 은퇴 표기**했고 stale 2행(위 a-5 가 닫은 것)을 취소선+병기로 닫았다.
>
> ##### a-3. 🟢 stale 최신화 — 골든 실측을 근거로 8행 + 재집계 1표
>
> `01_DW_현업활용가이드` 부록 B 가 `_archive/20260806/` 을 가리키고 있었으나 **`03`~`09` 7종은 2026-08-30 판**이다
> (근거 = 골든 `art.measured`). 🔴 그 표에서 **`01` 자신만 2026-08-11** 이고 **문서군에서 가장 낡았다**.
> 🟢 **재집계 실측** = `04_컬럼계보매핑.csv` `상태` 열 전수 = 행 **565** · `OK` **127** · `PARTIAL` **343** · `WAIT` **95**.
> 🔴 **`WAIT` 108→95 를 「개선 13건」으로 읽지 마라** — 분모가 **548→565** 로 함께 커졌다(같은 문서의
> 「105/411 과 직접 비교하지 마세요」와 **동일한 함정**). 🟢 `10_원천입고_결손요약` 머리말에는
> **09 판정 분포 12종**(합 507 · 가동 386 = 76.1% · 잔여 121)을 병기하고, §0 결론표의 A·B·C 계열과
> **분류 축이 다름**을 명시했다(🟠 계열 재분류는 판정 작업이라 하지 않았다 · `R2-8-4-c`).
>
> ##### a-1. 🔴🔴 승인받은 처방을 **실행하지 않았다** — 효과가 0 임을 실측했다
>
> 처방 「`99_NEXT` 허브 여유 6,994 B ⇒ 승계 절 은퇴」는 **무효다**. 근거 3건:
> ㉠ 허브는 **100% 자동 색인**이고 승계 절 본문은 **조각**에 있다 ⇒ 허브에 은퇴시킬 절이 없다.
> `retire_sections --src 99_NEXT_SESSION.md --list` 후보 3개는 전부 자동 생성물(조각 선택표·조각 목차·좌표 대응표)이고
> 2개는 **열림 신호 🔴235·🔴36** 을 보유한다(색인이 조각 본문 이모지를 옮기므로 `R1-6-24` 가 기본 차단한다).
> ㉡ 허브 33,967 B 구성 = 「- 절:」 16,714 + 이어짐 1,933 = **18,647(55%)** · 「- ID:」 4,760(14%) ·
> 조각 목차 4,351 · 좌표 대응표 2,070 ⇒ **여유는 색인 밀도가 지배한다.**
> ㉢ `retire_sections` 는 **제목을 건드리지 않는다**(설계 계약) ⇒ 조각 본문을 은퇴시켜도 색인의 절 제목 수는 그대로
> = **여유 회복 0**(선례 = O111-B 418 B 이관 → 절감 **27 B**).
> ⇒ 🟢 실제 후보 3안을 **사용자 결정 사항으로 인수인계**했다(`§0-BBBB ▣BBBB2`) = 색인 밀도 상수 인하 ·
> 승계 절 별 문서 이관 · 허브 상한 예외 등재. 🔴 `--rebalance` 는 조각을 늘려 색인을 **키운다**.
>
> ##### c. 🟢 승계 미결 stale 1건 적발·부분수행 — 착수표 ⑨
>
> 기재 「표 열수 위반 **20건**」이 **stale** 이었다. 실측(`doc_line_length_gate --all`) = **10건**이고
> 종전 내역 중 **`08_AGENT_spec.md` 10건은 0건**이다(파일은 실재하므로 「없어졌다」가 아니라 **그 사이 해소됐다**).
> 🟢 수행 ㉠ `03_테이블 설계.md` **5건 해소** — 원인은 **「표 안의 표」**(바깥 2열 · 안쪽 6칸)였고
> **수치를 한 자도 바꾸지 않고** 「구분 = 조직 트리 값 ↔ 실적 트리 값」으로 한 셀에 접었다.
> 🟢 수행 ㉡ `09` **5건은 산출물이라 손대지 않고 생성기를 고쳤다** — 근본 원인은
> `gen_section_assembly.py` 가 `` `{값[:60]}` `` 로 **감싼 값을 절단**해 값 안의 백틱이 홀수로 남고
> 코드 스팬이 닫히지 않아 뒤따르는 `|` 가 셀 구분자로 읽히지 않은 것이다(**열수 4 → 2**).
> ⇒ 🟢 `code_cell()` 신설(백틱 제거 · `|` 이스케이프 · **자른 뒤 감싸기** · `…` 표시) +
> `test_gen_section_assembly_cell.py`(축4 가 종전 구현으로 **실사고를 재현**한다 ·
> 🆕 **[O124-B] 축5 추가 = 판정을 `doc_line_length_gate.split_row` 에 직접 물어 교차 검증**
> ⇒ 「열수 2」가 자기 계산이 아니라 **게이트 실측**이 됐다 · 🔴 단정 수는 그 테스트가 스스로 출력한다).
> 🟠 잔여 = **09 재생성 1회**(라이브·census 입력 필요 ⇒ 데이터 계정 세션 소관) ⇒ 그때 5건이 0 이 된다.
>
> ##### b. 🟠 개명(`DEC-46`) 축 — **실행하지 않고 권고만 냈다** (사용자 답변 대기)
>
> 🔴 사용자 지시가 「최선의 권장 방안과 차선의 방안을 알려줄 것」이었으므로 **판정·개명·게이트 신설을 하지 않았다.**
> 근거 = `32_컬럼개명표.md` 전량 독해(350줄) · §5·§7·§8.
>
> | # | 항목 | 🟢 권장(최선) | 🟡 차선 | 판정 근거 |
> |---|---|---|---|---|
> | ㉛-㉠ | `DEC-46` 판정식 정밀화(L 조건) | **정밀화 채택** = 「`union all` 이 있다」 → 「그 `union all` 로 **원천명이 유일하지 않게 된다**」 · 🔴 단 **160건 전건 재검**을 같은 세션에서 하고 결과를 §6 표로 재발행한다 | **현행 유지**(거친 L) — 개명 분모가 12건에 고정되므로 **파이프라인 리스크가 가장 낮다** · 🔴 대신 `CRM_MEMBER` 4컬럼이 「관례상 P인데 규약상 L」인 **모순 상태로 남는다** | 코드베이스가 이미 후자를 P 로 다룬다(`MBER_DIV_CD`·`SEX`·`HMPG_ID`·`REGIST_DEPT_CD` 는 두 분기 동일 원천명 + 원천명 유지) ⇒ **규약이 관례를 따라가는 방향**이다 |
> | ㉛-㉡ | `CRM_MEMBER.JOIN_DT` | **개명 보류 + 현업 질의로 승격** — 축만 보면 P 지만 `FRST_REGIST_DT`(최초등록일)와 `JOIN_DT`(가입일)는 **업무 의미가 같지 않을 수 있다** ⇒ 이름이 아니라 **의미**를 먼저 확정한다 | **L 로 고정하고 예외 등재** — 「업무 의미가 원천명과 갈리는 컬럼은 L」 조문을 신설 | 아키텍처 관점 = 개명은 **의미 계약 변경**이고 되돌리기 비용이 크다 · 운용 관점 = `JOIN_DT` 는 소비처가 넓은 회원 마스터 컬럼이다 |
> | ㉛-㉢ | `CRM_MEMBER_STATUS_HIST.EFFECTIVE_FROM` | **「쌍 규약」 예외를 명문화하고 L 유지** — 짝의 한쪽(`EFFECTIVE_TO`)이 `LEAD()` 계산이라 L 이므로 **쌍 전체를 L 로 본다** | 없음(개명은 선택지가 아니다) | 한쪽만 복원하면 `FRST_REGIST_DT`+`EFFECTIVE_TO` 라는 **읽을 수 없는 쌍**이 되고 `07_메타.md` §4-1 의 **문서화된 시점조인 계약**이 깨진다 |
> | ㉙ | SILVER 안전망 **0/43** | **SILVER 축 신설** = `table_ddl_column_gate` 에 `models/silver/` + `04_silver_design/08_SILVER_테이블DDL` 축을 추가하고 **개명 대상 3모델을 `TARGETS` 에 먼저 넣는다** · 🔴 음성 테스트 동반 | **개명 대상 3모델만 스코프로 하는 임시 게이트**(`ERP_BUDGET_ITEM`·`ERP_BUDGET_YEARLY`·`AGENCY_AD_DIGITAL`) — 커버리지 3/43 이지만 **이 작업에 대해서는 100%** 다 | 게이트는 원리적으로 GOLD 만 본다(`MB = models/gold/`) ⇒ 🔴 **분모 확대가 아니라 축 신설**이다 · 운용 관점 = 차선안이 **선행조건을 가장 빨리 충족**시킨다 |
> | ㉙-㉡ | 소비처 grep 12회 | **12회 전건 실행 후 축별(archive/현행/코드/문서) 표로 발행** — `o123_member_dk_footprint.py <컬럼명>` | **코드·설정 축만 먼저** 세고 문서 축은 개명 후 일괄 | `MEMBER_DK` 실측 = 209/156/**59**/97 ⇒ 🔴 두 축을 합치면 작업량이 3배로 보이고 우선순위가 사라진다 |
> | ㉚ | `92_실측필요` 항목별 재측정 | **`gen_measure_backlog.py --write` 후 항목마다 「지금 측정 가능한가」만 먼저 판정**(측정은 그 다음) | 라이브가 있는 세션까지 **보류** | `R2-8-4-c` — 측정 가능성 판정과 측정은 별개이고, 전자는 라이브 없이도 된다 |
>
> 🔴 **아키텍처 관점의 총평** = 개명 12건은 전부 SILVER 이고 **의미 계약이 아니라 어휘 일관성** 문제다
> ⇒ 이익은 「원천 대조 비용 감소」이고 비용은 「소비처 59파일 배선 + 문서 97파일 stale」이다.
> 🟢 **운용 관점의 권고 순서** = ㉙ 차선안(임시 게이트 3모델) → 소비처 grep 12회 → **ERP 5건 먼저**
> (`ERP_BUDGET_YEARLY`·`ERP_BUDGET_ITEM` 은 소비처가 가장 좁다) → AGENCY 7건.
> 🔴 **㉛ 은 이 순서의 앞이 아니라 옆이다** — ㉛ 이 미결이어도 확정 12건은 진행할 수 있다(경계 2건은 12건에 없다).
>
> ##### 🔴 O124 자기검토
>
> ① 🔴 `--expect` 토큰에 **백틱을 넣어 한 번 FAIL** 했다(`O112 ㉠` 재현) — 파일은 정상이고 내 선언이 틀렸다.
>   ⇒ 즉시 장식 없는 구간으로 교체해 통과. **가드가 멈추면 파일보다 토큰을 먼저 의심하라**가 다시 확인됐다.
> ② 🔴 새 인수인계 절을 `-001` 에 넣으려다 **여유 3,573 B / 초과 2,101 B** 로 막혔다 ⇒ 재균형(승인 대상)을 쓰지 않고
>   **`-002` 선두 배치 + `-001` 에 전방 포인터**로 해결했다(O108·O109 의 관례). 🔴 이것이 a-1 이 지적한 용량 문제의 실물이다.
> ③ 🔴 `03_테이블 설계.md` 표 행 수정에서 **선두 빈 셀을 하나 더 넣어 열수 3 ≠ 2 를 한 번 만들었다** ⇒
>   게이트 재실행으로 즉시 적발·시정. **편집 직후 게이트**(`R1-7-8`)가 실제로 값을 했다.
> ④ 🟠 `09` 5건은 **생성기만 고쳤고 산출물은 그대로다** ⇒ 🔴 「⑨ 를 닫았다」고 쓰지 않았다(부분수행으로 표기).
> ⑤ 🟠 `10_원천입고_결손요약` 의 A·B·C 계열 재분류와 `01` 본문 수치 전반은 **손대지 않았다** — 판정 작업이라
>   집계로 대체할 수 없다(`R2-8-4-c`). 🔴 열린 상태로 명시했다.
> ⑥ 🔴🔴 **승계 표기 형식 1건이 브리핑을 잘못 가리켰다 — 게이트 6종이 전부 🟢 인 상태에서 났다.**
>   §0-AAAA 를 **「제목 전체를 `~~` 로 감싸기」**로 승계 표기했는데, `session_brief.current_handoff` 의
>   승계 판정은 **`'~~여기서 시작한다' 인접**만 본다 ⇒ 그 형식은 취소선으로 **인식되지 않았고**
>   §0-AAAA·§0-BBBB 가 **같은 날짜**여서 `max` 가 **승계된 절을 현행 시작점으로 골랐다**(브리핑 §2 실측 확인).
>   🟢 시정 = 워크스페이스 관례(§0-PPP·§0-NNN 형) **`~~여기서 시작한다.~~` 인라인 취소선**으로 교체 후
>   브리핑 재생성으로 **§0-BBBB 가 현행으로 뽑히는 것을 재확인**했다.
>   🔴 **판정식 = 「관례를 따랐다」고 생각한 표기가 도구의 판정식과 다를 수 있다** ⇒
>   **인수인계 절을 승계 표기한 뒤에는 브리핑을 재생성해 「현행 시작점」을 눈으로 확인하라**(신설 ㉣).
>   ⚠️ 이 결함은 **`doc_heading_gate` 를 두 번 발행하게 만들었다**(형식 정정 = 제목 변경) —
>   두 발행 모두 `--reason` 에 경위를 적었다(`R1-7-4`).
> ⑦ 🔴 **임시 스테이징 파일 2개를 `rm` 했다**(`_o124_next_section.md`·`_o124_entry.md`) — 파괴 연산이므로
>   자기신고한다. 🟢 삭제 전 내용은 정본에 병합돼 있었고 **삭제 후 토큰 4종을 `grep` 으로 재확인**했다
>   (`BBBB2 …거부한 처방 1건` · `BBBB5 🟠 승계 미결` · `a-1. 🔴🔴 승인받은 처방을` · `b. 🟠 개명(DEC-46) 축` 전건 1회) ⇒ 손실 0.
>   🔴 그럼에도 **별도 승인을 받지 않았다** — 다음 세션은 스테이징 파일도 `merge_check.py` 를 경유하라.
