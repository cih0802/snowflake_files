<!-- SPLIT-CHUNK 01_세션이력.md | 075/075 | 허브 = 01_세션이력.md | 원문 1~93행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-21 O173-B] 스킬 개발 관점 감사 — 자기결함 2건 추가 적발·시정 + 승인분 재균형 집행

> **착수 경위** = 사용자가 `/skill-development` 를 명시해 *"방금 작업을 스킬 개발 관점에서
> 비판적으로 바라보고, 원래 의도대로 잘 작업되었는지 검토해줘"* 라고 지시했다.
> ⇒ AUDIT(LINT) 모드로 `init_ihcho` 를 감사했고 **내 O173 작업에서 결함 2건이 더 나왔다**.

> 🔴🔴 **총평 = 기능은 달성했고 신고는 부정확했다.** `00_작업지침` 여유 해소와 승계 확인은
> 실측으로 달성했으나, **분모 등재를 1곳 빠뜨리고 「완료」로 신고**했고 **가필을 「무변경」으로
> 신고**했다. 둘 다 이 워크스페이스가 반복해서 당한 유형이다.

> **🔴 결함 1 — 분모 「4곳」은 조문 문서에 부족하다(5번째가 있다)**

> · `R1-6-18 ④` 는 분모 **4곳**(`doc_type_gate.EXTRA_DOCS` · `doc_heading_gate.DOCS` ·
>   `doc_line_length_gate.CANON` · `doc_census.SINGLES`)을 요구하고 나는 그 4곳을 넣고
>   원장·이력·등재표에 **「분모 4곳 편입 완료」**라고 적었다.
> · 🔴 그런데 **조문을 정의하는 문서는 `clause_order_gate.DOCS` 가 5번째로 필요하다** —
>   선례 `01_문서분할_규약`·`02_파일쓰기_안전규약` 은 **전부 거기 등재돼 있다**.
>   ⇒ 그 사이 **`R3` 정본의 조문 순서 역전·중복은 전혀 검사되지 않았다.**
> · 🟢 시정 = 등재 후 그 문서에서 조문 **9개** 인식 · 전체 **103 → 112개** · 역전 0 · 중복 0.
> · 🟢🟢 **판정식 = 「분모 4곳」은 문서 일반의 최소치이고, 조문 문서는 +1 이다.**
>   🔴 이것이 `R3-9 ㉢`(분모 등재와 기준선 발행은 별개)의 3번째 변종이다 — 종전 2회는
>   「골든을 안 냈다」였고 이번은 **「분모 하나를 셈에서 빠뜨렸다」**다.

> **🔴 결함 2 — 「무변경 이관」 오신고 (`R2-8-2`)**

> · 실측(분리 전 지침 스냅샷 `_archive/…O173-R3분리`) = `R3-3` **1건** · `R3-9` **4건**만 존재하고
>   `R3-1`·`R3-2`·`R3-4`~`R3-8` 은 **정의되어 있지 않았다**(원문은 번호 없는 `1.`~`9.` 목록).
> · 🔴 내 스텁은 그 9개를 **라벨로 정의**했다 ⇒ **가필**이다. 그런데 나는 원장·이력·등재표·
>   신규 문서 머리말에 **「무변경 이관」**이라고만 적었다. `R2-8-2` 는 *"문장을 재작성하거나
>   항목을 가필했으면 「이관 + 가필」로 적는다"* 고 명시한다.
> · 🔴 **부수 결함** = 스텁은 라벨을 정의했는데 **정본 본문에는 그 라벨이 없었다**
>   ⇒ `R3-2` 를 따라가면 정본에서 찾을 수 없는 **반쪽 ID** 상태였다.
> · 🟢 **방향 자체는 옳았다** — `R3-2` 는 `01_문서분할_규약-002:199` 와 스킬 본문이
>   **정의 없이 참조**하던 dangling reference 였다(내 라벨 부여가 그것을 닫았다).
> · 🟢 시정 3단 = ㉠ 정본 9개 항목에 라벨 부여 ㉡ 형식을 선례(`02_파일쓰기_안전규약` =
>   `  * **R3-N 제목**`)에 정렬해 `clause_order_gate` 정의 패턴에 맞춤 ㉢ 정본 머리말에
>   **「본문 무변경 + 라벨 가필」**을 구분해 명시.
> · 🟢🟢 **판정식 = 「무변경」은 블록 단위 주장이 아니라 「내가 아무것도 더하지 않았다」는
>   주장이다.** 라벨 하나만 더해도 무변경이 아니다. 🔴 기계 대조(토큰 부재 0 · 블록 포함 True)가
>   **통과했다는 사실이 「무변경」을 증명하지 않는다** — 그 대조는 **삭제**를 잡고 **추가**를 못 잡는다.

> **🔴 결함 3 — `J5` 를 인용하면서 나 자신에게 적용하지 않았다(이번 감사의 최대 수확)**

> · 나는 `R3` 분리 중 스킬 §5-2 의 *"그 문서는 Step 1 에서 전량 독해된다"* 가 분리로 거짓이 된
>   것을 발견해 고치고, 이력에 **`J5`(「폐기했으면 그 문장을 `grep` 으로 전수 찾아 표시하라」)의
>   실물**이라고 적었다.
> · 🔴🔴 **그런데 같은 패턴을 전수 검색하지 않았다.** 실측 = `Step 0.7`(O167 재편으로 사라진
>   좌표)이 **29곳**에 남아 있었고, 그중 **살아있는 계약 문장이 5곳**이었다:
>   `00_작업지침:181,182` · `02_파일쓰기_안전규약:17,21` · 원장 `-002:34`
>   — 전부 *"`init_ihcho` **Step 0.7** 이 담보한다"* 라는 **분리 정당화 근거**였다.
>   ⇒ 즉 `R1-7` 분리의 정당화가 **죽은 좌표를 가리키고 있었다**(내 `R3` 분리와 똑같은 구조).
> · 🟢 시정 = **계약 문장 5곳만** `§1 G4` 로 교정. 🔴 **이력(10) · 사례집(6) · 은퇴로그(3) ·
>   골든·pyc(5) 24곳은 고치지 않았다** — 역사 서술과 절 제목이고, 고치면 ㉠ 역사 재작성 ㉡
>   제목 게이트 유실이 된다. 🟢 그 판정을 원장 행에 명시했다.
> · 🟢🟢 **판정식 = 「판정식을 인용하는 것」과 「판정식을 실행하는 것」은 다르다.**
>   `P154`(「적발했다고 쓰는 것」 ≠ 「정본을 고치는 것」)의 자기적용 판본이고,
>   `J8`(자기참조 계측)이 예고한 형태다 — 🔴 **조문을 인용한 그 문장이 그 조문의 위반일 수 있다.**
> · 🟢 부수 관측 = 내 교정문에 「종전 `Step 0.7`」이 남아 잔존 카운트가 0 이 되지 않는다.
>   이것은 `J8`「부정문을 심으면 그 부정문이 탐지기에 걸린다」의 실물이므로 **정확한 토큰**
>   (계약 문장 형태)으로 재판정했다.

> **🟡 결함 4 — `판정식 8종` 하드코딩 (`I7` 분모가 원칙보다 좁다)**

> · 스킬 §3 표제와 §0 표가 **「판정식 8종」**이었다(현재 J1~J8 로 정확). 🔴 그런데 나는 같은
>   세션에 **같은 클래스**(Step 1 의 「분리된 정본 3종」)가 `R3` 추가로 **실제로 stale 이 된 것을
>   목격하고 지웠다** — 그러면서 **형제 인스턴스를 남겼다**.
> · 🔴 **검증기 `I7`(하드코딩 수치 금지)이 이것을 통과시켰다** ⇒ `I7` 의 분모가 원칙보다 좁다
>   (조각 수·문서 크기 패턴만 본다). 🟢 판정식 = **게이트가 통과시켰다는 것이 규약을 지켰다는
>   뜻이 아니다**(`R3-9` 그 자체).
> · 🟢 시정 = 표제를 **「판정식 `J1`~」**으로 바꾸고 §3 머리말에 근거를 남겼다(개수는 `### J` 를
>   세라). 제목 개명이므로 골든을 `--reason` 으로 재발행(신설 1 · 유실 1 · 총수 불변 실증).

> **🟢 승인분 집행 — `--rebalance`**

> · 원장 `--rebalance --fill 0.7` = 조각 **17 → 15**(잉여 2 삭제) · 최소 여유 **6,145 → 12,701 B** ·
>   concat SHA256 일치 · 토큰 유실 0(이동 확인 52종 · 행선지 없음 0) ⇒ **유형 여유부족 2 → 1건**.
>   🔴 `R1-6-17` 대로 게이트를 **2회** 재실행해 판정했다(낡은 뷰 배제).
> · 🔴🔴 **`03_init_ihcho_스킬_본문`(여유 6,634 B)은 재균형 대상이 아니다** — **미분할 단일
>   파일**이고 등재표가 *"허브+조각 분할은 마커를 갈라 빌더를 깨뜨린다"* 고 못박아 **분할이
>   금지**돼 있다. ⇒ 처방은 `--rebalance` 가 아니라 **지연 로드 분리**(O167 선례 = §5·§5-1·§4-1 →
>   `references/session-end.md`)이고 **무엇을 뺄지는 결정 사항**이라 집행하지 않았다.
>   🟢 판정식 = **「여유 부족」의 처방은 문서 유형이 정한다** — 분할 가능 여부를 먼저 보라.

> **📏 종료 기준선 (🔴 인용하지 말고 재라)**

> · 게이트 **13종 rc=0** · 음성 테스트 **38/38 rc=0** · 검증기 불변식 전건 PASS(경고 0)
> · 조문 **112개**(역전 0 · 중복 0) · `R3` 정본 조문 **9개** 인식
> · 제목 골든 **문서 24종 · 1,840개** · 이 세션 발행 **2회**(㉠ R3 이동 ㉡ §3 개명 · 전부 `--reason`)
> · 원장 조각 **15** · 최소 여유 **12,701 B** · 유형 여유부족 **1건**(`03_스킬_본문` 6,634 B)
> · `SKILL.md` **402줄 / 33,216 B** · `read` 미반환 **0** · 재호출 **0**

_Co-authored with CoCo_

> #### 🟢 [2026-09-21 O174] 백로그 §2·§3·§4 순차 집행 — 현업 문항 3절 발행 · 등재강제 구조 신설 · CPC 개명(dbt 대기)
>
> **지시** = *"2,3,4 순서대로 진행해줘. 승인이 필요한 작업은 전부 승인한다."*(`R4-4-2 ㉡` + `R4-4-3` 선승인)
> **선행** = 같은 세션 앞 턴에서 `O172-D` 잔여 2건이 **이미 `O173` 에 의해 종결**돼 있음을 실측 확인했다
> (브리핑 §2 다단위 승계 = 음성 축9 rc=0 · `R3` 분리 = 여유 3,935 → 9,638 B).
>
> ##### ▣ 1. 라이브 실측이 종전 인과를 기각했다 (§2 착수)
>
> · `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS` **203,138행** · `DATE` 채움 **9,080(4.47%)** · NULL 194,058(95.53%).
> · 🔴🔴 **포맷 경계 판정** = `CPC`·`CPA`·`DEV_UNIT_PRICE`·`CPM` 은 **신방식 구간**(`DATE` 채움 ·
>   2026-06-01~08-20 · 9,080행)에서 **전건 채움**이고 **구방식 구간**(텍스트 축만 · 2024-01-01~2026-05-31 ·
>   194,058행)에서 **전건 0** 이다. `CTR`·`CVR` 은 신방식 안에서도 **7,038/9,080** 부분 채움.
> · ⇒ 🟢🟢 **판정식 = 「건수 일치」는 인과의 증거가 아니다.** 인수인계가 *"9,080 이 AD_DATE 실값 건수와
>   정확히 같다 ⇒ 같은 재적재가 시간축·성과지표를 함께 비웠다"* 라고 적은 신호는 **삭제가 아니라 경계**였다.
>   🔴 이 신호를 그대로 현업 문항으로 냈다면 **없는 사건의 복구를 요청**했을 것이다.
> · 🟢 시간축은 이미 해소돼 있었다 — `O171-D` 의 COALESCE 폴백으로 `SILVER.AGENCY_AD_PERFORMANCE.AD_DATE`
>   가 **203,138 전건 채움**(DIGITAL 분기 · 파생 최대 2026-05-31 · 경계 깨끗).
> · 🔴 전건 부재 3컬럼은 실재 확인 = `CRM_DVLP_CNT` 0/203,138 · `MEDIA_PTNT_CUST_CNT` 0/203,138 ·
>   `VIDEO.CONV_CALL_CNT` 0/37,674. 대조군 정상(`GA_CONV_MBER_CNT` 202,739 · `CONV_VU_CNT` 203,063).
> · `VIDEO.CPC`(TEXT) = 35,201 채움 중 **변환 실패 482** 이고 **전부 `#DIV/0!`**(다른 오염 문자열 0).
>   `GOLD.FACT_AD_BROADCAST.CPC_SRC` 34,719/39,823.
> · `ERP.BDGT_ACMSLT_LEDGER` 258행 · `CHN`·`ADJ` non-NULL 258 · **non-zero 0** ⇒ 별건 유지.
> · 🔎 **기준선 문면 정정** = *"VENDOR 는 미디어모금 64행에만 값이 있다"* ⇒ 실제는 **그 64행 중 6행**만
>   채움(온라인모금 57·기관홍보 46 은 0). 🟢 `E-1` 금액은 기준선과 일치(온라인 54,553,600,496 ·
>   미디어 37,457,213,517 · 기관홍보 195,840,457).
>
> ##### ▣ 2. §2 집행 — 문서20 에 문항 3절 발행 (종전 0건)
>
> · `N-14` = AGENCY 디지털 원천 **한 문항**(포맷 경계 표 + 전건부재 3컬럼 + 행 −32,434) · 4개 질문
>   (구간 소급 공급 / 신방식 부분채움 정상성 / 3컬럼 공급중단·폐기 / GSheet 재적재 범위·삭제).
>   ⚠️ ERP 추경·조정은 **별건으로 명시 분리**했다.
> · `N-15` = `#DIV/0!` **482행** 원천 정정 요청(+ 의미 확정 + 사유코드 격리 승인 요청 = 감사 가능성 축).
> · `N-16` = **회신 패킷** — 흩어진 6건을 **선행조건 순**으로 묶었다(`CONF-2` → `DEC-33` → 행사 마스터 →
>   발송 코드매핑+`O59-P-1` → N-14 → N-15). 🟢 행사 고아는 라이브 재측정 = **281,701/1,258,775 = 22.38%**.
> · 문서20 재균형 = 조각 **7 → 9**(최소 여유 20,092 B) · 게이트 6종 전건 통과 · 골든 재발행(사유 기재).
>
> ##### ▣ 3. §3 집행 — 3건(①③④) · ②⑤ 는 이미 닫혀 있었다
>
> · ① **`A-R` 버킷 등재**(문서50 `-013`) — 🔴 **분모 386 표를 고치지 않고 병기**했다(O43 386 ↔ O170 598 ·
>   `㉡` 원집합/현잔여 혼합 금지). `A-R-1` AGENCY 3(회귀) ↔ `A-R-2` ERP 2(전건 0 · 미발생 가능) 분기.
>   🆕 **`CPC`·`CPA` 는 A-R 에 넣지 않는다** — 부분 채움(9,080/203,138)이라 미주입 판정식(전건0·전건NULL·0행)
>   에 해당하지 않아 **미주입 95 분모 밖**이고, 성격은 버킷 `F`(적재 범위·시점)다. 조각 19→**22**.
> · ③ **미분류 재발 방지 구조**(O170 4회 방치 · O173 임시 5개의 원인) = ㉠ `_scratch_*` 접두 **등재 면제**
>   ㉡ `gate_census --final` **잔존 FAIL**(수명 강제) ㉢ `new_tool.py` **원자성**(등재 먼저 · 실패 시 파일
>   미생성) ⇒ 「생성 후 등재 지연」이 **구조적으로 불가능**해졌다. 음성 `test_tool_registration.py`
>   **7축 23단정**(고치기 전 구현 실증 = 종전 미분류 1건 ↔ 신 0건). 🟢🟢 **판정식 = 면제를 주지 않으면
>   규율로 버티게 되고, 수명을 안 걸면 면제가 구멍이 된다 ⇒ 둘은 한 쌍으로만 도입한다.**
>   🔴 **자기검사가 결함을 잡았다** — 초판 축4 가 미등재 이름을 써서 **테스트가 유령 등재를 만들었다**
>   ⇒ 🟢 **원자성 축은 「실패가 보장된 입력」으로만 단정할 수 있다.** 스킬 본문 §4-2 + 산출물 재빌드(검증기 PASS).
> · ④ **SV advisory 2건 종결** = §6.9-(5) 에 **열린 집합 면제** 신설. 🔴 무조건 면제가 아니라
>   **COMMENT 에 `SELECT DISTINCT` 조회 지침이 실재할 때만** 면제하고 **정보 축으로 기록**한다
>   ⇒ advisory **2→0** · 정보 8→**10** · blocking 0 유지. 음성 `test_sv_enum_exemption.py` 6축 16단정
>   (유사 문구 3종 비면제 · blocking 은 면제와 무관 확인). 🟢 판정식 = **열린 집합에서는 「값 목록」이
>   아니라 「값을 얻는 방법」이 계약이다.** ⚠️ 라이브 SV **무변경**(재배포 없음 · COMMENT 는 O170 이 이미 고침).
> · ② 승계 절 19 은퇴 = `O172-C-3` **불채택 확정**(성장 경로 소멸) · ⑤ `00_작업지침` = `O173` 완료.
>
> ##### ▣ 4. §4 — CPC 개명 집행(사용자 결정) · `E-1` 은 사용자 지시로 중단
>
> · 소비처 **전수 실측** = `CPC_SRC` 60파일 · `BRDC_CPC_SRC` 18 · `CPA_SRC` 49 · `DEV_UNIT_PRICE_SRC` 56
>   (30_output_share 는 자동 생성물 ⇒ 재생성 대상 · `_archive` 제외).
> · 개명 = `CPC_SRC`→**`CPC_CLICK_SRC`**(디지털) · `CPC_SRC`/`BRDC_CPC_SRC`→**`CPC_CALL_SRC`**(방송).
>   편집 **18곳 / 18파일** = dbt 모델 7(SILVER 2 · FACT 2 · WIDE 3) · `_wide_schema.yml` 4곳 ·
>   DDL 정본 3(`06_DDL.sql`·`10_WIDE VIEW 코멘트.sql`·`08_SILVER_테이블DDL`) · 설계문서 3 ·
>   SV/Agent 문서 2 · `cortex_project` agent_spec 2. 🔴 앵커 **18/18 전건 일치** 후에만 썼다.
> · 🔴 `CPA_SRC`·`DEV_UNIT_PRICE_SRC` 는 **개명 제외** — 디지털 전용이라 `WIDE_AD_COMBINED` 에서
>   **나란히 놓이는 동명 충돌이 없다**(같은 축이 아니다). `BRDC_` 접두 폐지 사유도 모델 주석에 남겼다
>   = 접두는 「같은 지표의 다른 출처」로 읽히지만 실제 분모가 클릭 ↔ 콜로 다르다.
> · 🔴🔴 **라이브 반영은 `dbt build` 정지점**(`R4-1`) — 에이전트는 실행하지 않았다. 그 결과
>   `table_ddl_column_gate` rc=1(라이브에 `CPC_CALL_SRC` 없음) · `test_generators` **G 골든 12건 차이** ·
>   `test_verify_wide_doc` 3뷰 불일치가 **정상적으로** 난다(Live 구명 ↔ Doc 신명 · 컬럼 수 35/35·51/51·33/33
>   **동일**). 🟢 **골든을 덮지 않았다** — 차이 전량이 CPC 축임을 규명해 기록만 남겼다(`R1-7-4` 축).
> · **빌드 후 대사 기대값**(`DEC-54` 방식) = `FACT_AD_DIGITAL.CPC_CLICK_SRC` **9,080/203,138** ·
>   `FACT_AD_BROADCAST.CPC_CALL_SRC` **34,719/39,823** · `WIDE_AD_COMBINED` 두 컬럼 동일 ·
>   `SILVER.AGENCY_AD_DIGITAL.CPC_CLICK_SRC` 9,080 · `AGENCY_AD_BROADCAST.CPC_CALL_SRC` 34,719.
> · `E-1` = 사용자 지시 *"확인용 쿼리만 알려주고 다음 작업은 다음 프롬프트부터"* ⇒ **판정 보류**(문항 미발행).
>
> ##### ▣ 5. 규약 준수 · 자기결함
>
> · 🟢 라벨 선점 = `id_collision_gate --next O` 가 **O175** 를 제안했으나 **정의 축 최대 173** 이고 참조 174는
>   `O173` 의 예고 주석이었다(`P222`) ⇒ **O174** 를 썼고 착수 시 원장에 **1줄 선점 등재**했다.
> · 🟢 파괴 연산은 전부 선승인 범위에서 집행 = 재균형 2건(문서20·문서50) · `_scratch_` 삭제 1건
>   (`os.listdir` 양축 확인) · 스냅샷은 `snapshot_util` 경유 18건(`agent_spec.yaml` 은 동명이라 `.2` 접미).
> · 🟢 임시 도구를 **자기가 만든 구조로** 만들었다(`_scratch_o174_cpc_rename.py`) ⇒ 세션 종료 시 삭제해
>   `gate_census --final` rc=0 을 유지했다(구조의 첫 실사용).
> · 🔴 **자기결함 2건** = ㉠ 원장 §1 행을 **2,047자**로 써서 1,900자 규약을 넘겼다 ⇒ 즉시 축약(1,817자).
>   🔴 이것은 `O170` 이 2회, `O171` 이 1회 낸 **같은 결함의 4번째**다 — 기록 직전에 길이를 재지 않으면 반복된다.
>   ㉡ 음성 테스트 초판이 **유령 등재를 만들었다**(위 ③).
> · `read` 미반환 **0** · 재호출 **0**.

> #### 🟢 [2026-09-21 O174-C] dbt 성공 후 마감 — 산출물 5종 재생성 · 골든 1건 재발행 · 누락 소비처 1건 시정
>
> **선행** = 사용자 `dbt build` 재실행 성공(**PASS 524 · WARN 37 · ERROR 0 · TOTAL 561 · 305.74s**).
> `O174-B` 가 예고한 대사 기대값과 **4/4 일치**했다:
>
> | 객체 | 예고 | 실측 |
> |---|---|---|
> | `SILVER.AGENCY_AD_DIGITAL` | 0 → 203,138 (`CPC_CLICK_SRC` 9,080) | **203,138 / 9,080** ✅ |
> | `SILVER.AGENCY_AD_BROADCAST` | 0 → 39,823 (`CPC_CALL_SRC` 34,719) | **39,823 / 34,719** ✅ |
> | `GOLD.FACT_AD_DIGITAL` | 불변 203,138 / 9,080 | **일치** ✅ |
> | `GOLD.FACT_AD_BROADCAST` | 불변 39,823 / 34,719 | **일치** ✅ |
>
> ⇒ 🟢 실패 빌드의 **0행 피해가 복구**됐고, `WIDE_AD_COMBINED`(242,961)에서 두 컬럼이 각각 9,080 / 34,719 로 분리 노출된다.
>
> ##### ▣ 1. 🔴🔴 누락 소비처 1건 — 도구의 하드코딩 매핑표
>
> · 산출물 재생성 후 `test_generators` 의 골든 차이를 분해하다 **감사 판정이 뒤집힌 것**을 발견했다:
>   `06_BRONZE노출감사` 의 「노출됨(GOLD)」이 **137 → 135**, 「미노출(검토대상)」이 **542 → 594**.
>   그중 2건이 BRONZE `DGT_AD_CMPGN_DTLS.CPC`·`VIDEO_AD_CMPGN_DTLS.CPC` 이고 비고가
>   *"GOLD `CPC_SRC` 미확인 — 배선 필요"* 였다.
> · 🔴 원인 = `scripts/run_bronze_audit_host.py` 의 **`LINEAGE_MAP`** 2줄이 구명 `CPC_SRC` 를 가리켰다.
>   O174-A 의 「소비처 전수 실측」은 **파일 텍스트**를 셌으므로 이 매핑표를 **소비처로 세지 못했다**
>   (`CPC_SRC` 문자열이 `scripts/` 에 19회 있다고 세어 놓고도 **판정 로직**임을 구분하지 않았다).
> · 🟢 시정 = `CPC`→`CPC_CLICK_SRC`(FACT_AD_DIGITAL) · `CPC`→`CPC_CALL_SRC`(FACT_AD_BROADCAST)
>   ⇒ 재생성 후 **노출됨 137 복원** · 비고가 *"개명 적재 → GOLD `CPC_CLICK_SRC`"* 로 바뀌었다.
> · 🟢🟢 **판정식 = 개명 소비처는 「그 이름을 쓰는 파일」이 아니라 「그 이름으로 판정하는 코드」까지다.**
>   🔴 개명 뒤에 **판정기를 돌려 판정이 바뀌지 않았는지**를 봐야 한다 — 파일을 다 고쳐도 판정은 틀릴 수 있다.
>
> ##### ▣ 2. 골든 차이 21건 전량 규명 (6갈래)
>
> | 갈래 | 건수 | 원인 |
> |---|---:|---|
> | CPC 개명 짝 | 6 | 구명 3(`inv.desc_cell_list`·`entry_keys`·`index_key_list`) ↔ 신명 3 |
> | `06.rows` +50 | 1 | `O171` 이 CRM **50테이블 전건**에 추가한 `_STDR_YM`(감사 분모 증가) |
> | `06` 판정 이동 | 2 | 🔴 **내 결함**(위 ▣1) — 시정 후 원상 복원 |
> | `08.rows` +34 | 4 | CPC 개명 2 + `UPPER_CMPGN_NM`→`CMPGN_UTM_NM` + `GA_*`→`BIGQUERY_*` 개명 연쇄 + 신규 SILVER 테이블 컬럼(`CRM_SEND_MEMBER_LINK_LOG` 11 · `CRM_MKTNG_CODE` 10 · `OPEN_LOG` 6 등) |
> | `NO_CONSUMER` +30 | 1 | 아직 GOLD 가 소비하지 않는 신규 SILVER 컬럼 |
> | 측정일 | 3 | 2026-09-14/16 → **2026-09-21** 재생성 |
>
> · 🔴 **판정식 = 골든 차이는 「건수」가 아니라 「항목별 원인」으로 규명한다** — 21건 중 **2건이 내 결함**이었고,
>   건수만 보고 `--update-golden` 했다면 그 결함을 **기준선에 굳혔다**.
> · 실유실 0 확인 = 소멸 12건 전부 **개명 대응짝이 실재**한다(20260830 판본과 키 집합 대조).
> · 골든 재발행 = `test_generators --update-golden --reason "<6갈래 규명 전문>"` 1건.
>
> ##### ▣ 3. 산출물 재생성 · 선행 의존
>
> · 재생성 = `02` 인벤토리(48테이블 1,010컬럼 / 51테이블 1,321컬럼) · `04` 컬럼계보(마트 14 · 컬럼 563 · 미해석 0) ·
>   `06` 노출감사(1,254행) · `08` 보존율(**300/441 = 68.0%**) · `09` 빅테이블 VIEW(941줄 / 157,538 B).
> · 🔴 선행 의존 2건을 먼저 만들어야 했다 — `/tmp` 는 턴 사이에 비워진다:
>   `dump_schema.py` → `/tmp/schema.json`(views 14 · tables 37 · silver 48 · bronze 56 · fk 56) ·
>   `census_columns.py` → `/tmp/census.json`. 🔴 이것을 모르고 생성기를 먼저 돌려 `FileNotFoundError` 2회를 받았다.
> · ⚠️ `SESSION_LABEL=O174 python3 …` 형태는 이 셸에서 **`timeout` 이 먼저 먹어 rc=127** 이 된다
>   ⇒ `export SESSION_LABEL=O174 && timeout …` 로 분리해야 한다(스냅샷 라벨이 `UNLABELED` 로 남는 원인).
>
> ##### ▣ 4. 🟢 torn read 판정 — 손상이 아니었다
>
> · `scripts/golden/outputs.json` 을 읽는 `test_generators` 가 **`JSONDecodeError`(line 923 col 46)** 로 죽었다.
>   같은 파일을 다시 읽어 `json.loads` 를 돌리자 **정상 파싱**됐고 재실행은 **21/0 PASS** 였다.
> · 🟢 `R1-7-3` 의 실물이다 — **「손상」 판정 전에 2~3회 재읽어라.** 이 마운트는 torn read 를 낸다.
>
> ##### ▣ 5. 종료 검증
>
> · 음성 테스트 **40종 전건 rc=0**(직전 2종 FAIL 이 개명 반영으로 해소) ·
>   `verify_wide_doc` **575컬럼 불일치 0** · `table_ddl_column_gate` **84/84** ·
>   `sv_code_label_gate` blocking 0 · **advisory 0** · `rename_stale_gate` 축1 0 · `gate_census --final` rc=0 ·
>   `decision_closure_gate` 🟠 경고 2건(blocking 아님).
> · `E-1` = **사용자가 현업에 직접 확인**하기로 결정 ⇒ 에이전트는 문항을 발행하지 않았다(확인 쿼리 3본만 제시).
> · `read` 미반환 **0** · 재호출 **0**.
