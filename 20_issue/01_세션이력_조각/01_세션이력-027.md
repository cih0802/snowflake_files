<!-- SPLIT-CHUNK 01_세션이력.md | 027/027 | 허브 = 01_세션이력.md | 원문 1~182행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-18 O84] ML 예측층 재배포 + Agent 3종 추천질문 10유형 정리 + 게이트 2종 신설 — **「완료」로 은퇴된 판정이 라이브와 어긋난 사례 2건 적발**
>
> 🔒 **라벨 근거**(`R1-4-3` · `P222`) — 게이트 「다음 발급 **O86**」 ↔ **정의 최대 83** ⇒ **O84** 를 쓴다.
> 참조 축 85 는 이전 세션(§O83)이 이미 기록한 **자기증폭**이다(등재 행이 다음 번호를 인용해 참조가 오른다).
> 🔴 **자기위반 = 선점 등재를 하지 않았다.** 산출물 11종에 `O84` 를 먼저 새기고 이력·원장 등재를 마지막에 했다.
> `R1-4-3` 은 「원장에 한 줄 선점 등재하고 착수」를 요구한다. ⇒ 이 항목이 **사후 등재**이며 순서 위반이다.
>
> **트리거** = 사용자 지시 6단 = ① 이슈문서 재독 + 잔여 진행 ② Agent yaml 추천질문 10유형 정리
> ③ bronze 결손 요약 문서 생성 ④ 「Agent 구성 완료됐나」 확인 ⑤ `09_1`·`09_2` 를 3종에 맞게 갱신
> ⑥ 원장·인수인계 등재.
>
> ##### 🟢 A. ML 예측층 재배포 (사용자 승인 「지금 재배포 착수」)
>
> * **SERVING 뷰 6/7** + **SV 6/7** + **GRANT 36** 집행 · 실패 0. 러너 = `scripts/deploy_ml_serving_views.py` ·
>   `scripts/deploy_ml_semantic_views.py`(둘 다 `--dry-run` 선행).
> * 🟢 **사전 함정 회피 실증** — O74 가 경고한 `PREDICTION VARIANT` 문자열화가 이번 이관에는 없었다.
>   4테이블 전건 `TYPEOF = OBJECT`(단일 타입) ⇒ 뷰의 JSON 추출이 조용히 NULL 이 되는 경로가 닫혀 있었다.
> * 🟢 **사후 값 재현 검증** — 배포 성공이 정합의 증거가 아니므로 발행 수치와 대조했다.
>   `ML_SPONSOR_RISK_V` **829,609행 / 722,916명** · `ML_DVLP_FORECAST_V` **1,824 = 12+360+228+24+1200** ·
>   `ML_FEE_FORECAST_V` **660행 평균 2.88억** · `ML_LTV_SCORE_V` **100 = 50+50** ·
>   `ML_FEATURE_IMPORTANCE_V` **22 = 11+11** 이고 기여도 평균 **0.090909 = 1/11** ⇒ 「분석유형 내 합계 = 1」 설계 주장까지 검증.
> * 🟢 **SV 기능 확인** = `SEMANTIC_VIEW(SV_ML_DVLP_FORECAST …)` 가 O74 발행값을 자리수까지 재현했다:
>   TOTAL **365,486** ↔ DEPT **317,285** ↔ NEW_OLD **378,637** ↔ SPNSR_BSNS **456,037**(만원).
>   ⇒ 구 계정 발행값이 재배포로 **충실히 재현**됐다는 독립 증거다.
> * ⛔ **`SV_ML_MEMBER_RISK` 1종만 미배포** — 오류가 아니라 **범위 결정의 결과**다.
>   base `ML_MEMBER_RISK_V` 의 dedup CTE 가 `GN_DW.ML.MBER_MONTHLY_INFO` 를 읽는데, 그 테이블은
>   사용자가 확정한 「예측결과 16종만」 이관에서 제외된 학습·중간 37종에 속하고
>   `SNOWFLAKE.ACCOUNT_USAGE.TABLES` 전역 조회 **0건**으로 부재를 확정했다.
> * 🔴 **대체 dedup 을 만들지 않았다** — O74 가 **dedup 이 지표를 움직인다**고 실측했다
>   (확률평균 0.3829→0.4418 · ≥0.5 건수 29,690→29,121). 규칙을 바꾸면 발행 설계와 값이 갈린다.
>
> ##### 🔴🔴 B. 최대 발견 — 「배포·검증 완료」로 은퇴된 판정이 이 계정에서 성립하지 않는다 (2건)
>
> | # | 은퇴된 기재 | 라이브 실측 |
> |---|---|---|
> | ㉠ | O74 *"SERVING 뷰 7 + SV 7 배포·검증 완료"* (`90` §ML 예측 SV·Agent 배포) | 세션 시작 시점 **뷰 0 · SV 0** |
> | ㉡ | O74 *"`AGENT_MEMBER` **VERSION$6** 도구 10 · `AGENT_OVERALL` 도구 8"* | **VERSION$3** · 도구 **7** / 도구 **4** · ML 참조 **0** |
>
> * 원인은 **계정 이관**(구 `zx03676` → `DV07626`)이고 O75 가 ㉠의 부재를 이미 기록했다
>   (*"적재 후 재배포 필요"*). 🔴 그런데 그 조건이 **충족된 뒤에도** 아무도 실행하지 않았고,
>   그 사이 행은 **`90_해소완료_로그` 로 은퇴**됐다.
> * 🔴 **구조 결함 `C8` = 은퇴 판정이 라이브 실재를 검증하지 않는다.** `retire_rows.py` 는
>   행 키·토큰 보존만 본다(그게 그 도구의 축이다). 「닫혔다」의 근거가 **문서 서술**이었고
>   **객체 실재가 아니었다.** ⇒ 배포성 항목의 은퇴에는 라이브 대조가 선행돼야 한다.
> * 🟢 이번에 ㉠은 해소했고(A) ㉡은 `AGENT_OVERALL`·`AGENT_MARKETING` 까지 해소됐다(D).
>
> ##### 🟢 C. Agent 3종 추천질문 = 10유형 10문항 (사용자 지시 「딱 10개」)
>
> | Agent | 기존 → 10 | 설계 기준 |
> |---|---|---|
> | `AGENT_MARKETING` | 24 → **10** | 6도구 전건 + 마케팅 보고서 5분석구분 전건 · 오답 유발 규칙 우선(미매핑 총계 · 표 분리) |
> | `AGENT_MEMBER` | 30 → **10** | **도구 1개당 1문항**(9) + 원천 질의 1 · ⛔ `analyst_ml_member_risk` 제외(SV 미배포로 반드시 실패) |
> | `AGENT_OVERALL` | 19 → **10** | 8도구 전건 + 원천 1 · 개발금액 예측은 **계열 비교 문항 별도**(O74 실측 125% 불일치 가드레일) |
>
> * 🔴 **판단 = 산출 불가 트랩을 제안 목록에서 뺐다.** 종전 MARKETING 24번 「매체별 목표 달성율」은
>   의도된 트랩이었으나 `sample_questions` 는 **사용자에게 제안으로 노출**되므로 답할 수 없는 문항을
>   권하는 셈이 된다. 가드레일은 `instructions` 가 담당한다.
> * 검증 = 3종 전건 **문항 10 · 도구 = 리소스 일치 · `yaml.safe_load` OK**.
>
> ##### 🟢 D. `09_1`·`09_2` 를 Agent 3종으로 확장 + 죽은 도구 구조 차단
>
> * `09_1_AGENT_생성.sql` 159 → 225줄 · `09_2_AGENT_버전업.sql` 160 → 276줄.
> * 🟢🟢 **`09_2 [0]` 사전검증 신설이 이번 세션 최대 산출이다.** `ADD VERSION FROM` 은
>   **참조 SV 실재를 검사하지 않고 성공**한다 ⇒ 죽은 도구가 조용히 생긴다(무증상 오답 계열).
>   24건을 `INFORMATION_SCHEMA.SEMANTIC_VIEWS` 와 대조해 **실행 전에** 막는다.
>   실행 검증 = **1행**(`AGENT_MEMBER / SV_ML_MEMBER_RISK`) 검출 · 나머지 23건 통과.
>   🔴 컬럼명 실측 교정 = `SEMANTIC_VIEW_NAME` 이 아니라 **`"NAME"`·`"SCHEMA"`**(예약어 ⇒ 인용 필수).
> * 🟢 **`[5]` 검증을 기계 계수로 교체** — `RESULT_SCAN` + `REGEXP_COUNT` 로 도구·문항·ML 참조를 센다.
>   종전 `DESCRIBE AGENT` 육안 확인이 ㉡ 격차를 놓친 직접 원인이다.
> * 🟢 **`⑳` 결정을 문서에 확정** — 이 프로젝트는 **명명 버전(VERSION$n)** 방식이라
>   `cortex_agent_save`/`deploy`(live 버전 생성)를 쓰지 않고 `ADD VERSION FROM` 경로를 정본으로 한다.
>   조회용 `cortex_agent_read` 는 무해하므로 허용한다. 🔴 사용자 종전 선택(「스킬 경로로 전환」)은
>   이 실측 제약 때문에 **철회**됐다.
> * 🟢 배포 결과(사용자가 직접 실행) = `AGENT_OVERALL` **VERSION$4 · 도구 8 · 문항 10 · ML 4** ·
>   `AGENT_MARKETING` **VERSION$3 · 도구 6 · 문항 10**. ⇒ **마케팅 Agent 배포 완료**(O76 의 ⛔ 미배포 해제).
>
> ##### 🟢 E. 게이트 2종 신설 (착수표 ⑱ C3·C2 처방 이행)
>
> * `scripts/sv_identifier_gate.py` — 축3(① SV 실재 blocking · ② 정규화 참조 blocking · ③ 백틱 advisory).
>   **음성 테스트 6/6**(`scripts/test_sv_identifier_gate.py`) · 🔴 **면제 2종이 무력화가 아님을 시험에 포함**
>   (은퇴 등재를 비우면 FAIL 로 잡히는지 검증 = `P106` 방어).
> * `scripts/agent_tool_claim_gate.py` — 같은 SV 를 여러 Agent 가 중복 보유할 때 **가불가 주장 모순**을 본다.
>   중복 보유 SV **8종** · 모순 **0** · 문안 이격 advisory 2.
> * 🟢 이 게이트가 첫 실사용에서 **ML 드리프트 7건을 blocking 으로 검출**했다(B ㉠의 발견 경로).
>
> ##### 🟢 F. 신설 문서 — `30_output_share/10_원천입고_결손요약.md`
>
> * 사용자 요청 = 정식 입고요청서가 아닌 **심플 리스트**(지금 있는 것 / 부족한 것 / 필요한 것).
> * 🔴 **전제 정정 = 「Agent 요건 전부 = bronze 입고」는 절반만 맞다.** 판정 정본 507필드 전수 집계 =
>   가동 **368(72.6%)** · **A 입고 95** · **B 내부 배선 21** · **C 업무 확정 23** ⇒ 잔여 139 중 입고는 **68%** 뿐이다.
> * 🔴 **B 계열을 입고 요청에 넣으면 안 된다** — 원천에 값이 있고 병목은 DW FK 다
>   (`FACT_AD_PERFORMANCE → DIM_AD_CREATIVE` **243,545/243,545 전건 센티넬** · `P52`).
>   원천 담당은 *"이미 보내고 있다"* 고 답하게 된다. 문서에 이 경고를 명시했다.
> * 입고 우선순위 1번 = **「회원 3. 서비스」 54건**(UMS 발송 5컬럼 전건 NULL + GA4 세션 + 행사 어드민).
> * 🟢 **문서 계보 기록** — 선행본 `_archive/2026071 6·20260806/02_원천결손_Gap분석.md` 은 **후속 없이 은퇴**됐고
>   이 문서가 첫 후속본이다. 은퇴 README 가 경고한 **분모 차이**(선행 = GOLD 컬럼 주입률 105/411 ↔
>   본문 = 보고서 필드 507)를 **비교금지**로 적었다.
>
> ##### 🔴 G. 내 도구·판단에서 자기적발한 결함 (전부 시정)
>
> | # | 결함 | 잡은 방법 |
> |---|---|---|
> | ㄱ | `agent_tool_claim_gate` 파서가 **각 스펙 마지막 도구 설명을 전건 누락**(desc_len=0 **3건**) → 6쌍만 검사한 **거짓 PASS** | desc 길이 실측 |
> | ㄴ | `sv_identifier_gate` ②가 「제거했다」는 **역사적 서술을 오탐 3건**(`SV_MEMBER_EVENT.AVG_RETENTION_MONTHS`) | 문맥 확인 |
> | ㄷ | SV 배포 러너가 COMMENT 속 **참조 문자열**에 부분일치해 `SV_ML_SPONSOR_RISK` **오스킵** | `--dry-run` |
> | ㄹ | `30_output_share/10` §1 의 Agent 도구 수를 **파일 값(10·8)** 으로 적어 라이브(7·4)로 오독되게 썼다 | 사용자 질문 |
>
> * 🟢 ㄱ~ㄷ 은 **게이트를 만든 그 세션에서 그 게이트를 의심해** 잡았다(`P224` = 게이트의 축을 본다).
> * 🔴 ㄴ 의 처방은 「검사 끄기」가 아니라 **행선지를 명시한 화이트리스트**로 했다(`RETIRED_IDENTS`).
>   `index_row_gate`(의도적 삭제 ↔ 사고 구별 불가 · O82-C ④)·`split_doc --verify` 게이트3(은퇴를 유실로 오탐)과
>   **같은 계열의 설계 공백**이고 처방도 같다.
>
> ##### 🔴 H. 자기위반 (지침 축)
>
> * **`R1-4-3` 선점 등재 미이행** — 위 라벨 항 참조. 산출물 11종에 먼저 새겼다.
> * **`semantic_studio` 스킬의 `cortex_agent_write` 미사용** — `sample_questions` 블록만 `edit` 로 치환했다.
>   사유 = 그 도구가 **전체 YAML 재작성**을 요구하고 32~41KB 지시문을 손으로 재현하는 것이
>   **O76-C `A19`(이스케이프 파괴)를 재발시키는 경로**이기 때문이다. 🔴 이것은 **공개 편차**이며
>   A14 처럼 침묵 우회가 아니다 — 결과를 정식 리더(`cortex_agent_read`)와 게이트로 검증했다.
>   ⇒ 규약으로 어떻게 정리할지는 다음 세션 결정 사항이다.
> * **`R1-1` Step 2 부분 이행** — 세션 초 인덱스·이력·인수인계를 조각 단위로 읽었으나
>   `50_dbt`(상시 정독 대상)는 읽지 않았다. ⇒ 그 문서 소관 수치는 이 세션에서 **인용하지 않았다**.
>
> ##### 🔴 I. 신규 발견 — 레지스트리·소유권 정합 결함 2건
>
> * **`cortex-project.yaml` 에 `AGENT_MARKETING` 미등록** — `cortex_agent_read(source=workspace)` 가
>   *"File not found"* 로 거부했는데 파일은 로컬·스테이지 양쪽에 실재했다(스테이지 32,352B 확인).
>   원인 = O76 이 `cortex_agent_write` 대신 `write` 툴로 저작해 등록이 생기지 않은 것 ⇒ **`A14` 의 하류 결과**.
>   🟢 정본 경로 규약(`agents/<AGENT>/agent_spec.yaml`)을 지키며 **등록만 보충**했다(루트 `.agent.yaml` 미생성).
> * 🔴 **`AGENT_MARKETING` 소유자 = `ACCOUNTADMIN`** (다른 2종은 `GN_DW_ADMIN`) ⇒ **3종 소유자 불일치**.
>   `GN_DW_ADMIN` 은 이 Agent 에 **아무 권한이 없다**(OWNERSHIP→ACCOUNTADMIN · USAGE→ANALYST/VIEWER/SERVICE).
>   원인 = `09_1` 이 `USE ROLE GN_DW_ADMIN` 을 선두에 두었지만 실제 실행이 ACCOUNTADMIN 으로 됐고
>   **스크립트가 이를 조용히 통과**시켰다. ⬜ **잔여 = 소유권 이관**(`GRANT OWNERSHIP … COPY CURRENT GRANTS`).
>
> ##### 🔴 J. `09_1` 「신규 환경 최초 실행」 적합성 검토 (사용자 지시)
>
> 사용자가 `CREATE OR REPLACE` → **`CREATE AGENT IF NOT EXISTS`** 로 바꾸고 주석을 해제했다.
> 🟢 문법은 컴파일 검증 통과. 재실행 파괴가 사라진 것은 개선이다. 🔴 그러나 결함 **8건**이 남았다.
>
> * 🔴 ① **소유권 블록 `[2]` 가 주석인데 조건 성립을 검증하지 않는다** ⇒ I 의 소유자 불일치가 발생했다.
> * 🔴 ② **`IF NOT EXISTS` 는 COMMENT/PROFILE 을 갱신하지 않는다** ⇒ 파일이 선언한 「COMMENT 의 정본」이 깨진다.
>   실증 = 라이브 `AGENT_MEMBER` comment 가 아직 *"Phase-1 · SV 7종"* 이다. ⇒ `[5]` 를 상시 블록으로 승격해야 한다.
> * 🔴 ③ **CoWork SI 객체 존재를 전제하지만 생성 단계가 없다** — 최신 계정은 SI 객체 없이 자동 노출되고
>   `SHOW/ALTER SNOWFLAKE INTELLIGENCE` 는 레거시 경로다 ⇒ 신규 환경에서 `[4]` 가 죽을 수 있다.
> * 🔴 ④ **주석이 코드와 정면 모순 8곳** — 「`CREATE OR REPLACE` 재실행 금지」·「기존 2종에 재실행 금지 ·
>   MARKETING 만 실행」·「`[1]` 은 파괴적」·「실측 2행」 등이 전부 활성 코드와 어긋난다.
> * 🟠 ⑤ **`[6]` 검증이 무력** — "owner=GN_DW_ADMIN" 을 기대값으로 적었지만 **판정 쿼리가 없다**(①의 미발견 원인).
> * 🟠 ⑥ 웨어하우스 이원화(`GN_DW_ANALYTICS_WH` ↔ ML DDL 의 `GN_DW_DEV_WH`) 확인 필요.
> * 🟠 ⑦ **재현성 결함** — `09_2` 가 읽는 stage 가 `USER$.PUBLIC."snowflake_files"` = **TRIALADMIN 개인 워크스페이스**다.
>   실행자가 다르면 경로가 달라지고 `GN_DW_ADMIN` 이 타인 `USER$` 를 읽을 보장도 없다.
> * 🟠 ⑧ **`09_1` 에 전제검증 블록이 없다** — `09_2` 에는 `[0]` 이 있는데 대응물이 없다.
>
> ##### 🔴 K. 구조 결함 — 게이트가 동시 편집·부분 읽기에 취약하다
>
> * `doc_line_length_gate` 가 **두 형태로 죽었다**: ㉠ 스테이지 동기화 중 파일을 읽어
>   `UnicodeDecodeError`(9,742B 중 8,081B = **torn read**) ㉡ 타 세션 in-flight 파일에 `OSError [Errno 5]`.
>   ㉡의 원인 파일 = `scripts/_o83h_append.py`·`_o83h_r179.py`(**04:41 생성 · 이후 소멸**).
> * 🔴 **판정** = 게이트가 **부분 읽기·부재를 손상으로 오판하고 우아하게 실패하지 못한다**.
>   O82-B 가 기록한 **torn read 오판**(`A16`)과 같은 계열이다. 대상 316건 재검사 = 손상 **0** ·
>   재실행 2회 연속 PASS 로 확정했다. ⬜ **처방 = 읽기 실패를 파일 단위로 격리·재시도**하도록 게이트 보강.
> * 🟢 **`C7` 재확인** — 이 세션 내내 타 세션(O83-H)이 원장·지침을 편집했다(05:01~05:02).
>   등재 착수 전 `R1-7` 해시 안정성 검사(16파일 · 6초 간격) **불안정 0** 을 확인하고 진행했다.
>
> ##### ⬜ 잔여 (다음 세션)
>
> * ⬜ **`AGENT_MEMBER` 버전업** — `[0]` 사전검증 1행 선결: ㉮ `MBER_MONTHLY_INFO` 추가 이관 또는
>   ㉯ `analyst_ml_member_risk` 제거(도구 9종). 🔴 **사용자 결정 사항**이다.
> * ⬜ **`AGENT_MARKETING` 소유권 이관** → `GN_DW_ADMIN`(I 항).
> * ⬜ **`09_1` 결함 8건 시정**(J 항) — 우선순위 = `[0]` 전제검증 신설 · `[5]` 승격 · 주석 8곳 정리.
> * ⬜ **NL 라우팅 스모크 미실행** — 트라이얼은 `DATA_AGENT_RUN` 차단이라 CoWork UI 수동.
>   3종 × 추천질문 10 = 30문항이 최소 스모크다. 🔴 **「배포 완료」는 「라우팅 검증 완료」가 아니다.**
> * ⬜ **`C8` 은퇴 판정에 라이브 대조 선행** 조문화 · **`K` 게이트 읽기 격리** 보강.
> * ⬜ **⑱ 미독 2종 잔여** = `01_SV-Agent 작업계획`(331줄) · `04_SV_설계`(811줄).
>   이 세션은 그 예산을 ML 배포와 DDL 903줄 전량 독해에 썼다.
>
> ##### 🟢 게이트 (이번 세션)
>
> * `clause_order_gate` **PASS**(조문 75 · 역전 0 · 중복 0) · `sv_identifier_gate` 음성 6/6 ·
>   `agent_tool_claim_gate` 모순 0 · 내 산출물 줄길이 **위반 0**(경고 9 = 기존 지시문 장문).
> * `read` 미반환 = **9건**(정식 리더 3 · grep 1 · SQL SHOW 1 · 스킬/조각 4) · 재호출 **9건** ⇒ 판정식 충족.
> * 🔴 **`P` 발급 0** — 교훈 후보(은퇴 판정 라이브 대조 · 게이트 읽기 격리)는 다음 세션 등재 대상이다.
