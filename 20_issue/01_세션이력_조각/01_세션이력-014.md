<!-- SPLIT-CHUNK 01_세션이력.md | 014/025 | 허브 = 01_세션이력.md | 원문 3372~3627행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-11 O59-B] **§6.9-(5) 이행 4/9 → 9/9 완료** — 열거 누락 41건 기계 교정 · 오염값 1건 신규 적발
>
> **트리거** = 착수 순서 2번. 정본 = `04_SV_설계.md` **§6.9-(5)** — *"저카디널리티 코드 차원은 comment에 실제 코드값을 열거해야 한다 … 다른 SV의 코드 차원도 동일 점검 대상"*.
>
> ##### 🔴 종전 게이트의 공백을 먼저 메웠다 — ①과 ④는 **다른 결함**이다
> O59-A 게이트는 「적힌 값이 실재하는가」(①)만 봤다. §6.9-(5) 는 「**적혀 있는가**」(④)를 요구한다.
> ⇒ ④ **열거 누락 검사 신설**. 두 결함의 종착점은 같다 — 안 적히면 Analyst 가 값을 **추측**하고,
>   추측 결과도 `WHERE dim='없는값'` = **0행 무증상 오답**이다(§6.9-(5) 의 원래 사고 원인 그대로).
>
> ##### 🟢 임계를 **추측하지 않고 실측으로 정했다** (`scripts/sv_dim_cardinality.py` 신설)
> §6.9-(5) 는 「저카디널리티」의 임계를 정의하지 않는다. 임계를 찍으면 대상이 틀리고, 그 게이트는 통과해도 아무것도 보장하지 않는다.
> ⇒ SV 차원 **160개 전수 카디널리티 측정** 후 결정:
>
> | 결정 | 근거(실측) |
> |---|---|
> | **distinct ≤ 20** | 임계별 대상 규모 = 5→27 · 10→39 · **20→63** · 30→66 · 50→75 · 111→84. 20 아래에 코드/라벨 축이 전부 들어오고 그 위 추가분은 대부분 명칭·경로 축 |
> | **VARCHAR 한정** | 20 이하 63개 중 VARCHAR 45개가 실제 대상. 나머지는 아래 두 제외군 |
> | **BOOLEAN 제외** | `HAS_BILLING`·`UNPAID_FLAG`·`HAS_POSITIVE_GOAL`·`HAS_ACTUAL`·`IS_12M_OBSERVABLE` = TRUE/FALSE 자명 |
> | **NUMBER 제외** | 저카디널리티 NUMBER 축은 실측상 `CAL_QUARTER`(4)·`CAL_MONTH`(12) = **시간축뿐**이고 자기설명적 |
> | **`DEVICE_SCOPE_DESC` 제외**(`ENUM_SKIP` 등재) | **코드 차원이 아니라 설명문 차원**이다 — 값이 *"방송광고(TV·재방송) — 기기 개념 없음"* 같은 문장이고, 필터용 코드축은 짝인 `DEVICE_TYPE`(4종)이며 그쪽은 이미 열거를 갖고 있다 |
> ⇒ 라이브 위반 **41건** 확정(45 중 4건은 「실제값 N종」 선언이 이미 있어 대상 밖).
>
> ##### 🟢 41건 **기계 교정** — 손 이관 0 (`scripts/patch_sv_enum_comments.py` 신설)
> 🔴 41개 COMMENT 를 손으로 고치면 ① 오타가 그대로 0행 오답이 되고 ② 다음 적재에서 재현이 불가능하다.
> ⇒ 라이브 실측값을 읽어 `05_1~05_9_SV_DDL_*.sql` 의 COMMENT 말미에 `실제값 N종: 'a'·'b'…`(+ NULL) 를 삽입.
> **판정 로직은 게이트와 1벌**을 쓴다(`is_enum_target`·`enumerated_values` import) — 대상이 갈라질 수 없다.
> · 결과: **40/40 패치 · 미처리 0** · SV별 AD 9 · EVENT 9 · FEE 5 · SERVICE 5 · EP 5 · COHORT 2 · MONTHLY 2 · DEV_ACHV 2 · BUDGET 1.
> · 후처리(`scripts/polish_sv_enum_comments.py`): 문장 접합부 **마침표 31건** 보정.
> 🔴 **패처 자기적발 2건**: ㉮ 선언이 **3줄로 분리**된 파일(`05_8`)을 초판이 놓쳤다(선언 라인에서만 COMMENT 를 찾았다) → 아래 4줄 탐색으로 교정 ㉯ 값에 `'`·`·` 가 있으면 SQL 리터럴·열거 파서를 깨므로 **패치하지 않고 보고**하도록 가드를 뒀고, 그 가드가 `DEVICE_SCOPE_DESC` 를 실제로 잡아 위 제외 결정으로 이어졌다.
>
> ##### 🔴 신규 적발 — `WIDE_AD_COMBINED.CM_POSITION` **오염값**
> 열거를 기계 생성하자 값이 드러났다: **백틱 1문자(ASCII 96) 1행**. 정상 CM 위치(`E-1st`~`T-7th`·`middle`)가 아니다.
> ⇒ **O28 계열**(코드체계 오염값 · `PART_STATUS` 의 `)` 2행과 같은 유형)에 **1건 추가**.
> 처방 = 데이터 정정은 파이프라인 소관이므로 **문안에 오염 사실을 명시**했다(*"이 값으로 필터하지 말 것"*).
> 🟢 **부수 소득 — 열거 생성이 오염값 탐지기로 작동했다.** 저카디널리티 축을 전수 열거하면 오염값이 눈에 보인다(🆕 **P178**).
>
> ##### 🟢 판정 (게이트 3종 · 대상 존재 상태에서 실행 · P106)
> | 게이트 | 결과 |
> |---|---|
> | `sv_code_label_gate` 자기검사 | **축별 판정**으로 양성 **5/5** · 오탐 **0/14**(①②③ 9종 + ④ 5종) |
> | `sv_code_label_gate` 라이브 | **위반 0** (코드값부재 0 · 종수 0 · **열거누락 0** · 라벨축 0 · 폐기리터럴 0) |
> | `sv_unit_gate` 회귀 | 통과 — **GRANT 누락 0**(9종 `CREATE OR ALTER` 보존 실증 · P125) · **COMMENT 수치 신규 유입 0** · 폐기식 0 |
> | `eval_expectation_gate` | **PASS** — 불변식·DIMNAME 0건 |
> | 값 불변 | 총수납 **895,178,309,108** = FACT 일치 · 발송 **38,470,780** = FACT 일치 · 개발 2,291,878 불변 |
> ⚠️ 「N종」 표기가 **작업규칙 7(COMMENT 수치 금지)에 걸리지 않음**을 게이트로 확인했다 — 종수는 코드 도메인 크기이고
>   금지 대상(행수·금액·커버리지%·적재기간)이 아니다. 기존 문안도 이미 같은 표기를 쓰고 있었다.
>
> ##### 🔴🔴 자기적발 — **자기검사가 내 fixture 설계 오류를 잡았다**
> ④ 를 붙인 직후 자기검사가 *"오탐 2건"* 으로 실패했다. 확인하니 **오탐이 아니라 정탐**이었다 —
> `ZZ_NEG3`·`ZZ_NEG4` 는 ①②③ 용 음성 대조인데 ④ 기준으로는 **진짜 열거 누락**이다.
> 원인 = 내가 전 검사 hits 를 **한 집합으로 합쳐** 판정했다.
> ⇒ 🆕 **P179: 다축 게이트의 자기검사는 축별로 판정한다.** 음성 대조군은 **그 검사에 대해서만** 음성이다.
>   축을 섞으면 자기검사가 **거짓 실패**하고, 거짓 실패를 몇 번 보면 자기검사를 신뢰하지 않게 된다(P103-⑤ 의 자기검사 판본).
>   ⚠️ 이번엔 게이트가 스스로 잡아냈으나, 반대 방향(축을 섞어 **거짓 통과**)이면 조용히 지나간다.
>
> #### 🟢 [2026-08-11 O59-A] 게이트 신설 `scripts/sv_code_label_gate.py` — **첫 실행에서 신규 결함 3건 적발·교정**
>
> **트리거** = 재배열한 착수 순서의 1번(P120: 「무엇이 빠졌나」는 정본 기준으로 **먼저**). 라벨 작업 전에 검증 수단을 만든다.
>
> ##### 검사 3종 · 판정
> | 검사 | 근거 규약 | 라이브 결과 |
> |---|---|---|
> | ① 코드값 실존 + 종수 선언 정합 | DEC-35 R3 | 🟢 **위반 0**(교정 후) |
> | ② 라벨축 노출 — 라벨 컬럼이 있는데 코드축만 노출했는가 | DEC-35 **R1** | 🟢 위반 0 · ⚪ 라벨 부재 확인 3건(`EVENT_CATEGORY`·`SEND_STATUS`·`SUBTYPE` = DEC-35 1~2단계 대상) |
> | ③ 폐기 리터럴 노출 | DEC-35 **R5** · P174 | 🟢 0건 |
> 🔴 **② 는 정규식으로 판정하지 않는다** — 라벨 네이밍이 최소 4패턴이라 정규식은 오탐한다(§O58-D 실측).
>   **명시 등재부 `LABEL_PAIRS`** 로만 판정하고, 미등재 코드컬럼은 위반이 아니라 **정보**로 보고한다.
>   `None` 등재 = 「라벨이 없음을 확인했다」이며 **미등재와 구별**된다.
>
> ##### 🟢 첫 실행이 신규 결함 3건을 잡았다 (P103 재확인 — 첫 테스트는 반드시 무언가를 잡는다)
> | # | 대상 | 문안이 주장한 값 | 실측 실제값 | 성격 |
> |---|---|---|---|---|
> | 1·2 | `SV_MEMBER_COHORT`·`SV_MEMBER_EVENT` 의 `MARKETING_CAMPAIGN` | 두 값을 슬래시로 **합친 표기** 1개 | `유어턴(통합A)` **730행** · `유어턴(통합B)` **901행** = **별개 2행**(그 외 `유어턴(통합C)`·`유어턴(통합)` 등도 별개) | 🔴 합친 표기로 필터하면 **0행 무증상 오답** |
> | 3 | `SV_MEMBER_COHORT.PROMO_METHOD_NAME` | 접두 없는 형태 1개 | `PC기업공동캠페인` **998** · `M기업 공동캠페인` **436** · `구메뉴_기업공동캠페인` **51** — 접두 없는 값은 **부재** | 🔴 동일 |
> ✅ 교정 = 실측값으로 열거 교체 + *"부분 일치로 추측하지 말고 실제값을 조회해 확인한다"* 가드 부착.
>   **R5 준수 — 교정 문안에 폐기 리터럴을 인용하지 않았다**(P174).
>   배포 = `05_2`·`05_3` **`CREATE OR ALTER`**(GRANT 보존 · P125) → 게이트 재실행 **위반 0** ·
>   `sv_unit_gate` 회귀 **GRANT 누락 0 · COMMENT 수치 신규 유입 0 · 폐기식 0**.
>
> ##### 🔴🔴 자기적발 — **내 게이트가 3판까지 갔다. 1·2판은 쓸 수 없는 상태였다**
> | 판 | 열거 추출 방식 | 결과 | 무엇이 문제였나 |
> |---|---|---|---|
> | 1판 | 문안 **전체**에서 `'값'` 수집 | 위반 **64건** | 산문 인용을 열거로 오독 — `'왜 10대 미만이 많은가'`·`'현재 연령별'`·라벨축 안내값이 전부 위반. 🔴 **오탐 35건이 진짜를 덮었다** |
> | 2판 | 「실제값…:」 **뒤 문장 끝까지** | 위반 **21건** | 같은 줄에 이어지는 라벨축 안내(`라벨은 … 축('일반행사'·'캠페인행사')`)까지 삼켰다 |
> | **3판** | 콜론 직후 **연속 열거 블록만**(구분자로 끊기면 즉시 중단) + `'값'=라벨` 형식 흡수 | 위반 **3건 = 전부 진짜** | — |
> 🔴 **부수 버그**: 종수 비교에 **NULL 을 포함해 세서 28건 전부 `+1` 오차**로 오탐했다(문안의 「N종」은 NULL 제외 기준).
> 🔴 **부수 버그 2**: 3판 초판이 `실제값` 만 봐서 규약 변형 **`실제 코드값:`** 을 **검사조차 하지 않았다**(미탐) → 흡수.
>
> 🔴🔴 **그리고 내가 사용자에게 틀린 보고를 했다 — 즉시 정정한다.**
> 1판 결과를 보고 *"진짜 1건 = `SV_AD.DEVICE_TYPE`(`MOBILE`·`TABLET` 열거) · AD-4 는 해소로 기록됐는데 라이브에 남아 있다"* 라고 보고했다.
> **거짓이다.** 그 문안은 *"⚠필터 시 'MOBILE'/'TABLET' **아님** — 모바일은 'M'"* 이라는 **부정문**이고 열거가 아니다.
> 즉 AD-4 는 실제로 해소돼 있고 **내 1판 게이트가 부정문을 위반으로 오탐**한 것이다(**P114 의 정확한 재발**).
> ⇒ 🆕 **P176: 게이트 첫 결과에서 「진짜」를 골라내기 전에 그 문안을 열어 읽는다.** 오탐 다수 속에서 하나를
>   진짜로 지목하는 것은 **판정이 아니라 인상**이다 — 나는 문안을 읽지 않고 「AD-4 미해소」라는 그럴듯한 서사를 만들었다
>   (P115·P123 의 게이트 판본: 검증 없는 중간 판정이 그럴듯한 결함 서사를 만든다).
>
> ⇒ 🆕 **P177: 오탐률이 높은 게이트는 미탐보다 나쁘다 — P16 의 적용 한계.**
>   P16(*"침묵 통과보다 오탐이 안전하다"*)은 **소수 오탐** 전제다. 오탐이 진짜를 덮으면 P103-⑤
>   (*"상시 빨간 테스트는 무시되고, 무시되는 테스트는 없는 테스트보다 나쁘다"*)로 **뒤집힌다.**
>   ⇒ 게이트 신설 시 **오탐 대조군을 양성 대조군과 같은 수 이상** 넣는다. 이번에 음성 7종을 넣었고
>   그중 **4종은 내가 실제로 속은 케이스를 그대로 고정**한 것이다(회귀 방지).
>
> ##### 🟢 탐지력 자기검사 (P106 · 라이브 미접속 · `--self-check`)
> **양성 4/4 검출**(없는 코드값 · 종수 불일치 · 라벨축 미노출 · 폐기 리터럴) · **오탐 0/7**
> (음성 = 정확한 열거 · 산문 인용 · 라벨축 병기 · 라벨축 안내 문장 · NULL 포함 종수 · 열거 뒤 산문 · `'값'=라벨`+부정문).
> 🔴 순수 판정 함수 `judge()` 를 분리해 **라이브와 fixture 가 같은 코드로 돈다** — 자기검사가 실제 판정을 검사한다.
>
> ##### ⚠️ 이 게이트의 한계 (숨기지 않고 적는다)
> - **문안 규약 의존**: 「실제값…:」 형식을 쓰지 않은 문안은 **검사 대상 밖**이다(미탐). DEC-35 R3 로 규약을 강제해야 덮인다.
> - **고카디널리티 축은 `limit 500`** 으로 조회한다 — 500 초과 축의 열거는 오탐 가능(현 대상엔 해당 없음).
> - **코드군 ID 검증은 하지 않는다** — 값 실존만 본다. 코드군 지정 정합은 §O59 ① 의 원천 교차 검정 소관이다.
>
> #### 🔴 [2026-08-11 O59] DEC-35 착수 실측 — **헛점 2 를 깼다 · 결과코드는 1축이 아니라 2축이다**
>
> **트리거** = 사용자 결정 요청 3건(§O58-D · DEC-35 §23-F)에 답하기 위한 BRONZE 실측(R2-3).
>
> ##### 🟢🟢 ① 코드군 식별 불가(헛점 2)는 **원천 교차 검정으로 해소된다** — 도메인 포함관계는 방법이 틀렸다
> O58-D 는 *"MSG_AT `{2,3,4}` 가 후보 6개 그룹에 동시 포함되므로 값 도메인으로는 그룹을 특정할 수 없다"* 로 닫았다.
> **판정은 옳고 방법이 부족했다.** 같은 BRONZE 테이블의 **동반 컬럼**으로 교차하면 추론 없이 확정된다.
>
> | 검정 | 실측 (2026-08-11) | 판정 |
> |---|---|---|
> | `TD_MS_MSG_AT_SNDNG_DTLS` 의 `TRNSMS_STAT_CD` × `TRNSMS_FAILR_CD_ID` 교차 | `2` ⟺ 전달코드(`7000`·`6600`·`4100`) **17,230,668 전건 1:1 · 오차 0** · `3` ⟺ 오류코드 1,209,654(+전달 130 = 0.011% 예외) · `4` ⟺ **결과코드 자체 없음** 1,844,620 · NULL ⟺ 없음 276,376 | 🟢 **MSG_AT `2`=발송완료 · `3`=에러 · `4`=미발송/취소 확정** = `MS282`(0대기·1발송중·2발송완료·3에러·4예약취소)와 완전 정합 |
> | EMAIL — `SILVER.CRM_SEND_RESULT`(원천 집계) 대조 | `SUCCES_CNT` 7,039,761 ↔ `SNDNG_RST_CD='1'` **7,033,989**(차 0.082%) · `FAILR_CNT` 777,832 ↔ `='0'` **777,136**(차 0.089%). 차이는 dedup(QUALIFY) + 요청·상세 행수 격차 6,468 로 설명 | 🟢 **EMAIL `1`=성공 · `0`=실패 확정**(원천이 카운트로 독립 확증) |
> | SND — BRONZE COMMENT | `SND_MEMBER_LIST.SND_YN` COMMENT = **"발송 성공 여부"** (원천 자기서술) · `Y` 6,831,023 · `N` 824,532 · NULL 644,717 | 🟢 `Y`=성공. ⚠️ 단 `SND_REQ_MST` 의 `SEND_CNT`·`FAIL_CNT` 는 **전건 0** 이라 원천 카운트 교차는 불가 |
> ⇒ 🆕 **P175: 코드군 식별은 「사전 도메인 포함관계」가 아니라 「원천 동반 컬럼 교차」로 한다.** 작은 숫자 도메인은
>   다수 그룹의 부분집합이라 포함관계로는 원리적으로 특정할 수 없다(O58-D 판정은 옳다). 그러나 **같은 원천 테이블에
>   의미가 겹치는 동반 컬럼이 있으면 1:1 대응으로 확정된다** — 이것이 P29 3원 대조에 빠져 있던 4번째 축이다.
>   ⚠️ 확정되는 것은 **코드의 업무 의미**이고 **코드군 ID 는 여전히 정황**이다(MS282 는 분포 정합이며 지정 근거는 아니다).
>
> ##### 🔴🔴 ② 신규 결함 — 결과코드는 **2축**인데 SILVER 가 1축만 전파한다
> | 축 | 컬럼 | 성격 | SILVER 전파 |
> |---|---|---|---|
> | **A 채널 상태** | EMAIL `SNDNG_RST_CD` · MSG_AT `TRNSMS_STAT_CD` · SND `SND_YN` | **원천별로 반드시 다르다**(채널 추가 = 체계 추가) | 🟢 `SNDNG_RST_CD` 한 컬럼에 합침 = 다체계 원인 |
> | **B 통신사 결과** | MSG_AT `TRNSMS_FAILR_CD_ID` · SND `CALL_STATUS` | 🟢 **conformed** — 두 채널이 **같은 코드공간**을 쓴다(`7000`·`6600`·`7318`~`7320` 공유 실측) | 🔴 **전파 0건** — 두 컬럼 모두 SILVER 에 없다 |
> 🟢 **사전이 축 B 의 다체계를 이미 명시 설계해 뒀다** — **`MS283` = `MS056=공통 | MS057=알림톡 | MS058=SMS | MS059=MMS`**.
>   즉 「메시지 타입이 판별자, 결과코드 그룹은 4개」가 **원천 코드사전의 설계**다(우리가 만들 규약이 아니다).
>   실적재 대조: `7000`계=MS057(알림톡) · `6600`계=MS059(MMS) · `4100`계=MS058(SMS) · `9xxx`계=MS056(공통).
> 🔴 **사전 초과값 실재**(DEC-17-B 대상): `7319`·`7320`·`7321`·`9024`·`9027`·`9031`·`4434` 등이 MS057~MS059 에 없다.
> ⚠️ 축 B 미전파는 **`FSE.SUCCESS_MEMBERS`·`FAIL_MEMBERS` 전건 0 의 진짜 우회 경로**였다 — 모델 주석은
>   *"B1: `SNDNG_RST_CD`→성공/실패 코드매핑 확정 후"* 라고 적어 **코드매핑을 기다리는 것으로 규정**했으나,
>   ㉮ 축 A 의 의미는 위 ①로 확정 가능했고 ㉯ 원천이 `CRM_SEND_RESULT.SUCCES_CNT`·`FAILR_CNT` 로 **카운트를 직접 준다**.
>   ⇒ **P109 계열(「추측 금지」가 탐색 중단 근거가 됨)의 재사례.** 단 `CRM_SEND_RESULT` 는 **요청 grain** 이므로
>   회원 grain 팩트에 배분하면 P148·이중계상이 된다 — 용도는 **총계 검증**이다(정본 #85~87 은 회원번호 개수 기준).
>
> ##### 🟢 ③ `PSTMTR` 전건 NULL 의 원인 규명 — **업무 요건이 아니라 UNION arity 다**
> | 확인 | 실측 |
> |---|---|
> | 우편 상세 `TD_MS_PSTMTR_SNDNG_DTL` | **14컬럼 전수 — 결과코드 컬럼 0개** |
> | 우편 요청 마스터 `TM_MS_PSTMTR_SNDNG` | `PRCS_STAT_CD`(처리상태)는 **요청 처리 상태**이고 회원 단위 발송 결과가 아니다 |
> | 우편 집계 `TD_MS_PSTMTR_LQY_SNDNG` | `SNDNG_CNT`(발신건수)만 — **`SUCCES_CNT`·`FAILR_CNT` 컬럼 자체가 없다** ⇒ `CRM_SEND_RESULT` PSTMTR 행도 NULL |
> | 사전 | 🟢 **`MS001` = `1`정상·`2`주소불명·`3`이사·`4`수취거절·`5`수취인부재·`6`반송함투입·`7`폐문부재·`8`수취인불명 = 우편 반송사유 8종이 사전에 실재**한다 |
> ⇒ **컬럼이 존재하는 이유 = SILVER `CRM_SEND_MEMBER` 가 4원천을 `UNION ALL` 할 때 결과코드 슬롯을 1개 두었고
>   우편은 채울 원천이 없어 `NULL` 하드코딩된 것**이다(모델 15행). 업무 요건(정본 **#86 성공수(명)·#87 실패수(명)**)은
>   채널 무관하게 요구하지만 **우편은 상세·집계 양쪽에 원천이 없어 구조적으로 산출 불가**다(P21 유형 = 결손 아님).
> 🟠 **입고 요청 후보 신설 = `MS001` 반송 결과**: 사전은 있고 담는 컬럼이 BRONZE 에 없다. 우편 반송률은
>   현업 요건(#87)에 걸려 있으므로 **원천 항목 신설 요청 대상**이다(AD-5 와 같은 유형 = 미적재가 아니라 항목 부재).
>
> ##### ⬜ O59 가 남긴 것
> - 🟠 **`SEND_STATUS` 해소 방식 = 사용자 선택 대기**(축 A 개명/분리 vs 축 B conformed 신설) — 아래 §O59-A.
> - ⬜ **축 B 2컬럼 SILVER 미전파** = DEC-35 1단계에 편입할 신규 대상(`TRNSMS_FAILR_CD_ID`·`CALL_STATUS`).
> - ⬜ **`MS282` 코드군 ID 는 정황 등급** — 의미는 확정, 그룹 지정은 현업 확인 병렬(DEC-26 §16-C 규약).
> - 🟠 **우편 반송 결과 입고 요청**(문서40 후보 · `MS001` 사전 실재).
>
> #### 🟢 [2026-08-10 O56-C] EXPO-2 집행 완료 — **BRONZE 원천부터 확인 후 결정**(사용자 지시)
> **🔴 BRONZE 실측이 처리 방향을 갈랐다** (사용자 지시 = 「BRONZE 근거로」)
> | 항목 | 실측 |
> |---|---|
> | 회비 원천 `TM_PM_MBRFEE_ACMSLT` | `RQEST_AMT`(청구) **+** `PAY_AMT`(납입) **둘 다 존재** · 46,391,620행 |
> | 기부 원천 `TM_PM_DNTN_DTLS` | **`PAY_AMT` 만 존재 — 청구 컬럼(`RQEST_AMT`)이 아예 없다** · 1,130,252행 |
> | 회비 청구 | **891,959,790,888** = SILVER = GOLD **전 계층 정확 일치** |
> | 회비 납입 | **768,800,286,349** = `GOLD.PAID_FEE_BILLABLE` 정확 일치 |
> | 기부 납입 | **126,412,695,459** = 총수납의 **14.12%** · `PAY_STAT_CD='S'` 전건 |
> | SILVER 기부 branch 청구액 | **전건 NULL** — BRONZE 컬럼 부재가 그대로 전파(O40 의 구조적 뿌리) |
> | 기부 `MBRFEE_MT` | **전건 NULL**(회비월 개념 자체가 없다) |
>
> **⇒ 두 metric 의 성격이 다르다는 것이 실측으로 드러났다**
> · `PAYMENT_RATE` = **순수 결함**. 분자는 회비+기부금이고 분모는 회비 청구뿐인데 기부금은 **원천에 청구 컬럼이
>   없어** 분모에 들어갈 수 없다 ⇒ 비율이 **구조적으로 100% 를 넘는다**(전 기간 100.36% vs 정본 86.192258%).
>   문서 전역이 「쓰지 말 것」이고 **유효 용도 서술이 어디에도 없다** ⇒ **제거**.
> · `TOTAL_PAID_FEE` = **결함이 아니다**. 기부금 1,264억은 실재 수입이고 「총수납액」 질문의 정답이다.
>   문서 근거(`05_9` 문안·`AI_SQL_GENERATION`)가 「명시적으로 물을 때만 쓴다」는 **유효 용도를 명시**한다.
>   문제는 이름이었다(`_FEE` 인데 회비가 아니라서 문안이 「「납입회비」가 아니다」로 해명해야 했다) ⇒ **개명**.
>
> **🔴 등재된 「EXPO-1 과 동일 구조(㉮)」를 그대로 쓰면 안 되는 이유 — 문서에서 확인**
> 정본 `PAYMENT_RATE_FEE`·`TOTAL_PAID_FEE_BILLABLE` 은 **두 SV 가 이미 같은 이름**을 쓰고, 각각 **공64 정본**·
> **정본 #69·70** 으로 지표번호에 묶여 있다. EXPO-1 의 승격 근거(형제 SV 동명 정합)가 여기서는 **반대로 작용**해
> 승격하면 오히려 어긋난다 ⇒ **승격하지 않았다**. 사용자 지시(「문서 근거가 있으면 따르고 없으면 권고안」)에 따라
> **권고안(제거 1 + 개명 1)** 으로 집행했다.
>
> **집행·판정**
> · `PAYMENT_RATE` **제거** · `TOTAL_PAID_FEE` → **`TOTAL_PAID_ALL` 개명**(형제 SV 와 동일 이름 · 정의·값 불변)
> · 값 **895,178,309,108** 이 개명 전후 동일 · 두 SV 동일 · 회비만 **768,800,286,349** = BRONZE 정확 일치 ·
>   납부율 **86.192258%** 두 SV 동일
> · GRANT **7건 `created_on` 전건 불변** · 소비 3역할 **3/3** · `o54_sv_value_gate` 변동 **3건 = 전부 의도**
>   (`PAYMENT_RATE`→None · `TOTAL_PAID_FEE`→None · `TOTAL_PAID_ALL`→895,178,309,108) ⇒ **게이트가 개명을 정확히 포착**
> · 게이트 확장: `RETIRED_EXPR` **2종**(EXPO-1 차감식 + EXPO-2 납부율 불일치식) · 노출 **0건** ·
>   🟢 **대조 7/7**(양성 2 검출 · **음성 5 오탐 0** — `TOTAL_PAID_ALL`·`PAYMENT_RATE_FEE`·`TOTAL_BILLED_AMT`·
>   `AVG_PAID_FEE`·`UNPAID_RATIO` 를 넣어 오탐 없음을 증명)
> · ⇒ **이 SV 의 금액·비율 metric 은 전부 정본**이 됐다(제거 3종 · 개명 1종 · 승격 2종 누적).
>
> **🔴🔴 회수 반경이 EXPO-1 과 완전히 달랐다 — 내 사전 판단이 틀렸다**
> EXPO-1 때 「평가셋·VQR·Agent 스펙 인용 0건」이었으므로 EXPO-2 도 비슷하리라 가정하고 원장에 「같은 구조」로
> 적었는데, **실측하니 실행 가능한 인용이 22건** 있었다:
> · 🔴 **`05_1` 자체 검증 쿼리가 실행 실패 상태였다** — `SELECT TOTAL_PAID_FEE FROM SEMANTIC_VIEW(… METRICS
>   TOTAL_PAID_FEE)`. O52-A 선례(검증 쿼리를 놓쳐 10000배 직전)와 같은 유형이다.
> · 🔴 **평가셋이 결함값을 정답으로 고정하고 있었다** — M3 「2024년 납부율 **93.86%**」·M4 「93.66·93.86·93.98」은
>   **폐기식(모집단 불일치) 값**이다. 정본 기준 재측정 = **2023 86.05 · 2024 85.77 · 2025 85.65 (%)** 로 교체.
> · 🔴 **질문 문구 자체가 틀려 있었다** — M1·M5·O1 이 `TOTAL_PAID_FEE` 를 「**납입회비** 총액」이라 물었는데
>   그 값(895,178,309,108)은 기부금 포함 **총수납액**이다(회비만은 768,800,286,349). 문구까지 교정했다.
> · 회수 내역: `05_1`(검증쿼리·문안 2) · `06_검증쿼리_VQR`(6) · `07_평가셋_eval`(6) · `08_AGENT_spec`(4) ·
>   `10_SI연결_검증`(4) · `03_SV_metric_배속`·`04_SV_설계`(각 2) · `30_output_share/01_DW_현업활용가이드`(2) = **28곳**
> · 실행 가능 인용 **잔존 0**(잔존 2건은 `_archive` 이력) · 🆕 **P156**
>
> **⬜ EXPO-2 가 남긴 미결**
> - 🟠 **평가셋·SI연결검증 재실행 필요** — 기대값을 교체했으므로 M3·M4·M5·M1·O1 을 **실제로 다시 돌려** 판정해야
>   한다(문서만 고친 상태 = P33 미충족). `10_SI연결_검증.md` 판정칸이 여전히 `⬜` 다.
> - 🟠 **GOLD `PAID_FEE` 34,672,700 누락 미규명**(기부금의 0.027%) — SILVER 895,212,981,808 vs GOLD 895,178,309,108.
>   측정한 후보는 회원 미매칭 314행/74,880,671 이나 **금액이 일치하지 않아 원인 미특정**이다. `PAY_DE` 가
>   `'2020-1'` 형식이라 월 파싱 경로 확인이 더 필요하다. EXPO-2 판단과 무관해 중단했다.
>
> #### 🔴 O56 자기검토(사용자 지시)에서 내가 적발한 내 결함 — 별도로 적는다
> **① `write_banner` docstring 이 거짓이었다.** *"생성기가 이 파일을 읽어 끼워 넣는다"* 로 적혀 있었으나
>   전수 검색 결과 `/tmp/section_banner.json` 을 **읽는 코드는 0곳**이다(`gen_metric_gold_mapping.py` 포함).
>   05 md 의 배너는 **자동 주입되지 않는다** — 수기 삽입용 참고 산출물이다. → docstring 사실 교정(P132 자기적용).
> **② 배너 출력이 고정 경로여서 대조 실행이 정본을 오염시켰다.** `GN_DW_OUT` 을 무시하고 `/tmp` 에 썼기 때문에
>   **내가 만든 기준선·대조 실행이 정본 배너를 덮어썼다.** 그 오염된 파일을 읽고 「배너 조립가능 17 vs CSV 23
>   불일치」라는 **가짜 결함을 보고할 뻔했다**(P115 계열: 검증 없는 중간 판정). 게다가 `/tmp` 는 세션 중
>   초기화된다(P99). → 출력을 **`30_output_share/09_섹션배너.json`** 으로 이동 · 재생성 후 **29섹션 전건 정합
>   (불일치 0)** 확인 · `test_generators` **18/18** 유지. 🆕 **P152**.
> **③ `SECTION_ANCHOR_OVERRIDE` 는 현 입력에서 완전 no-op 이다**(`앵커_강제` **0행**). 05층 매핑 교정이
>   먼저 앵커를 옮기기 때문이다. 🟢 **양성 대조로 발동 능력은 증명했다** — 매핑 교정을 제거하면 **33행 발동**
>   (`FACT_MEMBER_MONTHLY → FACT_MEMBER_EVENT`) ⇒ 죽은 코드가 아니라 **이중 안전망**이다.
>   🔴 그러나 「신설했다」고만 보고하고 **no-op 사실을 적지 않은 것은 과장**이었다. 🆕 **P153**.
> **④ 「경합 처방 전량 실행 검증」은 과장이었다** — 위 ⑥ 에 정정했다.
> **⑤ 「17건 전량 교정」의 내용이 항목마다 다르다** — 위 ④ 에 분해해 적었다.
>
> #### ⬜ O56 이 남긴 미결
> - 🟠 **앵커 경합 34행의 분할 쿼리 검증** — 규모만 측정했다(위 ⑥ 정정). 경합 섹션은 「한 표로 만들 수 없다」가
>   판정이므로 처방은 **경합 팩트 수만큼 쿼리를 나눈 뒤 공통 축(회원·월)에서 합치기**다. 그 동일성을 실행으로
>   증명해야 한다. 규모 34행 · 대표 섹션 3-7좌(FGA 68,836 vs FSE 38,470,780 = **559배 격차**).
> - 🟠 **2-1 앵커가 `FME` 로 바뀌어 그 섹션 SV파생 8건의 base 가 바뀐다** — 중단 구성비·전년대비 증감·중단율2 등.
>   6단계 後半에서 SV metric 정의를 반영할 때 **base 를 `FME` 기준으로** 다시 봐야 한다(종전 base = `FMM`).
> - 🟠 **「일별 실적」(마케팅 1)은 해소 가능하다** — 판정은 `요구grain부정합`(정확)이지만 매핑이 이미
>   `FACT_MEMBER_EVENT.DEV_CNT` 다. **마케팅 1 섹션 앵커를 `FME` 로 교체하면 열린다.** 단 그 섹션은 목표(`월`)와
>   실적(`일`)이 섞여 있어 앵커 교체가 목표 필드 판정을 바꾼다 ⇒ 1-2 와 **같은 유형의 결정**이다.
> - 🔴 **P147 자기결함** — 산출물을 덮어쓰기 전에 **직전 판본 사본을 뜨지 않았다.** 행 단위 diff 를 하려는데
>   스테이지 `head` 도 이미 새 내용이어서 회수 불가였다(P107 계열). 등록부를 무력화한 재생성으로 우회했지만
>   **정공법은 덮어쓰기 전 사본**이다. ⇒ 생성기 실행 전 `$HOME/work/prev/` 로 사본을 뜬다.
> - 🟠 **1-2 「개발실적보고 (주간)」 섹션의 앵커가 월 팩트(`FACT_DEV_ACHIEVEMENT`)다.** 필드값에 「주차/주간」
>   문자열이 없어 요구grain 검사가 발동하지 않는다(섹션명에만 있다). 🔴 단 **목표 원천이 월 grain**
>   (`TM_CM_MBER_DVLP_GOAL` = `STDYY`+`STDR_MT`)이라 주간 목표는 원천적으로 불가하며 이는 기지 사실이다
>   (`STILL_BLOCKED` 「주차」 항목). ⇒ 「실적 단독은 `FME` 로 주간 산출 가능」이므로 **섹션을 목표·실적으로
>   쪼갤지**가 결정 사항이다. 규모 19행. ⛔ **사용자 결정 필요**(섹션 분할은 보고서 구조 변경이다).
> - 🟠 **`EXPO-1` 은 여전히 대기** — 4단계가 끝났으므로 이제 집행 가능한 상태다(§O55 참조).
>