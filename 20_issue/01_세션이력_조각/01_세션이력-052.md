<!-- SPLIT-CHUNK 01_세션이력.md | 052/052 | 허브 = 01_세션이력.md | 원문 1~172행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-30 O123-B] 자기검토 + 전건 승인 개선 — 내가 인용한 수치 4개를 실측하니 2개가 거짓이었고, Phase 1 을 25/25 로 완주했다
>
> **지시** = 「이번 세션 작업을 비판적으로 검토하고, 승인이 필요한 작업은 전부 승인하니 작업과 문서를 개선하라」.
>
> ##### ① 🔴🔴 최대 자기결함 — O122 기재값 4개를 재지 않고 3개 문서에 인용했다
>
> O123 초판은 「재라」 경고만 달고 **O122 의 수치를 그대로 옮겼다**(`R3-1` 위반 소지).
> 전건 실측한 결과 **절반이 재현되지 않았다**:
>
> | O122 기재 | 실측 | 판정 |
> |---|---|---|
> | `table_ddl_column_gate` **7/80** | 대상 7 · 분모 = GOLD 37 + SILVER 43 = 80 | 🟢 재현 · **정의가 선언돼 있지 않았다** |
> | 키 승격 **11건** | `MEMBER_DK` 보유 **GOLD BASE TABLE 11개** | 🟢 재현 · 라이브 전체는 **18객체**(범위 과소) |
> | `MEMBER_DK` **178파일** | 209(archive 포함) / **156**(현행) / **59**(코드·설정) | 🔴 **어느 축에서도 재현 불가** |
> | GOLD WIDE 접두 **93건** | WIDE 14뷰 · 565컬럼 · GOLD base 부재 63종/110회 | 🔴 **재현 불가**(결정 ① 이 개명 0건이라 수치는 무영향) |
>
> 🔴 **판정식 = 「재라」고 적는 것은 재는 것이 아니다.** 경고를 달고 인용하면 그 수치는 **여전히 정본에 박힌다.**
> 🟢 시정 = 재현 가능한 2건은 **정의를 명문화**하고, 재현 불가 2건은 **문서에서 인용 금지로 강등**했다.
>
> ##### ② 🔴🔴 그 실측이 「7/80 이라 부족」보다 나쁜 사실을 드러냈다 — **안전망이 0 이다**
>
> `table_ddl_column_gate` 의 `TARGETS` **7개가 전부 GOLD** 이고 분모 정의는 `MB = models/gold/` 다.
> 🔴 그런데 **본 세션이 확정한 개명 12건은 전건 SILVER** 다 ⇒ **커버리지 = 0/43.**
> ⇒ 🟢 **필요한 조치는 「분모 확대」가 아니라 「SILVER 축 신설」이다** — 착수표 ㉙ 를 그 문안으로 교체했다.
> 🔴 **판정식 = 커버리지는 전체 비율이 아니라 「내가 지금 건드리는 것」에 대해 재라.**
>
> ##### ③ 🟢 Phase 1 완주 — AGENCY 8 + CRM 15 계보를 전건 독해해 라벨링했다 (3/26 → 25/25)
>
> 모델 **23개를 전량 독해**(`R1-3`)해 P/L/X 를 계보 단위로 매겼다. 정본 = `20_issue/32_컬럼개명표.md`.
>
> | 범위 | 컬럼 | 계보 | L | P | X | 보류 | 개명 확정 |
> |---|---:|---:|---:|---:|---:|---:|---:|
> | ERP | 17 | 3 | 12 | 5 | 0 | 0 | **5** |
> | AGENCY | 75 | 8 | 61 | 7 | 7 | 0 | **7** |
> | CRM | 68 | 14 | 63 | 0 | 3 | 2 | **0** |
> | 합계 | **160** | **25** | **136** | **12** | **10** | **2** | **12** |
>
> 🟢 합계 검증 = 136+12+10+2 = 160 ✓ · **개명 실행은 0건**(선행조건 미충족).
>
> ##### ④ 🔴🔴 판정 분모 정본 자체의 결함을 찾아 고쳤다 — `UNSOURCED` 축 신설
>
> `CRM_BIZ_TARGET` 은 **`source()` 를 아예 참조하지 않는다** — 전 컬럼이 `CAST(NULL AS …)` 이고
> 말미가 `WHERE 1=0` 인 스키마 전용 빈 모델이다(원천 미입고 `E-6`).
> ⇒ 🔴 **비교할 원천명이 존재하지 않으므로 「원천 동명 부재」가 위반을 뜻하지 않는다.**
> `o122_name_drift.sql` 은 `EXTERNAL_PYTHON`(원천이 DW 밖)·`BRIDGE`(대응 BRONZE 없음)를 제외하면서
> **같은 성격의 세 번째 축을 빠뜨렸다** ⇒ 🟢 `src_scope = 'UNSOURCED'` 신설 + §A·§B 두 곳 반영 + 제외 목록 편입.
> ⇒ 🟢 **판정 분모가 169 → 160 으로 정정됐다**(O122 기재값이 9 만큼 과대였다).
> ⚠️ 그 모델의 업무 컬럼은 **10개**인데 종전 분모에 **9개만** 있었다 — 나머지 1개는 우연히 `BRONZE_CRM` 에
> 동명이 있어 「부재」로 잡히지 않았다. 🔴 **그 우연한 일치를 판정 근거로 쓸 수 없다.**
>
> 🔴 **그 수정에서 내가 버그를 냈고 즉시 잡았다** — `CRM_BIZ_TARGET` 은 `CRM%` 에 먼저 걸리므로
> `UNSOURCED` 분기를 접두 패턴 **뒤에** 두면 **도달 불가**가 된다. 🟢 순서를 앞으로 옮기고 **주석으로 고정** ·
> 라이브 재실행으로 판정대상 160 · `UNSOURCED` 10 을 **실증**했다.
> 🔴 **판정식 = `CASE` 분기를 추가할 때 선행 패턴이 그것을 삼키지 않는지 확인하라.**
>
> ##### ⑤ 🔴🔴 최대 실질 발견 — **`kind_hint` 가 개명 금지 10건을 전건 `P?` 로 오표시한다**
>
> 초판은 이 함정을 **2건**으로 적었다(그것도 모델을 읽지 않고 `DEC-25` 문안에서 추론한 것이었다).
> 전건 독해하니 **10건**이고, 초판의 2건은 **모델 실측으로 확증**됐다:
>
> | 컬럼 | 변별 축 | 개명 시 사고 |
> |---|---|---|
> | `AGENCY_AD_DIGITAL` `_SRC` 접미 **7종** | 대행사 산정값 ↔ DW 재계산값(`DEC-9` 전량 보존) | DW measure 와 **동명이의 충돌** |
> | `MBRFEE_PRCS_STAT_CD`·`PSTMTR_PRCS_STAT_CD` | 원천 테이블 변별토큰(`DEC-25` 15-A·15-C) | 도메인 `R,F,S` ↔ `0,1` **오조인** |
> | `CRM_MEMBER_DEV.SRC_LOAD_DT` | 원천 적재시각 ↔ 빌드시각(`DW_LOAD_TS`) | 증분 워터마크 의미 소실 |
>
> 🟢 **일반 규칙 승격 제안** = **접두·접미가 「무엇과 구별하기 위한 것」이면 분류 X 다.**
> 판정법 = 그 토큰을 떼면 **같은 층의 다른 컬럼과 같은 이름이 되는가.**
> 🔴 **이 판정은 원천만 봐서는 불가능하다** — 형제 컬럼 + 모델 헤더 주석을 함께 읽어야 한다.
> ⇒ 이것이 지시가 요구한 「계보 단위 라벨링」이 **왜** 필요한지의 두 번째 실증이다.
>
> ##### ⑥ 🟢 내가 검증하지 않고 쓴 판정 2건을 실측으로 닫았다
>
> 초판 §4 는 `MBRFEE_PRCS_STAT_CD`·`PSTMTR_PRCS_STAT_CD` 를 「분류 X」로 단정했는데
> **그 두 모델을 읽지 않은 상태**였다(`DEC-25` 문안에서 추론). 🔴 `R1-3-7` 「대체 근거 금지」 위반 소지다.
> 🟢 이번에 전량 독해해 확증했다 — `CRM_PAYMENT_BILLING.sql` **28행** · `CRM_SEND_REQUEST.sql` **6-7행**이
> 서로를 명시 참조하며 *"실측 도메인 `0,1` vs `R,F,S` 로 완전 분리"* 라고 적는다.
> 🔴 **결론이 맞았다는 것이 근거가 있었다는 뜻은 아니다** — 근거를 확보한 지금 비로소 인용 가능해졌다.
>
> ##### ⑦ 🟠 판정식 정밀화가 필요한 경계 2건을 발견해 상신했다 (착수표 ㉛)
>
> 두 건 다 「로직 0」 축으로는 **P** 인데 개명하면 **구조 계약이 깨진다**:
> · **`CRM_MEMBER.JOIN_DT`** ← `FRST_REGIST_DT` — 2-way `UNION ALL` 이지만 **양 분기가 같은 원천명**이다.
>   ⇒ 🔴 **32-A 의 「`union all` 이면 L」이 너무 거칠다**는 뜻이다. 정밀화 제안 =
>   **「그 `union all` 로 원천명이 유일하지 않게 되는가」**로 조건을 좁힌다.
>   🟢 근거 = **코드베이스가 후자를 P 로 다룬다** — `CRM_MEMBER` 의 `MBER_DIV_CD`·`SEX`·`HMPG_ID`·
>   `REGIST_DEPT_CD` 는 두 분기 동일 원천명이고 **원천명을 유지**한다(부재 목록에 없다).
>   🟢 반대 사례도 확증했다 — `CRM_EVENT.STRT_DE` ← `STRT_DATE`/`CRMN_STRT_DE`(분기마다 다름) ⇒ **L 이 옳다**.
> · **`CRM_MEMBER_STATUS_HIST.EFFECTIVE_FROM`** ← `FRST_REGIST_DT` — 로직 0 이지만 **SCD2 쌍의 한쪽**이고
>   짝인 `EFFECTIVE_TO` 는 `LEAD()` 계산이라 **L** 이다 ⇒ 한쪽만 복원하면 **읽을 수 없는 쌍**이 되고
>   `07_메타.md` §4-1 의 문서화된 시점조인 계약이 깨진다. 🟢 제안 = **「쌍 규약」 예외 명문화 + L 유지**.
> 🔴 **미결 상태로 개명하지 않았다.**
>
> ##### ⑧ 🟢 「적발만 하고 안 고쳤다」를 실제로 고쳤다 (`P154` 축)
>
> 초판 자기검토가 「C. `92_실측필요_후속작업` 등재 근거가 stale — 적발만 했다」로 남겼던 것을 **이번에 시정**했다:
> 원장 §0 유형 등재표의 그 행에서 「**이 계정은 문서작업용**이라」를 **취소선 + 실측 병기**로 갱신했다
> (계정 `KA98941` 에 `GN_DW` 13스키마 · SILVER 948 · BRONZE 1,153 · GOLD 1,320 컬럼 실재 ·
> O123 이 이 계정에서 판정 SQL 을 실제로 돌려 분모 160 을 확정했다).
> 🔴 **「이 문서의 존재 이유는 「계정이 비었다」가 아니라 「그 시점에 라이브 대조를 못 했다」」**로 재정의했다.
> 🟠 잔여 = 항목별 재측정(착수표 ㉚ 를 「부분완료」로 갱신).
>
> ##### ⑨ 🟢 게이트가 나를 두 번 막았고 두 번 다 정당했다
>
> · **`gate_census` rc=1** = 내가 신설한 `o123_member_dk_footprint.py` 가 **미분류**였다.
>   🟢 O121-B 가 만든 「미분류는 FAIL」 계약이 **신설 도구를 곧바로 잡았다** ⇒ `OBSERVE` 에 등재(판정 아님 명시).
> · **`split_doc --republish` rc=1** = `30_설계` 조각-012 가 **307줄**로 300줄 상한 초과.
>   🟢 승인받은 재균형으로 해소(12→13 조각 · max 27,106 B · 최소 여유 13,854 B · 토큰 유실 0).
> · **`doc_heading_gate` rc=1 · 2회** = 절 개칭(§3·§4 「미착수」→「완료」 · AAAA3~6 실측 갱신).
>   🟢 두 번 다 **grep 으로 실유실 0 을 실증한 뒤** `--reason` 에 그 근거를 적어 발행했다(이력 59회).
> 🔴 **세 건 다 「게이트가 늦게 돌았다」가 아니라 「편집 직후 돌아서 잡혔다」** — `R1-7-8` 의 정상 동작이다.
>
> ##### ⑩ 🟢 신설·시정 산출물
>
> · **신설** = `scripts/o123_member_dk_footprint.py`(소비처 footprint · **축 분리** = archive/현행/코드/문서 ·
>   토큰 인자화 → 개명 착수 시 각 컬럼명으로 재사용) · `OBSERVE` 등재.
> · **시정** = `scripts/o122_name_drift.sql`(`UNSOURCED` 축 3곳 + 순서 주석) ·
>   `scripts/gate_census.py`(등재) · `20_issue/32_컬럼개명표.md`(§1·§3·§4·§5·§6·§7·§8 전면) ·
>   `DEC-46` §32-F(요약 갱신) · 원장 §0 `92_…` 행 · 착수표 ㉘(종결)·㉙·㉚·**㉛ 신설** · 인수인계 AAAA3~6.
>
> ##### ⑪ 하지 않은 것 — 🔴 범위를 지켰다
>
> · **`ALTER TABLE … RENAME COLUMN` 0건 · 라이브 DDL/DML 0건**(라이브는 **메타데이터 조회만**).
> · **`dbt parse`/`compile`/`build` 0건**(`R4-1`).
> · **판정 경계 2건 미결**(사용자 확인 대기 · 착수표 ㉛) · **GA4 88컬럼 미착수**(결정 ④) ·
>   **`UNSOURCED` 10컬럼 미판정**(원천 입고 후) · **소비처 grep 미실시**(도구만 신설).
> · **`table_ddl_column_gate` SILVER 축 미신설**(진단만 · 착수표 ㉙).
> · **4-D 관측(L 끼리 `_NM`/`_NAME` 혼용)은 판정하지 않았다** — `DEC-46` 범위 밖이라 명시하고 관측만 남겼다.
>
> ##### ⑫ read 미반환 · 독해 기록
>
> **미반환 0건 · 재호출 0건**(판정식 = 재호출 ≥ 미반환 ⇒ 충족).
> 이번 턴 전량 독해 = **모델 23개 + 매크로 1**. 고유 토큰 표본(`R1-3-7-b`):
> · `AGENCY_AD_ROW_DGT.sql` 1-75 · `원천 무손실: BRONZE 36컬럼을 이름 그대로 보존`
> · `AGENCY_AD_DIGITAL.sql` 1-35 · `DEC-9(_SRC 전량 보존, 비가산)` / `VTR_SRC 는 base 가 원천에 없어`
> · `AGENCY_AD_BROADCAST.sql` 1-92 · `TRY_TO_TIME('30000000')` 이 실패하지 않고
> · `AGENCY_AD_PERFORMANCE.sql` 1-104 · `GA_CONV_MEMBERS 의 28.60%`
> · `AGENCY_AD_BROADCAST_CASE.sql` 1-46 · `원천 5속성 × 3반복 = 15컬럼을 CASE_SEQ 축으로 정규화`
> · `AGENCY_AD_CREATIVE.sql` 1-27 · `3소스 매체/소재 DISTINCT` · `AGENCY_AD_ROW_REBRDC/VIDEO` 전량
> · `CRM_MEMBER.sql` 1-77 · `CM013 = 국내/외국인 × 성별 + 법인유형 복합축`
> · `CRM_MEMBER_DEV.sql` 1-125 · `crm_member_dev_full_reload` / `s._LOAD_DT                       AS SRC_LOAD_DT`
> · `CRM_CAMPAIGN.sql` 1-88 · `O101` BRONZE 재스캔 대신 base CTE 재사용
> · `CRM_SEND_MEMBER.sql` 1-92 · `MS056` 의 단자리 등재는 `1`·`2` 뿐이다
> · `CRM_EVENT.sql` 1-47 · `CRMN_'||CRMN_CD` · `CRM_EVENT_PARTICIPATION.sql` 1-70 · `PRTCPNT_KEY`
> · `CRM_PAYMENT_BILLING.sql` 1-53 · `MBRFEE 접두` · `CRM_SEND_REQUEST.sql` 1-40 · `PSTMTR 접두`
> · `CRM_MEMBER_STATUS_HIST.sql` 1-22 · `LEAD 기반 EFFECTIVE_TO/IS_CURRENT`
> · `CRM_BIZ_TARGET.sql` 1-21 · `WHERE 1=0` · `CRM_MEMBER_SPONSOR_SPAN.sql` 1-58 · `month_key_clamp`
> · `CRM_SEND_RESULT`·`CRM_RELATION_ACTIVITY`·`CRM_MEMBER_AMT_CHANGE`·`CRM_MEMBER_DISCONTINUE` 전량
> · `macros/silver_helpers.sql` 5-7 · `clean_str` = `NULLIF(NULLIF(NULLIF(TRIM(col),''),'NULL'),'-')`
>
> ##### ⑬ 게이트 종료 상태 (🔴 전부 파이프 없이 rc 수신)
>
> `gate_census.py --run-tests` **rc=0**(음성 테스트 22종 전건 rc=0 · `NEEDS_ARGS` 3종 rc=2 · 미분류 0).
> 개별 = `doc_census`·`doc_type_gate`·`index_row_gate`·`doc_heading_gate`·`doc_coord_gate`·
> `clause_order_gate`·`doc_line_length_gate`·`id_collision_gate`·`decision_closure_gate`·
> **`table_ddl_column_gate`** 전건 **rc=0**.
>
> ##### ⑭ 🔴 자기검토 — 이 턴 이후에도 남는 것
>
> · **A. 개명 12건의 소비처 grep 을 아직 안 했다.** 도구는 만들었지만 **12개 컬럼에 대해 돌리지 않았다**
>   ⇒ 「선행조건을 진단했다」와 「선행조건을 충족했다」는 다르다.
> · **B. GOLD WIDE 93건은 끝까지 재현하지 못했다.** `DEC-12` 의 「평탄화 접두」 정의를 읽지 않고
>   후보 축 3개만 시도했다 ⇒ **정의를 읽어야 재현 여부를 말할 수 있다**(결정 ① 이 개명 0건이라 우선순위는 낮다).
> · **C. `_SRC` 7종의 「DW 재계산 measure 가 실재하는가」를 라이브로 확인하지 않았다.**
>   모델 헤더의 선언(*"SV 는 base measure 로 별도 재계산"*)을 근거로 X 로 판정했다 ⇒
>   충돌 상대가 **아직 없다면** `VTR_SRC` 처럼 성격이 다를 수 있다. 🟢 다만 `DEC-9` 가 **전량 보존**을
>   규정하므로 판정 결과(유지)는 어느 쪽이든 같다.
> · **D. 「25/25 완료」는 계보 축이고 컬럼 축은 160/169 다.** `UNSOURCED` 10 · GA4 88 은 **판정 밖**이다
>   ⇒ 「전건 완료」로 읽지 마라.
> · **E. 8-A 정밀화 제안은 내가 만든 규약 개정 제안이다** — 사용자 승인 전에 적용하지 않았고,
>   승인되면 **160건을 그 기준으로 재검**해야 한다(경계 2건만 바뀌는 것이 아닐 수 있다).

> #### 🟢 [2026-08-30 O123-C] 30_output_share 최신화(17종 재생성) + gate_census 오분류 2건·계정 하드코딩 1건 시정
>
> 🔴 **등재 경위** — 이 항목은 **O123-C 가 스스로 쓰지 못했다.** 다음 세션(**O123-D**)이
> 인수인계 프롬프트를 유일한 운반체로 받아 **파일·산출물에서 전건 재실측한 뒤** 소급 등재했다.
> 근거철 = **`_o123c_evidence.md`**(워크스페이스 **루트** · 판정보다 먼저 기록 · `R1-3-7-c`).
> ⚠️ 🔴🔴 **[2026-08-30 O124-B 자기정정] O124 가 이 인용을 「파일 부재」로 잘못 단정했다.**
> 원인 = `ls 20_issue/_o123c_evidence.md` **한 경로만** 보고 「부재」로 결론했다. 실제 경로는
> **루트**이고 파일은 **11,600 B · 166줄 · `## E0`~`## E11` 12절**로 실재한다(O124-B 재실측).
> 🔴 **판정식 = 경로를 한정한 검사로 「없다」를 단정하지 마라 — 그것은 「못 찾았다」다**(`P132` · `O111 ㉠`).
> 🟢 이 인용은 **유효하다.**
> 🔴 **프롬프트 서술을 정본으로 쓰지 않았다**(`R1-3-7` 대체 근거 금지) ⇒ **4건이 정정됐다**(§O123-D).
>
> **지시** = 「`_archive` 로 옮기고 오늘까지 기준으로 최신화」.
>
> ### 1. 🟢 산출물 최신화 — 아카이브 23종 · 재생성 17종 · 미생산 5종
>
> `30_output_share/_archive/20260830/` 신설 후 **23종 복사** · SHA256 전건 동일 검증.
> 🔴 **[O123-D 한계 명시] 그 「전건 동일」은 O123-C 의 자기신고다** — 소급 재현은 **5/23**만 가능했다
> (루트 == 아카이브인 미생산 5종). 나머지 **18종은 루트가 재생성돼 복사 시점 동일성을 원리적으로
> 재현할 수 없다.** 🔴 이는 ⑥ 「라이브 0건」과 **같은 성격**인데 O123-D 초판은 ⑥ 만 한계로 적고
> 이 문장은 **사실처럼 적었다** ⇒ 🟢 판정식 = **한계를 하나 발견하면 같은 축의 형제 문장을 전수로 찾아라**
> (`R3-9 ㉡` 의 문서판 · 한 곳만 고치면 나머지가 더 신뢰받는 거짓이 된다).
> 🔴 **승인 방식 = 「복사 후 제자리 덮어쓰기」** ⇒ **산출물 `rm` 0건**(공백 위험 0).
> 🔴 **그 방식을 택한 이유가 기술적이다** — `scripts/gen_silver_gold_retention.py:28` 이
> `PREV = os.environ.get("GN_DW_PREV", os.path.join(WS, "30_output_share"))` 로 **이전 판본을
> 델타 입력으로 읽는다**(`:89` `ppath = os.path.join(PREV, BASENAME + ".csv")`)
> ⇒ 원본을 먼저 지우면 08 의 「변동」 열이 근거를 잃는다.
> · 정리 = `_probe.bin`·`_probe.txt` 삭제(`R1-7-7` 절차) · `_o122_alias_census.csv` 아카이브 이동.
> · **재생성 17종** = `03` · `04{md,csv,xlsx}` · `05{md,csv,xlsx}` · `06{md,csv,xlsx}` · `07` ·
>   `08{md,csv}` · `09{md,csv}` · `09_섹션배너.json` · `20260826_아키텍처 지도 요약본.html`.
> · 선행 입력 = `dump_schema.py` → `/tmp/schema.json` · `census_columns.py` → `/tmp/census.json`.
> · 🟠 **미생산 5종**(해시 불변 · mtime 08:50) = `01_DW_현업활용가이드.md` · `10_원천입고_결손요약.md` ·
>   `12`·`13` 미해결이슈 쿼리 `.sql` · `미해결이슈_요약_O102.md` — **제네레이터가 없다.**
> 🔴 **`01`·`10` 은 이미 stale 이다** — 헤더가 `companion:` 으로 선언한 04·05·03 을 이 세션이 갱신했다.
>
> ### 2. 🔴🔴 gate_census 오분류 2건 시정 — 재발 축이다
>
> `gen_column_mapping` 과 `run_bronze_audit_host` 가 **`MUTATES`(실행 금지)로 등재돼 있어
> 04·06 을 재생성할 수 없었다.** 실측 = 접속 참조 **0** · DDL/DML 키워드 **0**
> (`ALTER VIEW` 는 `post_hook` 을 **파싱하는 대상 문자열**이다) ⇒ **`GEN` 으로 이동**.
> 현재 상태 = `[GEN] 12개` 안에 두 도구가 있다(`gen_column_mapping` = 「라이브 접속 0(모델 파싱 전용)」 ·
> `run_bronze_audit_host` = 「조회 전용(무인자 = 직접조회)」).
> 🔴 **O121-B 가 `audit_ddl_rule7`·`sv_unit_gate`·`table_ddl_column_gate` 에서 고친 것과 같은 유형**이다
> (그 3종은 현재 `[JUDGE]` 에 「DDL 파일을 **읽는다**」·「라이브를 **읽는다**」로 있다)
> ⇒ 🔴 **판정식 = 「DDL 문자열을 입력으로 읽는 도구」를 「DDL 을 발행하는 도구」로 오분류하는 축이
> 3건 → 2건으로 재발했다.** 분류 근거는 **키워드 등장이 아니라 그 키워드의 역할**(입력이냐 출력이냐)이다.
>
> ### 3. 🔴 gen_arch_map.py 계정 하드코딩 시정 (`P169` 축)
>
> `account="zl50263.ap-northeast-2.aws"` · `user="CHOIH"` · `role="GN_DW_ADMIN"` ·
> `warehouse="GN_DW_DEV_WH"` 가 박혀 있어 **403 Forbidden** — **이 제네레이터만 실패한 유일한 원인**이었다.
> ⇒ `scripts/sfconn.py` 경유로 교체(`:40-41` `import sfconn` / `return sfconn.conn()`).
> 🟢 **판정식 = 접속 정보는 도구마다 적지 말고 단일 경유점에서 받는다.**
> ⚠️ 같은 파일 `:735` 의 `account=account,` 는 **접속이 아니라 HTML 표시용 인자**다
> (`:731` `def write_html(path, account, …)`) ⇒ 「하드코딩 잔존」으로 오판하지 말 것.
>
> ### 4. 🔴🔴 `/tmp` 지뢰가 골든 지문을 stale 로 만들었다 — 처방 = 같은 호출 안에서 체인
>
> `/tmp` 가 **세션 중간에 비워진다**(13:36 생성한 `/tmp/schema.json` 이 14:18 부재).
> 그래서 `test_generators` 골든의 **스키마 지문이 구 값으로 보전**됐다
> (🟢 O111-B 가드가 **지우지는 않았다** — 보전한 것이 오히려 어긋남을 남겼다).
> ⇒ `dump_schema` + 골든 발행을 **한 셸 호출 안에서 체인**해 정정
> (`3581c638b4260bd9` → **`12ffa5c63c8ce470`** · **산출물 수치 차이 0건**).
> 🟢 **판정식 = `/tmp` 산출물에 의존하는 도구는 생성과 소비를 같은 호출에 묶어라.**
> 🔴 **[O123-D 정정] 이것은 「환경 지뢰 신규」가 아니다** — `/tmp` 소실은 `99_NEXT_SESSION-024.md:56`
> 에 **이미 등재돼 있었다**. 신규인 것은 **그 지뢰의 결과(골든 지문)와 체인 처방**이다.
>
> ### 5. 🟢 골든 재발행 근거 — 덮기 전 소실 0 실증
>
> 08 행 **536 → 752**(순증 216)인데 키 `(SILVER_TABLE, COLUMN)` **중복 0 · 사라진 키 0 ·
> 신규 키 216 순수 증가**이고, 원인은 **SILVER 테이블 39 → 43**으로 규명된다
> (신규 = `BIGQUERY_REFINED_DATA` · `GA4_BASIC` · `CRM_MEMBER_SPONSOR_SPAN` · `ERP_BUDGET_YEARLY`).
> `SILVER_ONLY_CHAIN` **127 → 292** 급증은 대부분 **GA 축**(`BIGQUERY_REFINED_DATA` +118 ·
> `GA4_BASIC` +46 · `GA4_IDENTITY` +1)이고, O120 이 실증한 **「GA 축이 GOLD 에서 끊긴다」와 정합**한다.
> 🔴 **[O123-D 신설 판정식] 소실 판정의 키는 「식별자」로만 잡아라** — 키에 `STATUS` 를 넣어 3열로 재면
> **「사라진 키 10 · 신규 226」** 이 나오는데 그 10건은 소실이 아니라 **상태 전이**다
> (`REFERENCED` 238→291 · `DROPPED` 143→141). **판정값을 키에 넣으면 전이가 소실로 보인다.**
>
> ### 6. 🔴 라이브 변경 범위
>
> **라이브 DDL/DML 0건 · dbt 0건 · 개명 실행 0건**(O123-C 자기신고).
> 🔴 **[O123-D 한계 명시] 이 값은 사후 재현이 불가하다**(`R2-8-4-c`) — 과거 시점의 부작용 부재는
> 파일로 증명되지 않는다. 🟢 파일 축으로 확인된 것 = 개명 확정 12건은 미실행이고
> 완료 판정식은 여전히 `INFORMATION_SCHEMA.COLUMNS` 구 컬럼명 0건이다(`§0-AAAA` AAAA5).
>
> ### 7. 🟠 미결 — 골든 2회 발행이 `UNLABELED` 다
>
> `scripts/golden/outputs.json` 발행 이력 `:13`·`:19` 가 `"label": "UNLABELED"`
> (대조군 `:7` = `"label": "O111-B"`).
> 🔴 **[O123-D 정정] 도구는 `--label` 을 이미 지원한다** —
> `test_generators.py:746` `def update_golden(reason=None, label=None)` · CLI `--label` ·
> `snapshot_util.resolve_label()`(인자 → `SESSION_LABEL` → `UNLABELED`).
> ⇒ **시정 대상은 도구가 아니라 절차다.** `--label` 도 `SESSION_LABEL` 도 주지 않았다
> ⇒ 🔴 이는 `§0-AAAA:49` 의 `export SESSION_LABEL=O1NN 을 착수 직후 먼저 하라`(`R1-7-10`) **미이행**이다.
> 🟢 그 발행의 `--reason` 은 충실했다(`/tmp` 소실 경위·구 지문·수치 차이 0 을 전부 적었다)
> ⇒ **사유는 지켰고 라벨은 놓쳤다.**
