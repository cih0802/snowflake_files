<!-- SPLIT-CHUNK 01_세션이력.md | 060/060 | 허브 = 01_세션이력.md | 원문 1~60행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## 🟢 [2026-09-08 O144-D] 산출물 15·16·17 파생 무결성 검증 + 11 재생성 — 창작 식별자 3개·닫힘 오판 1건·양쪽 누락 4절 봉합

**요청**: `30_output_share/11_미해결이슈_요약.md`·`15_문서 통합.md` 를 이번 세션 작업 기반으로 최신화하고, `16_파이프라인 배선 수정.md`(닫힘)·`17_9월3일기준 현업요청 확정.md`(열림)가 갱신된 15 를 토대로 올바르게 갈렸는지 확인.

### ▣ 1. 처리

| 문서 | 성격 | 조치 |
|---|---|---|
| `11_미해결이슈_요약.md` | 자동생성물 | `gen_unresolved_issue_summary.py --write` 재실행 ⇒ **dbt 18→9 · 착수표 6→5** (O144-C 결과 반영) · `00_BRIEF.md` 실측과 일치 |
| `15_문서 통합.md` | 수기 정본(16·17 의 분모) | 파생 규약 블록 신설 · ⑭ 라이브 실측 · K-2·H-3 닫힘 병기 · 누락 4절 표시 · **§8 파생 무결성 검증 결과표 신설** |
| `16_파이프라인 배선 수정.md` | 닫힘 정본 | 닫힘 판정식(㉠ 결정 확정 **AND** ㉡ 라이브 실효) 신설 · §3-3 인용 시정 · **§4-1 을 4-1/4-2 로 분리하고 §4-2 닫힘 취소** |
| `17_9월3일기준 현업요청 확정.md` | 열림 정본 | 범위 판정표 신설 · **§2-3~§2-6 4개 절 신설** · §1-3 에 중단 축 차단 실측·동시중단 귀속 선택지 보강 |

### ▣ 2. 🔴🔴 먼저 알아라 — 이 세션이 남긴 판정식

**㉠ 「배선 완료」는 문서 기재로 판정하면 반드시 틀린다 — 라이브 식별자 실재 + 채움률로 판정하라.**
`16` 이 🟢 로 닫아 둔 항목을 `INFORMATION_SCHEMA` 로 되받아 세자 **라이브에 없는 식별자 3개**가 나왔다:
`ACQUISITION_CAMPAIGN_SK` · `STOP_CAMPAIGN_SK` · `FACT_BUDGET_DEPARTMENT.DEPT_CD`.
🔴 **「🟢 배선 완료」 표기가 붙으면 아무도 다시 조회하지 않는다** ⇒ 초록 표기일수록 되받아 세야 한다.

**㉡ 「컬럼이 있다」와 「값이 있다」는 다른 축이다(`R3-2` 의 산출물판).**
`FACT_MEMBER_EVENT` 채움률 실측 = `STOP` 1,038,262행이 `CAMPAIGN_SK`·`SPONSORSHIP_SK` **둘 다 전건 `0`**,
`DEV` 3,594,843행은 캠페인 3,594,825건·사업 전건 채움.
⇒ 컬럼은 있고 값은 비어 있다. **집계는 성공하고 답은 빈다** ⇒ 이것을 「제공 완료」로 적으면 오독을 만든다.
🔴 착수표 ⑭ 「현업 결정 전 배선 금지」 정지점이 살아 있으므로 **닫힘이 아니라 열림**이다.

**㉢ 파생 문서는 「닫힘·열림」 두 갈래로 갈리므로 양쪽 누락을 세는 축이 따로 필요하다.**
분모(15)에는 있는데 16·17 **어디에도 없는** 절 4개가 조용히 사라져 있었다
(`D/SVL-1~4` · `A` 회비월 · `B, E` 마스터 부재 외래키 · `D` 원천 100% NULL).
🔴 각 파생 문서만 따로 보면 이 유형은 **영구히 보이지 않는다** — 반드시 분모 대조로만 잡힌다.

### ▣ 3. 검증 결과 (라이브 실측)

| 축 | 판정 |
|---|---|
| `16` 닫힘 9절 | 🟢 **7절 닫힘 맞다**(`N-10`·`N-11`·`N-8`·`N-12`·`H-3`·`K-2`·`DEC-50`) · 🟠 1절 인용 시정(㊶) · 🔴 1절 닫힘 취소(`L-2`) |
| `17` 열림 12절 | 🟢 **전건 열림 맞다** — 🟢 해소된 `N-8`·`N-10`·`N-11`·`N-12`·`M-3` 는 17 에 없음 ⇒ 역방향 오염 0건 |
| `H-3` 배선 | 🟢 `DIM_MEMBER` 에 `SEX`(CM013 raw)·`SEX_NM`·`GENDER_NAME`(CM017) **3축 동시 실재** |
| `K-2` 배선 | 🟢 GOLD 인원·횟수 계열 + `SV_EVENT_PARTICIPATION` 의 `DISTINCT_PARTICIPANTS` ↔ `TOTAL_PARTICIPATE_CNT` 이원화 실재 |
| `L-1 부속` D+5 | 🟢 `FACT_SERVICE_EVENT`·`WIDE_SERVICE_EVENT` `D5_*` 8종×2 실재 (채널별 차등은 미확정) |
| 획득 축 | 🟢 `DIM_MEMBER_ACQUISITION` 1,585,949행 · `ACQ_CAMPAIGN_SK` 1,585,945 채움 · `ACQ_SPONSORSHIP_SK` 전건 채움 |

### ▣ 4. 곁가지 발견과 처분

* 🟢 **`20_issue/32_컬럼개명표.md:276~277` 표 결손 시정** — 헤더 10열인데 본문이 11셀이라 **렌더러가 11번째 셀을 잘라 버려** 「원천 입고 후 재판정(4-A)」·「결정 ④ · `DEC-39 ㉠`」 메모가 보이지 않았다. 문안을 보존한 채 구조만 고쳤다(창작 0). 이 세션 편집이 아닌 **기존 결함**이며 `doc_line_length_gate` FAIL 의 실제 원인이었다.
* 🔴 **미조치로 남긴 것(의도적)**: `FACT_MEMBER_EVENT.SPONSORSHIP_SK` 의 COMMENT 는 「후원사업 (FK→DIM_SPONSORSHIP)」뿐이고 **전건 0 센티넬 사유가 발행돼 있지 않다.** 분석가가 조회하면 빈 값의 이유를 알 수 없다. 🔴 그러나 착수표 ⑭ 정지점이 살아 있어 FME 메타데이터를 건드리지 않았다 ⇒ **다음 세션 후보**로 남긴다(COMMENT 발행은 배선이 아니라 현재 차단 상태의 문서화라는 판단은 사람이 확정할 일이다).
* 🔴 **원장 표기 괴리(미조치)**: `H-3`·`K-2` 는 배선이 닫혔는데 문서20 절 상태 표기가 없어 `11_미해결이슈_요약.md` 가 「⚪ 표기 없음」으로 센다. 15·16 에 괴리 사실을 병기했고 **문서20 은 고치지 않았다**(현업 회신 문서의 상태 표기 변경은 판정 주체가 다르다).

### ▣ 5. 게이트

| 게이트 | 결과 |
|---|---|
| `doc_line_length_gate` | 🔴 FAIL(32_컬럼개명표 표 결손) ➔ 시정 후 **3회 연속 rc=0**(`O112 ㉡` 안정 확인) |
| `doc_census`·`doc_heading_gate`·`index_row_gate`·`clause_order_gate`·`doc_coord_gate`·`doc_type_gate` | 🟢 전건 rc=0 |
| `gate_census.py --run-tests` | 🟢 대상 전건 rc=0 · `NEEDS_ARGS` 전건 rc=2 · **PASS — 미분류 0 · 유령 0 · 중복 0** |
| `decision_closure_gate` | 🟢 미봉합 인용처 0건 |
| `session_brief.py --write` | 🟢 rc=0 — 열린 착수표 5 · 문서50 열린 절 9 · stale 0 |

* 🟢 일회성 검증 스크립트 5종은 `scripts/_archive/` 로 보관(`gate_census` 미분류 0 유지).

## 🟢 [2026-09-08 O144-E] 문서17 실증 쿼리 전량 실행 검증 — 기재 6건 반증·시정 + 보강 쿼리 17종 신설 + 전 쿼리 의미 주석

**요청**: 문서17 각 항목에 첨부된 쿼리를 실행해 보고, 근거가 충분한지 검토한 뒤 부족한 항목에 쿼리를 추가하고 각 쿼리의 의미를 주석으로 설명.

### ▣ 1. 실행 결과

| 축 | 값 |
|---|---|
| 갱신 전 추출·실행 문장 | 25개 ⇒ 🟢 결과있음 24 · 🟠 0행 1 · 🔴 실패 0 |
| 갱신 후 최종 실행 문장 | **42개**(보강 17종 신설) ⇒ 🟢 41 · 🟠 0행 1 · 🔴 **실패 0** |
| 원천 분모 | 문서17 참조 BRONZE 테이블 **29개 전건 실재**(BRONZE_CRM 46 · AGENCY 4 · ERP 2 · GA4 2 · GSC 2) ⇒ 「0건」이 의미를 갖는 상태 확보 후 착수 |

### ▣ 2. 🔴🔴 먼저 알아라 — 이 세션이 남긴 판정식

**㉠ 「이상치」라고 부르기 전에 반대쪽 마스터를 대조하라 — 가장 위험했던 오류다.**
문서17 §2-1 은 `S+8자리` 회원번호 **1,016건**을 「마스터에 없는 비표준」으로 규정하고 **삭제 승인**을 요청하려 했다.
실측 = **1,631행 / 1,016종**이고 그중 **997종이 일시회원 마스터(`TM_MM_ONCE_MBER_INFO`)에 정상 매칭**된다. 진짜 미매칭은 **19종**뿐.
`S+8자리` 는 **일시회원의 정규 회원번호 체계**였다.
🔴 **이대로 요청했다면 정상 회원의 참여기록을 지웠다.** 한 마스터에 없다는 사실은 **다른 마스터에 있다**는 가능성을 배제하지 않는다.
⇒ **삭제·필터링을 요청하는 문서는 매칭률을 먼저 세는 쿼리를 반드시 첨부한다.**

**㉡ `NOT IN (서브쿼리)` 를 근거 산출에 쓰지 마라.**
문서가 「미등재 실패코드 **17종**」이라 적었으나 `NOT EXISTS` 로 다시 세니 **5종**(505,541행)이었다.
`NOT IN` 은 서브쿼리에 NULL 이 하나라도 섞이면 비교가 UNKNOWN 이 되어 **행이 조용히 사라진다.**
⇒ 사전 `MS056~MS059` 는 **146개 코드**를 보유하므로 「사전이 부실」이 아니라 **이 5종만 누락**이었다(요청 문안 자체가 달라진다).

**㉢ 「제안한 보정식」도 실행해 봐야 한다 — 식이 틀릴 수 있다.**
문서는 회비월 5자리에 **`LPAD` 보정 승인**을 요청하려 했다. 그런데 `LPAD('20251',6,'0')` = **`020251`** 로 **연도가 0202년**이 된다.
올바른 식은 `LEFT(4)‖LPAD(RIGHT(1),2,'0')` = `202501` 이다.
⇒ **잘못된 식을 현업 승인받는 사고**를 실행 검증이 막았다. (병합 대상 `202501` 에는 이미 882,909행 존재 ⇒ 키 충돌이 아니라 정상 합산)

**㉣ 「0건」 근거에는 대조군을 붙여라.**
§4-2 `F-5` 캘린더 부재는 0행이 정답인 유형인데, 0행은 **「쿼리가 틀렸다」와 구별되지 않는다.**
⇒ 같은 조건으로 BRONZE 테이블 총수(**56**)를 함께 내는 대조군 쿼리를 붙여 **조건은 살아 있고 대상이 없음**을 증명하게 했다.

### ▣ 3. 실측과 달라 시정한 기재 6건

| 절 | 종전 기재 | 실측 |
|---|---|---|
| §2-1 `I-2` | `S+8자리` 미매칭 **1,016건** | 1,631행/1,016종 · **997종 정상 매칭** · 미매칭 19종 |
| §2-4 `A` | `LPAD` 보정 | 🔴 `LPAD`→`020251` 무의미 · 올바른 식 `→202501` |
| §2-2 `M-5` | 미등재 **17종** | **5종**(`7320` 291,869 · `7319` 208,713 · `9034` 2,793 · `7321` 1,893 · `7205` 273) · 전체 33종 · 사전 146코드 |
| §2-5 `B, E` | 캠페인행사 **전건 100% 미매칭** | 고아 **52행(0.03%)** · 마스터 3,410행 실재 ⇒ 사실상 해소 |
| §3-2 `F-2` | 후원사업 **4그룹** | 약어코드 **6종**(1:17·2:1·3:6·4:3·5:21·6:2) |
| §3-2 `F-1` | 혼재 `LVL=2~4` | **레벨 2~5** · `STATS_DEPT_LVL` 결측 1,306/1,314(**99.4%**) |

### ▣ 4. 🟢 실행으로 **새로 확보한** 근거 (질문에 답이 생긴 것)

| 절 | 새 근거 |
|---|---|
| §1-2 `L-1 부속` | 🟢 **채널별 차등의 실증** — 이메일 D+5 누적 **64.8%** vs 알림톡 **77.3%** · 평균 간격 **41.5일 vs 9.8일** ⇒ 일괄 D+5 면 이메일 반응 35% 누락 |
| §1-3 `L-2` | 🟢 **동시중단 팬아웃 규모** — 사업 1개 **95.61%**(1,128,656) · 2개 이상 **4.39%(51,859 사건)** · 최대 12개 ⇒ **⑭ 결정의 영향 범위 확정** |
| §5-2 `J-2` | 🟢 **8자리 초수 = 마이크로초 입증** — `60000000÷1,000,000=60초` 이고 시분초 최다값 `00:01:00`(60초)과 1:1 대응(30·90초도 일치) ⇒ 질문이 「30초인가?」에서 「÷1,000,000 채택 승인」으로 바뀜 |
| §5-3 `N-9` | 🔴 **동결이 실효하지 않는다** — `ACQ_CAMPAIGN_NAME` ≠ 현재명 인 행이 **0 / 1,585,949**. 마스터는 최근 3년 **14,336건** 수정됨 ⇒ 「동결 컬럼」이 실시간 조인과 구별되지 않는다 |
| §3-1 `N-1~N-4` | 🔴 개발구분 `증액`(5,844행)·`지정교체`(5,832행)의 **목표값 입력 행이 36개·12개** ⇒ 달성률 분모가 성립하지 않는 **빈 껍데기** |
| §2-5 `B, E` | 🔴 고아 = **종수 8.8%(30/341) / 행수 26.79%** ⇒ 대형 행사 누락 · 연도별 2025년 36.7% → 2026년 **49.9%** 급증 · 마스터 **376행뿐** ⇒ 「기간 누락」이 아니라 **증분 적재 미추종** |

### ▣ 5. 보강 쿼리 17종 신설 · 전 쿼리 의미 주석

* 🟢 **판별력 없는 쿼리 1종 교체**: §2-3 `D/SVL` 은 `dtl_cd_id` 단독 조인이라 무관 코드군(`PM019 휴대폰결과`·`CM021 직군코드`·`CM008 홍보방법`)까지 붙어 후보를 좁히지 못했다 ⇒ **「원천 값집합 ⊆ 코드군 도메인」 완전포함 판정식**으로 교체.
* 🟢 **신설 목록**: `1-1-B`(행정통보 비중 집계) · `1-2-B`(채널별 누적) · `1-3-B`(불일치율 직접 계산) · `1-3-C`(팬아웃 규모) · `2-2-B`(S형식 마스터 매칭) · `2-4-B`(사전 분모 대조) · `2-6`(코드군 완전포함) · `2-9`(보정식 검증) · `2-12`(연도별 고아 추이) · `2-13`(종수 대비 행수) · `3-1-B`(개발구분 라벨+목표 미설정) · `3-2-B`(STATS_DEPT_LVL 결측률) · `3-3-B`(4그룹 전제 검증) · `4-1-B`(대체 피처 실재) · `4-2-B`(0행 대조군) · `5-4-B`(µs 환산 대조) · `5-5-B`(동결 실효 대조) · `5-5-C`(개칭 활발도).
* 🟢 **전 쿼리에 4단 주석**을 달았다: `▣ 이 쿼리가 답하는 질문` / `▣ 읽는 법` / `▣ 주의(오독 방지)` / (해당 시) `▣ 왜 추가했나`·`▣ 판정 규칙`.
  🔴 특히 **분모가 무엇인지**(예: '중단 전체'가 아니라 '직전 발송이 있는 중단'), **LEFT JOIN 이어야 하는 이유**, **`COUNT(*)` vs `COUNT(컬럼)`** 차이, **문자열 비교의 함정**, **하드코딩 상한의 시효**를 주석으로 명시했다.

### ▣ 6. 곁가지 시정

* 🟢 **내가 추가한 쿼리도 1건 실패했고 즉시 고쳤다** — `1-2-B` 에서 이메일 발송일 컬럼을 `SNDNG_DT` 로 썼으나 실제는 **`SNDNG_DE`** 였다(알림톡만 `SNDNG_DT`). 🔴 주석에 「컬럼명이 다르므로 맞춰 적었다」고 써 놓고 정작 안 맞춘 것이다 ⇒ **주석의 주장도 실행으로 검증해야 한다.**
* 🟢 **문서15(분모) 5건 동기화** — 15 에서 고치지 않으면 같은 오류가 16·17 로 다시 파생되므로 취소선 + 반증 병기로 정리하고 §8-5·§8-6 을 신설했다.

### ▣ 7. 게이트

| 게이트 | 결과 |
|---|---|
| 문서17 쿼리 전량 재실행 | 🟢 42문장 · **실패 0** (0행 1건은 `F-5` 의도된 부재) |
| `doc_line_length_gate`·`doc_census`·`doc_heading_gate`·`index_row_gate`·`clause_order_gate`·`doc_coord_gate`·`doc_type_gate` | 🟢 전건 rc=0 |

* 🟢 일회성 실행·측정 스크립트 5종은 `scripts/_archive/` 로 보관.
