<!-- SPLIT-CHUNK 01_세션이력.md | 046/046 | 허브 = 01_세션이력.md | 원문 1~80행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-29 O116-B] O116 자기검토 — 오진 1건 정정 · 미규명 3건 규명 · 구조결함 1건 시정 · 낡은 문안 6곳 갱신
>
> **범위** = 사용자 지시 「이번 세션 작업을 비판적으로 검토 + 승인 필요 작업 전건 승인 + 개선」.
> 승인 사용 = **재균형 1회**(`99_NEXT` 조각 317줄 상한 초과 해소 · `R4-4-3`). `DEC-44`·`A.7` 은 불변(미착수).
>
> **① 🔴🔴 최대 발견 — `SPNSR_BSNS_ID` 18,091 은 「마스터 미완전」이 아니라 우리 설계 결함이다**
> 첫 회는 이 항목을 「미규명」으로 다음 세션에 넘겼다. 실측하니 **기존 진단 자체가 오진**이었다.
> `CRM_CAMPAIGN.SPNSR_BSNS_ID` 는 **쉼표 다중값 문자열**이다(`4,43,50` · `1,2,3,4,5,6,9,38,39,41,42`).
> · 형태로 완전히 갈린다 = **`SINGLE` 16,613행 고아 0.00%(27종 전부 매칭)** ↔
>   **`MULTI` 18,091행 고아 100.00%(445종 전부 고아)**.
> · 🟢 **결정적 검증** = 쉼표 분해 시 성분 **76,761건 전건 매칭** · 성분 고유 **14종 전부 마스터(50행) 실재** ·
>   **미매칭 0** ⇒ **원천에 결손이 0** 이다.
> ⇒ 🔴 종전 처방 「전량입고 후 error 복귀」는 **영구히 충족되지 않는 조건**이었다(입고와 무관한 사안이다).
> 🟢 처방을 **설계 결정**으로 승격했다 = ① 단일값 부분집합 한정(`where … NOT LIKE '%,%'`)
> ② **캠페인×후원사업 브릿지 정규화**. 🔴 ② 는 소비 계약(캠페인 1행 = 후원사업 N개)을 바꾸므로 사람 결정이다.
> 🔴 `relationships_CRM_PAYMENT_BILLING_SPNSR_BSNS_ID`(1)도 **같은 축인지 확인 대상**으로 남겼다.
>
> **② 🔴 `MK_CMPGN_NM` 7,052 — 「회귀 감지용」 테스트가 실제로 회귀를 잡았다**
> 테스트 정의를 읽자마자 나왔다: 주석이 *"323종, 고아 0"* · *"실측 고아 0 — 회귀 감지용"* 인데
> 라이브는 **7,052**(분모 = 코드 보유 33,956 ⇒ **20.77%**)다.
> 🟢 원인은 **코드 단 1종 = `395`** ⇒ 라벨 체계 붕괴가 아니라 **사전 미등재 1건의 파급**이다
> (`MKTG_UTM` 고아 `192` 와 같은 패턴 ⇒ 같은 현업 질의로 묶었다).
>
> **③ 🟢 `SNDNG_KEY` 11,313 규명 — 채널로 갈린다**
> `PSTMTR` **8,232**(그 채널 1,798,680행의 0.46% · 🔴 **고유 키 11,005 중 7,375 = 67% 가 마스터 부재**) +
> `MSG_AT` **3,081**(고유 83) · 🟢 `SND`·`EMAIL` **고아 0**(합 정확 일치).
> 🔴 시간 축 실측 = 고아 `2024-01-02~2026-06-30` ↔ 매칭 `2024-01-03~2026-07-03` ⇒ **같은 기간에 산발**
> ⇒ **기간 부분적재가 아니다** ⇒ 「전량입고로 해소된다」는 보장이 없다(원천 소관 확인 필요).
>
> **④ 🔴🔴 구조결함 자력 적발·시정 — 근거철을 「정본」으로 선언했다**
> 첫 회는 루트의 `_o116_warn_census.md` 를 「WARN 목록의 정본」이라 선언했다.
> 🔴 그 파일은 **`20_issue/` 밖**이라 `doc_heading_gate`·`doc_line_length_gate`·`doc_census`·`doc_type_gate`
> **어느 분모에도 없다** ⇒ 손상·stale 을 잡아 줄 장치가 0 인 곳에 정본을 둔 셈이다.
> 🟢 시정 = 전수표·규명·처방을 **문서50 §O116**(`§O116-1`~`§O116-5`)으로 이관 ⇒ 게이트 분모 자동 편입.
> 근거철은 **쿼리·중간 관측·기각된 가설**을 담은 부속 근거로 강등하고 파일 선두에 그 사실을 새겼다
> (🔴 지우지 않는다 · `R2-8-3`). 🟢 스킬의 「새 문서보다 기존 문서에 절을 추가」 원칙과도 일치한다.
>
> **⑤ 🔴 낡은 문안 6곳 시정 — 「적발했다」와 「정본을 고쳤다」는 다르다(`P154` 축)**
> 첫 회는 기각·부정한 가설을 **문서에만 적고 코드 주석은 그대로 뒀다** ⇒ 다음 세션이 코드를 읽으면
> 기각된 가설을 다시 믿는다. 시정 내역:
> · 🟢 **`_crm_schema.yml` 헤더 정책**(가장 중요) — 「마스터 전량입고·키체계 확정 후 error 복귀」가
>   **축별로 무효화**됐음을 4축(㉠ MEMBER_DK ㉡ EVENT_KEY ㉢ SPNSR_BSNS_ID ㉣ SNDNG_KEY)으로 명기.
>   🟢 **한 곳을 고쳐 14개 이상 테스트의 근거를 함께 갱신**했다(정책 선언이 개별 주석을 지배한다).
> · `CRM_CAMPAIGN.SPNSR_BSNS_ID`(오진 정정 + 처방 폐기) · `MK_CMPGN_NM`(회귀 실측 병기) ·
>   `CRM_EVENT_PARTICIPATION.EVENT_KEY`(「키체계 미확정」 기각) · `CRM_SEND_MEMBER.SNDNG_KEY`(채널 분해) ·
>   `CRM_SEND_MEMBER.MBER_NO`(전량입고 확인 + 형태 실측) · `CRM_SEND_MEMBER.sql` 축B
>   (「🟢 conformed」 → **🟠 부분 부정** · 「사전에 값이 추가되면 깨진다」 예고의 실현을 병기).
> 🟢 **안전 검증** = `yaml.safe_load` 로 파싱 + 편집한 5개 컬럼의 `tests` 구조를 출력 대조
> ⇒ **주석만 바뀌고 테스트 선언은 무변경**(모델 20종 · 컬럼 테스트 선언 117).
> 🔴 **검증 조건을 인수인계에 남겼다 = 다음 `dbt build` 의 `TOTAL` 이 461 로 유지되는가**
> (바뀌면 내 무변경 단정이 틀린 것이다 · `R4-1` 이므로 실행은 사용자 몫).
>
> **⑥ 🔴 첫 회 판정약점 2건 자력 정정**
> · **「수십 행」은 추정이었다**(`R3-1` 위반 소지) ⇒ 실측 = 한글 인명 오염 **139행 · 고유 93종**
>   (전체 분해 = 숫자만 31,262 / 한글 139 / 기타 85 · 합 31,486 정확).
> · **회차 순서 판정에 실측이 빠져 있었다** — 첫 회는 선두 블록의 `12:45` 스탬프가 채택 회차보다
>   늦어 보이는 것을 확인 없이 넘겼다. 실측 = 선두 `12:44~14:16` / 채택 `03:54~03:59`
>   ⇒ 🔴🔴 **로그에 날짜가 없다(시:분:초뿐)** ⇒ **시각으로 회차 순서를 판정하면 틀린다.**
>   🟢 채택 회차 종료 `03:59:59` 가 O115-C 기재와 정확히 일치 ⇒ 회차 귀속이 **독립 축으로 교차검증**됐다.
>   ⇒ 첫 회 결론은 **옳았지만 근거가 미완**이었다(「성공 신호도 의심하라」의 실물).
>
> **⑦ 🟠 가독성 시정** — 첫 회가 `▣VVV3` 표 셀에 판정 전문(300자+)을 넣어 색인을 정본처럼 만들었다
> ⇒ **요약 + 정본 좌표**로 축약했다(원장 §1 과 같은 계약).
>
> **⑧ 🔴 이 세션의 자기결함 1건 — 도구 출력 표기를 믿고 정상 편집을 되돌리려 했다**
> 원장 §1 에 O116-B 행을 넣을 때 `edit` 의 `Edit location` 표기가 **`lines 30-31`(문서맵 표)** 로 나와
> 「엉뚱한 표를 오염시켰다」고 판정하고 **즉시 되돌리는 편집을 시도**했다.
> 🟢 되돌리기가 문자열 부재로 **실패**한 뒤 재읽으니 행은 **147행 = §1 표의 첫 데이터 행**으로 정상이었다.
> 🔴 **`R1-7-3`(손상 판정 전에 2~3회 재읽어 대조) 위반**이고, 되돌리기가 성공했다면 **정상 행을 지웠을 것**이다.
> ⇒ 교훈 = **`R1-7-3` 은 「파일이 깨졌을 때」만이 아니라 「도구가 이상한 좌표를 보고할 때」에도 적용된다.**
> 🔴 파괴적 되돌리기를 하기 전에 **먼저 `grep` 으로 실제 위치를 확인하라**(1회 호출로 끝난다).
>
> **⑨ 게이트** — `line_len` 🟢(원장 행 **1,704자** · 1,900 이내 한 번에 통과) ·
> `index_row_gate` **행 유실 0 · 중복 증가 0** · 허브 재발행 3건 + 재균형 1건 모두 `--expect`
> **영속성 가드 통과** · 조각 상한 초과 0 · `read` 미반환 **0** · 재호출 **0**.
>
> **⑩ 🔴 남긴 것** = ㉠ **`SPNSR_BSNS_ID` 처방 결정**(문서30 · 최우선) ㉡ 잔여 미규명 6종
> ㉢ 현업 질의 **5건**(`EVENT_105/106` · SND 코드공간 · 한글 오염 · 다중값 의도 · 코드 `395`)
> ㉣ `DEC-44` 회신 대기 ㉤ `A.7` 승인 대기 ㉥ **다음 build 의 `TOTAL`=461 확인**.

> #### 🟢 [2026-08-29 O117] 잔여 미규명 6종 **6/6 종결** · 설계 결정 `DEC-45` 등재 · 낡은 처방 5곳 + 테스트 헤더 시정
>
> **범위** = 사용자 지시 「이번 세션 작업을 비판적으로 검토 + 승인 필요 작업 전건 승인 + 이슈 폴더 참고해 문서 개선」.
> 승인 사용 = **재균형 1회**(문서50 조각 001 이 373줄로 상한 초과 ⇒ 18→**19조각** · 유실 0 · `R4-4-3`).
> `R4-4-2 ㉡` 적용(지시 동봉 ⇒ 브리핑 후 즉시 착수) · 확정위반 **0**.
> 🔒 라벨 = `id_collision_gate --next O` = **O117**(정의 116 · 참조 116 양축 일치) ⇒ 착수 전 원장 §1 선점 등재(`R1-4-3` 7세션 연속).
>
> **① 🟢🟢 잔여 미규명 6종을 6/6 종결했다 — 그런데 「미규명」이라는 라벨 자체가 과대했다**
> 인수인계(`▣WWW3`)와 문서50 §O116-5 는 6종을 「미규명」으로 넘겼다. 방법 ㉠(**테스트 `where`·주석 먼저 읽기**)을
> 이행하자 **6종 전부 「건수」는 이미 `_crm_schema.yml` 주석·테스트 헤더에 적혀 있었다**. 빠져 있던 것은 **분모와 원인**이다.
> 🔴 그 중 **2종은 읽기만 하면 닫히는 것**이었다: `warn_fep_nonnumeric_member_dk` 는 **자기 헤더가 이미 진단**해 뒀고,
> `accepted_values_…PART_STATUS` 는 **`store_failures` 감사 테이블에 답이 라이브로 보관**돼 있었다.
> ⇒ 🟢 **신설 방법 1조** = **`store_failures` 가 붙은 테스트는 분모를 재기 전에 그 감사 테이블을 먼저 조회하라.**
> ⇒ 🔴 **신설 경고 1조** = 「미규명」 표기를 그대로 믿지 말고 **건수/분모/원인 중 무엇이 미규명인지 먼저 갈라라.**
> 정본 = **문서50 §O116-6**(분모·원인 병기 · §O116-5 는 취소선 + 포인터로 종결 표기).
>
> **② 🟢 `RQEST_RST_CD` 1,096 — 커버리지 결손이 아니라 결제수단별 구조적 부재다**
> 분모 = 미납 `PAY_STAT_CD='F'` 행 **6,262,245** ⇒ **0.0175%**(yml 주석 기재와 라이브가 정확히 일치).
> 결제수단으로 완전히 갈린다: `1`(자동이체) 4,834,247행 NULL **0.000%**·사유코드 25종 / `2`(신용카드) 1,148,911 NULL 0.000%·47종 /
> `12`(네이버페이) 277,397 NULL 0.000%·12종 / `5`(휴대폰) 594 NULL 0.000%·13종 /
> 🔴 **`8`(OCR) 1,096행 NULL 100.000% · 고유 사유코드 0종** ⇒ 부재가 **한 수단에 전량 집중**한다.
> 기간 2022~2026 산발(282/274/230/209/101) ⇒ 부분적재 아님. 수단 라벨 = `CRM_CODE` `PM030`·`PM040`(`8`=OCR).
> ⇒ 🟢 `warn` 유지 정당 · 🔴 사유코드 창작 금지(`R2-7-1`) · 🔴 `where` 에 수단 제외 금지(넣으면 1,096 이 사라진다).
>
> **③ 🟢 `not_null MBER_NO` 745 — 원천 NULL 전파이고 모델이 만든 것이 아니다**
> 분모 38,467,887 ⇒ 0.00194% · 채널 = `MSG_AT` **741** + `EMAIL` **4** + `SND` 0 + `PSTMTR` 0(합 정확 일치).
> 🟢 **원천 직접 대조** = BRONZE 두 테이블 행수가 SILVER 와 **동수**(20,561,448 / 7,811,125)이고 NULL 도 741·4 로 같다 ·
> **공백 유래 인공 NULL 0** ⇒ `NULLIF(TRIM(),'')` 가 만든 것이 아니다.
> 🔴 종전 주석 「마스터 재추출/판정 후 error 복귀」는 **축이 틀렸다** — 이 축은 마스터 고아가 아니라 원천 값 부재다.
>
> **④ 🔴 `warn_fep_nonnumeric_member_dk` 62 — 게이트가 자기 헤더가 금지한 「두 성격 혼합」을 하고 있었다**
> 분류별 실측(합 62 · 보고값 일치) = `ALT_MEMBER_SCHEME` **29** · `SCANNER_CANARY_TOKEN` 11 · `SCANNER_SQL_PROBE` 10 ·
> `SCANNER_HTML_INJECTION` 4 · `OTHER` 3 · `SCANNER_PATH_TRAVERSAL` 2 · 산술·특수문자·XSS 각 1.
> 🔴 **최대 계열이 스캐너가 아니다** — 29종(**46.8%**)은 `S00023664` 류 **정규 `S`+8자리**이고 `SILVER.CRM_MEMBER` 에 **0건**
> ⇒ 그 테스트가 「별건」이라 분리해 둔 **② 마스터 미포함 축**이며 **`BLOCKING-1`·§O116-3 ㉢ 과 같은 탈퇴·삭제분 축**이다.
> `OTHER` 3 실제 값 = `1793 568`(공백 혼입 · 길이 8) · `null`(문자열) · `회원번호`(헤더값).
> 🔴 첫 시도의 `listagg` 구분자가 공백이라 값이 모호해졌고, **구분자를 바꿔 재측정**해 확정했다(J㉣ 이행).
> ⇒ 🔴 「스캐너 62건」은 **과대 진술**이다(확정 30종) · 🔴 테스트를 좁히지 않았다(좁히면 29종이 사라진다).
>
> **⑤ 🟢 `CMPGN_CD` 관계 2종은 두 건이 아니라 한 축이다**
> 고아 = `CRM_MEMBER` 4행/3종(분모 1,585,949 · 0.00025%) · `CRM_MEMBER_DEV` 18행/4종(분모 3,594,843 · 0.0005%) ·
> **합집합 5종** `{188·3358·3484·4178·11954}`(`4178`·`11954` 공통 ⇒ 더해 세면 중복).
> 🟢 **원천 대조** = `BRONZE_CRM.TM_CM_CMPGN_MNG` 에도 **5종 전부 0건** · 그 마스터는 **전량입고**
> (BRONZE 36,163행 · 고유 36,163 = SILVER 36,163) · 코드 범위 **1~13,568 안쪽의 결번**
> ⇒ `EVENT_105`·`106` 과 같은 성격이고 🔴 **입고·확정으로 채워지지 않는다** ⇒ 「확정 후 error 복귀」 폐기.
>
> **⑥ 🔴🔴 설계 결정을 등재했다 = `DEC-45`(문서30 §31) · 그리고 인수인계의 전제 2개를 실측으로 반박했다**
> 🔒 라벨 = `--next DEC` = **DEC45**(정의 36 · 참조 44). 선택지 ①단일값 한정 ②브릿지 정규화 **③원천 정규화 요청**.
> · 🔴 **㉢ 하류 소비자 0건** — `CRM_CAMPAIGN.SPNSR_BSNS_ID` 를 읽는 GOLD·SERVING 객체가 **없다**
>   (`DIM_CAMPAIGN` 에 후원사업 FK 부재 · `FACT_EVENT_PARTICIPATION.SPONSORSHIP_SK` 는 하드코딩 `0` 센티넬 = `O8` 차단분)
>   ⇒ 🔴🔴 **인수인계의 「② 는 소비 계약이 바뀐다」는 실측으로 지지되지 않는다** — 바꿀 계약이 아직 없고 ②는 **추가**다.
> · 🔴 **㉠ 범위 실측** — 다중값은 `SPNSR_BSNS_ID` 보유 **7테이블 중 2곳**뿐이다(`CRM_CAMPAIGN` 18,091행/445종 ·
>   `CRM_PAYMENT_BILLING` 1행/1종) · 나머지 4소비 테이블과 마스터는 **0** ⇒ 결정 대상은 **1모델**이다.
> · 🟢 **㉡ `▣WWW A` 질문에 답했다** — `CRM_PAYMENT_BILLING`(1)은 **같은 다중값 축이 맞다**(값 `4,43` · 성분 2건 전건 매칭).
>   🔴 그러나 **1/47,439,264 = 0.000002%** 라 설계 축이 아니라 **원천 오염 1건**이므로 `DEC-45` 에서 **분리**했다
>   — 묶어 두면 1행 때문에 설계 결정이 지연된다.
> 🔴 **배선·`where` 축소·브릿지 생성은 하지 않았다**(사람 결정 전 금지 조항 준수).
>
> **⑦ 🔴 낡은 처방 시정 — O116-B 가 빠뜨린 1곳을 포함해 코드 5곳 + 헤더 2블록**
> `_crm_schema.yml` = ㉠ `CRM_PAYMENT_BILLING.SPNSR_BSNS_ID`(**O116-B 가 5곳을 고치며 빠뜨린 곳** — 기각된
> 「전량입고 후 error 복귀」가 코드에 살아 있었다 · `P154` 축) ㉡ `RQEST_RST_CD` ㉢ `CRM_SEND_MEMBER.MBER_NO`
> ㉣ `CRM_MEMBER.CMPGN_CD` ㉤ `CRM_MEMBER_DEV.CMPGN_CD` + **헤더 정책에 축 ㉤(`CMPGN_CD`)·㉥(`not_null` 계열) 신설** ·
> `tests/warn_fep_nonnumeric_member_dk.sql` **헤더**(분류 실측 + 혼합 적발). 문서50 §O116-4 ㉡ 의 낡은 전제도 취소선 + 정정.
> 🟢 **교훈 명문화** = 「축을 고칠 때 같은 축의 다른 테이블 주석도 함께 `grep` 하라」를 yml 헤더와 인수인계에 새겼다.
> 🔴🔴 **검증 조건 = 다음 `dbt build` 의 `TOTAL` 이 461 유지**. 근거 = **주석만** 고쳤고 YAML 파서로
> **모델 86 · 테스트 선언 351**(`_crm_schema.yml` **20 / 117** — O116-B 기재와 일치)이 **편집 전후 동일**함을 단정했다.
> 🔴 `dbt` 명령은 실행하지 않았다(`R4-1`) — `dbt build --project-dir 10_dbt_pipeline` 을 제시만 했다.
>
> **⑧ 🟠 게이트 판정을 의심해 오탐 1건을 걸러냈다(판정식 4조 신설)**
> `decision_closure_gate` 가 **미봉합 34건**을 냈다. 분해하니 **기존 19건**(`DEC-41`·`DEC-42`·`DEC-43` 축 · 인수인계 기재와 일치) +
> **`DEC-45` 15건**이고, 후자는 🔴 **오탐**이다 — 그 게이트가 **열린 절의 표 행을 「종결 선언」으로 읽고** 인접 토큰 때문에
> `DEC-45` 를 엉뚱한 `O8` 축에 붙였다. `DEC-45` 는 종결 선언을 한 적이 없다.
> 🟢 무시하지 않고 **그 이유를 문서30 §31 머리말에 적었다**(`R3-9 ㉥` 「열린 이유를 그 자리에 적어라」 이행).
> ⇒ 🆕 **판정식 ㉦ 신설** = **게이트가 「종결 선언」이라 부르는 것이 실제 종결이 아닐 수 있다** — 종전 3조는
> 「몇 건을」만 의심하게 했고 「무엇을」은 의심하지 않았다.
>
> **⑨ 🟢 유지 연산·게이트**
> 재균형 1회(문서50 18→19조각 · max 26,944 B · 최소 여유 14,016 B) · 허브 재발행 3건(원장 · 문서30 · `99_NEXT`) ·
> 전부 `--expect` **연속 리터럴** 토큰으로 신선도·영속성 양축 가드 통과(`R1-6-23` · `O112 ㉠` 함정 회피) ·
> 스냅샷 4건(`snapshot_util` 경유 · `O117-prehub`·`O117-prerebalance`).
> `99_NEXT §0-XXX` 신설 + `§0-WWW` 승계 표기 · 원장 §1 O117 행 선점 → 결과로 대체.
> **`read` 미반환 0건 · 재호출 0건**(판정식 = 재호출 ≥ 미반환 충족).
>
> **⑩ 🔴 자기결함 · 남은 것**
> · 🔴 `listagg` 구분자 충돌로 값 3종을 한 번 모호하게 관측했다(즉시 재측정해 확정 · ④).
> · 🟠 기존 미봉합 19건은 손대지 않았다(승계 항목이고 이번 결정과 무관한 축이다) — `▣XXX5` 에 명시.
> · 🔴 `DEC-44`·`A.7`·`BLOCKING-1` 은 **성격 불변**이라 손대지 않았다(각각 회신·승인·원천 대조 대기).
> · 🟢 `BLOCKING-1` 에 **새 근거 1건**이 붙었다 = ④ 의 `ALT_MEMBER_SCHEME` 29종이 **같은 탈퇴·삭제분 축**이다.
> 🔴 **정본 = 문서50 §O116-6 · 문서30 §31(DEC-45) · `99_NEXT §0-XXX` · 원장 §1 O117 행 · 이 항목.**

> #### 🟢 [2026-08-29 O118] SV 라이브 실측 최신화 — 인수인계 전제 3건 반박 · `DEC-44` 의 SV 층 노출 적발 · 문서 최신화
>
> **지시** = ① 열린 이슈 중 **현재 적재 데이터로 해결 가능한 항목** 처리 ② SV 설계 최신화 ③ `05_1`~`05_10` DDL 갱신.
> **`R4-4-2` ㉡ 적용**(지시 동봉 ⇒ 브리핑 후 즉시 수행). **확정위반 = 0** · **자기결함 3건**(아래 ⑦).
>
> **① 🔴🔴 인수인계 전제 3건이 실측으로 반박됐다 — 「부재 기재」도 믿으면 안 된다**
> ㉠ `show semantic views in account` = **10행**(O117 기재 **0**). 전건 `GN_DW.SERVING` · base 전건 `GOLD` ·
>    `created_on` **2026-08-29 05:59:12~05:59:46**(34초 배치) ⇒ **O117 측정 이후에 배포됐다**(`P168` 재현).
> ㉡ 「`GN_DW.SERVING` 객체 0 ⇒ SV 부재」는 **판정식 오류**였다 — **semantic view 는
>    `INFORMATION_SCHEMA.TABLES` 에 나타나지 않는다.** 실제로 `SERVING` 의 테이블·뷰는 **지금도 0** 이고
>    SV 는 **10** 이다(**두 진술이 동시에 참**) ⇒ 그 축은 애초에 SV 부재의 근거가 될 수 없었다.
> ㉢ 「`SERVING` `USAGE` 미부여 = SV 배포 선행조건」도 **stale** — ANALYST·ENGINEER·SERVICE·VIEWER
>    **4역할 부여 완료**(06:06:33~35). 배포(05:59)와 권한 부여(06:06)가 **한 작업**이었다.
> 🟢 **유효 승계** = `show agents in account` **0행** · `ML` `USAGE` **미부여**(3회 재조회 일관).
> ⇒ 🔴 **신설 판정식 2조** = ㉠ **SV 의 분모는 `SHOW SEMANTIC VIEWS`(또는 `INFORMATION_SCHEMA.SEMANTIC_VIEWS`)뿐이다** —
>   스키마 객체 목록으로 SV 유무를 판정하지 마라. ㉡ **`R2-8-4-d` 는 양방향이다** — 종전 규약은
>   「완료 기재를 라이브 근거로 쓰지 마라」였고, O118 은 **「부재 기재를 라이브 근거로 쓰지 마라」**가 필요했다.
>
> **② 🔴🔴 최대 발견 — `DEC-44` 차단조항이 지목한 객체가 틀렸다(실효 위반)**
> 조항 = *"`05_6_SV_DDL_BUDGET.sql` 을 `FACT_BUDGET_YEARLY` 에 붙이지 마라"*. 라이브 `SV_BUDGET` base 는
> `GOLD.FACT_BUDGET`(월 · 2,964행)이라 **문자적으로는 준수**됐다. 그런데 실측:
> `FACT_BUDGET.EXEC_BUDGET_ERP` 합 **99,006,005,048** = `FACT_BUDGET_YEARLY.EXEC_BUDGET_YEAR` 합
> **99,006,005,048**(**차이 0**) ⇒ **결함이 그대로 상속됐다.**
> `SEMANTIC_VIEW(SV_BUDGET METRICS TOTAL_EXEC_BUDGET)` 실측 = **99,006,005,048**(정합 55,094,546,653 · **+79.7%**).
> 🔴 **단계가 올라갔다** = 「돌리면 틀릴 것」(O114-B) → 「조회하면 틀린다」(O115-C) → **「의미 계층의 확정 metric 이다」**.
> 🟢 다만 **Agent 0** 이라 오답이 사용자에게 나가는 경로는 **미개통** ⇒ 🔴 **Agent 배포 전에 이 결정을 닫아야 한다.**
> ⇒ 🟢 **신설 판정식** = **차단조항이 객체명으로 쓰여 있으면 그 결함값이 다른 객체에도 있는지 대조하라.**
>   「그 테이블을 안 썼다」 ≠ 「그 결함을 피했다」. 🔴 **§30-F ③ 표에 두 팩트가 이미 나란히 적혀 있었다** —
>   근거는 옳았고 **처방 문구가 좁았다.** 🟢 fan-out 스모크는 PASS 였다 ⇒ **스모크 PASS 는 「값이 옳다」가 아니다.**
>
> **③ 🟢 `DEC-44` 「재작성」 판정은 내가 낸 것이 아니다 — 재현 + 축 1개 보강이다**
> 🔴 **정직 표기**(`R2-8-2`): 「추가경정 = 재작성」은 **§30-F(O115)가 금액 다중집합 대조로 이미 확정**했다.
> O118 기여 = ㉠ **다른 축의 재현**(세세목 8키 × 연도 피벗 · 항목×연도 24키 중 **집행 상이 0 / 동일 23**)
> ㉡ 🆕 **편성 축 정량 신규 = 상이 17 / 24 (70.8%)** — 종전 정본에 이 비율이 **없었다**.
> ⇒ 🔴 **처방이 이것으로 갈린다** = **차수를 키·축에 보존하고 합산에서만 배제한다.**
>   「키에서 빼라」와 「합산하지 마라」는 **다른 처방**이다 — 빼면 **편성 정보가 사라진다**
>   (§30-C ㉠·㉣ 가 이 구분을 흐린다 · **㉡·㉢ + 행 선택 규칙**이 실측과 정합 = §30-F ④ 와 동일 결론).
> 🔴 **배선·metric 삭제·`where`·차수 필터·키 변경·`severity` 승격 = 전건 미실행**(`R1-2` 결정 선점 금지).
> 🟢 정본 = 문서30 **§30-G 신설**.
>
> **④ 🟢 차단조항 ㉡㉢ 는 라이브에서 전건 준수 확인**
> `DEC-45` = `SPNSR_BSNS_ID` 를 읽는 SV dim/metric/fact 식 **0** ⇒ O117 기재 유효 ·
> `O8` = `SV_EVENT_PARTICIPATION` 논리테이블에 `DIM_SPONSORSHIP` **부재** · 후원사업 dim/metric **0** ·
> `FEP.SPONSORSHIP_SK` 비-0 **0행**(전건 센티넬 확증).
> 🔴 **`SV_MEMBER_SPONSOR_BIZ` 는 위반이 아니다** — 그 후원사업 축은 약정 grain(`SPNSR_BSNS_NO`)에서 오고
> `CRM_CAMPAIGN.SPNSR_BSNS_ID`(쉼표 다중값)를 읽지 않는다. **두 축을 혼동하지 말 것.**
>
> **⑤ 🟢 문서 최신화 — 차이를 3축으로 갈랐다(한 축으로 뭉개지 않았다)**
> `04_SV_설계.md` **§0.9 신설(A~G)** = 라이브 전수 · **차이 3축 분해**
> (**축① 설계 변경** 5건: `SV_MEMBER_MONTHLY` 7→4 · `SV_SERVICE` 5→4 · `SV_BUDGET` 5→3 ·
>  `SV_AD` 3→4(`DIM_MARKETING_CAMPAIGN` 추가) · SV 종수 /
>  **축② 문서 오기** 5건: `SV_MEMBER_EVENT` 제목↔본문 6↔7 · §0.7 「미배포」 · §9 세세목 2,041→**180** ·
>  §6.6 「6행」 · §0.8 「7개」 / **축③ 라이브 결손** 5건: `SV_BUDGET` 과대 · Agent 0 · `ML` `USAGE` · 하위 4종 구조절 부재).
> 🔴 **축마다 처방이 다르다** = ①은 문서를 · ②는 문장을 · ③은 라이브·결정을 고친다.
> **사용 지점 병기 5곳**(제목·§0.7·§0.8·§1.1·§2.1·§3.1·§4.1·§5.1·§6.1·§6.6·§9·§10 — **취소선 + 실측 병기** · 삭제 0) ·
> `05_0_SV_DDL.sql` = **`05_10` 행 누락 시정**(인덱스가 정본을 주장하며 대상 1개를 빠뜨렸다) + 범위 `05_1~05_10` ·
> `05_6_SV_DDL_BUDGET.sql` = **「보류」 선언 + 사용 금지 규칙** · `08_AGENT_spec.md`·`00_README.md` = **SV↔Agent 축 분리**.
> 🔴 **stale 약 50곳을 일괄 치환하지 않았다** — **축A(실적 SV 단정 9곳 = 전건 처리) / 축B(이력 · 손대지 않음) /
>   축C(ML SV·Agent 도구 수 = 다른 축)** 로 갈랐다(전수 좌표 = 근거철 §12).
>   ⇒ 🟢 **신설 판정식** = 「`7 SV`·`9종` 을 만나면 이력/타축/실적SV단정 을 먼저 가르고 **단정형만** 고쳐라.」
>   🟢 **근본 처방** = 개수를 세는 지점을 **§0.9-B 쿼리 한 곳**으로 모았다.
>
> **⑥ 🔴 ML 축은 전건 미배포다 — 이슈 6 이 반으로 갈린다**
> ML 예측결과 SV(`22_ML_SV_DDL.sql` 7종) **0** · ML SERVING 뷰(`21_…`) **0**(`SERVING` 뷰·테이블 0) ·
> `ML` `USAGE` **0** · Agent **0** ⇒ **네 축이 서로 정합한다**(ML 축 전체가 미배포).
> 🔴 종전 인수인계가 이를 「SV 배포 선행조건」으로 적은 것은 **오분류**였다 — SV base 는 전건 `GOLD` 이므로
> ML 권한과 **무관하게** 동작한다(실측 확증). ⇒ **이슈 6 = `SERVING` 해소 / `ML` 미해소.**
>
> **⑦ 🔴 자기결함 3건(전부 자력 적발·시정)**
> ㉠ **`write` 가 성공(292줄)을 보고하고 파일을 만들지 않았다** — 로컬·스테이지 **모두 부재**.
>   `R2-5` `cortex ws ls` 대조로 적발(같은 폴더 `_o117_entry.md` 등은 정상 ⇒ 경로 문제가 아니라 **쓰기 유실**).
>   ⇒ 🔴 **`write` 의 성공 메시지는 실체의 증거가 아니다** · 판정은 `ls` **두 축**으로.
> ㉡ 🔴🔴 **가장 위험했던 것 — 「`SERVING` `USAGE` 미부여 ⇒ SV 소비 차단」을 단정했다.**
>   `SHOW GRANTS ON SCHEMA` 첫 반환이 **1행**(`OWNERSHIP` 만)이었고 그것을 그대로 믿었다.
>   **다른 축**(`SHOW GRANTS TO ROLE GN_DW_ANALYST`)이 `USAGE ON SCHEMA GN_DW.SERVING` 실재를 보여줘 반증되고,
>   **같은 문장을 재실행하니 5행**이 나왔다(`OPS-3` 낡은 뷰 · O112 ㉡).
>   ⇒ 🔴 **거짓 이슈를 발행할 직전이었다.** 🟢 **살린 것은 교차검증 습관이고 게이트가 아니었다**(`R3-9`).
>   ⇒ 🟢 **신설 판정식** = **`SHOW GRANTS` 로 「부재」를 단정하기 전에 2~3회 재실행 + 반대 방향(`TO ROLE`)으로 대조하라.**
>     **부재 판정은 실재 판정보다 약하다.**
> ㉢ `edit` 로 **`## 1. SV_MEMBER_MONTHLY` 제목을 파괴**했다(`R1-7-8` 유형) — 편집 직후 확인해 즉시 복구 ·
>   `doc_heading_gate` 제목 유실 **0** 으로 확증.
> 🟠 **도구 한계 기록** = 역할 세션 실증이 **툴 호출 간 유지되지 않았다**(검증 문장에 `current_role()` 을
>   동봉하니 `ACCOUNTADMIN` 복귀) ⇒ 🔴 **역할 실증 관측은 근거로 쓰지 않았다**(판정 근거 = grant 메타데이터 2축).
>   ⚠️ `ACCOUNT_USAGE.GRANTS_TO_ROLES` 는 **0행**(당일 부여 · 지연) ⇒ **지연 뷰를 부재 근거로 쓰지 마라.**
>
> **⑧ 유지 연산·게이트**
> 🟠 **재균형 1회(사용자 승인)** — 문서30 조각 `-012` 가 **편집 전 이미 299줄**(상한 300)이어서
> `§30-G` 를 어디에 두든 재균형이 필요했다 ⇒ `--rebalance --fill 0.7 --expect "30-G"` ·
> **12조각 유지 · 유실 0 · 상한 초과 0 · 스냅샷 `_archive/30_설계_의사결정.md.O118-prerebalance`**.
> 🟢 게이트 = `doc_census` ✅(stale 0 · 분할 분모 위반 0) · `doc_type` ✅ · `clause_order` ✅ ·
> `index_row` ✅(행 유실 0) · `doc_heading` ✅(유실 0 · §30-G 신설 1 골든 발행 `--reason` 동봉).
> 🟢 `line_len` PASS(전 편집 파일) · 원장 행 **1,896자**(1,900 이내 · `O112 ㉢` 이행).
> 🔴 **`read` 미반환 = 0 · 재호출 = 0** — `04_SV_설계.md` 815줄 4토리 전량 확보(독해 기록 = 근거철 §10).
> 🔴 **dbt 명령 미실행**(`R4-1`) — 미검증 항목은 **다음 build 에서 `TOTAL` 461 유지** 그대로 승계.
> 🔴 **배포·`ML` `USAGE` 부여·`A.7` REMOVE = 전건 미실행**(별도 승인 대상 · `R4-4-3`).
>
> 🔴 **정본 = `05_SV-Agent_ai/04_SV_설계.md` §0.9 · 문서30 §30-G · 원장 §1 `O118` · `99_NEXT §0-YYY`**
> 🔎 **부속 근거철**(정본 아님 · 쿼리·원시 관측·전수 좌표) = `_o118_sv_live_census.md`
