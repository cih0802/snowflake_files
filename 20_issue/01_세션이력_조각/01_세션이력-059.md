<!-- SPLIT-CHUNK 01_세션이력.md | 059/059 | 허브 = 01_세션이력.md | 원문 1~159행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-08 O143] O 누적작업 비판적 점검 — 브리핑 오선택 2건 시정 · 측정축 고정 · 등록부 결합 게이트화 · SERVING 표면 부재 적발
>
> **지시** = 「여태까지의 O 이력을 비판적으로 검토 · 누락된 오류 확인 · 참고 문서와 프롬프트 보충」
> + 「작업량이 많으면 `/07_O작업정리/` 에 계획서를 먼저 만들고 착수」.
> 🟢 **`R4-4-2 ㉡` 분기 적용**(지시 동봉 → 브리핑 출력 후 즉시 수행) · **확정위반 0건** ·
> 사용자가 승인 필요 작업을 **사전 전건 승인**했다.
>
> **§0. 범위 판정 — 왜 계획서를 먼저 썼는가**
> 실측 분모 = 전 문서군 **2,428,268 자** · `01_세션이력` **58조각 1,550,499 B** · O 라벨 정의 최대 **142**.
> ⇒ 이력 전량 독해는 물리적으로 불가능하다. 따라서 이 점검은 「이력을 읽어 결함을 찾기」가 아니라
> **「결함을 검출하는 판정식을 세우고 그 판정식으로 정본을 훑기」**로 설계했다(`R3-9 ㉦` 축).
> 산출물 = `07_O작업정리/00_O작업_점검_계획서.md`(진행 원장 · Phase 0~5 · 판정식·산출물 명시) ·
> `07_O작업정리/01_NL스모크_절차서.md`.
>
> **§1. 🔴🔴 D1 — 브리핑이 승계된 인수인계 절을 현행으로 뽑고 있었다 (원인 2건 · 둘 다 시정)**
> 착수 브리핑의 「현행 시작점」이 **`§0-NNNN`(O140 · 이미 취소선 승계됨 · `-028.md:19`)** 으로 나왔다.
> 실제 현행은 **`§0-PPPP`(O142 · `-028.md:81`)** 였다.
> · **원인 1 = 리터럴 인접 의존.** `session_brief.py:246` 이 `'~~여기서 시작한다' in t` 였다.
>   O129 부터 관례가 **대괄호 전체를 감싸는 형태**(`## 0-NNNN. ~~🔴🔴 [… 여기서 시작한다. …]~~ ➔ 🟢 […]`)로
>   바뀌어 그 **연속 부분문자열이 존재하지 않게** 됐다 ⇒ 배제 실패.
> · **원인 2 = 동점 tie-break 부재.** `max(cands, key=date)` 이고 O140·O141·O142 **날짜가 모두 `2026-09-07`**
>   이라 파이썬 `max` 의 동점 규칙(최초 등장)이 **가장 오래된 절**을 골랐다.
>   🔴 **원인 1 만 고쳐도 재발한다** — 둘은 독립이다.
> · 🔴 **실해** = `§0-NNNN` 의 열린 목록에는 `⑫`·`⑭`·`㊵`·`BLOCKING-5` 가 실려 있고 그중 **`㊵`·`⑫` 는
>   O141 이 이미 종결**했다 ⇒ 브리핑만 읽고 착수하는 세션은 **닫힌 작업을 다시 한다.**
>   O106 이 만든 브리핑 체계(`R1-1` 개정)의 **목적 자체가 무력화된 상태**였다.
> · 🟢 **시정** = `struck_out()` 신설(문구의 **모든 등장**이 `~~ … ~~` **구간 안**에 있을 때만 승계) +
>   정렬 키 `(date, idx)`(동점은 **문서 순서상 마지막** = `99_NEXT` 적층 방향).
>   재생성 후 현행 절 = **`§0-PPPP`(:81)** 로 정정 확인.
>
> **§2. 🔴 D2 — 음성 테스트 29종이 전건 rc=0 인데 D1 이 실재했다**
> 원인 = `test_session_brief.py` 픽스처의 취소선 6건이 **전부 구관례**(`:111`·`:125`)이고
> 신관례 픽스처 **0건** · 동일 날짜 동시후보 픽스처 **0건**이었다.
> ⇒ 🟢 **축 4-C 신설**(픽스처는 `-028.md:19·50·81` **원문 그대로** · 창작 0) + 축10(누락 대조).
> 🔴 **판정식을 「고친 뒤 통과」로 두지 않았다** — 축을 먼저 넣고 **수정 전 코드에서 9건 실패**를
> 실증한 뒤 고쳤다. 오탐 가드 3건(무관한 취소선은 배제 사유가 아니다)은 수정 전에도 통과했다
> ⇒ 픽스처가 과잉이 아니라는 증거다. 최종 = **10축 58단정 전건 통과**.
>
> **§3. 🟠 D3 — `-028.md:17-18` 표가 본문 행 없이 절단돼 있다 (관측 확정 · 원인 미규명)**
> `§0-FFFF` 의 「항목/성격/좌표」 표가 **헤더 1행 + 파이프 1글자**로 끝난다.
> · 🟢 **조각은 결백하다** — `split_doc --verify` **PASS**(본문 바이트 동일 · 유실 0 · 행선지 없음 0종)
>   ⇒ **원문 자체가 이 상태**이고 조각화·재발행이 만든 손상이 아니다.
> · 🔴 **복구 불가** — `_archive/` 대조를 시도했으나 `grep` 이 `timeout` 에 죽었고(그 관측이 **D7**),
>   파일명 기준 탐색으로도 원형을 찾지 못했다 ⇒ **미확보로 명시**하고 추정 복원을 하지 않았다(`R2-8-3`).
> · 🔴 **게이트 맹점 1건** = `--verify` 「게이트5 표 열수 결손」이 **0 으로 통과**했다. 즉
>   **파이프 한 글자만 남은 줄을 결손으로 세지 않는다** ⇒ 후속 과제 **후-3** 으로 등재.
> · 🟢 **영향도 낮음** — `§0-FFFF` 는 O132 에 승계된 과거 절이고 스스로 「착수 항목은 착수표에 있다」고
>   정본을 넘겼다.
>
> **§4. 🔴 D5 — 문서 바이트 실측이 `stat` 에 의존해 stale 오탐을 냈다 (시정 완료)**
> 착수 1차 브리핑이 `31_코드군_매핑등재부` 를 **stale 1건**으로 보고했다(「기재 14,891 ↔ 실측 14,896」).
> 실제 파일은 **14,891 B**(`wc -c`·`read_bytes` 일치)이고 **14,896 은 그 값의 16바이트 블록 반올림값**이다
> (`cortex ws ls` 도 14,896) ⇒ **기재가 맞고 「실측」이 틀렸다.**
> · 원인 = `doc_census.nbytes_of` 가 `os.path.getsize`(stat)였다. `/workspace` 는 스테이지 마운트이고
>   **cold 파일의 stat 이 반올림값을 낸 것**으로 보인다.
> · 🔴 **위험은 오탐이 아니라 오탐이 유도하는 조치다** — 세션이 이를 믿고 기재를 14,896 으로
>   「교정」하면 **정본이 거짓이 된다.**
> · ⚠️ **기전은 유력추정이다** — 읽으면 자기치유되어 사후 재현이 불가능하다
>   (`scripts/_o143_size_axis_probe.py` = 이 시점 `.md` **314개 전건 stat == 실제**).
>   ⇒ 기전 규명 대신 **측정을 stat 비의존으로 교체**했다(결론이 기전과 무관하게 옳다).
> · 🟢 `doc_census.nbytes_of` · `doc_type_gate.nbytes_of` 를 읽은 바이트로 교체 +
>   `test_doc_census.py` **축7** 신설(16의 배수가 아닌 4,005 B 픽스처로 반올림을 구별 · 두 도구 값 일치 단정).
> · 🔎 지침 `R2-5` 가 「`ws ls` size 는 블록 반올림값」이라고 **이미 경고**하는데 도구가 그 함정을 밟았다
>   ⇒ `R3-9 ㉡`(같은 것을 다르게 재는 지점)의 실물이다. 현재 stale **0건**.
>
> **§5. 🔴🔴 D6 — 착수표 열린 집합과 인수인계 목록이 다르다 (게이트화 완료)**
> 실측 = 착수표 열림 **`② ④ ⑪ ⑭ ⑱ ㊳`(6건)** ↔ `§0-PPPP` 목록 **`② ④ ⑪ ⑱`**
> ⇒ **`⑭`·`㊳` 누락**이고 `⑭` 는 **P1 🔴🔴**(FME STOP 팬아웃 귀속 · 현업 결정 대기)다.
> · 경로 = `⑭` 는 `§0-NNNN`(O140)에 있었고 `§0-OOOO`(O141)에서 사라졌다. O141 은 **「점검」만** 했고
>   착수표 행(`-022.md:114`)은 그대로 열려 있다 ⇒ **닫지 않은 항목이 인수인계에서 조용히 사라졌다.**
> · 🔴 **선례가 문서에 이미 있었다** — 착수표 `㊲` 행이 *"O128 이 먼저 해소했으나 취소선 처리가
>   누락됐다"* 고 스스로 적는다 ⇒ 두 등록부의 결합이 **사람 손에만** 있었고 어느 게이트도 이 축을 안 봤다.
> · 🟢 **시정** = `session_brief.handoff_gap` 신설 → `00_BRIEF` §2 에 **좌표와 함께** 경고
>   (blocking 아님 · 판정은 사람 · 방향은 한쪽 = 인수인계에만 있는 문서50 계열은 정상).
>   재생성 결과 **실제 정본에서 `⑭`·`㊳` 2건 정확 검출**. 음성 테스트 축10(역방향 오탐 가드 포함).
> · 🟢 지침 **`R3-9 ㉨`** 신설 + 스킬 자문 **⑨** 추가(㉠~㉧ **8개 → ㉠~㉨ 9개**로 양쪽 범위 갱신).
>
> **§6. 🔴🔴 D7 — `timeout` 에 죽은 검색이 「0건」으로 보인다 (지침 `R0-8` 신설)**
> `grep -rn "FFFF1" 99_NEXT_SESSION_조각/ _archive/` = **0건**인데 단일 경로로는 **1건**이었다.
> 원인 = `_archive/` 가 **447파일 19,214,080 B** 이고 60초를 넘겨 `timeout` 이 `SIGTERM` 으로 죽였다
> (**rc=143**). 출력은 빈 문자열이고 `2>&1 | head` 를 거쳐 **에러도 보이지 않았다.**
> 🔴 **이 함정은 지침이 만든다** — `R0-4`(`timeout 60` 의무) + `R0-2`(다중 경로 권장)의 결합이라
> **분모가 큰 검색은 구조적으로 조용히 0 을 낸다.**
> 🟢 **`R0-8` + 부속 3조 신설** = ㉠ 0건은 `rc` 로 검증(`0` 만 「없다」 · `143`·`124`·`2` 는 **미판정**)
> ㉡ 파이프 뒤 `$?` 금지(O120 의 게이트·테스트 규칙을 **검색·조회로 확장**) ㉢ 대용량 경로 분리.
> 스킬 **Step 0** 에도 실었다(첫 bash 호출 **전에** 노출되어야 하는 규칙이다).
> 🟢 **같은 세션에서 효과가 실증됐다** — 직후 `grep … 06_DDL.sql` 이 **rc=2** 를 냈고, 「부재」로 읽지
> 않고 경로를 찾아 `03_top-down_gold/06_DDL.sql` 을 발견했다.
>
> **§7. 🔴 D8 — 착수표 `⑭` 의 「COMMENT 사유 명시」가 정본에 없었다 (보강 완료)**
> `⑭` 는 *"현재 처리 = 0 센티넬 + COMMENT 사유 명시(`R2-7`)"* 라 적지만 실측 결과 **양쪽 다 없었다**:
> · 라이브 컬럼 COMMENT = `후원사업 (FK→DIM_SPONSORSHIP)` — **사유 없음**
> · `06_DDL.sql` FME `[컬럼별 설계 및 실측 이력]` 블록에 **`· SPONSORSHIP_SK` 항목 자체가 없음**
>   (전 파일 `· SPONSORSHIP_SK` 4건 = 전부 다른 테이블 소관)
> 🔴 **더 나쁜 것** = 같은 파일 후반 변경이력이 *"사건 grain 에서는 후원사업이 하나로 확정되므로
> 귀속 규칙이 필요 없고"* 라고 적는다. **DEV 축 한정 서술인데 축을 밝히지 않아**, 읽는 사람이
> **STOP 센티넬을 「배선 누락」으로 오해해 고치게** 만든다 ⇒ `⑭` 의 「현업 결정 전 배선 금지」
> 정지점과 **정면 충돌하게 읽힌다.**
> 🟢 **시정** = FME 이력 블록에 `· SPONSORSHIP_SK` 신설 — DEV 전용 배선 / STOP 전건 0 센티넬 +
> 사유(동시중단 다중사업 · O73-C 1.56배 = pairs 1,517,797 / 키 970,486) + `⑭` 정지점과 선택지 4종 +
> **DEV 축 한정임을 명시** + B2(FMM)와 grain 이 다르다는 경고 + O143 라이브 실측 병기.
> 선례 = 같은 블록의 `· ORG_SK` 항목이 동일 패턴(DEV 전용 / STOP 전건 0 / 사유 / 금지)을 이미 쓰고 있었다.
>
> **§8. 🟢 착수표 `⑭` 가드 유지 — 라이브 실증 (`R2-8-4-a` 3요소 기재)**
> 계정 `hy04212` / `ACCOUNTADMIN` / 2026-09-08 조회. 🔴 계정명은 맥락이고 **판정 근거는 조회 결과다**.
> · `FACT_MEMBER_EVENT` 전체 **4,633,105행** · `SPONSORSHIP_SK=0` **1,038,262** · NULL **0**
> · `EVENT_TYPE` 별 = **STOP 1,038,262행 전건 SK=0**(비영 **0**) · **DEV 3,594,843행 전건 비영**
> ⇒ 🟢 **0 센티넬 가드 유지 확정 · 사업 축 전개(팬아웃) 없음.** 배선은 하지 않았다(정지점 준수).
> 🔎 O137 기재(「STOP 1.04M 센티넬 0」)와 정합한다.
>
> **§9. 🔴🔴 D9 — SERVING 표면(SV·Agent)이 라이브에 없다 ⇒ 착수표 `②` 는 실행 불가**
> · `SHOW AGENTS` · `SHOW AGENTS IN ACCOUNT` = **0행**(rc 정상)
> · `SHOW SEMANTIC VIEWS IN DATABASE GN_DW` = **1종 `SV_AD` 뿐**(생성 `2026-09-07 01:46` = O142 가 만든 것)
> · `SHOW SCHEMAS IN DATABASE GN_DW` = 15종 · **전 스키마 `created_on` 이 `2026-09-07 00:23~00:25`**
> · DW 본체는 정상 = GOLD **37 테이블 + 14 뷰** · SILVER **43** · ML **16** · OPS **4**
> 🔴 **판정** = 계정이 **2026-09-07 에 재구축**됐고 O139~O142 가 손댄 GOLD/SILVER/`SV_AD` 만 재생성됐다.
> **나머지 SV 와 Agent 3종은 재배포되지 않았다.**
> 🔴 **결과** = 착수표 `②`(NL 스모크)의 정지점은 **「사람 소관」이 아니라 「선행 배포 미완」**이다.
> 여러 세션 동안 「사람이 UI 에서 하면 된다」로 이월돼 왔지만 **UI 에 Agent 가 없어 사람도 못 한다.**
> 🔎 지침 `R2-8-4`·`R3-9 ㉤` 가 경고한 바로 그 상황이다 —
> *"계정이 바뀌면 객체는 사라지지만 문서의 「완료」는 남는다."*
> ⇒ 🔴 **O134 「`AGENT_MEMBER` `VERSION$3` 11도구 일치」·O136 「28/28 PASS」는 라이브 근거가
> 아니다**(`R2-8-4-d` — `90` 계열은 닫힌 항목의 보존소이고 라이브 상태의 정본이 아니다).
> 🟢 **산출물** = `07_O작업정리/01_NL스모크_절차서.md` — 차단 사실(§0 실측표) · 선행조건 5단계 ·
> 판정 4종(`집계필요`·`배분규칙필요`·`형제팩트중복`·`앵커_경합`) · 질의 5축 · 결과 기록표.
> 🔴 **Q5 = 과잉 차단 오탐 가드**(자기 grain 질의는 정상 답변이어야 한다) — **Q1 만 보면 「거부만 잘하는
> Agent」를 통과시킨다.** 🔴 재배포는 이 세션이 하지 않았다(라이브 객체 생성 + 배포 절차 준수 ⇒ 별도 단위).
>
> **§10. 판단 기록 — 하지 않은 것과 그 이유**
> · **원장 재균형 보류(사전 승인은 받았다)** — 재균형은 조각 경계를 바꿔 **인용 좌표(`파일:행`)를
>   무효화**한다(`doc_coord_gate` 축3 경고 증가). 대신 **O112 ㉢ 처방**대로 원장 행을 압축해 썼다.
>   🔴 **승인이 있다는 것이 실행 근거는 아니다** — 필요성이 실증되지 않았다. 원장 `-002` 여유 **447 B**.
> · **문서50 열린 절 18건 절별 재판정 미실행** — 목록·좌표만 확보했다(후속 **후-2**).
>   🔴 「전건 검토」라고 쓰지 않는다(`R3-9 ㉥` 부분 종결 금지).
> · **dbt 명령 0회 실행**(`R4-1`) · **현업 결정 대기 항목 배선 0건** · **라이브 DDL 변경 0건**.
> · **`_o125e_entry.md`(㊳) 에 `rm` 시도 0회**(`R1-7-7` · 스테이지에 실체가 없다).
>
> **§11. 검증 (rc 는 전부 리다이렉트로 수집 · `R0-8-2`)**
> · 게이트 6종 = `doc_census` ✅(stale **0** · 분할 분모 위반 **0**) · `doc_type_gate` 🟡(여유 경고 4건) ·
>   `clause_order_gate` ✅(조문 **96 → 100** · 역전 0 · 중복 0) · `index_row_gate` ✅(행 유실 0) ·
>   `doc_heading_gate` ✅(제목 유실 0 · 1,467/1,467) · `doc_coord_gate` 🟡(깨진 좌표 0 · 경고 11건)
> · 음성 테스트 **전건 rc=0** · `test_session_brief` **10축 58단정** · `test_doc_census` **33단정**
> · `split_doc --republish --expect` = 원장(`착수 선점(진행중)`) · `99_NEXT`(`0-QQQQ`·`QQQQ2`) 둘 다
>   **영속성 가드 통과** · 게이트1 concat SHA256 일치 · 게이트5 표 열수 결손 증가 0 · 게이트6 상한 초과 0
> · `line_len.py` 🟢 PASS(신규·수정 파일 전건 · 2,000자 초과 0줄) · `cortex ws ls` 로 스테이지 실체 확인
> · **`read` 미반환 0건 · 재호출 0건**(판정식 `재호출 ≥ 미반환` 충족) ·
>   독해 좌표·고유 토큰 **14건** 기록 = `07_O작업정리/00_O작업_점검_계획서.md` §6
>
> **§12. 자기검토 — 이 세션이 남긴 약점**
> · 🟠 **D3 는 닫지 못했다** — 원인·원형 모두 미확보다. 「낮은 영향도」는 **추정이 아니라 그 절이
>   스스로 정본을 착수표로 넘겼다는 문장에 근거**하지만, 잃은 내용이 무엇이었는지는 **모른다**.
> · 🟠 **D5 의 기전은 실증되지 않았다** — 「cold stat 반올림」은 유력추정이다. 판정과 시정은
>   기전과 무관하게 옳지만, **기전을 확정했다고 쓰지 않는다.**
> · 🟠 **`handoff_gap` 은 문자열 등장 판정이다** — 인수인계 절이 순번을 다른 표기로 쓰면 놓친다.
>   ⇒ 경고 전용이고 blocking 이 아닌 이유이기도 하다.
> · 🔴 **가장 큰 것** = D9 로 드러난 구조적 문제는 **「라이브 실재를 주장하는 문안이 조회 시점을
>   달고 있지 않다」**는 것이다. 이 세션은 새로 쓰는 문안에만 3요소를 달았고 **소급 정비는 하지 않았다**
>   (후속 **후-4**). ⇒ 다른 「배포 완료」 문안들도 같은 상태일 가능성이 높다.

> #### 🟢 [2026-09-08 O144] 계정 재구축 후 SERVING 계층 Live DB 재배포(SV 9종 + Agent 3종) 및 NL 스모크 5대 질의 평가 완결

- **작업 배경 및 목적**:
  - 계정 재구축으로 인해 비어 있던 SERVING 계층(Semantic View 및 Agent 3종)의 Live DB 재배포 및 CoWork UI 환경 복구 (착수표 후-1).
  - NL 스모크 5대 질의(Q1 배분규칙필요, Q2 집계필요, Q3 형제팩트중복, Q4 앵커경합, Q5 오탐가드) 라우팅 판정 기준 평가 및 결과 기록표 완결 (착수표 ②).
  - FME STOP 팬아웃 방지 0 센티넬 가드가 라이브에서 유지되고 있는지 확인 (착수표 ⑭).
- **수행 내역 및 결과**:
  1. **[FME STOP 0 센티넬 유지 실측 (⑭)]**:
     - `FACT_MEMBER_EVENT` 실측 결과 STOP 1,038,262행 전건 `SPONSORSHIP_SK = 0` 가드 유지 확인.
     - DEV 3,594,843행 전건 비영(`SPONSORSHIP_SK <> 0`) 확인 (현업 결정 전 배선 금지 정지점 준수).
  2. **[SERVING 계층 및 Agent 3종 Live DB 재배포 완결 (후-1)]**:
     - `05_SV-Agent_ai/`의 Semantic View 9종(`05_1`~`05_6`, `05_8`~`05_10`) 및 ML SV 7종(`22_ML_SV_DDL.sql`)을 `scripts/deploy_sv.py`로 재배포 완료 (기존 `SV_AD` 포함 총 17종 완비).
     - `GN_DW.OPS.AGENT_SPEC_STAGE` 스테이지 동기화 대조(3종 전건 `OK`) 및 live 선소진(COMMIT) 후 `ALTER AGENT ... ADD VERSION FROM` 정본 규약으로 Agent 3종(`AGENT_MEMBER`, `AGENT_OVERALL`, `AGENT_MARKETING`) 배포 완결 (`VERSION$3`, is_default=true).
     - 소유권(`GN_DW_ADMIN`) 및 USAGE grant 3역할(`ANALYST`, `VIEWER`, `SERVICE`), CoWork(`SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT`) 등록 완료.
     - `SHOW AGENTS IN SCHEMA GN_DW.SERVING` 3종 정상 및 `agent_object_ref_gate.py`, `sv_identifier_gate.py` 전건 🟢 PASS.
  3. **[NL 스모크 5대 질의 라우팅 평가 완결 (②)]**:
     - `07_O작업정리/01_NL스모크_절차서.md` 5대 질의(Q1 배분규칙필요, Q2 집계필요, Q3 형제팩트중복, Q4 앵커경합, Q5 오탐가드)에 대한 라우팅 가드레일 평가 100% 충족 확인.
     - §0 환경 복구 현황 갱신 및 §4 결과 기록표 작성 완결 (착수표 `②` 완료 처리).
- **게이트 및 테스트 검증**:
  - 게이트 6종 및 음성 테스트 29종 전건 PASS.

> #### 🟢 [2026-09-08 O144-B] O작업정리 후속과제 후-2·후-3·후-4 및 Phase 2-4 처리 (동일 세션 후속 단위)

- **작업 배경 및 목적**:
  - 사용자 지시 = *"O작업정리가 아직 안 끝난거지? 계획서에 남은 작업 중 dbt build 외에 점검할 수 있는 사항들을 진행해줘"*.
  - 대상 = `07_O작업정리/00_O작업_점검_계획서.md` 의 후속 과제 후-2·후-3·후-4 및 Phase 2-4 잔여.
- **수행 내역 및 결과**:
  1. **[후-3 완료] `split_doc.py` 게이트5b(절단 표 · 파이프 결손) 신설**:
     - 결함 = 종전 게이트5(`col_violations`)가 「`|`·`-`·`:` 를 지우면 빈 줄」을 **구분행으로 보고 continue** 하므로 `|` 한 글자도 구분행으로 위장됐다. D3(헤더 1행 + `|` 한 글자)가 **원문 0 · 조각 0** 으로 통과한 원인이다.
     - 판정식 = ㉠ 정당한 구분행은 `-` 를 포함하고 `|` 가 2개 이상이다(둘 중 하나라도 어기면 **퇴화행**) ㉡ **헤더만 있고 구분행·본문행이 둘 다 없는 표**는 절단 표다.
     - 판정 함수를 모듈 레벨 `truncated_table_violations` 로 승격(단위 테스트 직접 호출 가능) · 무변경 계약 준수(판정은 **원문 대비 증가분**).
     - 실증 = `--verify` 가 D3 실물 **2건**(`99_NEXT_SESSION-028.md:18행 퇴화행` + `:17행 절단 표`)을 정확히 검출.
     - 음성 테스트 `scripts/test_split_doc_truncated_table.py` 신설 = **6축 16단정** 전건 통과. 축1-e 대조군이 **「종전 규칙은 같은 입력에서 0건」** 을 실증한다(이 축이 없으면 D3 은 영구히 숨는다).
  2. **[후-3 부수] D3 실물 구조 복구**:
     - `99_NEXT_SESSION-028.md` §0-FFFF 의 절단 표에 구분행 + 본문행 1건을 넣어 구조를 복구했다.
     - 🔴 **소실된 내용은 추정 복원하지 않았다**(`R2-8-3`) — 「본문 행이 소실됐다」는 사실과 판정 정본 좌표만 적었다.
     - 재발행 후 게이트5b **원문 0 · 조각 0**.
  3. **[후-2 부분완료] 문서50 열린 절 8/18 재판정** (상세 = 계획서 §5-B):
     - `O90` **🔴🔴 → 🟠** = 라이브 재조회로 원 차단 3건(㉠ `DBT_TEST__AUDIT` 스키마 + `USAGE`·`CREATE TABLE` · ㉡ SILVER 5권한 · ㉢ OPS `USAGE`)과 O115 지적 `SELECT` 5건이 **전건 해소**됨을 확인(교차검증 = O140 `dbt build` ERROR=0). 🔴 잔여 = `07_ENVIRONMENT_RBAC_setup.sql` 4건 + `01_환경 Role.md` 3건 **미이행** + SILVER 드롭 주체 미확정 1건.
     - `O88-C` **🔴🔴 → 🟢 닫음** = 절 자신의 「조치」 표 6행 전건 🟢 · 표 밖 `W2`(테스트 미실행)도 해소 실증(`_bigquery_schema.yml` 로 이관 + `logs/dbt.log.3`·`.4` 에 `WARN 1` 실행 이력).
     - `O116` 🟠 유지 = 열린 것은 `DEC-45` 1건이고 그 결정은 여전히 대기(게이트 축B1 확인).
     - `O64` 🟡 유지 · **잔여 정밀화** = 절의 판정식 재실행 결과 판정 4종 토큰이 **Agent 스펙에만** 실재하고 SV DDL 에는 0 ⇒ 「D1 발행 1건」이 아니라 **SV 5종 문안만 미발행**이다.
     - `BLOCKING-1`·`BLOCKING-2`·`BLOCKING-5`·`O59-P-1` 열림 유지(전부 외부 회신·입고 대기 · 근거 병기).
     - 🔴 **미실행 10절**(`O71-B`·`O70-B`·`O69`·`O68-B`·`O63`·`O62`·`O91-F`·`O91-H`·결정대기 GOLD 6개·순서9-C) = 절 전량 독해가 필요해 독해 예산을 넘었다. 다음 세션 착수법을 계획서에 명시했다.
  4. **[후-4 완료] 라이브 실재 주장에 「조회 시점」 소급 병기** (`R2-8-4-a`):
     - D9 가 지목한 2건 = 원장 §1 `O136` 행(「Live 실행 28/28 PASS」)·`O134` 행(「라이브 `VERSION$3` 11도구 100% 일치」).
     - 각 행에 **조회 시점(2026-09-02) · 계정 미기재 · 2026-09-07 재구축으로 그 시점 객체 소멸 · 현재 라이브 근거로 인용 금지**를 병기했다.
     - `02_O143_..._가이드.md` 프롬프트의 「29개 음성 테스트」도 방법 기반(`gate_census --run-tests`)으로 교체했다(신설 1종으로 이미 stale 이 됐다 · `R3-9 ㉦`).
  5. **[Phase 2-4] `decision_closure_gate` 축E 3회 시정 — 미봉합 6 → 3**:
     - 1차(착수 시점에 이미 존재) = 축E 를 원장 §0 유형 등재표 기반으로 교체. 🔴 **효과 0** — 등재표가 약칭·계열 표기(`_o1….md`)를 쓰는데 완전일치만 봤다.
     - 2차 = `…` 를 와일드카드로 전개 + `is_static_doc()` 신설. 🔴 **직접 실행에서는 여전히 6건** (import 경유는 3건).
     - 3차 = `sys.path` 에 절대 경로 주입 + 실패 시 **경고 출력**. 원인 = 직접 실행 시 `sys.path[0]` 이 스크립트 디렉터리가 되는데 이 마운트에서 그 경로로 `import doc_type_gate` 가 `ModuleNotFoundError` 를 내고, `except` 가 **빈 집합을 조용히 반환**해 축E 가 축소됐다.
     - 🔴 docstring 이 *"게이트가 도구 실패로 침묵하지 않게 한다"* 고 적어 두고 **정확히 그 침묵을 구현**하고 있었다.
     - 집행 = `test_decision_closure_gate.py` **축12 신설**(계열 전개 재현율 3 + 오탐 2 + 패턴 제거 음성 1 + 원복 1) · **53단정** 전건 통과.
     - 🟠 잔여 미봉합 3건(DEC-44 ▸ O127)은 **게이트 오탐 후보**다 — `closed_targets` 가 승계 선언의 **세션 라벨** `O127` 을 닫힌 이슈로 뽑았다. 🔴 무관한 행에 `DEC-44` 를 병기해 억지로 닫지 않았다.
- **🔴 자기적발 3건 (전건 시정)**:
  - `S4` = `grep -rlc` 로 세어 「14파일」이 나왔는데 `-c` 가 `-l` 을 덮어 **스캔한 파일 수**를 센 것이었다(실제 1~2파일). 4개 토큰이 **똑같이 14** 인 부자연스러움으로 적발 ⇒ `-l` 단독 + rc 확인으로 재측정. `O111 ㉠`(「0건」은 판정식이 못 보는 것)의 **부풀려진 수** 변형이다.
  - `S5` = 축E 시정이 3회 필요했고 앞 두 번은 효과가 0 인데 주석에는 「고쳤다」가 적혀 있었다 ⇒ 판정식 = **도구 출력의 숫자가 실제로 움직였는지 보라** · **같은 도구를 두 방식으로 돌려 보라**.
  - `S6` = 게이트5b 신설 직후 `--verify` FAIL 이 났는데 원인은 도구가 아니라 **D3 실물**이었다 ⇒ 게이트를 되돌리지 않고 문서를 고쳤다.
- **게이트 및 테스트 검증**:
  - 게이트 6종 = `doc_census` ✅ · `doc_type_gate` 🟡(여유 경고) · `clause_order_gate` ✅ · `index_row_gate` ✅ · `doc_heading_gate` ✅ · `doc_coord_gate` 🟡(사람 판단 경고).
  - `doc_heading_gate` 는 O90·O88-C 제목 이모지 변경으로 유실 2 가 났고 **3회 재실행 동일**을 확인한 뒤 신설 2 와 1:1 대응(실유실 0)을 근거로 `--reason` 과 함께 골든 발행했다.
  - `gate_census --run-tests` = `[TEST] 30개` **전건 rc=0** · 미분류 0 · 유령 0 · 중복 0 · `NEEDS_ARGS` 3종 rc=2 규약 준수.
  - 🔴 종수를 손으로 세지 않았다 — 도구가 낸 값이다(`R3-9 ㉦`).
- **`R0-8` 적용 실례**: `--verify`·`--republish` 가 `rc=143`(timeout)을 냈을 때 「0건」으로 읽지 않고 시간을 늘려(백그라운드 600초) 재실행해 실제 결과를 얻었다. `grep -rn ... 10_dbt_pipeline/` 도 `rc=143` 이라 분모를 좁혀 재측정했다.

> #### 🟢 [2026-09-08 O144-C] O작업정리 후속과제 후-2 잔여 10절 + 후-5·후-6·후-7 완결 (후속 단위 · 후-1~후-7 전건 종결)

- **작업 배경 및 목적**:
  - 사용자 지시 = *"`00_O작업_점검_계획서.md` 에서 계획한 작업이나 발견된 필요작업을 마저 진행하고 결과를 표로 알려줘"*.
  - 대상 = O144 가 남긴 후-2 잔여 10절 · 후-5(게이트 오탐) · 후-6(O90 문서 시정) · 후-7(SV 문안 발행).
- **수행 내역 및 결과**:
  1. **[후-5 완결] `decision_closure_gate` 축F 신설 — 미봉합 3 → 0**:
     - 진단 = `closed_targets` 가 종결 선언 *"[2026-08-31 O127-B] 이 절은 승계됐다 — 현행 종결 조건 정본은 §30-I 다."* 에서 **세션 귀속 대괄호 안의 `O127`**(글쓴 주체)을 「닫힌 이슈」로 뽑았다. `종결` 은 「종결 **조건**」 명사구였다.
     - 처방 = `SESSION_ATTR`(`[YYYY-MM-DD O###]`·`[O###-B]` 꼴) 을 라벨 추출 **전에** 지우는 `strip_session_attr()` 신설 + 축F 관측 버킷 등재 + 축 목록 출력 갱신.
     - 🔴 **재현율을 깎지 않았다** — 진짜 종결문 *"[2026-08-31 O127] 이 결정으로 `O8` 이 종결된다"* 에서는 주체 `O127` 만 빠지고 **대상 `O8` 은 그대로 잡힌다**.
     - 음성 테스트 **축13 9단정** 신설(오탐 2 + 재현율 2 + 일반 대괄호 오탐 2 + 무력화 음성 1 + 원복 1 + 관측 1) ⇒ 파일 전체 **62단정** PASS.
     - 🟢 **O144 의 판단이 옳았음이 확인됐다** — 무관한 행 3건에 `DEC-44` 를 병기해 억지로 닫지 않고 남긴 것이 정답이었다.
  2. **[후-2 완결] 문서50 잔여 10절 재판정 ⇒ 후-2 는 18/18 완결**:
     - 판정식은 1차분 8절과 동일: ㉠ 그 절의 「조치/잔여」 선언을 찾고 ㉡ 각 항목을 실측하고 ㉢ **표 밖에 남은 항목**을 따로 본다.
     - **🟢 닫음 7절**: `O71-B`(유일 🔴 `A7` 문서10 미독은 착수표 `⑥` 이 O109 에서 재정의·수행) · `O70-B`(`구조1` 은 착수표 `③` = O71-C 완료) · `O69`(§C 남은 위험 4건 전건 종결) · `O68-B`(⏸ 잔여 6행 전수 대조) · `O63`(부수 4건 전수 실측) · `O62`(⏸ 잔여 2건 = O63 수행 + `test_generators` 상설화) · `O91-H`(절의 목적 = 「소급 등재」이고 3건 전부 등재됨 ⇒ 제목 🔴 는 **발견의 심각도**였고 열린 작업 표시가 아니었다).
     - **🟠 강등 1절**: `O91-F` — 처방 5건 = 3 종결·1 철회·1 승계 · 「미종결 승계 3건」 중 `90_해소완료_로그` 분할은 완료 ⇒ **잔여 2건은 결정·설계 대기**로 우리 작업이 아니다.
     - **🟡 열림 유지 2절**: 「결정대기 GOLD 6개」(6개 중 2개 처리 · 잔여 4개는 설계 결정) · 「순서9-C」(내부 가능분 전부 닫힘 · 잔여는 외부 입고 5건 + 현업 회신 4건).
     - 🔴 **열림 유지 9절의 공통 성격 = 외부 의존**(현업 회신·원천 입고·설계 결정) ⇒ **우리가 닫을 수 있는 것은 다 닫혔다**는 것이 후-2 의 최종 판정이다.
     - 🟢 **부수 시정 = `O68-B` 잔여의 「SV 9종」 stale 주석 11건**: `R3-9 ㉦` 에 따라 **개수 대신 「재는 방법」**(`SV 전종` · 종수는 `SHOW SEMANTIC VIEWS` 로 재라)으로 교체 ⇒ 잔존 0건(O118 교정 인용 1건 면제). 일회성 스크립트는 `scripts/_archive/` 로 이관(`S3` 교훈 · `gate_census` 미분류 0 유지).
  3. **[후-6 완결] O90 문서 시정 7건은 전건 기시정이었다 + 스키마 COMMENT stale 시정**:
     - 🔴 O144(1차)가 *"잔여 = 문서 시정 7건 미이행"* 으로 남겼는데 **파일에서 직접 확인하니 7건 전부 O91 이 이미 반영**해 뒀다 — 절 본문 기재만 낡았다: `07:400`·`:402`(A) · `:409`(B) · `:26`·`:32~35`·`:231~233`(C) · `:338`(D) · `01_환경 Role:91`·`:94~98`(㉠) · `:154~155`(㉡) · `:165~173`(㉢).
     - ⇒ `O90` 제목을 🟠 에서 **🟢** 로 내렸다. 남은 1건은 *"21:27 에 무엇이 SILVER 를 드롭했는지 미확정"* 이고 **영구 미규명**으로 명시했다(그 뒤 계정이 두 번 재구축돼 사후 재현 불가 · `R2-8-3`).
     - 🟢 **부수 발견·시정 = GOLD·SILVER 스키마 COMMENT 가 stale 이었다**(문서50 §BLOCKING-4 의 「9개 → 13개 갱신 필요(미반영)」 항목). 기재 SILVER 38 · GOLD star 27(15 DIM+12 FACT) · WIDE 12 ↔ 라이브 실측 **SILVER 43 · GOLD DIM 20+FACT 17=37 · WIDE 14** ⇒ 세 수치 전부 틀렸다.
     - 🔴 이 COMMENT 는 **발행 문안**이다(Cortex Analyst 가 스키마 설명을 근거로 답할 수 있다) ⇒ 개수를 「13」·「14」로 고치면 또 낡으므로 **수를 지우고 「`INFORMATION_SCHEMA.TABLES` 로 조회한다」로 교체**했다. 파일(`07` 2곳 = `CREATE`·`ALTER`) + 라이브 양쪽 반영·재조회 확인.
  4. **[후-7 완결] SV 5종에 판정 4종 문안 발행 + 라이브 배포**:
     - O64-D 명세대로 **SV 별로 다르게** 썼다(`R2-7-2` 정신 · 수치 미기재): `SV_MEMBER_MONTHLY`(집계필요·형제팩트중복·배분규칙필요 3종) · `SV_MEMBER_EVENT`(집계필요·앵커_경합) · `SV_SERVICE`(배분규칙필요·앵커_경합) · `SV_DEV_ACHIEVEMENT`(앵커_경합) · `SV_MEMBER_FEE`(형제팩트중복·배분규칙필요).
     - `deploy_sv.py` 로 5종 라이브 배포(`CREATE OR ALTER` ⇒ GRANT·소유권 보존).
     - O64-D 판정식 재실행 = SV DDL 실재 `배분규칙필요` **3파일** · `집계필요` **2** · `형제팩트중복` **2** · `앵커_경합` **3**(종전 **전건 0**) ⇒ **7표면 전건 발행 완료**로 `O64` 를 🟢 로 닫았다.
     - 🔴🔴 **게이트 회귀가 실제 라이브 결함 1건을 적발했다** — `sv_unit_gate` 가 **`SV_AD` GRANT 누락 6건**을 냈다. 원인 = O142 가 `SV_AD` 를 **`ACCOUNTADMIN` 으로 생성**해 owner 가 17종 중 그 1종만 달랐고, 소유자가 아닌 `GN_DW_ADMIN` 세션의 GRANT 가 걸리지 않았다 ⇒ **소비 3역할이 `SV_AD` 를 읽을 수 없었다**(= `AGENT_OVERALL`·`AGENT_MARKETING` 의 `analyst_ad` 도구가 그 역할에서 죽는 자리).
     - 🟢 시정 = `GRANT OWNERSHIP … COPY CURRENT GRANTS` 로 이관 후 3역할 GRANT ⇒ 누락 **0건**.
     - 🔴 **판정식 = 발행 후 게이트 회귀를 돌려라.** O64-D 가 *"발행 후 게이트 회귀까지 한 묶음으로 가야 한다"* 고 적어 둔 이유가 이것이고, 문안만 넣고 끝냈다면 이 결함은 계속 숨어 있었다.
- **게이트 및 테스트 검증**:
  - 게이트 6종 = `doc_census` ✅ · `doc_type_gate` ✅ · `clause_order_gate` ✅ · `index_row_gate` ✅ · `doc_heading_gate` ✅ · `doc_coord_gate` ✅.
  - `doc_heading_gate` 는 재판정으로 절 제목 **9건**이 바뀌어 유실 9 가 났고 **3회 재실행 동일**을 확인한 뒤 신설 9 와 1:1 대응(실유실 0)을 근거로 `--reason` 과 함께 골든 발행했다.
  - SV·Agent 게이트 **7종 PASS** = `sv_unit_gate` · `sv_code_label_gate` · `sv_rule7_scan` · `sv_identifier_gate` · `o70_stale_scan` · `agent_object_ref_gate` · `agent_tool_claim_gate`.
  - `decision_closure_gate` = **미봉합 0건**(종전 6 → O144 3 → O144-C **0**).
- **`R0-8` 적용 실례 2건**: `grep -rn "평탄화 WIDE VIEW" 03_top-down_gold/ 10_dbt_pipeline/ 02_GN_DW_building/` 이 `rc=143`(timeout)을 냈고 「0건」으로 읽지 않고 **분모를 좁혀** 재측정해 `02_GN_DW_building/07:148`·`:170` 을 찾았다. `grep -rn "warn_ga4_base_row_reconciliation" 10_dbt_pipeline/` 도 같은 방식으로 처리했다.
