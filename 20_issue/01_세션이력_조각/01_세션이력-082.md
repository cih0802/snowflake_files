<!-- SPLIT-CHUNK 01_세션이력.md | 082/082 | 허브 = 01_세션이력.md | 원문 1~139행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-22 O180-B] 문서20·문서50 증분형 재정의 + 강제 장치 신설(인수인계 【2】 종결) · 🔴🔴 내가 심은 가드가 조용히 죽어 그 연산을 실수로 집행했다
>
> 🔒 라벨 = 같은 세션 후속 단위이므로 **접미 `-B`** 로 잇는다(`R1-4-3` 선점 등재).
> 사용자 결정 = 「`20_현업확인_요청`·`50_dbt_파이프라인_미결조치` **둘 다 증분형**으로 재정의 ·
> 인수인계 【2】~【4】도 닫자」 ⇒ 실제로 닫힌 것은 **【2】 전건**이고 【3】·【4】는 이월했다
> (사용자 결정 = 「지금 기록부터 닫기」 · 규모 보고 후 선택).
>
> ---
>
> **㉠ 착수 실측 — 공백은 유형이 아니라 강제 장치였다**
>
> 【2】㉢ 이 *"`--rollover` 가 갱신형을 거부하는지 확인"* 을 요구했다. 답 = **거부하지 않는다.**
> 🔴🔴 그리고 그 확인 과정에서 더 큰 것이 나왔다 — **`split_doc.py` 는 원장 §0 유형 등재표를
> 한 번도 읽지 않았다.** `grep -n "갱신형\|append형\|KINDS\|유형"` 전수 = 주석 언급뿐이고
> 판정 호출이 **0건**이었다.
> ⇒ 유형 선언은 `doc_type_gate` 의 **여유 판정에만** 쓰였고 **연산을 막지 못했다**:
> · `--rollover` 는 유형 무관하게 동작한다(그래서 갱신형에도 쓸 수 있었다).
> · 🔴 **`--rebalance` 가 증분형 좌표를 깨는 것을 막는 장치가 없었다.**
> 🟢 **판정식 = 「선언」과 「강제」는 다른 것이다** — 등재표는 문서였고 도구는 그것을 몰랐다.
>
> **㉡ 유형 `증분형` 신설 (다섯째 유형)**
>
> **정의** = 항목 본문은 **append**(신규 문항·절이 꼬리로만 간다) + **판정 줄만 제자리 갱신**
> (`**판정**:` 채움 · 절 제목 이모지 승격 ⇒ 비꼬리 조각도 **소폭** 바뀐다).
> 🔴 **왜 기존 둘로 안 되는가** = 두 축이 **서로 다른 조각**에서 일어난다.
> · 갱신형으로 보면 **처방이 틀린다** — 상한 초과에 `--rebalance` 를 권하는데 그것이
>   조각 번호를 밀어 타 문서의 `-0NN.md:행` 인용 좌표를 **전부 깨뜨린다**.
>   🔎 실증 2건 = O179 가 문서20 `-010` 을, O180 이 문서50 꼬리 조각을 **둘 다 수기 신설**해
>   그 처방을 피했다 ⇒ **관례가 도구를 앞질렀고 도구는 틀린 처방을 계속 냈다.**
> · append형으로 보면 **불변식이 거짓**이다 — append형은 「비꼬리 조각 불변」을 전제하는데
>   이 두 문서는 회신·종결이 오면 과거 조각의 판정 줄을 고친다.
> 🟢 전환 근거(문서별 실측)
> · 문서20 = 종전 근거 *"회신되면 항목이 나간다(제자리 변경)"* 가 **관측과 어긋났다** —
>   종결절 **10** · 열린 문항 **28** 이고 **은퇴로 줄지 않는다**(원인 = 현업 회신 대기 누적).
> · 문서50 = 종전 근거 *"BLOCKING-N 상태 전이"* 는 **절반만 맞았다** — 전이는 제목·처분 줄
>   한정이고 세션마다 **새 절이 꼬리로** 추가된다(`O179-1`·`-2`·`-3`·`O180-1` 전부 신설 절).
> 🟢 게이트 편입 = `doc_type_gate.KINDS` 에 `증분형` · `TAIL_KINDS = ('append형','증분형')`
> ⇒ **여유를 꼬리 기준으로** 본다(문서20 29,451→**34,415** · 문서50 30,204→**35,942**).
> 🟢 상한초과 **처방 문안도 유형별로 갈랐다** — 증분형에는 `--rollover` 를 권한다.
>
> **㉢ 🔴🔴 강제 장치 = `split_doc.declared_kind()` + 증분형 `--rebalance` 거부**
>
> 이것이 이 단위의 실질 산출물이다. `declared_kind()` 가 `doc_type_gate.parse_registry` 를
> 호출해 선언 유형을 읽고, `rebalance()` 가 증분형이면 **rc=1 로 거부**한다.
> 탈출구 = `--force-rebalance`(🔴 좌표 파괴 감수 · 파괴 작업이므로 `R4-4-3` 승인 대상 ·
> 뚫으면 `doc_coord_gate` 로 전수 복구해야 한다는 경고를 출력한다).
> 🟢 판정 정본은 **하나**다 — `parse_registry` 를 재구현하지 않았다(`R1-6-17` 「같은 것을
> 다르게 재는 지점」 축).
>
> **㉣ 🔴🔴 확정위반 1 — 그 가드가 1차 구현에서 조용히 죽었고, 나는 그 연산을 실제로 집행했다**
>
> 함수명을 **`read_registry`** 로 호출했다(정답 = `parse_registry`). 그리고 **내가 같은 커밋에
> 넣은 `except Exception: return None` 이 그 `AttributeError` 를 삼켰다.**
> ⇒ `declared_kind` 가 **전건 `None`** 을 돌려 가드 분기가 통째로 건너뛰어졌고,
> 내가 「거부되는지 확인하겠다」며 돌린 `--rebalance` 가 **문서50 에 실제로 집행됐다.**
>
> 📏 피해 실측(즉시 전수)
> · 조각 **23 → 24** 재기록 · 🟢 **본문 바이트 동일**(concat SHA256 일치) · **유실 0** · 상한 초과 0
> · 좌표 인용 **19건 전수 검증** = **17건 🟢 정확 유지** · 2건 🟠 빈 줄
> · 🟢 **그 2건은 내 사고가 아니다** — `_o170_evidence.md`(2026-09-17 근거철)가 `-014:180`·
>   `-015:18` 을 가리키는데 같은 내용이 지금 `-017:180`·`-018:36` 에 있고 `00_BRIEF` 는 그
>   새 좌표를 🟢 로 가리킨다 ⇒ **이전 세션 재균형이 이미 깬 좌표**이고 아무도 몰랐다.
>   🔴 이유 = **`doc_coord_gate` 는 경로만 보고 줄 내용을 보지 않는다** ⇒ 가드의 필요성이
>   내 사고로 실증됐다(역설적으로 그 사고가 가드를 정당화했다).
> · 🟢 부수 효과 = 문서50 최대 조각이 **327줄 → 203줄** 로 여유를 회복했다.
> · 🔴 내가 만든 피해 = 내가 이번 세션에 쓴 `-023` 인용이 **`-024`** 로 밀렸다
>   ⇒ 4곳 전수 정정(문서50 `-024` 본문 · 원장 O180 행 · 이력 §O180 `-081` · 인수인계 `-O0180-A`).
>
> 🟢🟢 **판정식 신설 = 「안전측 폴백」이 가드를 죽이는 가장 흔한 경로다.**
> 폴백은 **「게이트가 고장났다」와 「이 문서는 미등재다」를 구별해야 한다** — 둘을 같은 `None`
> 으로 뭉개면 고장이 **정상 동작처럼** 보인다. ⇒ 예외를 좁히고 **stderr 경고를 출력**하게 고쳤다.
> 🔴 그리고 더 일반적인 교훈 = **「가드를 심었다」는 「가드가 동작한다」가 아니다.**
> 심은 직후 **거부되는지 실증**하라(나는 그 실증을 했고, 그래서 사고를 즉시 발견했다 —
> 실증을 안 했으면 다음 세션이 「가드가 있다」고 믿고 좌표를 계속 깼을 것이다).
>
> **㉤ 음성 테스트 `test_doc_type_increment.py` — 19단정**
>
> 축 = 등재 4 · 판정 5 · 여유 2 · **회귀 3** · 차단 5.
> 🔴 **회귀 축이 `R3-2` 「고치기 전 구현으로 돌려 실패 실증」을 이행한다** —
> `parse_registry` 를 숨겨 고장을 재현하면 ㉠ `None` 이 재현되고 ㉡ **경고가 실제로 출력되며**
> ㉢ 되돌림 해제 후 **정상 복귀**한다. 즉 1차 구현의 **침묵**과 그 시정을 둘 다 단정한다.
> 🟢 차단 축은 **파일을 쓰지 않는다** — `force=False` 거부 분기(rc=1)와 타 유형의 유형 판정만
> 단정한다(테스트가 정본을 재분할하면 그 자체가 사고다).
> 🟢 역방향 오탐 축 = 갱신형·append형 3문서가 **증분형이 아님**을 단정한다.
>
> **㉥ 【2】㉢ 전제 기각 — 수기 조각은 이미 정규였다**
>
> 인수인계가 *"`-010` 은 수기 신설이라 `--verify` 원문 바이트 대조가 이 조각에 성립하지 않는다"*
> 고 적었다. 🔴 **거짓이다** — `--verify` 는 원문이 아니라 **발행 기준 concat SHA256** 과 대조하고
> 게이트2 가 *"원문 1071 · concat 1814 (재발행 — 차이 허용)"* 을 명시한다.
> 실측 = 문서20 `--verify` **rc=0 · 유실 0 · 상한 초과 0**.
> ⇒ 「원문 대응 없음」은 결함이 아니라 **사실 기재**다(append형 rollover 조각도 같다).
> 🟢 `J4` class(「선행조건이 저절로 충족됐다」) ⇒ 헤더의 낡은 예고문만 종결 기재로 교체했다.
> 🟢 문서50 쪽은 **내 사고가 부수적으로 정규화**했다 — `-024` 헤더가 도구 생성 형태
> (`원문 3478~`)로 다시 쓰였다.
>
> **㉦ 스킬 정본 갱신 (【2】㉣)**
>
> `00_guides/03_init_ihcho_스킬_본문.md` 3곳(여유 5,592 B 🟠 라 최소 문안):
> ① 브리핑 템플릿에 `Jinja <판정>` 추가(O180 신설 게이트 노출) ② `R4-4-3` 파괴 작업 절에
> **증분형 `--rebalance` 거부 + 실사고** 기재 ③ §4-2 문서 신설 절차에 **증분형 판정식**
> (*"은퇴로 건수가 줄어드는가 — 줄지 않으면 append 축이다"*) 추가.
> 🔴 빌더는 **dry-run 기본**이라 `--apply` 를 붙여야 집행된다(1차에 빠뜨려 검증기 `I1` FAIL).
> 🟢 재바상 후 `verify_init_ihcho_skill.py` **rc=0**(`I1`~`I8` 전건 · 420줄 35,347 B).
>
> ---
>
> **📏 종료 실측 (2026-09-22 · O180-B)**
>
> · 게이트 **7종 rc=0** = `doc_census`(stale 0 · 분할 분모 위반 0) · `doc_type_gate` ·
>   `doc_coord_gate`(깨진 경로 0) · `doc_heading_gate`(제목 유실 0) · `index_row_gate` ·
>   `clause_order_gate` · `jinja_config_gate`(blocking 0)
> · 🆕 `test_doc_type_increment.py` **19/19** · 스킬 검증기 rc=0
> · `declared_kind` 판정 5축 = 문서20 **증분형** · 문서50 **증분형** · 이력 **append형** ·
>   원장 **갱신형** · 해소완료 **append형** (🔴 종전 전건 `None` 이었다)
> · 가드 실증 = 문서50·문서20 `--rebalance` **rc=1 거부** · 타 유형 3종 비적용
> · 문서50 조각 **24** · 최대 **203줄**(사고 전 327줄) · 문서20 조각 **11** · 최대 297줄
> · 원장 O180-B 행 **1,453자**(≤1,900) · 파이프 5
>
> **🔴 확정위반 2건**
>
> 1. 🔴🔴 **가드를 심고도 그 가드의 구현 결함으로 보호 대상에 파괴 연산을 집행했다**
>    (함수명 오류 + 광범위 `except` 침묵). 피해 = 조각 23→24 · 좌표 인용 2건 재정정 4곳.
>    🟢 즉시 전수 측정·복구하고 판정식을 신설했다.
> 2. 🔴 **`edit` 컨텍스트 미리보기를 위치 근거로 읽어 정상 편집을 「사고」로 오진**했다
>    (원장 행이 `:87` 대시보드에 정상 삽입됐는데 등재표 오염으로 읽었다).
>    🟢 `grep -n` 재확인으로 즉시 정정 — 인수인계 【7】이 경고한 그 함정이고, **검증 절차가
>    오진을 잡았다**(도구 보고를 위치 근거로 쓰지 마라).
>
> **🟠 이월 (사용자 결정 = 「지금 기록부터 닫기」)**
>
> · 【3】 **I2 MUTATES 무플래그 11건** — 착수 실측 완료: 전건 실재 · 전건 무플래그 ·
>   진입점 구조 = `main()`+`__main__` **8건** / **최상위 실행 3건**(`_o169_sv_redeploy` ·
>   `_o170_reserve` · `_o170_handoff_append` ⇒ 🔴 **실행 = 즉시 집행**이라 가장 위험).
>   🟢 처방 확정 = 공용 `--apply` 드라이런 가드를 심고 기지목록을 **0** 으로 만든다.
>   🟢 분류 재판정 1건 = `nl_routing_smoke` 는 DDL 이 없지만 **LLM 대량 호출(과금)** 이므로
>   `R4-4-3` 대상이 맞다 ⇒ MUTATES 유지가 옳다(OBSERVE 로 내리지 마라).
> · 【3】 **W3 결정·설계 대기 8건** · 【4】 **열린 문항 28건 전수 `J3` 실측** — 미착수.
