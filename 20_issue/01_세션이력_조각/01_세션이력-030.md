<!-- SPLIT-CHUNK 01_세션이력.md | 030/030 | 허브 = 01_세션이력.md | 원문 1~126행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-19 O87] GA4 SILVER 평탄화 통합 기반 테이블 신설 — `GA4-PK-1`·`GA4-LEN-1` 해소(코드) · DEC-37/38 확정
>
> **트리거** = 사용자 지시: *"지금 합쳐둔 EVENTS 테이블을 BIGQUERY_REFINED_DATA 라는 테이블명으로
> silver스키마에 적재하고, 이걸 이용해서 GA4테이블을 만들고 싶어(GN_DW.SILVER.BIGQUERY_REFINED_DATA).
> 이 요건이 GN_DW 설계상 맞는지 데이터 아키텍처 관점에서 검토하고, 그에 맞게 dbt 모델을 수정해줘."*
> + 이전 세션 로그(dbt GA4 5모델 실재 · 매크로 패턴 불일치 지뢰) 동봉.
>
> 🔒 **라벨** = `id_collision_gate` 「다음 O88」 ↔ **정의 최대 86** ⇒ **O87**(`P222` 참조축 과대 15연속).
>
> ##### 사용자 결정 4건
> | # | 결정 |
> |---|---|
> | ㉠ | 명명 = **기반 테이블만 `BIGQUERY_REFINED_DATA`** · 기존 `GA4_*` 5종 유지(전면 리네임 기각) |
> | ㉡ | 계층 규칙 = **좁은 예외를 DEC 로 명문화**(질의 「BRONZE→BRONZE→SILVER→GOLD 는 되고 BRONZE→SILVER→SILVER→GOLD 는 안되나」에 대한 답이 이 결정이다) |
> | ㉢ | `GA4-PK-1` = **`SRC_FILE_NAME` 계보 tiebreaker(손실 0)** |
> | ㉣ | 범위 = **코드·문서 수정까지 · build 전 정지** |
>
> ##### 🔴🔴 최대 발견 — 「SILVER→SILVER 파생 금지」는 사문(死文)이었다
> 문서 **4곳**이 명문 금지로 선언한다(`15` §113 · `02_SILVER_작업계획` §68·§107 · `09` 527·546·722행
> · `14` §59). **그런데 라이브 dbt 코드는 이미 31곳에서 하고 있었다**(`models/silver/` 전수 grep):
> `AGENCY_AD_ROW_DGT`/`_REBRDC`/`_VIDEO` → 성과·방송·사례·디지털 **7** · `CRM_CODE` → CRM 11모델 **20**
> · `CRM_MEMBER_SPONSOR_BIZ` → `CRM_SPONSOR_RELATION` **1** · `GA4_IDENTITY` → `IDENTITY_MEMBER_XREF` **1**.
> 🔴 그리고 `AGENCY_AD_ROW_*` **3종은 `02_DB_BRONZE_SILVER.md` §3.4 인벤토리에 등재조차 안 돼 있었다**
> (agency 2종만 기재) ⇒ 문서가 실태를 3개월 이상 못 따라온 상태였다.
>
> 🟢 **`AGENCY_COST` 삭제의 실제 사유는 「계층 내 참조」가 아니었다.** 사유 3개 중 결정력을 가진 것은
> **월 롤업**과 **중복**이다. 결정적 반증 = **같은 2026-07-14 리뷰가 `CRM_SEND_RESULT` 의 SILVER
> 집계는 *「검토된 집계로 명시」*하고 유지했다**(`03_SILVER_작업계획_CRM전용` 136행 · 근거 = 「롤업
> 아님 · 누출 0 · 원천 distinct 키 합 정확 일치」). 보강 = **GOLD 는 계층 내 참조를 15파일에서 한다**
> (WIDE 14뷰 + `FACT_GA_BEHAVIOR` 가 `DIM_GA_EVENT`·`DIM_GA_SOURCE` 를 `ref()`).
> ⇒ **DEC-37 신설** — 판정 기준을 「참조 방향」에서 **「연산 성격」**으로 옮겼다.
>
> 🔴 **역설 = 금지가 오히려 중복을 만들고 있었다.** `09` 722행이 *"단방향 유지 : `GA4_EVENT` 참조 금지
> → BRONZE 에서 세션 채움 CTE 재계산"* 을 강제한 결과 `event_params` 승격 `GROUP BY` 절이
> `GA4_EVENT`(11컬럼) ↔ `GA4_EVENT_DIM`(4컬럼) ↔ `GA4_IDENTITY`(5컬럼) **세 가지로 갈라졌고**,
> `DEVICE_TYPE` CASE 는 2곳에 복제돼 2026-07-27 교정 때 두 곳을 따로 고쳐야 했다.
> **그 불일치는 어떤 게이트에도 걸리지 않았다** ⇒ 금지의 목적(중복 소유권 차단)이 금지 때문에
> 달성되지 못하고 있었다.
>
> ##### 🟢 DEC-38 = 명명 이원화 (전면 리네임에 반대한 근거 4개)
> 사용자 질의 = *"도메인명을 기존 GA4를 전부 BIGQUERY로 바꾸는건 어떤가?"*
> ㉠ 🔴 **2026-08-14 개명의 목적이 정반대였다** — `_sources.yml` 7~12행 원문 *"GA4 외 BigQuery 유입도
> 수용하기 위해 원천 시스템 기준 명명으로 통일 … 개명 대상은 스키마명뿐 — `GA4_*` 모델명은 불변"*
> ⇒ **BRONZE 를 넓힌 이유가 SILVER 를 GA4 로 좁게 유지하는 것**이었다.
> ㉡ BigQuery 는 **전달 경로**다(Openflow·GA4 Data API·GCS 로 바뀌면 `BIGQUERY_EVENT` 는 거짓이 되지만
> `GA4_EVENT` 는 참) ㉢ 갈림이 BRONZE↔SILVER → SILVER↔GOLD 로 **이동만** 한다(소비 계층이 이미 GA 축)
> ㉣ 리네임 범위 대비 이득 0.
> ⇒ 사용자 결정 = **기반 테이블만 원천 접두**. 근거 = 기반 테이블은 도메인 해석을 하지 않고
> 「BigQuery export 를 평탄화한 것」이 내용의 전부다.
>
> ##### 🟢 하드블로커 2건 해소(코드)
> · **`GA4-PK-1`** = PK 4번째 키 `BATCH_ORDERING_ID` → **`EVENT_SEQ`**(`ROW_NUMBER()` · 계보
>   `SRC_FILE_NAME` 순 결정적 정렬) ⇒ **48,862,926행(17.10%) 복구 + 3.679% 중복 흡수 · 손실 0**.
>   `BATCH_ORDERING_ID` 는 nullable 컬럼으로 보존(계보·정렬 근거).
> · **`GA4-LEN-1`** = `VARCHAR(10)` → **`VARCHAR(64)`** + **`ID_SCHEME` 6분류** 신설
>   (`MBER_NO`/`ONCE_MBER_NO`/`APP`/`EMAIL`/`INVALID`/`UNCLASSIFIED`) + `XREF.MATCH_METHOD` 에
>   **`NOT_A_MEMBER_ID`** 신설. 🔴 **길이만 늘리면 안 되는 이유** = 종전 `S%`/`else` 2분기가
>   `app-`(241 id)·이메일(1)·문자열 `'null'`(1)을 **전부 `FDRM`/`MBER_NO` 로 밀어넣었고**, 그 상태로
>   확장하면 매칭 분모에 비회원 ID 가 섞여 채움률이 조용히 왜곡된다.
>   ⇒ **채움률 = `MEMBER_ID_EXACT` / (`MEMBER_ID_EXACT` + `UNMATCHED`)** 로 정의를 고정했다.
>
> ##### 🔴🔴 자기발견 — dbt 는 hook 을 「누적」한다 (에러 없는 사고)
> 프로젝트 레벨 `silver: +pre-hook: TRUNCATE` 에 모델 레벨 `config(pre_hook=범위 DELETE)` 를 더하면
> **더 구체적인 것이 덮어쓰는 게 아니라 둘 다 실행된다** ⇒ 전체가 비워진 뒤 범위만 append 된다.
> **에러가 나지 않고 행수만 줄어든다.** ⇒ `macros/silver_purge.sql` **단일 진입점**으로 구조 차단
> (분기는 매크로 안에서만 · 모델 파일에 `pre_hook` 금지). 처음 작성한 모델 2종의 `pre_hook` 을 제거했다.
>
> ##### 산출물
> · **신설 3** = `models/silver/ga4/BIGQUERY_REFINED_DATA.sql`(245줄) · `macros/silver_purge.sql`
>   · `macros/ga4_range_purge.sql`
> · **전환 6** = `GA4_EVENT`·`GA4_EVENT_DIM`·`GA4_DEVICE`·`GA4_TRAFFIC_SOURCE`·`GA4_IDENTITY`
>   · `IDENTITY_MEMBER_XREF` (전부 `ga4_union_shards` → `ref('BIGQUERY_REFINED_DATA')`)
> · **개정 5** = `_sources.yml`(**`bronze_bigquery.EVENTS` source 신설** — 종전 「동적조회라 선언 안 함」 폐기)
>   · `_ga4_schema.yml` · `dbt_project.yml`(vars `ga4_start/end` → `ga4_dt_start/end` · pre-hook 통합)
>   · `08_SILVER_테이블DDL`(38 → **39테이블**) · 설계문서 3종(`07`·`22`·`02_DB_BRONZE_SILVER` §3.4)
> · **폐기 표기** = `macros/ga4_union_shards.sql`(파일 삭제는 승인 대상이라 미실행)
> · **원장** = 00 §1 O87 행 · 30 §24 **DEC-37** · §25 **DEC-38** · 90 §1-A 갱신 + **§1-B 신설**
>
> ##### 🔴 검증 결과 (미완 포함)
> · `dbt parse` = **89 models · 345 tests · 41 sources** 로 정상 파싱(신설 1 + source 1 반영 확인).
> · 🔴 **`dbt compile` 미완** — 프로젝트 `profiles.yml` 은 account/user 가 빈 문자열이어야 하는
>   Snowflake-native 규약이라 샌드박스 CLI 에서 연결되지 않는다. `~/.dbt/profiles.yml` 에 database 를
>   보강했으나 **샌드박스가 매 dbt 호출마다 그 파일을 재생성**해 지워진다 ⇒ **SQL 컴파일 검증 미실행**.
>   ⚠️ 따라서 **SQL 문법 오류가 남아 있을 수 있다.** `dbt run` 전에 `dbt compile` 을 먼저 돌릴 것.
> · 게이트 = `index_row_gate` **534행 · 유실 0 · 중복 0** · `clause_order_gate` **88조문 · 역전 0** ·
>   `doc_type_gate` **선언 누락 0 · 상한 초과 0** · 🟠 **경고 1 = `90` 꼬리 여유 7,989B(<20%)**.
>
> ##### 🔴 자기위반 3건
> · **`R1-4` ID 게이트 미이행** — `--next DEC` 를 먼저 돌리지 않고 **DEC-36 을 오선점**했다.
>   DEC-36 은 이미 확정(구 `DEC-31` §22 FMM↔FMF 수렴 · 2026-08-12 O61 재번호). 코드·문서 **19곳**을
>   `scripts/_o87_dec_renumber.py` 로 DEC-37/38 로 일괄 정정(잔존 0 검증).
>   🔴 **O85 는 `O` 계열에 게이트를 돌렸는데 `DEC` 계열에는 돌리지 않았다** — 계열별로 따로 필요하다.
> · **`R1-7-8` 앵커 사고 2회** — ㉠ `GA4_EVENT_DIM.sql` 헤더 5줄 중복(new_string 에 헤더 재포함)
>   ㉡ `90_해소완료_로그.md` `DDL-ORDER-1` 행 중복(§1-B 표에 구 행이 남음). **둘 다 즉시 시정**.
>   ⇒ 이 결함은 O85 에서도 났고 **연속 재발**이다.
> · **`R4-4-1` 순서 역전** — 브리핑을 「Step 2 직후」가 아니라 조사·설계 검토를 상당히 진행한 뒤에 냈다.
>   조문은 「무조건 먼저」를 요구한다.
>
> ##### 🔴 환경 사고 2건 (둘 다 조문에 있던 함정의 실제 발생)
> · **마운트 교체** — 세션 중 `/workspace` 심링크가 `snowflake_files` → `user__public_default_`(My
>   Workspace)로 바뀌어 `dbt --project-dir` 이 「Path does not exist」를 냈다(`04_silver_design/07` §7
>   O73 등재 함정). 🟢 **손실 0** — `cortex ws ls` 로 GA4 7파일 전건 실재 확인 후 **직접 스테이지
>   경로**(`/snowflake/stages/user__public__snowflake_files_/`)로 우회해 작업을 계속했다.
>   ⚠️ 첫 `ls` 는 그 경로도 「No such file」을 냈는데 **재시도로 정상 복귀**했다(`OPS-3` 낡은 뷰).
> · **`C5` 부분 반환 실측** — `90_해소완료_로그.md` 가 스테이지 **33,792B** 인데 마운트가
>   **28,816B** 만 반환하고 UTF-8 이 마지막 바이트에서 절단됐다. 🔴 **해시 4회 동일**이라
>   torn read 가 아니라 **안정적으로 잘린 뷰**였다 ⇒ `R1-7-2` 의 해시 안정성 검사만으로는
>   이 경우를 못 잡는다(안정적이어서 통과한다). **스테이지 실체 크기와 대조해야 잡힌다.**
>   20초 대기 후 33,776B·decode OK 로 정상화 ⇒ 그 뒤에 편집했다. **잘린 버퍼로 쓰지 않았다.**
>
> ##### 🔴 신규 미결 3건 (전부 적재 후에만 판정 가능 · 정본 = `90` §1-B)
> · **`GA4-SEQ-1`** — `EVENT_SEQ` 재실행 간 안정성 미실증. 깨지면 PK 가 run 마다 재배치되어
>   GOLD 재적재 diff 가 무의미해진다.
> · **`GA4-DUP-1`** — `BIGQUERY_REFINED_DATA` ↔ `GA4_EVENT` 저장 중복 **약 50GB 환산**(실측 아님).
>   대안 = `GA4_EVENT` 를 뷰로 내리기(저장 0) — O87 범위 밖으로 보류.
> · **`GA4-TV-1`** — `smart tv` **499행**이 `(unknown)` 으로 격리된다(종전 09번은 `PC` 로 오분류).
>
> ##### 🔴 미갱신으로 남긴 것
> 「SILVER→SILVER 금지」 문구가 남은 문서 **4종**(`09`·`14`·`15`·`02_SILVER_작업계획`)은 갱신하지
> 않았다(역사 기록 성격이라 우선순위를 낮췄다). ⇒ 인용 시 **DEC-37 을 함께 볼 것**.
>
> ##### `read` 미반환·재호출
> **미반환 0 · 재호출 0**(판정식 충족). 단 위 `C5` 부분 반환 **1건**은 「미반환」이 아니라
> **「잘린 반환」**이므로 별도 축으로 기록한다 — `R1-3-7` 의 3형태(㉠ `edited out` ㉡ `Output too large`
> ㉢ 미리보기·꼬리)에 **해당하지 않는 네 번째 형태**다. 🟠 조문 보강 후보로 등재한다.

> #### 🟢 [2026-08-19 O88] GA4 3개월 샘플링 전환 · `dbt compile` 최초 통과 · 결함 3건 시정 · `07` 4판
>
> 🔒 **라벨** = `id_collision_gate` 「다음 O89」 ↔ **정의 최대 87** ⇒ **O88**(`P222` 참조축 과대 **16연속**
> — 참조 88 의 출처는 O87 이 남긴 「다음 O88」 예고 문장 2건뿐이다).
> 🔴 **자기위반 = `R1-4-3` 선점 등재 미이행** — 작업을 먼저 하고 원장 행을 나중에 썼다(O84 와 같은 결함).
> 🟢 **`R4-4-2`(㉡ 지시 동봉) 적용 세션** — 브리핑 출력 후 즉시 착수했다. 확정위반 = **1건**(위 `R1-4-3`).
>
> **착수 지시(사용자 3건)**
> ㉠ *"여태까지 진행된 dbt 작업 확인 및 수정사항 있는지 검토 및 수정"*
> ㉡ *"`50_handoff/07` 중 dbt로 작업을 지정하니 **bronze 에 데이터를 적재하는 부분만 남기고** 나머지 주석처리"*
> ㉢ *"권장순서에서 지시하는 걸 **문서 전체를 실행했을 때 건너뛸 건 자동으로 건너뛰도록**"*(O88-B)
> **범위 결정** = `bronze_bigquery` 가 너무 커서 **2024-06 · 2025-06 · 2026-06 3개월만 샘플링** ·
> **BigQuery 전체 수치 기재는 현행 유지**(강력한 수정 지시가 없는 한).
>
> **🔴🔴 발견1 — 계정이 또 바뀌었다(`P169` 4차)**
> 실측 `CURRENT_ACCOUNT()` = **`NX55103`** ↔ 문서·인수인계 전건 기재 **`UA93987`**.
> 계보 = `ls82944` → `os09358` → `DV07626` → `UA93987` → **`NX55103`**.
> `BRONZE_BIGQUERY` 에 **`EVENTS` 부재**(빈 `events_20260501`·`events_20260719` + `SYNC_ERR_INFO` 3개뿐
> = 04번 DDL 까지만 집행) ⇒ 「✅ 완료 (2026-08-18)」는 **구 계정 기록**이고 현 상태는 **재적재 대기**(`R2-8-4-b`).
> 🟢 반면 **SILVER 40테이블은 이미 선생성**돼 있었다 ⇒ 인수인계 항목 2「`08` DDL 의 GA4 구간 실행」은
> **이미 해소된 상태**였다(사용자 실행분).
> 🔴 부수 stale = 「SILVER **39**」 ↔ 실측 **40**. 차이 = `CRM_MARKETING_CAMPAIGN` 한 건만
> `CREATE TABLE IF NOT EXISTS` 로 선언돼 `CREATE OR REPLACE TABLE` 기준 계수에서 빠졌다(`08` DDL 1085행).
> **실재하고 COMMENT 도 있다** ⇒ 결함이 아니라 **계수 방식의 누락**이다.
>
> **🔴🔴 발견2 — 범위 술어가 3곳에 하드코딩돼 있었다(`R1-6-17`)**
> O87 이 만든 GA4 range 경로는 같은 술어를 **세 파일에 각각** 적었다:
> `macros/ga4_range_purge.sql`(pre-hook DELETE) · `BIGQUERY_REFINED_DATA.sql`(src WHERE) ·
> `GA4_EVENT.sql`(최종 WHERE). 🔴 **세 술어가 어긋나면 행이 남거나(중복) 사라지는데(누락) 그 어긋남을
> 잡는 게이트가 없었다.** 두 모델은 스스로 *"pre-hook DELETE 범위와 동일해야 멱등이다"* 라고 적어
> 두었지만 그 동일성은 **사람이 세 파일을 맞춰 고치는 것에만** 의존했다.
> ⇒ 🟢 **신설 `macros/ga4_range_predicate.sql`** 로 정의 지점을 하나로 만들었다(선례 = O87 자신의
> `silver_purge` pre-hook 단일화). 동시에 **불연속 월 목록**(`ga4_dt_ranges`)을 지원한다 —
> 종전 `ga4_dt_start`/`ga4_dt_end`(연속 1구간)로는 3개월 샘플링을 표현하면 **2년치 전량**이 됐다.
> 🟢 빈 목록은 「전 범위」가 아니라 **컴파일 실패**로 처리한다(fail-fast · 2.86억행 전량 스캔 차단).
>
> **🔴🔴 발견3 — `LATERAL FLATTEN` 이 행을 조용히 버리고 있었다**
> `BIGQUERY_REFINED_DATA` 의 `flat` CTE = `FROM src e, LATERAL FLATTEN(input => e.event_params) p`.
> 🔴 **`LATERAL FLATTEN` 의 기본은 INNER 다** ⇒ `event_params` 가 NULL 이거나 빈 배열인 행은 전개 결과가
> 0행이므로 **그 이벤트 자체가 탈락**한다. **에러도 워닝도 없다.** O87 은 이 CTE 를 *"종전 `GA4_EVENT` 와
> 동일 동작"* 으로 계승했고 **그 동작에 이 소실이 포함돼 있었다** ⇒ `GA4-SEQ-1`(「손실 0」 철회)과
> **같은 축의 두 번째 손실원**이다.
> ⇒ 🟢 **`OUTER => TRUE` 로 시정.** params 가 있는 행에는 결과가 동일하고(전개 불변) 없는 행만 param
> 컬럼 전건 NULL 로 보존한다 ⇒ **엄격히 비손실 방향**이다.
> ⚠️ 이 모델 행수는 O87 기준선보다 **늘 수 있다**(줄지 않는다). 규모는 적재 후 실측 대상.
>
> **🟢 `dbt compile` 최초 통과 — 인수인계 1번 항목 해소**
> O87 은 *"`profiles.yml` 이 `account`/`user` 를 빈 문자열로 두어 샌드박스 CLI 가 연결되지 않고
> `~/.dbt/profiles.yml` 보강은 매 호출마다 재생성돼 지워진다"* 로 막혀 컴파일 검증을 못 했다.
> 🔴 **진짜 원인은 「연결 불가」가 아니었다** — 샌드박스 생성 `/root/.dbt/profiles.yml` 에
> **`database`·`schema` 키가 없다**(에러 = *"'database' is a required property"*) ⇒ O87 의 진단은 증상만 맞았다.
> 🟢 **우회 = `--profiles-dir` 로 프로젝트 밖 프로파일을 지정**한다.
> 🔴 **경로 주의**: `/tmp` 는 세션 간 소멸한다(작업 중 실제로 사라져 재작성했다) ⇒ **`/root/dbtprof/`** 사용.
> 🟢 결과 = **EXIT=0 · 89 models · 348 data tests · 41 sources · 550 macros.**
> 컴파일 산출물 대조 = 3개월 OR 3구간 전개 · `OUTER => TRUE` 반영 확인.
> 🟢 **음성 테스트** = `--vars "{ga4_dt_ranges: []}"` ⇒ 의도한 문안으로 **컴파일 실패(EXIT=2)**(`R3` 게이트2).
>
> **🟢 신설 게이트 2종 — `GA4-SEQ-1` 축에 검사가 하나도 없었다**
> 🔴 공백의 정체: `08` DDL 이 `PRIMARY KEY (…, EVENT_SEQ)` 를 선언하지만 **Snowflake 는 PK 를 강제하지
> 않는다**(informational). 그리고 `_ga4_schema.yml` 은 4키 각각의 `not_null` 만 검사했다 —
> **`not_null` 4개는 조합 유일성을 전혀 증명하지 않는다** ⇒ PK 가 깨져도 build 가 초록으로 끝난다.
> · 🟢 `tests/assert_ga4_pk_unique.sql`(**ERROR**) = 두 테이블 4키 조합 중복 검출. ERROR 근거 = PK 중복은
>   `FACT_GA_BEHAVIOR` **팬아웃**으로 직결된다(선례 = `D2` 중복 현재행 1,264,753 사고).
> · 🟢 `tests/warn_ga4_base_row_reconciliation.sql`(**WARN** · `store_failures`) = BRONZE↔기반 행수 정합
>   = **`GA4-SEQ-1` 조치 후보 ①「접힌 양 확정」의 기계 구현**. 접힌 양을 ㉠ `PARAMS_NULL_ROWS`(= O88
>   시정이 구한 행수) ㉡ `FOLDED_ROWS`(`GROUP BY` 접힘) **두 원인으로 분해**한다.
>   🔴 임계값을 두지 않는다 — 「접힘」은 의도된 동작이라 근거 없는 숫자가 된다(`R2-3`) ⇒ 역할은
>   **판정이 아니라 노출**이고 추이를 `store_failures` 로 본다(`P207`).
> · ⚠️ **둘 다 재실행 간 순번 안정성(`GA4-SEQ-1` 본체)은 잡지 못한다** — 두 run 대조가 필요하다.
>   통과를 해소로 읽지 않도록 `_ga4_schema.yml` 머리말과 두 테스트 헤더에 **명시**했다.
>
> **🟢 `50_handoff/07` 3판 — 적재 + 완료판정까지만 활성**
> 사용자 선택 = 「적재 + 완료판정만 남김」. 활성 = 1~6장 + 7장 (1)(2) / 주석 = 7장 (3)~(6)·8장·10장·
> 부록(**149줄**).
> 🔴 **8장 주석의 대가를 명시**했다 — 8장은 0장 (3)ⓑ 정정에 따라 **위치 기반 적재의 유일한 사전
> 방어선**이다(파일 포맷 옵션은 아무 것도 막지 않는다). 사후 확증으로 대체하되 *"사후 확증은 「이미
> 적재한 뒤」 판정이므로 사전 방어가 아니다"* 를 그대로 남겼다.
> 🟢 **사용자 질의에 대한 답 = CALL 에 연월 하드코딩은 없다.** 6장 (3) `CALL(NULL,NULL,FALSE)` 는
> 5장 (2) 가 **디렉터리 테이블에서 실제 존재하는 연월**을 읽으므로 스테이지에 3개월만 올리면 그 3개월만
> 적재된다. 🔴 **유일한 예외를 찾아 교체** = 6장 (2) 스모크 `'202503'` → **`'202406'`**. 종전 값은 그
> 달이 스테이지에 없어 `NO_FILES`(files=0 · errors=0)로 **조용히 통과**해 착시를 만든다.
>
> **🔴🔴 O88-B — 3판이 낸 지시가 틀렸다(같은 세션에서 자기 정정)**
> 3판은 4장에 *"통제총계도 3개월분만 등재하라"* 를 조문화했다. 🔴 **성립하지 않는다** — 실측 =
> `GN_DW.BRONZE_BIGQUERY` 의 `events_YYYYMMDD` **2개 · 둘 다 0행** ⇒ **A 계정 원천 911테이블이 이 계정에
> 없다.** 즉 「3개월만 등재」가 아니라 **등재할 원천이 없다.**
> 🔴 **원인 = 「4장을 실행할 수 있는가」를 확인하지 않고 「어떻게 실행할까」로 건너뛰었다.** `R2-3` 을
> 통과한 것처럼 보였지만 확인한 것은 **7장 (2) 의 판정식 성질**이었고 **4장의 실행 가능성**은 재지 않았다
> ⇒ **같은 축의 두 지점 중 하나만 실측한 결함**이다. ⇒ 4장·0-B장 양쪽에 정정을 붙였다.
>
> **🟢 O88-B — `07` 4판: 「문서 전체 실행 = 자동 통과」**
> 사용자 요청 = 업로드 대기 → **07 전체 실행** → `dbt build` 3단계로 끝나게 할 것.
> · 🔴 **끔** = **4장 A-3**(등재할 원천이 없어 항상 0 / NULL · 활성이면 잡음만 남는다) ·
>   **7장 (2)**(기준선이 비면 전 폴더가 `NOT_IN_CONTROL_TOTAL` 로 쏟아진다 = 판정 불가).
>   🟢 **삭제하지 않았다** — A 계정 접근이 생기면 되살려 (2-B) 와 **함께** 쓴다(둘은 배타가 아니다).
>   🟢 3-C 의 `MIG_GA4_SRC_ROWCOUNT` **생성은 남겼다**(`IF NOT EXISTS` 라 무해 · 되살릴 때 구조 필요).
> · 🟢 **켬 = 7장 (2-B) 스테이지 직접 대조**(신설 · 이 판의 완료 판정 게이트). 기준선을
>   **A 원천 → 스테이지 자신**으로 바꾼다(스테이지는 적재의 실제 입력이고 지금 여기 있다).
>   폴더명 `FULL OUTER JOIN` 으로 **폴더 수 · 폴더별 파일 수**를 대조하고
>   `MISSING_IN_C`/`EXTRA_IN_C`/`SHORT_LOAD`/`DUP_LOAD` 로 분기한다.
>   🟢 **지금의 실제 위험과 정확히 겹친다** — 0장 (4)의 사고는 *"업로드 진행 중 1회만 적재하면 조용히
>   누락되고 status=LOADED / errors=0 을 반환한다"* = **파일 단위 누락**이다.
>   🔴 **한계를 문서에 명시했다: 파일 안의 행이 다 들어왔는지는 증명하지 못한다** ⇒ (2) 와 **동급으로
>   읽으면 안 된다.** 보완 근거 = ① COPY `errors=0`(부분 적재는 엄격 PARSE_JSON 에서 중단)
>   ② **파일은 COPY 의 최소 원자 단위**다 ⇒ 「파일 전수 일치 + errors 0」이면 행 누락 경로는 닫힌다.
>   🔴 **문자열이 아니라 건수로 비교**하도록 설계했다 — `DIRECTORY().RELATIVE_PATH`(스테이지 루트 기준)와
>   `METADATA$FILENAME`(=`SRC_FILE_NAME` · COPY `FROM` 경로 기준)은 **접두가 다를 수 있다** ⇒ 경로를 직접
>   조인하면 전건 불일치로 오판한다. 폴더명으로 조인하고 파일은 세기만 한다.
>
> **🟢 O88-B 실증 — 신설 게이트를 말로 끝내지 않았다**
> · 🔴 첫 실행이 *"DIRECTORY not enabled for the stage"* 로 실패했다 ⇒ **1장을 건너뛸 수 없다**가 실측
>   확정됐다(5장 프로시저는 디렉터리 테이블로 연월을 찾으므로 **폴더를 0개로 본다**). ⇒ 1장의
>   `SET DIRECTORY = (ENABLE = TRUE)` + `REFRESH` 를 **직접 실행**해 켰다(멱등·비파괴 · 문서 1장과 동일
>   문장이라 사용자 실행분과 충돌하지 않는다).
> · 🟢 `stg` CTE 실증 = **57폴더 / 348파일 · `events_20240601`~`20250627` · NULL 폴더 0** ⇒ 정규식·
>   `RLIKE` 전체일치·`REGEXP_SUBSTR` 이 라이브 경로 형식에서 동작한다.
>   ⚠️ 전체 게이트는 `EVENTS` 부재로 컴파일되지 않는다 — **3장이 만든다**(전체 실행 순서상 정상).
> · 🔴 **업로드가 계속 진행 중이다** — 같은 세션 안에서 **313 → 348파일** · **2026-06 은 아직 0개**
>   (`max_ym` = 202506) ⇒ 사용자의 「업로드 대기」 판단이 여전히 유효하다.
> · 🟢 **선행조건 실측** = `FF_CSV_LOAD` **존재**(SKIP_HEADER=1) ⇒ 6장 COPY 성립(4번째 막힘 없음) ·
>   `MIG_GA4_*` 2종·`V_GA4_EVENTS_DATA_COLS`·`LOAD_GA4_EVENTS` **전부 부재** ⇒ 3장·3-B·3-C·5장을 전부
>   활성으로 뒀다 · BRONZE_CRM 45테이블 **115,871,475행** · ERP 4,301 · AGENCY 243,550 **적재 완료** ·
>   SILVER 40 / GOLD 35 선생성 ⇒ **GA4 만 채우면 full `dbt build`(89 models · 348 tests)가 성립**한다.
> · 🔴 **구조 무결성** — 주석 작업의 진짜 위험은 **블록 구분자 짝이 어긋나 이후 전체가 주석이 되거나
>   구문 오류가 나는 것**이다. 검증 = `/* */` **3쌍 균형**(전부 비활성 구간) · `$$` **활성 프로시저만
>   열려 있다** · **부록의 것은 주석**.
>
> **게이트 결과 · 인수받은 FAIL 1건**
> · 🟢 `line_len` 초과 0 · `index_row_gate` **행 535 · 유실 0 · 중복 0** · `doc_line_length_gate`
>   **판독 325파일 PASS** · `clause_order_gate` **88조문 역전 0** · `doc_type_gate` 🟡(경고만).
> · 🔴🔴 **`doc_heading_gate` FAIL 은 O87-B 가 남긴 것이다** — `90_해소완료_로그.md` §1-B 제목을
>   「…해소로 파생된 신규 미결 3건 (…O87 · 신규)」 → 「…조치로 파생된 신규 미결 (…O87 · O87-B 실측)」
>   으로 **바꾸고 골든을 발행하지 않았다**(유실 1 + 신설 2). O88 은 그 파일의 제목을 만지지 않았다.
>   🟢 **O83-H 가 격상한 「분모 편입과 기준선 발행은 별개」 축이 실제로 작동한 사례**다.
>   ⇒ **사용자 승인 후 발행**했다(`R1-7-4`·`C2` 자기승인 금지를 지켰다).
> · 🟠 `02` 조각이 상한(300줄)을 넘었다 ⇒ 승인 대상 연산(`R4-4-3`)이라 즉시 돌리지 않고 **O88-B 본문만
>   압축**해 299줄로 맞춘 뒤, **사용자 승인 후 `--rebalance`** 로 구조적으로 해소했다.
>
> **🔴 미실행·미결로 남긴 것**
> · **`dbt run`·`dbt build` 미실행**(`R4-1` 정지점). 라이브 미적용 — 사용자 3단계 순서의 ③이다.
> · **pre-hook `DELETE` 렌더링은 `dbt run` 시점에만 확정된다** — `compile` 은 hook 을 내지 않는다.
>   술어 매크로 자체는 실증됐고 `ga4_range_purge` 는 그 매크로만 호출하므로 잔여 위험은
>   `is_incremental()` 분기(O87 작성분)뿐이다.
> · 🟠 **SILVER 에 `on_schema_change` 미설정** — GOLD fact 에만 `append_new_columns` 가 있다(O42 처방).
>   SILVER 는 기본값 `ignore` 라 **모델에만 있는 신규 컬럼이 무증상 폐기**된다(O40 유형).
>   🟢 GA4 6테이블은 **모델↔DDL 컬럼 집합 대조 결과 일치**(기반 51 / `GA4_EVENT` 44)라 현재는 무해하다.
>   🔴 **SILVER 전역 변경은 34테이블에 영향**하므로 실측 없이 넣지 않았다(`R2-3`) ⇒ **미결**.
> · 🟠 「SILVER→SILVER 금지」 잔존 문서 4종(O87 미갱신분)은 이번에도 손대지 않았다.
> · 🟠 「SILVER **39**테이블」 stale 은 원장에 등재만 했고 `08` DDL 헤더 문구는 고치지 않았다.
>
> **read 미반환** = **0건** · 재호출 **0건**(전건 도착). **장문 원문** = `02 §O88` · `02 §O88-B`.
