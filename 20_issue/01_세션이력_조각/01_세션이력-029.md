<!-- SPLIT-CHUNK 01_세션이력.md | 029/029 | 허브 = 01_세션이력.md | 원문 1~137행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-08-18 O85-C] 착수표 ⑲ 조문화 + ㉔ ⑦ 해소 — Agent 정본 yaml 을 `GN_DW.OPS` 스테이지로 이관 · dbt 는 의식적 잔존

>
> 🔒 **라벨** = O85 접미 `-C`(같은 세션 3번째 작업 단위 · 새 번호 미발급 · `R1-4-3` 🟢 권장 경로).
> 🟢 **`R4-4-2` 「지시 동봉」 분기 3회째 적용** — 브리핑 이후 되묻지 않고 수행했다.
> ⇒ `R4-4-4` 롤백 판단 근거 = 이 구간 **확정위반 0 · 게이트 차단 1건**(아래 §게이트가 나를 막았다).
>
> **트리거** = 사용자 질문 2단 + 결정 2건.
> ① *"이게 뭔지 자세히 알려줘. 갑자기 스테이지가 왜 필요하지 AGENT를 만드는데?"*
> ② ⑲ 결정 = **㉠+㉡ 병행** · ㉔ ⑦ 은 「OPS 와 SERVING 중 뭐가 적절한가」 재질의 후 **OPS 채택**
> ③ *"DBT는 모델이 많으니까 WORKSPACE 경로로 두고 … YAML은 파일 하나니까 STAGE로 옮겨도 되겠다"*
> ④ *"SNOWFLAKE에 관련 스킬이 있으면 로드해서 작업 진행해"* ⇒ `semantic_studio` 로드(O84 자기위반 시정).
>
> ---
>
> ##### 🟢 ⑲ 완료 — 지침 `R1-3-7-b`·`R1-3-7-c` 신설
>
> 🔴 **문제의 구조**: `R1-3-7-a` 는 **사후 역산을 요구하는데 그것이 불가능하다.** 미반환된 호출은
> 컨텍스트에서 사라지므로 나중에 세려 하면 이미 증거가 없다(O82-C 가 총계를 「확정 불가」로 적었다).
> 같은 이유로 **성공한 독해도 트리밍되면 「읽었다」를 사후 검증할 수 없다.** ⇒ 역산을 포기한다.
>
> * **`R1-3-7-b`(전건 적용 · 사용자 ㉠)** — 매 독해 직후 1~2줄: 경로 · `offset`/`limit` · 도착 여부
>   · **그 구간에만 있는 고유 토큰 2~3개**.
>   🟢 **설계의 핵심** = 「도착 O」는 **자기신고라 검증 불가**이지만, 그 구간 고유 토큰은
>   나중에 **`grep` 으로 대조**할 수 있다 ⇒ **검증 가능한 주장**으로 바뀐다.
>   🔴 **작업 끝에 몰아 쓰지 마라** — 그 시점엔 이미 트리밍됐다(`R1-7-8` 「게이트를 늦게 돌렸다」와 같은 축).
> * **`R1-3-7-c`(인용 대상 한정 · 사용자 ㉡)** — 순서를 뒤집는다: `read` → **근거를 파일에 기록** → 판정.
>   ⇒ 판정 시점에 근거가 컨텍스트가 아니라 **파일**에 있으므로 트리밍과 무관해진다.
>   이것이 `R1-3-7` 「대체 근거 금지」의 **짝**이다 — 대체 근거를 금지하려면 정본 근거를 먼저 확보해야 한다.
>   기록은 **원문 구절 그대로 + 좌표**(요약 금지 · `R2-8` 축). 🔴 **근거를 못 썼으면 판정하지 않는다.**
> * 🟢 **적용 범위를 갈랐다** = `-b` 는 **전건** · `-c` 는 **수치·판정을 인용할 문서**에만.
>   두 조문에 같은 비용을 요구하면 과대해져 **둘 다 안 지켜진다.**
> * 집행 = `init_ihcho` **Step 0.8 을 「미반환 좌표 기록 의무」 → 「독해 기록 의무」로 확장**.
>
> ---
>
> ##### 🟢 ㉔ ⑦ 완료 — 왜 스테이지가 필요했는지부터 확정했다
>
> 🔴 **사용자 질문이 정확했다** — 「Agent 만드는데 왜 스테이지?」. 답 = **Agent 의 요구사항이 아니다.**
> 문법 탐침 3회(compile-only)로 확정했다:
>
> | 시도 | 결과 |
> |---|---|
> | `ADD VERSION FROM SPECIFICATION $$…$$` | 🔴 `syntax error … unexpected 'SPECIFICATION'` — **인라인 불가** |
> | `ADD VERSION FROM '<스테이지 경로>'` | 🟢 문법 통과(객체 부재 단계까지 진행) |
> | `MODIFY LIVE VERSION SET SPECIFICATION = '<yaml>'` | 🟢 문법 통과 — **인라인 가능하나 `live` 전용** |
>
> ⇒ **스테이지는 「명명 버전(`VERSION$n`)을 쓰기로 한 결정(⑳)」의 귀결**이다. 인라인은 `live` 에만 있고,
> `live` 경로는 `ADD VERSION FROM` 을 거부하며 버전을 누적시켜 ⑳ 이 배제했다.
>
> 🔴🔴 **스킬이 제시한 경로는 이 프로젝트가 이미 배제한 경로였다 — 그대로 따르지 않았다.**
> `semantic_studio` → `agent/upload` 표준 = `cortex_agent_save` → `cortex_agent_publish`(= live 경로).
> 🟢 **공정하게 적으면 그 경로는 스테이지 문제를 아예 없앤다**(도구가 파일을 읽어 인라인 전송).
> 🔴 **그런데 채택하지 않았다. 이유는 하나이고 결정적이다**: `cortex_agent_save` 는 **SQL 이 아니다.**
> CoCo/Snowsight 도구가 있어야 돌아가므로 **`09_1`→`09_2` 를 위에서 아래로 실행하는 재구축 런북**에
> 넣을 수 없고, 스케줄 태스크·일반 워크시트에서도 못 돈다. `ADD VERSION FROM` 은 순수 SQL 이다.
> ⇒ **명명 버전 + 스테이지 유지** · 스킬 도구는 **조회(`cortex_agent_read`)에만** 쓴다.
>
> ##### 🟢 왜 `OPS` 이고 `SERVING` 이 아닌가 — 3축 전건 OPS
>
> | 축 | 근거(실측) |
> |---|---|
> | **권한** | `SERVING` = 소비 3역할에 USAGE 부여(`07` §D.7-2 → `08 §E.1`) ↔ `OPS` = ENGINEER 의 `CREATE TABLE` 뿐(`07` 308행). spec 은 instructions·도구 배선 전문 31~40KB — 소비자가 볼 것이 아니다 |
> | **선례** | `07` 296~299행 dbt `store_failures` 배치 논리 = 「COMMENT 가 내용물을 열거하는 스키마에 다른 것을 섞으면 서술이 깨지고 개수 검증이 오염된다」. `SERVING` COMMENT 가 정확히 그 유형이고 **`09_1 [6]` 이 그 개수를 센다** |
> | **정렬** | `deploy_dbt_project.sql` 78·112·175행이 **동일한 `USER$` 경로**를 쓴다 ⇒ 같은 결함이 배포 표면 2종에 있었고 그중 dbt 객체는 이미 OPS 에 있다 |
>
> ##### 🟢 집행 — 라이브 + 파일
>
> * 라이브(계정 `UA93987` · `R2-8-4-a` 3요소): `CREATE STAGE GN_DW.OPS.AGENT_SPEC_STAGE`
>   (`DIRECTORY=(ENABLE=TRUE)`) · `COPY FILES` 3종 · `ALTER SCHEMA GN_DW.OPS SET COMMENT`.
> * `09_2` **`[0-B]` 동기화 + `[0-C]` stale 대조 blocking 신설** · `[3]` 3경로 교체(문법 검증).
> * `07` 스테이지 생성절 + **`ALTER SCHEMA OPS SET COMMENT` 신설**
>   🔴 이것이 `09_1` ㉔ ② 와 **같은 결함 유형**이다 — `IF NOT EXISTS` 는 멱등이지만 **속성을 갱신하지 않는다.**
> * `cortex-project.yaml` 경로 규약 갱신(정본=워크스페이스 / **배포 소스=OPS 스테이지** 로 분리 명시).
>
> ##### 🔴🔴 설계 중 실측 반증 2건 — 계획대로 만들었으면 둘 다 고장이었다
>
> 1. **`md5` 대조는 원리적으로 불가능하다.** 직전 응답에서 나는 *"워크스페이스↔스테이지 크기·MD5 대조를
>    blocking 으로 넣으면 기계로 막힌다"* 라고 적었다. **틀렸다.** `COPY FILES` 직후 실측:
>    · 워크스페이스 `AGENT_MEMBER` md5 `1c99a361db6716a078a2bc7f9ad26dbc`
>    · OPS 스테이지 `AGENT_MEMBER` md5 `344254073f3391715a32fff09bf2f563`
>    · ↔ **`size` 는 40,256 으로 완전 동일**(3종 전건 동일).
>    원인 = 내부 스테이지는 **암호화 저장**이고 `LIST` 의 md5 는 **암호화 blob 의 해시**다 ⇒ 스테이지가
>    다르면 키가 달라 값도 다르다. **내용 동일성의 지표가 아니다.**
>    🔴 **계획대로 md5 게이트를 만들었으면 「항상 FAIL 하는 게이트」가 됐다** — `R1-6-17` 의 거울상이다
>    (그쪽은 「만들었지만 효과 0」, 이쪽은 「만들었지만 항상 빨강」). 둘 다 **실측만이 가른다.**
>    ⇒ 판정축 교체 = **`size` 동일 + 스테이지 `last_modified` ≥ 워크스페이스 `last_modified`**.
>    ②가 「고쳤는데 복사를 잊었다」를 잡는다(크기가 같은 수정도 시각 역전으로 걸린다). 실행 = 3종 `OK`.
> 2. **로컬 바이트와 스테이지 크기도 비교할 수 없다** — 로컬 `wc -c` 40,246B ↔ 스테이지 40,256B(패딩).
>    ⇒ 대조는 **스테이지 ↔ 스테이지**로만 한다(양쪽이 같은 방식으로 패딩된다). 조문화해 주석에 박았다.
> 3. ⚠️ 부수: `COPY FILES` 에 `PATTERN` 을 주지 않으면 워크스페이스의 **`.folder` placeholder 까지 복사**된다
>    (첫 시도에서 실제로 복사됨 · `REMOVE` 로 제거 · 이후 전건 `PATTERN = '.*agent_spec[.]yaml'`).
>
> ##### 🟠 dbt — 같은 결함, 다른 처방 (사용자 결정 · 「해소」가 아니다)
>
> 🔴 **`10_dbt_pipeline/deploy_dbt_project.sql` 은 워크스페이스 경로를 유지한다.** 근거(사용자):
> models **88** · yml **6** 이라 편집 사이클이 길고, 매 사이클 「편집→스테이지 복사」는 **누락 위험이 크다.**
> Agent 정본은 **Agent 당 파일 1개**라 복사 비용이 낮다. 그리고 `DBT PROJECT` 객체 자체는 이미 OPS 에 있어
> 운영·스케줄은 사람과 무관하게 돈다 — **개인 경로 의존은 배포 순간뿐**이다.
> ⇒ 상태 = 🟠 **의식적 잔존 리스크(accepted risk)** · 담당 = **dbt 작업자** ·
> 재구축 시 선택지 ㉠ 세션에서 자기 워크스페이스 URI 로 교체 ㉡ 일회성으로 `COPY FILES` → OPS 스테이지.
> 🔴 **머리말에 「해소로 적지 마라」와 담당자를 명문화**했다 — 암묵지로 남기면 재구축에서 막힌다.
>
> ##### 🔴 미해소 적발 1건 — `is_default` 자동 여부 문서 모순
>
> `09_2 [3]` 주석 = *"성공 시 새 VERSION$N 이 생성되고 **자동으로 is_default=true** 가 된다"*
> ↔ `cortex-project.yaml` 24행 = *"④ `SET DEFAULT_VERSION` (**발행만으로 default 가 되지 않는다** — P66)"*.
> 🔴 **정면 모순이고 O85-C 는 어느 쪽도 확정하지 못했다** — 이 계정에 Agent 가 **0종**이라 실측 불가다.
> ⇒ `09_2 [3]` 에 **모순 사실 + 배포 직후 `is_default` 를 눈으로 확인하고 틀린 기재를 그때 철회**하라는
> 지시를 박았다. 🔴 **둘 다 남겨두지 말 것**(지금은 남아 있다 = 다음 세션 승계 항목).
>
> ##### 🟢 게이트가 나를 막았다 (`R4-4-4` 롤백 판단 근거)
>
> 🔴 착수표 ㉔ 행 갱신에서 **`line_len` 상한 게이트가 2회 연속 차단**했다(2,302자 → 2,025자 → 통과 1,949자).
> 🟢 두 번 다 **내용을 줄이고 상세를 이력으로 옮겨** 통과시켰다 — **게이트를 끄거나 우회하지 않았다.**
> 🟢 표 행 편집은 **처음부터 python 줄 치환**으로 했다(O85-B 의 `R1-7-8` 앵커 사고를 반복하지 않았다).
> ⇒ **이 구간 확정위반 0.** 「지시 동봉 즉시 진행」 분기가 품질을 떨어뜨리지 않았다는 3회째 관측이다.
>
> ##### 🔴 `R1-3-7-b` 자기적용 — 이 구간의 독해 좌표
>
> 🟢 **신설 조문을 같은 세션에 스스로 적용했다**(조문을 쓰고 안 지키는 패턴을 피했다):
> · `/app/skills/semantic_studio/agent/upload/SKILL.md` 전량 · 도착 O ·
>   고유 토큰 = `cortex_agent_publish` · *"The saved version is not in use…"* · `parent_skill`
> · `cortex_project/cortex-project.yaml` 전량 · 도착 O · 고유 토큰 = `P62-B` · `P66` · `AGENT_MEMBER.agent_STALE_20260804.yaml`
> · `05_SV-Agent_ai/09_2` `offset=125 limit=50` / `offset=180 limit=40` · 도착 O ·
>   고유 토큰 = `"Version nullsuccessfully created"` · `no_live` · `SV_ML_MEMBER_RISK`
> · `10_dbt_pipeline/deploy_dbt_project.sql` `offset=30 limit=16` · 도착 O · 고유 토큰 = `Redeploy dbt project`
> **미반환 = 0건 · 재호출 = 0건** ⇒ 판정식 충족.
>
> ##### ⏭ 다음 세션 승계
>
> * 🔴 **`is_default` 모순 미해소** — 배포 직후 실측해 **틀린 기재 한쪽을 철회**할 것(위 §미해소 적발).
> * 🟠 **dbt accepted risk** — 재구축 담당자에게 반드시 인계(`deploy_dbt_project.sql` 머리말).
> * 🟠 착수표 잔여 **17건**(㉔ ⑦ 완결 + ⑲ 완료로 18 → 17). 문서축 다음 후보 = **⑱ ⑥ ④ ⑨ ⑦ ⑩**.
> * 🔴 라이브 의존 8건(**㉒ ㉓ ㉗ ⑫ ⑬ ⑭ ⑤ ②**)은 BRONZE 입고 전 착수 불가.
> * 🟠 지침 여유 **9,303B** — 다음 조문 신설 시 계열 이관 필요(분할 금지).
