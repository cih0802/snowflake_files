<!-- SPLIT-CHUNK 01_세션이력.md | 061/061 | 허브 = 01_세션이력.md | 원문 1~184행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-08 O145] 문서18 목표데이터 요건 재검토 — BRONZE 실측 기반 재작성
>
> **지시** = *"이 문서에서 요구하는 데이터가 진짜 새롭게 필요한걸까? 이미 bronze에 적재되는
> 데이터들로 해결할 수 있는 항목들이 있는지 브론즈 ddl과 실적재 데이터를 확인하고 문서를
> 검토해줘. 그리고 진짜 필요한 최소한의 테이블셀로 문서를 재구성해줘."*
> `R4-4-2 ㉡` 적용(브리핑 후 즉시 수행) · 확정위반 0.
>
> ▣ **① 실측 — 문서18 초판(r1)의 전제가 틀려 있었다**
>
> 라이브 조회 33건(전건 `SELECT`/`WITH` · DDL/DML **0건**)으로 대조했다.
>
> | 초판 주장 | 실측 | 판정 |
> |---|---|---|
> | 목표 원천이 0행 스텁 | `TM_CM_MBER_DVLP_GOAL` **25,344행**(2012~2026 · 월 12 · 부서 56~94 · 개발구분 3) | 🔴 반박 |
> | 목표 파이프라인 신규 구축 필요 | `SILVER.CRM_DEV_TARGET` 25,344 · `GOLD.FACT_TARGET_DEV` 25,344 · `FACT_DEV_ACHIEVEMENT` 37,522 · `FACT_BUDGET` 1,332 **가동 중** | 🔴 반박 |
> | 0행 스텁 해소 | `SILVER.CRM_BIZ_TARGET`·`GOLD.FACT_TARGET_BIZ` **2종만** 0행 | 🟡 범위 한정 |
> | 건수만 존재하는 편향 | 실적 금액 `TM_MM_FDRM_MBER_DVLP_AMT.SPNSR_AMT` **3,594,843행** 완비 | 🟠 과장(목표 측에만 참) |
>
> ▣ **② 진짜 결손 확정 — 3건 (자기검토로 4건이 됨 · ③ 참조)**
>
> · **유입채널** = 사전 `MM293` 개발인입경로 **13종** 실재 · 실적 측
>   `FACT_MEMBER_EVENT.MBER_INFLOW_PATH_NM_AT_EVENT` **3,594,823/4,633,105**(77.6%) 채움 ·
>   `DIM_CAMPAIGN.INFLOW_PATH` **34,057/36,164** ⇒ **목표 측 1컬럼만 없다**
> · **목표유형(버전)** = grain 유일성 실측 = 전체 **25,344** ↔
>   `(STDYY, STDR_MT, DEPT_ID, MBER_DVLP_DIV_CD)` 고유 **25,344** ⇒ 중복 **0** ·
>   `LAST_UPDT_DT` 채움 **25,344(100%)** · 최종 2026-04-10 11:30:59
>   ⇒ 🔴 추경이 **같은 행을 UPDATE 로 덮어써** 「당초→추경」 비교가 구조적으로 불가능하다
> · **금액(수입) 목표** = `BDGT_ACMSLT_LEDGER` 는 축을 갖췄으나(`BDGT_PRCD_NM` 연사업/추가경정 ·
>   `BDGT_UNIT_NM` 팀 · `DVLP_INBOUND_PATH` 채널 · 월별 12) `INCOME_EXPS_DIV_NM` 이
>   **전건 `지출`**(연사업 198 + 추가경정 49 = **247행**) · **`수입` 0건**
>   ⇒ 예산원장 금액은 광고비이고 모금 목표금액이 아니다
>
> ▣ **③ 사용자 질의로 드러난 grain 하자 3건 (현업 확인 대상)**
>
> 사용자 지적 = *"`TM_CM_MBER_DVLP_GOAL` 테이블에는 코드가 법인 / 부서 / 팀 으로
> 나뉘어있지 않는 것처럼 보이는데 grain 에 대해 명확히 확인해줘."* — **정확한 지적이었고**
> 그 확인 과정에서 하자 3건이 나왔다.
>
> · **하자 ① 레벨 혼재** = 목표 행의 도출 레벨이 **4와 5에 걸쳐 있다**(2026 = LVL4 24 · LVL5 46)
> · **하자 ② 권역본부가 롤업이 아니라 형제 노드** = `경기강원`(LVL3) 하위 LVL4 실측 =
>   강원지부 2,580 · 경기2사업본부 6,630 · **경기강원권역본부 6,630** · 경기남부 6,240 · 경기동부 6,240
>   ⇒ 하위 합 **21,690** ↔ 권역본부 자체 **6,630**(롤업 아님) ·
>   🔴 권역본부 6,630 과 경기2사업본부 6,630 이 **완전 동일**(배분 규칙 불명) ·
>   `서울인천` 동일 패턴(하위 합 20,752 ↔ 권역본부 7,508)
>   ⇒ 🔴 **`SUM(GOAL_CNT)` 을 계층 롤업으로 쓰면 이중계상 위험**
> · **하자 ③ 조직 마스터에 코드 체계 2종 병존** = `ZD` 424 · `ZE` 295 · `ZC` 70 · `ZB` 8 · `ZA` 4 …(Z 계열)
>   ↔ `E0` 227 · `D0` 164 · `F0` 56 · `C0` 43 · `B0` 9 …(비-Z) ·
>   목표는 **비-Z**, 실적 롤업 축 `ACMSLT_UPPER_DEPT_ID` 는 **Z** 를 가리킨다 ·
>   실증 = `E001147 회원참여팀` → `ACMSLT_UPPER_DEPT_ID = ZD000287` 이고 그 이름도 `회원참여팀`
>   ⇒ **같은 조직이 두 코드로 이중 등재**
> · 보조 = `ACMSLT_DEPT_YN` NULL **8부서**가 2026 목표 **64,942건(18.4%)** 보유(전건 `USE_YN='Y'`) ·
>   `STATS_DEPT_LVL` 은 1,314 중 **1,306(99.4%) NULL**(단 `DEC-5` 가 이미 「미사용」 확정)
>
> ▣ **④ 재구성 결과**
>
> · 신규 테이블 **1개**(`BRONZE_CRM.CRM_BIZ_TARGET`) + 기존 확장(`TM_CM_MBER_DVLP_GOAL`)
> · 초판 22컬럼 → 필수·선택 재판정(전건 대조표를 문서18 §5 에 발행)
> · 기대효과 5건 → 실측 뒷받침 **3건** · 반박 2건은 §2-4 에 근거와 함께 남김(삭제하지 않음)
> · 🟢 세션 내 자기시정 1건 = §0·§3·§5 집계가 어긋난 것을 발견해 정정(🟢 6→5 · 🟠 2→3 · 합 22 검산)
>
> ▣ **⑤ 사용자 결정 2건**
> · 최소 테이블셋 방향 = 「기존 확장 + 신규 1개」(사용자가 grain 정밀 확인을 우선 지시)
> · 반박된 기대효과 처리 = **「삭제하고 재작성」**
>
> ▣ **⑥ 독해 기록**(`R1-3-7-b`) — 미반환 **0건** · 재호출 **0건**
> | 파일 | offset/limit | 고유 토큰 |
> |---|---|---|
> | `00_guides/00_작업지침_세션운영규칙.md` | 1/349 전량 | `R0-8-3` · `handoff_gap` · `㉨ 는 O143` |
> | `20_issue/00_BRIEF.md` | 1/124 전량 | `0-RRRR` · `2,471,459 자` |
> | `30_output_share/18_…정의서.md` | 1/176 전량 | `ACTIVE_MEMBER_GOAL_CNT` · `지표 #166` |
> | `20_issue/41_입고요청서_CRM_BIZ_TARGET.md` | 1/161 전량 | `원천 변경 (2026-07-20)` · `BIZ_TARGET_DK` |
>
> 🔴 **근거 정본 = `20_issue/_o145_실측근거.md`**(판정 선행 기록 · `R1-3-7-c`).

> #### 🔴 [2026-09-08 O145-B] O145 자기검토 — 확정 결함 9건 적발·전건 처리
>
> **지시** = ① `20_issue` 배경 파악 ② 트랜스크립트 추출 + `scripts/` 제네레이터 부합·stale 검토
> ③ 논리모순·규칙위반·누락 비판적 자기검토 ④ 승인 전건 처리 후 작업물·문서 최종 개선.
> `R4-4-2 ㉡` 적용 · 확정위반 0.
>
> ▣ **① 트랜스크립트 기계 추출** — 도구 신설 = `scripts/o145_transcript_audit.py`
>
> 세션 `87365144`(39 msgs · 422,272 B · 노드 936) 추출 결과 =
> 실행 SQL **33건**(전건 조회) · 파일 쓰기/수정 **19건**(`write` 1 · `edit` 8 · `read` 10) · bash **18건**.
> 위험 신호 6종 자동 점검 = `rm -rf` 🟢 · dbt 실행 🟢 · 파이프 뒤 rc 🟢 · `awk length` 🟢 ·
> quoted heredoc 🟢 · **`python3 -c` 🔴 1건**(→ `D7`).
> 🔴 초판이 단일 `json.loads` 만 써서 죽었다 — 산출물은 **JSONL** 이다(음성 테스트 축12 로 고정).
>
> ▣ **② 🔴🔴 `D1` 확정 판정오류 — 「법인 축 100% 도출」이 틀렸다**
>
> r2 가 쓴 문장 = *"🟢 법인·본부·팀 축은 신규 입고 대상이 아니다. `DEPT_ID` 1개로 전 계층이 복원된다."*
>
> · **반박 ① 정본이 이미 반박하고 있었다** — `10_진단_원인분석` §26 `DIM_ORG.CORP` COMMENT =
>   *"🔴 부서 차원에서 산출 불가(CONF-4). 부서→법인 1:1 아님: 200부서에 복수법인 혼재
>   (2종 69부서/23,130명 · 3종 131부서/1,544,005명=98.6%). **부서트리 LVL1도 부적합**
>   (ZA 구조노드 587부서가 법인 루트 미연결…). 법인 축의 정본 원천 = 회원 속성 `CPR_DIV_CD`"*
> · **반박 ② 라이브 재측정** = 2026 목표 부서 70개에 대해 소속 정기회원의 `CPR_DIV_CD` 종수 =
>   1종 **14부서/13,735명** · 2종 **25부서/9,568명** · **3종 31부서/1,108,939명**
>   ⇒ **56/70(80.0%) 복수 법인 혼재** · 회원 기준 **1,108,939/1,132,242 = 98.0%**
> · **반박 ③** 재귀 루트 9종 중 **법인 아닌 것이 5종**(`국내사업장`·`국내사업장(사복)`·`본부`·
>   `본부(사복)`·`이사장(사복)(사복)`) — 정본이 말한 「법인 루트 미연결」 현상
>
> 🔴 **원인 = 분모 오류.** 내가 실제로 잰 것은 **트리 매칭률**(2,520/2,520)이고 주장한 것은
> **법인 확정 가능성**이었다 ⇒ `R3-9 ㉡`(같은 것을 다르게 재는 지점)의 실물 사례다.
> 2026 목표 행이 **우연히 법인 루트 2종 아래에만** 떨어져 깨끗하게 보였다.
> ⇒ **시정** = §1-3 을 `1-3-A`(트리 매칭 100% · 참) / `1-3-B`(법인 확정 불가 · 철회)로 분리 ·
> `CORP_CD` 🟡→🔴🔴 · **결손 3→4건** · §0·§3·§5 집계 동시 갱신.
>
> ▣ **③ 🔴🔴 `D2` 확정 규칙위반 — 등재 이슈를 신규 발견으로 제시하고 미결을 무시했다**
>
> r2 §3-3 B-1 = *"🔴🔴 `DIM_ORG` 계층 컬럼이 전건 NULL 이다 … 재귀 계층 도출 로직을 구현하면
> 해소된다"* + §6 에서 **`P0` 즉시 가능**으로 배치.
>
> · 실제 = 이슈 **`ORG-H`** 로 원장 §1 에 등재 · 상태 **🟠 현업 회신 대기** ·
>   미결 3건 명시(6레벨→4컬럼 매핑 · 본부/지부 구별축 부재 · 「5th=실적부서」 실측 불일치 49.6%)
> · 결정 **`CONF-4`** = *"정의·산출규칙 미확정 … 슬롯 유지 — **정의 창작 금지** …
>   `DIM_ORG.DIVISION`·`TEAM` + `FDA.ORG_*` 3(CONF-4)"*
> · 현업 질의 = 문서20 **§F-1** 질문 4건 전부 **`판정: ____` 미답**
> ⇒ 🔴 내 처방은 **`CONF-4` 를 뒤집는 것**이었고 지침 **`R1-2`**(`20_issue/` 우선 · 임의 판단 금지) 위반이다.
> ⇒ **시정** = B-1 을 「`ORG-H`/`CONF-4` 귀속」으로 재기술 · **P0 → P1 현업 판정 대기**로 재배치 ·
> §2-4·§4 도식·§6 전체 재배열(차단 요인 기준).
> 🟢 **신규 기여 1건은 남는다** = 문서50 `BLOCKING-5` 표가 FMM·FME·FSE·FEP·FBD **5팩트만** 담고
> **`FACT_DEV_ACHIEVEMENT` 가 없다** ⇒ 그 표에 FDA 행 추가 필요(잔여 등재).
>
> ▣ **④ 잔여 결함 7건**
>
> | ID | 요지 | 조치 |
> |---|---|---|
> | `D3` | `DIVISION` 기반 트리가 문서 내 2가지(§3-3 조직트리 ↔ §3-4 실적트리) — 정본은 **실적트리** | 축별 분리(`CORP`=회원속성 · `DIVISION`=실적트리 · `TEAM`=5th) |
> | `D4` | 기존 도구(`_o144e_bronze_presence`·`o125_layer_census`·`census_columns`·`gen_measure_backlog`) 대신 임시 SQL. 🔴 `o125_layer_census` 헤더가 *"㉢ 을 이 도구의 ✅ 로 덮어 읽으면 「구성됐다」를 「작동한다」로 오독한다"* 로 **내가 빠진 함정을 미리 경고**하고 있었다 | `o145_transcript_audit.py` 신설 + 음성 테스트 |
> | `D5` | 근거 파일이 `tmp/`(선례는 `20_issue/_o123c_evidence.md`) · 문서18 메타가 그 경로를 인용 ⇒ 좌표 붕괴 위험 | `20_issue/_o145_실측근거.md` 이전(바이트 동일 확인) + 포인터 갱신 |
> | `D6` | 입고요청서 갱신 필요를 **적어 놓고 안 고쳤다**(`R3-3` 마커만 남기기 금지). 첨부 DDL 은 언급조차 안 했다 | `41_…md`·`41_…_BRONZE_DDL.sql` **r2 갱신** · DDL 컴파일 검증 |
> | `D7` | `python3 -c` 로 스냅샷 호출(`R1-7-9` 경계 · 백틱 없어 실해 0). 🔴 `snapshot_util` 에 CLI 가 없어 **금지된 경로가 유일한 경로**였다 | `scripts/snapshot_cli.py` 신설 |
> | `D8` | 문서40 stale — `BRONZE_BIGQUERY` 스키마가 **라이브에 없는데** §1 G-5 는 「285,676,588행 적재 완료」 · §4 는 계정 `UA93987`(실제 `hy04212`) | 🔴 소관 밖 ⇒ **등재만** |
> | `D9` | 🔴 **`D1` 시정이 만든 과잉교정** — `CPR_DIV_CD` 를 두 테이블 모두 필수로 넣었으나 `TM_CM_SPNSR_BSNS_INFO` 실측 = 행 **50** = ID 고유 **50** = (ID,CPR) 쌍 **50** · 채움 100% ⇒ 사업목표(②)는 **파생 가능** | ② 는 **선택(권장)** 강등 · ① 은 필수 유지 |
>
> ▣ **⑤ 한정 표기 시정 — `BLOCKING-5` 교훈의 자기위반이었다**
>
> r2 §3-4 = *"롤업 축은 `ACMSLT_UPPER_DEPT_ID` 를 쓴다 … (100% 채움)"*
> · 마스터 실측 `SILVER.CRM_ORG` = 1,314행 · `UPPER_DEPT_ID` **1,314(100%)** ·
>   `ACMSLT_UPPER_DEPT_ID` **1,014(77.2%)** · 종수 321 ⇒ 정본 `ORG-H` 기재 「1,014행」과 **정확히 일치**
> · 🔴 「100%」는 **2026 목표 부서 부분집합**에서만 참이었다 ⇒ 마스터 **300행(22.8%)** 에서 성립하지 않는다
> · 🔎 문서50 `BLOCKING-5` 절 말미 = *"**부분 적재 축은 반드시 한정 표기를 붙인다.**"*
>   ⇒ 내가 그 조문을 어겼다. 한정 표기 + 마스터 실측표를 §1-4 에 추가.
>
> ▣ **⑥ 게이트 FAIL 1건 해소 (착수 시점부터 깨져 있었다)**
>
> `gate_census` 가 **미분류 8건**으로 FAIL — O144 프로브 **6종** + 내 신설 **2종**.
> · 🔴 착수 시점 이미 FAIL 이었다(내가 만든 것이 아니다) · 🔴 `ls scripts/` 출력이 **조용히 절단**돼
>   `_o144e_*` 6종 중 2종만 보였다(출력 절단 함정의 실물 사례).
> · 분류 근거 실측 = 6종 전부 DDL/DML 키워드 **0건** · 파일 쓰기 **0건** ⇒ `OBSERVE`(판정 아님) ·
>   ⚠️ 6종 전부 `GN_DW_DEV_WH`·`GN_DW_ADMIN` 하드코딩(현 세션 컨텍스트와 다르다)
> · 신설 2종 = `NEEDS_ARGS`(무인자 exit 2 규약 준수)
> ⇒ 미분류 **0** · `test_gate_census.py` **11/11** · 음성 테스트 **31종 전건 rc=0**
> (🔴 종료코드를 파이프 뒤에서 읽지 않았다 · `R0-8-2`).
>
> ▣ **⑦ 신설 도구 3종 + 음성 테스트**
> · `scripts/o145_transcript_audit.py` — 트랜스크립트 감사(JSON/JSONL 양형 · 위험 신호 6종)
> · `scripts/snapshot_cli.py` — `snapshot_util` CLI 진입점(`R1-7-10` 단일 경유 유지 · `R1-7-9` 우회 제거)
> · `scripts/test_o145_tools.py` — **16축 전건 통과**(부재 파일·경로탈출·라벨 미지정·중복 재사용·
>   접미 신설·부분실패 rc·JSONL·JSON 배열·위험 검출 4/4·오탐 0·빈 파일)
>
> ▣ **⑧ 유지 연산 실전 재확인**
> `--republish --expect "O145-B 확정 판정오류 2건"` 이 **신선도 FAIL** — 원인은 파일이 아니라
> **내 토큰 선언**이었다(문구 중간에 백틱이 끼어 연속 리터럴이 아니다 · O112 교훈 ㉠ 재확인).
> 장식 없는 구간(`1,108,939/1,132,242` · `50=50=50`)으로 교체하니 통과했다.
> 🟢 **가드가 파일을 쓰지 않고 멈춘 것이 정상 동작이다.**
>
> ▣ **⑨ 독해 기록**(`R1-3-7-b`) — 미반환 **0건** · 재호출 **0건**
> | 파일 | offset/limit | 고유 토큰 |
> |---|---|---|
> | `20_issue/40_입고대기_원천의존.md` | 1/108 전량 | `AD-5` · `PST-1` · `UA93987` · `HOL-1` |
> | `20_issue/41_…_BRONZE_DDL.sql` | 1/66 전량 | `STG_CRM_FILES/biz_target/` · `NUMBER(18,4)` |
> | `20_issue/50_dbt_…-013.md` | 100/80 | `BLOCKING-5` · `O73-C` · `1.56배` · `970,486` |
> | `20_issue/10_진단_…-019.md` | 62/30 | `23,130` · `ZA 구조노드 587부서` · `441건 상이` |
> | `20_issue/20_현업확인_…-002.md` | 1/45 | `F-1` · `49.6%` · `LVL1 2·LVL2 6` |
> | `scripts/_o144e_bronze_presence.py` | 1/61 전량 | `rc_ok` · `GN_DW_DEV_WH` |
> | `scripts/o125_layer_census.py` | 1/80 | `CREATE OR ALTER SEMANTIC VIEW` |
> | `scripts/gate_census.py` | 20/50 · 94/55 | `NEEDS_ARGS` · `MUTATES` · `O123-C` |
>
> 🔴 **근거 정본 = `20_issue/_o145_evidence.md`**(결함 9건 · 실측 SQL · 정본 verbatim 인용 · 좌표).
> 🟢 **교훈 = 「파생 불가」를 선언할 때는 파생 경로 후보를 전부 열거하라** — `D9` 는
> 후보 3개(조직트리 · 회원속성 · **후원사업 마스터**) 중 셋째를 빼먹어 생겼다.

## 🟢 [2026-09-08 O144-E 추록] 게이트 등록부 동시세션 경합 — 유령 등재 6건 철회 + 도구 간 grep 불일치 자기검출

O144-E 본작업(문서17 쿼리 전량 실행 검증)을 종료 게이트로 마감하던 중 `gate_census` 가 FAIL 했다. 원인은 **내 작업 결함이 아니라 동시 세션과의 경합**이었고, 그 과정에서 도구 신뢰성 결함 1건을 자기검출했다.

### ▣ 1. 무슨 일이 있었나 (시간순)

| 순 | 관측 | 판정 |
|---|---|---|
| 1 | `gate_census --run-tests` ➔ 🔴 **미분류 3건**(`_o144e_fix_channel` · `o145_transcript_audit` · `snapshot_cli`) | `_o144e_fix_channel` 은 내가 만들려다 인라인으로 대체하고 남긴 **잔여물** ⇒ 보관 |
| 2 | 나머지 2종은 헤더에 `[2026-09-08 O145-B 신설]` 이 박혀 있었다 | 🔴 **다른 세션(O145)의 정식 도구** ⇒ 건드리지 않음 |
| 3 | 잔여물 보관 후 재실행 ➔ 미분류 0건이 됐는데 🔴 **유령 등재 6건**이 새로 나타났다 | 내 조치(1개 이동)로 6건이 생길 수 없다 ⇒ **동시 세션이 등록부를 실시간 수정 중**이라 판정 |
| 4 | `gate_census.py:118` 확인 ➔ O145-B 가 **내 일회성 프로브 6종을 `OBSERVE` 로 등재**해 두었다 | 등재 직후 원작자(나)가 관례대로 `_archive/` 로 내리면서 **미분류 6 ➔ 유령 6** 으로 결함 유형만 바뀐 것 |
| 5 | 등재 철회 + 경위 주석 ➔ **3회 연속 미분류 0 · 유령 0 · 중복 0** | 🟢 불변식 안정 확인 |

### ▣ 2. 🔴🔴 판정식 — 남의 세션이 만든 파일을 등재해 미분류를 지우지 마라

`gate_census` 가 「미분류」를 내면 반사적으로 등록부에 올리고 싶어진다. 그러나 **파일의 성격을 먼저 판정해야 한다.**

* 🟢 **상시 도구**(`*_gate` · `gen_*` · `test_*`) ⇒ **등재**가 정답이다.
* 🔴 **일회성 프로브**(`_oNNN*` 접두) ⇒ **등재가 아니라 `_archive/` 보관**이 정답이다.
  보관하면 `ls scripts/*.py` 분모에서 빠지므로 미분류로도 잡히지 않는다.
* 🔴🔴 **원작자가 아닌 세션이 일회성 파일을 등재하면, 원작자가 정리할 때 유령으로 되살아난다.**
  ⇒ 미분류가 **남의 세션 산출물**이면 등재하지 말고 **그 세션에 정리를 넘겨라.** 결함 유형만 바뀌고 FAIL 은 남는다.

### ▣ 3. 🔴 도구 신뢰성 자기검출 — `Grep` 도구가 거짓 음성을 냈다

유령 등재의 출처를 찾을 때:

| 도구 | 같은 문자열(`_o144e_run_doc17`) 질의 결과 |
|---|---|
| `Grep` 도구 (워크스페이스 전역) | 🔴 **「No matches found」** |
| `bash grep -n ... scripts/gate_census.py` | 🟢 **6줄 적중**(`:119`~`:124`) |

🔴 **`Grep` 의 「0건」을 「그 문자열은 어디에도 없다」로 읽었다면 「등록부가 아닌 캐시가 원인」이라는 엉뚱한 가설로 갔다.**
🟢 실제로는 `bash grep` 으로 되받아 세어 즉시 위치를 찾았다.
⇒ **판정식**: 「없다」를 결론으로 쓰기 전에 **다른 도구로 한 번 더 세라**(`R0-8` 의 확장 — rc 뿐 아니라 **도구 자체**도 의심 대상이다). 스테이지 마운트 경로에서 검색 도구 간 결과가 갈린 사례로 기록한다.

### ▣ 4. 🟠 간헐 실패 — `test_split_doc_toc` (원인 미규명 · 고치지 않았다)

* `gate_census --run-tests` 순회에서 `test_split_doc_toc rc=1` 이 **회차3 에서만** 나왔다(회차1·2 는 rc=0).
* **직접 실행 4회 전건 rc=0** · 단정 **81건 전건 통과**.
* 최종 재실행 ➔ 🟢 `PASS`.
* ⇒ 🟠 **일시적 실패로 판정하고 테스트·문서를 고치지 않았다**(`O112 ㉡`·`R1-7-3`).
* 🔴 **「고쳤다」로 적지 않고 「재현 실패」로 적는다.** 다만 지난 세션 `S7`(`test_retire_sections` 동일 양상)과 합쳐 보면
  **동시 세션이 파일을 쓰는 중에 테스트가 읽어 생기는 경합**이 유력 가설이다 — 이번에는 O145 세션의 동시 편집이 실제로 관측됐다.
  🔴 그러나 **가설이고 규명은 아니다.** 같은 양상이 3회째 나오면 subprocess 순회에 재시도·잠금을 넣는 것을 검토하라.

### ▣ 5. 게이트 (최종)

| 게이트 | 결과 |
|---|---|
| `gate_census --run-tests` | 🟢 **PASS — 미분류 0 · 유령 0 · 중복 0** (불변식 3회 연속 안정) |
| `doc_line_length_gate`·`doc_census`·`doc_heading_gate`·`index_row_gate`·`clause_order_gate`·`doc_coord_gate`·`doc_type_gate` | 🟢 전건 rc=0 |
| `session_brief.py --write` | 🟢 rc=0 — 열린 착수표 5 · 문서50 열린 절 9 · stale 0 |
| 문서17 실증 쿼리 42문장 | 🟢 실패 0 (0행 1건은 `F-5` 의도된 부재) |
