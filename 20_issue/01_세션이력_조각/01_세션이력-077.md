<!-- SPLIT-CHUNK 01_세션이력.md | 077/077 | 허브 = 01_세션이력.md | 원문 1~181행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
> #### 🟢 [2026-09-22 O176] §4 결정대기 5건 전건 처리 + A 파생 2건 — 🔴 게이트가 「약속한 검사」를 안 돌고 있었다

> **트리거** = 인수인계 `O175-B` 의 지시 *"🔴🔴 넘겨받은 전제를 현업 문항·설계 근거로 쓰지 마라 … 먼저 분해해서 재라"*.
> 🟢 **그 지시가 실제로 작동했다** — 이 세션이 기각한 전제가 **5건**이다(아래 ▣3).
> 🔴 **진행 방식** = 사용자 결정 **8건**을 항목마다 ㉠ 라이브 실측 → ㉡ 선택지 숫자 → ㉢ 질문 1건 → ㉣ 집행 순으로 받았다.
> 🟠 **`R4-4-2 ㉡` 적용 세션**(지시 동봉) — 확정위반 **1건**(아래 ▣6).

> ### 독해 근거 (`R1-3-7-b` · 독해 직후 수집)
>
> | # | 파일 | offset/limit | 도착 | 그 구간 고유 토큰 |
> |---|---|---|---|---|
> | 1 | `00_guides/00_작업지침_세션운영규칙.md` | 1/304 | O | `R0-8-4` · `R2-7-3` · `R4-4-4` |
> | 2 | `20_issue/00_BRIEF.md` | 1/129 | O | `O175-B-5` · `조각수 stale 0` · `doc_coord_gate 경고 1` |
> | 3 | `03_top-down_gold/06_DDL.sql` | 1480/40 | O | `FACT 9: FACT_BUDGET (FBD)` · `:1506 AD_COST '광고비'` |
> | 4 | `scripts/comment_drift_gate.py` | 55/30 | O | `RE_TBL_COMMENT` · `SELF_VIEW` · `21,453자` |
> | 5 | `scripts/run_bronze_audit_host.py` | 26/30 | O | `BRONZE_INGEST_META_COLS = {"_STDR_YM"}` · `AGENCY.DEVICE` |
> | 6 | `99_NEXT_SESSION-023.md` | 10/60 | O | `stage == 16*(local//16)+16` · `Unsupported feature 'Data Lineage'` |
> | 7 | `99_NEXT_SESSION-025.md` | 4/42 | O | `§0-E 결정 2 로 승격` · `P216~P225 는 문서50 에 있다` |
> | 8 | `99_NEXT_SESSION-O0174-C.md` | 52/22 | O | `온라인모금 57행 54,553,600,496` · `개명 런북 미작성(O174-B-3)` |
> | 9 | `50_dbt_…-019.md` | 1/40 · 122/60 | O | `확정위반 9 · 판정약점 4` · `계정 NX55103` · `events_20240608 11` |
> | 10 | `50_dbt_…-018.md` | 68/30 | O | `온라인모금(광고) 12 SK` · `10,770,355행` |
> | 11 | `05_SV-Agent_ai/04_SV_설계.md` | 157/75 | O | `증액 537% · 재후원 1700%` · `891,959,790,888` |
> | 12 | `20_현업확인_요청-008.md` | 85/40 | O | `② 다중사업 25,251 / 62,725 / 6.04%` · `(가) 대표 1건 귀속` |
> | 13 | `50_handoff/04_데이터마이그 GN_DW_BRONZE_DDL.sql` | 100/60 | O | `게이트 축6 = 0건` · `SEQ_SYNC_ERR_INFO` |
> | 14 | `references/session-end.md` | 1/105 | O | `암호화 blob 해시` · `--expect TOKEN` · `1,900자 이내` |
>
> 📏 **`read` 미반환 0 · 재호출 0**(`R3-8`).

> ### ▣1 🔴🔴 최대 발견 — 게이트가 머리말에서 약속한 검사를 돌리지 않았다
>
> `scripts/id_collision_gate.py` 머리말은 *"부가 검사 — **미정의 번호**(참조는 있는데 DEF 가 없다).
> `P214` 가 정확히 이 상태로 O59-T 에서 유입돼 다음 세션이 정의를 역추적해야 했다"* 라고 적는다.
> 🔴 **그러나 구현은 계열별 최대값 비교**(`정의<참조` 플래그)**뿐이었다** ⇒ **번호가 최대값보다 작은 미정의는
> 원리적으로 탐지되지 않았다.**
> · 실측 = `P33` 참조 **115회** · `P106` **75회** · `P102` **47회** · `DEC-40` **54회** 가 전부 정의 형태 0 으로 장기 생존.
> · 착수표 ⑩ ㉤ 는 그중 **P102·P106 2종만** 열어뒀는데 같은 class 가 **334종 / 총참조 5,879회** 였다.
>
> 🟢 **처방** = **ID 단위 전수 열거 축 신설**(advisory · `--undefined-all`). 🔴 blocking 으로 올리지 않은 이유 =
> 신설 시점 잔여가 334종이라 올리면 **모든 세션이 즉시 막힌다**(선례 = 같은 파일의 「문서 간 정의 중복」 관측 모드).
> 🟢 **분모를 문면에 적었다** — 「정의 0」은 *어디에도 없다*가 아니라 **「정의 스캔 분모(크로스워크 3문서 제외)에 없다」**다.
> 🟢 **축이 드러낸 3상태를 구별해 출력한다**(같은 「미정의」가 원인·처방이 다르다):
> ㉠ **원장 선점 행만 있는 진행 중 라벨**(`O175` · 26종) = 정상 ㉡ **이력 항목 자체가 없는 완료 세션**(`O167` — `####` 줄 **0건**)
> ㉢ **교훈 번호가 정의 없이 대량 참조**(㉤ 의 본래 대상).
> 🔴 **한계(다음 세션이 알아야 한다)** = **절제목형 정의를 인정하지 않는다.** `DEC-40` 은
> `30_설계_의사결정-011.md:4` 에 `## 27. ✅ 결정 — DEC-40: …` 로 **정의가 실재**하는데 이 축은 미정의로 센다.
> 🟢 음성 테스트 = `scripts/test_id_collision_undefined.py` **4축 5단정** ·
> 🔴🔴 **`T4` 회귀축이 핵심이다** — 열거 블록을 도려낸 「고치기 전 구현」으로 돌리면 주입한 `P902` 를 **잡지 못한다**
> ⇒ `T1` 이 신설분을 단정한다는 것이 기계로 증명된다(`R3-2`).
>
> 🟢🟢 **판정식(신설) = 「내가 약속한 검사가 실제로 도는가」를 음성 테스트로 단정하라.**
> 문서·헤더의 약속과 구현은 **자동으로 일치하지 않는다.**

> ### ▣2 🔴🔴 폐지된 설계 등급이 실측으로 부활해 있었다 — `DEC-34` 개정 · 등급 ④ 신설
>
> `04_SV_설계.md §0.8-D`(O53)가 **② 등급(SERVING helper 뷰)을 폐지**하고 「해당 0」으로 적었고,
> `§0.8-D:227`(O54)이 *"SV **9종 전건**의 `BASE_TABLE_SCHEMA_NAME` = `GOLD`"* 를 물리 실측으로 실증했다.
> 채택된 제약은 *"SV 는 GOLD 만 본다"* 였다.
>
> 📏 **재실측(판정식 = `DESCRIBE SEMANTIC VIEW` × `INFORMATION_SCHEMA.TABLES.TABLE_TYPE`)**
> 🔴 `SHOW SEMANTIC VIEWS` 의 `comment` 로 base 를 읽으면 안 된다 — **사람이 쓴 주장**이고 실제 배선이 아니다(`J2`).
>
> | 계열 | 논리테이블 | 판정 |
> |---|---:|---|
> | SV 총수 / 논리테이블 | 17 / **42** | — |
> | ① `BASE TABLE` base | **33** | 🟢 준수 |
> | ③ dbt 소유 GOLD 뷰 | **2** | 🟢 등재분 |
> | **폐지된 ② 형태 = `SERVING.ML_*_V`** | **7** | 🔴 **위반 · 표에 근거 미기재** |
>
> 🟢 **사용자 결정 = 원칙 개정 + 새 등급 ④ 신설**(`§0.8-F`). base = `SERVING.ML_*_V` ·
> 소유주 = `21_ML_SERVING_뷰_DDL.sql`(**dbt 소관 아님**).
> 🟢🟢 **판정식 = 「GOLD 로 올릴 수 있는가」와 「GOLD 에 둘 근거가 있는가」는 다른 질문이다.**
> ② 는 GOLD 로 올릴 수 있었으므로 폐지가 옳았고, ④ 는 **원천이 GOLD 가 아니다**(`GN_DW.ML` 모델 산출) ⇒ 올릴 근거 자체가 없다.
> ⚠️ **④ 의 대가를 명시했다** — dbt `ref()` 위상정렬·build 게이트를 못 받으므로
> `sv_identifier_gate`·`sv_comment_object_gate` 가 **유일한 방어선**이다.

> ### ▣3 🔴 기각된 전제 5건 (넘겨받은 것 + 문서 기재)
>
> | # | 기재된 전제 | 실측 | 결과 |
> |---|---|---|---|
> | 1 | *"`FACT_BUDGET` measure COMMENT **정본 경로가 없다**"*(yml 등재가 키 6컬럼뿐) | 정본은 yml 이 아니라 **`06_DDL.sql:1506`** 이고 `comment_drift_gate` 분모(746컬럼)에 **이미 있다** | 🔴 기각 |
> | 2 | *"yml 에 measure 를 등재하면 된다"* | `persist_docs` 가 **어디에도 없다** ⇒ yml `description` 은 라이브에 **닿지 않는다**(효과 0) | 🔴 기각 |
> | 3 | `A4` *"해법은 grain 에 맞지 않는 컬럼 제외"* | 전건 센티넬의 원인은 **FK 미배선** — 반증 = `FACT_MEMBER_EVENT.CAMPAIGN_SK` distinct **16,655** | 🔴 절반 기각 |
> | 4 | §N-13 *"중단이력 행수(`SER_NO` **1..N**)"* | `SER_NO` 는 **전역 시퀀스**(113~1,493,754 · `=1` **0건** · 다중그룹 `1..N` **0/48,713**) | 🔴 기각 |
> | 5 | 인수인계 *"GSC 두 테이블 COMMENT **오기**"* | **원천 문안을 의도적으로 따른 것**(병합 규칙) · 이미 현업 확인 대상으로 등재됨 | 🔴 기각(고치면 위반) |
>
> 🟢 4번의 파생 근거 = 다중 그룹의 **99.75%(48,590/48,713)가 중단사유까지 동일**하다
> ⇒ 「한 결정으로 여러 사업 동시 중단」에 가깝고 **순서 대응의 업무 근거가 약하다**.
> 🟢 5번의 파생 근거 = 원천 `16_bronze_gsc_ddl.sql` 의 **컬럼 COMMENT 10개가 전부 검색 의미**인데
> **테이블 COMMENT 만** 방송 재송출을 가리킨다 ⇒ 원천 오기 가능성이 매우 높다(현업 문항 `§N-18 ④` 로 발행).

> ### ▣4 🟢 집행분 (사용자 결정 8건)
>
> | 항목 | 결정 | 집행 · 실측 |
> |---|---|---|
> | `A1` `FACT_BUDGET` COMMENT | B안 = measure **5컬럼** | `06_DDL.sql:1497,1503~1506` 치환 → 드리프트 **5건 예측 적중** → 반영 후 **rc=0 · 746/746** · OWNERSHIP `GN_DW_ADMIN` 보존 · base↔뷰 비대칭 **4 → 0** |
> | `A2` `_BATCH_ID` ×50 | A안 = 적재제어메타 | 쌍둥이 **2곳 동시** 수정 → 미노출 **542→492** · 제외(적재제어메타) **50→100** · 합 보존 검산 **542+50=592=492+100** · 전체 1,254 불변 |
> | ④ `D8 ㉤` | B안 | ▣1 · 정의 2줄은 **이 항목**에 기재(아래 ▣7) |
> | ④ `D8 ㉥` | ㅢ안 | ▣2 ⇒ ㉥-1 **선행 해제** · ㉥-2(로드맵 9)는 **현업 소관** |
> | ⑤ `D9 A4/A2-B` | A′안 | **6:8 분리 재정의** — `A4-W` 배선 6(BIGQUERY 2 확실 + AD 4 부분) / `A4-X` 원천부재 8 |
> | ⑤ `D9 A1` | B안 | `warn_ga4_null_user_pseudo_id` 에 `store_failures` + **행수준 짝 테스트 신설** · 🔴 `dbt build` **사용자 대기** |
> | ⑥ `D11` | A안 | **종결** — BRD = **33개월 × 각 1일 표본** ⇒ 「행수 인용금지」 규약 신설 |
> | ⑦ 착수표 ⑭ | A안 | 전제 기각 실증 + 문항② 기본값 **(나) 센티넬 유지** 명문화 |
> | ⑧ GSC | A안 | **`§N-18` 신설** — 드롭 근거 **3/4축** 고정 · 🔴 드롭 미집행 |
>
> 🟢 **`A1`(⑤) 기대값 사전 계산**(대사용) = TOTAL 561→**562** · PASS 524→**525** · WARN **37 유지** ·
> ERROR **0** · OPS 테이블 5→**7** · 신설 2테이블 **둘 다 0행**.
> 근거 = `USER_PSEUDO_ID IS NULL` **0행** · 0행이어도 테이블이 생김은 `OPS.WARN_GOLD_VIEW_COMMENT_COVERAGE`(ROW_COUNT 0)로 실증.

> ### ▣5 🟠 신규 발견 (R3-5 등재 · 이번 세션 소관 아님)
>
> 1. 🔴 **`GN_DW.ML.ML_RST_DATA_SPNSR_CHURN_12M` = 0행** ⇒ `SV_ML_SPONSOR_RISK` 가 라이브에서 **전건 0** 을 돌려준다.
>    ML 스키마 **16테이블 중 유일하게** 0행이라 계정 이동(기준선 `RY53492` → 현 `LK96056`)으로 설명되지 않는다.
>    🔴 기준선 기재 **829,609행** 과 불일치.
> 2. 🔴 **`O167` 이 이력 항목 없이 완료됐다** — `01_세션이력_조각` 에 `####` 줄 **0건**인데 참조는 **109회**다.
> 3. 🔴 **`SV_SERVICE` 가 거짓 grain 을 발행 중** — COMMENT 가 *"[Grain: … × **캠페인**]"* 인데
>    `FACT_MESSAGE_DISPATCH.CAMPAIGN_SK` 는 전건 센티넬이고 원천(`CRM_SEND*`)에 캠페인 키가 **없다**.
> 4. 🟠 **`DEC-44` 의 「42개 키 붕괴」가 현재 데이터에서 재현되지 않는다** —
>    `FACT_BUDGET_YEARLY` 111행 · (연,과목) 중복 **0** · (연,과목,차수) 중복 **0** · 두 차수가 과목을 공유하지 않는다.
> 5. 🟠 **`_o169_evidence.md:387` 오기** — `D8` 을 *"⑩ ㉤㉥ **컬럼 개명**"* 으로 적었으나 ⑩ 6축에 개명이 없다
>    (개명은 `O174-B-3` 「개명 런북 3안」 별건).
> 6. 🟠 **인수인계 좌표 1건 stale** — `D9` 를 `-016.md:4`(실제는 `BLOCKING-5`)로 가리켰다. 정본은 **`-019.md:4`**.
> 7. 🔴 **`new_tool.py` 결함** — `test_` 접두를 특별 취급하지 않아 음성 테스트를 **등재표에 넣는다**
>    ⇒ `test_gate_census` 축⑨(*"`test_*` 는 등재표에 없다"*)가 즉시 FAIL 했다(등재 되돌려 해소).
> 8. 🔴 **`apply_table_comment_drift.py` 에 dry-run 경로가 없다** — `--help` 를 파싱하지 않고 **즉시 집행**한다.

> ### ▣6 🔴 자기검토 — 확정위반 1 · 자기결함 2 (사용자 지적으로 촉발)
>
> **V1 🔴🔴 `R2-7-4`·`R3-1` — base COMMENT 에 거짓 주장을 발행했다(시정 완료)**
> `PLAN_BUDGET_MONTH` COMMENT 에 *"차수별 분해는 현재 불가능하다(`DEC-44`)"* 를 심었다.
> 🔴 **실측은 반대다** — `BUDGET_PROCEDURE` 컬럼이 실재하고 분해가 된다(연사업 816행/68과목 · 추가경정 516행/43과목).
> **원인 = `WIDE_BUDGET` 뷰 문안을 base 테이블로 무검증 승계**했다. 뷰에서는 참이다(그 뷰는 `BUDGET_PROCEDURE` 를
> 노출하지 않는다 · 실측 **0**) ⇒ **`J2`「내가 본 그릇이 그것을 담는 그릇인가」의 재발**이다.
> 🟢 **판정식(신설) = 뷰 문안을 base 로 승계할 때 「그 뷰에서 참인 이유가 base 에서도 성립하는가」를 먼저 재라.**
> ⇒ 2컬럼 재치환 + 라이브 반영 + 거짓문안 부재 검사 CLEAN.
>
> **V2 🔴 `R1-4-3` 라벨 선점 등재 미이행 — 확정위반**
> `--next O` 로 `O176` 을 확인만 하고 **원장 선점 등재 없이 착수**했다. 늦게 해도 경합 방지 효과는 살지 않는다.
> 🔴 **원인 = 확인과 등재를 한 동작으로 묶지 않았다**(`new_tool.py` 의 원자성 설계와 같은 축).
>
> **W1 🟠 `R3-2` 음성 테스트를 즉시 돌리지 않았다** — `A2` 가 생성기 2곳을 고쳤는데 사용자 지적 후에야 돌렸고
> 실제로 **1건 FAIL**(골든 미갱신)이 있었다. 골든 재발행(차이 3건 전량 규명 + 합 보존 검산)으로 해소.
>
> **W2 🟠 판정을 성급히 발행했다 2회** — ㉠ 「GSC COMMENT 오기 확정」(정본을 읽기 전) ㉡ 「파일 손상 확정」.
> ㉡ 의 경위 = `-019.md` 를 3회 재읽어 **20,880 B 동일 해시 + 동일 decode 실패**를 받고 손상으로 판정했으나,
> 다음 읽기는 **21,957 B 로 정상**이었다 ⇒ **마운트가 일관되게 낡은 뷰를 주고 있었다.**
> 🔴🔴 **판정식(신설) = `R1-7-3` 의 「2~3회 재읽어 해시 대조」는 「같은 낡은 값을 3번 주는 경우」를 잡지 못한다.**
> 🟢 처방 = 손상 판정 전에 **크기 변화**를 함께 보고, `cortex ws ls` 또는 `stage_mount_size_gate` 로 **다른 축**을 대라.
>
> 🟢 **지킨 것** = `R1-7-2` 해시 안정성 전건 선확인 · `R1-7-8` 편집 직후 게이트 · `R4-1` dbt 미실행 ·
> `R4-4-3` 파괴 작업(DROP) 미집행 · `R1-7-7` 삭제 후 `os.listdir` 재확인 · `R2-6` COMMENT 에 실측 수치 미기재 ·
> `R0-8` rc 선판정(`logs/` 분모로 `rc=143` 을 받고 분모를 좁혔다).

> ### ▣7 🟢 `P102`·`P106` 정의 복원 (착수표 ⑩ ㉤ · 원문 근거 확보분만)
>
> 🔴 **근거를 확보한 2종만 복원한다** — 나머지는 의미를 창작하게 되므로 백로그로 남긴다(`R2-8-4-c`).
>
> 🆕 **P102: `/workspace` 마운트는 스테이지가 아니다** — 마운트 `ls`·`wc -c`·`grep` 이 전부 통과해도 스테이지에 파일이 없을 수 있다.
> 근거 = `01_세션이력-017.md:226~234`(마운트 27,187 B/367행 통과 ↔ 스테이지에 **구판조차 없었다** ↔ 몇 분 뒤 마운트에서도 소멸).
> ⇒ 파일 생성·수정 후 **`cortex ws ls` 로 스테이지 실체를 확인한다**(`R2-5`).
>
> 🆕 **P106: 게이트를 대상이 존재하는 상태에서 실행했는가 — 분모 0 은 통과가 아니다.**
> 근거 = `00_guides/00_작업지침:272`(`R3-4`) · `50_dbt_…-008.md:69` · 거울상 `P230`(전건 실패 시 대상 집합을 먼저 의심).
> ⇒ 신설 게이트는 **실패 케이스를 주입해 검출됨을 먼저 증명**한다(음성 샘플).

> ### ▣8 📏 종료 실측 (🔴 다음 세션은 인용하지 말고 재라)
>
> · 음성 테스트 **42/42** rc=0(신설 `test_id_collision_undefined` 5단정) · `gate_census --final` rc=0(임시 계측기 **0**)
> · `comment_drift_gate` rc=0 = GOLD 테이블 **746/746** · SILVER **892/892** · GOLD 뷰 **574/574** · 금지문안 **0**
> · `doc_census` stale **0** · 분할 분모 위반 **0** · 🟠 여유 부족 **2**(원장 **+223 B** · 스킬 본문)
> · `stage_mount_size_gate` 대조 **592건 · 드리프트 0** · `index_row_gate`·`doc_heading_gate`·`doc_coord_gate`·`clause_order_gate` rc=0
> · BRONZE 노출감사 = rows **1,254** · 노출됨 **137** · 미노출 **492** · 제외(적재제어메타) **100**
> · `id_collision_gate` = 정의 식별자 **364** · 미정의 **334종/5,879회**(크로스워크 전용 26) · `O` 정의·참조 **175** ⇒ 다음 **O177**
> · GOLD `WIDE_*` = 14뷰 **575컬럼** · 정보량0 **168(29.2%)** = ㄱ10·ㄴ85·ㄷ20·ㄹ53
> · SV **17종 / 논리테이블 42** = ① 33 · ③ 2 · ④ 7
> · `FACT_MEMBER_EVENT` **4,716,360** · STOP **1,061,431** · SPONSORSHIP_SK 배선 **979,762(92.30%)** · 센티넬 **81,669(7.70%)**
> · `BIGQUERY_REFINED_DATA` **10,332,737**(33개월×1일) → `BIGQUERY_BASIC` **1,299,412**(3일) · `USER_PSEUDO_ID` NULL **0**
> · `BRONZE_GSC` = `SEARCH_CONSOLE_DATA` **958,419** / `DATA2` **941,298** · `DATA2 EXCEPT DATA` **0행**
> · 🔴 **dbt build 미실행**(`A1` 정지점) — 기준선 PASS 524 / WARN 37 / ERROR 0 / TOTAL 561 은 **이번에 재측정하지 않았다**

_Co-authored with CoCo_
