<!-- LLM-METADATA
doc_id: O166_EVIDENCE
doc_role: O166 근거철 — 개명 잔여 분모 결함(E1~E6)의 판정식·실측값·처방 (판정보다 먼저 기록)
project: GN_DW (굿네이버스)
created: 2026-09-16
created_by: O166
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# _o166_evidence — O166 근거철 (산출물 복구 + 개명 분모 결함)

> 승계 = `99_NEXT_SESSION_조각/99_NEXT_SESSION-022.md` §0-LLLL(O165) · 근거철 = `_o165_inspection_evidence.md`
> 🔴 **이 문서는 정본 근거철이다.** 판정·수치를 인용할 때 이 문서의 좌표를 대라(`R1-3-7-c`).
> 🔴 착수 지시 = ㉠ 원천 실데이터 측정 ㉡ build 후 산출물 6종 재생성(순서 고정) ㉢ `test_generators` 골든 갱신.

---

## ▣ 0. 라이브 실측 (2026-09-16 · `GN_DW` · dbt run/test 직후)

사용자가 `dbt run`(PASS 98) · `dbt test`(PASS 425 · WARN 37 · ERROR 0)을 직접 실행했다(`R4-1` 준수).
🟢 **측정 시점이 그 산출물 반영 이후임을 실증**했다 = `GOLD.LAST_ALTERED` 최대 **19:34:31(-0700)** ·
`SILVER` 최대 19:32:13 · 조회 시각 20:05:05 ⇒ 같은 세션의 빌드 결과를 재고 있다.

| 스키마 | 테이블 | 행 합계 | 0행 |
|---|---:|---:|---:|
| `BRONZE_CRM` | 50 | 122,474,251 | 0 |
| `BRONZE_AGENCY` | 4 | 248,214 | 0 |
| `BRONZE_ERP` | 2 | 160,587 | 0 |
| `BRONZE_GA4` | 2 | 45,340 | 1 (`SYNC_ERR_INFO`) |
| `BRONZE_GSC` | 2 | 921,701 | 0 |
| **BRONZE 합** | **60** | **123,850,093** | 1 |
| `SILVER` | 48 | 132,813,020 | 1 (`CRM_BIZ_TARGET`) |
| `GOLD` | 37 | 149,106,156 | 1 (`FACT_TARGET_PROJECT`) |
| `ML` | 16 | 1,045,732 | 0 |
| `OPS` | 4 | 7 | 0 |

* 🟢 **판정 = O165 §▣5 수치와 전건 동일하다**(BRONZE 60/123,850,093 · SILVER 48/132,813,020 ·
  GOLD 37/149,106,156). ⇒ 사용자 관측 *"이전과 변화는 없는걸로 보인다"* 가 **실측으로 확증**됐다.
* 🔴 **「bronze 에 일부 입고」의 실체 = 2026-09-15 00:58~00:59 적재분이고 그 이후 신규 적재는 없다**
  (BRONZE `LAST_ALTERED` 최대 = `BRONZE_GSC.SEARCH_CONSOLE_DATA` **2026-09-15 00:59:22**).

### ▣ 0-b 🔴 `D8`(BRD) 재측정 — **미판정 유지**

| 축 | 값 |
|---|---|
| O163 원장 §1 기재 | *"BRD 11,600,680행 보존"* |
| O165 실측(2026-09-16) | 10,337,767 |
| **O166 재측정(2026-09-16)** | **10,337,767** (차이 **−1,262,913** 불변) |
| `CREATED` / `LAST_ALTERED` | 2026-09-15 00:12:59 / **2026-09-15 01:00:17** |
| 컬럼 수 | **118** |

* 🔴 **판정 보류 유지(`R2-8-4-c`)** — `LAST_ALTERED` 가 09-15 01:00 이므로 **그 뒤 외부 적재가 없다**.
  ⇒ 차이는 **줄지도 늘지도 않았다**. 가릴 수 있는 것은 여전히 **외부 적재기 이력**뿐이다(현업 소관).
* 🟢 **추가 소실 위험 없음** 재확인 = DDL 정본의 `:1283` 커밋아웃(보호 장치)이 살아 있다(O165 `D7`).

---

## ▣ 1. 🟢 산출물 04·05·09 복구 완료 — `test_generators` rc=1 → **rc=0**

절차 = §1-2 가 고정한 순서를 그대로 지켰다(🔴 build 전 재생성 금지 · O163 이 그 순서를 어겼다):
`dump_schema.py` → `census_columns.py` → `gen_column_inventory` · `gen_column_mapping` ·
`gen_metric_gold_mapping` · `gen_section_assembly` → `test_generators`.

| 축 | 착수 시점(O165 측정) | O166 1차 재생성 | O166 최종 |
|---|---:|---:|---:|
| `test_generators` 판정 | 🔴 rc=1 · **14건 차이** | 🔴 rc=1 · 6건 차이 | 🟢 **rc=0 · PASS 21 · FAIL 0** |
| `04.rows` | 369 (골든 563) | **563** (골든 일치) | 563 |
| `09.조립가능` | 182 (골든 267) | 278 | **301** |
| `09.값없음` | 148 (골든 52) | 52 | 52 |

* 🟢 **`04.rows` 가 골든과 정확히 일치**했다 ⇒ **골든 563 이 옳고 O164 산출물 369 가 stale** 이었다
  (O165 가 *"골든을 갱신하지 마라 — 기준선이다"* 라고 판단한 것이 맞았다).
* 🔴 **그러나 1차 재생성만으로 닫으면 안 됐다** — 남은 6건 차이를 파고들자 `E1`~`E6` 이 나왔다.
  ⇒ 🟢 **판정식** = **「골든과 붙었다」는 「옳아졌다」가 아니다.** 차이의 **부호와 귀속**을 봐야 한다.

---

## ▣ E1 🔴🔴 `rename_stale_gate.SCAN_DIRS` 에 `scripts/` 가 없었다 (분모 결함 · 재발)

* **판정식** = `R1-6-18 ④`「분모 4곳 전건 편입」 + O165 `D11`「**「분모에 넣었다」를 말하기 전에 분모를 세라**」
* **실측** = 게이트 `SCAN_DIRS` 11개에 `scripts` 부재 ⇒ `grep -rn "gen_section_assembly" scripts/*.py` 로
  게이트 출력에 코드 좌표가 **0건**. 분모를 손으로 넓혀 재니 **개명 잔여 343건 / 51파일**이 검사 밖이었다.
* 🔴 **이것이 O165 `D11` 과 같은 형태의 재발이다** — 게이트가 없어서가 아니라 **분모가 좁아서** 조용했다.
* 🔴 **연쇄 정정** = O165 `D5` 의 판정 *"축1 529건은 **전건 문서·주석 표기**이므로 기준선 계약이 타당하다"* 는
  **`scripts/` 를 제외한 분모에서의 판정**이었다 ⇒ 실제로는 **실행 의미를 가진 코드**가 섞여 있었다.
  🟢 그 판정의 **결론(기준선 계약)은 유지**되지만 **근거 문장은 정정 대상**이다.

---

## ▣ E2 🔴🔴 실해 — `09` 산출물의 「타원천」 18건이 **전건 오판**이었다

* **판정식** = O111 ㉠「**0건·소수 건수를 받으면 내 판정식이 못 보는 것 아닌지 의심하라**」
  + O165 `D5`「매치의 이웃을 보라」
* **실측** = 타원천 18건의 근거 문안을 전수 파싱 ⇒ **18/18 전건이 `=?`(원천 미상) 경유**다.
  예 = *"원천 상이 — `FACT_MEMBER_MONTHLY`=CRM vs **`FACT_MEMBER_DEV_ACHIEVEMENT`=?**"*
* **원인** = `gen_section_assembly.SRC_SYS` 의 키가 **O163 개명 전 이름**이라 `SRC_SYS.get()` 이 `?` 를
  돌렸고, 판정기는 「원천이 다르다」로 읽었다. 🔴 **원천이 다른 것이 아니라 원천을 모르는 것이었다.**
* 🔴 **그 파일의 주석이 이미 두 번 경고하고 있었다** — O45 *"미등재 시 `SRC_SYS.get()` 이 `?` 를 돌려
  타원천으로 오판한다(실측 10행)"* · O56 *"**GOLD 에 팩트가 추가되면 이 등록부를 함께 갱신한다**"*
  ⇒ **경고를 적어 둔 것으로는 멈추지 않는다**(절차서 §4-6 규칙 ㉢의 3회차 실물).

### 정정한 등록부 6종 (전부 라이브 GOLD 명으로 교체)

| 등록부 | 역할 | 오류가 만든 결과 |
|---|---|---|
| `SRC_SYS` | 팩트 → 원천 시스템 | `?` → 타원천 오판 18건 |
| `IDENTITY_BRIDGE` | 브리지 경유 도달 | 키가 `FACT_GA_BEHAVIOR` 라 **브리지가 죽어** 도달불가 오판 |
| `BRONZE_EVIDENCE` | 차단 사유의 BRONZE 근거 | 키 불일치 → 근거 문안 미출력 |
| `FACT_PAIR_EVIDENCE` | 팩트쌍 grain 근거·대안 | 같음 |
| `ABBREV` | 약칭 → 팩트명 | 약칭이 죽은 이름으로 풀렸다 |
| `SCD2_DIM` | 현재행 필터 필수 차원 | **양방향 오류**(`E3`) |

---

## ▣ E3 🔴 `SCD2_DIM` 이 **양방향으로 틀려** 있었다 (오탐 + 미탐 동시)

* **판정식** = #1「문서·코드에 박힌 수가 실측과 같은가」 + `R2-8-4`「라이브 실재를 지금 조회했는가」
* **실측(2026-09-16 · 라이브)**

| 축 | 종전 등재(O56) | O166 실측 |
|---|---|---|
| `DIM_MEMBER` | *"SCD2 · 7,925,716행 / 회원 1,763,065 · 4.49배 팬아웃"* | **1,785,299행 / `MEMBER_DK` 유일 1,785,299 / `MEMBER_SK` 유일 1,785,299 = 1행/회원 · 팬아웃 0** |
| `DIM_MEMBER_STATUS_HISTORY` | **미등재** | **8,069,279행 / 회원 1,785,299 = 회원당 4.52행** |
| `DIM_MEMBER.IS_CURRENT` | 처방이 이 컬럼을 지시 | **컬럼 부재**(24컬럼 전수 확인) |
| `DIM_MEMBER_CURRENT` | 처방이 이 객체를 지시 | **라이브 부재 · dbt 모델 파일도 부재**(dim 20종 전수) |

* 🔴 **판정** = 팬아웃 위험이 **없는** 차원에 경고를 붙이고(오탐), **실제로 위험한** 차원은 무경고(미탐)였다.
  ⇒ 🟢 **새 판정식** = **개편은 「이름」만 바꾸지 않는다 — grain 을 옮긴다.**
  개명 대응표(`RENAMES`)로 기계 치환하면 **이름은 맞고 의미는 틀린** 문안이 남는다.
* 🔴 **그래서 `DIM_MEMBER_CURRENT` 를 자동 치환에서 제외했다**(`_o166_rename_fix.MANUAL_ONLY`) —
  ㉠ `gen_column_inventory` 에 `"DIM_MEMBER"` 키가 **이미 있어** 치환하면 **dict 키가 겹쳐 조용히 덮어쓴다**
  ㉡ 치환하면 처방이 *"`DIM_MEMBER` 는 SCD2 라 위험하니 `DIM_MEMBER` 를 쓰라"* 는 **자기모순**이 된다.

---

## ▣ E4 🔴 `table_ddl_column_gate` 의 폴백이 **항상 참**이었다 (조용한 오류 경로)

* **실측** = `resolve_ref()` 의 조건이
  `if (uname == 'FACT_MEMBER_EVENT' and (...LIFECYCLE...) in _KNOWN_TABLES) or uname == 'FACT_MEMBER_EVENT':`
  ⇒ **`or` 뒤 항이 앞 조건을 무력화**해 라이브 등재 여부와 무관하게 참이고,
  반환값은 **라이브에 없는** `GN_DW.GOLD.FACT_MEMBER_LIFECYCLE` 이었다.
* 🟢 현재 계정에서는 앞선 `_KNOWN_TABLES` 분기가 먼저 잡아 **발현하지 않았다**(잠복).
  ⇒ **그 팩트가 없는 계정에서 실행되면 존재하지 않는 객체를 조회한다.**
* **처방** = 폴백을 제거하고 **해석 실패를 `sys.exit` 로 드러낸다**(폴백으로 감추지 않는다).

---

## ▣ E5 🟢 정정 후 판정 변화 — 차이 20건 **전량 규명** (총량 보존 · 순증 0)

| 판정 | 골든(09-14) | O166 | 증감 |
|---|---:|---:|---:|
| 조립가능 | 267 | **301** | **+34** |
| 타원천 | 28 | **3** | **−25** |
| 도달불가 | 32 | **19** | **−13** |
| 배분규칙필요 | 9 | 11 | +2 |
| grain부정합 | 1 | 5 | +4 |
| 집계필요 | 2 | 1 | −1 |
| 판정불가 | 9 | 8 | −1 |
| 판정불가(SV파생) · 값없음 · 원천부재 · 형제팩트중복 · 요구grain부정합 | 92 · 52 · 10 · 4 · 1 | 동일 | 0 |
| **합계** | **507** | **507** | **0** |

* 🟢 **총량 507 보존 · 순증 0**(+34 = −25 −13 −1 −1 +2 +4 의 부호 합이 0) ⇒ **행 유실·창작 0**.
* **귀속**
  · **타원천 −25** = `SRC_SYS` 키 정정으로 `?` 소멸. 🟢 **잔여 3건은 정당**하다 —
    실제 원천 상이(`FACT_EVENT_ATTENDANCE`=CRM vs `FACT_BIGQUERY_BEHAVIOR`=**GA4**) 및 앵커 경합 2건.
  · **도달불가 −13** = `IDENTITY_BRIDGE` 키가 죽어 브리지 경유 도달이 **평가되지 않았다**.
    🔴 그 등록부 주석이 스스로 *"이것이 17건 중 7건의 원인이다"* 라고 적고 있었고,
    실제로 3-7 좌측이 **도달불가 7 → 0 · 조립가능 6 → 13** 으로 바뀌었다.
  · **grain부정합 +4 · 배분규칙필요 +2 는 악화가 아니라 재분류다** —
    「원천 미상」에 가려져 있던 **진짜 원인**이 드러났다(근거 문안에 축이 명시된다):
    시간축 `FACT_MESSAGE_DISPATCH`=일 vs `FACT_MEMBER_MONTHLY`=월 ·
    엔티티축 `=MEMBER_DK` vs `=MEMBER_DK,ORG_SK`.
    🔴 **이것이 이번 세션에서 가장 중요한 판정이다** — 오판을 고치면 **불가 건수가 줄지 않고 사유가 바뀐다.**
  · **앵커 변경 3건 · `tie_rows` 53 → 34** = 원천 상이 배제가 풀려 동수 경합이 정상 해소됐다
    (3-2·3-3 앵커 → `FACT_MESSAGE_DISPATCH` · 마케팅 §1 tie 해소 → `FACT_MEMBER_DEV_ACHIEVEMENT` 단독).
  · **측정일 09-14 → 09-16** 은 재생성의 정상 결과 · **스키마 지문 변경**은 O162·O163 개편
    (SILVER 48 · FK 56 · GOLD 개명)을 반영한 `dump_schema` 재실행 결과다.
* **발행** = `test_generators.py --update-golden --reason "<위 귀속 전문>"` ⇒ 재실행 **rc=0 · PASS 21 · FAIL 0**.

---

## ▣ E6 🟢 게이트 분모 확장과 음성 테스트 3축 신설

| 축 | 값 |
|---|---|
| `SCAN_DIRS` | 11 → **12**(`scripts` 편입) · 스캔 파일 501 → **645** |
| 축1 추이 | 529(편입 전) → **592**(편입 직후) → 563(자기참조·픽스처 제외 + 병기 마커 5종) → **531**(코드 정정 후) |
| 기준선 | 533 → **531** 재발행(`--reason` 동봉) ⇒ 🟢 **기준선을 내려 진척을 증명**했다(증가 차단이 아니라 해소) |
| 정정한 코드 | 활성 생성기 6종 + 게이트·검증기 7종 = **13파일 · 자동 88건 + 수동 7건** |
| `scripts/` 잔여 | **5건** — 전부 과거 실측 경위 서사(`comment_drift_gate`·`gen_concept_diagram`·`gen_gold_erd` 등) · 실행 의미 0 |

**신설 음성 테스트**(`test_rename_stale_gate.py` 축9·10·11 · 전 11축 통과)

* 축9 = `SCAN_DIRS` 에 `scripts` 가 있다 · 축10 = **오염 기반** — `scripts/` 에 옛 이름을 심어 축1 검출을 단정
* 축11 = **역방향 오탐** — 대응표 파일·음성 테스트·골든 json 이 축1 에 섞이지 않는다
* 🟢 **「고치기 전 실패」 실증**(절차서 §5 규약 #1 · O165 `D4` 처방) =
  `SCAN_DIRS`·`SELF_REF`·버킷을 시정 전 값으로 되돌려 재판정 ⇒ 시정 전 `(False, False, False)` ·
  시정 후 `(True, True, True)` · **뒤집힌 단정 2건 이상 실증**.
* 🔴 **자기시정 1** = 축10 초판이 프로브 파일명을 `_o166_probe_planted.py` 로 지어
  **`EVIDENCE` 정규식(`/_o\d+[a-z]*_`)에 걸려 근거철로 오분류**됐고 **검출 0건**을 냈다.
  ⇒ 🟢 **새 판정식** = **오염 기반 테스트는 「심은 것이 분모에 들어가는 이름인지」를 먼저 확인하라.**
  파일명 자체가 제외 규칙에 걸리면 그 축은 **조용히 무력**해진다(O111 ㉠ 의 픽스처 판본).
* 🔴 **자기시정 2** = `gate_census` 가 신규 스크립트 `_o166_rename_fix` 를 **미분류 FAIL** 로 막았다.
  🟢 게이트가 제 일을 했다 ⇒ `MUTATES` 에 축과 함께 등재(다중 파일 재작성 · `R4-4-3`).

---

## ▣ 8. 🔴🔴 [O166-B 자기검토] 트랜스크립트 전건 감사 + **내 판정 3건 정정**

`cortex conversations transcript --output=json 87677468`(**JSONL** · 169줄 · 메시지 169)을 파싱했다.
🔴 **자기시정 3(파싱)** = 첫 시도가 `json.load()` 였고 **`Extra data` 로 죽었다**(JSONL 이다).
O165 `D12` 가 남긴 「스키마를 먼저 덤프하라」를 지켰기 때문에 **0건 오판 없이** 즉시 정정했다.

### ▣ 8-1 🟢 규칙 준수 축 — 전건 청결

| 축 | 실측 | 판정 |
|---|---|---|
| `tool_use` 총 | **155**(bash 82 · edit 33 · read 28 · SQL 6 · write 3 · bash_output 1 · skill 1 · 질문 1) | — |
| **SQL 6건 전건 성격** | `SELECT` **6** ⇒ DDL·DML **0건** | 🟢 라이브 무변경 |
| `write` 대상 3종 | 전건 **신규 파일**(`_o166_rename_fix.py` · 근거철 · 이력 엔트리) | 🟢 `R1-7-1` |
| `edit` 대상 12종 | 전건 **기존 파일** · **허브 직접 편집 0건**(조각만) | 🟢 `R1-6` |
| `00_BRIEF.md` | edit/write **0건** | 🟢 `R3-9 ㉧` |
| dbt 명령 | **0건** | 🟢 `R4-1` 정지점 |
| `rm`·`ABORT`·discard | **0건** | 🟢 `R1-7-6`·`R1-7-7` |

* 🔴 **자기시정 4(판정식)** = 미반환 탐지기가 `'edited out to manage context'` 문자열을 세어 **2건**을 냈다.
  두 건을 열어 보니 **지침 문서 본문 인용**이다(`R1-3-7` 조문이 그 신호 문구 자체를 적고 있다) ⇒ **오탐**.
  🟢 **판정 = `read` 미반환 0건**(초판 보고가 맞았다). ⇒ 🟢 **새 판정식** =
  **「신호 문자열」로 미반환을 세면 그 신호를 설명하는 문서가 분모에 있을 때 오탐한다** — `O111 ㉢`
  (판정 축과 관측 축을 분리하라)의 실물이다. 🔴 **맞는 보고를 「정정」하지 않도록 먼저 열어 보라.**

### ▣ 8-2 🔴🔴 정정 1 — `E3` 「미탐 시정」은 **과대 주장**이었다 (`SCD2_DIM` 은 잠재 가드다)

* **판정식** = `R2-4`「완료 판정은 실제 스캔 결과」 + O165 `D5`「처방을 문면대로 구현하면 무효일 수 있다」
* **실측**(2026-09-16)

| 축 | 값 |
|---|---|
| `DIM_MEMBER_STATUS_HISTORY` 로 가는 **FK** | **0건**(`schema["fks"]` 전수) |
| `gen_section_assembly.DK_JOIN` 등재 | **없음** |
| `05_지표GOLD매핑` 에 그 차원 등장 | **0회** |
| `09` 재생성 후 골든 차이 | **0건** ⇒ 판정이 하나도 바뀌지 않았다 |

* 🔴 **판정** = 판정기가 그 차원에 **도달하지 않으므로 경고가 발동하지 않는다.**
  초판이 원장·근거철·이력에 쓴 *"실제로 위험한 차원은 무경고(미탐)였다 ⇒ 시정했다"* 는 **과대 주장**이다.
  🟢 **정확한 문안** = **「`DIM_MEMBER` 오탐 제거는 유효하고(실측 1행/회원), `DIM_MEMBER_STATUS_HISTORY`
  등재는 키를 라이브 실물로 맞춘 「잠재 가드」이며 발동 조건이 생길 때 살아난다」.**
* 🟢 **발동 조건 2개를 코드 주석에 명시**했다 = ㉠ 그 차원으로 **FK** 가 생긴다 ㉡ `DK_JOIN` 에 **추가**한다.
* 🔴 **㉡ 를 지금 하지 않았다** — 05 매핑 필드가 0건이라 **판정을 바꿀 근거가 없고**, 근거 없이 도달 경로를
  늘리면 `조립가능` 이 실측 없이 증가한다(`R2-3` 위반). 🟢 **`09` 재생성 차이 0 이 이 판단의 실증이다.**

### ▣ 8-3 🔴 정정 2 — `E4` 의 「항상 참」은 부정확했다

* 종전 문안 = *"조건문이 `or uname == …` 여서 **앞 조건과 무관하게 항상 참**"*.
* 🟢 **정확히는** 조건이 `(A and B) or A` 이므로 **무조건 참이 아니라 `A` 조건부로 참**이고,
  무력화된 것은 **`B` 검사**(`('GOLD','FACT_MEMBER_LIFECYCLE') in _KNOWN_TABLES`)다.
* 🔴 **왜 중요한가** = 이 문안은 원장·이력에 인용됐다. **틀린 서술을 근거로 다음 세션이 판단하면 안 된다**
  (`R2-8-2` 「기계 대조로만 주장한다」의 문안 판본). ⇒ 코드 주석·근거철·원장·이력 **4곳 전건 정정**.

### ▣ 8-4 🔴🔴 정정 3 — **`06` 산출물이 2주 stale 이었고 나는 그것을 모르고 골든을 발행했다**

* 🔎 **경위** = O166 초판은 `gen_bronze_exposure_audit.py`(9곳)와 `run_bronze_audit_host.py`(9곳)의
  개명 잔여를 **정정했는데 `06` 을 재생성하지 않았다.** 그 상태로 골든을 발행했으므로
  **골든의 `06` 지표는 2026-09-02 판본**을 고정하고 있었다 — 🔴 **코드와 골든이 어긋난 채 🟢 로 닫혔다.**
* 🟢 **처방 집행** = 정본 경로(`run_bronze_audit_host.py` 무인자 = 라이브 직접조회)로 재생성 ⇒
  개명 반영 확인(`FACT_SERVICE_EVENT` **6회 → 0회** · `FACT_MESSAGE_DISPATCH` **0 → 6회** ·
  `FACT_GA_BEHAVIOR` **0회** · `FACT_BIGQUERY_BEHAVIOR` 4회) 후 골든 재발행 ⇒ `test_generators` **rc=0**.
* **차이 6건 규명**

| 축 | 골든(09-02) | O166-B | 귀속 |
|---|---:|---:|---|
| `06.rows` | 1,153 | **1,204** | 🟢 실측 확정 = CRM **1,014** + AGENCY **106** + ERP **84**(GA4 10·GSC 13 은 **분모 밖**) |
| SILVER까지만 | 410 | 439 | 🟢 신규·보완 컬럼이 아직 GOLD 미배선(방향 정상) |
| 미노출(검토대상) | 523 | 542 | 🟢 같은 축 |
| 제외(PII·본문·메타) | 54 | 58 | 🟢 같은 축 |
| 노출됨(GOLD) | 138 | **137** | 🔴 **행 단위 귀속 불가**(아래) |
| 측정일 | 2026-09-02 | 2026-09-16 | 🟢 재생성의 정상 결과 |

* 🟢 **합계 정합** = 137+15+439+13+542+58 = **1,204** · 골든 138+15+410+13+523+54 = **1,153** ⇒ **유실·창작 0**.
* 🟢 **분모 +51 의 구성** = O161·O162 CRM 개편 = 신규 4종 **42컬럼**(`SND_MEMBER_OPEN_LOG` 8 ·
  `SND_MEMBER_MAIL_LINK_LOG` 13 · `TC_MKTNG_DTL_CD` 16 · `TM_MM_FDRM_MBER_DT_DTLS` 5) +
  O161 이 적발한 누락 보완 2종 **30컬럼**(`TM_PM_INSTT_ACNUT` 16 · `TM_PM_SETLE_CMPNY_ACNT` 14) +
  `MKTG_CHANNEL` 1 − 삭제 2종(`TM_CM_MKTNG_CMPGN_MNG`·`TM_CM_MKTNG_UTM` · **라이브 부재 확인**) −
  `OPEN_DT` 이설(`SND_MEMBER_LIST` **76컬럼** 실측 · 신규 `SND_MEMBER_OPEN_LOG` 로 이동 · 판정 SILVER까지만).
* 🔴 **`노출됨 −1` 은 관측으로 남긴다** — 이전 판본 사본이 `_archive/` 에 **0건**이다(러너가 스냅샷 없이 덮었다).
  🟢 **단 「내 개명 정정이 원인」이라는 가설은 반증했다**: 분모에 드는 `LINEAGE_MAP` 수정 **2건**
  (`SND_MEMBER_LIST.CALL_STATUS` · `TD_MS_MSG_AT_SNDNG_DTLS.TRNSMS_FAILR_CD_ID`)은 재생성 후에도
  **둘 다 `노출됨(GOLD)`** 이고, 나머지 수정 4건은 **GA4 축이라 이 감사 분모 밖**이다.
* 🟢 **근본 원인 처방 집행** = `run_bronze_audit_host.main()` 에 **`snapshot_util` 경유 스냅샷**을 추가했다
  (`R1-7-10`). 🔴 **골든 대조 도구가 있는데 비교 대상이 사라지는 구조였다** ⇒ 다음 갱신부터는
  `노출됨 −1` 류를 **행 단위로 규명할 수 있다.**
* 🟢 **부수 정정** = 그 생성기 docstring 의 「BRONZE 1,121 컬럼 (… GA4 30)」은 **두 축에서 틀렸다**
  (수치 stale + **GA4 는 분모가 아니다**) ⇒ 수를 지우고 **재는 방법**(러너 로그 줄)으로 교체(`R3-9 ㉦`).

### ▣ 8-5 🟢 새 판정식 3개 (이 세션이 남기는 것)

* 🔴🔴 **「등록부의 키를 고쳤다」는 「그 등록부가 작동한다」가 아니다.** 판정기가 그 객체에 **도달하는지**를
  따로 실측하라(FK · 자연키 조인 · 매핑 등장 횟수). ⇒ `E3` 가 그 실물이다.
* 🔴🔴 **생성기 코드를 고쳤으면 그 생성기의 산출물을 재생성하기 전에 골든을 발행하지 마라.**
  ⇒ `06` 이 그 실물이고, **코드·산출물·골든 3자가 어긋난 상태로 🟢 가 났다.**
  🟢 판정식 = **골든 발행 전에 「내가 만진 생성기 전부를 돌렸는가」를 세라**(이번엔 4/6 만 돌렸다).
* 🔴 **덮어쓰는 생성기는 스냅샷 없이 두지 마라** — 골든이 차이를 알려 주는데 **비교 대상이 없으면**
  그 차이는 영구히 미귀속으로 남는다.

---

## ▣ 7. 이 세션이 남기는 것

🔴 **[O166-B 자기시정 5 · `R1-7-8` 위반 1회 · 5회차]** 위 §8 을 삽입하는 `edit` 이 이 제목(`## ▣ 7.`)을
`old_string` 에 넣고 **`new_string` 에 되넣지 않아 삼켰다.** 인수인계 §0-LLLL 이 *"제목을 `old_string` 에
넣었으면 `new_string` 첫 줄에 그 제목을 먼저 되넣어라"* 라고 **명시 경고했는데도 어겼다**(O165 4회 + 이번 1회).
🟢 **복구 = 제목만 되넣었고 본문 유실 0**(아래 표 전건 보존 · `doc_heading_gate` 로 확인).
🔴🔴 **그리고 복구 시도 1회가 「문자열 없음」으로 실패했는데 그것은 파일 문제가 아니라 낡은 마운트 뷰였다**
(`D13` 재현) — 마운트 **15,296 B / 227줄** ↔ 스테이지 **23,440 B** ⇒ 25초 후 재측정하니 **23,433 B / 320줄**.
🟢 **판정식 재확인** = 「손상·부재」를 단정하기 전에 **스테이지 크기 + 시간 간격**을 교차하라(`R1-7-3` 보강).

| 남긴 것 | 좌표 |
|---|---|
| 🟢 라이브 실측 + `D8` 재측정(미판정 유지) | 이 문서 §0 |
| 🟢 산출물 04·05·09 복구 = `test_generators` **rc=0** | `scripts/golden/outputs.json`(O166 발행) |
| 🟢 판정 등록부 6종 라이브명 정정 | `scripts/gen_section_assembly.py` |
| 🟢 개명 잔여 정정 13파일 · 95건 | `scripts/_o166_rename_fix.py`(도구) |
| 🟢 게이트 분모 `scripts/` 편입 + 기준선 531 | `scripts/rename_stale_gate.py` · `golden/rename_stale_baseline.json` |
| 🟢 음성 테스트 축9·10·11 | `scripts/test_rename_stale_gate.py` |
| 🔴 **고치지 않고 남긴 것** | `D8` 외부 적재 이력(현업) · 원천 18번 재공유 · CSV 재언로드 · 문서20 §M·§N-13 · 착수표 ㉗ NL 스모크(사람) · O164 원장·이력 미등재 · O165 `D11` 후속(`doc_census` 미분할 분모 축) · `scripts/` 잔여 5건 · 개명 축1 잔여 531건 · 세션 날짜 미래 기재(O163 = 09-17) |

🔴 **「전부 고쳤다」로 닫지 않는다.** 이 세션의 성과는
**「골든과 붙는 것」과 「옳아지는 것」이 다르다는 것을 실측으로 보인 것**이다 —
1차 재생성으로 차이가 14 → 6 으로 줄었을 때 닫았다면 **타원천 18건 전건 오판이 골든에 굳었을 것**이다.

_Co-authored with CoCo_
