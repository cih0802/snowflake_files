<!-- SPLIT-CHUNK 20_현업확인_요청.md | 008/010 | 허브 = 20_현업확인_요청.md | 원문 1141~1330행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### N-9. 🆕 🟢 **[2026-08-26 O102 / 2026-09-09 O150 회신 확정] `SV_MEMBER_COHORT.CAMPAIGN_NAME` 스냅샷(SCD Type 2) Two-tier 배선 완료**

> 경위: `DEC-43`(12속성 동결) 구현 후 `SV_MEMBER_COHORT` 를 재점검했다. `DIM_MEMBER_ACQUISITION`
> 이 노출하는 캠페인 속성 12개 중 8개는 **회원 획득 시점 동결값**(`ACQ_*`)인데,
> `CAMPAIGN_NAME`(캠페인 이름 그 자체)만 `DIM_CAMPAIGN` **실시간 조인**으로 남아 있다
> (`DEC-43` §29-B 12속성 목록에 `CAMPAIGN_NAME` 자체는 포함되지 않았다 — 의도적 범위 밖).
> 캠페인명이 나중에 개칭되면 이 SV 에서 「이름은 최신, 분류(UTM·브랜드·유형 등)는 과거」인
> 상태가 될 수 있다.
> 🔴 **[2026-08-26 O102 정정] 초판의 *"현재는 SV COMMENT 에 이 사실만 명시해 두었다"* 는 거짓이었다** —
> 라이브 `SV_MEMBER_COHORT` COMMENT 와 정본 파일 SV-level COMMENT 양쪽에 동결·시점혼합 언급이
> **전무**하고(동결 사실은 `--` SQL 주석에만 있어 라이브에 실리지 않는다) `CAMPAIGN_NAME` 컬럼
> COMMENT 에도 예외 표기가 없었다. 이웃 8속성만 *"적재 시점 동결값"* 을 달고 있어 **오히려 전부
> 동결로 오독**되는 상태였다 ⇒ 🟢 정본 파일 컬럼 COMMENT 보강 · 🔴 **라이브 재배포는 승인 대기**
> (경위 = `99_NEXT §0-JJJ ▣NN ①`).

**여쭙는 것**

1. `CAMPAIGN_NAME` 도 획득 시점 동결로 확장해야 합니까, 지금처럼 실시간으로 두어야 합니까?
2. "예"라면 `FACT_MEMBER_COHORT.ACQ_CAMPAIGN_NAME` 신설(패턴은 이미 있는 `ACQ_BRAND` 등과
   동일) → `DIM_MEMBER_ACQUISITION`(실측 결과 `ACQ_CAMPAIGN_NAME` **이미 존재** — 값이
   동결원인지 실시간 조인인지 재확인) → SV `SV_MEMBER_COHORT` 재배선 순서로 진행하면 됩니까?

**판정**: 🟢 **[2026-09-09 O150 확정]** 현업 1차 회신에서 스냅샷 방식(B안) 공식 승인에 따라 dbt Snapshot 11종 구축 및 `DIM_MEMBER_ACQUISITION` Two-tier(`ACQ_CAMPAIGN_NAME_AT_ACQ` + `CURRENT_CAMPAIGN_NAME`) 배선 완료.

### N-10. 🆕 🟢 **[2026-08-31 O127 / 2026-09-07 O141 회신 확정] `CRM_MEMBER.JOIN_DT` ➔ `FRST_REGIST_DT` 원천명 복원 완료**

> 경위: `DEC-46`(컬럼 명명 규약) 판정 경계 2건 중 하나다(`32_컬럼개명표.md` §8-B).
> 기술 판정은 이렇게 났다 — `SILVER.CRM_MEMBER.JOIN_DT` 는 원천 `FRST_REGIST_DT` 를
> **2-way `UNION ALL`(정기회원 ∪ 일시회원) 양 분기에서 같은 이름으로** 받아 오고
> **로직이 0**(가공 없음)이다(`CRM_MEMBER.sql` 31·64행).
> ⇒ 2026-08-31 사용자 확정 판정식(`DEC-46` §32-G ㉠)으로는 **분류 P = 원천명 복원 대상**이다.
> 🔴 그런데 규약이 아니라 **업무 의미**가 걸린다 — 원천명 `FRST_REGIST_DT`(최초등록일)와
> 현재 이름 `JOIN_DT`(가입일)가 **같은 사건을 가리키는지**가 확인되지 않았다.
> 🔴 다르다면 개명은 **이름이 사실을 왜곡**하는 결과가 되고, 같다면 규약대로 복원하는 것이 옳다.
> 🟢 **회신 전에는 개명하지 않는다**(`ALTER … RENAME COLUMN` 미실행 · 개명표 §6-A 12건에 미포함).

**여쭙는 것**

1. 원천 `FRST_REGIST_DT`(최초등록일)는 **회원 가입 시점**과 같은 사건입니까?
   (예: 가입 전 단순 등록·문의 이력이 먼저 잡히는 경우가 있습니까?)
2. 두 개념이 다르다면, 이 컬럼이 담고 있는 값은 둘 중 **어느 쪽**입니까?
3. 같다면 컬럼명을 원천명 `FRST_REGIST_DT` 로 복원해도 분석·보고서에 문제가 없습니까?

**판정**: 🟢 **[2026-09-07 O141 회신 확정]** 원천 `FRST_REGIST_DT`(최초등록일)는 가입일과 업무적으로 완전히 동일한 개념(지표 28번 '최초가입일=회원번호 생성일' 정합 입증)이며, 원천명 `FRST_REGIST_DT`로 복원 적용 완료.

### N-11. 🆕 🟢 **[2026-08-31 O127 / 2026-09-07 O141 회신 확정] `CONF-3` 월말활동회원 판정 — 「재후원 우세 (후원사업 유효 기준)」 채택**

> 경위: 정본 #51 의 월말활동회원 판정 조건이 **내부적으로 상반**된다(원장 §1 `CONF-3` · 2026-07-31 등재).
> · 한 문장은 *"마지막 중단일이 마지막 재후원일보다 커야 함"*(**중단 우세**)이라고 쓰고
> · 다른 문장은 *"재후원 넘버링이 중단 넘버링보다 크다는 조건 필요"*(**재후원 우세**)라고 쓴다.
> 🔴 두 조건은 **같은 회원을 반대로 판정**하므로 하나를 골라야 배선이 가능하다.
> ⇒ 그래서 `DEC-19`(d) 구현과 **활동 스냅샷 as-of 배선**(착수표 `⑫` · `DEC-47`)이 함께 멈춰 있다.
> 🟢 O72-C 가 경로는 실증했다(as-of 재구성 **832,794** ↔ 플래그 **832,792** · 차 2 = 센티넬 2건).
> 🟢 2026-08-31 사용자 확정으로 「활동(건) = 미중단 약정금액 ÷ 10,000」과 센티넬 처리는 정해졌다(`DEC-47` 33-A·33-C).
> 🔴 **남은 것은 이 판정 방향 하나뿐이고, 그것은 업무 규칙이라 우리가 정할 수 없다.**
> 🔴 에이전트가 「후원 단위 미중단 플래그」로 **우회 규칙을 창작하지 않았다**(`DEC-41` 축 — 근거 없는 대표 규칙 금지).

**여쭙는 것**

1. 같은 달에 **중단과 재후원이 모두 있는 회원**은 그 달 말에 **활동 상태입니까, 비활동 상태입니까?**
2. 판정 기준은 **날짜**(마지막 중단일 vs 마지막 재후원일)입니까, **넘버링·순번**입니까?
   (둘이 어긋나는 사례가 있다면 어느 쪽이 정본입니까?)
3. 회원이 **여러 후원사업**을 가진 경우, 「활동」은 **후원 단위로 각각** 판정합니까,
   아니면 **회원 단위로 하나**로 판정합니까?(하나라면 일부만 중단된 회원은 어느 쪽입니까?)

**판정**: 🟢 **[2026-09-07 O141 회신 확정]** 「재후원 우세 (후원사업 유효 기준)」 채택 필수. 회원이 동일 월에 중단과 재후원을 진행했더라도 당월 말 기준 미중단 상태의 후원사업(SPNSR_DSCNTC_YN = 'N')을 1개 이상 보유 시 월말활동회원으로 판정(TM_MM_FDRM_MBER_SPNSR_BSNS As-of 산식 확정 및 착수표 ⑫ 완결).

### N-12. 🆕 🟢 **[2026-09-02 O131 / 2026-09-03 확정] 요건 `#13` 매체유형 — 대행사 3개 테이블 원천별 컬럼 매핑으로 100% 산출 확정**

> 경위: 요건 `#13` (`DIM_AD_CREATIVE.PLATFORM_TYPE`) 은 AGENCY 원천 리포트에 「매체유형」 개념으로 정의되어 있으나, 실측 결과 BRONZE AGENCY 3테이블(`DGT_AD_CMPGN_DTLS`, `VIDEO_AD_CMPGN_DTLS`, `REBRDC_AD_CMPGN_DTLS`)에 매체유형 전용 컬럼이 부재하여 현재 전건 NULL 상태다(문서30 §7-C-1).
> 인접 유형 컬럼(`AD_TY_NM`, `MATR_TY_NM`, `PAGE_TYPE_NM`)은 각각 `AD_TYPE_NM`, `CREATIVE_TYPE`, `PAGE_TYPE` 등 다른 목적지에 이미 배선되어 있어 대체물이 아니다.

**여쭙는 것**

1. 요건 `#13` 「매체유형」의 구체적인 업무 분류 기준(예: 포털, SNS, 동영상, 디스플레이 등)은 무엇입니까?
2. 이 매체유형은 대행사 원천의 어떤 컬럼 또는 매체명(`MEDIA_NM`) 룰 기반 파생으로 산출되어야 합니까?

**판정**: 🟢 **[2026-09-03 확정]** DIGITAL(`AD_TY_NM`), VIDEO(`CHNNL_CMPNY_TY_NM`), REBROADCAST(`RE_BRDC_TY_NM`) 매핑으로 `PLATFORM_TYPE` 100% 산출 확정.

---

### N-13. 🆕 🔴 **[2026-09-14 O160 신규] 후원중단(STOP) 사건의 다중 후원사업 귀속 룰 (착수표 ⑭ / MSTR-4)**

> 경위: 회원 사건 팩트(`FACT_MEMBER_EVENT`) STOP 브랜치(1,038,262행)는 회원 중단이력(`TM_MM_FDRM_MBER_SPNSR_DSCNTC`) 원천입니다.
> 원천 실측 결과, 중단일 기준 후원사업 매칭 시 **단일사업 중단(92.45%)**은 고유 확정되어 **팬아웃 0·Grain 불변으로 1차 배선**을 완료했습니다(O160). 잔여 **동시중단 다중사업 건(7.29%)**의 사업 귀속 처리에 대해 현업 기준 확인이 필요합니다.

**① 실측 분해 결과 (FME STOP 1,038,262행 기준)**

| 구분 | 키 | STOP 행수 | 비중 | 현행 처리 | 현업 확인 사항 |
|---|---|---|---|---|---|
| ① 단일사업 | 932,963 | 959,872 | **92.45%** | 🟢 **1:1 배선 완료** | 룰 불요 (고유 확정) |
| ② 다중사업 (행수 일치) | 25,251 | 62,725 | **6.04%** | 🟡 0 센티넬 유지 | 순번(SER_NO) 1:1 매칭 가설 타당성 |
| ③ 다중사업 (사업 초과) | 12,272 | 12,935 | **1.25%** | 🔴 0 센티넬 유지 | 귀속 룰(대표 1건 vs N등분 vs 미귀속) 결정 |
| ④ 원천 미매칭 | 2,729 | 2,730 | 0.26% | 🟢 0 센티넬 유지 | 원천 결손 |

**② 여쭙는 것**
1. **[② 영역 6.04%]** 회원의 동일 일자 중단이력 행수(`SER_NO` 1..N)와 후원사업 중단 행수가 동일하게 N개인 경우, **`SER_NO` 순서와 후원사업 순서를 1:1 순번 대응하여 각각의 후원사업을 배선**해도 업무적으로 타당합니까?
2. **[③ 영역 1.25%]** 동일 일자 중단이력 행수보다 중단된 후원사업 수가 더 많은 경우(예: 중단이력 1건인데 중단 사업 2개 이상), 분석 팩트에서는 어떻게 귀속해야 합니까?
   - **(가) 대표 1건 귀속**: 약정금액 최대(`MAX(SPNSR_AMT)`) 또는 최근 후원번호(`SPNSR_NO DESC`) 기준 대표 1건 선정
   - **(나) 0 센티넬 유지**: 귀속 모호 건으로 보아 `SPONSORSHIP_SK = 0 (미매핑)` 유지 (현행 유지)
   - **(다) 기타 업무 룰**: 현업 표준 귀속 기준 제시

**판정**: 🔴 **현업 회신 대기 (착수표 ⑭ 연계)**

> 🆕 🔴🔴 **[2026-09-22 O176 · 사용자 결정 A안] 위 문항 ①의 전제가 실측으로 기각됐다 — 이 문항을 그대로 보내지 마라.**
> 🔴 위 *"회원의 동일 일자 중단이력 행수(**`SER_NO` 1..N**)"* 는 **거짓 기재**다.
>
> 📏 **실측(2026-09-22 · 계정 `LK96056` · 원천 `BRONZE_CRM.TM_MM_FDRM_MBER_SPNSR_DSCNTC`)**
>
> | 축 | 실측 | 판정 |
> |---|---|---|
> | `SER_NO` 범위 / distinct | **113 ~ 1,493,754** / 1,061,431 = 전체 행수 | 🔴 **전역 유일 시퀀스**(회원·날짜 내 순번이 아니다) |
> | `SER_NO = 1` 인 행 | **0건** | 🔴 `1..N` 형태가 아니다 |
> | (회원×중단일) 다중 그룹 중 `SER_NO` 가 `1..N` 인 것 | **0 / 48,713** | 🔴 전제 성립 0 |
> | 다중 그룹 중 **중단사유(`DSCNTC_RSN_CD`)까지 동일** | **48,590 / 48,713 = 99.75%** | 🟠 순서 대응의 업무 근거를 약화시킨다 |
> | 다중 그룹 중 경로(`DSCNTC_PATH`) 상이 | 18 / 48,713 | — |
> | 그룹 총수 / 단일 / 다중 | 993,337 / 944,624 / **48,713(4.90%)** | — |
>
> 🔴 **원천에 후원사업 식별자가 없다** — 이 테이블의 컬럼은 `MBER_NO`·`SPNSR_DSCNTC_DE`·`SER_NO`·
> `DSCNTC_RSN_CD`·`DSCNTC_PATH`·`REGIST_DEPT_CD` 뿐이다. 그래서 매칭이 필요한 것이고,
> **중단이력 자체는 「어느 사업을 중단했는지」를 담지 않는다.**
>
> ⇒ 🟢 **문항 ①을 이렇게 바꿔 물어야 한다**(전제 교체):
> *"동시 중단 시 원천이 **후원사업의 순서를 보존**합니까, 아니면 순서 없는 집합입니까?
> (중단이력의 `SER_NO` 는 전역 적재 시퀀스라 업무 순번이 아니고, 다중 건의 99.75% 는 중단사유까지
> 같아서 **「한 결정으로 여러 사업을 동시 중단」**에 가깝게 보입니다.)"*
> 🔴 종전 문안(`SER_NO` 순번 1:1 대응 타당성)으로 물으면 **현업이 없는 순번을 전제로 답한다.**
>
> 🟢🟢 **문항 ②의 기본값을 명문화한다(우리 결정 · 회신 없이도 상태가 정의되게 한다)**
> · **기본값 = (나) `SPONSORSHIP_SK = 0` 센티넬 유지.** 근거 = `R2-7-1`(값이 없는 자리를 창작하지 않는다) ·
>   `R2-7-3`(`(미매핑)` = 조인 실패 표기). **(가) 대표 1건 귀속은 없는 사실을 만드는 것**이므로
>   현업이 명시 지시하지 않는 한 채택하지 않는다.
> · 🔴 이것은 **「회신 불요」가 아니다** — 회신이 오면 그때 바꾼다. 다만 **회신 전 상태가 미정의로 남지 않게** 고정한다.
> · 🟢 판정식 = **「결정 대기」와 「기본값 없음」은 다른 상태다.** 기본값을 명문화하면 대기가 무한해도 데이터는 일관된다.
>
> 📏 **라이브 재측정 — 위 ① 표의 O160 수치는 stale 이다**(원천이 증가했다):
> `FACT_MEMBER_EVENT` 전체 **4,716,360**(O160 기재 4,633,105) · STOP **1,061,431**(기재 1,038,262) ·
> 배선됨 **979,762 = 92.30%**(기재 92.45%) · 센티넬 **81,669 = 7.70%**(기재 7.55%).
> 🟢 **비율 구조는 동일하다** ⇒ 🔴 **절대 건수를 인용하지 말고 비율로 비교하라**(`D11` 에서 같은 교훈).
---

### N-14. 🆕 🔴🔴 **[2026-09-21 O174 신규] AGENCY 디지털 원천의 시간축·파생지표 공급 구조 — 한 문항(같은 사건)**

> 경위: `BRONZE_AGENCY.DGT_AD_CMPGN_DTLS`(대행사 디지털 광고 상세)에서 **시간축과 파생지표가
> 같은 경계로 갈립니다.** 2026-09-21 라이브 실측(계정 `RY53492` · DB `GN_DW`)입니다.
> 🔴 종전 내부 가설(「재적재가 시간축과 성과지표를 함께 비웠다」)은 **실측으로 기각**됐습니다 —
> 두 축이 같은 9,080건이었던 것은 삭제가 아니라 **원천 포맷 경계**였습니다.

**① 실측 (전체 203,138행)**

| 구간 | 행수 | 파생 날짜 범위 | `CPC`·`CPA`·`DEV_UNIT_PRICE`·`CPM` | `CTR`·`CVR` |
|---|---:|---|---:|---:|
| 신방식(`DATE` 컬럼 채움) | 9,080 | 2026-06-01 ~ 2026-08-20 | **9,080 전건 채움** | 7,038 (부분) |
| 구방식(텍스트 `YEAR`·`MONTH`·`DAY` 만) | 194,058 | 2024-01-01 ~ 2026-05-31 | **0 (전건 부재)** | 0 |

· 🟢 시간축은 DW 에서 이미 복원했습니다(`SILVER.AGENCY_AD_PERFORMANCE.AD_DATE` **203,138 전건 채움**).
  근거 = `DATE` 가 있는 9,080행에서 텍스트 파생값과 **9,080/9,080 일치 · 불일치 0** · 경계 6월 이후 구방식 0건.
· 🔴 파생지표(`CPC` 등)는 **구방식 구간에 값이 없어 DW 도 채울 수 없습니다**(창작 금지 · `R2-7-1`).

**② 전건 부재 컬럼 3종 (구간과 무관하게 0건)**

| 원천 테이블 | 컬럼 | 비어 있지 않은 행 |
|---|---|---:|
| `DGT_AD_CMPGN_DTLS` | `CRM_DVLP_CNT`(CRM 개발수) | **0 / 203,138** |
| `DGT_AD_CMPGN_DTLS` | `MEDIA_PTNT_CUST_CNT`(매체 잠재고객수) | **0 / 203,138** |
| `VIDEO_AD_CMPGN_DTLS` | `CONV_CALL_CNT`(전환콜수) | **0 / 37,674** |

· 대조군은 정상입니다 = `GA_CONV_MBER_CNT` 202,739 · `CONV_VU_CNT` 203,063 · `GA_AD_COST` 203,133.
  ⇒ 테이블 전체가 아니라 **이 3컬럼만** 비어 있습니다.

**③ 행수 변동**

· 종전 관측 **235,572행** → 현재 **203,138행**(**−32,434**). 감소분이 어느 기간인지 DW 에서는 판정할 수 없습니다
  (원천이 재적재되면 이전 상태가 남지 않습니다).

**④ 여쭙는 것**

1. **[구방식 구간 소급]** 2024-01-01~2026-05-31 구간(194,058행)의 `CPC`·`CPA`·`DEV_UNIT_PRICE`·`CPM`·`CTR`·`CVR`
   을 **소급해서 채워 주실 수 있습니까?** 불가하다면 「신방식 전환일(2026-06-01) 이전은 파생지표 미제공」으로
   확정해도 됩니까?(확정되면 DW 는 그 구간을 NULL 로 두고 컬럼 COMMENT 에 사유를 적습니다)
2. **[신방식 부분 채움]** 신방식 구간에서도 `CTR`·`CVR` 은 7,038/9,080 입니다. 나머지 2,042행이 비는 것은
   **정상**입니까(예: 노출·클릭 0 이면 계산 불가), 아니면 공급 누락입니까?
3. **[3컬럼 공급]** `CRM_DVLP_CNT`·`MEDIA_PTNT_CUST_CNT`·`CONV_CALL_CNT` 는 **공급이 중단된 것**입니까,
   아니면 **아직 산출하지 않는 지표**입니까? 공급 예정이면 시점을, 아니면 **컬럼 폐기 여부**를 알려 주십시오.
4. **[행수 −32,434]** 원천 GSheet 의 **재적재 범위**(전체 교체 / 기간 교체)와 **행 삭제 여부**를 알려 주십시오.
   삭제라면 그 기준(중복 정리 · 테스트 행 제거 등)을 알려 주십시오 — 광고비 총계가 함께 변합니다.

⚠️ **별건으로 분리했습니다**: `BRONZE_ERP.BDGT_ACMSLT_LEDGER`(258행)의 `CHN_BDGT_TOT_AMT`·
`ADJ_BDGT_TOT_AMT` 는 **NULL 이 아니라 전건 0** 입니다(추경·조정이 실제로 없었을 가능성) ⇒ 이 문항에 섞지 않습니다.

**판정**: 🔴 **현업 회신 대기 (2026-09-21 O174 발행)**

---

### N-15. 🆕 🔴 **[2026-09-21 O174 신규] `VIDEO_AD_CMPGN_DTLS.CPC` 에 엑셀 오류 문자열 `#DIV/0!` 유입 — 원천 정정 요청**

> 경위: 영상 광고 상세의 `CPC` 컬럼이 **TEXT** 이고, 값 중 **`#DIV/0!`** (엑셀 0 나눗셈 오류 표시)가
> 섞여 있습니다. DW 는 숫자 변환에 실패한 행을 NULL 로 떨어뜨리므로 **오염값이 조용히 사라집니다**
> ⇒ 탈락 행을 데이터로 재현할 경로가 없습니다(감사 불가 · `O91-F A1` 축).

**① 실측 (2026-09-21 라이브)**

| 축 | 값 |
|---|---:|
| `BRONZE_AGENCY.VIDEO_AD_CMPGN_DTLS` 행수 | 37,674 |
| `CPC` 값이 있는 행 | 35,201 |
| 숫자 변환 실패 행(= `#DIV/0!`) | **482** |
| `MIN(CPC)` (문자 정렬) | `#DIV/0!` |
| `SILVER.AGENCY_AD_ROW_VIDEO` 변환 성공 | 34,719 / 37,674 |
| `GOLD.FACT_AD_BROADCAST.CPC_SRC` 채움 | 34,719 / 39,823 |

· 🟢 변환 실패 **482 = `#DIV/0!` 건수와 정확히 일치**합니다 ⇒ 다른 오염 문자열은 없습니다.

**② 여쭙는 것**

1. **[원천 정정]** 원천(GSheet)에서 `#DIV/0!` 를 **빈칸 또는 0** 으로 정정해 주실 수 있습니까?
   (DW 는 원천을 고칠 수 없습니다 · 무손실 staging 이 원천값을 그대로 보존하는 설계입니다)
2. **[의미 확정]** 이 482행은 **분모(콜수)가 0** 이어서 발생한 것입니까? 그렇다면 DW 에서
   **NULL(계산 불가)** 로 두는 것이 맞습니까, 아니면 **0** 으로 보아야 합니까?
3. **[임시 처방 승인]** 원천 정정이 어려우면, DW 에 **사유코드 컬럼**(예: `CPC_EXCLUDE_REASON = 'SRC_DIV0'`)
   을 추가해 탈락 행을 **감사 가능**하게 두는 방식을 승인해 주시겠습니까?

**판정**: 🔴 **현업 회신 대기 (2026-09-21 O174 발행 · 원천 정정이 정석)**

---
