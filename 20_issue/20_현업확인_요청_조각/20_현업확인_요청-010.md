<!-- SPLIT-CHUNK 20_현업확인_요청.md | 010/010 | 허브 = 20_현업확인_요청.md | 신설 조각(원문 대응 없음) -->
<!-- 🔴 [2026-09-22 O179] 이 조각은 `split_doc.py` 가 만든 것이 아니라 **수기 신설**이다(사용자 결정). -->
<!--    이유 = `-009` 가 300줄 불변식을 54줄 초과했고, `--rebalance` 는 조각 번호를 밀어 -->
<!--    타 문서의 `-00N.md:행` 인용 좌표를 전부 깨뜨린다. 반면 **꼬리 조각 신설은 기존 조각을 -->
<!--    한 바이트도 건드리지 않으므로 좌표가 보존된다** — `--rollover`(R1-6-15)와 동일한 결과다. -->
<!-- 🆕 🟢 [2026-09-22 O180-B 정규화 완료] 이 조각의 잔여 과제가 **닫혔다.** -->
<!--    ㉠ 이 문서는 원장 §0 에서 **`증분형`** 으로 재정의됐다(사용자 결정) ⇒ 꼬리 신설이 -->
<!--       예외가 아니라 **정규 처리**다. 앞으로 신규 문항은 `--rollover` 로 넣는다. -->
<!--    ㉡ 🔴 `--rebalance` 는 이제 `split_doc.py` 가 **거부**한다(증분형 가드) ⇒ 좌표 파괴가 -->
<!--       구조적으로 불가능해졌다. 뚫으려면 `--force-rebalance`(승인 대상)가 필요하다. -->
<!--    ㉢ 🟢 **종전 우려 *"--verify 원문 바이트 대조가 이 조각에 성립하지 않는다"* 는 기각됐다** — -->
<!--       `--verify` 는 원문이 아니라 **발행 기준 concat SHA256** 과 대조하고(게이트2 가 -->
<!--       「재발행 — 차이 허용」을 명시한다) 실측 `--verify` 는 **rc=0 · 유실 0** 이다(O180-B). -->
<!--       ⇒ 「원문 대응 없음」은 결함이 아니라 **사실 기재**다(append형 조각도 같다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->

### N-20. 🆕 🟠 **[2026-09-22 O179 신설] BRONZE `AGE` COMMENT 오기 3건 + 코드/라벨 혼재 — 원천 정정 요청**

> 🔴 **DW 가 먼저 고치지 않는 이유** = BRONZE COMMENT 는 **원천 제공 DDL 이 정본**이고 병합 규칙(원천 무변경)이
> 적용된다(`§N-18 ④` 와 동일 축). 원천이 고쳐지면 DW 가 함께 맞춘다.
> 🟢 **DW 소관이었던 2건은 이미 고쳤다** — `SILVER.CRM_MEMBER_DEV.AGE` · `SILVER.CRM_MEMBER_AMT_CHANGE.AGE`
> COMMENT 를 `'연령'` → **`'연령대 코드(CM014) raw — … 🔴 연수(나이)가 아니다 ⇒ 평균·합계·구간비교 금지'`**
> 로 정정하고 라이브에 전파했다(`comment_drift_gate` 892/892 일치).

**실측(2026-09-22 · 계정 LK96056)** — `AGE` 라는 한 이름이 **세 가지 다른 것**을 담고 있다:

| 객체 | 타입 | 현재 COMMENT | 실제 값 | 판정 |
|---|---|---|---|---|
| `BRONZE_CRM.TM_MM_FDRM_MBER_DVLP_AMT.AGE` | NUMBER | `연령` | **1~12 코드**(CM014) | 🔴 **오기** — 원천 정정 요청 |
| `BRONZE_CRM.TM_MM_FDRM_MBER_IRSD.AGE` | NUMBER | `연령` | **1~12 코드**(CM014) | 🔴 **오기** — 원천 정정 요청 |
| `BRONZE_CRM.SND_MEMBER_LIST.AGE` | TEXT | `연령대` | **라벨 12종** + 🔴 코드 `'12'` **8행** | 🟠 COMMENT 는 맞고 **값이 혼재** |
| `SILVER.CRM_MEMBER_DEV.AGE` | NUMBER | ✅ 정정 완료 | 1~12 · 3,654,658 전건 채움 | 🟢 DW 소관 · 종결 |
| `SILVER.CRM_MEMBER_AMT_CHANGE.AGE` | NUMBER | ✅ 정정 완료 | 1~12 · 325,056 / 331,440 | 🟢 DW 소관 · 종결 |
| `GOLD.DIM_MEMBER_STATUS_HISTORY.AGE` | NUMBER | `연령대 코드 raw. 코드id:CM014` | 1~12 | 🟢 원래 정확 |

📏 `CM014` 사전 = `1` 10대 미만 · `2` 10대 · `3` 20대 · `4` 30대 · `5` 40대 · `6` 50대 · `7` 60대 · `8` 70대 · `9` 70대 이상 · `10` 단체 · `11` 기업 · `12` 기타 (`SILVER.CRM_CODE` 실측 12종).

🔴 **왜 이것이 위험한가** — COMMENT 가 `연령` 이면 소비자가 `AVG(AGE)` 를 「평균 나이」로 읽습니다.
실제로는 **40대 회원이 `5`** 이므로 평균이 **4.7** 처럼 나오고, 그 값은 나이도 아니고 코드도 아닙니다.
에러가 나지 않으므로 **보고서가 조용히 틀립니다.**

🔴 `SND_MEMBER_LIST.AGE` 는 **NULL 이 53.26%**(4,760,225 / 10,183,916 채움)입니다.
그리고 라벨형 컬럼에 코드 `'12'` 가 **8행** 섞여 있어, 라벨 집계 시 `기타`(=코드 12)가 **두 값으로 쪼개집니다.**

**질문(현업)**
1. `TM_MM_FDRM_MBER_DVLP_AMT.AGE` · `TM_MM_FDRM_MBER_IRSD.AGE` 의 원천 COMMENT 를 **`연령` → `연령대 코드(CM014)`** 로 정정해 주실 수 있습니까? (정정해 주시면 DW 가 함께 맞춥니다)
2. `SND_MEMBER_LIST.AGE` 의 코드 `'12'` **8행**은 적재 오류입니까? 라벨(`기타`)로 통일해 주실 수 있습니까? 🔴 DW 가 임의로 치환하면 원천과 어긋납니다.
3. `SND_MEMBER_LIST.AGE` **NULL 53.26%** 는 정상입니까(발송 대상에 연령대 미보유 회원이 절반)? 아니면 추출 누락입니까?

> 현재 조치: BRONZE 3건 **무변경** · SILVER 2건 **정정 완료** · 값 치환 0.
**판정**: ____________

### N-21. 🆕 🟢 **[2026-09-22 O179 신설] PII 전제 기록 — 생년월일은 원천이 의도적으로 제외했다(적재분 비민감)**

> 🔴 이 절은 **질문이 아니라 전제 기록**이다. 회신 불요. 🟢 목적 = 다음 세션·감사가 「생년월일이 없다」를
> **결손으로 오판하지 않게** 하는 것이다(실제로는 **설계된 부재**다).

📏 실측 = 전 스키마에 생년월일 컬럼 **0건**(`INFORMATION_SCHEMA` 전수). 대신 **연령대 코드(`CM014`)** 만 들어온다.
⇒ 🟢 **연령대는 개인을 식별하지 못하는 구간값**이므로 **적재분은 비민감**이다.
🔴 그래서 나이 기반 정밀 분석(정확한 만나이 · 생일 마케팅 · 코호트 일자)은 **원천 제약상 불가능**하고,
   이것은 DW 결함이 아니다. 🔴 「생년월일을 받아 오라」는 요청은 **PII 수집 확대**이므로 별도 승인 사안이다.
🟢 `O34` 인용처에 이 전제를 병기한다(그 항목이 연령 축 결손을 다룬다).

### N-22. 🟠 **[2026-09-22 O179 신설] `BRONZE_GA4` 스키마명 유지 확인 + `GA4_USER` 명칭 미결**

> 🔴 배경 = O178-B 에서 DW 내부 명칭을 `GA4` → `BIGQUERY` 로 통일했다(`DW_SOURCE_SYSTEM` 전건 `BIGQUERY`).
> 그러나 **BRONZE 스키마명 `BRONZE_GA4` 는 그대로 두었다** — 사용자 결정(현황 유지).
> 🔴 **이유 = 외부 Python 적재기가 그 이름으로 쓴다** ⇒ DW 가 개명하면 **적재가 깨진다.**
> 📏 실측 = `BRONZE_GA4` 2테이블(`GA4_USER_DEMOGRAPHIC` · `SYNC_ERR_INFO`).

**질문(현업/적재 담당)**
1. `BRONZE_GA4` 스키마명을 **유지**하는 것으로 확정합니까? (DW 내부는 이미 `BIGQUERY` 로 통일돼 있어 **스키마명만 역사적 이름으로 남습니다**)
2. 장차 개명하실 계획이 있으면 **시점**을 알려 주십시오 — DW 는 그때 `source()` 정의만 바꾸면 됩니다(모델 수정 불요).

🟠 **미결 1건(DW 내부 · 회신 불요)** = `models/gold/wide/_wide_schema.yml` 의 `GA4_USER` 명칭 —
phase-2 에서 그 모델을 실제로 만들 때 **`BIGQUERY_USER`** 로 할지 결정한다. 🔴 지금 바꾸면
아직 없는 모델의 이름을 선점하는 것이라 **미결로 남긴다**(O178-B 개명 분모에서 의도적으로 제외).

---
_Co-authored with CoCo_
