<!-- SPLIT-CHUNK 20_현업확인_요청.md | 009/010 | 허브 = 20_현업확인_요청.md | 원문 1331~1349행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### N-16. 🆕 🔴 **[2026-09-21 O174 신규] 회신 대기 문항 패킷 — 선행조건 순서대로 묶음**

> 🔴 이 절은 **색인**입니다(정본은 각 절). 목적 = 흩어진 문항을 **한 번에 회신**받는 것이고,
> 순서는 **선행조건 순**입니다(앞을 정하지 않으면 뒤를 구현할 수 없습니다).

| 순 | 문항 | 왜 지금 필요한가 | 정본 좌표 |
|---|---|---|---|
| 1 | **`CONF-2` 「(건)」 정의** = 약정금액 ÷ 10,000 인가 | 🔴 **B 버킷 45컬럼 구현의 선행조건** — 증액·감액·회비분해가 모두 이 단위에 걸립니다. `FMM.DEV_CNT`(사건수)와 **다른 단위**라 섞으면 조용히 다른 값이 나옵니다 | `20_현업확인_요청-007.md:82` |
| 2 | **`DEC-33` 코호트 3문항** | 🔴 **귀속창보다 모집단 정의가 선행**입니다(D+0 이 73.1% = 중단 통보 성격) ⇒ 모집단을 정하지 않으면 귀속창을 어떻게 잡아도 해석이 갈립니다 | `20_현업확인_요청-004.md:39` (§L) |
| 3 | **행사 마스터 미입고** | 🔴 라이브 재측정(2026-09-21) = `GOLD.FACT_EVENT_ATTENDANCE` 고아 **281,701 / 1,258,775 = 22.38%** · 상위 2키가 99.48%(`EVENT_105` · `EVENT_106`) ⇒ **마스터 2건 입고**로 해소됩니다. 🔴 집계 제외로 닫지 않습니다 | `50_dbt_…-013.md:77` |
| 4 | **채널별 발송 성공/실패 코드 매핑** + **`SEND_STATUS2` 처분** | 🟠 같은 축(발송 상태 코드체계)이므로 **함께 닫아야** 합니다 — 따로 정하면 두 컬럼이 서로 다른 코드계를 갖습니다 | `50_dbt_…-012.md:90` (`O59-P-1`) |
| 5 | **N-14 AGENCY 원천 4문항** | 🔴 광고비 집계 축(`E-4`)이 이 회신에 걸립니다 | 이 문서 §N-14 |
| 6 | **N-15 `#DIV/0!` 원천 정정** | 🔴 방송 `CPC` 지표의 신뢰성 | 이 문서 §N-15 |

**판정**: 🔴 **현업 회신 대기 — 1·2 가 최우선(뒤 항목의 선행조건)**

---

### N-17. 🆕 🔴 **[2026-09-21 O175 신규] `E-1` 모금성비용(`FUNDRAISING_COST`) — 원천에 전용 플래그가 이미 있습니다**

> 🔴🔴 **이 절은 종전 전제를 기각하고 발행합니다.** 문서50 `-018.md:47-49` 는 잔여를
> *「어느 `MOK_NM` 을 모금비용으로 볼 것인가 = 분류 결정」* 이라 적었으나,
> 원천에 **직접모금비 플래그 컬럼 2개가 실재**합니다 ⇒ `MOK_NM` 분류는 애초에 답이 아닙니다.

**㉠ 발견한 단서 (제공 정의·현업 인벤토리 2축)**

| 축 | 좌표 | 내용 |
|---|---|---|
| 원천 DDL | `90_provided_definition/13_bronze_erp_ddl.sql:18` | `DIRECT_MNYRS_YN_1 VARCHAR COMMENT '직접모금비1'` |
| 원천 DDL | `90_provided_definition/13_bronze_erp_ddl.sql:19` | `DIRECT_MNYRS_YN_2 VARCHAR COMMENT '직접모금비2'` |
| 신규지표 9 | `90_provided_definition/신규지표_20260820.csv:21` | *"26년 직접모금비 기준(모금방송 편성비, 디지털/영상광고 광고비, 콜센터운영비) 모금성비용"* |
| 신규지표 11 | `90_provided_definition/신규지표_20260820.csv:23` | *"ERP 매체별 직접모금비 / 매체별 신규 획득 개발 건수"* · *"ERP내 관련 세세목 추출 필요"* |

**㉡ 라이브 실측 (계정 `RY53492` · `GN_DW.BRONZE_ERP.BDGT_ACMSLT_LEDGER` 258행 · 2026-09-21)**

`DIRECT_MNYRS_YN_1='Y'` = **91행 / 집행 96,515,405,630원**. 세세목이 신규지표 9 정의와 **1:1 대응**합니다:

| 세세목(`SUBDTL_ITEM_NM`) | 집행액 | 현업 정의 대응 |
|---|---|---|
| 온라인모금(광고) | 53,139,972,661 | 디지털/영상광고 광고비 |
| 방송모금 | 22,110,622,560 | 모금방송 편성비 |
| 광고모금 | 12,089,638,584 | 디지털/영상광고 광고비 |
| 사회복지법인예산 | 5,232,150,464 | 🔴 **정의에 없습니다 — 문항 2** |
| 콜센터운영비(미디어모금) | 3,245,192,517 | 콜센터운영비 |
| 온라인모금 · 옹호사업 · 홍보사업(온라인) 등 | 697,828,844 | 잔여 소액 |

연도 축 = 2024년 56행 39,327,309,002 · 2025년 24행 46,038,547,302 · 2026년 11행 11,149,549,326.

**㉢ 🔴 `MOK_NM` 으로는 포함·제외가 갈리지 않습니다 (종전 전제 기각 근거)**

- `MOK_NM` 2종(`온라인모금`+`미디어모금`) 합 = **92,010,814,013** ≠ 플래그 합 **96,515,405,630**.
- 차액의 정체 = `사회복지법인예산` 2행 5,232,150,464 · `권리옹호사업` 11행 124,397,800 ·
  `기관홍보사업` 4행 3,733,540 에도 `YN_1='Y'` 가 붙어 있습니다.
- 반대로 `온라인모금` 32행(843,930,331 + 0원)은 `YN_1` 이 NULL 입니다.
- 3종(+`기관홍보사업`)도 답이 아닙니다 — 기관홍보사업 12행 192,106,917 은 `YN_1` NULL 이고
  `Y` 는 4행 3,733,540 뿐입니다.

**㉣ 현업 확인 문항 (4건)**

1. 🔴 **`DIRECT_MNYRS_YN_1` 과 `DIRECT_MNYRS_YN_2` 의 범위 차이가 무엇입니까?**
   실측 = `YN_1='Y'` 91행 / `YN_2='Y'` 125행이고 **`YN_2` 단독 `Y` 가 34행 2,490,599,420원** 입니다
   (`YN_1='Y'` 인 행은 전건 `YN_2='Y'` ⇒ 포함 관계). `FUNDRAISING_COST` 는 **어느 쪽**입니까?
2. 🔴 **`사회복지법인예산` 5,232,150,464원(2행)을 모금성비용에 포함합니까?**
   플래그는 `Y` 인데 신규지표 9 의 3항목(방송 편성비 · 광고비 · 콜센터운영비)에 **해당이 없습니다.**
   ⚠️ 이 항목은 **2024년에만** 있습니다(2025·2026년 0행) ⇒ 일회성 편성일 가능성이 있습니다.
3. 🟠 **`DIRECT_MNYRS_YN_1/2` 가 삭제된 `MNYRS_COST_DIV_YN`(모금비구분)의 대체입니까?**
   ⚠️ 이 문항은 `30_설계_의사결정-012.md:114` 에 **이미 발행돼 있습니다** — 같은 축이므로 함께 회신받습니다.
4. 🟠 **집계 단위를 연도(`YEAR`)로 고정합니까?** 신규지표 9 는 *"26년 직접모금비 기준"* 이라
   2026년만 대상으로 읽히나, 원천은 2024~2026 3개년을 담고 있습니다(신규지표 11 은 *"최근 2개년"*).

**㉤ 🔴 정정 대상 (이 절 발행과 함께 확정된 기존 문면 결함)**

| 좌표 | 문면 | 판정 |
|---|---|---|
| `30_output_share/15_문서 통합.md:855` | 실증 쿼리 `fct_yy`·`bgt_cd_nm`·`exec_amt` | 🔴 **실행 불가** — 3컬럼 전건 부재(`INFORMATION_SCHEMA` 대조). 실제 컬럼 = `YEAR`·`BDGT_ITEM_NM`·`EXEC_TOT_AMT` |
| `30_output_share/15_문서 통합.md:850` | *"ERP 회계 원장의 상세 모금 비용 연동이 부재함"* | 🔴 **거짓** — O170 이 258행 실재를 이미 확인(문서50 `-018.md:47`) |
| `30_output_share/20260826_아키텍처 지도 요약본.html:2703` | `FUNDRAISING_COST` COMMENT *"원천 자체가 비어 있다 · 대행사가 항목을 보고하지 않는다 · E-1 하드블로커"* | 🔴 **거짓 3주장** — `O96` 이 `PLAN_BUDGET_YEAR` 에서 시정한 **같은 패턴이 이 컬럼에 남아 있습니다** |

> ⚠️ 위 3건은 **`30_output_share/` 자동 생성물**이라 손으로 고치지 않습니다(`R1-6` 계약) —
> 정본 시정 후 재생성 대상으로 등재합니다.

**㉥ 정정 실증 쿼리 (위 ㉤ 의 대체본 · 실행 검증 완료)**

```sql
-- [N-17] E-1 모금성비용 후보 — 플래그 축이 정본이다
SELECT YEAR                AS "회계연도",
       DIRECT_MNYRS_YN_1   AS "직접모금비1",
       DIRECT_MNYRS_YN_2   AS "직접모금비2",
       MOK_NM              AS "목",
       DTL_ITEM_NM         AS "세목",
       SUBDTL_ITEM_NM      AS "세세목",
       COUNT(*)            AS "행수",
       SUM(EXEC_TOT_AMT)   AS "집행금액합계"
FROM GN_DW.BRONZE_ERP.BDGT_ACMSLT_LEDGER
WHERE DIRECT_MNYRS_YN_1 = 'Y' OR DIRECT_MNYRS_YN_2 = 'Y'
GROUP BY 1, 2, 3, 4, 5, 6
ORDER BY 1 DESC, 8 DESC;
```

**판정**: 🔴 **현업 회신 대기 — 문항 1·2 가 `GOLD.FACT_BUDGET.FUNDRAISING_COST` 배선의 선행조건**
(🟢 종전 「외부 원천 입고 대기」는 아닙니다 — 원천은 실재하고 **정의 확정만** 남았습니다)

---

### N-18. 🆕 🟠 **[2026-09-22 O176 신규] `BRONZE_GSC.SEARCH_CONSOLE_DATA2` 용도 — 드롭 가능 여부 (착수표 ⑧)**

> 경위: `BRONZE_GSC` 에 구조가 **완전히 동일한** 두 테이블이 있습니다.
> DW 측 조사로 **드롭 근거 3축이 확보**됐고 **용도 확인 1축만** 남았습니다.
> 🔴 드롭은 파괴 작업이라 회신 없이 집행하지 않습니다.

**① 실측 (2026-09-22 · 계정 `LK96056`)**

| 축 | `SEARCH_CONSOLE_DATA` | `SEARCH_CONSOLE_DATA2` | 판정 |
|---|---:|---:|---|
| 행수 | 958,419 | 941,298 | — |
| 컬럼 구성 | 10 | 10 · **이름·순서 완전 동일** | 🟢 동일 스키마 |
| 날짜 범위 | 2025-04-08 ~ **2026-09-18**(529일) | 2025-04-08 ~ **2026-09-08**(519일) | 🟢 `DATA2` 가 **10일 이르다** |
| 클릭 / 노출 합 | 64,217 / 3,436,887 | 63,026 / 3,375,761 | — |
| **`DATA2` EXCEPT `DATA`** | — | **0행** | 🟢🟢 **진부분집합 — 고유 정보 0** |
| `DATA` EXCEPT `DATA2` | **17,121행** | — | 🟢 `DATA` 가 최신 |

⇒ 🟢 **`DATA2` 는 `DATA` 의 선행 스냅샷이며 잃을 정보가 0건입니다.**

**② 드롭 근거 축 (3/4 확보)**

| # | 축 | 실증 | 결과 |
|---|---|---|---|
| 1 | 적재 경로 | 🟢🟢 **원천 프로시저 코드 직접 확인**(`90_provided_definition/16_bronze_gsc_ddl.sql`) — `SEARCH_CONSOLE_DATA2` 는 파일 전체에 **1회**(`:17` CREATE TABLE 선언)만 등장하고, 적재 프로시저 `SP_LOAD_GOOGLE_SEARCH`(`:35`~)는 `:84` 에서 `table_name = 'SEARCH_CONSOLE_DATA'` 로 **대상을 하드코딩**한다 ⇒ 프로시저 본문의 `DATA2` 참조 **0건** | 🟢 **적재기 미사용(코드 실증)** |
| 2 | 코드 소비처 | `10_dbt_pipeline/models`·`macros`·`05_SV-Agent_ai`·`03_top-down_gold` 전수 | 🟢 **0건** |
| 3 | 라이브 소비처 | `BRONZE_GSC` 스키마 내 뷰 **0** · `INFORMATION_SCHEMA.PROCEDURES` **0** · `SHOW TASKS IN DATABASE` **0** | 🟢 0건 |
| 4 | **용도** | 같은 파일 `:111` 이 **"용도 확인 대상"** 으로 남겨 둔 축 | 🔴 **미회신 — 이 문항** |

⚠️ **축 3의 한계**: 이 계정은 **개발계**이고 프로시저·태스크가 0건입니다 ⇒ **운영계 적재기가 `DATA2` 를 쓰는지는 이 계정 조회로 판정할 수 없습니다**(`O170-B` 판정식 = *"라이브 부재로 판정을 뒤집기 전에 「이 계정이 그 판정의 분모인가」를 물어라"*).

**③ 여쭙는 것**
1. `SEARCH_CONSOLE_DATA2` 는 **무슨 용도**입니까? (예: 적재 검증용 이전본 / 백업 / 폐기 잔존물)
2. **운영계에서 이 테이블에 적재하거나 이 테이블을 조회하는 경로가 있습니까?**
3. 없다면 **드롭해도 됩니까?** (DW 측 근거 = 고유 정보 0 · 소비처 0 · 적재기 미사용)

**④ 🔴 함께 알려 주실 것 — 테이블 COMMENT 문안**
두 테이블의 테이블 COMMENT 가 모두 **`'재송출 광고 성과 내역'`** 입니다(원천 `16_bronze_gsc_ddl.sql` `:15`·`:28`).
🟢🟢 **원천 안에서 이미 어긋납니다** — 같은 파일의 **컬럼 COMMENT 10개는 전부 검색 의미**입니다:
`검색 발생 날짜` · `사용자 검색 쿼리(키워드)` · `검색 결과에 노출된 페이지 URL` · `검색이 발생한 국가 코드` ·
`검색에 사용된 디바이스 유형` · `검색 결과 클릭 수` · `검색 결과 노출 수` · `클릭률` …
⇒ **테이블 COMMENT 만 방송 재송출 광고를 가리킵니다** ⇒ 원천 오기 가능성이 매우 높습니다.
🟢 그러나 DW 는 **고치지 않았습니다** — `50_handoff/04_데이터마이그 GN_DW_BRONZE_DDL.sql:112~115` 가
*"원천 16번이 … '재송출 광고 성과 내역' 으로 적고 있다(**원천 오기 추정**). **병합 규칙(원천 무변경)에 따라**
… 원천 문안으로 되돌렸다. 🔴 원천 오기 여부는 현업 확인 대상이며, **원천이 고쳐지면 함께 되돌린다**"* 로
이미 판정해 두었기 때문입니다.
⇒ **원천 16번의 테이블 COMMENT 를 고쳐 주시면 DW 가 함께 맞춥니다.**
🔴 **DW 가 먼저 고치면 병합 규칙 위반**이 되어 다음 기계 대조에서 차이로 잡힙니다.

**판정**: 🟠 **현업 회신 대기** — 🔴 회신 전 드롭 금지(`R4-4-3` 파괴 작업 · 별도 승인 대상).
🟢 회신이 「용도 없음 + 드롭 가능」이면 그 즉시 집행 가능하다(위 3축이 이미 확보돼 있다).

### N-19. 🆕 🔴 **[2026-09-22 O179 신설] 「그 밖」 9항목 BRONZE 실측 일괄 — 원천에 데이터가 있는지 전수 판정**

> 🔴 사용자 지시 = *"bronze 원천에 데이터 있는지 확인해줘. 없으면 자세한 설명 요청한다."*
> 🟢 **판정식 = 「컬럼이 없다」·「컬럼은 있고 값이 없다」·「값은 있고 뜻을 모른다」·「값도 뜻도 있고 연결키가 없다」는 서로 다른 상태다.**
> 네 상태의 처방이 전부 다르므로(입고요청 / 원천보완 / 어의확인 / 키설계) 한 덩어리로 묻지 않는다.
> 📏 전건 2026-09-22 실측 · 계정 LK96056 · 🔴 인용하지 말고 재라(`R2-8-4`).

| 항목 | BRONZE 데이터 | 실측 | 상태 |
|---|---|---|---|
| `§M-2` `SUBTYPE`(`SNDNG_TY_CD`) | 🟢 **있다**(전건 채움) | EMAIL 524,891/524,895 `{0,1,2}` · MSG_AT 1,191,219/1,191,219 `{0,1}` · PSTMTR 4,016/4,016 `{1,2,3}` · 🔴 `TC_CMMN_CD` 에 `SNDNG`·`TY` 계열 코드군 **0건** | **어의 미확정**(라벨 원천이 원천에 없다) |
| `§M-6` 발송상태2 | 🟢 **있다**(축B 대량 채움) | MSG_AT `TRNSMS_FAILR_CD_ID` **19,613,208/21,976,994 · 33값** · SND `CALL_STATUS` **9,699,141/10,183,916 · 34값** · 축A `TRNSMS_STAT_CD` 21,689,849 · 3값 | **어의 미확정**(축B=상태2 인지) |
| `§N-13` STOP 다중사업 | 🟢 **있다** | 중단 **1,061,431행 / 921,345명** · 회원 grain 단일사업 782,162(**84.89%**) · **다중 137,776**(최대 14) · 사업부재 1,407 · 🔴 `TM_MM_FDRM_MBER_SPNSR_DSCNTC` 에 **`SPNSR_NO` 컬럼이 없다**(키 = `MBER_NO`+`SPNSR_DSCNTC_DE`+`SER_NO`) ⇒ 중단을 후원건에 직접 붙일 수 없다 | **귀속규칙 부재**(데이터 문제 아님) |
| `§N-17` `FUNDRAISING_COST` | 🟢 **있다**(전용 플래그) | `BDGT_ACMSLT_LEDGER` 258행 = `YN_1=Y,YN_2=Y` **91** · `YN_1=NULL,YN_2=Y` **34** · 양쪽 NULL **133** | **범위 미확정**(`YN_1`↔`YN_2` 차 34행) |
| `§N-18` GSC 중복 | 🟢 **양쪽 다 있다** | `SEARCH_CONSOLE_DATA` **958,419행**(CLICKS 합 64,217) · `SEARCH_CONSOLE_DATA2` **941,298행**(합 63,026) ⇒ 부분집합 아님 · 둘 다 유값 | **용도 미확정**(드롭 가능 여부) |
| `Q10` 세세목↔캠페인 | 🟠 **이름만 있다** | `BDGT_ACMSLT_LEDGER` 258행 · `SUBDTL_ITEM_NM` 258/258 채움 · distinct **48** · 🔴 **캠페인 코드 컬럼이 원천에 없다**(계층 = `JANG`·`KWAN`·`HANG`·`MOK`·`DTL_ITEM`·`SUBDTL_ITEM` 전부 `_NM`) · 🔴 이름 정확일치 = `DIM_CAMPAIGN.CAMPAIGN_NAME` **1/48** · `DIM_MARKETING_CAMPAIGN` **1/48** | **연결키 부재**(이름매칭 2%로 불가) |
| `AGENCY_CONV_CNT` 어의 | 🔴 **컬럼은 있고 값이 없다** | `BRONZE_AGENCY.VIDEO_AD_CMPGN_DTLS.CONV_CALL_CNT` **0 / 37,769 전건 NULL** | **원천 미수집**(재적재로 안 채워짐) |
| `BLOCKING-2` | — | 구성 = `E-6`(사업목표) + `E-1`(모금성비용) | **`§D-2`·`§N-17` 로 분해 완료** |
| `BLOCKING-5` | — | GOLD 팩트 차원FK 미적재 = `A4-W`(배선 가능성) + `A4-X`(원천 부재 8) | **`§W-1` 로 분해 완료** |

🟢 **결론 = 9항목 중 「BRONZE 에 데이터가 없다」는 것은 `AGENCY_CONV_CNT` 1건뿐입니다.**
나머지는 데이터가 있고 **뜻·규칙·키가 없습니다** ⇒ 입고 요청이 아니라 **판정 회신**이 필요합니다.

🟠 **동반 적발 — `§N-13` 배선률이 인수인계 기재와 어긋난다(0.14pt).** 실측 `GOLD.FACT_MEMBER_EVENT` =
`STOP` 1,061,431행 중 `SPONSORSHIP_SK` 배선 **979,762(92.31%)** · 미배선 **81,669(7.69%)** ·
`DEV` 3,654,929행은 **100% 배선**. 🔴 인수인계는 *단일사업 92.45% / 잔여 7.29%* 로 적고 있다.
🟢 **두 수치는 grain 이 다르다** — 92.31% 는 **FME STOP 행 grain**, 84.89% 는 **중단회원 grain**(위 표),
92.45% 는 `O160` 시점의 발행값이다 ⇒ 🔴 **셋 중 어느 것도 다른 것의 반증이 아니다**(`O111 ㉠` 분모 병기).
🔴 그래도 **같은 grain 에서 92.45 → 92.31 로 움직였다면 그것은 변동이다** ⇒ 착수표 ⑭ 를 닫기 전에
`O160` 의 산출식을 열어 분모를 대조하라(이번 세션은 재측정만 하고 원인은 규명하지 않았다).

**질문(현업) — 항목별 택1**
1. `§M-2` 채널별 발신유형 `SNDNG_TY_CD` 의 뜻을 알려 주십시오. 코드사전(`TC_CMMN_CD`)에 해당 코드군이 **등재되어 있지 않습니다** ⇒ 사전에 추가해 주시거나 값별 의미를 직접 알려 주셔야 라벨을 만들 수 있습니다.
2. `§M-6` 「발송상태2」 = 전송실패코드/통화상태(축B)입니까? 축B 는 이미 **1,960만·970만행 적재**돼 있어 **이름만 맞추면 끝납니다**.
3. `§N-13` 중단 사건이 **다중 후원사업 회원 137,776명**에게 발생합니다. 어느 사업의 중단으로 귀속합니까? ⓐ 중단일과 후원 종료일이 일치하는 사업 ⓑ 최근 개시 사업 ⓒ 금액 최대 사업 ⓓ 전 사업에 분배 ⓔ 미귀속(현행). 🔴 원천에 `SPNSR_NO` 가 없어 **DW 가 규칙 없이는 고를 수 없습니다.**
4. `§N-17` `DIRECT_MNYRS_YN_1` 과 `YN_2` 의 차 **34행**(`YN_2` 만 `Y`)은 모금성비용에 **포함**입니까?
5. `§N-18` `SEARCH_CONSOLE_DATA2` 를 쓰는 곳이 있습니까? 두 테이블이 **부분집합 관계가 아니라** 행수·클릭수가 모두 다릅니다(958,419 ↔ 941,298) ⇒ 어느 쪽이 정본입니까?
6. `Q10` 예산 세세목(48종)과 캠페인을 잇는 **코드**가 원천에 있습니까? 이름 일치가 **48개 중 1개**라 이름매칭은 성립하지 않습니다. 코드가 없다면 이 축은 **영구 미연결로 확정**하겠습니다.
7. `AGENCY_CONV_CNT` 전환콜은 대행사가 **원래 제공하지 않는 항목**입니까? (컬럼은 있고 37,769행 전건 비어 있습니다) 필요하면 대행사에 항목 채움을 요청해 주십시오.

> 현재 조치: 전 항목 **현행 유지**(값 창작 0 · 라벨 창작 0 · 드롭 0). 🔴 `§N-18` 드롭은 회신 후 별도 승인 대상입니다.
**판정**: ____________

### D-2. 사업목표(FTG-B) 원천이 CRM 인지 ERP 인지 확정 부탁드립니다 🔴
> 🔄 **[2026-09-22 O179 이전]** 이 절은 원래 `-001` 에 신설했으나 그 조각이 300줄 불변식을 넘겨
> **여기로 옮겼다**(사용자 결정 · 내용 무변경 · `R2-8-2` 「이관」 표기).

**[2026-09-22 O179 신설 — 회신이 종전 확정과 반대입니다. 개명 착수 전 재확인이 필요합니다.]** 경위 = 이 테이블의 원천 표기는 **한 번 바뀐 적이 있습니다.**

| 시점 | 판정 | 기록된 근거 |
|---|---|---|
| 초기 | 원천 = **ERP** (`SILVER.ERP_BIZ_TARGET` · `TARGET_AMT` 금액) | — |
| 2026-07-20 | 원천 = **CRM** 확정 · `SILVER.CRM_BIZ_TARGET`(`TARGET_CNT` 건 + `TARGET_TYPE`)로 개명·재구축 | 정본 인벤토리 마케팅 §1 *"CRM에 부서별 목표만 존재"* + **현업 확언** |
| 2026-09-22 | 회신 = **ERP 로 들어온다** | 금차 구두 회신 |

**실측(2026-09-22 · 계정 LK96056)**

| 축 | 값 |
|---|---|
| `SILVER.CRM_BIZ_TARGET` | 테이블 실재 · **0행**(`WHERE 1=0` 스키마-only 스텁 · `E-6`) |
| `BRONZE_ERP` 실재 테이블 | **2종뿐** = `BDGT_ACMSLT_LEDGER` · `EXPENSE_RESOLUTION` ⇒ 목표 테이블 **없음** |
| `BRONZE_CRM` 실재 테이블 | 50종 · 그중 목표 관련 = `TM_CM_MBER_DVLP_GOAL`(**25,344행** · 개발목표 축) |
| 발행된 입고요청서 | `41_입고요청서_CRM_BIZ_TARGET.md` — 적재 경로를 `CRM → BRONZE_CRM.CRM_BIZ_TARGET` 로 명시 |

즉 **현재 발행된 입고요청서의 적재 경로가 CRM 기준**입니다. 원천이 ERP 라면 그 경로·스키마·테이블명이 모두 바뀝니다.

**질문(현업)**
1. 사업목표(연/추경 목표 · 월×조직×후원사업×목표유형)의 원천 시스템은 **CRM 과 ERP 중 무엇**입니까? 2026-07-20 「CRM 확정」은 **철회**되는 것입니까?
2. ERP 라면 적재 경로를 `ERP → BRONZE_ERP.<테이블명>` 으로 바꿉니다. **BRONZE 테이블명**을 지정해 주십시오(현재 작업 중이신 원천의 실제 이름).
3. 기존 **개발목표**(`TM_CM_MBER_DVLP_GOAL` · CRM · 25,344행)와 이 사업목표는 **별개**라는 전제가 유지됩니까?

> 현재 조치: **개명을 보류**했습니다(사용자 결정 2026-09-22). `SILVER.CRM_BIZ_TARGET` 이름과 `DW_SOURCE_SYSTEM='CRM'` 을 그대로 두고, 회신 후 일괄 정정합니다. 🔴 개명 영향 범위가 문서 수십 곳 + SILVER DDL 재생성 + GOLD `FACT_TARGET_PROJECT` 참조 + 입고요청서 2종이므로, **회신 없이 되돌리면 같은 작업을 세 번째로 하게 됩니다.**
**판정**: ____________

### W-1. BigQuery UTM 을 캠페인 축에 붙일 수 있습니까 — 마케팅 캠페인코드 작업과의 관계 🔴
> 🔄 **[2026-09-22 O179 이전]** 이 절도 `-001` 에서 여기로 옮겼다(내용 무변경).

**[2026-09-22 O179 신설 — 배선 과제 `A4-W` #1 의 전제가 실측으로 깨졌습니다.]** 배경 = `GOLD.FACT_BIGQUERY_BEHAVIOR.CAMPAIGN_SK` 는 현재 **전건 센티넬**(`(미매핑)`)이고, 배선 후보로 `SILVER.BIGQUERY_EVENT.UTM_CAMPAIGN` 이 지목돼 있었습니다. 원천 컬럼은 실재하지만 **캠페인 키로 쓸 수 없습니다.**

**실측(2026-09-22 · 계정 LK96056 · 참고자료)**

| 축 | 값 |
|---|---|
| `BIGQUERY_EVENT` 전체 | 10,332,737행 |
| `UTM_CAMPAIGN` 유값 | **5,463,457행**(52.9%) · distinct **700** |
| `DIM_CAMPAIGN.MKTG_UTM_NM` 유값 | 19,512행이 **252개 값**을 공유 |
| 🔴 **1 UTM ↔ 캠페인 수** | **최대 902** (평균 77) ⇒ 그대로 조인하면 팩트가 팬아웃한다 |
| UTM ↔ `MKTG_CAMPAIGN_SK` | 최대 **6** · 유일한 UTM **224 / 252** |
| 매칭 커버리지 | distinct **108 / 700** · 행 **2,863,010 / 5,463,457**(52.4%) |
| `GOLD.DIM_MARKETING_CAMPAIGN` | 591행 · **UTM 컬럼 없음**(UTM 정본 차원이 부재) |
| 상위 UTM 값 예 | `turn24b`(282,201) · `veteran26`(242,048) · `children2511a`(235,630) · `(not set)`(852,450) · `(organic)`·`(referral)` |

즉 **UTM 은 캠페인보다 한 단계 거친 축**입니다. 하나를 골라 `CAMPAIGN_SK` 에 넣으면 값 창작이 되고, 그대로 조인하면 행이 부풀어 모든 행동 지표가 과대계상됩니다.

**질문(현업)**
1. 마케팅 부서에서 진행 중인 **캠페인 코드 체계 작업**이 이 축에 영향을 줍니까? 즉 작업 후에는 **UTM ↔ 캠페인이 1:1 로 정리**됩니까? (정리되면 `CAMPAIGN_SK` 직접 배선이 가능해집니다)
2. 정리되지 않는다면, BigQuery 행동 데이터를 붙일 축은 **어느 것**입니까? ⓐ UTM 전용 차원 신설(252개) ⓑ 팩트에 `UTM_CAMPAIGN` 원값만 실기(차원 없음) ⓒ `MKTG_CAMPAIGN_SK`(마케팅캠페인 · UTM당 최대 6이라 부분 배선)
3. `(not set)`·`(organic)`·`(referral)` 같은 **비캠페인 값 852,450+행**은 캠페인 미식별로 두는 것이 맞습니까?

> 현재 조치: **배선을 보류**했습니다(사용자 결정 2026-09-22). `CAMPAIGN_SK` 는 센티넬을 유지하고, 위 실측을 참고자료로 제공합니다. 🔴 배선은 **전량 백필 의무**를 동반하므로 축이 확정된 뒤 1회로 집행합니다.
**판정**: ____________

### B-EXEC. 🆕 ✅ **[2026-09-22 O179] 이슈 `B` 집행 내역 — 고아 회원 SILVER 정제**
> 🔄 정본 문항·판정은 `-001 §B` 다. 이 절은 **집행 기록**이고, `-001` 이 300줄 불변식을 넘겨 여기로 분리했다.

> 회신 = *"이벤트나 캠페인 등을 삭제할 때 그 곳에 귀속된 회원들도 삭제되어야 하나 남은 데이터들이다.
>   지금 **회원 마스터 데이터를 정본**으로 하고, 고아 회원에 대한 건은 **SILVER 에서 데이터 정제 시 없앤다**."*
> ⇒ 🟢 **질문 1·2·3 이 한 번에 닫혔다** — 「마스터 추출 누락」이 아니라 **삭제 잔여물**이었고,
>   따라서 재추출 대상이 아니라 **정제 대상**이다. 불량 ID·NULL 회원도 같은 처분에 포함된다.

**집행 내역 (2026-09-22 O179 · 계정 LK96056)**
- 정제 술어 = `macros/gn_member_master_filter.sql` **신설**(정의 지점 1곳 · `R1-6-17`)
- 방법 = **semi-join(`EXISTS`)** — `INNER JOIN` 은 마스터가 이력화되면 조용히 팬아웃하므로 배제했다(`EXISTS` 는 정의상 행을 늘릴 수 없다). NULL 키도 자연 탈락한다.
- 마스터 = `SILVER.CRM_MEMBER.MEMBER_DK` 📏 **1,785,299 전건 유일·NOT NULL**(= `DIM_MEMBER` 일치)
- 배선 = SILVER **10모델** 전건 · `dbt build` PASS **251** / WARN 13 / ERROR **0** / SKIP 0

| SILVER 모델 | 제거 | 적재 후 | 고아 잔존 |
|---|--:|--:|--:|
| `CRM_SEND_MEMBER` | 34,246 + NULL 746 | **41,935,344** | 0 |
| `CRM_EVENT_PARTICIPATION` | 10,048 | **1,248,727** | 0 |
| `CRM_MEMBER_DEV` | 271 | **3,654,658** | 0 |
| `CRM_MEMBER_SPONSOR_SPAN` | 106 | **2,201,695** | 0 |
| `CRM_MEMBER_STATUS_HIST` | 86 | **7,644,142** | 0 |
| `CRM_PAYMENT_METHOD` | 39 | **2,588,966** | 0 |
| `CRM_MEMBER_DISCONTINUE` | 1 | **1,061,430** | 0 |
| `CRM_MEMBER_RESPONSOR` | 1 | **118,198** | 0 |
| `CRM_MEMBER_AMT_CHANGE` · `CRM_SPONSOR_RELATION` | 0 | 331,440 · 869,027 | 0 |

🟢 **대사 10/10 적중** · 제거 합 **45,544행**(전체 약 61.6M 의 0.074%) · **고아 회원 0명**.
🟢 하류 GOLD 도 설명된다 = `FACT_MESSAGE_DISPATCH` **41,935,344**(1:1) · `FACT_EVENT_ATTENDANCE` **1,248,727**(1:1) · `FACT_MEMBER_EVENT` **4,716,088**(−272 = 271+1) · `FACT_MEMBER_MONTHLY` **41,508,824**(−18 · 집계 grain 이라 원천 감소와 1:1 아님).
🔴 **부수 효과 = 이슈 `E` 가 처음으로 줄었다**(`-001 §E`) — 고아 회원과 고아 행사키는 한 사건이었다.
🟠 **잔여 1건(설계 결정 · 데이터 문제 아님)** = `CRM_MEMBER_DEV` 의 증분 설계가 실제로는 동작하지 않고 있음이 이 작업 중 드러났다(폴더 `pre-hook` TRUNCATE 가 매 run 실행됨 · `pre_hook=[]` 는 dbt 의 hook 누적 성질 때문에 취소가 안 된다). 🔴 지금은 그 TRUNCATE 덕분에 이 정제가 집행되므로 **고치면 삭제 반영이 사라진다** ⇒ 문서50 등재 · 사용자 결정 대기.

---
_Co-authored with CoCo_
