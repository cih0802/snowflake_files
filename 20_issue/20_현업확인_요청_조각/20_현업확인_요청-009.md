<!-- SPLIT-CHUNK 20_현업확인_요청.md | 009/009 | 허브 = 20_현업확인_요청.md | 원문 1331~1349행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### N-16. 🆕 🔴 **[2026-09-21 O174 신규] 회신 대기 문항 패킷 — 선행조건 순서대로 묶음**

> 🔴 이 절은 **색인**입니다(정본은 각 절). 목적 = 흩어진 문항을 **한 번에 회신**받는 것이고,
> 순서는 **선행조건 순**입니다(앞을 정하지 않으면 뒤를 구현할 수 없습니다).

| 순 | 문항 | 왜 지금 필요한가 | 정본 좌표 |
|---|---|---|---|
| 1 | **`CONF-2` 「(건)」 정의** = 약정금액 ÷ 10,000 인가 | 🔴 **B 버킷 45컬럼 구현의 선행조건** — 증액·감액·회비분해가 모두 이 단위에 걸립니다. `FMM.DEV_CNT`(사건수)와 **다른 단위**라 섞으면 조용히 다른 값이 나옵니다 | `20_현업확인_요청-007.md:82` |
| 2 | **`DEC-33` 코호트 3문항** | 🔴 **귀속창보다 모집단 정의가 선행**입니다(D+0 이 73.1% = 중단 통보 성격) ⇒ 모집단을 정하지 않으면 귀속창을 어떻게 잡아도 해석이 갈립니다 | `20_현업확인_요청-004.md:39` (§L) |
| 3 | **행사 마스터 미입고** | 🔴 라이브 재측정(2026-09-21) = `GOLD.FACT_EVENT_ATTENDANCE` 고아 **281,701 / 1,258,775 = 22.38%** · 상위 2키가 99.48%(`EVENT_105` · `EVENT_106`) ⇒ **마스터 2건 입고**로 해소됩니다. 🔴 집계 제외로 닫지 않습니다 | `50_dbt_…-013.md:77` |
| 4 | **채널별 발송 성공/실패 코드 매핑** + **`SEND_STATUS2` 처분** | 🟠 같은 축(발송 상태 코드체계)이므로 **함께 닫아야** 합니다 — 따로 정하면 두 컬럼이 서로 다른 코드계를 갖습니다 | `50_dbt_…-012.md:90` (`O59-P-1`) |
| 5 | **N-14 AGENCY 원천 4문항** | 🔴 광고비 집계 축(`E-4`)이 이 회신에 걸립니다 | 이 문서 §N-14 |
| 6 | **N-15 `#DIV/0!` 원천 정정** | 🔴 방송 `CPC` 지표의 신뢰성 | 이 문서 §N-15 |

**판정**: 🔴 **현업 회신 대기 — 1·2 가 최우선(뒤 항목의 선행조건)**

---

### N-17. 🆕 🔴 **[2026-09-21 O175 신규] `E-1` 모금성비용(`FUNDRAISING_COST`) — 원천에 전용 플래그가 이미 있습니다**

> 🔴🔴 **이 절은 종전 전제를 기각하고 발행합니다.** 문서50 `-018.md:47-49` 는 잔여를
> *「어느 `MOK_NM` 을 모금비용으로 볼 것인가 = 분류 결정」* 이라 적었으나,
> 원천에 **직접모금비 플래그 컬럼 2개가 실재**합니다 ⇒ `MOK_NM` 분류는 애초에 답이 아닙니다.

**㉠ 발견한 단서 (제공 정의·현업 인벤토리 2축)**

| 축 | 좌표 | 내용 |
|---|---|---|
| 원천 DDL | `90_provided_definition/13_bronze_erp_ddl.sql:18` | `DIRECT_MNYRS_YN_1 VARCHAR COMMENT '직접모금비1'` |
| 원천 DDL | `90_provided_definition/13_bronze_erp_ddl.sql:19` | `DIRECT_MNYRS_YN_2 VARCHAR COMMENT '직접모금비2'` |
| 신규지표 9 | `90_provided_definition/신규지표_20260820.csv:21` | *"26년 직접모금비 기준(모금방송 편성비, 디지털/영상광고 광고비, 콜센터운영비) 모금성비용"* |
| 신규지표 11 | `90_provided_definition/신규지표_20260820.csv:23` | *"ERP 매체별 직접모금비 / 매체별 신규 획득 개발 건수"* · *"ERP내 관련 세세목 추출 필요"* |

**㉡ 라이브 실측 (계정 `RY53492` · `GN_DW.BRONZE_ERP.BDGT_ACMSLT_LEDGER` 258행 · 2026-09-21)**

`DIRECT_MNYRS_YN_1='Y'` = **91행 / 집행 96,515,405,630원**. 세세목이 신규지표 9 정의와 **1:1 대응**합니다:

| 세세목(`SUBDTL_ITEM_NM`) | 집행액 | 현업 정의 대응 |
|---|---|---|
| 온라인모금(광고) | 53,139,972,661 | 디지털/영상광고 광고비 |
| 방송모금 | 22,110,622,560 | 모금방송 편성비 |
| 광고모금 | 12,089,638,584 | 디지털/영상광고 광고비 |
| 사회복지법인예산 | 5,232,150,464 | 🔴 **정의에 없습니다 — 문항 2** |
| 콜센터운영비(미디어모금) | 3,245,192,517 | 콜센터운영비 |
| 온라인모금 · 옹호사업 · 홍보사업(온라인) 등 | 697,828,844 | 잔여 소액 |

연도 축 = 2024년 56행 39,327,309,002 · 2025년 24행 46,038,547,302 · 2026년 11행 11,149,549,326.

**㉢ 🔴 `MOK_NM` 으로는 포함·제외가 갈리지 않습니다 (종전 전제 기각 근거)**

- `MOK_NM` 2종(`온라인모금`+`미디어모금`) 합 = **92,010,814,013** ≠ 플래그 합 **96,515,405,630**.
- 차액의 정체 = `사회복지법인예산` 2행 5,232,150,464 · `권리옹호사업` 11행 124,397,800 ·
  `기관홍보사업` 4행 3,733,540 에도 `YN_1='Y'` 가 붙어 있습니다.
- 반대로 `온라인모금` 32행(843,930,331 + 0원)은 `YN_1` 이 NULL 입니다.
- 3종(+`기관홍보사업`)도 답이 아닙니다 — 기관홍보사업 12행 192,106,917 은 `YN_1` NULL 이고
  `Y` 는 4행 3,733,540 뿐입니다.

**㉣ 현업 확인 문항 (4건)**

1. 🔴 **`DIRECT_MNYRS_YN_1` 과 `DIRECT_MNYRS_YN_2` 의 범위 차이가 무엇입니까?**
   실측 = `YN_1='Y'` 91행 / `YN_2='Y'` 125행이고 **`YN_2` 단독 `Y` 가 34행 2,490,599,420원** 입니다
   (`YN_1='Y'` 인 행은 전건 `YN_2='Y'` ⇒ 포함 관계). `FUNDRAISING_COST` 는 **어느 쪽**입니까?
2. 🔴 **`사회복지법인예산` 5,232,150,464원(2행)을 모금성비용에 포함합니까?**
   플래그는 `Y` 인데 신규지표 9 의 3항목(방송 편성비 · 광고비 · 콜센터운영비)에 **해당이 없습니다.**
   ⚠️ 이 항목은 **2024년에만** 있습니다(2025·2026년 0행) ⇒ 일회성 편성일 가능성이 있습니다.
3. 🟠 **`DIRECT_MNYRS_YN_1/2` 가 삭제된 `MNYRS_COST_DIV_YN`(모금비구분)의 대체입니까?**
   ⚠️ 이 문항은 `30_설계_의사결정-012.md:114` 에 **이미 발행돼 있습니다** — 같은 축이므로 함께 회신받습니다.
4. 🟠 **집계 단위를 연도(`YEAR`)로 고정합니까?** 신규지표 9 는 *"26년 직접모금비 기준"* 이라
   2026년만 대상으로 읽히나, 원천은 2024~2026 3개년을 담고 있습니다(신규지표 11 은 *"최근 2개년"*).

**㉤ 🔴 정정 대상 (이 절 발행과 함께 확정된 기존 문면 결함)**

| 좌표 | 문면 | 판정 |
|---|---|---|
| `30_output_share/15_문서 통합.md:855` | 실증 쿼리 `fct_yy`·`bgt_cd_nm`·`exec_amt` | 🔴 **실행 불가** — 3컬럼 전건 부재(`INFORMATION_SCHEMA` 대조). 실제 컬럼 = `YEAR`·`BDGT_ITEM_NM`·`EXEC_TOT_AMT` |
| `30_output_share/15_문서 통합.md:850` | *"ERP 회계 원장의 상세 모금 비용 연동이 부재함"* | 🔴 **거짓** — O170 이 258행 실재를 이미 확인(문서50 `-018.md:47`) |
| `30_output_share/20260826_아키텍처 지도 요약본.html:2703` | `FUNDRAISING_COST` COMMENT *"원천 자체가 비어 있다 · 대행사가 항목을 보고하지 않는다 · E-1 하드블로커"* | 🔴 **거짓 3주장** — `O96` 이 `PLAN_BUDGET_YEAR` 에서 시정한 **같은 패턴이 이 컬럼에 남아 있습니다** |

> ⚠️ 위 3건은 **`30_output_share/` 자동 생성물**이라 손으로 고치지 않습니다(`R1-6` 계약) —
> 정본 시정 후 재생성 대상으로 등재합니다.

**㉥ 정정 실증 쿼리 (위 ㉤ 의 대체본 · 실행 검증 완료)**

```sql
-- [N-17] E-1 모금성비용 후보 — 플래그 축이 정본이다
SELECT YEAR                AS "회계연도",
       DIRECT_MNYRS_YN_1   AS "직접모금비1",
       DIRECT_MNYRS_YN_2   AS "직접모금비2",
       MOK_NM              AS "목",
       DTL_ITEM_NM         AS "세목",
       SUBDTL_ITEM_NM      AS "세세목",
       COUNT(*)            AS "행수",
       SUM(EXEC_TOT_AMT)   AS "집행금액합계"
FROM GN_DW.BRONZE_ERP.BDGT_ACMSLT_LEDGER
WHERE DIRECT_MNYRS_YN_1 = 'Y' OR DIRECT_MNYRS_YN_2 = 'Y'
GROUP BY 1, 2, 3, 4, 5, 6
ORDER BY 1 DESC, 8 DESC;
```

**판정**: 🔴 **현업 회신 대기 — 문항 1·2 가 `GOLD.FACT_BUDGET.FUNDRAISING_COST` 배선의 선행조건**
(🟢 종전 「외부 원천 입고 대기」는 아닙니다 — 원천은 실재하고 **정의 확정만** 남았습니다)

---

### N-18. 🆕 🟠 **[2026-09-22 O176 신규] `BRONZE_GSC.SEARCH_CONSOLE_DATA2` 용도 — 드롭 가능 여부 (착수표 ⑧)**

> 경위: `BRONZE_GSC` 에 구조가 **완전히 동일한** 두 테이블이 있습니다.
> DW 측 조사로 **드롭 근거 3축이 확보**됐고 **용도 확인 1축만** 남았습니다.
> 🔴 드롭은 파괴 작업이라 회신 없이 집행하지 않습니다.

**① 실측 (2026-09-22 · 계정 `LK96056`)**

| 축 | `SEARCH_CONSOLE_DATA` | `SEARCH_CONSOLE_DATA2` | 판정 |
|---|---:|---:|---|
| 행수 | 958,419 | 941,298 | — |
| 컬럼 구성 | 10 | 10 · **이름·순서 완전 동일** | 🟢 동일 스키마 |
| 날짜 범위 | 2025-04-08 ~ **2026-09-18**(529일) | 2025-04-08 ~ **2026-09-08**(519일) | 🟢 `DATA2` 가 **10일 이르다** |
| 클릭 / 노출 합 | 64,217 / 3,436,887 | 63,026 / 3,375,761 | — |
| **`DATA2` EXCEPT `DATA`** | — | **0행** | 🟢🟢 **진부분집합 — 고유 정보 0** |
| `DATA` EXCEPT `DATA2` | **17,121행** | — | 🟢 `DATA` 가 최신 |

⇒ 🟢 **`DATA2` 는 `DATA` 의 선행 스냅샷이며 잃을 정보가 0건입니다.**

**② 드롭 근거 축 (3/4 확보)**

| # | 축 | 실증 | 결과 |
|---|---|---|---|
| 1 | 적재 경로 | 🟢🟢 **원천 프로시저 코드 직접 확인**(`90_provided_definition/16_bronze_gsc_ddl.sql`) — `SEARCH_CONSOLE_DATA2` 는 파일 전체에 **1회**(`:17` CREATE TABLE 선언)만 등장하고, 적재 프로시저 `SP_LOAD_GOOGLE_SEARCH`(`:35`~)는 `:84` 에서 `table_name = 'SEARCH_CONSOLE_DATA'` 로 **대상을 하드코딩**한다 ⇒ 프로시저 본문의 `DATA2` 참조 **0건** | 🟢 **적재기 미사용(코드 실증)** |
| 2 | 코드 소비처 | `10_dbt_pipeline/models`·`macros`·`05_SV-Agent_ai`·`03_top-down_gold` 전수 | 🟢 **0건** |
| 3 | 라이브 소비처 | `BRONZE_GSC` 스키마 내 뷰 **0** · `INFORMATION_SCHEMA.PROCEDURES` **0** · `SHOW TASKS IN DATABASE` **0** | 🟢 0건 |
| 4 | **용도** | 같은 파일 `:111` 이 **"용도 확인 대상"** 으로 남겨 둔 축 | 🔴 **미회신 — 이 문항** |

⚠️ **축 3의 한계**: 이 계정은 **개발계**이고 프로시저·태스크가 0건입니다 ⇒ **운영계 적재기가 `DATA2` 를 쓰는지는 이 계정 조회로 판정할 수 없습니다**(`O170-B` 판정식 = *"라이브 부재로 판정을 뒤집기 전에 「이 계정이 그 판정의 분모인가」를 물어라"*).

**③ 여쭙는 것**
1. `SEARCH_CONSOLE_DATA2` 는 **무슨 용도**입니까? (예: 적재 검증용 이전본 / 백업 / 폐기 잔존물)
2. **운영계에서 이 테이블에 적재하거나 이 테이블을 조회하는 경로가 있습니까?**
3. 없다면 **드롭해도 됩니까?** (DW 측 근거 = 고유 정보 0 · 소비처 0 · 적재기 미사용)

**④ 🔴 함께 알려 주실 것 — 테이블 COMMENT 문안**
두 테이블의 테이블 COMMENT 가 모두 **`'재송출 광고 성과 내역'`** 입니다(원천 `16_bronze_gsc_ddl.sql` `:15`·`:28`).
🟢🟢 **원천 안에서 이미 어긋납니다** — 같은 파일의 **컬럼 COMMENT 10개는 전부 검색 의미**입니다:
`검색 발생 날짜` · `사용자 검색 쿼리(키워드)` · `검색 결과에 노출된 페이지 URL` · `검색이 발생한 국가 코드` ·
`검색에 사용된 디바이스 유형` · `검색 결과 클릭 수` · `검색 결과 노출 수` · `클릭률` …
⇒ **테이블 COMMENT 만 방송 재송출 광고를 가리킵니다** ⇒ 원천 오기 가능성이 매우 높습니다.
🟢 그러나 DW 는 **고치지 않았습니다** — `50_handoff/04_데이터마이그 GN_DW_BRONZE_DDL.sql:112~115` 가
*"원천 16번이 … '재송출 광고 성과 내역' 으로 적고 있다(**원천 오기 추정**). **병합 규칙(원천 무변경)에 따라**
… 원천 문안으로 되돌렸다. 🔴 원천 오기 여부는 현업 확인 대상이며, **원천이 고쳐지면 함께 되돌린다**"* 로
이미 판정해 두었기 때문입니다.
⇒ **원천 16번의 테이블 COMMENT 를 고쳐 주시면 DW 가 함께 맞춥니다.**
🔴 **DW 가 먼저 고치면 병합 규칙 위반**이 되어 다음 기계 대조에서 차이로 잡힙니다.

**판정**: 🟠 **현업 회신 대기** — 🔴 회신 전 드롭 금지(`R4-4-3` 파괴 작업 · 별도 승인 대상).
🟢 회신이 「용도 없음 + 드롭 가능」이면 그 즉시 집행 가능하다(위 3축이 이미 확보돼 있다).

---
_Co-authored with CoCo_
