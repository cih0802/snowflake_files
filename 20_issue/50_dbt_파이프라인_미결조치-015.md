<!-- SPLIT-CHUNK 50_dbt_파이프라인_미결조치.md | 015/015 | 허브 = 50_dbt_파이프라인_미결조치.md | 원문 2118~2270행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## 🔜 [G-5 게이트] SILVER identity 2모델 — merge 전략 전환 (전기간 샤드 입고 시)
> 트리거: **G-5(GA4 전기간 샤드 입고) 확정 시점.** 그 전까지는 현행 유지(착수 불요).

**배경(아키텍처 검토 2026-07-15)**: `GA4_IDENTITY`·`IDENTITY_MEMBER_XREF`는 grain=`user_pseudo_id`. 활성 회원은 기기·쿠키이탈(ITP 7일 만료·시크릿 등)로 pseudo가 지속 증가 → 두 모델은 계속 성장. 현재는 프로젝트 SILVER 설정(`incremental_strategy: append` + `pre-hook: TRUNCATE` + `ga4_union_shards(var)` 윈도우)이 **매 실행 TRUNCATE 후 윈도우 전량 재계산**이라 크기에 상한은 걸리나, 전기간·상시운영 전환 시 **매 실행 전기간 재스캔 = 비용 급증**.
> GOLD `DIM_MEMBER_IDENTITY`·FACT(`IDENTITY_SK`)는 **회원 grain이라 pseudo 증가와 무관·bounded**(안전). 조치 대상은 SILVER pseudo-grain 2모델뿐.

**조치(G-5 시)**:
1. `GA4_IDENTITY`·`IDENTITY_MEMBER_XREF` → `incremental_strategy: merge`, `unique_key='USER_PSEUDO_ID'`(XREF는 동일), `pre-hook: TRUNCATE` 제거 → 신규 pseudo만 upsert(전량 재스캔 회피).
2. **보존/시점 정책** 결정: pseudo→member 매핑에 `VALID_FROM/VALID_TO`(SCD2) 부여 or 롤링 보존(예: 24개월).
3. **충돌·지연도착 규칙** 명문화: pseudo가 나중에 user_id 획득(NULL→DIRECT)·타회원 재매핑(공용기기, n_id≥2=CONFLICT) 시 last-wins/신뢰도 우선.
4. 성장 시 `CLUSTER BY`(member 또는 event_date)로 조인 프루닝 검토.
- **주의**: 이 전환은 프로젝트 SILVER 공통설정(append+TRUNCATE)과 다르므로 **모델별 config 오버라이드**로 격리(다른 SILVER 모델 영향 금지).
- **검증**: 전환 후 `dbt build --select GA4_IDENTITY IDENTITY_MEMBER_XREF` green + 행수 단조증가(재계산 아님) 확인.

> 참고: `DIM_MEMBER_IDENTITY`는 2026-07-15 세션에서 `enabled=true` 전환·XREF dedup 조인 배선 완료(문서40 E-6 인접). 위 §"결정 대기 GOLD 6"의 "disabled 유지 권고"는 이로써 갱신됨.
>
> **⚠️ [G-5 재확인·재실행 필수] identity 결선 다운스트림 3모델** — `DIM_MEMBER_IDENTITY`·`FACT_GA_BEHAVIOR`(IDENTITY_SK)·`WIDE_GA_BEHAVIOR`(IDENTITY_* 4컬럼)는 **GA4 1일 샤드(커버리지 4.22%, 매칭 1,274명) 기반으로 배선**된 상태. GA4 전기간 입고(G-5) 시 반드시: ① `GA4_IDENTITY`·`IDENTITY_MEMBER_XREF` 재적재 → ② `DIM_MEMBER_IDENTITY`·`FACT_GA_BEHAVIOR`·`WIDE_GA_BEHAVIOR` 재빌드 → ③ 매칭 커버리지·fan-out(IDENTITY_SK 유일성)·FK 무결성 재검증. 커버리지 급증으로 매칭수·grain이 크게 변동하므로 **1일 기반 수치를 확정치로 오인 금지**.

---

### 🔴 [2026-08-20 O91-C] `06_DDL` 재실행 후 `build` 실측 — **1 ERROR / 18 모델 SKIP · WARN 회귀 0**

> 경위: 사용자가 `06_DDL` 전량 재실행(19:29:40~56 · 35테이블 `CREATE OR REPLACE`) 후 `build` 를 돌렸고
> *"중간에 멈춘 것 같다"* 고 보고했다. 🔴 **멈춘 것이 아니라 명확히 실패했다.**
> 근거 정본 = `10_dbt_pipeline/target/run_results.json`(dbt **1.9.4** · elapsed **530.3s** · nodes **437**)
> + `QUERY_ID 01c681b7-3204-689b-0008-215e000e2516`(19:31:09→19:40:17 · `GN_DW_ENGINEER`/`GN_DW_ETL_WH`).
> 🔴 **`SHOW ... / INFORMATION_SCHEMA` 만으로 판정하지 말 것** — 「LAST_ALTERED 가 build 이전」인 팩트를
> 「build 가 건드리지 않았다」로 읽으려면 **SKIP 목록이 필요하다**. 이번에 실제로 그 함정에 빠졌다가 정본으로 교정했다.

**① 상태 집계 (`run_results.json` 확정 · 437노드)**

| status | n | 비고 |
|---|---:|---|
| `pass`(테스트) | 256 | |
| `success`(모델) | 70 | |
| `warn` | **31** | 아래 ③ |
| `skipped` | **79** | 모델 **18** + 테스트 **61** |
| `error` | **1** | `model.gn_dw_silver.BIGQUERY_REFINED_DATA` |

**② 🔴 근인 = `USER_PSEUDO_ID` NOT NULL 위반 111행 (0.0003%) 이 19노드를 막았다**

`100072: DML operation to table GN_DW.SILVER.BIGQUERY_REFINED_DATA failed on column USER_PSEUDO_ID with error: NULL result in a non-nullable column`

· 원천 실측 = `BRONZE_BIGQUERY.EVENTS` **32,718,672행 중 `"user_pseudo_id"` NULL 111행**(빈문자 0).
  🟢 창 = **2024-06-05 ~ 2024-06-10 6일 · 6샤드**에 국한 · 전건 `"user_id"` 도 NULL · `"stream_id"` 는 존재 ·
  이벤트는 정상 4종(`scroll`·`page_view`·`session_start`·`first_visit`) ⇒ **동의모드/쿠키리스 추정**(미확정).
· 🟢 **연쇄 위험 없음** — SILVER 정본의 `NOT NULL` **10컬럼** 전수를 원천에서 검사해 **`USER_PSEUDO_ID` 하나만** 걸렸다
  (`event_timestamp`·`event_name`·`EVENT_DT`·`SRC_TABLE`·`SRC_FILE_NAME`·`LOAD_TS` 전부 NULL 0).
  ⇒ **이 한 축만 처리하면 19노드가 함께 풀린다.**
· 🔴 **파급 = 스킵된 18모델** — GA4 SILVER 파생 5(`GA4_DEVICE`·`GA4_EVENT`·`GA4_EVENT_DIM`·`GA4_IDENTITY`·`GA4_TRAFFIC_SOURCE`) ·
  `IDENTITY_MEMBER_XREF` · DIM 4(`DIM_GA_SOURCE`·**`DIM_DEVICE`**·`DIM_GA_EVENT`·`DIM_MEMBER_IDENTITY`) ·
  FACT 2(**`FACT_AD_PERFORMANCE`**·`FACT_GA_BEHAVIOR`) · WIDE 6(`AD_BROADCAST`·`AD_BROADCAST_CASE`·`AD_COMBINED`·`AD_DIGITAL`·`AD_PERFORMANCE`·`GA_BEHAVIOR`).
· 🔴🔴 **왜 광고 계열까지 죽었나 = `DIM_DEVICE` 가 GA4 파생이다.** `FACT_AD_PERFORMANCE` 가 `DIM_DEVICE` 를 `ref` 하므로
  **GA4 실패가 광고 도메인 전체로 전파**된다. ⇒ 「GA4 실패는 GA4 만 막는다」는 직관은 **틀렸다.**
  🟠 이 결합은 구조 리스크로 별도 등재 대상이다(광고 성과가 GA4 입고 상태에 인질로 잡힌다).
· 🔴 **처리 방향은 결정 사안이다**(임의 선택 금지): ㉠ 원천 정정 요청 ㉡ 111행 필터 제외(계보에 기록) ㉢ 센티넬 라우팅
  ㉣ `NOT NULL` 완화. ⚠️ `USER_PSEUDO_ID` 는 **grain 1번 컬럼**이라 ㉣ 는 키 정의를 바꾼다 — 가볍지 않다.
  🔴 `DEC-39`(SILVER 원천 겸용 · `30 §26`)로 가면 **119컬럼 고정 DDL 이 같은 제약을 영구화**하므로 그 결정과 함께 판단할 것.

**③ 🟢 WARN 31 판정 = 기지 축 회귀 0** (기지값 = 위 §「WARN 27건 전량 목록」 · 2026-08-07 `TOTAL=402`)

· 🟢 **기지 27건 중 26건이 항목·건수까지 전량 일치**(263,611 · 31,486 · 18,091 · 11,313 · 9,480 · 1,096 · 745 · 740 · 309 · 271 · 270 · 199 · 85 · 37 · 23 · 18 · 16 · 9 · 5 · 4 · 1 · 1 · 1 …).
· ⚠️ **미평가 1건** = `relationships GA4_IDENTITY.MBER_NO → CRM_MEMBER`(기지 172). GA4 체인 SKIP 으로 **실행되지 않았다** —
  🔴 **「해소」가 아니라 「미측정」이다.** 재실행 후 172 와 대조할 것.
· 🟢 **신규 5건은 전부 「나중에 추가된 테스트가 기지 이슈를 드러낸 것」이고 회귀가 아니다**(노드 총계 **402 → 437 = +35** 가 방증):
  ㉠ `warn_fep_nonnumeric_member_dk` **62** = O59-H 가 신설한 테스트. **문서 기재 「비수치 62종」과 정확히 일치**(스캐너 페이로드 `bxss.me` 등).
  ㉡ `not_null CRM_SEND_MEMBER.SEND_RESULT_NAME` **1,401,419** — `where SEND_RESULT_CD is not null` ⇒ **코드는 있고 이름 매핑이 없는 행**.
     🟢 이것은 위 **B1(채널별 성공/실패 코드매핑 업무확정 대기)** 축 그 자체다. 신규 결함이 아니라 **기지 결손의 계량**이다.
  ㉢~㉤ `not_null CRM_EVENT_PARTICIPATION.PARTCPT_{CHNNL,PATH,STAT}_NM` **각 2** (`where *_CD is not null`)
     = O59-G 가 *"나머지 16행은 dbt 로 드러나지 않는다"* 고 적은 **빈값 6행 축**을 테스트로 노출한 것(2×3=6 로 정합).
· 🔴 **따라서 「WARN 이 27이 아니면 회귀」라는 단순 판정식은 쓰지 말 것** — 테스트가 추가되면 총계는 당연히 변한다.
  **판정은 항목 단위 대조로 한다**(기지 항목의 건수 변화 + 신규 항목의 출처 규명). 이 절이 그 방식의 선례다.

**④ 🟢 부수 확인 — 지난 취소가 남긴 3건은 이 build 가 전부 정리했다**

· `FACT_MEMBER_MONTHLY` **0 → 40,054,883** = **기준선과 정확히 일치**(강한 회귀 무증거). `WIDE_MEMBER_MONTHLY` 도 생성됨.
· `FACT_MEMBER_MONTHLY__DBT_TMP` 고아 뷰 **소멸**(`%__DBT_TMP` 잔존 0) ⇒ 별도 정리 승인 **불요**.
· GOLD 팩트 적재 = `FACT_MEMBER_FEE` 40,262,076 · `FACT_SERVICE_EVENT` 38,467,142 · `FACT_MEMBER_EVENT` 4,633,105 ·
  `FACT_MEMBER_COHORT` 1,585,949 · `FACT_EVENT_PARTICIPATION` 1,134,126 · `FACT_AD_DIGITAL` 205,059 ·
  `FACT_BUDGET` 51,576 · `FACT_AD_BROADCAST` 38,486 · `FACT_DEV_ACHIEVEMENT` 37,522 · `FACT_TARGET_DEV` 25,344 ·
  `FACT_AD_BROADCAST_CASE` 5,340. **0행 = `FACT_AD_PERFORMANCE`(SKIP) · `FACT_GA_BEHAVIOR`(SKIP) · `FACT_TARGET_BIZ`(실행됨·원천 결손)**.
· 🟢 **`06_DDL` 의 `CREATE OR REPLACE` 가 GOLD grant 를 지우지 않았다** — GOLD FUTURE grant 35건이 새 테이블에 자동 적용돼
  ENGINEER 적재가 성공했다. ⇒ `07` 머리말 C 항목(재생성 시 §D 재실행)의 **예외 조건**이 실증됐다:
  **FUTURE grant 가 살아 있으면 테이블 재생성은 안전하다. 위험한 것은 「스키마」 재생성이다**(그때 FUTURE 까지 사라진다).
· 🟢 `dbt_test__audit` 테이블 **0** · `OPS` 테이블 **4**(`store_failures`) ⇒ O91 의 audit 스키마 선생성·grant 가 **실동작 확인**됐다.
· 🔴 **`FACT_BUDGET` 51,576 은 이 build 에서도 동일** ⇒ 「취소 때문」 가설은 **반증**됐다. 대장 24.5K 대비 2배 괴리는
  **재현되는 현상**이므로 원천/조인 축으로 좁혀야 한다(정본 = `-012` §O91-B ③).

---

### 🟢 [2026-08-20 O91-D] `DEC-40` — GA4 `USER_PSEUDO_ID` 결측 행은 **DW 미편입(필터 제외)** · 이 절이 내용 정본

> 사용자 결정(2026-08-20) = **필터 제외**. 원천 정정은 불가하다고 확인됐다.
> 🔒 라벨 = `id_collision_gate --next DEC` = **DEC40**(정의 36 ↔ 참조 39).
> 🟢 **[2026-08-20 갱신] 정식 `30_설계_의사결정.md` §27 등재 완료** — 사용자 승인으로 `--rebalance`(7→8조각)를 돌려
> 자리를 확보한 뒤 등재했다. ⇒ **결정 정본은 `30 §27` 이고, 이 절은 실측 근거·경위의 정본이다**(역할 분리).
> 🟠 `id_collision_gate` 정의축이 `DEC-40` 을 못 세는 것은 **기지 한계**다 — 게이트가 표 행 형태만 인식하고
> `30_설계` 는 헤딩 형태를 쓴다(`DEC-37~39` 동일 · 게이트 주석 `:224` 에 명시 · 규약 미결 = 원장 §O64).
> `R1-4-2` 의 **양축 최댓값 + 1** 규칙이 이를 보정하므로 관례를 유지했다.

**① 규칙 (구현 세부가 아니라 규칙으로 적는 이유)**

> **「`USER_PSEUDO_ID` 없는 GA4 이벤트는 DW 에 넣지 않는다.」**

🔴 `WHERE` 절이 아니라 규칙으로 등재한 이유 = `DEC-39`(SILVER 원천 겸용)로 Python 프로시저가 119컬럼
고정 DDL 을 직접 채우게 되면 **같은 `NOT NULL` 제약이 그대로 재발**한다. 필터가 dbt 모델에만 있으면
프로시저 경로에서 다시 터진다. ⇒ **프로시저 이관 시 이 필터를 함께 옮기는 것이 DoD 다.**

**② 왜 센티넬이 아닌가 — 판단 기준을 남긴다**

| | 센티넬(`SK=0`)이 맞는 경우 | 이 건(`USER_PSEUDO_ID`) |
|---|---|---|
| 컬럼 역할 | **차원 FK**(부가 축) | **grain 구성 컬럼 = 사실의 정체성** |
| 행을 버리면 | 총계가 틀어진다 | 이벤트 111건만 빠진다(0.0003%) |
| 센티넬을 넣으면 | 총계 보존 · 축만 Unknown | 111행이 `COUNT(DISTINCT USER_PSEUDO_ID)` 에서 **1명으로 뭉친다** |

· 🔴 센티넬은 「정체불명 1명이 6일간 111번 활동」이라는 **없는 사실을 만든다.** `user_id` 도 전건 NULL 이라
  실제 인원 복원은 **원천적으로 불가**하다 ⇒ 어떤 값을 넣어도 틀린다. 제외하면 「모수에서 빠졌다」는
  사실만 남고 그건 정확히 기록할 수 있다.
· 🟢 **판정 기준(일반화)** = **grain 구성 컬럼이면 제외 · 차원 FK 면 센티넬.**
  🔴 이 예외는 **grain 컬럼 한정**이다 — `SERVICE_SK=0`·`EVENT_SK=0` 등 센티넬 관례(`P21`·`DEC-17-B`)는 그대로 유지한다.
· ⚠️ 참고로 센티넬은 **기술적으로는 가능**했다 — 111행의 `(event_timestamp, event_name)` 이 전부 유일해
  `EVENT_SEQ` tiebreak 없이도 grain 충돌이 없다(실측). 즉 **기술 제약이 아니라 의미 판단으로 제외를 골랐다.**

**③ 구현 3종 (전부 완료 · compile 검증됨)**

| # | 산출물 | 내용 |
|---|---|---|
| ㉠ | `models/silver/ga4/BIGQUERY_REFINED_DATA.sql` | `src` CTE 에 `AND "user_pseudo_id" IS NOT NULL` + **계보 주석 32줄**(규모·창·근거·승계 의무) |
| ㉡ | `tests/warn_ga4_null_user_pseudo_id.sql` | **신규** · `severity=warn` · **기지 창(2024-06-05~10) 제외** ⇒ 새 사고만 울린다 |
| ㉢ | 원장 §1 `DEC-40` 행 | 라벨 선점 등재(`R1-4-3`) |

· 🟢 **검증** = `EXECUTE DBT PROJECT FROM WORKSPACE … args='compile --select BIGQUERY_REFINED_DATA warn_ga4_null_user_pseudo_id'`
  → **SUCCESS=TRUE** · `Found 89 models, 349 data tests`(종전 **348** ⇒ 신규 테스트 1건 등록 확인).
  렌더링 확인 = 범위 술어 뒤에 `AND "user_pseudo_id" IS NOT NULL` 이 정상 결합(술어 재작성 없음 · `R1-6-17` 준수).
· 🟢 **테스트 사전 실측** = 기지 창 밖 NULL **0행** ⇒ 창이 111행을 **완전히 덮는다**(현재 정상 상태 = 0행).
· 🔴 **`--project-dir` 로컬 dbt 는 쓸 수 없다** — 스테이지 경로 로깅이 `OSError: [Errno 95] Operation not supported`
  로 죽는다. ⇒ **컴파일 검증은 `EXECUTE DBT PROJECT … args='compile …'` 로 한다**(환경 지뢰 · 다음 세션 승계).

**④ 🔴 이 절이 만드는 새 부채 — 반드시 승계할 것**

· **BRONZE ↔ SILVER 행수가 111행 어긋난다**(32,718,672 ↔ 32,718,561 예상). 🔴 **이것을 적재 손실로 오진하지 마라.**
  🟠 `warn_ga4_base_row_reconciliation`(O88 신설)이 이 차이를 어떻게 취급하는지 **미확인**이다 —
  그 게이트가 BRONZE 기준으로 대조하면 **111행 차이로 상시 WARN** 이 될 수 있다. ⇒ `build` 후 확인 대상.
· 🟠 **`P103-⑤` 위험은 옮겨간 것이지 사라진 것이 아니다** — 기지 창을 뺀 것은 이 테스트를 살리기 위한 선택이고,
  그 대가로 **창 안의 변화는 보이지 않는다**. 그 창을 재적재하면 이 테스트가 아니라 위 §O91-C 수치를 직접 재라.
· ⚠️ **원인은 여전히 미확정**(동의모드/쿠키리스 **추정**). 원천 정정 불가라 규명 실익이 낮아 **추정으로 남긴다** —
  🔴 추정을 확정으로 인용하지 말 것.

---

### 🟡 [2026-08-20 O91-E] `DEC-40` 적용 후 `build` 2차 — **GA4 체인 해소 · 잔여 실패 1건은 stale 테스트**

> 사용자 실행(`QUERY_ID 01c681ed-3204-6801-0008-215e000f6612` · 20:25:12→20:32:07 · **415초**) ·
> 사용자 질문 = *"에러인지 확실하지 않다"* → **에러다**(`ERROR=1`). 단 성격이 O91-C 와 완전히 다르다.
> 정본 = `run_results.json`(dbt 1.9.4 · elapsed **393.5s** · nodes **438**).

**① 🟢 `DEC-40` 필터가 통했다 — 근인이 교체됐다**

| status | O91-C(1차) | O91-E(2차) | 판정 |
|---|---:|---:|---|
| `success`(모델) | 70 | **86** | 🟢 +16 |
| `pass` | 256 | 299 | 🟢 |
| `warn` | 31 | 33 | 아래 ③ |
| `skipped` | **79** | **19** | 🟢 −60 |
| 실패 | `error` 1(**모델**) | `fail` 1(**테스트**) | 🟢 등급 하락 |

· 🟢 `SILVER.BIGQUERY_REFINED_DATA` **28,655,586행 적재**(1차 = 0행 · `error`) ⇒ **`DEC-40` 이 목적을 달성했다.**
· 🟢 **광고 도메인 완전 복구** — `FACT_AD_PERFORMANCE` **243,545행**(1차 0 · SKIP) · `WIDE_AD_*` **5종 전부** ·
  WIDE 총 **13/14** · `DIM_DEVICE` 4행. ⇒ O91-C 가 지적한 `DIM_DEVICE` 경유 GA4→광고 전파가 **해소됐다.**

**② 🔴 잔여 실패 = 테스트가 stale 이고 데이터는 정상이다**

`fail | accepted_values_IDENTITY_MEMBER_XREF_MATCH_METHOD__MEMBER_ID_EXACT__UNMATCHED` — `Got 1 result`.

| 축 | 값 |
|---|---|
| 라이브 `MATCH_METHOD` | `MEMBER_ID_EXACT` **100,504** · `UNMATCHED` **165** · **`NOT_A_MEMBER_ID` 113** |
| 종전 `_silver_bridge_schema.yml:12` | `values: ['MEMBER_ID_EXACT', 'UNMATCHED']` ← **`NOT_A_MEMBER_ID` 누락** |

· 🔴 O87 이 `IDENTITY_MEMBER_XREF.sql:9~16` 에서 이 값을 **의도적으로 신설**했다(「회원번호인데 CRM 부재」와
  「회원번호 형식 아님」을 뭉개면 **채움률 분모가 왜곡**된다 · `DEC-17-B` 라벨 창작 아님). **짝 테스트만 안 고쳤다.**
· 🔴🔴 **같은 노드가 같은 방식으로 두 번째다** — 같은 yml 의 `MEMBER_DK` 주석(2026-07-29 교정)이
  *"하류 18노드가 SKIP 되어 … 스테일로 남았다"* 고 **이미 기록**하고 있다. 그때는 `not_null`, 이번은 `accepted_values`.
  ⇒ **교훈이 파일 안에 있었는데도 재발했다.** 규칙으로 승격 = **모델이 분류값을 늘리면 같은 커밋에서 목록을 고친다.**
· 🟢 **시정 완료** = `accepted_values` 에 `'NOT_A_MEMBER_ID'` 추가 + 경위 주석. `parse` **SUCCESS** 검증.
· 🟠 `NOT_A_MEMBER_ID` **113행**이 O59-H 의 스캐너 페이로드 축(비수치 62종)과 겹치는지는 **미측정** — 별건.

**③ 🟢 WARN 33 = 기지 31건 항목·건수 전량 일치 + 신규 2건 (회귀 0)**

· 🟢 **`relationships GA4_IDENTITY.MBER_NO` = 153** ← O91-C 가 **「미평가·해소 아님」**으로 남긴 항목이 드디어 측정됐다
  (1차엔 SKIP). 기지 **172 → 153**. ⚠️ 원천 범위가 다르므로 **단순 감소로 읽지 말 것** — 항목 존재 확인까지가 이번 성과다.
· 🔴 **`warn_ga4_base_row_reconciliation` = 1 발화** ← O91-D 가 **미확인**으로 남긴 게이트. **발화했다.**

**④ 🟢 정합성 게이트 분해 — O91-D 가 남긴 숙제의 답**

게이트는 `where BRONZE_ROWS <> BASE_ROWS` 이고 **차이 전부를 `FOLDED_ROWS` 로 부른다.**
`store_failures` 실측(`GN_DW.dbt_test__audit.warn_ga4_base_row_reconciliation`) =
`BRONZE 29,279,246` · `BASE 28,655,586` · `FOLDED 623,660`(**2.13%**) · `PARAMS_NULL 0` · `2024-06-01~2026-06-30`.

| 구성 | 행수 | 비중 |
|---|---:|---|
| 게이트 보고 `FOLDED_ROWS` | 623,660 | — |
| ↳ **진짜 GROUP BY 접힘** | **623,549** | BRONZE 대비 **2.1297%** |
| ↳ **`DEC-40` 의도적 제외** | **111** | gap 의 **0.0178%** |

· 🟢 **결론 = `DEC-40` 과 무관하게 발화했을 게이트다**(접힘만으로 623,549). O91-D 의 「상시 WARN 우려」는
  **원인이 다르다**는 형태로 해소됐다. 🔴 다만 **`FOLDED_ROWS` 라벨이 111행만큼 부정확**하다 —
  게이트가 「접힘」과 「의도적 제외」를 **구분하지 못한다**. 🟠 개선안 = 분자에서 `DEC-40` 제외분을 빼거나 별 컬럼으로 분리.
· 🔴 **`R1-6-17` 위반 자기신고** — 이 분해를 하며 범위 술어를 `BETWEEN '2024-06-01'~'2026-06-30'` 로 **추측해 다시 썼고**
  게이트 값과 어긋났다(31,690,408 ↔ 29,279,246). 정본 = `macros/ga4_range_predicate.sql` + `dbt_project.yml:46~49`
  = **3개 6월 구간**(2024-06 · 2025-06 · 2026-06). 매크로를 읽어 재계산해 게이트 값과 일치시켰다.
  ⇒ **조문이 왜 있는지 그 자리에서 증명됐다. 술어는 언제나 매크로에서 읽어라.**

**⑤ 🟢 영향 범위 = GA4 계열 한정 (사용자 질문에 대한 답)**

`manifest.json child_map` 전수 추적 = `IDENTITY_MEMBER_XREF` → `DIM_MEMBER_IDENTITY` → `FACT_GA_BEHAVIOR`
→ `WIDE_GA_BEHAVIOR` **딱 3모델**이고 **비-GA4 모델 의존 0**이다. CRM·ERP·AGENCY·광고 계열은 무영향.

· 🔴 **단 한 가지 도메인 교차 경로가 있다** — `05_SV-Agent_ai/05_4_SV_DDL_SERVICE.sql` 의 SV COMMENT(Agent 지침)가
  *"GA 지표는 `GOLD.WIDE_GA_BEHAVIOR` 직접 조회로 안내"* 라고 지시하는데 **그 뷰가 라이브에 없다**
  (`DIM_MEMBER_IDENTITY` 0행 · `FACT_GA_BEHAVIOR` 0행 · `WIDE_GA_BEHAVIOR` **부재**).
  ⇒ **CRM 축 SV 를 쓰는 Agent 가 GA 질문에 존재하지 않는 객체로 안내한다.** 구조 의존은 아니지만
  **답변 품질로 새는 경로**다. 🟢 이 build 재실행으로 3모델이 만들어지면 해소된다.
· 🟢 SV 층 **구조 의존은 0** — GA 팩트 위에 세운 SV 가 없다(SV COMMENT 자체가 *"GA 쪽에는 SV 가 없다"* 고 명시).

---
_생성: 순서 9-B 세션. 갱신 시 항목별 `[DONE]`·날짜 표기._
_Co-authored with CoCo_
