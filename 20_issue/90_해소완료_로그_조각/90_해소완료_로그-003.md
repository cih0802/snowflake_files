<!-- SPLIT-CHUNK 90_해소완료_로그.md | 003/003 | 허브 = 90_해소완료_로그.md | 원문 193~223행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## O85 은퇴 이관 2회차 — 문서·규약 축 닫힌 행 4건 (2026-08-18)

> 🔴 **원장 §1 대시보드에서 무변경 이관**(`R2-8` · `O85`). 행 키는 원장에 **그대로 남아 있고**
> 이 문서에는 그 행의 **장문 셀 원문**이 있다. 원장 쪽에는 이 절을 가리키는 포인터만 남았다.
> ⚠️ **행을 삭제한 것이 아니다** — `R1-7-4`(행 키 = 인용 좌표) 준수. 열 수도 그대로다.

### 🆕 수정 문서 지침 대조 자기검증 — 결함 4건 적발·시정(내부)

✅ **[2026-08-18 O83-H]** 사용자 지시로 **이 세션 수정분만** 지침 축별 대조 ⇒ 기계 게이트는 전수 PASS 였으나 **결함 4건**이 나왔다. 🔴 **게이트 전수 PASS 는 「지침 준수」의 증거가 아니다** — 게이트는 자기가 보는 축만 본다. ① **대시보드 stale 재발**(`HEADER_BYTES 400` 잔여 표기 · `P107` 3차 · 이력에만 적고 원장 미갱신) ② **조문 번호 순서 역전 2건**(`R1-7-8`↔`R1-7-7` · `R1-6-18`↔`R1-6-17` · 규약 전체가 `1,2→10~18→3~9`) ⇒ 지침 이동 + 규약 **18조문 전건 번호순 정렬**(줄 다중집합 일치로 무변경 실증) ③ **`doc_heading_gate` 골든 미등재 = 게이트 침묵**(`01_문서분할_규약.md` 가 분모엔 있고 골든이 없어 제목 유실 **실질 미검사** · `R1-6-18` ④ 가 골든 발행을 빼놓음) ⇒ 사유 기재 발행 · 현재 815 = 골든 815 ④ **`HEADER_BYTES` 교정이 `split_doc.py` 리포트 축에 미전파**(`R1-6-17` 3차 · dry-run 이 머리말을 빼고 재어 파일 초과를 🟢 로 표시 · `--verify` 게이트6 과 **판정 기준이 어긋나 있었다**) ⇒ 머리말 포함으로 통일. 🔴 **⑤ 시정 중 백틱 사고 1건** — 본문을 `bash -c` 인라인 python 으로 넣어 **백틱 구간 23줄이 명령치환으로 삭제**된 채 기록(되돌림 · 손실 0) ⇒ `R1-7-9` 신설(셸 경유 금지 · 파일로 쓴다). 🟢 **후속 = 「게이트가 보지 않는 축」을 게이트로 만들었다**(사용자 지시) — ① `scripts/clause_order_gate.py` **신설**(축1 역전·축2 중복 blocking · 축3 결번 경고 · 접미 조문 `R1-3-7-a` 처리 · **self-check 5축**으로 `R1-7-8`↔`R1-7-7` 실사례 검출 실증 · 현재 조문 75개/계열 12종 역전 0·중복 0) ② `doc_heading_gate` **골든 미등재를 ⚪ → 🔴 FAIL 로 격상**(분모 편입과 기준선 발행이 별개라 침묵했던 축 · 격상 실증 = 골든에서 1건 제거 시 FAIL 검출) ③ 지침 **R3 게이트 8종 → 9종**(9 = 게이트가 보지 않는 축 ㉠원장 stale ㉡다르게 재는 지점 ㉢기준선 발행 ㉣조문 순서) ④ `R1-7-9` **신설**(셸 경유 금지) ⑤ **`init_ihcho` 스킬 stale 6건 교정**(지침 267→298줄 · 규약 157→191줄 · `R1-6-17`→`R1-6-18` · 이력 조각 25→26 · 허브 95줄→102줄 · 원장 조각 최대 29,163→29,677B) + **하드코딩 조각 번호를 실측 명령으로 대체**(stale 재발원 제거). 🟢 검증 = 게이트 7종 + 허브 재발행 PASS · 제목 815 유실 0 · 행 519 유실 0·중복 0

### 원장 용량 아키텍처 — 갱신형 불변식 확립(내부)

✅ **[2026-08-18 O83-B~O83-G 종결]** 불변식 = **「모든 조각이 상한 이내」** 확립 · 실측 = 16문서 전건 **상한 초과 0 · 여유 부족 0**(최소 여유 11.8KB↑). 신설 연산 = `--rebalance`(부분충전 `fill 0.7`) · `--rollover`(꼬리 O(1)) · `retire_rows.py`(닫힌 행 셀 비우기 · 행 키 보존) · `doc_type_gate.py`(유형 선언 강제 16문서). 🔴 **A 단독은 무효였다** — 탐욕 적재라 재균형 전후 여유 178B 그대로 ⇒ 부분충전 도입 후 **178B → 12,355B**, `atomize` 전파 후 `10_진단` **7,146B → 14,039B**. 🟢 **임계값 근거 도출**(O83-F) = 작업단위 61건/6일 실측 ⇒ 단위당 1,200~1,400B · `fill 0.7` = 8~10단위 흡수. 🟢 **[O83-H] `HEADER_BYTES` 근거 도출** — 조각 머리말 **89개 전수 측정**(min 332 · median 349 · **max 381B**) ⇒ **400 → 448** 교정(마진 19B → 67B · 안전측 · 정본 `scripts/split_doc.py` 상단 주석). ⏸ 잔여 = 분모 6일치 한계 · 🔴 **O83-E C3(줄 수 축 완화) 다음 세션 재검토**

### 인덱스·이력 분할 — R1-6-2 ㉣ 정지 구간 합의(내부)

✅ **[2026-08-18 O83 종결]** 사용자 합의로 잔여 2건 분할 = 인덱스 **형제 조각**(현 5) · 이력 **폴더 `01_세션이력_조각/` 25조각 + 허브 포인터**. 게이트 6종 PASS. 🔴🔴 **파괴사고 1건** = `rm -rf` 접두 충돌로 허브가 스테이지에서 삭제됐다(스냅샷 SHA256 대조 복원 · **손실 0**) ⇒ 폴더명 접미 `_조각` 필수 · `R1-6-10`·`R1-7-7` 조문화. 도구 결함 **5건** 시정(인용 경계·머리말 상한·재발행 도구·폴더 탐색·행 키 조각 경계). 🔴 **자기위반** = 미반환 2건을 「전량 확보」로 오보고 후 재호출 2회로 복구

### read 한도 실측 + 잔여 정본 조각 분할(내부)

✅ **[2026-08-18 O83 계열에서 종결]** ① 상한 근거 = 성공 **300줄·22,499자·37.6KB** ↔ 실패 **565줄·약40,000자·71.6KB** ⇒ 하드 한도 **(37.6KB, 71.6KB]**. 🔴🔴 **㉠·㉢ 원인 분리** — ㉢은 **크기**(상한으로 막힘) · ㉠(`edited out`)은 **남은 컨텍스트**(크기 무관 · 상한으로 못 막음) ⇒ `R1-3-7` 호출별 도착 확인만이 잡는다. `READ_OUTPUT` 100000→**40000** 교정. 🟢 ② 잔여 6파일 분할 **완료** + 인덱스·이력까지 **완료**(O83 · 사용자 합의) ⇒ `20_issue` **상한 초과 0**. 🟢 ③ 이력 등재 **완료**(§O82-C·§O83~§O83-G). ⚠️ **[O83-E 후속 판정]** 줄 수 300 축은 **조각에만** 차단 적용으로 분리됐다(비분할 정본은 바이트·문자 축만 차단 · 근거 = 도구 한도 2,000줄 + 지침 377줄 1회 반환 실측) 🔴 이 판정은 **자기 이해가 걸린 규칙 완화**이므로 다음 세션 **재검토 대상**이다

🟡 **[2026-08-20 O92 · 부분 해소]** `O8`(회원 다중후원 귀속 규칙) 의 **후원사업 축**이 `DEC-41` 로 닫혔다 —
「단일 확정 월만 `FMM.SPONSORSHIP_SK` 채움 · 다중은 `SK=0` + `IS_MULTI_SPONSORSHIP=TRUE`」(대표를 고르지 않는 결정).
근거 = 「최대 납입액」 대표가 **다중 키의 39.79% 에서 동점**이라 결정 불가 ⇒ 2차 tiebreak 는 임의 규칙(`P21`).
🔴 **「해소완료」가 아니다 — 이 로그에 완결로 인용하지 말 것**: ① `dbt build` **미실행**(`R4-1`)이라
**라이브 `SPONSORSHIP_SK` 는 여전히 전건 0** 이고 ② `O8` 본체인 **캠페인 귀속(`CAMPAIGN_SK`)·`PAYMENT_SK` 는 미결**이다.
정본 = `30 §28`(DEC-41) · 경위·자기검토 = `02 §O92`·`§O92-B` · 착수점 = `99_NEXT §0-FFF`.

_Co-authored with CoCo_
