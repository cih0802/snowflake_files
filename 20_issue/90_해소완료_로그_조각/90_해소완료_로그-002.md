<!-- SPLIT-CHUNK 90_해소완료_로그.md | 002/003 | 허브 = 90_해소완료_로그.md | 원문 125~192행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## 3. SILVER Q-이슈 — 해소
| Q | 이슈 | 해소 |
|---|---|---|
| Q4 | 회원상태 코드그룹 | MM010 확정(1=활동·2~11=미납·12=중단)·복합조인 |
| Q5 | 발송키 이원화 | 적재완료(2독립 시스템·`SEND_CHANNEL` PK 포함) |
| Q6 | 정기/일시 UNION | 적재완료(`MEMBER_DK`=VARCHAR(10)·수신동의 정규화) |
| Q7 | 조직 역할 3종(=O10) | 원천확정(`ACT/ACMSLT/REGIST_DEPT_CD`) |
| Q11 | `EHGT` 의미(=C-10) | 현업확정(그시점 환율·불필요)→미연동 |
| Q12 | 컬럼정의서 CSV↔DDL 대조 | 적재 매핑 전부 성공(◐미사용컬럼 전수대조=입고팀·저영향) |
| Q13 | 약정 3중 grain 병합 | 폭발 없음(PK 스파인+N:1 LEFT) |
| Q14 | 납입+청구 SUM 중복 | 납입=dedup·청구=행 기준 분리 |
| Q15 | 후원사업 키 NO vs ID | ID=DIM 키(마스터 50)·NO=관계번호·크로스워크 |
| **Q16** | 캠페인↔마케팅캠페인 조인키 | 🔴 **[2026-08-06 O44] 오진 철회 — 재개함.** ~~무효화(`MKTG_CMPGN_NM` 전건 NULL)~~ 실측은 정반대다: `TM_CM_CMPGN_MNG.MKTG_CMPGN_NM` **33,915/36,143(93.8%) 채움**·323 distinct 이고, 브리지 조인(`MK_CMPGN_CD = MKTG_CMPGN_NM::varchar`)은 **100% 해소**된다. `GOLD.DIM_CAMPAIGN.MARKETING_CAMPAIGN`(322종)으로 **이미 배선까지 끝나 있었고** 개발실적 커버리지 **99.42%** 다. 광고측도 `SILVER.AGENCY_AD_PERFORMANCE.CAMPAIGN_NM` 이 이 축에 **89.7% 도달**한다 → **마케팅캠페인 grain 개발단가 73,842원 실산출 확인**. 🔴 실제 차단 요인은 키 부재가 아니라 **개발캠페인 grain 팬아웃 200배**(배분 규칙 부재) → **Q10 재정의**. 정본 = 00 §O44 |
| **질문8** | `IRSD.AREA_CD` 코드그룹 = CM011(정식명) vs CM018(약칭) | ✅ **[2026-08-03 O26 부수해소] CM018 확정.** 근거 = 정본 공#131 지역 정의가 *"서울/인천/경기/강원"* 즉 **약칭**이다. 정본 컬럼정의서는 CM011 을 지정하고 있어 **지표 정의와 어긋난다** — 컬럼정의서 코드 관련 위상을 참고본으로 강등한 근거 사례 3건 중 하나(DEC-26 §16-B). **현업 회신 불요**(대조로 확정). 상세 = 문서10 §13-C · 문서20 §H-4 |
| **G5-동명이의** | `SEX`·`PRCS_STAT_CD` 동명이의 2건 처리 방침(O25 잔여 (a) "결정 대기") | ✅ **[2026-08-03 O26] 해법이 서로 다르게 확정됨.** `PRCS_STAT_CD` = **개명**(`MBRFEE_PRCS_STAT_CD` 회비 / `PSTMTR_PRCS_STAT_CD` 발송 — 양쪽 다 원천 정당) · `SEX` = **값 복원**(SILVER 의 CM013 8종→M/F/U 축약이 원천 파괴였으므로 개명이 아니라 raw 복원). 선택 기준 = **"양쪽이 다 원천 충실한가"**(신규 **P32**). 결정 정본 = 문서30 **DEC-25 §15-C** |
| **O25 조치** | BRONZE 코드체계 SILVER 보존율 37.6% → 38컬럼 신설 | ✅ **빌드 검증 완료**(`TOTAL=293` 시점 · 이후 O26 가드 반영으로 336). 신설 컬럼 distinct 집합 BRONZE 완전 일치 · 행수 9/9 불변 · `FME.REASON_SK` 고아 0. ⚠️ **잔여는 종결 아님** — G3 미보존 21건·감사정본 재생성은 문서10 §13-I 로 이관 |
| **O26 조치** | 성별축 정본 위반(공#130 5종 vs 하드코딩 3종) + 명명 규칙 전환 | ✅ **빌드 검증 완료 2026-08-03**(`PASS=311 WARN=25 ERROR=0 TOTAL=336`) — `DIM_MEMBER` 7,925,716행 불변 · `SEX` CM013 8종 복구 · `SEX_NM` 8종 · `GENDER_NAME` 정본 5종(+NULL 421행 `'미상'`) · WIDE 12 재생성 · O26 가드 전건 pass · WARN 25 는 전부 기존 고아키. 상세 = 문서10 §13-F. ⚠️ 잔여 = CM017 현업 확인(문서20 §H)·`'미상'` 421행 표기 통일 판단 |

## 4. 닫힌 항목 (재론 불요)
| 대표 ID | 별칭 | 확정 |
|---|---|---|
| C-3 | DEC-3 | 미납정의 = `PAY_STAT_CD IN('F',NULL)` |
| C-7 | DEC-5 | 팀 = `UPPER_DEPT_ID` 5th 레벨 |
| C-10 | DEC-6·Q11 | EHGT 미연동(그시점 환율) |
| G-1 | Q1 | user_id = 회원번호(정규 7자리/`S`+8자리) |
| O7 | — | 215지표 구조 커버리지 완전 |
| O12 | AC-1 | `MEMBER_DK` = VARCHAR(10) |
| O13 | S13 | DIM_SPONSORSHIP 카디널리티(50) |

## 5. 제외 (❌ 채택 안 함)
| ID | 이슈 | 사유 |
|---|---|---|
| A-5 | 앱푸시 발송·성공(어드민) | 어드민 미채택(2026-07-09)→`FSE.APP_PUSH_*` 삭제 |
| A-6 | 이벤트 조회수(어드민) | 미채택→`FEP.VIEW_CNT` 삭제 |
| A-10 | 행사기간·참여경로·채널 | CRM-backed 확인(삭제없음)·어드민분만 제외 |

---

## 6. 은퇴 이관 — 원장 §1 대시보드 닫힌 항목 (2026-08-18 O83-B)

> 🔴 **원장 §1 대시보드에서 무변경 이관**(`R2-8` · `O83-B`). 행 키는 원장에 **그대로 남아 있고**
> 이 문서에는 그 행의 **장문 셀 원문**이 있다. 원장 쪽에는 이 절을 가리키는 포인터만 남았다.
> ⚠️ **행을 삭제한 것이 아니다** — `R1-7-4`(행 키 = 인용 좌표) 준수. 열 수도 그대로다.

### O82-C 진행 체크포인트(내부)

**[2026-08-18 O82-C]** 사용자 승인 = 「1,2,3 다 진행 · 예산 다 써」. 🟢 **① 행번호 인용 환산 완료** — 인덱스는 **분할 금지**라 허브 자동 대응표가 없어 **§0 좌표 환산표를 손으로 신설**(인용된 7항목을 현재 인덱스에서 **의미로 재탐색** = `OPS-5` 93행 · 이력항목형식 102행 · `CONF-1`/`CONF-2`/`DEC-19`/`DEC-20`/납입(명) 101~104행). 문서50·`99_NEXT` 는 허브 대응표 **자동 생성 확인**. 🟢 **② 미반환 건수 정정** — 「8건」→**「최소 14건」** + 🔴 **`R1-3-7-a` 의 구조적 한계 등재**(미반환 호출은 컨텍스트에서 사라져 **사후 역산 불가** ⇒ 세션 중 카운터 유지가 유일한 방법). 🟢 **`99_NEXT` 조각 편집 → 허브 SHA256 재발행** — 그 과정에서 **도구 공백 발견·수정**: `R1-6-9` 가 「조각 편집 후 허브 재발행」을 규정하는데 `split_doc.py --verify` 가 **그 워크플로를 지원하지 않았다**(라벨 파싱 실패 · 게이트2·3 이 정상 갱신을 FAIL 처리) ⇒ `발행 SHA256` 라벨 수용 + 재발행 상태 분기 추가 · 회귀 무해 확인. 🟢 **③ 이력 등재 완료**(§O82-B 16위반 + §O82-C). ⬜ **④ 전량 독해 진행중** — 🟢 지침 **313줄**(2회) · 🟢 인덱스 **366줄**(4회) · 🟢 문서50 조각 **003/009** · ⬜ 문서50 004~009 · ⬜ `99_NEXT` 조각 4개 · **미반환 0** 유지. 🔴 **독해가 결함 3건을 잡았다**(=문서50 을 읽어야 하는 이유의 재실증) — 인덱스 자기참조 줄수 stale 2건 + **§세션이력 안내가 실측과 반대**(「최신은 앞쪽 O59 군집」 ↔ 실측 **최신은 파일 끝에 append**)

### O82-C 전량 독해 완료 — B3·B4 해소(내부)

**[2026-08-18 O82-C]** 사용자 지시 「예산 다 써」. 🟢 **전량 확보 4종** = 지침 **313줄**(2회) · 인덱스 **366줄**(4회 · 30,000자 이하 분할) · **문서50 1,796행**(조각 9개 = 9회) · **`99_NEXT` 830행**(조각 4개 = 5회) ⇒ **총 20회 · 미반환 0 · 재호출 0** ⇒ `R1-3-7-a` **판정식 충족**(재호출 0 ≥ 미반환 0). 🟢 **B4 해소 — 문서50 미독이 끊겼다**(O71-C 이후 처음 · 종전 「7세션 연속」 이후 미집계분 포함). 🟢 **B3 해소 — 지침 통합본 전량 재독**. 🔴🔴 **독해가 결함 4건을 잡았다** = ㉠ 인덱스 자기참조 줄수 stale 2건 ⇒ **줄수 기재 자체를 제거**(O72 결론 「자기참조 계측은 수렴하지 않는다」 적용) ㉡ **§세션이력 안내가 실측과 반대** — 「최신은 앞쪽 O59 군집」이라 안내하는데 실측은 **O75·O76 계열·O82 계열이 전부 파일 끝**이다 ⇒ 정정(그 안내를 따르면 최신을 놓친다) ㉢ `99_NEXT` §6 「지침 186줄」·§필독 「문서50 1,759줄」이 stale(각각 313·1,796) — 🔴 **조각 편집은 허브 SHA256 재발행을 요구하므로 미수정으로 남긴다**(그 문서 자신이 `P231` 로 「줄수 기재는 stale 이 된다」고 경고한 항목이라 관계식 판정이 정답이다). 🟢 **독해로 확인된 것** = `R1-6-1` 상한 실측이 `P231`(마운트 낡은 스냅샷)·`C5`(부분 반환)와 **다른 축**임이 문서50 §O71-C 로 교차 확인됐다

### 원장 쓰기 안전 규약 + 행 파괴 게이트(내부)

**[2026-08-18 O82-B]** 트리거 = 사용자 지시 「이번 세션 작업이 지침을 지켰는지 비판적으로 검토」 → 확정위반 **16건** 적발 → 「작업할 수 있는 건 전부」 승인. 🔴 **핵심 = 원장이 4회 깨졌고 전부 같은 뿌리이며 기존 게이트를 전부 통과했다**(`A13` 꼬리 10줄 · `A15` O76행 덮어쓰기+중복 · O82 편집 2건 소실 · 줄길이 분모 14파일 소실). 🟢 **`scripts/index_row_gate.py` 신설** — 표 **첫 셀 = 행 키** 집합을 골든(**448행**/4표면)과 대조해 **유실·중복은 FAIL · append 는 PASS**. 음성테스트 4종 통과(무변경 PASS · 소실 FAIL · 중복 FAIL · append PASS) ⇒ `C6`·착수표 ㉑ 처방 이행. 🟢 **지침 `R1-7` 신설**(6조 = 전체재작성금지·해시안정성·손상판정전재읽기·행키게이트·토큰보존단정·discard 파괴성) + **R3 게이트 6→8종**. 🟢 **`scripts/line_len.py` 신설** — `R1-5-4` 의 awk 가 **바이트**를 세어 한글에서 오탐(1,907자→3,091)하므로 문자 기준으로 교체. 🟢 **줄길이 분모 195→213파일 복원**(discard 소실분 · 증거 기반 재도출). 🔴 **오염 수치 3건 교정** = 「8세션 연속」(창작 · 정본 최신 **7세션**) · 「인덱스 321줄/100.8KB」(대화로그 인용 · 내 실측 325줄) · 「211건」(**219** 오기). 🔴 **`R1-6-1` 실증 철회** — 「292줄 read 1회 반환」의 그 read 가 `edited out` 이었다 ⇒ 상한은 **잠정값**. 🟢 **`R1-6-3` 사용자 승인 획득**(종전 자기허가 = `R1-2` 위반). ⚠️ **redo 불가 실측** = `USER$` 는 `head`==`live`(md5 동일) · 파일별 버전이력은 Git 워크스페이스 전용

### 정본 장문문서 조각 분할(내부)

**[2026-08-14 O82]** 🔒 라벨 선점 등재(게이트 「다음 **O82**」 = 정의 76 · 참조 81 · `R1-4-3`). 트리거 = 사용자 지시 「이슈문서가 너무 길어 LLM 이 읽기 어렵다 · 작게 나눠라」 + **직전 세션(O81 계열) 설계가 사용자 질의 2건 미응답으로 정지**했던 것을 이어받음. 사용자 결정 4 = ㉮ 1회차 **문서10 단독** ㉯ 허브 **원 파일명 유지**(O59-K 정합) ㉰ 이력 경계 = **세션 항목**(`> #### [날짜 O##]`) ㉱ 행번호 인용 = **허브 대응표만**(인용문 무수정 · R2-8 유지). 🟢 **신설** `scripts/split_doc.py`(465줄 · `--dry-run`/실행/`--verify` · 게이트 6종). 🔴 **무변경의 정의** = 조각 본문 concat 이 원문과 **SHA256 완전 일치**(조각 머리말은 `BODY-BEGIN` 센티넬 위에 두고 게이트는 아래만 대조) ⇒ 본문 문자 추가·삭제·수정 **0건** = O59-J 형 **「이동」이고 「편집」이 아니다**. 🔴 **문서10 은 여전히 미독이다**(연속 회차 **미측정** — 정본 최신 기재는 `99_NEXT` ⑥ 의 **7세션 연속**(O72 시점)이고 그 이후는 집계하지 않았다. 🔴 **[O82-B 정정]** 종전 기재 「8세션 연속」은 **실측 없는 창작**이었다) — 이 분할은 기계 대조로만 주장하며 문서10 소관 수치·판정을 **인용하지 않았다**(`R1-3-7`). 실측 = **3,318줄/364,101B → 조각 14개**(최대 292줄·37.7KB · 상한 300줄 AND 40KB 이내 전건)

### ML 데이터 이관 문서화

**[2026-08-14 O75] 데이터 마이그레이션 문서군에 `GN_DW.ML` 예측결과 16종 편입** — 신설 `50_handoff/06_데이터마이그 GN_DW_ML_DDL_20260814.sql`(355줄 · `20_ML_ddl.sql` 무변경 발췌) + 수정 5종(01 본문 §3.1-B·§5.1-B·§5.6·**§11 신설** · 02 **2-B 부여** · 03 3.1-B·언로드 커서 · 05 **A.5-B 적재** · 02_1 **A7·A8**). 🔴 **범위 = 사용자 확정 16종만**(학습 20·스냅샷 12·로그 1·뷰 4·프로시저 14·모델 14 제외). 🔴 **설계 판단 3건** = ① 스키마 통짜 GRANT 금지(테이블 단위 16건 — `ALL TABLES` 는 학습 33종을 조용히 열고 적재 후에야 발견된다) ② `PREDICTION VARIANT` 4종을 일괄 프로시저에서 제외(일반 COPY 시 JSON→문자열 ⇒ **SERVING 뷰에서 NULL** · GA4 함정 재발) ③ CSV 경로 유지(VARIANT 가 테이블당 1개·전부 마지막 컬럼). 🟢 실측 = `PREDICTION` 위치 python 파싱 확정($18/$18/$21/$22) · ML 객체 49 BASE+4 VIEW+14 SP+14 모델 · **모델은 프로시저 본문에서 생성** ⇒ 🔴 **C 는 예측 「소비 환경」이고 「실행 환경」이 아니다**. 🔴 **신규 발견(판정 보류)** = O74 §7 의 배포 기재(SERVING 뷰 7·SV 7·Agent 도구 7)가 **현재 계정 `DV07626` 에 없다** (`SHOW VIEWS` 0건 · `SHOW SEMANTIC VIEWS` 9종에 `SV_ML_*` 없음 · `ML` 스키마 부재) ⇒ 원인 미판정 · **적재 후 재배포 필요**(01 §10 10-B). 🔴 C 계정 식별자 stale 정정(`zx03676`→**`DV07626`**). ⬜ **미실측 3건**(A 행수 · B 폴더/파일 · C 합계) = 01 §11-5 표 · 참고값 1,045,732행은 **A 실측 아님**

### ML 예측 SV·Agent 배포

**[2026-08-14 O74] SERVING 뷰 7 + SV 7 + Agent 도구 7(MEMBER +3 · OVERALL +4) 배포·검증 완료** — 소비 역할 `GN_DW_ANALYST` 세션으로 SV 7종 조회 성공(`P126`) · `AGENT_MEMBER` **VERSION$6** 도구 10 · `AGENT_OVERALL` 도구 8 · Agent GRANT 전건 보존. 🟢 **`GN_DW.ML` 그랜트 0건으로 충분**(소비 역할은 base 권한 없이 SV 만으로 조회 · 뷰는 소유자 권한) ⇒ **학습·중간 37종이 자동 차폐**. 🔴 **배포 중 실측이 설계를 뒤집은 것 5건** = ① SV 5분할→**7분할**(단위 만원/원 + LTV 평균/총액 혼재) ② **LTV 2종 계열축 교집합 0**(`CMPGN_LTV` 는 상위캠페인 0/50 — 요건 기술과 불일치) ③ **개발금액 유형별 12개월 합계 불일치**(TOTAL 365,486 ↔ DEPT 317,285 87% ↔ NEW_OLD 378,637 104% ↔ **SPNSR_BSNS 456,037 125%** · 만원) ⇒ 초안 문안 「전사=부서합」 정정 · **후원사업 125% 는 신규 확인항목** ④ `SETLE_CD` 코드그룹 추정 회피(정본 **PM040** 재사용 · `P36`) ⑤ dedup 이 지표를 움직인다(확률평균 0.3829→0.4418 · ≥0.5 건수 29,690→29,121). 🆕 **SV metric 이름을 base 컬럼명과 같게 두면 metric 중첩으로 컴파일 거부**(3곳 개명). 🔴 **미발행 = 예측 지평·업무 위험 임계값**(원천 부정합·현업 미확정). 🔴 **잔여 = NL 스모크 미실행**(라우팅 미검증) · 성현 프로·현업 확인 4건
