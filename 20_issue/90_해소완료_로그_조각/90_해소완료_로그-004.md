<!-- SPLIT-CHUNK 90_해소완료_로그.md | 004/004 | 허브 = 90_해소완료_로그.md | 원문 1~177행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->

## 🟢 [O170 은퇴] 99_NEXT 승계 인수인계 절 — 99_NEXT_SESSION-001.md

> 🔴 **절 본문 무변경 이관**(`R2-8` · `O170`). 원본에는 **제목 줄이 그대로 남아 있고**
> 이 문서에는 그 절의 **본문 원문**이 있다. 원본에는 이 절을 가리키는 포인터 1줄만 남았다.
> ⚠️ **절을 삭제한 것이 아니다** — `§제목` 인용 좌표와 `doc_heading_gate` 제목 집합을
> 보존하기 위해 **제목(하위 제목 포함)은 한 줄도 옮기지 않았다**.

### 좌표 대응표 (원본 → 이 문서)

| 원본 파일 | 원본 행범위 | lv | 절 제목 | 본문 B | 본문 SHA256(앞16) |
|---|---|---|---|---|---|
| `99_NEXT_SESSION-001.md` | 46~208 | 2 | 0-AAAA. 🔴🔴 [2026-08-30 O123 필독 — ~~여기서 시작한다.~~ **⇒ [2026-08-30 O124] 시작점은 §0-BBBB 다**(`99_NEXT_SESSION-002.md` 선두 · 🔴 이 조각은 여유 3,573 B 뿐이어서 새 절을 담을 수 없었다) · §0-ZZZ 는 승계됐다] | 15,491 | `9168e7d4d8b9946c` |

#### 이관 본문 — 0-AAAA. 🔴🔴 [2026-08-30 O123 필독 — ~~여기서 시작한다.~~ **⇒ [2026-08-30 O124] 시작점은 §0-BBBB 다**(`99_NEXT_SESSION-002.md` 선두 · 🔴 이 조각은 여유 3,573 B 뿐이어서 새 절을 담을 수 없었다) · §0-ZZZ 는 승계됐다]

> 🟢 **세션 시작 절차 불변** = `python3 scripts/session_brief.py --write` → `20_issue/00_BRIEF.md` 1회 `read`.
> 🔴 **`export SESSION_LABEL=O1NN` 을 착수 직후 먼저** 하고(`R1-7-10`) **라벨을 원장 §1 에 선점 등재**하라
> (`R1-4-3` · O112~**O123** **13세션 연속** 준수). 🔴 정본 = 이력 **§O123**.
>
> ### ▣ AAAA1 🟢 착수 직후 게이트는 이제 한 줄이다 — 🔴 **rc 를 리다이렉트로 받아라**
>
> ```
> python3 scripts/gate_census.py             >/tmp/g1.out 2>&1; echo "rc=$?"
> python3 scripts/gate_census.py --run-tests >/tmp/g2.out 2>&1; echo "rc=$?"
> ```
> O123 실측 = **둘 다 rc=0**(음성 테스트 **22종 전건 rc=0** · `NEEDS_ARGS` 3종 규약 **rc=2**).
> 🔴 **`| tail` 뒤의 `$?` 는 남의 것이다**(`O120`). 🔴 **브리핑에 옮겨 적는 것은 실행이 아니다** — 실제로 돌린 rc 를 보고하라.
> 🟢 **게이트 목록을 손으로 고르지 마라** — `gate_census.py` 가 `ls scripts/*.py` 를 분모로 삼아 미분류를 FAIL 한다
> (종전 `§0-ZZZ ▣ZZZ1` 의 「분모를 다시 세라」는 **이 도구로 기계화되어 닫혔다** — 손으로 세지 마라).
>
> ### ▣ AAAA2 🟢 O123 이 확정한 것 — **다시 논의하지 마라**
>
> · **`DEC-46` 발급 · 등재 완료** = `30_설계_의사결정_조각/30_설계_의사결정-012.md` **§32**.
>   🔴 **신규 규약이 아니라 `DEC-25`(§15)의 전 계층 확장**이다. §15 에 양방향 포인터 병기 완료.
> · **3분류 = P(로직 0 → 원천명) / L(join·union all·계산·키승격·**타입 캐스트** → 의미명) / X(동명이의 → 변별 접두).**
> · **판정 원칙 4건**(정본 = `20_issue/32_컬럼개명표.md` **§0-A**):
>   ㉠ 타입 캐스트(`TRY_TO_NUMBER`·`CAST`)는 **로직 ⇒ L** ㉡ `NULLIF`/`TRIM` 만이면 **로직 0 ⇒ P**
>   ㉢ **형제 모델 어휘 불일치는 수용**(규약 우선) ㉣ `UNION ALL` 위치 별칭은 **L**.
> · **`ERP_BUDGET` 의 `YB`·`CB`·`AB`·`EX` = 분류 L · 개명 0건**(`DEC-46` 32-C).
> · **`BUDGET_YEAR` 3건 = 분류 L · 개명 0건**(원천명 `YEAR` 가 SQL 예약어인 문제가 이 판정으로 사라졌다).
> 🔴 **㉢ 의 귀결을 결함으로 오판하지 마라** — 개명 후 `ERP_BUDGET`(월)은 `*_BUDGET_AMT`,
> `ERP_BUDGET_YEARLY`(연)은 `*_BDGT_TOT_AMT` 로 갈린다. **사용자가 승인한 결과다. 되돌리지 마라.**
>
> ### ▣ AAAA3 🟢 Phase 1 은 **판정대상 25/25 계보 완료**다 (착수표 ~~㉘~~ 종결)
>
> 🟢 **분모를 라이브로 확정하고 정정했다** = **160컬럼 / 25 계보**(AGENCY 75 · CRM 68 · ERP 17).
> 정본 = `20_issue/32_컬럼개명표.md` · 판정 SQL = `scripts/o122_name_drift.sql` **§A·§B**.
> 🔴 **O122 기재 169 는 9 만큼 과대였다** — `CRM_BIZ_TARGET` 은 `source()` 미참조 빈 스캐폴드(`WHERE 1=0` · E-6)라
> **비교할 원천명이 없다** ⇒ 그 SQL 에 **`UNSOURCED` 축을 신설**해 제외했다(SQL 자체의 결함이었다).
>
> | 범위 | 컬럼 | 계보 | L | P | X | 보류 | 개명 확정 |
> |---|---:|---:|---:|---:|---:|---:|---:|
> | ERP | 17 | 3 | 12 | **5** | 0 | 0 | **5** |
> | AGENCY | 75 | 8 | 61 | **7** | 7 | 0 | **7** |
> | CRM | 68 | 14 | 63 | 0 | 3 | **2** | **0** |
> | **합계** | **160** | **25** | **136** | **12** | **10** | **2** | **12** |
>
> 🔴 **개명 실행은 0건이다**(선행조건 = AAAA5). 🔴 **개명 확정 12건은 전건 SILVER** 다.
> 🟠 **잔여 3축** = 판정 경계 2건(착수표 **㉛**) · GA4 88컬럼(결정 ④) · `UNSOURCED` 10컬럼(원천 입고 후).
>
> ### ▣ AAAA4 🔴🔴 함정 — `kind_hint` 를 판정으로 쓰면 **10건을 잘못 개명한다**(실측)
>
> `o122_name_drift.sql` §B 의 `kind_hint` 는 **분류 X 10건 전부를 `P?(원천명 복원 후보)` 로 표시**한다:
> · **`_SRC` 접미 7종**(`AGENCY_AD_DIGITAL` 의 `CTR_SRC`·`CVR_SRC`·`CPC_SRC`·`CPM_SRC`·`CPA_SRC`·
>   `DEV_UNIT_PRICE_SRC`·`VTR_SRC`) — `DEC-9` 가 전량 보존을 규정한 **「대행사 산정값 ↔ DW 재계산값」 변별 접미**다.
>   🔴 복원하면 DW 재계산 measure 와 **실제 동명이의 충돌**이 생긴다.
> · **`MBRFEE_`/`PSTMTR_` 접두 2종**(`CRM_PAYMENT_BILLING`·`CRM_SEND_REQUEST` 의 `*_PRCS_STAT_CD`) —
>   `DEC-25` **15-A·15-C** 가 해소한 동명이의 그 자체다(도메인 `R,F,S` ↔ `0,1`). 🔴 복원하면 **오조인**이 살아난다.
> · **`SRC_` 접두 1종**(`CRM_MEMBER_DEV.SRC_LOAD_DT`) — `DW_LOAD_TS`(빌드시각)와 가르는 증분 워터마크다.
> ⇒ 🔴 **힌트를 판정으로 승격시키지 마라.** 그 SQL 자신도 94행에서 *"`kind` 는 힌트다"* 라고 적는다.
> 🟢 **일반 규칙 = 접두·접미가 「무엇과 구별하기 위한 것」이면 분류 X 다.** 판정법 = 그 토큰을 떼면
> **같은 층의 다른 컬럼과 같은 이름이 되는가.** 🔴 원천만 봐서는 불가능하다 — **형제 컬럼 + 모델 헤더 주석**을 함께 읽어라.
>
> ### ▣ AAAA5 🔴🔴 개명 실행 전 선행조건 — **SILVER 안전망이 0/43 이다** (착수표 **㉙**)
>
> 🔴 **㉠ `table_ddl_column_gate` 는 개명 대상 12건을 하나도 보지 않는다.** O123 실측 =
> 게이트 `TARGETS` **7개가 전부 GOLD** 이고(`MB = models/gold/`) 분모 **80 = GOLD 37 + SILVER 43**(라이브 BASE TABLE).
> 개명 확정 12건은 **전건 SILVER** ⇒ 🔴 **커버리지 0/43.** 「7/80 이라 부족」이 아니라 **안전망이 0** 이다.
> ⇒ 🟢 **필요한 것은 분모 확대가 아니라 SILVER 축 신설**이다.
> 🔴 **㉡ 소비처 grep** = `DEC-25` 15-D 요구 · `P17` 재발 주의. 🟢 도구 신설 =
> `python3 scripts/o123_member_dk_footprint.py <컬럼명>` — **축을 분리해 센다**.
> `MEMBER_DK` 실측 = 209(archive 포함) / **156**(현행) / **59**(코드·설정 = 깨지는 것) / 97(문서 = stale 되는 것).
> 🔴 **O122 기재 「178파일」은 어느 축에서도 재현되지 않았다 — 인용하지 마라.**
> 🔴 **㉢ COMMENT DoD 3종** = 물리 `ALTER … COMMENT` + 뷰는 `post_hook` + **거짓 경고문 회수**.
> 🔴 **`CREATE OR REPLACE TABLE` 금지 — `ALTER TABLE … RENAME COLUMN` 만**(FK·GRANT 파괴).
> 🔴 **완료 판정 = `INFORMATION_SCHEMA.COLUMNS` 구 컬럼명 0건**(문서 아님).
> 🔴 **착수표 ⑫·⑭ 컬럼은 현업 결정 전 배선 금지.** 🔴 **GA4 는 결정 ④ 로 범위 제외**(O121 잔여와 함께 재개).
>
> ### ▣ AAAA6 🟠 O123 잔여 3건 (🟢 정본 시정은 끝났고 후속 실행이 남았다)
>
> · **계정이 또 바뀌었다** = O120 기재 `YQ47212` ↔ 실측 **`KA98941`**. 🟢 `GN_DW` 는 **실재**한다
>   (13스키마 · SILVER 948 · BRONZE 1,153 · GOLD 1,320 컬럼) ⇒ 🔴 **이 계정은 「문서작업 전용」이 아니다.**
>   🟢 **원장 §0 의 `92_실측필요_후속작업` 근거문은 O123 이 취소선 + 실측 병기로 고쳤다**(적발만 하지 않았다 · `P154` 축).
>   🟠 **잔여 = 그 문서의 항목별 재판정**(착수표 **㉚**) — 지금 측정 가능한 항목을 하나씩 골라 실측한다.
> · **`99_NEXT` 허브 여유 7,308 B** = 🔴 **조각이 아니라 허브다**(자동 목차 · 조각이 늘면 커진다)
>   ⇒ **`--rebalance` 로는 줄지 않는다.** 상한에 닿으면 재균형이 아니라 **목차 형식**을 봐야 한다.
> · **개명 확정 12건의 소비처 grep 미실시** — 개명 실행의 필수 선행이다(AAAA5 ㉡ · 도구는 신설했다).
>
> ### ▣ AAAA7 🟢 O123 이 실증한 판정식 2건 (승계 · 성격 불변)
>
> · 🔴 **분류는 줄 단위가 아니라 계보 단위로 하라** — 실증: 겉모습이 같은 `BDGT`→`BUDGET` 약어확장인데
>   `ERP_BUDGET`(월 · `UNION ALL` 경유)은 **L**, `ERP_BUDGET_YEARLY`(연 · 로직 0)은 **P** 로 갈렸다.
>   **줄 단위로 봤다면 한쪽이 반드시 틀렸다.**
> · 🟢 **규약 판정의 근거를 코드베이스 자신에서 찾아라** — `ERP_BUDGET_ITEM.sql` 10-16행이 7개 컬럼에
>   `NULLIF(TRIM(...))` 를 걸면서 **원천명을 유지**한다 ⇒ 그것이 「`NULLIF`/`TRIM` 은 로직이 아니다」의 증거였다.
> · 🔴 **분모 편입과 골든 발행은 별개다**(`R3-9 ㉢`) — O123 이 실제로 걸렸다(`doc_heading_gate` rc=1 → 유실 0
>   확인 후 `--reason` 달아 발행 → rc=0). **신규 문서를 만들면 골든도 발행하라.**
>
> ### ▣ AAAA8 🟢 O123-C·O123-D 는 정본화됐다 — **프롬프트로 운반하지 마라**
>
> 🔴 **경위** = O123 세션의 3번째 작업 단위(**O123-C** = `30_output_share` 최신화)가 **정본에 0건 등재**였고
> 인수인계 프롬프트가 **유일한 운반체**였다. **O123-D** 가 파일·산출물에서 전건 재실측해 소급 등재했다.
> 🟢 **정본 = 이력 §O123-C**(작업) · **§O123-D**(등재 + 정정) · 근거철 = **`_o123c_evidence.md`**(루트 · E0~E11).
> ⚠️ 🔴 **[O124-B] O124 가 이 근거철을 「부재」로 오판했다** — 경로는 `20_issue/` 가 아니라 **루트**다.
> ⇒ 🔴 **이 블록을 프롬프트에 다시 펼치지 마라. 좌표만 넘겨라.**
>
> 🟢 **O123-C 가 확정한 것**(다시 논의하지 마라):
> · **재생성 17종 · 미생산 5종 · `rm` 0건** — 승인 방식은 **「복사 후 제자리 덮어쓰기」**다.
>   🔴 **원본을 먼저 지우지 마라** — `gen_silver_gold_retention.py:28` 의 `PREV` 가 **이전 판본 CSV 를
>   델타 입력으로 읽는다** ⇒ 지우면 08 의 「변동」 열이 근거를 잃는다.
> · **`gate_census` 오분류 2건 시정** = `gen_column_mapping`·`run_bronze_audit_host` → **`GEN`**.
>   🔴 **O121-B 의 3건과 같은 유형이다**(`audit_ddl_rule7`·`sv_unit_gate`·`table_ddl_column_gate`)
>   ⇒ 🔴 **분류 근거는 키워드 등장이 아니라 그 키워드의 역할**(입력으로 **읽는가**, 출력으로 **발행하는가**)다.
> · **`gen_arch_map.py` 계정 하드코딩 시정**(403 의 유일 원인) ⇒ `sfconn` 경유.
>   🟢 판정식 = **접속 정보는 도구마다 적지 말고 단일 경유점에서 받는다.**
>   ⚠️ 같은 파일 `:735` 의 `account=account,` 는 **HTML 표시용 인자**다 — 잔존으로 오판하지 마라.
> · **골든 지문 정정** `3581c638b4260bd9` → **`12ffa5c63c8ce470`**(산출물 수치 차이 **0**).
>   🟢 처방 = **`/tmp` 산출물에 의존하는 도구는 생성과 소비를 같은 셸 호출에 묶어라**
>   (등재 위치 = **§7 `-024.md:56`** 의 `/tmp` 항목 보강 · 🔴 **새 지뢰 항목이 아니다**).
>
> 🔴🔴 **O123-D 가 정정한 프롬프트 서술 4건 — 옛 프롬프트를 다시 인용하지 마라:**
> ㉠ 파일명은 **`20260826_아키텍처 지도 요약본.html`**(「 요약본」 포함)이다.
> ㉡ 「환경 지뢰 **신규**」는 **과대 주장** — `/tmp` 소실은 `-024.md:56` 에 **기등재**였다.
> ㉢ 「`-013 §7`」은 **stale 좌표** — §7 은 **`-024.md:4`** 다(`-013` 에는 0건 · 재균형으로 이동).
> ㉣ 「`--label` 지원 여부를 확인하라」 → **이미 지원한다**(`test_generators.py:746` + CLI `--label` +
>   `snapshot_util.resolve_label()`) ⇒ 🔴 **시정 대상은 도구가 아니라 절차다** —
>   골든 2회가 `UNLABELED` 인 원인은 **`export SESSION_LABEL` 미이행**이고 그 지시는 위 49행에 이미 있다.
> ⇒ 🔴 **판정식 = 인수인계 프롬프트는 「무엇을 확인할지」의 목록이고 「확인된 사실」이 아니다.**
> **서술은 낡지 않지만 좌표는 재균형으로 낡는다**(4건 중 2건이 좌표·명칭이었다).
>
> 🆕 🟢 **O123-D 신설 판정식 — 소실·중복 판정의 키에는 식별자만 넣어라.**
> 실물: 08 델타를 키 3열(`SILVER_TABLE`·`COLUMN`·**`STATUS`**)로 재면 **「사라진 키 10 · 신규 226」**
> 이 나오는데 그 10건은 소실이 아니라 **상태 전이**다(`REFERENCED` 238→291 · `DROPPED` 143→141).
> 2열 키로는 **사라진 0 · 신규 216 순수 증가**다(SILVER 테이블 **39→43** 이 원인 ·
> `SILVER_ONLY_CHAIN` **127→292** 의 증가분은 대부분 **GA 축** = `BIGQUERY_REFINED_DATA` +118 ·
> `GA4_BASIC` +46 ⇒ O120 의 「GA 축이 GOLD 에서 끊긴다」와 정합).
> 🔴 이것은 `O111 ㉠`(「0건」은 판정식이 못 본 것)의 **역방향 쌍**이다 —
> **「N건 있다」도 판정식이 만들어낸 것일 수 있다.**
>
> 🟠 **O123-C 가 남긴 열린 것**(착수표는 **㉙㉚㉛ 그대로 열려 있다** — 등재는 해소가 아니다):
> · **미생산 5종 방향 결정** = `01`·`10`·`12`·`13`·`미해결이슈_요약_O102`.
>   🔴 **`01`·`10` 은 이미 stale 이다**(헤더가 `companion:` 으로 선언한 04·05·03 을 O123-C 가 갱신했다).
>   🟢 권고 = `01`·`10` 은 **생성기를 만든다**(입력이 방금 갱신된 산출물이라 손저작은 매번 stale 이 된다) ·
>   `12`·`13`·`미해결이슈_요약` 은 **회신 상태 의존**이라 생성기보다 **재집계 스크립트**가 맞다.
> · **라이브 0건 주장은 사후 재현 불가**다(`R2-8-4-c`) ⇒ O123-C 의 **자기신고**로 읽어라.
>
> 🆕 🔴🔴 **[O123-D 자기검토 산출] 브리핑이 이 절의 항목을 8건째 0건으로 보고 있었다 — 시정했다.**
> `session_brief.py` 가 `line.startswith('### ')` 로 `▣` 를 찾는데 **이 절은 전부 `> ### ▣`**(blockquote)다
> ⇒ **AAAA1~AAAA8 전건이 브리핑에 안 보였다**(실측 `인수인계 항목 0` → 시정 후 **8**).
> 🔴 **`§0-ZZZ`(O119)는 비-quote 였기 때문에 그때는 동작했다** — 즉 **형식 드리프트가 조용히 껐다.**
> 🟢 시정 = `dequote()` 신설(인용 접두 + 선행 공백 정규화) + **「항목 0」이면 브리핑에 🔴 경고를 박는다**
> + 음성 테스트 축 신설(`test_session_brief.py` 축5 · 실제 정본 형태를 픽스처로 넣었다).
> ⇒ 🔴🔴 **판정식 2건**:
>   ㉠ **마크다운 구조를 볼 때는 인용 접두를 먼저 정규화하라** — 서식은 의미가 아니다.
>   ㉡ **테스트 픽스처는 「정본이 실제로 쓰는 형태」를 넣어라** — 내가 만든 깔끔한 형태만 넣으면
>     그 테스트는 형식 드리프트를 **구조적으로 못 잡는다**(축4 는 통과하는데 실물은 0건이었다).
> 🔴 **이 절을 계속 blockquote 로 써도 된다** — 추출기가 이제 본다. 형식을 바꾸지 말고 **도구를 믿어라.**
>
> 🆕 🟠 **[O123-D] 용량 — 원장 조각을 내가 압박했고 재균형으로 회복했다.**
> O123-D 의 원장 2행(1,517 + 1,688자)이 `00_INDEX` 조각 여유를 **6,931 → 1,687 B** 로 깎았다
> ⇒ `--rebalance --fill 0.7` 로 **12,575 B** 회복(조각 11개 · max 28,385 B · 유실 0).
> 🔴 **판정식 = 원장에 장문 행을 2개 넣으면 그 조각은 상한에 닿는다** — 행을 쓰기 전에
> `doc_type_gate.py` 로 그 조각의 여유를 먼저 보라(쓴 뒤에 보면 재균형이 필수가 된다).
> 🟠 **`99_NEXT_SESSION.md` 허브 여유 6,994 B 는 여전히 열려 있다** — 🔴 **`--rebalance` 로는 줄지 않는다**
> (허브는 자동 목차라 조각 수에 비례한다 · `▣ ZZZ7` 참조). 처방은 **승계 절 은퇴**이고 `R4-4-3` 승인 대상이다.
