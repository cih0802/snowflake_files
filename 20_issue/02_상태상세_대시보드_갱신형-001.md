<!-- SPLIT-CHUNK 02_상태상세_대시보드_갱신형.md | 001/008 | 허브 = 02_상태상세_대시보드_갱신형.md | 원문 1~187행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
<!-- LLM-METADATA
doc_id: ISSUE_STATUS_DETAIL
doc_role: 상태 대시보드 상세 (갱신형) — 원장 §1 표의 장문 셀 무변경 이관부
project: GN_DW (굿네이버스)
created: 2026-08-13
created_by: O66
parent: 20_issue/00_INDEX_이슈원장.md §1
END-METADATA -->

# 상태 대시보드 상세 (갱신형)

> 허브 = `00_INDEX_이슈원장.md` **§1 상태 대시보드**. 이 문서는 그 표의 장문 셀을 **무변경 이관**한 것이다.
> 성격 = **갱신형**(항목 상태가 바뀌면 그 자리에서 고친다). append-only 이력은 `01_세션이력.md` 소관이다.

> 🔴 **이관 계약 (O66)**
> · 이관은 **단일 공백을 줄바꿈으로 치환**한 것뿐이다. 문자 추가·삭제·수정 **0건**.
> · 판정 = 공백 정규화(`\s+`→' ') 후 원문과 **문자열 완전 일치**(`scripts/split_issue_index.py --verify`).
> · ⇒ 수치·판정 문장은 손대지 않았다. 마크다운 렌더링 결과도 이관 전과 같다.
> · 인덱스에는 각 행의 **verbatim 접두**만 남겼다. 접두가 잘린 곳은 `…` 로 명시했다.

> 🔴 **왜 옮겼나**: 표 셀 하나에 세션 서술 전체가 들어가 1줄이 **최대 5,259자**였고
> `read` 툴 1줄 한도 **2,000자**를 넘겨 행 말미가 절단됐다 — 정본을 읽을 수 없는 상태(`R1-3` 미충족).
> 마크다운 표 셀은 줄바꿈이 불가하므로 **표에 둔 채로는 줄 길이를 줄일 수 없다**.
> 지침 `R1-3-6` 이 규정한 「정본에 이력이 섞였다는 신호 ⇒ 분리」의 집행이다.

---

## §O71-B · 🔴🔴 **O71 자기검토 — 확정위반 8 · 판정약점 6 · 구조결함 3**

| 필드 | 값 |
|---|---|
| 현황 | 🔴🔴 **미독이 잘못된 현업 질의를 만들었다** — 확정 8 · 약점 6 · 구조 3 · 시정 9 |
| 건수 | **위반 8 · 시정 9 완료 · 잔여 1**(문서10 독해 = 착수표 ⑥) |
| 관할 문서 | 🔴 **판정 정본 = `50_dbt_파이프라인_미결조치.md` §O71-B**(2026-08-13 **O71-C** 이관) · 20 §N·§N-6 · 01 `[O71-B]` |
| 인덱스 행 | `00_INDEX_이슈원장.md` §1 |

> 🟢 **[2026-08-13 O71-C 이관 완료] 판정 정본은 문서50 §O71-B 다.** 이 절은 **좌표표**이며 수치·판정을 새로 만들지 않는다
> (`R3-5` 충족 · §O68-B·§O69 선례와 동형). 이관은 `R2-8-1` **토큰 대조 44개 · 부재 0** 확인 후 축소했다.
> ⚠️ **「무변경 이관」이 아니다**(`R2-8-2`) — O71-C 의 문서50 전량 독해가 **판정 3건을 교정**했고 그 교정은 정본 **§E** 에 있다.
>
> **왜 처음에 문서50 에 두지 않았나**: O71-B 시점에 문서50 이 **미독**이었고, 미독 문서 편집은 인용보다 무거운 위반이다(§O68-B `A1`).
> ⇒ 읽은 문서(이 파일)에 두고 이관을 유보한 판단이며, O71-C 가 1,632줄을 100줄 × 17회(**미반환 0**)로 확보한 뒤 옮겼다.

### 좌표 (정본 = 문서50 §O71-B)

> 🔴 **아래 소제목 6개는 O71-C 축소 때 `doc_heading_gate` 가 「유실」로 잡아 복원한 것이다**(O70 선례 = 포인터에 소제목 복원 ·
> 골든 자기승인을 피하는 방법이다 · 착수표 ⑧). 각 소제목 아래 1줄은 **verbatim 접두**이고 전문은 문서50 에 있다.

### A. 확정 위반 (8)

**A1** 🔴🔴 `read` **29회 중 13회 미반환** ↔ 재호출 **1회** — 지침·`02`·`03` 은 본문을 한 번도 못 받았는데 §N 을 그 인용으로 발행하고 「전량 독해」로 보고했다…
**A2** 🔴🔴 그 미독이 **이미 확정된 결정(`CONF-1` · 활동 = 코드 1~11 · 727,958명 · 2026-07-31)을 현업에 재질의**하게 만들었다 ⇒ §N-6-B 철회…
**A3~A8** stale ID(`DEC-15·16` ↔ `DEC-19~22` 확정) · 「최초 수치화」 거짓 공적 · `R1-5-4` 직후검증 미실행 · `R2-8-1` 미실행 · 문서10 미독인용 **6세션 연속** · `R0-4` **3세션 연속**

### B. 판정 약점 (6)

사용자 오류 미고지 · `R2-3` 위반 발행 4건 중 **3건 실측 반증** · 「215→235」 단정 · 「23종↔24종」 자기모순 ·
**공156·공157 대조 누락** · 기존 등재를 신규처럼(**O71-C 가 2건 추가**)

### C. 구조 결함 (3)

**`R1-3-7-a` 계수 장치 부재**(기재 형식 강제로 처방 · 게이트화는 하지 않는다) ·
게이트 분모(🟢 O71-C 해소 **193파일**) · 골든 드리프트(🟢 O71-C 해소 **741**)

### D. 지켜진 것

라벨 선점 · `P222` 4연속 판정 · 실측 판정 · 값 무변경 120/120 · 문서50 미독 자진 명시 · 게이트가 `R1-5` 위반 실제 차단.
🔴 그러나 **「게이트 통과 ≠ 지침 준수」**(O59-S 재현).

### E. 즉시 시정 (9/9 완료)

§N-6-B 철회 · DEC-19/20 확정 반영 + `CONF-3` 로 블로커 교체 · 「최초 수치화」 철회 · 23→24종 · 235→상한 ·
`sql:215-220`→**216·218·219·220행** · 공156·공157 대조 추가 · `CONF-2`/「납입(명)」/`#80` 출처 병기 · `R2-8-1` 사후 대조.
🔴 **O71-C 가 3건 더 교정했다**(정본 §E) — E1 170 「즉시 가능」 철회 · E2 171·172 는 폐기된 금액 measure 안 · E3 `CONF-3`·`CONF-2` 선행 등재.

### F. 잔여

⬜ `10_진단_원인분석.md` 전량 독해(착수표 ⑥ · **6세션 연속**) · `FACT_MEMBER_MONTHLY.sql` 전량.
🟢 O71-C 해소 = 문서50 전량 · `99_NEXT` 전량 · 게이트 분모 · 골든 · 「지침 166줄」 규명(`P231`).

## §O71 · 🟢 **`07_추가_지표사전_20260813` 정규화 + 대조 등재 + GOLD 영향 실측(내부)**

| 필드 | 값 |
|---|---|
| 현황 | 🟢 **정규화 완료 · 현업확인 6절(§N) 신규 · GOLD 영향 실측 완료** |
| 건수 | **20지표 판정 · DDL 변경 필요 5** |
| 관할 문서 | **20 §N·§N-6**(정본) · 99_provided_definition 00·07 |
| 인덱스 행 | `00_INDEX_이슈원장.md` §1 |

### 대표 항목

**[2026-08-13 O71]** 🔒 라벨 선점 등재(`R1-4-3`). 게이트는 「다음 **O72**」였으나 **참조 축 과대**
(정의 최대 **70** · 참조 71 = O70 행의 예고 문장 · `P222`) ⇒ **O71** 확정. **4연속 재현**이다.

착수 지시 = 사용자 「`99_provided_definition/07_추가_지표사전_20260813기준md` 를 LLM 이 인식할 수 있게 수정」
\+ 사용자 결정 3건(㉮ 같은 폴더 정규화 + raw 보존 · ㉯ 단독 문서 2표 · ㉰ **대조는 07 이 아니라 이슈원장에**).

**① 정규화** = 확장자 누락(`기준md`) 교정 · 탭 원문 → 마크다운 표 2개(공통 **163~178** 16 + 신규 **54~57** 4 = **20지표**)
· LLM-METADATA·정본 선언·정규화 규칙 5항 부착. 판정 = **값 무변경 기계 검증**
(raw 사본 SHA256 동일 · 셀 **120/120** 공백정규화 완전 일치 · 차이 0) · 줄길이 최대 **249자**.

**② 문서 대조(§N 신설)** = N-1 진짜 중복 후보 **1건**(신168 ↔ 공152 「월」↔「당월」 한 단어차) ·
N-2 단위·분해 보완 **10건** · N-3 캠페인 축 4건 값 도메인 충돌 · N-4 `데이터 출처` **20건 중 2건만 채움** ·
N-5 내부파급(*"215 지표"* 기재 **26파일 40줄** + **라이브 스키마·컬럼 COMMENT 에도 실재** ⇒ 215→235
일괄 치환 금지 = 분모 재측정 선행 `P128` + blast radius 과대 O59-K).

**③ 🟢 후속 실측(§N-6 신설) — 사용자 질의 「이 추가분으로 GOLD 설계가 바뀌어야 하나」**
BRONZE·SILVER·GOLD 를 직접 실측했다(문서 대조가 아니다).

**ⓐ 캠페인 4축은 이미 전 계층 배선 완료**(`GOLD.DIM_CAMPAIGN` 36,144행) — 그리고 **07 이 정본을 교정한다**:
· 🟢 **신57** `BIZ_CASE_TYPE` 실측 **4종**(사례 13,818·기타 8,008·사업 7,294·굿즈 4,795) = **07 과 정확 일치**
  ⇒ **`02` 공16 의 「통합」은 라이브 0건**(공16 이 틀렸다).
· 🟡 **신56** `DOMESTIC_OVERSEAS` 실측 **3종**(해외 12,864·국내 12,577·통합 7,734) ⇒ 07 의 「전체사업」과
  공15 의 「전체」가 **둘 다 라이브 0건**(두 문서가 모두 4종으로 적었다).
· 🔴 **신54** 는 `BRAND`(84종 = `PC광고마케팅`·`M캠페인`·`지상파방송` 등 **매체 세분**)가 아니라
  **`INFLOW_PATH`**(16종 ⊃ 07 의 14종 · 라이브에 `일시` 524·`직원개발` 4 가 더 있다)
  ⇒ 07 의 *"'브랜드'를 지칭"* **서술이 값과 어긋난다**.
· 🟢 **신55** `CAMPAIGN_TYPE` **55종 ⊃ 07 이 열거한 24종**(공백·대소문자 차만)
  ⇒ 잘린 `…..` 의 **회신 요청을 취소**했다(대조로 확정 · DEC-26·`P29` 3단계).

**ⓑ 🔴 새 질문(N-6-B)** — `BRONZE_CRM.TC_CMMN_DTL_CD` `MM010` 실측 **12종**에
**「미납1~미납5」 단독 계열이 없다**(1 활동회원 · **2~6 신규미납1~5** · **7~11 장기미납1~5** · 12 후원중단).
그런데 정본 공51·공52 와 07 의 165·175·176 이 모두 *"활동, 미납1~미납5"* 로 적고 있다
⇒ 활동회원(명)이 **690,518(활동+신규미납) ↔ 727,955(활동+신규+장기)** 로 갈린다(현재행 실측 · **45,058명** 차).
🔴🔴 **[O71-B 철회] 이 「새 질문」은 새 질문이 아니었다** — 원장 §2-B **`CONF-1`** 이
*"활동 = 코드 **1~11**, 727,958명"* 으로 **2026-07-31 확정**(사용자 재확인)해 두었다. **§N-6-B 는 철회했다.**
그리고 *"CONF-3 의 크기를 처음으로 수치화했다"* 는 주장도 **거짓**이다(`CONF-1` 에 727,958/683,095 기재) ⇒ **재측정**이다.

**ⓒ GOLD DDL 변경 필요 = 20건 중 5건** (나머지 15건은 설계 변경 없이 해결)
⚠️ **[O71-B] 그중 신규는 3건**이다 — 163·164 의 (명) 축은 원장 `#80`/`DEC-4`(`UNPAID_MEMBERS` 🔵 구현 대기)로
**이미 등재**돼 있었고, 178 의 분모도 「납입(명)」 항목이 **1,126,391명 실측 + SV distinct 산출 가능**으로 이미 판정해 두었다.
· 🔴 **DDL 변경 5** = 163·164(신규/장기 **분리 컬럼 부재** — `STATUS_UNPAID_CNT` 하나뿐) ·
  165(`MONTH_END_ACTIVE_MEMBERS` 부재 — 있는 것은 `_CNT`=건) ·
  171·172(**금액목표 컬럼이 `SILVER.CRM_BIZ_TARGET`·`GOLD.FACT_TARGET_BIZ` 양쪽에 없다** — `TARGET_CNT`/`*_GOAL_CNT` 뿐)
· 🟢 **즉시 산출 3** = 170(`FMM.DEV_CNT` 비영 49.5%) · 173·174(`FMM.PAID_FEE` 채움 **90.1%** = 36,089,538/40,054,883 · 누계는 SV YTD)
· 🟡 **컬럼 있음·값 미주입 5** = `FMM` 활동·미납 계열 **비영 0/40,054,883**(`FACT_MEMBER_MONTHLY.sql` **216·218·219·220행**이 `0 as …` 하드코딩 · 🔴 **[O71-B 정정] 종전 「215-220」 표기는 근거 없는 좌표였다** — `grep` 실측은 216·218·219·220 이고 215행은 확인되지 않았다) ·
  `NEW_EXISTING_FLAG` **FMM·FME 양쪽 전건 NULL** · `FACT_TARGET_BIZ` 컬럼 4종 있으나 **0행**
· 🔴 **원천 부재 2** = 166·167. BRONZE 전 스키마 목표성 컬럼 스캔 결과
  **`TM_CM_MBER_DVLP_GOAL.GOAL_CNT`(개발목표 건수) 단 1종** ⇒ **E-6 입고 범위 확대** 대상
· 🟢 **배선 완료 4**(신54~57) · 🟢 **SV metric 1**(178 = `COUNT_IF(PAID_FEE>0)` 분모로 산출 가능)

**ⓓ 🔴 선행조건은 「활동 스냅샷」 하나다 — 6건이 여기 걸린다**(163·164·165·175·176·177).
🔴🔴 **[O71-B 정정] 종전 여기 적은 「DEC-15·DEC-16」은 stale 이다** — `DOC-1` 이 2026-07-31 에 **`DEC-19~22` 로 재번호**했고
두 결정은 **확정** 상태다. **진짜 블로커는 `CONF-3`**(정본 공51 판정 조건 내부 모순 · 현업 확인)이며
`DEC-19` 가 *"CONF-3 해소 전 구현 불가"* 를 명시한다. `CONF-2`((건)=약정금액/10,000)도 함께 적용된다.
원천은 **완비**돼 Phase-1 이다: `SILVER.CRM_MEMBER_STATUS_HIST` **7,501,761행** SCD2 ·
`GOLD.DIM_MEMBER` 현재행 상태 완비(활동 682,897·신규미납 7,621·장기미납 37,437·중단 859,387·(해당없음) 175,722·NULL 1
= 1,763,065 = `CRM_MEMBER` 행수 일치). **그러나 월말 시계열 재구성에 제약 3종이 실측됐다**:
· **이력 보유 회원 71.84%**(1,264,790/1,760,504) ⇒ **495,714명은 이력이 없다**. 「이력 없음 = 무변동」으로
  현재상태를 소급 적용할지가 **결정 사항**(근거 없이 소급하면 과거 활동회원을 창작한다).
· **이력 시작 2012-06-12** ↔ `FMM` **199101~202911**. 이전 구간 **287,667행(0.72%)** 은 상태 판정 불가.
· 🔴 **「활동(건)」의 분자를 만들 수 없다** — 정본 계산식은 *"활동회원의 후원사업금액/10,000"* 인데
  **회원 마스터에 약정금액 컬럼이 없다**(`SILVER.CRM_MEMBER` 금액 컬럼 0건 · BRONZE `SPNSR_AMT` 는
  **개발 사건**(`TM_MM_FDRM_MBER_DVLP_AMT`)과 **증감 이력**(`TM_MM_FDRM_MBER_IRSD`)에만 존재)
  ⇒ 월말 유효 약정금액을 **재구성**해야 한다(`FME.SPNSR_AMT` 채움 77.6%·양수 2,180,968 · `CRM_MEMBER_AMT_CHANGE` 324,947).
  🔴 **이 축이 DEC-15·DEC-16 결정문에 빠져 있다** — 결정 전에 반영해야 한다.

**ⓔ 소비계층 파급 표면** = `SV_MEMBER_MONTHLY` COMMENT 의
*"비활성(적재 대기): 활동/누계/미납 카운트 비율, 신규기존 분해"* 문구(값 주입 시 **회수 대상** · `P61`).

⚠️ **미독 명시**(`R1-3-7`) — `50_dbt_파이프라인_미결조치.md`(1,457줄·167,917자)는 이 작업과 무관해 **읽지 않았고**
그 소관 수치·판정을 **인용하지 않았다**.

---

## §O51 · 🟠 **뷰 컬럼 COMMENT 메커니즘 부재(내부)**

| 필드 | 값 |
|---|---|
| 현황 | 🟠 **뷰 컬럼 COMMENT 메커니즘 부재(내부)** |
| 건수 | **1 진행중** |
| 관할 문서 | 00 §O51 |
| 인덱스 행 | `00_INDEX_이슈원장.md` §1 |

### 대표 항목 (무변경 이관)

**[2026-08-07 O51]** GOLD **뷰 컬럼 COMMENT 0/520(0.0%)** vs 같은 시점 GOLD **테이블 535/535(100%)** 실측. 🔴 원인 = `ALTER VIEW ... ALTER COLUMN ... COMMENT` 가 **Snowflake 에 없는 문법**(4변형 전부 실패 ·
`COMMENT ON COLUMN` 은 TABLE 전용). `10_WIDE VIEW 코멘트.sql` 헤더의 *"12뷰 411컬럼 전량 적용(누락 0)"* 은 **거짓 성공 기록**이고, 같은 문법을 복제한 dbt `post_hook` 이 **15/16 뷰 ERROR**.
⚠️`persist_docs:{columns:true}` 도 같은 이유로 대안 아님. ✅ 유일 경로 = **`CREATE VIEW` 인라인 컬럼목록** → 커스텀 머티리얼라이제이션 `macros/gn_view_commented.sql` 신설(probe 10/10). ✅ **O51-C 6뷰 136컬럼 적용**(커버리지 **136/559 = 24.3%**)
/ 🟢 **O51-D 완료(2026-08-10 build 실측)** — `PASS=375 WARN=28 ERROR=0 SKIP=0`, 물리 **8객체 355/355** 확인(baseline 0 → 355) · GOLD 뷰 커버리지 **491/559**(잔여 68 = DROP 예정 2뷰)
· 규칙7 위반 0 · 전건NULL 문안 35/35 · WARN +1 규명(신설 커버리지 게이트, 위반은 DROP 예정 2뷰뿐) · 🆕 **P114**(금칙어 게이트가 부정문을 오탐). 종전 기재 = 8객체 **355/355 컬럼 등재**(신규 132 = BRONZE 코드사전×실적재 전수 재스캔 근거 ·
`10_` 이관 206 · 유령 20 폐기) · 8모델 `gn_view_commented` 전환 + post_hook 전량 제거 · `DIM_MEMBER_CURRENT`/`ACQUISITION` yml 신규 등재(O50 잔여 해소) · build 전 게이트(SELECT 순서 대조)
통과. 🔴 스캔이 종전 문서 오류 **5건 적발**(MM014 실적재 6종≠사전 8종 · MM294 사전외 코드 '58' · CM014 '70대'/'70대 이상' 중복 · CM017=CM013 재라벨 · **O45-B 해소**). 🔴 자기검토로 내 결함 4건 적발·교정(멱등 가드 오탐으로 8모델 조용히 스킵 등).
🆕 **P105**(경고문은 게이트가 아니다) · **P102-B**(스테이지 크기 판정식 정정)

---
