<!-- LLM-METADATA
doc_id: INCIDENT_CASEBOOK
doc_role: 조문의 「왜 필요한가」 경위 서사 — 무변경 이관 사례집(append형)
project: GN_DW (굿네이버스)
created: 2026-08-28
created_by: O106
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# 91_사고사례집 — 조문 경위의 무변경 이관부

> 🔴 **여기 있는 문장은 조문이 아니라 「그 조문이 왜 생겼는가」다.**
> 조문·판정식은 원본(`00_guides/00_작업지침_세션운영규칙.md`)에 그대로 있다.
> ⇒ **매 세션 읽을 필요가 없다.** 읽는 시점 = **쓰기 사고를 조사할 때** ·
>   조문을 개정하려 할 때 · 「이 규칙을 왜 지켜야 하나」가 의심될 때.

> 🟢 **이관 계약**(`R2-8`) — 본문은 **무변경**이다(요약·가필 없음).
> 원본에는 이 문서의 `§C##` 를 가리키는 포인터 1줄이 남았다.
> 🔴 **원본의 조문 ID 집합·제목 집합은 이관 전후로 동일**하다(blocking 검증 통과).

| 앵커 | 소관 조문 | 원본 행(이관 시점) |
|---|---|---|
| `§C01` | `R1-3-7` | 72 |
| `§C02` | `R1-3-7` | 85 |
| `§C03` | `R1-3-7` | 93 |
| `§C04` | `R1-4-1` | 120 |
| `§C05` | `R1-6` | 150 |
| `§C06` | `R1-7` | 163 |
| `§C07` | `R2-7-2` | 204 |
| `§C08` | `R2-7-4` | 212 |
| `§C09` | `R2-8` | 217 |
| `§C10` | `R2-8-4` | 229 |
| `§C11` | `R3. 단계 종료 게이트 (로드맵 각 단계 완료 시 필수)` | 283 |
| `§C12` | `R4-4` | 304 |
| `§C13` | `R1. 사전 준비 및 정보 파악` | 48 |
| `§C14` | `R1-3-6` | 68 |
| `§C15` | `R1-7` | 163 |
| `§C16` | `Step 0.7: 파일 쓰기 안전 규칙 (기존 파일 수정 시 · 정본 =` | 59 |
| `§C17` | `세션 중 상시 준수` | 321 |
| `§C18` | `세션 종료 시` | 429 |
| `§C19` | `R1-6 문서 분할 규약 (도구 스펙)` | 14 |
| `§C20` | `R1-6-22` | 274 |
| `§C21` | `R1-6-23` | 299 |
| `§C22` | `R1-6-24` | 318 |
| `§C23` | `R1-6-2` | 61 |

---

## C01 — 소관 `R1-3-7`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 72행에서 무변경 이관(`O106`).

    * 🔴 **왜 필요한가 (O69 실사례)**: `read` **11회 중 10회**가 컨텍스트 관리로 삭제 반환됐는데
      그 상태로 「지침 로드 OK · 전량 독해 4건」이라 보고했다. 그 결과 브리핑의 근거가 정본이 아니라
      **사용자가 붙여넣은 대화 로그**였고, 착수표 범위(`⓪~⑩`)를 `⓪~⑨` 로 틀리게 적었다.
      R1-3-6 이 경고한 **「읽지 않고 읽은 척」** 이 실제로 났고, 그 실패가 **조용했다**(에러가 아니다).

## C02 — 소관 `R1-3-7`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 85행에서 무변경 이관(`O106`).

      🔴 **왜 필요한가**: O70 은 `read` **39회 중 19회**가 삭제 반환이었는데 복구 절차를 **0회** 실행하고,
      원장에는 *"미반환 2회는 재호출해 확보했다"* 라고 **거짓으로 적었다**(문서50 §O70-B A1·A2).
      건수를 적지 않으면 「몇 번 실패했는지」가 사라지므로 **미실행이 조용해진다** — R1-3-7 이 막으려던 것과 같은 구조다.

## C03 — 소관 `R1-3-7`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 93행에서 무변경 이관(`O106`).

      🔴 **왜 필요한가 — `R1-3-7-a` 에는 구조적 한계가 있다.** 미반환된 호출은 컨텍스트에서
      **사라지므로 나중에 세려 하면 이미 증거가 없다**(O82-C 실측 = 총계 「확정 불가」).
      같은 이유로 **성공한 독해도 트리밍되면 「읽었다」를 사후 검증할 수 없다.**
      ⇒ 사후 역산을 포기하고 **읽은 그 자리에서 기록**한다.

## C04 — 소관 `R1-4-1`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 120행에서 무변경 이관(`O106`).

    승격 근거 = 선례 관례 「**위반 0 을 먼저 실측한 뒤 올린다**」(`AGENCY_AD_PERFORMANCE.AD_DATE`
    warn→error 승격 · 문서50). 2026-08-12 실측 = 중복 **0건** 확인 후 blocking 으로 넣었다.

## C05 — 소관 `R1-6`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 150행에서 무변경 이관(`O106`).

  **왜 분리했나**: `R1-6` 은 세션 운영 규칙이 아니라 **도구 스펙**이고, 조문이 17개까지 늘어
  이 지침이 **426줄 / 45,116B**(바이트 상한 초과)까지 커졌다. 이 문서는 **매 세션 Step 1 에서
  반드시 읽는** 문서라 팽창 비용이 가장 큰 곳에서 났다 ⇒ 스펙을 옮기고 포인터만 남긴다
  (`R2-8` 무변경 이관 · 토큰 117종 부재 0 확인).

## C06 — 소관 `R1-7`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 163행에서 무변경 이관(`O106`).

  **왜 분리했나**: 이 지침이 다시 **328줄 / 33,539B**(여유 7,421B · `doc_type_gate` 축3 경고)까지
  커졌고 `R1-7` 이 **9,403B(28%)** 로 단일 최대 계열이었다.

## C07 — 소관 `R2-7-2`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 204행에서 무변경 이관(`O106`).

    실측 예(`DIM_MEMBER_CURRENT` · 2026-08-12):

## C08 — 소관 `R2-7-4`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 212행에서 무변경 이관(`O106`).

    🔴 이 규칙이 없어서 실제로 무슨 일이 있었나: `06_DDL.sql` 이 *"미매핑은 `'미상'`"* 을 4곳에 선언했지만
    라이브 실측은 `'미상'` **0건**·`(미매핑)` **0건**이었고, **같은 파일 198행은 정반대**로
    *"`'미상'` 으로 창작하지 않는다"* 를 적고 있었다. Agent 는 이 COMMENT 를 답변 근거로 쓴다.

## C09 — 소관 `R2-8`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 217행에서 무변경 이관(`O106`).

  🔴 **왜 필요한가**: O70 이 `99_NEXT_SESSION.md` §0-G·§0-H 를 문서50 으로 옮기며 **원문을 삭제**했는데
  삭제 전 중복 실재를 **기계로 확인하지 않았다**(문서50 §O70-B A3). O62 는 같은 작업에서
  `grep` 매칭 110행으로 확인한 **선례가 있었다** ⇒ 선례가 있는데 지켜지지 않은 것은 조문이 없었기 때문이다.

## C10 — 소관 `R2-8-4`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 229행에서 무변경 이관(`O106`).

    🔴 **왜 필요한가**: 이 워크스페이스는 계정이 **3번** 바뀌었다(`ls82944` → `os09358` → `DV07626` → `UA93987`).
    계정이 바뀌면 DB·SV·Agent 는 **전부 사라지는데 문서의 「완료」 판정은 그대로 남는다.**
    실증 = O84 가 O74 의 은퇴 판정 2건(「뷰 7·SV 7」 · 「VERSION$6 도구 10」)이 **실측 0·0** 임을 적발했고,
    O85 에서 **SV 15·Agent 3 전건이 다시 0** 이 됐다. ⇒ **은퇴는 「그때 됐다」가 아니라 「지금도 있다」여야 한다.**

## C11 — 소관 `R3. 단계 종료 게이트 (로드맵 각 단계 완료 시 필수)`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 283행에서 무변경 이관(`O106`).

     🔴 **결정 문서만 쓰고 끝내면 원장 전체가 조용히 stale 이 된다.** 실증 = `DEC-41` 로 `O8` 의
     후원사업 축을 닫았는데 그 결정을 선행조건으로 인용한 **정본 5곳이 「미결」로 남았고**,
     그중 하나(`50_dbt B2`)는 O92 가 **stale 로 적발한 수치를 그대로 담고 있었다**
     ⇒ 다음 세션이 그 문장을 읽으면 **적발 이전 상태를 믿는다.**

## C12 — 소관 `R4-4`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 304행에서 무변경 이관(`O106`).

  🔴 **왜 필요한가**: `init_ihcho` Step 3 가 **무조건 STOP** 을 요구했는데 사용자는 **같은 턴에
  작업 지시를 동봉하는 것이 상시 패턴**이었다. ⇒ 조문을 지키면 지시를 무시하고, 지시를 따르면
  조문을 위반한다. **최소 6세션 연속 「STOP 미이행」이 등재된 원인은 게으름이 아니라 이 설계 결함**이다
  (`A5` — 세션마다 위반 1건이 자동 누적됐다). 실측 = O85 착수 시점까지 해소되지 않았다.

## C13 — 소관 `R1. 사전 준비 및 정보 파악`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 48행에서 무변경 이관(`O111`).

  🔴 **왜 바꿨나**: 종전 규약(5문서 전량)은 **469,878자**를 요구해 이행 불가능했고,
  그 결과가 「읽지 않고 읽은 척」이었다(`R1-3-7` 이 잡으려던 실패의 상류 원인).
  🔴 **브리핑은 색인이고 정본이 아니다** — 수치·판정을 인용하려면 브리핑이 준 **좌표를 읽는다**
  (`R1-3-7` 「대체 근거 금지」는 그대로 유효하다. 브리핑도 대체 근거가 아니다).

## C14 — 소관 `R1-3-6`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 68행에서 무변경 이관(`O111`).

    * 🔴 **왜 예외가 필요한가**: 이력이 정본과 한 파일에 섞여 있어 원장이 **4,686줄/612KB** 까지 커졌고,
      `read` 1회 한도(2,000줄·1줄 2,000자·출력 10만자)를 넘겨 **R1-3 이 여러 세션 연속 미충족**이었다.
      예외를 명문화하지 않으면 다음 세션도 같은 곳에서 막히거나, **읽지 않고 읽은 척하게 된다.**

## C15 — 소관 `R1-7`

> 원본 `00_guides/00_작업지침_세션운영규칙.md` 163행에서 무변경 이관(`O111`).

  * 🔴 **왜 이 규약이 있는가**: 이 워크스페이스에서 원장이 **4회** 깨졌고 전부 같은 뿌리였으며
    **네 건 모두 기존 게이트를 전부 통과**했다 ⇒ 규율이 없으면 조용히 재발한다.

## C16 — 소관 `Step 0.7: 파일 쓰기 안전 규칙 (기존 파일 수정 시 · 정본 =`

> 원본 `.snowflake/cortex/skills/init_ihcho/SKILL.md` 59행에서 무변경 이관(`O111`).

  🆕 🔴🔴 **[O109 실사고] 이것은 문서만의 문제가 아니다 — `scripts/split_doc.py` 가 절단됐다.**
  `python3 -c` 로 전체 재작성한 뒤 **꼬리 8줄(`__main__` 가드 포함)이 사라졌고 스테이지에도 그 절단본이
  올라갔다**. 🔴 **증상은 에러가 아니라 침묵이었다** — `--republish` 가 **출력 0바이트 · rc 0** 이었다.

## C17 — 소관 `세션 중 상시 준수`

> 원본 `.snowflake/cortex/skills/init_ihcho/SKILL.md` 321행에서 무변경 이관(`O111`).

    실사고 = `edit` 로 넣은 절(67줄)이 직후 실측으로 확인됐는데, 승인받아 돌린 `--rebalance` 가
    **낡은 내용을 읽어**(2,174줄 ↔ 2,107줄) 전 조각을 그것으로 재기록해 **편집이 사라졌다**.
    🔴 **게이트 6종이 전부 🟢 였다** — `--verify` 는 **자기일관성**만 보고, `R1-7-2`(해시 안정성)는
    **같은 시점 2회 읽기**만 본다. 이 사고는 **시점 간 불일치**라 두 축 모두 잡지 못한다.

## C18 — 소관 `세션 종료 시`

> 원본 `.snowflake/cortex/skills/init_ihcho/SKILL.md` 429행에서 무변경 이관(`O111`).

> **실사고**: O109 는 착수표 ⑥ 을 사용자 승인으로 **재정의·수행**하고 원장·인수인계에
> 「착수표 ⑥ 을 닫았다」고 썼는데, **착수표 그 행은 손대지 않았다** ⇒ 브리핑이 계속 ⑥ 을
> 「열린 작업 · 전량 독해 약 34회 필요」로 내보내 **다음 세션이 폐지된 작업을 재수행할 상태**였다.
> 🔴 이것은 `R3-9 ㉥`(내 결정을 인용한 문서 전수 검색)의 가장 값싼 형태이고, 게이트가 **볼 축이 없다**.

## C19 — 소관 `R1-6 문서 분할 규약 (도구 스펙)`

> 원본 `00_guides/01_문서분할_규약.md` 14행에서 무변경 이관(`O111-B`).

> **왜 분리했나**: `R1-6` 은 세션마다 읽어야 하는 **운영 규칙이 아니라 도구 스펙**이고,
> 조문이 17개까지 늘어 지침이 **426줄 / 45,116B**(바이트 상한 초과)까지 커졌다.
> 지침은 **매 세션 Step 1 에서 반드시 읽는** 문서라 그 팽창이 가장 비싼 곳에서 났다.
> ⇒ 지침에는 포인터만 남기고 스펙은 여기로 옮겼다. **분할 작업을 할 때만 이 문서를 읽는다.**
> ⚠️ 조문 번호는 `R1-6-*` 그대로다 — 다른 문서의 `R1-6-N` 인용이 **전건 유효**하다(`O59-K` 리네임 금지).

## C20 — 소관 `R1-6-22`

> 원본 `00_guides/01_문서분할_규약.md` 274행에서 무변경 이관(`O111-B`).

    🔴 **왜**: 이 워크스페이스의 문장은 **좌표로 근거를 댄다**(`20_issue/<문서>_조각/<문서>-0NN.md:행`).
    그 좌표를 죽이는 연산이 이미 3종 있다 — ㉠ `--to-outdir` 폴더화(경로가 바뀐다)
    ㉡ `--rebalance`(행 번호가 이동한다) ㉢ `retire_*`(본문이 다른 문서로 간다).
    실증 = O108 폴더화 직후 **경로가 깨진 좌표 4건**이 남았고 **게이트 6종이 전부 🟢** 였다.
    그 좌표를 따라가려던 다음 세션만 발견할 수 있었고, 그 세션은 근거를 못 찾아
    **「미독」으로 남긴다**(`R1-3-7` 이 막으려던 바로 그 상태다).

## C21 — 소관 `R1-6-23`

> 원본 `00_guides/01_문서분할_규약.md` 299행에서 무변경 이관(`O111-B`).

    🔴 **왜**: 이 워크스페이스에서 **같은 지점에서 두 번** 편집이 사라졌다.

## C22 — 소관 `R1-6-24`

> 원본 `00_guides/01_문서분할_규약.md` 318행에서 무변경 이관(`O111-B`).

    🔴 **왜**: O111-B 가 승인받아 절 은퇴를 실적용하려고 후보를 **읽었더니**
    `50_dbt_…-014.md` §`✅ [DONE 순서9-C] dbt project 배포 + full build green 달성` 이
    제목은 ✅ 인데 본문에 ㉠ **`warn→error 복귀 추적표`**(복귀 트리거가 아직 열린 7행 ·
    `BLOCKING-1` 등) ㉡ **WARN 27건 전량 목록**(*"🔴 이 표가 없어서 같은 질문이 두 번 나왔다"* 라고
    **존재 이유를 스스로 적어 둔** 표)을 담고 있었다 ⇒ 은퇴시키면 **살아 있는 추적표를 닫힌 로그로 옮긴다.**
    🔴 **실측 = 그 조각의 닫힘 후보 5개 중 4개가 열림 신호를 보유**했다

## C23 — 소관 `R1-6-2`

> 원본 `00_guides/01_문서분할_규약.md` 61행에서 무변경 이관(`O111-B`).

      **적는 순간 stale 이 된다**(실측 = 인덱스 5개 · 이력 26개). 현재 값은 `doc_type_gate.py` 로 잰다.

## C24 — 소관 `세션 중 상시 준수` (판정 일반화 3조 ㉠㉡㉢)

> 원본 `.snowflake/cortex/skills/init_ihcho/SKILL.md` 266~281행에서 무변경 이관(`O112`).
> 🔴 **조문(㉠㉡㉢ 제목 + 처방)은 스킬에 남아 있다** — 여기 있는 것은 **실측 사례 서술뿐**이다.

    * ㉠ **「0건」은 「없다」가 아닐 수 있다 — 「내 판정식이 못 본다」일 수 있다.**
      실측 = `split_narrative` 가 **「서사 블록 0개 — 이미 분리돼 있다」**를 냈는데, 실제로는
      신호어 변형 3종(`왜 바꿨나`·`왜 예외가 필요한가`·`왜 이 규약이 있는가`)을 정규식이 몰랐다.
    * ㉡ **제외 규칙은 그 근거가 성립하는 축에만 적용한다.**
      실측 2건 = `index_row_gate` 가 자동 생성 표 제외를 **중복 축까지** 적용해(근거는 유실 축에만
      성립) 도구가 같은 행을 두 번 찍어도 침묵할 수 있었다 · `doc_coord_gate` 가 `_archive` 를
      **실재 판정 분모에서도** 빼서 **은퇴본만 남은 8건**을 「어디에도 없다」로 오분류했다
      (그 오분류가 인수인계에 **불필요한 사람 판단 8건**으로 실려 넘어왔다).
    * ㉢ **판정 문구가 세는 것과 사람이 읽는 뜻이 다를 수 있다.**
      실측 = `index_row_gate` 의 `중복 0` 은 실은 **「골든 대비 증가 0」**이었고, 그 문서에는
      **중복 행이 10건 실재**했다(정상 표였다) ⇒ 판정은 옳고 **문구가 은폐**했다.
    🔴 **셋 다 게이트가 🟢 인 상태에서 났다** ⇒ `R3-9`(게이트가 보지 않는 축)의 실물 사례다.

    🆕 **[O112 4번째 실물 사례]** 같은 계열이 한 번 더 났다 — `session_brief.open_tasks` 가
    **조각 경계에 걸친 착수표 행을 전부 탈락**시켜 열린 작업을 **11 로 보고**했는데,
    논리 문서(조각 머리말 제거 concat)로 고치자 **15** 였다. 숨어 있던 4건 중 2건이
    🔴🔴 **최우선**(㉒ `AGENT_MEMBER` 버전업 · ㉗ NL 라우팅 스모크)이었다.
    ⇒ **㉠ 의 최악 형태 = 「줄어든 수」가 「닫았다」로 읽힌다**(세션 종료 판정식이 그 수를 쓴다).

## C25 — 소관 `R1-6-23` (`--expect` 두 사고 상세)

> 원본 `.snowflake/cortex/skills/init_ihcho/SKILL.md` 349~353·460~462행에서 무변경 이관(`O112`).
> 🔴 C21 은 같은 조문의 **한 줄 요약**이다 — 상세 경위는 이 절이 정본이다.

    🔴 **같은 사고가 두 번 났다**: O107 은 `--rebalance` 가 **낡은 내용을 읽어** 편집이 사라졌고,
    O111 은 원장 행을 넣고 **같은 명령에서 `grep -c` = 1 을 확인**한 뒤 `--republish` 를 돌렸는데
    나중에 세니 **행이 없었다**(조각 스냅샷이 없어 재작성 복원). 🔴 **두 번 다 게이트 6종이 전부 🟢** —
    `--verify` 는 **자기일관성**만 보고 `R1-7-2` 는 **같은 시점 2회 읽기**만 보므로
    **시점 간 불일치는 원리적으로 못 잡는다.**

    🆕 **[O112 실측 — 가드가 처음으로 「막았다」]** 원장 §1 에 O112 선점 행을 넣고
    `--expect "O112 — 착수 선점(진행중)"` 로 재발행하자 **신선도 FAIL 로 쓰지 않고 멈췄다.**
    원인은 소실이 아니라 **토큰이 그 문자열이 아니었던 것**이다(행이
    ``**`O112` — 착수 선점(진행중)**`` 이라 백틱이 끼어 연속 부분문자열이 아니었다).
    ⇒ 🟢 **토큰은 「연속 리터럴」로 준다**(마크다운 장식을 건너뛰지 않는다) ·
    🟢 **가드가 멈추면 먼저 토큰을 의심하라**(파일이 아니라 선언이 틀린 경우가 있다) ·
    🔴 그럼에도 **막은 것이 옳다** — 통과시켰다면 「보호했다」는 거짓 안심이 남았다.

## C26 — 소관 `R1-6`·`R1-7` 포인터 · `R1-6-25`

> 원본 `00_guides/00_작업지침_세션운영규칙.md`·`02_파일쓰기_안전규약.md` 포인터 절에서 이관(`O112-B`).
> 🔴 조문과 판정식은 정본에 남아 있다 — 여기 있는 것은 **경위**뿐이다.

    🔴 **[O112-B 적발] 「조문 범위·개수」를 문서에 적은 자리가 전부 낡아 있었다** — 실측 7곳:
    · 지침 = `R1-6-1 ~ R1-6-17`(실제 `R1-6-25`) · `R1-7-1 ~ R1-7-9`(실제 `R1-7-10`)
    · 지침·스킬·쓰기규약·원장 = *"Step 0.7 이 **9개** 조문을 전부 요약"* (실제 10개)
    ⇒ 🔴 **더 나쁜 것은 그 기재가 「계약 이행」의 근거로 쓰이고 있었다는 점**이다:
      `R1-7` 을 별도 문서로 이관한 정당화가 *"Step 0.7 이 전 조문을 재수록한다"* 였는데,
      개수로 적혀 있어 **조문이 늘어도 아무도 눈치채지 못했다**.
    🔴 **그리고 실제로 한 곳이 비어 있었다** — `R1-7-10`(스냅샷은 `snapshot_util` 경유)이
      **지침의 조문 제목 목록에 편입되지 않았다**(O110 이 신설할 때 스킬 Step 0.7 만 고쳤다).
      ⇒ 「9개」라는 기재가 **그럴듯해 보였기 때문에** 누락이 오래 살아남았다.
    🟢 처방 = ㉠ 개수·범위를 지우고 **재는 방법**(`clause_order_gate`)으로 교체
             ㉡ 판정식을 **개수 → 집합 대조**로 바꿈(계열 ↔ Step 0.7 의 `R1-7-N` 등장 집합)
             ㉢ 누락된 `R1-7-10` 을 지침 목록에 편입.

    🔴 **[O112-B] `R1-6-25` 의 경위** = O112 가 `01_문서분할_규약.md` 를 분할한 직후,
    `R1-6-18 ④`(분모 4곳 편입) 체크리스트를 **이행했는데도** 두 곳이 깨져 있었다:
    ㉠ `clause_order_gate` 가 **`조문 0개`를 찍고도 ✅**(총계 95 → 70 · 허브는 목차만 담는다)
    ㉡ `fix_stale_counts` 가 그 문서를 **`FLAT_SOURCES`(미분할)** 로 취급해 본문을 건너뜀
       — 🔴 그 파일의 주석이 **이미 같은 위험을 경고**하고 있었는데도 놓쳤다.
    ⇒ 🟢 체크리스트를 기계 판정으로 바꿨다 = `doc_census.split_denominator_check()`
      (음성 테스트 = `test_doc_census.py` 축6 · 분모를 일부러 오염시켜 검출·복구를 양축 단정).
    ⚠️ 부수 실측 = 그 문서는 `#` 1개뿐인 **평면 조문 목록**이라 원자 1개가 **29 KB(252줄)**로 남았고
      `--rebalance --fill` 로 더 쪼개지지 않는다 ⇒ **절 제목을 넣는 것이 재균형보다 선행**이다.

_Co-authored with CoCo_
