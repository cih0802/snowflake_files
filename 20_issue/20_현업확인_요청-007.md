<!-- SPLIT-CHUNK 20_현업확인_요청.md | 007/007 | 허브 = 20_현업확인_요청.md | 원문 946~1071행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### N-6. 🟢 **[2026-08-13 O71 실측] 라이브 대조로 스스로 답한 것 · 새로 생긴 질문**

> 위 N-1~N-4 를 발행한 뒤 **BRONZE·SILVER·GOLD 를 직접 실측**했습니다(문서 대조가 아닙니다).
> 그 결과 **N-3 의 4건 중 3건은 회신 없이 판정할 수 있게 됐고**(DEC-26·`P29` 3단계 = 코드사전 × 실적재 대조),
> 대신 **새 질문 1건**이 생겼습니다.

#### N-6-A. 캠페인 4축 — 값이 라이브에 실재하고, 07 이 오히려 기존 문서를 교정합니다

`GOLD.DIM_CAMPAIGN` 36,144행 실측:

| 07 신규 | 라이브 컬럼 | 라이브 실제 값 | 07 목록과의 관계 | 판정 |
|---|---|---|---|---|
| 57 캠페인유형(사업/사례) | `BIZ_CASE_TYPE` 33,915행 채움 | **사례 13,818 · 기타 8,008 · 사업 7,294 · 굿즈 4,795** (4종) | 🟢 **07 의 4종과 정확 일치** | 🟢 **07 이 맞고 `02` 공16 이 틀렸다** — 공16 의 「통합」은 **라이브에 0건**이다 |
| 56 캠페인유형(국내/해외) | `DOMESTIC_OVERSEAS` 33,175행 | **해외 12,864 · 국내 12,577 · 통합 7,734** (3종) | 07 의 「전체사업」 **라이브 0건** | 🟡 07 의 「전체사업」도 공15 의 「전체」도 **실재하지 않는다** ⇒ 두 문서 모두 4종으로 적었으나 실측은 **3종** |
| 54 개발인입경로 | `INFLOW_PATH` 33,915행 · **16종** | 디지털 22,425 · 회원 온라인개발 3,037 · 지역개발 1,786 · 영상광고 1,080 · 방송 1,075 · 교육기관 913 · 회원 콜개발 819 · **일시 524** · 기업 472 · 회원 기타 459 · 뉴미디어 379 · 재송출 352 · 마케팅콜개발 254 · 회원 오프라인개발 245 · 대면모금 91 · **직원개발 4** | 🟢 07 의 **14종이 전부 실재**(공백 표기차만: `회원 온라인개발` ↔ `회원온라인개발`) + 라이브에 **`일시`·`직원개발` 2종이 더 있다** | 🔴 **07 의 서술이 값과 어긋난다** — 07 은 *"'브랜드'를 지칭"* 이라 적었으나 `BRAND`(84종)는 `PC광고마케팅`·`M캠페인`·`지상파방송` 같은 **매체·채널 세분 라벨**이고 07 의 14종과 다르다. 값 기준으로는 **`INFLOW_PATH`(회원인입경로 `MBER_INFLOW_PATH_CD`)** 다 |
| 55 캠페인카테고리 | `CAMPAIGN_TYPE` 33,892행 · **55종** | 국내사례캠페인 7,763 · 굿즈캠페인 4,175 · 해외캠페인 2,879 · … | 🟢 07 이 열거한 **24종 전부 실재**(공백·대소문자 차만: `기타 교육기관캠페인` · `홈페이지(PC/모바일)`) | 🟢 **잘린 `…..` 를 회신으로 받지 않아도 됩니다** — 라이브 55종이 전체 도메인이다 |

**여쭙는 것이 줄었습니다 (택1)**
1. 🔴 **54 는 「브랜드」가 아니라 「회원인입경로」가 맞습니까?** 값 14종이 `INFLOW_PATH` 와 일치합니다.
   (맞다면 07 문서의 *"'브랜드'를 지칭"* 표현만 정정하면 되고, 배선은 이미 끝나 있습니다.)
2. 🟡 **56 의 「전체사업」/「전체」는 폐기된 값입니까?** 라이브에 0건입니다. 신설 예정 값이면 알려주십시오.
3. 🟢 **57 은 07 기준으로 확정하겠습니다**(사업·사례·굿즈·기타). 공16 의 「통합」은 오기로 처리합니다 — 이견 있으면 알려주십시오.
4. 🟢 **55 의 잘린 목록은 요청 취소합니다** — 라이브 55종으로 확정합니다. 단 07 이 *"신규규칙"* 이라 했으므로
   **55종에서 줄이거나 새 값을 넣는 계획이 있으면** 알려주십시오.

**판정**: ____________

#### N-6-B. ❌ **철회 — 이미 확정된 결정을 재질의한 것이었습니다 (2026-08-13 O71-B)**

> 🔴🔴 **이 항목은 현업에 묻지 마십시오.** 종전에 *"「미납1~미납5」가 신규미납 5단계입니까, 신규+장기 10단계입니까"* 를
> 새 질문으로 발행했으나, **이미 2026-07-31 `CONF-1` 에서 확정된 사항**입니다.
>
> **`CONF-1`(이슈원장 §2-B)** = *"활동회원 범위 충돌 — 현업 정본은 활동 = 활동+미납1~5(**코드 1~11, 727,958명**),
> 최초 확정은 코드 1만(683,095명)"* → ✅ **해결 2026-07-31: 사용자 재확인 → 정본을 따른다(코드 1~11)**.
> `DEC-19` 도 *"활동 = `MBER_STAT_CD` **1~11**(정본 준수, 중단12만 제외)"* 로 같은 결론을 명시합니다.
> ⇒ 답은 **(다) 신규미납·장기미납 전부 포함**이고, 이미 결정·기록돼 있었습니다.
>
> 🔴 **왜 틀렸나**: 이 절을 쓸 때 이슈원장 **§2-B(152~201행)를 읽지 않았습니다**(`R1-1`·`R1-3-7` 위반).
> 그 구간에 `CONF-1`·`CONF-2`·`DEC-19`·`DEC-20`·`#80` 이 전부 있었습니다.
> ⇒ 미독이 **실제로 잘못된 현업 질의를 만들어냈습니다** — 문서 대조·실측을 아무리 해도 원장을 안 읽으면 이렇게 됩니다.
>
> 🔴 **「CONF-3 의 크기를 처음으로 수치화했다」는 주장도 철회합니다** — `CONF-1` 에 이미
> **727,958(1~11) / 683,095(1만)** 이 기재돼 있었습니다. 이번 실측(2026-08-13)은 **최초 측정이 아니라 재측정**입니다.

**참고 — 재측정 결과(현재행 기준 · 2026-08-13)**: 종전 기재와 시점 차이만 있고 결론은 같습니다.

| 해석 | 포함 상태 | CONF-1 기재(2026-07-31) | 재측정(2026-08-13) |
|---|--:|--:|--:|
| ✅ **확정: 활동 + 신규미납1~5 + 장기미납1~5** | 11종 | **727,958** | **727,955** |
| (참고) 활동회원만 | 1종 | 683,095 | 682,897 |
| (참고) 활동 + 신규미납만 | 6종 | — | 690,518 |

**코드 실측 근거는 유효합니다** — `BRONZE_CRM.TC_CMMN_DTL_CD` `MM010` **12종** 전량:

| 코드 | 라벨 | 코드 | 라벨 |
|---|---|---|---|
| 1 | 활동회원 | 7~11 | **장기미납1 ~ 장기미납5** |
| 2~6 | **신규미납1 ~ 신규미납5** | 12 | 후원중단 |

정본 `02` **공132 회원상태**도 *"활동 / 신규미납1~5 / 장기미납1~5 / 중단"* 으로 코드와 일치합니다.
⇒ 공51·공52·공84 와 07 의 165·175·176 이 쓰는 *"미납1~미납5"* 는 **10종을 줄여 쓴 관용 표기**이고,
`CONF-1` 이 그 해석을 이미 확정했습니다.

⚠️ **다만 `CONF-1` 이 남긴 파생 주의는 07 에 그대로 적용됩니다** —
*"미납회원이 활동회원의 **부분집합**이 되므로 `ACTIVE_*` 와 `UNPAID_*` 는 배타가 아니다 →
「활동+미납=전체」 합산 시 **이중계상**"*.
🔴 **07 의 163·164 는 정확히 이 위험 지점입니다**: 165(월말활동회원)에 163·164 를 더하면 이중계상입니다.
⇒ 신설 컬럼 COMMENT 와 SV 문안에 이 배타성 부재를 명시해야 합니다(`CONF-1` 이 요구한 조치).

**판정**: ❌ **철회 — 회신 불요**

#### N-6-C. GOLD 설계 변경 범위 (현업 회신 불요 · 우리 소관)

실측 결론: **20건 중 GOLD DDL 변경이 필요한 것은 5건**이고 나머지 15건은 설계 변경 없이 해결됩니다.

| 구분 | 건수 | 지표 | 근거(실측) |
|---|--:|---|---|
| 🟢 **지금 산출 가능** | ~~3~~ **2** | 173 납입회비(원) · 174 누계 납입회비(원) | `FMM.PAID_FEE` 채움 90.1%(36,089,538/40,054,883) · 누계는 SV metric YTD(공63 선례) |
| 🟠 **`CONF-2` 종속**<br>🔴 **[O71-C 정정]** | 1 | **170 월 누계개발(건)** — 종전 「즉시 산출 가능」으로 분류했으나 **철회** | 🔴 문서50 **1404행**: *"`DEV_CNT` 의미 = FME **사건수**(금액/10000 아님) · 금액기반 「건」은 원천 금액컬럼 + FME 변경 필요(**별도트랙**)"* ⇒ 정본 「건」은 **`CONF-2`**(=약정금액÷10,000)에 종속이고 `FMM.DEV_CNT`(비영 49.5%)는 **사건수**다. 두 단위를 같은 것으로 보면 **조용히 다른 값**이 나온다 |
| 🟡 **컬럼 있음 · 값 미주입**(설계 변경 아님) | 5 | 165 월말활동회원(명)의 (건) 축 · 175·176 신규/기존 · 177 평균 약정금액 · 168·169 월(누계)목표(건) | `FMM` 활동·미납 계열 **전건 0**(비영 0/40,054,883 · `FACT_MEMBER_MONTHLY.sql` **216·218·219·220행**이 `0 as …` 하드코딩) · `NEW_EXISTING_FLAG` **FMM·FME 양쪽 전건 NULL** · `FACT_TARGET_BIZ` 는 컬럼 4종 있으나 **0행** |
| 🔴 **GOLD DDL 변경 필요** | **5**<br>(🔴 **신규는 3** · 2건은 기존 등재) | **163 신규미납 · 164 장기미납**(신규/장기 분리 컬럼 부재 — `STATUS_UNPAID_CNT` 하나뿐) · **165 월말활동회원(명)**(`MONTH_END_ACTIVE_MEMBERS` 부재 — 있는 것은 `_CNT`=건) · **171·172 월(누계)목표(원)**(`FACT_TARGET_BIZ`·`SILVER.CRM_BIZ_TARGET` 에 **금액 컬럼 자체가 없다** — `TARGET_CNT`/`*_GOAL_CNT` 뿐) | `INFORMATION_SCHEMA` 전수 스캔 · 🔴 **[O71-B 정정] 163·164 의 (명) 축은 신규가 아니다** — 원장 §2-B **`#80`/`DEC-4` = `UNPAID_MEMBERS` 신설 🔵 구현 대기**로 **이미 등재**돼 있었다. 07 은 그 미결 항목에 **신규/장기 분리 요건을 추가**하는 것이다 ⇒ 신설 판단이 아니라 **`#80` 확장**으로 처리한다 · 🔴🔴 **[O71-C 추가 · 171·172 는 「폐기된 설계안」이다]** 문서50 **1519행** = FACT_TARGET_BIZ 단위충돌 처리에서 *"**①금액 measure 신설안 폐기**"*(2026-07-20)로 **의도적으로 버린** 슬롯이다(SILVER 를 `TARGET_CNT`(건)+`TARGET_TYPE` 으로 재구축). ⇒ 되살릴 때 그때의 폐기 사유(**금액을 건 슬롯에 강제투입 = `FACT_BUDGET` 「재무 오귀속 방지」 위배**)를 다시 어기지 않도록 **건과 별개 컬럼**으로 신설해야 한다 |
| 🔴 **원천 부재 = 입고 선행** | 2 | 166 월말활동목표(건) · 167 연활동율목표(%) | BRONZE 전 스키마 목표성 컬럼 스캔 = **`TM_CM_MBER_DVLP_GOAL.GOAL_CNT`(개발목표 건수) 단 1종**. 활동목표·활동율목표·금액목표는 **원천에 없다** ⇒ **E-6 입고 범위 확대** 대상(§N-4) |
| 🟢 **이미 전 계층 배선 완료** | 4 | 신54~57 캠페인 4축 | 위 N-6-A |
| 🟢 **SV metric 신설로 해결**(GOLD 무변경) | 1 | 178 평균 납입금액(원) | 분자 `PAID_FEE` 실재 · 분모 「납입명」은 전용 컬럼이 없지만 FMM grain=회원×월이라 `COUNT_IF(PAID_FEE>0)` 로 산출 가능. ⚠️ **[O71-B 정정] 이것도 새 판정이 아니다** — 원장 §2-B **「납입(명)」 항목**이 *"원천 존재·적재확인(`TM_PM_MBRFEE_ACMSLT` 46.4M · 납입성공 회원 **1,126,391명** 실측 2026-07-27) · 🟢 **SV distinct-member 산출 가능**"* 로 **이미 같은 결론**을 등재해 두었다 |

🔴 **최상위 선행조건은 「활동 스냅샷」 하나다** — 163·164·165·175·176·177 = **6건이 모두 여기 걸린다**.

🔴🔴 **[O71-B 정정 — 선행조건의 정체를 틀리게 지목했다]** 종전 이 절은 *"DEC-15·DEC-16 **결정 대기**"* 라고 적었으나
**두 결정은 이미 확정됐고 번호도 바뀌었다**(이슈원장 §2-B 실측):
* **`DOC-1`** = *"`DEC-15·16·17` 번호 중복 → §3-B 를 **`DEC-19~22` 로 재번호**"* ✅ 해결 2026-07-31.
  ⇒ 내가 인용한 「DEC-15·DEC-16」은 **2026-07-30 시점의 구 번호**다(그 구간만 읽었기 때문에 생긴 stale 인용).
* **`DEC-19`**(구 DEC-15) `PREV_MONTH_END_ACTIVE_CNT` ✅ **확정 2026-07-31** — **(d) 원천 as-of 재평가** ·
  활동 = `MBER_STAT_CD` **1~11** · FDRM 한정. ⚠️ 그리고 ***"CONF-3 해소 전 구현 불가"* 를 명시**한다.
* **`DEC-20`**(구 DEC-16) `YEAR_START/END_ACTIVE_CNT` ✅ **확정 2026-07-31** — (a) 12개월 반복+SUM 금지 · DDL 무변경.
* ⇒ 🔴 **진짜 블로커는 결정이 아니라 `CONF-3`(현업 확인)이다** — 정본 공51 「합의 필요사항」의
  *"마지막 중단일이 마지막 재후원일보다 커야함"*(중단 우세) ↔ *"재후원 넘버링이 중단 넘버링보다 크다는 조건 필요"*(재후원 우세)
  가 **방향이 상반**된다(원장 `CONF-3` = 🟠 현업 확인 필요 · 2026-07-31 등재).
  나는 `02` 를 읽으며 그 문장을 보았지만 **원장의 `CONF-3` 와 연결하지 못했다.**

⚠️ **`CONF-2` 도 함께 적용된다**(이미 등재 · 내가 신규 발견처럼 적었다) —
*"`(건)` 지표는 건수가 아니라 **약정금액 ÷ 10,000**(정본 #36·37·52·84·157)"* ·
*"`UNPAID_CNT`(#36) 재사용 시 정본 정의 파괴 → **신규 컬럼 사용 필수**"*.
⇒ 07 의 163·164·165·175·176 의 (건) 축은 전부 이 규약을 따라야 한다.
🔴 **[O71-C 추가] 문서50 이 이미 같은 귀속을 등재해 두었다** — §BLOCKING-5(O43 · 2026-08-06) **1368~1371행**:
*"활동회원 8종 → **CONF-3**(정본 #51 판정 조건 내부 모순) 현업 확인 · 미납 카운트 3종 → **CONF-2**"*.
⇒ 07 의 6건은 **새 트랙이 아니라 그 「B 버킷(원천은 있으나 산출 로직 미구현 51건 · 외부 의존 0)」에 편입**된다.
🟢 다만 이번에 **새로 실측된 것**은 아래 제약 3종이다(문서50 에 없다).

원천은 **완비**돼 있어 Phase-1 이다: `SILVER.CRM_MEMBER_STATUS_HIST` **7,501,761행** SCD2(`BF_STAT_CD`→`CHN_STAT_CD` + `EFFECTIVE_FROM/TO` + `IS_CURRENT`) ·
`GOLD.DIM_MEMBER` 현재행에 `MBER_STAT_CD`·`MEMBER_STATUS_NAME`·`MEMBER_STATUS_GROUP` **완비**(활동 682,897 · 신규미납 7,621 · 장기미납 37,437 · 중단 859,387 · (해당없음) 175,722 · NULL 1 = 1,763,065 = `CRM_MEMBER` 행수 일치).

⚠️ **그러나 월말 시계열 재구성에는 실측 제약 3개가 있다**(설계 결정 전에 반드시 반영할 것):
1. **회원 커버리지 71.84%** — 상태이력 보유 회원 1,264,790 / `FMM` 회원 1,760,504 ⇒ **495,714명(28.16%)은 이력이 없다.**
   「이력 없음 = 상태 무변동」으로 보고 `DIM_MEMBER` 현재상태를 소급 적용할지가 **결정 사항**이다(근거 없이 소급하면 과거 활동회원이 창작된다).
2. **이력 시작이 2012-06-12** 인데 `FMM` 은 **199101~202911** 이다. 2012-06 이전은 **287,667행(0.72%)** 이라 영향은 작지만 **그 구간은 상태 판정 불가**다.
3. 🔴 **「활동(건)」의 분자를 만들 수 없다** — 정본 계산식은 *"활동회원의 후원사업금액/10,000"* 인데
   **회원 마스터에 현재 약정금액 컬럼이 없다**(`SILVER.CRM_MEMBER` 금액 컬럼 0건 · BRONZE 스캔 결과 `SPNSR_AMT` 는 **개발 사건**(`TM_MM_FDRM_MBER_DVLP_AMT`)과 **증감 이력**(`TM_MM_FDRM_MBER_IRSD`)에만 존재).
   ⇒ 월말 유효 약정금액은 **개발 사건 + 증감 이력으로 재구성**해야 한다(`FME.SPNSR_AMT` 채움 77.6%·양수 2,180,968 · `CRM_MEMBER_AMT_CHANGE` 324,947행).
   이 재구성이 165·175·176·177 의 **진짜 선행조건**이며 **`DEC-19`·`DEC-20` 결정문에 이 축이 명시돼 있지 않다**
   (`DEC-19` 는 상태 as-of 재평가만 정하고 **약정금액 as-of 는 다루지 않는다**).
   🔴🔴 **[2026-08-20 O95 정정] 위 항목 3 은 틀렸다 — 철회한다.**
   위 문장은 *"`SPNSR_AMT` 는 개발 사건과 증감 이력에만 존재"* 라고 적었으나 **BRONZE 스캔이 불완전했다.**
   재실측 결과 `SPNSR_AMT` 는 **4개 테이블**에 있고 그중 하나가 **회원×후원사업 마스터**다:
   · `TM_MM_FDRM_MBER_DVLP_AMT` (개발 사건) · `TM_MM_FDRM_MBER_IRSD` (증감 이력)
   · `TM_MM_FDRM_MBER_RELATNSP_DVLP_AMT` (결연 개발)
   · 🟢 **`TM_MM_FDRM_MBER_SPNSR_BSNS`** (회원×후원사업 **마스터** · 채움 **100%** · 합 **524억**)
   ⇒ **재구성이 필요하지 않다.** 마스터의 미중단 사업 금액을 그대로 합하면 정본 #52 분자가 된다.
   🟢 **`O93-B` 가 이미 구현·검증을 끝냈다** — `SILVER.CRM_MEMBER_SPONSOR_SPAN` 신설 후
   `GOLD.FACT_MEMBER_MONTHLY.ACTIVE_CNT` 배선 완료(202608 기준 산출됨).
   🔴 R2-6: 실측 수치 정본은 `30_output_share/10_원천입고_결손요약.md` §6-1 ②·§6-2 다.
   ⚠️ **위 「재구성 선행조건」을 인용한 문서가 있으면 함께 정정해야 한다**(`R3-9 ㉥`).
   🟢 **다만 항목 1·2 의 제약은 유효하다** — 그 둘은 **상태이력** 축이고 이 정정은 **금액** 축이다.
   🔴 **교훈**: 이 오류의 원인은 *"BRONZE 스캔 결과"* 라고 적으면서 **스캔 범위를 남기지 않은 것**이다.
   4개 중 2개만 열거했는데 문장은 「~에만 존재」라는 **전량 단정**이었다 ⇒ 전량 단정에는 분모를 함께 적는다.

⚠️ 소비계층 파급 표면 = `SV_MEMBER_MONTHLY` COMMENT 의 *"비활성(적재 대기): 활동/누계/미납 카운트 비율, 신규기존 분해"* 문구(값 주입 시 회수 대상 · `P61`).

---

### N-7. 🆕 **[2026-08-25] 캠페인 SV 3종 — 캠페인 속성 스냅샷 시점 확인**

> 경위: O98/O99 에서 `SV_MEMBER_EVENT` 는 캠페인 속성을 **개발사건 시점 동결값**(`CRM_MEMBER_DEV._AT_EVENT`)으로
> 전환했습니다. 같은 캠페인 속성 계열을 쓰는 `SV_MEMBER_COHORT`·`SV_MEMBER_FEE`·`SV_MEMBER_SPONSOR_BIZ`
> 3종은 아직 `DIM_CAMPAIGN` 실시간 조인이라, 동일 전환을 하려면 **동결 시점**을 grain 별로 정해야 합니다.

**여쭙는 것**

1. `DIM_MEMBER_ACQUISITION`(회원 1행 = 획득 grain, `SV_MEMBER_COHORT`·`SV_MEMBER_FEE` 가 사용)의
   캠페인 속성(브랜드·상위캠페인·홍보방법·인입경로·캠페인분류·유형1/2·UTM·후원구분·법인구분)을
   **회원 획득 시점 값으로 고정**해도 됩니까? (현재는 매 build 시 `DIM_CAMPAIGN` 최신값을 다시 조인 —
   마스터 정정 시 과거 획득 건의 캠페인 속성도 조용히 바뀝니다.)
2. `FACT_MEMBER_SPONSOR_BIZ`(회원×약정 grain, `SV_MEMBER_SPONSOR_BIZ` 가 사용)의 대표캠페인 속성도
   **약정(후원) 개시 시점 값으로 고정**해도 됩니까?
3. 위 2건 모두 "예"라면 동결 대상 속성 범위는 요건1·2 와 동일한 9속성(안내1 참조)으로 맞추면 됩니까,
   아니면 grain 별로 필요한 속성만 선별합니까?

**판정**: 🟢 [2026-08-25] **확정 — `DEC-43`**(정본 = `30_설계_의사결정.md` §29).
① 획득시점 동결(`DIM_MEMBER_ACQUISITION`) ② 약정개시시점 동결(`FACT_MEMBER_SPONSOR_BIZ`)
③ 동결 대상 = 9속성 + `BRAND`·`PARENT_CAMPAIGN_NAME`·`PROMO_METHOD_NAME` **12개 전체**(사용자 확정 — "3개 포함 전체 동결").
⚠️ [2026-08-25 O101] 라벨이 `DEC-42` → **`DEC-43`** 으로 개번됐다(`DEC42` 는 O96 이 선점 · `30-009 §29` 참조).

---

### N-8. 🆕 🔴 **[2026-08-25 O101] `MKTG_UTM` 라벨 사전 미등재 — 코드 1종이 캠페인의 63.8% 를 덮는다**

> 경위: `DEC-43` 검토 중 실측. 동결 12속성 중 **UTM·마케팅캠페인 두 축의 라벨 커버리지가 낮다.**
> 종전 `CRM_CAMPAIGN` 주석은 *"`MK_UTM` 191종 유일(fan-out 없음)"* 만 검증했고
> **커버리지(고아 코드)는 측정하지 않았다** ⇒ 낮은 채움률이 그대로 GOLD 3테이블·차원 1종에 전파됐다.

**① 실측 (2026-08-25)**

| 축 | 코드 채움 | 라벨 채움 | 차이 | 원인 |
|---|---|---|---|---|
| `MKTG_UTM` (캠페인 마스터 36,163행) | **93.96%** | **34.00%** | **-59.96%p** | 고아코드 **1종**(`192`)이 **21,682건 = 63.8%** |
| `MKTG_CMPGN_NM` (동) | 93.90% | 74.40% | -19.50%p | 브리지 미도달분 |
| `ACQ_MKTG_UTM_NM`(`FACT_MEMBER_COHORT` 1,585,949행) | — | **18.27%** | — | 위 고아코드가 회원 축으로 전파 |
| `ACQ_MKTG_CMPGN_NM`(동) | — | 63.74% | — | 〃 |

**② 여쭙는 것**

1. `TM_CM_MKTNG_UTM` 에 **`MK_UTM = 192` 가 없습니다**(사전 191종 = 1~191 추정 · 캠페인은 192종).
   `192` 는 ㉠ **사전 등재 누락**(추가 예정)입니까 ㉡ **"UTM 없음"을 뜻하는 기본값/센티넬**입니까?
   🔴 저희가 추측해 라벨을 만들지 않겠습니다(`R2-7-1` 창작 금지 · `P36` 숫자 코드 추측 금지).
2. ㉡(센티넬)이면 **코드 채움률 93.96% 는 오해를 부릅니다** — 실제 UTM 보유는 34% 입니다.
   이 경우 `192` 를 `NULL` 로 정규화해도 됩니까?
3. `MKTG_CMPGN_NM` 라벨 미도달 19.5%p 도 같은 성격(사전 미등재)인지 확인 부탁드립니다.

**판정**: ____________

---

### N-9. 🆕 **[2026-08-26 O102] `SV_MEMBER_COHORT.CAMPAIGN_NAME` 시점 혼합 — 8속성 동결 ↔ 1속성 실시간**

> 경위: `DEC-43`(12속성 동결) 구현 후 `SV_MEMBER_COHORT` 를 재점검했다. `DIM_MEMBER_ACQUISITION`
> 이 노출하는 캠페인 속성 12개 중 8개는 **회원 획득 시점 동결값**(`ACQ_*`)인데,
> `CAMPAIGN_NAME`(캠페인 이름 그 자체)만 `DIM_CAMPAIGN` **실시간 조인**으로 남아 있다
> (`DEC-43` §29-B 12속성 목록에 `CAMPAIGN_NAME` 자체는 포함되지 않았다 — 의도적 범위 밖).
> 캠페인명이 나중에 개칭되면 이 SV 에서 「이름은 최신, 분류(UTM·브랜드·유형 등)는 과거」인
> 상태가 될 수 있다.
> 🔴 **[2026-08-26 O102 정정] 초판의 *"현재는 SV COMMENT 에 이 사실만 명시해 두었다"* 는 거짓이었다** —
> 라이브 `SV_MEMBER_COHORT` COMMENT 와 정본 파일 SV-level COMMENT 양쪽에 동결·시점혼합 언급이
> **전무**하고(동결 사실은 `--` SQL 주석에만 있어 라이브에 실리지 않는다) `CAMPAIGN_NAME` 컬럼
> COMMENT 에도 예외 표기가 없었다. 이웃 8속성만 *"적재 시점 동결값"* 을 달고 있어 **오히려 전부
> 동결로 오독**되는 상태였다 ⇒ 🟢 정본 파일 컬럼 COMMENT 보강 · 🔴 **라이브 재배포는 승인 대기**
> (경위 = `99_NEXT §0-JJJ ▣NN ①`).

**여쭙는 것**

1. `CAMPAIGN_NAME` 도 획득 시점 동결로 확장해야 합니까, 지금처럼 실시간으로 두어야 합니까?
2. "예"라면 `FACT_MEMBER_COHORT.ACQ_CAMPAIGN_NAME` 신설(패턴은 이미 있는 `ACQ_BRAND` 등과
   동일) → `DIM_MEMBER_ACQUISITION`(실측 결과 `ACQ_CAMPAIGN_NAME` **이미 존재** — 값이
   동결원인지 실시간 조인인지 재확인) → SV `SV_MEMBER_COHORT` 재배선 순서로 진행하면 됩니까?

**판정**: ____________

---
_Co-authored with CoCo_
