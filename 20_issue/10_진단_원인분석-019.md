<!-- SPLIT-CHUNK 10_진단_원인분석.md | 019/020 | 허브 = 10_진단_원인분석.md | 원문 3123~3242행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## §25. 2026-08-05 세션 자기검토 — 내가 틀린 5건과 그것을 잡은 것 [2026-08-05]

> 이 절은 성과 요약이 아니다. **내가 어디서 틀렸고 무엇이 그것을 잡았는지**를 남긴다.
> 이번 세션 산출물(O38-E·O39·O40·O40-B)의 신뢰도는 이 목록을 함께 읽어야 판단할 수 있다.

### 25-A. 오판 5건 — 전부 「이름·주석·근사치」를 근거로 삼았다

| # | 내가 한 판단 | 실측 결과 | 만약 그대로 갔다면 |
|---|---|---|---|
| 1 | 「일별 실적 뷰를 신설해야 한다」 | `WIDE_MEMBER_EVENT` 가 **이미** 일×부서×브랜드×개발구분·개발건/명을 갖고 있었고, `WIDE_DEV_ACHIEVEMENT.sql` 주석이 **그 분리를 설계로 명시**해 뒀다 | 같은 데이터를 두 곳에서 유지 = 중복 스캐폴드 |
| 2 | 「SV 에 `SUM(DEV_MEMBERS)` 가산 metric 이 없다 = 공백」 | `DEV_MEMBERS` 는 **0/1 플래그**이고 SUM 은 개발(건) | 「개발(명)」에 **2,291,878**(실제 1,585,923 · **44.5% 과대**) 을 답하는 metric 신설 |
| 3 | 「동일 패턴 **일괄** 교정」 | `%MEMBERS%` 43컬럼 전수 시험 → **패턴 불균일**. FMM·COHORT 는 **옳다** | **옳은 컬럼에 「틀렸다」 경고**를 실어 정상 지표를 불신하게 만듦 |
| 4 | 「`ONETIME_ONETIME_FEE` = 기부금」(40,000원 근사 일치 근거) | 그 컬럼은 **`MEMBER_TYPE='ONCE'`** 기준이고 기부금 테이블 기준이 아니다. 정확한 판별자는 **`PAYMENT_TYPE`** | 마스터부재 314행이 새는 근사 정의를 정본으로 못박음 |
| 5 | 「ALTER 순서를 문서에 적었으니 됐다」 | §1 ALTER 를 **실행하지 않고** 넘겼다 → 사용자 build 가 `ERROR=0` 인데 컬럼이 안 생김 | build 1회 낭비 + 「성공했다」는 오인 |

🔴 **공통 원인은 하나다** — 컬럼명·COMMENT·합계 근사치를 **증거로 취급**했다. 이 프로젝트가 P24-B
(「전건 NULL 컬럼의 매핑은 미검증 매핑」)로 이미 적어둔 실패를 형태만 바꿔 반복했다.

🟢 **무엇이 잡았나**: 5건 전부 **사용자가 「추론이 아닌 실제 데이터로 비판적으로 검토」를 명시적으로
요구**해서 잡혔다. 자율 실행만 했다면 2·3·4 는 **정본 문서에 박혀** 다음 세션이 근거로 인용했을 것이다.
→ 🆕 **P83**: 결함을 「찾았다」고 판단한 직후가 가장 위험하다. **교정안을 만든 뒤 반드시 반대 방향
(내 교정이 옳은 것을 망치지 않는가 · 내 근거가 구조인가 근사치인가)을 같은 강도로 시험**한다.
근사 일치(합계가 비슷하다)는 **증거가 아니다** — 컬럼 존재·행수 정확 일치·전 구간 불변식만 증거다.

### 25-B. 잘 작동한 절차 3가지 (유지할 것)

1. **저작 전 시뮬레이션**(P70): O40 신규 컬럼을 모델에 넣기 전에 `MONTH_KEY` 로직을 SILVER 에 재현해
   기존 `PAID_FEE`·`BILLED_AMT` 를 **GOLD 실측과 완전 일치**로 재현 → 같은 쿼리의 신규 값도 신뢰 가능.
   build 후 실적재값이 기대값과 **정확히 일치**해 이 절차의 유효성이 입증됐다.
2. **교정 검증에 「제외 대상 미훼손」 관문을 넣기**(O39 §4-B): 대상 6컬럼 반영 확인과 **별개로**
   제외 3컬럼이 오염되지 않았는지 확인 → 오판 3을 되돌릴 수 있게 만든 안전장치.
3. **적용 전 영향범위 전수 대조**(O40-B): `dbt compile` 산출물로 13개 팩트의 **모델 컬럼 vs 테이블 컬럼**
   을 전량 대조해 드리프트 0 을 확인한 뒤 `on_schema_change: fail` 을 적용 → 설정 변경이 파이프라인을
   깨지 않음을 사전에 보장.

### 25-C. 이번 세션이 남긴 미결 (다음 세션 판단 필요)

| ID | 내용 | 성격 |
|---|---|---|
| **O39-B** | 전건 0 컬럼군 — `FMM` `ACTIVE_CNT`·`ACTIVE_MEMBERS`·`INCREASE_*`·`ACTIVE_CUM_*` **6종 40,054,883행 전건 0**(NULL 아님) · `FME` `UNPAID_STOP_*` · `FSE` `SUCCESS/FAIL/OPEN_MEMBERS` 등. ⚠️`ACTIVE_CNT` 는 활동회원 지표의 **분모** 축 → 영향범위 조사 선결 · 기존 C-9-R·B1 과 중복 가능 → **대조 후 병합** | 🔴 조사 필요 |
| **O39-C** | `*_MEMBERS` → `*_FLAG` 개명 여부. §22-H 교훈은 "이름이 오해를 부르면 개명"이지만 WIDE·SV·문서·저장쿼리를 깬다 | 🟠 결정 필요 |
| **O38-D** | 개발목표가 신규에만 편성되는 것이 방침인가 · `GOAL_CNT=0` 이 「미편성」인가 「0건 목표」인가 | 🟠 현업 |
| **브랜드2** | 「매체명(브랜드2)」가 CRM `BRAND`(채널 대분류)인가 AGENCY `MEDIA_NAME`(매체사)인가 — 인벤토리 §1·§3 이 서로 다르게 적었다 | 🟠 현업 |
| **NL 스모크** | #8~14 미검증(트라이얼 계정 차단 → 사람이 UI 확인). #13(부서 달성율 소표본)·#14(발송 회원수)가 이번 세션 신설분 | 🔴 사람만 가능 |

### 25-D. 선행 문서 정정 2건 (P62-B)

① 이슈원장의 *"장표 13항목 재판정 — 온전히 답하는 것은 1개(예산구분)뿐이다"* 는 **자기모순**이었다
(바로 다음 줄이 "예산구분 × 부서 교차 불가"). 정정: **예산구분이 유일한 불가 항목**이고 9개는 즉시 가능.
② 같은 기록이 매체 축을 **"도달 불가(P52)"** 로 판정했는데, 그것은 `DIM_AD_CREATIVE.MEDIA_NAME`(AGENCY)
경로에 한정된 판정이었다. **CRM `DIM_CAMPAIGN.BRAND` 경로가 별도로 있고 실적측 커버리지 100%** 다.
→ 정본은 `03_top-down_gold/05_필드 인벤토리.md` 로 이관.

---

## §26. O59-M — `06_DDL.sql` 테이블 COMMENT 실측치 이관 대장 [2026-08-11]

> **왜 이 절이 있나**: 작업규칙 7(COMMENT 에 실측 수치 금지 · P58)에 따라 `03_top-down_gold/06_DDL.sql` 의
> 테이블 COMMENT 에서 실측 수치를 제거했다. 🔴 **지우기 전에 원문을 여기 verbatim 으로 옮긴다** — COMMENT 가
> 그 수치의 **유일 사본**인 경우가 실제로 있었다(`97.76%`·`19.73%` 는 정본 문서에 없고 `_archive` SQL 에만 있었다 · P107).
> ⇒ 교정된 COMMENT 는 「규모는 문서10 §26 참조」로 이 절을 가리킨다.
>
> ⚠️ **여기 적힌 수치는 이관 시점(2026-08-11)의 스냅샷이다** — 재적재로 변한다. 판정에 쓰려면 재실측할 것(P169).
> 게이트 = `python3 scripts/audit_ddl_rule7.py --detail`(선언=파싱 커버리지 자기판정 포함).

| # | 테이블.컬럼 | 제거한 수치 | 이관 시점 COMMENT 원문(verbatim) |
|---:|---|---|---|
| 1 | `DIM_ORG.CORP` (L115) | 천단위 `23,130` · 백분율 `98.6%` | 법인(#114) — 🔴 부서 차원에서 산출 불가(CONF-4). 부서→법인 1:1 아님: 200부서에 복수법인 혼재(2종 69부서/23,130명 · 3종 131부서/1,544,005명=98.6%). 부서트리 LVL1도 부적합(ZA 구조노드 587부서가 법인 루트 미연결 + 직위 B000007 + 회원0 재단법인 2개 혼재). 법인 축의 정본 원천 = 회원 속성 CPR_DIV_CD(CM019: I=사단/S=사복/A=통합) → DIM_MEMBER 또는 팩트 degen 배속 판단 필요. 값 NULL 유지 |
| 2 | `DIM_ORG.DIVISION` (L116) | 백분율 `91.9%` | 실적지부 — 정본 용어사전 430(실적 지부 명)·431(실적지부(본부/지부) 구분). 🔴 재정의(CONF-4): 정본 보고서는 "본부/지부" 단독 사용 0건이고 "실적지부(본부/지부)"(05:275)·"실적지부"(05:311) 형태로만 쓴다 → 조직트리(UPPER_DEPT_ID)가 아니라 실적트리(ACMSLT_UPPER_DEPT_ID) 기반. ⚠️ 산출 규칙 미확정 — 실적부서 455개 중 명칭기반 최근접 본부/지부 도달 418개(91.9%)·미도달 37개이고 명칭 판정은 범주오류 위험 → 규칙 확정까지 값 NULL 유지 |
| 3 | `DIM_ORG.ACMSLT_UPPER_DEPT_ID` (L120) | 백분율 `98.0%` · 소규모 `441건` | 실적상위부서ID (원천 그대로) — 실적트리 부모. 조직트리 UPPER_DEPT_ID 와 441건 상이·573건 동일·NULL 300. 실적부서 455개 중 446개(98.0%)가 이 트리 LVL5 → DEC-5 "5th=실적부서" 근거 |
| 4 | `DIM_ORG.USE_YN` (L122) | 천단위 `410,506` · 백분율 `58.1%` | 사용여부 Y/N (원천 그대로) — 🔴 N이 764개(58.1%)지만 제외 금지(O16): 팩트가 대량 참조(CRM_MEMBER.ACT_DEPT_CD 미사용 156부서에 410,506명 · AMT_CHANGE 미사용 189부서에 100,931건) + 필터 시 트리 파편화(LVL1 9→42). 소비 측 필터용으로만 사용 |
| 5 | `DIM_MEMBER.REGION` (L147) | 천단위 `7,681,020` · 백분율 `96.91%` | 지역 (#131) — CM018 약칭 라벨(서울/경기/인천/강원…). 코드=AREA_CD. [O27] 시점귀속(as-of): 그 버전 EFFECTIVE_FROM 이하 최근 개발약정의 값. 적중 96.91%(7,681,020/7,925,716). ⚠️ONCE(일시회원) 175,722행은 개발약정에 행이 없어 NULL — ''(해당없음)''이 아니다(개념은 있고 원천이 없다). sentinel AREA_CD=''0'' 도 라벨 NULL |
| 6 | `DIM_MEMBER.AGE` (L148) | 천단위 `21,920` | 연령대 코드 raw — CM014(1~12). 🔴**연속형 나이가 아니다**: 1=10대미만·2=10대·3=20대·4=30대·5=40대·6=50대·7=60대·8=70대·9=70대이상·10=단체·11=기업·12=기타. 라벨=AGE_BAND. 판정근거: CM014 12종 × 실적재 12종 = 12/12 일치 · 독립 교차검증 AGE=''10''(단체) 21,920행 전건 SEX=''6''(단체) · AGE=''11''(기업) 64,581행 전건 SEX=''7''(기업). ⚠️BRONZE TM_MM_FDRM_MBER_DVLP_AMT.AGE COMMENT ''연령''(NUMBER)은 오류다 |
| 7 | `DIM_MEMBER.AGE_BAND` (L149) | 백분율 `97.63%` | 연령대 — CM014 라벨. 코드=AGE. [O27] 시점귀속(as-of) · 적중 97.63%. 🔴구간을 우리가 만든 것이 아니라 **원천이 이미 구간화**해 제공한다(DEC-28 §18-B 로 DEC-27 §17-C ''구간 정의 없음→보류'' 판정을 정정). ⚠️생년월일(MBER_BIRTHDAY) 입고는 이 컬럼의 선행조건이 아니다 — 시점정확 연령에만 필요. ⚠️ONCE 는 NULL |
| 8 | `DIM_MEMBER.PREV_MBER_STAT_CD` (L155) | 천단위 `7,501,761` | 상태전이 **이전상태** 코드 raw — MM010. 현재상태=MBER_STAT_CD 와 짝지어 전이를 표현한다(이 SCD2 버전행이 곧 전이 사건이므로 fan-out 0). 원천=CRM_MEMBER_STATUS_HIST.BF_STAT_CD(채움 100%·7,501,761·12종 = MM010 12/12 일치). ⚠️이력 미보유행(FDRM 무이력·ONCE 전체)은 NULL — ''이전상태가 없다''가 아니라 ''이력이 없다''. ⚠️동일자 다중전이는 최종 전이로 축약된다(중간 단계 소실) |
| 9 | `DIM_MEMBER.FIRST_SPONSORSHIP` (L161) | 백분율 `97.76%` | 최초 후원사업 — CRM_MEMBER_DEV 최소 발생일(OCCRRNC_DE)의 SPNSR_BSNS_ID. ''최초''는 시점 불변이라 as-of 불요(SCD1). 적중 97.76%. ⚠️ONCE 는 개발약정 부재로 NULL. ⚠️**현재 후원사업**은 제공하지 않는다 — 동시 다중후원이 정상(14.2%·최대 14)이라 단일값이 성립하지 않아 CURRENT_SPONSORSHIP 을 DROP 했다(O13 계열) |
| 10 | `DIM_MEMBER.LAST_STOP_DATE` (L162) | 천단위 `1,563,872` · 백분율 `19.73%` | 최종 중단일 — 원천 CRM_MEMBER_DISCONTINUE.SPNSR_DSCNTC_DE. 🔴**그 버전 시점까지의 as-of max** 다(단순 max 아님). 단순 max 는 미래 정보를 과거 버전에 누설해 예측 피처(LTV·유지기간 신4·6~8)를 오염시킨다. 적중 19.73%(1,563,872/7,925,716) — 중단 이력이 없는 회원·중단 이전 버전은 NULL |
| 11 | `DIM_MEMBER.MEMBER_TYPE` (L172) | 천단위 `1,587,343` | 회원 등록계통 구분 — SILVER CRM_MEMBER.MEMBER_TYPE 전파: FDRM=정기회원(TM_MM_FDRM_MBER_INFO 1,587,343명) / ONCE=일시회원(TM_MM_ONCE_MBER_INFO 175,722명). 🔴 일시회원은 회원상태(MM010)·가입경로(MM014) 개념이 원천에 없다 — 상태 기반 분포·이탈률·예측 모집단은 FDRM 으로 한정할 것. ⚠️ MEMBER_TYPE_NAME(개인/기업/단체, MM018)은 이 컬럼의 라벨이 아니다 — 완전히 다른 축이며 코드는 MBER_DIV_CD 다 |
| 12 | `DIM_MEMBER_ACQUISITION.FIRST_STOP_REASON_NM` (L239) | 소규모 `366행` | 최초 중단의 사유명. 코드그룹 **MM005(후원중단사유)**. 미중단 회원은 NULL. 코드사전에는 **폐지코드(USE_YN=''N'')가 다수 섞여** 있고 실적재는 사전의 일부만 쓴다. 🔴🔴**USE_YN 필터 금지** — 실적재 20종 중 6종(366행)이 폐지코드이며 필터를 걸면 그 라벨이 사라진다. |
| 13 | `DIM_MEMBER_IDENTITY.MEMNUM` (L256) | 천단위 `17,795` | memnum(#111) — 🔴 전건 NULL(미배선). 원천 실재 = SILVER.GA4_EVENT.PAGE_LOCATION 의 memnum= (17,795행·1,589종). 조회 시 항상 0행 |
| 14 | `DIM_MEMBER_IDENTITY.CHILD_CODE` (L259) | 천단위 `6,827` | 결연아동코드(#122, URL 파싱) — 🔴 전건 NULL(미배선). 원천 실재 = SILVER.GA4_EVENT.PAGE_LOCATION 의 childnum= (6,827행·594종). 조회 시 항상 0행 |
| 15 | `DIM_CAMPAIGN.MKTG_CAMPAIGN_SK` (L305) | 천단위 `218,402` · 배수 `181.6배` | [O45] 마케팅캠페인 대리키 (FK→DIM_MARKETING_CAMPAIGN). 광고(AGENCY)와 개발실적(CRM)을 잇는 conformed 축. 0=미매핑. 🔴개발캠페인 grain 으로 광고비를 내리면 181.6배 팬아웃(218,402행 → 39,669,103행 · 마케팅캠페인당 개발캠페인 평균 120.4·최대 901, 광고 도달 81개 한정) — 결합은 마케팅캠페인 grain 에서만 할 것 |
| 16 | `DIM_MARKETING_CAMPAIGN.MKTG_CAMPAIGN_NAME` (L320) | 천단위 `218,402` · 백분율 `89.7%` | 마케팅캠페인명. 🔴광고측(AGENCY CAMPAIGN_NM)과의 **조인 키**다 — 이름매칭이 유일 경로다(AGENCY 원천 3종에 캠페인 코드 컬럼 0개). 실측 광고 도달 218,402/243,545 = 89.7% · 미도달 10.3% 는 SK=0 으로 간다 |
| 17 | `DIM_MARKETING_CAMPAIGN.DEV_CAMPAIGN_CNT` (L322) | 천단위 `13,176` · 배수 `181.6배` | 🔴**팬아웃 경고축**: 이 마케팅캠페인에 매달린 개발캠페인 수. 1 보다 크면 개발캠페인 단위로 광고비를 내릴 때 그 배수만큼 복제된다. 실측 2026-08-06 — 이 컬럼의 모집단 = 마스터 전체 323개: 평균 105.0 · 최대 13,176 · 합 33,915 / 광고 도달분 81개 한정: 평균 120.4 · 최대 901 · 합 9,750 / naive 조인 218,402행 → 39,669,103행 = **181.6배**. 결합은 마케팅캠페인 grain 에서만 할 것 |
| 18 | `DIM_GA_SOURCE.DEFAULT_CHANNEL_GROUP` (L379) | 천단위 `2,167` · 백분율 `93.8%` | [DEC-30] GA4 표준 채널그룹 ← GA4_TRAFFIC_SOURCE.DEFAULT_CHANNEL_GROUP. 채움 100%(2,167)·14종 · grain 에 93.8% 함수종속(다중 9/146·최대 4)이라 MAX() 대표값. ⚠️SOURCE_MEDIUM(파생 문자열)과 다른 개념 — GA4 가 산정한 표준 분류다 |
| 19 | `DIM_REASON.REASON_TYPE` (L468) | 천단위 `5,839` | 사유 코드그룹 ID. ⚠️주석상 "중단/미납 구분"이었으나 실제값은 CRM 코드그룹 ID 336종(PM019·MS049·PM018·PM002·PM032·PM033 등) — ''중단''/''미납'' 리터럴 0건(전체 5,839행). 중단/미납 구분 필터가 필요하면 별도 분류 컬럼 신설 필요(O21, 2026-07-31 실측 교정) |
| 20 | `DIM_EVENT.RECRUIT_HEADCOUNT` (L506) | 천단위 `3,361` · 백분율 `88.8%` · 배수 `101.0배` | [DEC-30] 모집인원 ← CRM_EVENT.RCRIT_PSNNL_CO. 채움 3,361/3,786=88.8%·74종. 🔴행사 속성이므로 참여 팩트가 아니라 행사 차원이 정본 — 참여행 반복 시 SUM 이 101.0배 과대계상(행사 참값 4,513,184 vs 456,007,553). 행사 단위로만 합산 |
| 21 | `FACT_MEMBER_MONTHLY.REASON_SK` (L537) | 천단위 `3,302,535` · 백분율 `95.83%` | 미납 대표사유 (FK→DIM_REASON) — ✅ W3(DEC-24, 2026-07-31) 배선 완료. 🔴 미납(PAY_STAT_CD=''F'') 행 한정 — 최종차수(MBRFEE_SQNC 최대)의 RQEST_RST_CD를 (코드그룹, 코드) 복합키로 DIM_REASON 조인. 코드그룹 = SETLE_CD 1&2자리→PM002 / 1&4자리→PM032 / 2→PM018 / 12→PM033 / 5→PM019. 실측 미납 월×회원 3,302,535 중 3,164,724(95.83%) 매핑 · 0=비미납 또는 구조적 사유부재 137,811(수기처리 127,155 + F코드부재 10,623 + DIM미존재 33). ⚠️ 대표 1개로 축약 — 복수사유 분포는 SILVER 직접 조회. ⚠️ 중단사유는 별도 트랙(FME) |
| 22 | `FACT_MEMBER_MONTHLY.UNPAID_BILLED_AMT` (L563) | 배수 `2.37배` | 미납 청구액(원) — 정본 **DEC-3** 정의(O40): `PAY_STAT_CD IN (''F'', NULL)` 인 행의 **RQEST_AMT** 합. 🔴차감식(BILLED−PAID)을 쓰지 말 것 — 기부금이 미납을 상쇄해 2.37배 과소해진다(2025 실측 123억 vs 정본 293억). |
| 23 | `FACT_MEMBER_EVENT.DVLP_DIV_NM` (L608) | 천단위 `1,010,680` · 백분율 `99.99%` | 개발구분명 — MM015 라벨(신규/증액/감액/재후원/후원중단). ⚠️ 값 ''후원중단''(1,010,680건)은 EVENT_TYPE=''STOP''(1,038,262건)과 동일 사건이 중복 존재(동일 회원·일자 99.99%) → 두 축 합산 금지, O24 현업확인 대기 |
| 24 | `FACT_MEMBER_EVENT.DEV_CNT` (L610) | 천단위 `3,594,843` · 백분율 `56.86%` | 개발(건) (#149) — 정본 공#121 개발구분 = 신규(1)·증액(2)·재후원(4) 한정. ⚠️ 2026-08-03 O24 교정: 종전은 감액·후원중단까지 포함해 56.86% 과대계상(3,594,843 → 2,291,878) |
| 25 | `FACT_MEMBER_EVENT.DEV_MEMBERS` (L611) | 천단위 `2,291,878` · 백분율 `44.5%` | 🔴「명」이 아니다 — 개발 사건 플래그(0/1). SUM 은 개발(건)이며 실측 2,291,878 로 실제 고유회원 1,585,923 대비 44.5% 과대다(O39). 개발(명)(#148)은 COUNT(DISTINCT MEMBER_DK). 월 단위는 FACT_MEMBER_MONTHLY.DEV_MEMBERS 사용. |
| 26 | `FACT_MEMBER_EVENT.STOP_MEMBERS` (L613) | 천단위 `1,038,262` · 백분율 `15.0%` | 🔴「명」이 아니다 — 중단 사건 플래그(0/1). SUM 은 중단(건)이며 실측 1,038,262 로 실제 고유회원 903,064 대비 15.0% 과대다(O39). 중단(명)은 COUNT(DISTINCT MEMBER_DK). |
| 27 | `FACT_MEMBER_EVENT.STOP_REASON_NM` (L623) | 소규모 `366행` | 중단사유명 — 정본 공#162. MM005 라벨(SILVER CRM_MEMBER_DISCONTINUE.DSCNTC_RSN_NM 전파). 코드는 STOP_REASON. ⚠️USE_YN 무필터 조인 — 실측 20종 중 6종(366행)이 폐지코드이며 필터를 걸면 라벨이 사라진다. 개발원천 행은 개념 부재로 NULL (O25) |
| 28 | `FACT_SERVICE_EVENT.SEND_MEMBERS` (L698) | 천단위 `38,470,780` · 배수 `37.3배` | 🔴「명」이 아니다 — 발송 플래그(전 행 1). SUM 은 행수=발송 건수이며 실측 38,470,780 로 실제 고유회원 1,031,971 대비 37.3배 과대다(O39). 발송(명)(#85)은 COUNT(DISTINCT MEMBER_DK). |
| 29 | `FACT_SERVICE_EVENT.SEND_TYPE_SK` (L723) | 천단위 `8,300,272` · 백분율 `21.58%` | [DEC-30] 발송구분 (FK→DIM_SEND_TYPE). 🟢커버리지 실측 21.58%(8,300,272/38,470,780) — 미매칭은 센티넬 0. ⚠️DEC-28 이 인용한 0.106% 는 요청 grain 분모였다(P39) |
| 30 | `FACT_GA_BEHAVIOR.CAMPAIGN_SK` (L740) | 백분율 `88.0%` | 세션캠페인(#102) — 🔴 상수 0 하드코딩(센티넬). GA UTM 캠페인 244종이 하나로 뭉개져 있다(P51 위반). SILVER.GA4_EVENT.UTM_CAMPAIGN(채움 88.0%) 미배선 → 캠페인축 분석 불가. WIDE 의 CAMPAIGN_BK/NAME/BRAND 도 전건 (미매핑) |
| 31 | `FACT_GA_BEHAVIOR.PAGE_LOCATION` (L742) | 천단위 `75,474` · 백분율 `88.9%` | 페이지위치(#106) — 🔴 grain 내 MAX() 대표값(URL 전체 아님). 원천 distinct 75,474종 → GOLD 8,408종만 생존(67,066종·88.9% 소실). childnum 594→352종 · memnum 1,589→680종. 특정 URL 유무 판정 금지 |
| 32 | `FACT_GA_BEHAVIOR.SESSION_CNT` (L746) | 천단위 `243,156` · 배수 `3.15배` | 세션수(명) (#97) — 🔴**비가산**. COUNT(DISTINCT user\|\|session) 인데 집계 grain 이라 같은 세션이 여러 행에 반복된다. 실측 SUM=243,156 vs 실제 distinct 77,172 = **3.15배 과대계상** → SUM 금지. 가산 대체 = VISITS |
| 33 | `FACT_GA_BEHAVIOR.ENGAGED_SESSIONS` (L747) | 천단위 `161,117` · 배수 `4.30배` | 참여세션수 — 🔴**비가산**. COUNT(DISTINCT) + 집계 grain. 실측 SUM=161,117 vs 실제 37,505 = **4.30배 과대계상** → SUM 금지 |
| 34 | `FACT_AD_PERFORMANCE.GA_CONV_MEMBERS` (L784) | 백분율 `28.60%` | GA전환수(명) — **DIGITAL 전용**. ⚠️O16 교정 2026-07-28: 종전 REBRDC 개발회원수가 혼입돼 합계의 28.60%를 차지했음(재방송 개발실적은 FACT_AD_BROADCAST.DVLP_MEMBER_CNT 로 이관) |
| 35 | `FACT_AD_PERFORMANCE.GA_CONV_CNT` (L785) | 백분율 `60.32%` | GA전환수(건/VU) — **DIGITAL 전용**. ⚠️O16 교정 2026-07-28: 종전 REBRDC 개발건수가 혼입돼 합계의 60.32%(과반)를 차지했음 → FACT_AD_BROADCAST.DVLP_CNT 로 이관. 합계 소수=비건수, 어의 현업확인 잔여(O5) |
| 36 | `FACT_AD_PERFORMANCE.MKTG_CAMPAIGN_SK` (L797) | 천단위 `218,402` · 백분율 `89.7%` · 배수 `181.6배` | [O45] 마케팅캠페인 대리키 (FK→DIM_MARKETING_CAMPAIGN). 광고↔CRM 결합축. 도달 89.7%(218,402/243,545) · 미도달 10.3%는 0(미매핑) — 이 버킷을 「미집행」으로 읽지 말 것. 🔴개발캠페인(CAMPAIGN_SK) grain 결합 금지 — 181.6배 팬아웃(218,402행 → 39,669,103행, 실측 2026-08-06) |
| 37 | `FACT_AD_BROADCAST.DURATION_SEC` (L819) | 천단위 `30,000,000` · 백분율 `96.6%` | 🔴 광고 초수 ← VIDEO.AD_SEC(TEXT→TRY_TO_NUMBER) [VIDEO 전용] — **현재 값 신뢰 금지(O29)**. 적재값 30,000,000/60,000,000/90,000,000 이라 "초"로 읽으면 오답(µs 해석 유력하나 미확정·현업 확인 대기). 원천 HH:MM:SS 32,739행(96.6%)이 캐스팅에서 무성 소실 → 유효 커버리지 1,151/36,416=3.2%(파싱 시 93.1% 회복). REBRDC NULL 은 결손 아니라 원천 부재 |
| 38 | `FACT_AD_BROADCAST.DVLP_MEMBER_CNT` (L826) | 천단위 `49,093` · 소규모 `2행` | 개발회원수 ← REBRDC.DVLP_MBER_CNT [REBRDC 전용]. ⚠️O16 이관: 종전 코어 GA_CONV_MEMBERS 로 혼입(GA 전환이 아니라 재방송 개발실적). ⚠️소수 척도 유지 이유: 원천에 0.5 단위 값 실존(2행: 12.5·18.5) → NUMBER(38,0) 은 반올림으로 총합을 +1 왜곡(49,093→49,094). 원천값 보존 우선 |
| 39 | `FACT_EVENT_PARTICIPATION.PART_STATUS` (L918) | 천단위 `707,476` · 백분율 `23.2%` | 🔴 참여상태 — 코드체계 2개 혼입(O28). 일반행사=MS304(110 Success·120 Fail·130~220 N_step_right/fail) 707,476행 / 캠페인행사=소정수 1~6 152,046행(의미 미확정·문서20 §I). 판별자=EVENT_KEY 접두(고아 23.2% 안전). 두 체계 합산·GROUP BY 금지 · 한글 비교는 0행 |
| 40 | `FACT_EVENT_PARTICIPATION.EVENT_BK` (L922) | 천단위 `263,611` · 백분율 `23.2%` | [DEC-30] degenerate key — 원천 행사키 ← CRM_EVENT_PARTICIPATION.EVENT_KEY(채움 100%). 🔴고아 행사 식별자 보존용: 마스터 부재 53개 행사·263,611행(23.2%)이 EVENT_SK=0 으로 뭉개져 서로 구별되지 않았다(SILVER distinct 3,715 vs GOLD EVENT_SK 3,663). 🔷(EVENT_BK,MEMBER_DK,PARTCPT_SEQ) 가 행 유일 식별 — EVENT_SK 로는 31,831키·63,783행 충돌. ⚠️접두(EVENT_/CRMN_)가 O28 코드체계 판별자 |
| 41 | `FACT_EVENT_PARTICIPATION.PARTCPT_SEQ` (L923) | 천단위 `802,298` · 소규모 `1행` | [DEC-30] degenerate key — 참여 일련번호 ← CRM_EVENT_PARTICIPATION.PARTCPT_SEQ(채움 100%). 🔷(EVENT_SK,MEMBER_DK,PARTCPT_SEQ) 가 행을 유일 식별 — (행사,회원)만으로는 802,298 ≠ 1,134,126 중복. ⚠️전역 순번 아님((행사,SEQ) 201,817) · 음수 20,844행·INT_MIN 1행 → 식별자 전용, 정렬·범위조건 금지 |
| 42 | `FACT_MEMBER_FEE.SPONSORSHIP_SK` (L1035) | 백분율 `99.83%` | 🔴**납입 대상** 후원사업 대리키 (FK→DIM_SPONSORSHIP). 원천 채움 99.83%. 획득 후원사업(FMC.ACQ_SPONSORSHIP_SK)과 **의미가 다르다** |
| 43 | `FACT_MEMBER_FEE.PAYMENT_SK` (L1036) | 백분율 `99.3%` | 결제수단 대리키 (FK→DIM_PAYMENT). ⚠️라벨 커버리지 99.3% — 원천 11종 중 5종은 코드그룹 미특정으로 0(미매핑). 원본은 SETLE_CD 참조(O45-B) |
| 44 | `FACT_MEMBER_FEE.SETLE_CD` (L1040) | 천단위 `225,855` | degen: 결제수단 원본 코드. 라벨 없는 5종(3·10·6·13·7 · 225,855행)을 잃지 않기 위해 보존 — 현업 코드그룹 확인 대상(O45-B) |
| 45 | `FACT_MEMBER_FEE.BILLED_AMT` (L1043) | 천단위 `891,959,790,888` | 청구액(원) = SUM(RQEST_AMT). FMM·SILVER 와 총계 일치 실측 = 891,959,790,888 (GATE-D) |
| 46 | `FACT_MEMBER_FEE.PAID_FEE` (L1044) | 천단위 `34,672,700` · 소규모 `5행` | 납입 총액(원) = 회비 + 기부금. 🔴납부율 분자로 쓰지 말 것(O40). ⚠️O45-C: FMM 대비 +34,672,700(불량 5행 포함 차이) |
| 47 | `FACT_MEMBER_FEE.PAID_FEE_BILLABLE` (L1045) | 천단위 `768,800,286,349` | 회비 납입액(원) — 납부율 분자 **정본**(O40). FMM 과 완전일치 실측 = 768,800,286,349 |
| 48 | `FACT_MEMBER_FEE.UNPAID_BILLED_AMT` (L1046) | 천단위 `122,621,758,323` | 미납 청구액(원) — DEC-3 정본 = PAY_STAT_CD IN (F, NULL) 인 청구액. 🔴차감식 아님. ⚠️조회 시점 스냅샷. FMM 과 완전일치 = 122,621,758,323 |

**대상 = 48컬럼 / 17테이블**(2026-08-11 O59-M 실측 · 종전 기재 「37건·15테이블」은 게이트 블록 경계 결함으로 인한 **오측**이었다 — `blocks()` 가 COMMENT 문안 속 `;` 에서 잘려 `DIM_ORG`·`DIM_MEMBER` 등 40컬럼이 검사되지 않았다).

---
