<!-- SPLIT-CHUNK 02_상태상세_대시보드_갱신형.md | 005/007 | 허브 = 02_상태상세_대시보드_갱신형.md | 원문 615~796행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
### 🆕 GA4 3개월 샘플링 전환 + `dbt compile` 최초 통과 + 결함 3건 시정

**[2026-08-19 O88]** 🔒 라벨 = `id_collision_gate` 「다음 O89」 ↔ **정의 최대 87** ⇒ **O88**
(`P222` 참조축 과대 **16연속** — 참조 88 의 출처는 O87 이 남긴 「다음 O88」 예고 문장 2건뿐이다).
🔴 **자기위반 = `R1-4-3` 선점 등재 미이행**(작업을 먼저 하고 이 행을 나중에 썼다 · O84 와 같은 결함).

착수 지시 = 사용자 2건 ㉠ 「여태까지 진행된 dbt 작업 확인 및 수정사항 검토·수정」
㉡ 「`50_handoff/07` 중 **bronze 적재 부분만 남기고 나머지 주석처리**」
+ 범위 결정 = **`bronze_bigquery` 가 너무 커서 2024-06 · 2025-06 · 2026-06 3개월만 샘플링** ·
**BigQuery 전체 수치 기재는 현행 유지**(강력한 수정 지시가 없는 한).

#### 🔴🔴 최대 발견 1 — 계정이 또 바뀌었다 (`P169` **4차**)

실측 `CURRENT_ACCOUNT()` = **`NX55103`** ↔ 문서·인수인계 전건 기재 **`UA93987`**(O85 판정).
계보 = `ls82944` → `os09358` → `DV07626` → `UA93987` → **`NX55103`**.
⇒ `R2-8-4` 가 예고한 대로 **「완료」 판정이 또 미성립**했다. 실측 실태:
· `BRONZE_BIGQUERY` = **`EVENTS` 테이블 없음** · 빈 `events_20260501`·`events_20260719` +
  `SYNC_ERR_INFO` **3개뿐** ⇒ 04번 DDL 까지만 집행된 상태.
· 🟢 **반면 SILVER 는 이미 선생성돼 있었다** — `BIGQUERY_REFINED_DATA` 포함 **40테이블 실재**(전건 0행)
  ⇒ 🟢 **인수인계 항목 2「`08` DDL 의 GA4 구간을 실행해야 한다」는 이미 해소된 상태였다**(사용자 실행).
· 스테이지 실측 = `events_20240601`~`0630`(2024-06 **30일 전량**) + `events_2025060x`(2025-06 **진행중**)
  ⇒ 사용자의 3개월 샘플 업로드가 **실제로 진행 중**이다 ⇒ `07` 0장 (4) 「2회차 경고」가 지금 적용된다.

🔴 **부수 stale 1건** = `08` DDL 헤더·O87 이력의 「SILVER **39**테이블」 ↔ 라이브 실측 **40**.
차이 = `CRM_MARKETING_CAMPAIGN` — 그 한 건만 `CREATE TABLE IF NOT EXISTS` 로 선언돼
`CREATE OR REPLACE TABLE` 기준 계수에서 빠졌다(파일 1085행). **실재하고 COMMENT 도 있다** ⇒
결함이 아니라 **계수 방식의 누락**이다. 🟠 그대로 두면 다음 세션이 또 39 를 인용한다.

#### 🔴🔴 최대 발견 2 — 범위 술어가 3곳에 하드코딩돼 있었다 (`R1-6-17`)

O87 이 만든 GA4 range 경로는 같은 술어를 **세 파일에 각각** 적었다:
`macros/ga4_range_purge.sql`(pre-hook DELETE) · `BIGQUERY_REFINED_DATA.sql`(src WHERE) ·
`GA4_EVENT.sql`(최종 WHERE). 🔴 **세 술어가 어긋나면 행이 남거나(중복) 사라지는데(누락)
그 어긋남을 잡는 게이트가 없었다.** 두 모델 파일은 스스로 *"pre-hook DELETE 범위와 동일해야
멱등이다"* 라고 적어 두었지만, 그 동일성은 **사람이 세 파일을 맞춰 고치는 것에만** 의존했다.
⇒ 🟢 **신설 `macros/ga4_range_predicate.sql`** 로 정의 지점을 하나로 만들었다
(같은 처방의 선례 = O87 자신의 `silver_purge` pre-hook 단일화).

#### 🔴🔴 최대 발견 3 — `LATERAL FLATTEN` 이 행을 조용히 버리고 있었다

`BIGQUERY_REFINED_DATA` 의 `flat` CTE = `FROM src e, LATERAL FLATTEN(input => e.event_params) p`.
🔴 **`LATERAL FLATTEN` 의 기본은 INNER 다** ⇒ `event_params` 가 NULL 이거나 빈 배열인 행은
전개 결과가 0행이므로 **그 이벤트 자체가 탈락**한다. **에러도 워닝도 없다.**
O87 은 이 CTE 를 *"종전 `GA4_EVENT` 와 동일 동작"* 으로 계승했고 **그 동작에 이 소실이 포함돼
있었다** ⇒ `GA4-SEQ-1`(「손실 0」 철회)과 **같은 축의 두 번째 손실원**이다.
⇒ 🟢 **`OUTER => TRUE` 로 시정**. params 가 있는 행에는 결과가 동일하고(전개 불변) 없는 행만
param 컬럼 전건 NULL 로 보존한다 ⇒ **엄격히 비손실 방향**이다.
⚠️ 그래서 이 모델 행수는 O87 기준선보다 **늘 수 있다**(줄지 않는다). 규모는 적재 후 실측 대상.

#### 🟢 `dbt compile` 최초 통과 — 인수인계 1번 항목 해소

O87 이 *"`profiles.yml` 이 Snowflake-native 규약상 `account`/`user` 를 빈 문자열로 두어 샌드박스
CLI 가 연결되지 않고, `~/.dbt/profiles.yml` 보강은 매 호출마다 재생성돼 지워진다"* 로 막혀
**컴파일 검증을 못 했다**. 🟢 **우회 확립 = `--profiles-dir` 로 프로젝트 밖 프로파일을 지정**한다.
· 실패 원인의 정확한 정체 = 샌드박스 생성 `/root/.dbt/profiles.yml` 에 **`database`·`schema` 키가 없다**
  (에러 = *"Credentials in profile 'default', target 'sandbox' invalid: 'database' is a required property"*).
  즉 「연결 불가」가 아니라 **필수 속성 누락**이었다 ⇒ O87 의 진단은 증상만 맞았다.
· 🔴 **경로 주의**: `/tmp` 는 세션 간 소멸한다(작업 중 실제로 사라져 재작성했다) ⇒ **`/root/dbtprof/`** 사용.
· 🟢 결과 = **`dbt compile` EXIT=0 · 89 models · 348 data tests · 41 sources · 550 macros**.
  컴파일 산출물 대조로 술어 전개 확인 = 3개월 OR 3구간 · `OUTER => TRUE` 반영.
· 🟢 **음성 테스트** = `--vars "{ga4_dt_ranges: []}"` ⇒ 의도한 문안으로 **컴파일 실패(EXIT=2)**
  ⇒ 빈 목록이 「전 범위」로 조용히 해석되지 않음을 실증(`R3` 게이트2).

#### 🟢 신설 게이트 2종 — `GA4-SEQ-1` 축에 검사가 하나도 없었다

🔴 **공백의 정체**: `08` DDL 이 두 테이블에 `PRIMARY KEY (…, EVENT_SEQ)` 를 선언하지만
**Snowflake 는 PK 를 강제하지 않는다**(informational). 그리고 `_ga4_schema.yml` 은 4키 각각의
`not_null` 만 검사했다 — **`not_null` 4개는 조합 유일성을 전혀 증명하지 않는다.**
⇒ PK 가 깨져도 build 가 초록으로 끝난다.
· 🟢 `tests/assert_ga4_pk_unique.sql`(**ERROR**) = 두 테이블 4키 조합 중복 검출.
  ERROR 근거 = PK 중복은 `FACT_GA_BEHAVIOR` **팬아웃**으로 직결된다(선례 = `D2` 중복 현재행 사고).
· 🟢 `tests/warn_ga4_base_row_reconciliation.sql`(**WARN** · `store_failures`) =
  BRONZE↔기반 행수 정합 = **`GA4-SEQ-1` 조치 후보 ①「접힌 양 확정」의 기계 구현**.
  접힌 양을 ㉠ `PARAMS_NULL_ROWS`(= O88 시정이 구한 행수) ㉡ `FOLDED_ROWS`(`GROUP BY` 접힘)
  **두 원인으로 분해**한다. 🔴 임계값을 두지 않는다 — 「접힘」은 의도된 동작이라 근거 없는 숫자가
  된다(`R2-3`) ⇒ 역할은 **판정이 아니라 노출**이고 추이를 `store_failures` 로 본다(`P207`).
· ⚠️ **둘 다 재실행 간 순번 안정성(`GA4-SEQ-1` 본체)은 잡지 못한다** — 두 run 대조가 필요하다.
  통과를 해소로 읽지 않도록 `_ga4_schema.yml` 머리말과 두 테스트 헤더에 **명시**했다.

#### 🟢 `50_handoff/07` 3판 — 적재 + 완료판정까지만 활성

사용자 선택 = **「적재 + 완료판정만 남김」**.
· 활성 = 1~6장 + **7장 (1)(2)**  /  주석 = 7장 (3)~(6) · 8장 · 10장 · 부록(**149줄**).
· 🔴 **7장 (2) 를 남긴 근거** = 이 문서 자신이 *"통제총계 대조가 **완료 판정의 유일한 근거**"* 라
  못박았고, 0장 (4) 가 *"업로드 진행 중 1회만 적재하면 **조용히 누락**되고 status=LOADED /
  errors=0 을 반환한다"* 고 경고한다 ⇒ **지금이 정확히 그 상황**이다(스테이지 업로드 진행중 실측).
· 🔴 **8장 주석의 대가를 명시**했다 — 8장은 0장 (3)ⓑ 정정에 따라 **위치 기반 적재의 유일한
  사전 방어선**이다(파일 포맷 옵션은 아무 것도 막지 않는다). 사후 확증으로 대체하되
  *"사후 확증은 「이미 적재한 뒤」 판정이므로 사전 방어가 아니다"* 를 그대로 남겼다.
· 🟢 **사용자 질의에 대한 답 = CALL 에 연월 하드코딩은 없다.** 6장 (3) `CALL(NULL,NULL,FALSE)` 는
  5장 (2) 가 **디렉터리 테이블에서 실제 존재하는 연월**을 읽으므로 스테이지에 3개월만 올리면
  그 3개월만 적재된다 ⇒ 전량 CALL 을 그대로 둔다.
  🔴 **유일한 예외를 찾아 교체** = 6장 (2) 스모크 `'202503'` → **`'202406'`**. 종전 값은 그 달이
  스테이지에 없어 `NO_FILES`(files=0 · errors=0)로 **조용히 통과**해 「스모크했다」는 착시를 만든다.
· 🔴🔴 **주석만으로 끝나지 않는 파생 결함 1건을 잡아 조문화**했다 — **통제총계도 3개월분만
  등재해야 한다.** 7장 (2) 는 `FULL OUTER JOIN` 이라 911건을 등재하고 3개월만 적재하면
  `MISSING_IN_C` 가 **약 880건** 쏟아지고 그 안의 진짜 `SHORT_LOAD` 를 사람이 못 찾는다
  ⇒ **「0건이어야 완료」 판정식이 구조적으로 성립 불가**가 된다(게이트 사문화).
  4장에 연월 필터(`SUBSTR(table_name,8,6) IN ('202406','202506','202606')`)를 명시했다.

#### 🔴 게이트 결과 · 인수받은 FAIL 1건

· 🟢 `line_len` **11파일 초과 0** · `index_row_gate` **행 534 · 유실 0 · 중복 0** ·
  `doc_line_length_gate` **판독 325파일 PASS** · `clause_order_gate` **88조문 역전 0** ·
  `doc_type_gate` 선언 0 · 초과 0 · 🟠 경고 1(`90` 꼬리 여유 3,047B).
· 🔴🔴 **`doc_heading_gate` FAIL 은 O87-B 가 남긴 것이다** — `90_해소완료_로그.md` §1-B 제목을
  「…해소로 파생된 신규 미결 3건 (…O87 · 신규)」 → 「…조치로 파생된 신규 미결 (…O87 · O87-B 실측)」
  으로 **바꾸고 골든을 발행하지 않았다**(유실 1 + 신설 2). O88 은 그 파일의 제목을 만지지 않았다.
  🔴 **골든 자기승인 금지**(`R1-7-4` · `C2`)이므로 **사용자 승인 대기**로 둔다.
  ⇒ 🟢 이것이 O83-H 가 격상한 **「분모 편입과 기준선 발행은 별개」** 축이 실제로 작동한 사례다.

#### 🔴 미실행·미결로 남긴 것

· **`dbt run`·`dbt build` 미실행**(`R4-1` 정지점 · 사용자 승인 대기). 라이브 미적용.
· **pre-hook `DELETE` 렌더링은 `dbt run` 시점에만 확정된다** — `compile` 은 hook 을 내지 않는다.
  술어 매크로 자체는 실증됐고 `ga4_range_purge` 는 그 매크로만 호출하므로 잔여 위험은
  `is_incremental()` 분기(O87 작성분)뿐이다.
· 🟠 **SILVER 에 `on_schema_change` 미설정** — GOLD fact 에만 `append_new_columns` 가 있다(O42 처방).
  SILVER 는 기본값 `ignore` 라 **모델에만 있는 신규 컬럼이 무증상 폐기**된다(O40 유형).
  🟢 GA4 6테이블은 **모델↔DDL 컬럼 집합 대조 결과 일치**(기반 51 / `GA4_EVENT` 44)라 현재는 무해하다.
  🔴 **SILVER 전역 설정 변경은 34테이블에 영향**하므로 실측 없이 넣지 않았다(`R2-3`) ⇒ **미결**.
· 🟠 「SILVER→SILVER 금지」 잔존 문서 4종(O87 미갱신분)은 이번에도 손대지 않았다.

---

### 🆕 `07` 4판 — 「문서 전체 실행 = 자동 통과」 + 완료 게이트 대체

**[2026-08-19 O88-B]** 사용자 지시 = *"권장순서에서 지시하는 걸 **문서 전체를 실행했을 때 건너뛸 건
자동으로 건너뛰도록** 주석처리해줘. 그러면 내가 해야할 일이 **업로드 대기 → 07번 문서 전체 실행 →
DBT BUILD** 순으로 진행될 수 있도록"*.

#### 🔴🔴 O88(3판)이 낸 지시가 틀렸다 — 실측으로 잡았다

3판은 4장에 *"통제총계도 3개월분만 등재하라"* 를 조문화했다. 🔴 **그 지시는 성립하지 않는다** —
실측 = 현 계정 `NX55103` 의 `GN_DW.BRONZE_BIGQUERY` 에 `events_YYYYMMDD` 가 **2개뿐이고 둘 다 0행**
⇒ **A 계정 원천 911테이블이 이 계정에 없다.** 즉 「3개월만 등재」가 아니라 **등재할 원천이 없다.**
🔴 **원인 = 3판이 「4장을 실행할 수 있는가」를 확인하지 않고 「어떻게 실행할까」로 건너뛰었다.**
`R2-3` 을 통과한 것처럼 보였지만 확인한 것은 **7장 (2) 의 판정식 성질**이었고 **4장의 실행 가능성**은
재지 않았다 ⇒ 같은 축의 두 지점 중 하나만 실측한 결함이다.

#### 🔴 4판에서 끈 것 2개 (실행 불가) · 🟢 켠 것 1개 (대체 게이트)

· **끔 = 4장 A-3**(등재할 원천이 없어 항상 0 / NULL) · **7장 (2)**(기준선이 비면 전 폴더가
  `NOT_IN_CONTROL_TOTAL` 로 쏟아진다 = 판정 불가). 🟢 **삭제하지 않았다** — A 계정 접근이 생기면
  되살려 (2-B) 와 **함께** 쓴다(둘은 배타가 아니다).
· **켬 = 7장 (2-B) 스테이지 직접 대조** — 기준선을 **A 원천 → 스테이지 자신**으로 바꾼다(스테이지는
  적재의 실제 입력이고 지금 여기 있다). 폴더명 `FULL OUTER JOIN` 으로 **폴더 수 · 폴더별 파일 수**를
  대조하고 `MISSING_IN_C`/`EXTRA_IN_C`/`SHORT_LOAD`/`DUP_LOAD` 로 분기한다.
· 🟢 **지금의 실제 위험과 정확히 겹친다** — 0장 (4)의 사고는 *"업로드 진행 중 1회만 적재하면 조용히
  누락되고 status=LOADED / errors=0 을 반환한다"* = **파일 단위 누락**이다.
· 🔴 **한계를 문서에 명시했다: 파일 안의 행이 다 들어왔는지는 증명하지 못한다** ⇒ (2) 와 **동급으로
  읽으면 안 된다.** 보완 근거 2개를 함께 적었다: ① COPY `errors=0`(부분 적재는 엄격 PARSE_JSON 에서
  중단) ② **파일은 COPY 의 최소 원자 단위**다 ⇒ 「파일 전수 일치 + errors 0」이면 행 누락 경로는
  실질적으로 닫힌다.
· 🔴 **문자열이 아니라 건수로 비교**하도록 설계했다 — `DIRECTORY().RELATIVE_PATH`(스테이지 루트 기준)
  와 `METADATA$FILENAME`(=`SRC_FILE_NAME` · COPY `FROM` 경로 기준)은 **접두가 다를 수 있다** ⇒ 경로를
  직접 조인하면 전건 불일치로 오판한다. 폴더명으로 조인하고 파일은 세기만 한다.

#### 🟢 실증 · 선행조건 · 구조 무결성 (4판 머리말에도 등재)

· 🔴 신설 게이트 첫 실행이 *"DIRECTORY not enabled for the stage"* 로 실패했다 ⇒ **1장을 건너뛸 수
  없다**가 실측 확정됐다(5장 프로시저는 디렉터리 테이블로 연월을 찾으므로 **폴더를 0개로 본다**).
  ⇒ 1장의 `SET DIRECTORY = (ENABLE = TRUE)` + `REFRESH` 를 **직접 실행**해 켰다(멱등·비파괴 ·
  문서 1장과 동일 문장이라 사용자 실행분과 충돌하지 않는다).
· 🟢 `stg` CTE 실증 = **57폴더 / 348파일 · `events_20240601`~`20250627` · NULL 폴더 0** ⇒ 정규식·
  `RLIKE` 전체일치·`REGEXP_SUBSTR` 이 라이브 경로 형식에서 동작한다. ⚠️ 전체 게이트는 `EVENTS` 부재로
  컴파일되지 않는다 — **3장이 만든다**(순서상 정상). 🔴 **업로드는 계속 진행 중이다** — 같은 세션 안에서
  **313 → 348파일** · **2026-06 은 아직 0개**(`max_ym` = 202506) ⇒ ① 대기 판단이 여전히 유효하다.
· 🟢 선행조건 = `FF_CSV_LOAD` **존재**(SKIP_HEADER=1) ⇒ 6장 COPY 성립(4번째 막힘 없음) · `MIG_GA4_*`
  2종·`V_GA4_EVENTS_DATA_COLS`·`LOAD_GA4_EVENTS` **전부 부재** ⇒ 3장·3-B·3-C·5장을 전부 활성으로 뒀다 ·
  BRONZE_CRM 45테이블 **115,871,475행** · ERP 4,301 · AGENCY 243,550 **적재 완료** · SILVER 40 /
  GOLD 35 선생성 ⇒ **GA4 만 채우면 full `dbt build`(89 models · 348 tests)가 성립**한다.
· 🔴 주석 작업의 진짜 위험 = **블록 구분자 짝이 어긋나 이후 전체가 주석이 되거나 구문 오류**. 검증 =
  `/* */` **3쌍 균형**(430–441 · 456–470 · 962–991 · 전부 비활성) · `$$` **활성 프로시저만 열려 있다**
  (548 · 700) · **부록은 주석**(1225 · 1316) · `line_len` 🟢 PASS · 스테이지 96,880B.
· 🔴 **남은 수동 판단은 업로드 완료 확인 1개**다. 🟢 늦게 끝났어도 문서를 **다시 전체 실행**하면 파일
  단위 스킵으로 누락분만 채워지고 (2-B) 가 0건이 된다. 🔴 `dbt build` 전 **(2-B) 0건 육안 확인**을 명시.
· 🟠 이 조각이 상한(300줄)을 넘어 **`--rebalance` 가 필요한 상태**였고 승인 대상 연산(`R4-4-3`)이라
  돌리지 않고 **O88-B 본문만 압축**해 해소했다(301→300 경계) ⇒ 구조적 해소는 사용자 승인 후 할 일이다.

---
