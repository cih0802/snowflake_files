<!-- LLM-METADATA
doc_id: O169_EVIDENCE
doc_role: 세션 근거철 — R1-3-7-c 집행물(판정 근거를 판정보다 먼저 기록). 정본 아님.
project: GN_DW (굿네이버스)
created: 2026-09-16
created_by: O169
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# _o169_evidence — O169 세션 근거철

> 🔴 이 문서는 **정본이 아니다.** 원문 구절 + 좌표의 보관소이며 발행 후 갱신하지 않는다(정적).
> 🔴 수치를 인용할 때는 여기가 가리키는 **정본 좌표**를 읽어라.
> 🟢 유형 등재 = 원장 `00_INDEX_이슈원장-001.md:114` 의 `_o1….md` **관례 1행**이 덮는다.

---

## E0. 독해 좌표 · 고유 토큰 (도착 실증)

| # | 경로 | offset/limit | 도착 | 그 구간 고유 토큰 |
|---|---|---|---|---|
| 1 | `00_guides/00_작업지침_세션운영규칙.md` | 1/355 (전량) | 🟢 | `R0-8-4` Grep 거짓음성 3조건 · `175,722행` · `handoff_gap` |
| 2 | `20_issue/00_BRIEF.md` | 1/140 (전량) | 🟢 | 열린 착수표 4 · 문서50 열린 절 6 · `2,787,439 자` |
| 3 | `20_issue/50_dbt_파이프라인_미결조치_조각/50_dbt_파이프라인_미결조치-013.md` | 100/80 | 🟢 | `35,785,665/40,054,883` · `3,164,724` · `O73-C` |
| 4 | `20_issue/30_설계_의사결정_조각/30_설계_의사결정-015.md` | 138/50 | 🟢 | `SPNSR_DSCNTC_YN = 'N'` · 센티넬 93건/2건 · `832,794` |
| 5 | `20_issue/30_설계_의사결정_조각/30_설계_의사결정-002.md` | 66/40 | 🟢 | 영구 NULL 후보 52컬럼 · 드랍 대상 2건 · `4,255`/4,298 |
| 6 | `20_issue/00_INDEX_이슈원장_조각/00_INDEX_이슈원장-001.md` | 110/10 | 🟢 | `_o1….md` 관례 1행 · `KA98941` · O125 실측 4,772 B |

**read 미반환 = 0건 · 재호출 = 0건** (판정식 `R1-3-7-a` 충족).

---

## E1. 【1】 CONF-3 종결 여부 — 원문 구절 + 좌표

### E1-1. 차단 기재(피정정 대상)

> *"- 활동회원 8종 → **CONF-3**(정본 #51 판정 조건 내부 모순) 현업 확인"*
> 좌표 = `20_issue/50_dbt_파이프라인_미결조치_조각/50_dbt_파이프라인_미결조치-013.md:137`

### E1-2. 종결 기재 3건

| 기재 | 원문 구절(그대로) | 좌표 |
|---|---|---|
| O93-B | *"`CONF-3` **회신 없이 해소** — 정본 #51 두 문장은 모순이 아니라 **층위가 다르다**"* | `99_NEXT_SESSION_조각/99_NEXT_SESSION-011.md:197` |
| O95 | *"`CONF-3` **「방향 상반」 판정 철회** — 물음 자체가 잘못된 층위였다"* | `99_NEXT_SESSION_조각/99_NEXT_SESSION-011.md:200` |
| O141 | *"### 33-B. ✅ ㉡ CONF-3 확정 — 「재후원 우세 (후원사업 유효 기준)」 채택 [현업 회신 완료]"* | `20_issue/30_설계_의사결정_조각/30_설계_의사결정-015.md:157` |

보강 구절(같은 절):

> *"🟢 **전건 확정 및 완결** — ㉠㉢ 사용자 확정(`O127`) + ㉡ `CONF-3`(문서20 §N-11) 현업 회신 완료(`O141`)."*
> 좌표 = `30_설계_의사결정-015.md:144`

> *"· 🟢 `CONF-3` 현업 회신 확정 = 당월 말 기준 미중단 상태의 후원사업(`SPNSR_DSCNTC_YN = 'N'`)을
> 1개 이상 보유 시 월말활동회원으로 판정."*
> 좌표 = `30_설계_의사결정-015.md:159`

> *"㉠ 문서20 §N-11 회신 완료(…) ➔ ㉡ 확정 ➔ ㉢ SILVER `CRM_MEMBER_SPONSOR_SPAN` 및
> GOLD `FACT_MEMBER_MONTHLY`, `FACT_MEMBER_SPONSORSHIP_SPAN` 배선 완료 ➔ 🟢 종결."*
> 좌표 = `30_설계_의사결정-015.md:178` (종결 조건)

> 회신 확정 절 = *"### N-11. 🆕 🟢 **[2026-08-31 O127 / 2026-09-07 O141 회신 확정] `CONF-3` 월말활동회원 판정"*
> 좌표 = `20_issue/20_현업확인_요청_조각/20_현업확인_요청-007.md:240`

> 모델 측 집행 = *"-- ═══ [2026-08-20 O93 · CONF-3 해소] 활동회원 as-of 판정 ═══"*
> 좌표 = `10_dbt_pipeline/models/gold/fact/FACT_MEMBER_MONTHLY.sql:226`
> 및 *"-- 🟢 [CONF-3 해소] 정본 #51 비고의 두 문장은 모순이 아니었다 — 층위가 다르다"*
> 좌표 = `10_dbt_pipeline/models/silver/crm/CRM_MEMBER_SPONSOR_SPAN.sql:10`

🟢 **판정 = `CONF-3` 은 닫혔다.** 세 기재는 상충이 아니라 **한 축의 3단계**다:
㉠ O93-B = 「모순이라는 전제 자체가 틀렸다」(층위 구분) ⇒ 기술적 해소
㉡ O95 = 그 전제에서 나온 「방향 상반」 판정을 철회 ⇒ 물음 폐기
㉢ O141 = 그래도 **업무 규칙을 확정**받아 회신 등재 ⇒ 규칙 확정 · 배선

### E1-3. 🔴 활동회원 8종 라이브 실측 (2026-09-16 O169 · `GN_DW.GOLD.FACT_MEMBER_MONTHLY`)

판정식 = `COUNT_IF(<컬럼> IS NOT NULL AND <컬럼> <> 0)` (문서50 `-013.md:107` 규약).
총 행수 **41,508,842**.

| # | 컬럼 | 지표 | 채움 | 비중 | 판정 |
|---|---|---|---:|---:|---|
| 1 | `ACTIVE_CNT` | 활동(건) #37·157 | 39,854,326 | 96.01% | 🟢 적재 |
| 2 | `ACTIVE_MEMBERS` | 활동(명) #156 | 39,854,335 | 96.01% | 🟢 적재 |
| 3 | `ACTIVE_CUM_CNT` | 활동누계(건) #159 | **0** | 0% | 🔴 미주입 |
| 4 | `ACTIVE_CUM_MEMBERS` | 활동누계(명) #158 | **0** | 0% | 🔴 미주입 |
| 5 | `YEAR_START_ACTIVE_CNT` | 연도초 활동회원(건) #49 | 36,847,772 | 88.77% | 🟢 적재 |
| 6 | `YEAR_END_ACTIVE_CNT` | 연도말 활동회원(건) #50 | 37,676,363 | 90.77% | 🟢 적재 |
| 7 | `MONTH_END_ACTIVE_CNT` | 월말활동회원(건) #52 | 39,854,326 | 96.01% | 🟢 적재 |
| 8 | `PREV_MONTH_END_ACTIVE_CNT` | 전월말 활동회원(건) #53 | 39,122,763 | 94.25% | 🟢 적재 |

🔴🔴 **「활동회원 8종 미주입」은 stale 이다 — 6종이 이미 적재됐다.** 미주입은 **누계 2종**뿐이다.

### E1-4. 🔴 「전건 0」이 아니라 「전건 NULL」이다 (그릇 정정 · `J2` 축)

실측 = `COUNT(ACTIVE_CUM_CNT)` **0** · `COUNT(ACTIVE_CUM_MEMBERS)` **0** ·
`SUM(CASE WHEN … = 0 THEN 1 END)` **0** · `MAX(…)` **NULL** (41,508,842행).
⇒ non-null 이 0건이므로 **전건 NULL** 이다. 「전건 0」은 서로 다른 버킷이다.

과거 기재는 「전건 0(NULL 아님)」이었다:

> *"`ACTIVE_CUM_*` **6종 40,054,883행 전건 0**(NULL 아님)"*
> 좌표 = `20_issue/01_세션이력_조각/01_세션이력-022.md:80`
> 동문 = `20_issue/10_진단_원인분석_조각/10_진단_원인분석-017.md:179` · `-019.md:43`

원인 = 모델이 그 사이 **NULL 리터럴로 바뀌었다**:

> *"        CAST(NULL AS NUMBER(18,4)) as ACTIVE_CUM_CNT,"*
> *"        CAST(NULL AS NUMBER(38,0)) as ACTIVE_CUM_MEMBERS,"*
> 좌표 = `10_dbt_pipeline/models/gold/fact/FACT_MEMBER_MONTHLY.sql:318~319`

⇒ 🟢 두 기재는 **둘 다 그 시점에 참**이었다. 「0 → NULL」로 이행했으므로
버킷 집계에서 이 2건은 **전건NULL 39** 쪽에 있어야 한다(전건0 47 쪽이 아니다).

### E1-5. 🔴 잔여 2종의 진짜 차단 근거 = CONF-3 이 아니다

> *"| C. 정의·산출규칙 미확정 | 8 | 슬롯 유지 — **정의 창작 금지** |
> `FMM.ACTIVE_CUM_CNT`·`ACTIVE_CUM_MEMBERS`(정본 정의 부재) · `DIM_ORG.DIVISION`·`TEAM`
> + `FDA.ORG_*` 3(CONF-4) · `FEP.SELF_PART_FLAG` |"*
> 좌표 = `20_issue/30_설계_의사결정_조각/30_설계_의사결정-002.md:78`

> *"| `GOLD.FACT_MEMBER_MONTHLY` | `ACTIVE_CUM_CNT` · `ACTIVE_CUM_MEMBERS` | C 정의 부재 | 🟢 |
> 슬롯 유지 — **정의 창작 금지** |"*
> 좌표 = `20_issue/30_설계_의사결정_조각/30_설계_의사결정-002.md:155`

⇒ 🔴 **차단 근거는 「CONF-3 현업 확인」이 아니라 「정본 정의 부재」(버킷 C)** 이고
처방은 **정의 창작 금지 · 슬롯 유지**다. ⇒ 내부 착수 불가 · **결정 문항으로 승격**(D12).

🔴 선례 = 「누계」 정의를 창작하려던 시도는 이미 **창작으로 판정**된 바 있다:

> *"| `CUMULATIVE_DEV_MEMBERS` | `ACTIVE_CUM_CNT`·`ACTIVE_CUM_MEMBERS`·`AMT_DECREASE_CUM_CNT` | 🔴 창작 |"*
> 좌표 = `20_issue/_o159_mstr_impact_measure.md:141`

### E1-6. 판정 요약 (사용자 지시 ①②③ 에 대한 답)

| 지시 | 판정 | 근거 |
|---|---|---|
| ① CONF-3 이 실제로 닫혔는가 | 🟢 **닫혔다** (2026-09-07 O141 회신 확정 · DEC-47 §33-B) | E1-2 |
| ② 닫혔다면 활동회원 8종 내부 착수 | 🟠 **부분 무효** — 6종은 **이미 적재 완료**라 착수 대상이 아니다 | E1-3 |
| ③ 열렸다면 문항으로 | 🔴 **잔여 2종은 다른 이유로 열려 있다** = 정본 정의 부재 ⇒ **D12 문항** | E1-5 |

🟢 **판정식(신설) = 「묶음의 차단 근거는 묶음 단위로 상속되지 않는다.」**
활동회원 8종은 한 이름으로 묶여 한 블로커(`CONF-3`)에 걸려 있었으나,
실측하면 **6종은 블로커 해소로 적재됐고 2종은 처음부터 다른 블로커**였다.
⇒ 묶음 표기는 **최초 등재 시점의 최대공약수**일 뿐이다.

**음성 테스트 축**(이 판정식이 틀리는 방향):
㉠ 묶음 전건이 같은 블로커인 경우에 「분해하라」가 오탐이 되는가
  ⇒ 아니다. 분해 후 근거가 같으면 결론이 같다(비용만 든다).
㉡ 역방향 = **일부가 적재됐다고 묶음을 「해소」로 닫는 것** ⇒ 🔴 금지.
  판정은 **집합 차이**로 한다(해소 6 · 잔여 2 · 신규 0) — `총계`로 닫지 마라.

---

## E2. 【2】【3】 개명 축1·축2 집행 근거

### E2-1. 라이브 실측 — 「개명이 아니라 객체 소멸」의 독립 확증

`GN_DW.INFORMATION_SCHEMA.TABLES` (2026-09-16 O169):

| 객체 | 실재 | 행수 |
|---|---|---:|
| `GOLD.DIM_MEMBER` | 🟢 | 1,785,299 |
| `GOLD.DIM_MEMBER_ACQUISITION` | 🟢 | 1,606,887 |
| `GOLD.DIM_MEMBER_IDENTITY` | 🟢 | 1,785,300 |
| `GOLD.DIM_MEMBER_STATUS_HISTORY` | 🟢 | 8,069,279 |
| `GOLD.DIM_MEMBER_CURRENT` | 🔴 **부재** | — |

팬아웃 실측 = `COUNT(*)` **1,785,299** = `COUNT(DISTINCT MEMBER_DK)` **1,785,299** ⇒ **차 0**.
⇒ 🔴 *"`DIM_MEMBER` 는 SCD2라 팬아웃한다"* 는 전제는 **라이브에서 거짓**이다.
🟢 게이트 자신이 같은 판정을 이미 적고 있었다(독립 일치):

> *"이 항목은 **개명이 아니라 객체 소멸**이다 — 검출용 좌변은 유효하지만
> 우변을 **치환 제안으로 읽으면 안 된다**"*
> 좌표 = `scripts/rename_stale_gate.py:63~67`

모델 측 확증:

> *"-- DIM_MEMBER: 정규 회원 마스터 차원 (회원 1명 = 1행, IS_CURRENT=TRUE 투영)
> — 분석가 및 Cortex Analyst 기본 진입점"*
> 좌표 = `10_dbt_pipeline/models/gold/dim/DIM_MEMBER.sql:1` · 같은 파일 `:36` = `where IS_CURRENT`

### E2-2. 줄마다 「지시」/「기록」을 가른 결과

분모는 창작하지 않고 **게이트의 `scan()`** 에서 받았다(J8) — 축1 `DIM_MEMBER_CURRENT` **68건 / 66줄 / 29파일**
(🔴 순수 문자열 카운트는 **71** 이었다 ⇒ 차 3 은 `qualified()` 가 면제한 출현이다. 내 분모로 세면 틀린다).

| 축 | 건 | 처방 |
|---|---:|---|
| **기록**(진단·결정·경위의 서술 대상) | 60줄 | 원문 보존 + 병기 — 축A 「구 」 삽입 57 · 축B 줄말미 병기 3 |
| **지시**(독자·도구를 그 객체로 보낸다) | 6줄 | 손으로 현행 객체로 고치거나 폐기 선언 |

「지시」 6줄의 판정 근거:

| 좌표 | 왜 지시인가 | 처방 |
|---|---|---|
| `02_GN_DW_building/06_RUNBOOK.md:736` | 표가 그 객체를 **「분석가 진입점」**으로 지목 | 현행 = `GOLD.DIM_MEMBER` 로 고침 + 실측 병기 |
| `20_issue/30_설계_의사결정_조각/30_설계_의사결정-005.md:150` | **grep 대상 목록** = 도구를 그 객체로 보낸다 | `DIM_MEMBER` + 이력축 `DIM_MEMBER_STATUS_HISTORY` 로 고침 |
| `20_issue/30_설계_의사결정_조각/30_설계_의사결정-009.md:62` | **「라벨이 이미 있는 축」** 소재지 안내 | 소재지 1개(`DIM_MEMBER`)로 정정 |
| `10_dbt_pipeline/10_성능검토_정적코드분석_20260716.md:66` | **「권고」** = 예약 조치(`J5`) | 🔴 **폐기 선언** + 실측 근거 |
| 같은 파일 `:109` | 종합판정 표의 **핵심 조치** 셀 | 🔴 폐기 병기 |
| `scripts/gen_section_assembly.py:283` | O56 판본 **인용** — 원문 보존 우선 | 인용 **밖**에만 병기어 삽입 |

🔴 **집행 중 적발한 자기결함 2건**(둘 다 예행에서 잡았다 · 집행 전 수정):

| # | 결함 | 처방 |
|---|---|---|
| 1 | 삽입점을 `m.start()` 로 두면 `` `GOLD.DIM_MEMBER_CURRENT` `` 가 `` `GOLD.구 DIM_MEMBER_CURRENT` `` 가 되어 **식별자 내부를 깨뜨린다** | `insert_point()` = 코드 스팬·점수식의 **머리**로 이동 · 음성 테스트 축2 가 고치기 전 구현의 실패를 실증 |
| 2 | 인용문(`*"…"*`) 내부에 병기를 끼우면 **인용 개변**이다(`R2-8-2`) | `quote_spans()` 로 감지해 **원문 무변경 + 줄말미 병기**(축B)로 전환 |

🔴🔴 **집행 중 겪은 쓰기 사고 1건 — `edit` 도구가 보고한 줄 수와 실제 파일이 달랐다.**
`scripts/_o169_axis1_biwi.py` 에 `edit` 를 걸자 도구는 *"File now has 169 lines"* 를 보고했으나
실제 파일은 **127줄에서 문장 중간에 잘려** 있었다(`for rel, lineno, _ in li`).
`R1-7-3` 대로 **3회 재읽어 해시 대조**했고 **5,632 B / `cfac55fe…` 3회 동일** ⇒ torn read 가 아니라 **실손상**.
복구 = 전량 재작성 후 `R1-7-1` 복구 4축 확인(꼬리 되읽기 · `py_compile` OK · `def` 6개 · 해시).
🟢 **판정식(신설) = 「도구의 성공 보고는 도달의 증거가 아니다」** — O167 의 *「썼다」와 「닿았다」는 다르다*
가 `cat >>` 판본이었고, 이번은 **`edit` 판본**이다. ⇒ 🟢 편집 직후 **줄 수·꼬리·컴파일을 재라.**

### E2-3. 축1 집합 차이 (총계가 아니라 집합으로 증명)

| 축 | 착수 시 | 종료 시 | 차 |
|---|---:|---:|---|
| 축1 살아있는 정본 | **68** | **0** | 해소 68 · 신규 0 · 누락 0 |
| 병기(자격 있는 인용) | 190 | 266 | +76 |
| 축2 자동 생성 산출물 | **301** | **300** | −1 |
| 축3 이력·근거철 | 432 | 438 | +6 (이 세션이 만든 근거철·음성테스트) |

🔴 **기준선 문서는 축2 를 302 로 적었으나 착수 실측은 301 이었다**(차 1) ⇒ 총계를 인용하지 말고 재라.

### E2-4. 축2 — 「재생성으로 해소」가 참인 부분과 거짓인 부분

🟢 **참인 부분(실증)** = 인벤토리 CSV. `python3 scripts/gen_column_inventory_20260811.py 20260916` 실행 →
`02_gold 스키마 컬럼 인벤토리_20260916.csv`(51테이블/1,321컬럼) 생성 · **옛 이름 0건**.
⇒ 생성기는 라이브를 사영하므로 재실행이 곧 해소다.

🔴 **그런데 잔여 202 는 사라지지 않는다** — 그것은 **`…_20260904.csv` 라는 별도 날짜 파일**이고
재생성은 그 파일을 덮지 않는다(날짜가 파일명이다). ⇒ **은퇴가 필요하고 그것은 파괴 가능 작업**이다
(`R4-4-3`) ⇒ **결정 문항 `D13`**. 🟢 참고 = `…_20260903.csv` 는 옛 이름 **0건**이므로 20260904 만 이상치다.

🟢 **문서 산출물 2건은 해소했다**:
· `20_issue/92_실측필요_후속작업.md` = `gen_measure_backlog.py --write` (7항목 · 6,602 B) ⇒ 1 → 0
· `30_output_share/11_미해결이슈_요약.md` = `gen_unresolved_issue_summary.py --write` ⇒ 1 → 0
  🔴 **여기서도 「썼다와 닿았다」가 갈렸다** — `--write` 없이 돌리면 **표준출력에만 찍고 파일은 그대로**다
  (mtime 이 9-15 에 머물러 있었다). 게이트가 그 1건을 계속 보고해서 잡았다.

🔴 **거짓인 부분 = 골든 json 18건**(`doc_headings.json` 7 + 골든 7 + `index_rows.json` 2 + 골든 2).
실측하니 그중 6건이 골든 **자신의 감사 이력**이었다:

> *"[2026-09-16 O168] 개명 잔여 축1 일괄 정정(…)에 제목 6개가 포함됐다. 유실 6 ↔ 신설 6 ·
> 총계 1716 불변 = 삭제가 아니라 개명이다. 대응: FACT_TARGET_DEV→FA…"*
> 좌표 = `scripts/golden/doc_headings.json` `_updates/reason`

⇒ 🔴 **개명을 기록한 문장이 개명 감시 게이트에 걸린다**(`J8` 자기참조 계측의 json 판본).
   재생성해도 남는다 — 감사 이력은 **append-only** 이기 때문이다.
   🟢 처방 = 재생성이 아니라 **분모 정정**(골든의 `_updates` 는 축3 성격) ⇒ **결정 문항 `D13`** 에 합산.

🟢 **골든 대조 = `python3 scripts/test_generators.py` → `PASS 21 · FAIL 0`**
⇒ 드리프트 0 이므로 **골든 재발행은 하지 않았다**(`--reason` 발행 대상이 없다).
🔴 이것이 「재발행 사유를 남겨라」에 대한 답이다 — **발행할 것이 없으면 사유도 없다**를 명시한다.

---

## E3. 【4】 결정 문항 — (a) 실측 근거 · (b) 선례 · (c) 권장안

🔴 **근거 없이 권장하지 않는다.** 실측으로 뒷받침되지 않는 문항은 **권장을 비우고
「무엇을 재면 갈리는가」를 적는다**. 사용자가 고른 뒤 집행한다.

### D1. CONF-2 — 지표사전의 「(건)」이 무엇인가

**(a) 실측** — `GN_DW.GOLD` COMMENT 전수(2026-09-16). 🔴 **이미 갈려 있다**:

| 컬럼 | COMMENT 실물 | 산식 명문 |
|---|---|---|
| `FACT_MEMBER_MONTHLY.DECREASE_CNT` | `감액(건) SUM(감액금액)/10000 (#38)` | 🟢 **(b) 명문** |
| `FACT_MEMBER_MONTHLY.INCREASE_CNT` | `증액(건) (#151)` | 🔴 없음 |
| `FACT_MEMBER_MONTHLY.UNPAID_CNT` | `미납(건) (#36)` | 🔴 없음 |
| `FACT_MEMBER_MONTHLY.CAMPAIGN_UNPAID_CNT` | `캠페인별 미납(건) (#83)` | 🔴 없음 |
| `FACT_MEMBER_MONTHLY.STATUS_UNPAID_CNT` | `회원상태별 미납(건) (#84)` | 🔴 없음 |
| `FACT_MEMBER_MONTHLY.AMT_INCREASE_CUM_CNT` | `AMT_INCREASE_CUM_CNT (#151).` | 🔴 라벨조차 없음 |
| `FACT_MEMBER_MONTHLY.AMT_DECREASE_CUM_CNT` | `AMT_DECREASE_CUM_CNT (#38).` | 🔴 라벨조차 없음 |

🟢 **금액 축은 별 컬럼으로 이미 존재한다** — `UNPAID_BILLED_AMT`(`FACT_MEMBER_FEE`·`FMM`·`WIDE_MEMBER_FEE`)
가 「미납 청구액(원)」으로 따로 있고 그 COMMENT 는 정본까지 명시한다:
*"DEC-3 정본 = PAY_STAT_CD IN (F, NULL) 인 청구액. 🔴차감식(청구−납입) 아님"*.
⇒ 🔴 **미납 축에는 「금액」 슬롯이 따로 있으므로 「(건)」을 다시 금액으로 읽으면 두 컬럼이 같아진다.**

**(b) 선례** — `DEC-47 §33-A`(`30_설계_의사결정-015.md:148~155`) = *"활동(건) = 그 시점 **미중단**
약정금액 합 ÷ **10,000**"* 이고 그 근거를 *"`CONF-2` 규약의 **재사용**"* 이라 적으며
*"정본 공#38 감액(건)·#151 증액(건)이 이미 **금액 ÷ 10,000**"* 이라고 단정한다.
⚠️ 그런데 **실측은 #38 만 명문이고 #151 은 미명문**이다 ⇒ 그 문장은 **#151 에 대해 근거 없이 단정**했다.

**(c) 권장 = (c) 지표별로 다름 · 사전 재정의.** 단 재정의는 **묶음 단위 2분할**로 족하다:

| 축 | 판정 | 근거 |
|---|---|---|
| **금액환산 축** = 증액(#151·#150) · 감액(#38) · 회비 분해 4 · 누계 2 | **(b) 금액 ÷ 10,000** | #38 COMMENT 명문 + `DEC-47` 이 활동(건)에 같은 규약 채용 |
| **미납 축** = #36 · #83 · #84 | **(a) 실제 건수** | 🔴 금액 슬롯(`UNPAID_BILLED_AMT`)이 **이미 따로 있다** ⇒ 금액으로 읽으면 중복 |

🔴 **이 권장을 확정하려면 재야 할 것 하나** = 원천 미납 청구의 **행수 ↔ 금액÷10,000** 배율.
두 값이 같은 자리수면 (a)/(b) 가 구분되지 않는다 ⇒ **결정 전 1회 측정 권장**.
🟢 채택 시 집행 = 컬럼 COMMENT 에 **산식을 명문화**(`DEC-47 §33-A` 가 *"구현 시 컬럼 COMMENT 에
정의를 명문화한다"* 를 이미 의무화했다) · `AMT_*_CUM_CNT` 2종은 **라벨부터** 복구해야 한다.

### D3. BLOCKING-2 E — 행사 고아

**(a) 실측**(2026-09-16 · `FACT_EVENT_ATTENDANCE` ⟂ `DIM_EVENT` on `EVENT_BK`):

| 축 | 값 |
|---|---:|
| 전체 행 | 1,258,775 |
| 고아 행 | **281,701 (22.38%)** |
| 고아키 | **54종** |
| `EVENT_105` | **208,618** (고아의 74.06%) |
| `EVENT_106` | **71,631** (25.43%) |
| 상위 2키 소계 | 280,249 = **99.48%** |

🔴 **기준선 대조** = 총계·비중·키종수는 정확 일치했으나 `EVENT_105` 는 기준선 **208,654** ↔ 실측 **208,618**
(**차 36**) ⇒ 총계만 맞아서 「같다」고 읽으면 안 된다.

🔴🔴 **처분을 가르는 결정적 실측 2건**:
㉠ **연속 번호 사이의 구멍이다** — `DIM_EVENT` 에 `EVENT_100`·`101`·`102`·`103`·`104`·`107`·`108`·`109`·`110`
  이 **전부 실재**하고 **105·106 만 없다**(`DIM_EVENT` 총 3,937 · `EVENT_` 접두 383).
  ⇒ 🔴 「타 시스템 행사」라면 번호 체계가 **겹치지 않아야** 한다 ⇒ **(c) 기각**.
㉡ **원천 마스터에 없다** — `BRONZE_CRM.TM_MS_EVENT` **383행** · `EVENT_CD` 100~110 구간 **9행** 실재 ·
  **105 = 0행 · 106 = 0행** (`MIN` 1 · `MAX` 440). 참여 사실은 `TD_MS_EVENT_PRTCPNT_DTL` **1,097,970행**
  에 실재한다. ⇒ 🔴 참여가 28만 건인 행사를 「폐지」로 보고 **집계에서 지우면 22.38% 가 조용히 사라진다**
  (`R2-7-1` 축 — 조용한 필터가 분모를 줄이면 관측에서 사라진다) ⇒ **(b) 기각**.

**(b) 선례** — `DEC-47 §33-C`(`30_설계_의사결정-015.md:169~174`) = *"음수 약정금액은 **제거·보정하지 않고**
`severity: warn` 관측 축으로 분리한다 … 🔴 **필터로 0 을 만들지 마라.** 차이를 **설명**하는 것이 정답이고
**없애는 것**이 아니다"*. 같은 축의 처방이다.

**(c) 권장 = (a) 마스터 미입고 → 입고 요청.** 근거 3중(㉠ 번호 구멍 ㉡ 원천 0행 ㉢ 참여 28만 건 실재).
🟢 입고 전 임시 처방 = `SK=0` 시드 멤버로 `(미매핑)` 표기(`R2-7-3`)하고 **분모에 남긴다** ·
고아율 22.38% 를 `severity: warn` 관측 축으로 노출. 🔴 **집계 제외로 닫지 마라.**
⚠️ **회원 고아 축은 별건**이다(10,048행 · 0.80%) — 이 판정을 그쪽에 상속시키지 않는다.

### D5. HOL-1 공휴일

**(a) 실측**(2026-09-16 · `GOLD.DIM_DATE`) = **16,437행 · `IS_HOLIDAY` TRUE 0 · FALSE 16,437 · NULL 0**
⇒ 전건 FALSE 이고 **NULL 이 아니다** ⇒ 「값이 없다」가 아니라 **「없다고 단정된 값이 주입돼 있다」**.
🔴 이것이 위험의 성격이다 — 분석가·Agent 는 *"공휴일이 하루도 없다"* 로 읽는다(0행 무증상 오답).

**(b) 선례** — `R2-7-1`(*"값이 없는 자리를 창작하지 않는다"*)의 **역방향 위반**이다.
그리고 `DEC-49 표준 사유 COMMENT` 가 이미 「미주입 분모에서 제외」 경로를 갖고 있다.

**(c) 권장 = (b) 상수 유지 + `DEC-49` 표준 사유 COMMENT 명기** — 단 **조건 2개를 붙인다**:
㉠ COMMENT 문안은 *"원천 미입고 · 전건 `FALSE` 는 **주입값이 아니라 자리표시**"* 로 **거짓 단정을 취소**한다.
㉡ 🔴 **`FALSE` 를 유지하되 소비층에서 「공휴일 지표」를 노출하지 않는다** — 값이 있으면 SV·Agent 가
  집계에 쓴다. `R2-7-1` 대로라면 정답은 **NULL 로 되돌리는 것**이지만, `BOOLEAN NOT NULL` 제약과
  하류 영향 때문에 (a) 원천 입고 없이 바꾸면 부수효과가 크다 ⇒ **(b) + 노출 차단**이 최소 위험이다.
🔴 (a) 원천 입고를 고르면 그것이 정답이지만 **외부 입고 대기**로 성격이 바뀐다(버킷 A).

### D12. 🆕 활동누계 2종 — 정본 정의 부재 (【1】 에서 승격)

**(a) 실측** = `FMM.ACTIVE_CUM_CNT`(#159) · `ACTIVE_CUM_MEMBERS`(#158) **전건 NULL** / 41,508,842행.
차단 근거는 `CONF-3` 이 **아니고** 버킷 **C 「정본 정의 부재」**다(§E1-5).
**(b) 선례** = `_o159_mstr_impact_measure.md:141` 이 이 두 컬럼에 「누계」 정의를 붙이려는 시도를
**🔴 창작**으로 판정했다. 그리고 `30_설계_의사결정-002.md:78`·`:155` 가 **「슬롯 유지 · 정의 창작 금지」**.
**(c) 권장 = 슬롯 유지 + 정의 질의 등재(현업).** 갈라야 할 것 3개:
㉠ 누계의 **기산점**(연도초 / 사업연도초 / 전체기간) ㉡ 「건」의 단위(D1 과 같은 축 ⇒ **D1 종속**)
㉢ **비가산 여부**(누계를 기간 합산하면 이중계상 ⇒ `NONADDITIVE_MEASURE` 등재 필요).
🔴 **D1 이 먼저 닫혀야 한다** — 단위가 안 정해지면 누계도 못 정한다.

### D13. 🆕 축2 잔여의 처분 (【3】 에서 승격 · 🔴 파괴 가능 ⇒ 승인 필요)

**(a) 실측** = 축2 잔여 **300건** = ㉠ `30_output_share/02_gold 스키마 컬럼 인벤토리_20260904.csv` **202**
㉡ `scripts/outputs.json` **77**(그중 62 = `metrics/inv.entry_keys` = ㉠ 을 측정한 값 · 15 = 09 앵커)
㉢ 골든 json **18**(6건은 개명을 기록한 **감사 이력** 자체) ㉣ `golden/outputs.json` 2 · `index_rows` 2.
🟢 재생성이 듣는다는 것은 실증됐다(`…_20260916.csv` = **0건**) · `…_20260903.csv` 도 **0건**
⇒ **20260904 판만 이상치**다.
**(b) 선례** = `R2-8-3`(*"확신이 없으면 원본을 남기고 포인터만 추가"*) · `R1-7-7`(스테이지 삭제 위험) ·
`R4-4-3`(파괴 가능 작업은 지시가 동봉돼도 별도 승인).
**(c) 권장 = ㉠ 은 `_archive/` 로 이동**(삭제 아님 · `_archive` 는 게이트 `SKIP_DIR` 이라 축2 가 −202)
**+ ㉢ 은 게이트 분모 정정**(골든의 `_updates` 감사 이력을 축3 취급) **+ ㉡ 는 ㉠ 처리 후 재측정**.
🔴 **파일 이동은 승인 없이 하지 않았다** — 승인해 주면 그 순서로 집행하고 축2 를 재라.

### D2 · D4 · D6 ~ D11 — 🟠 이 세션에서 근거를 확보하지 못했다

🔴 **권장을 비운다**(`R1-3-7` 「대체 근거 금지」 · *"근거를 못 썼으면 판정하지 않는다"*).
아래는 **무엇을 재면 갈리는지**만 적는다 — 다음 세션이 이 좌표부터 재라.

| 문항 | 재야 할 것 | 좌표 |
|---|---|---|
| **D2** `DEC-33` 코호트 3문항 | 🔴 사용자 실측 경고를 먼저 검증하라 — D+0 84,451(73.1%)의 상위 제목이 **전건 중단 통보**(`NN2-사단-CRM중단-A(경제)` 48,909)면 **창을 정하는 순간 「중단 통보가 중단을 유발」로 집계된다**. ⇒ 귀속창보다 **모집단 정의가 선행**이고, 「처리통보성」을 제목 패턴이 아니라 **발송요청 유형 코드**로 가를 수 있는지부터 재라 | 문서20 §L-1 · `FACT_MESSAGE_DISPATCH.D5_*` 8컬럼 |
| **D4** 채널별 성공/실패 코드 | 채널(`EMAIL`·`MSG_AT`·`SMS`·`APP_PUSH`)별 `SEND_STATUS` 값 분포를 **채널 교차로** 전수 덤프 — 🔴 `O59-P-1`(`SEND_STATUS2` 처분)이 **같은 축의 열린 항목**이므로 함께 닫아야 한다 | 문서50 `-012.md:90` · 문서20 §M-6 · `31_코드군_매핑등재부.md` |
| **D6** 버킷 처분 6군(A18·C11·D14·E4·F8) | 🔴 **묶음 이름으로 세지 마라**(이 세션의 판정식) — 군마다 컬럼을 특정해 **각 컬럼의 차단 근거를 개별 실측**하라. 활동 8종에서 6/8 이 이미 닫혀 있었다 | `30_설계_의사결정-002.md:74~81` · 문서50 `-013.md:126~133` |
| **D7** 누락/과잉 GOLD 4 | 브리핑 §3 은 *"결정 대기 — 누락/과잉 GOLD **6개**"* 로 적는다 ⇒ 🔴 **4 ↔ 6 이 어긋난다.** 집합을 먼저 맞춰라 | 문서50 `-014.md:180` |
| **D8** ⑩ ㉤㉥ 컬럼 개명 | `P102`·`P106` 정의 복원이 선행 · 로드맵 10 `DEC-34` | 착수표 ⑩ = `99_NEXT-016.md:46` |
| **D9** `O91-F` 잔여 2 | 문서50 `-016.md:4` 이 *"처방 5건 중 4건 종결 · 잔여 2건"* 이라 적어 **5−4=1 ↔ 2 가 어긋난다** ⇒ 집합부터 | 문서50 `-016.md:4` |
| **D10** 순서9-C 잔여 4 | *"내부분 착수·검증 완료 / 잔여 외부의존"* ⇒ 잔여 4가 **전부 외부의존인지** 실측 | 문서50 `-015.md:15` |
| **D11** `D8` BRD 외부 로더 −1,262,913 | 라이브 10,337,767 **무변**이 확인됐다면 그것은 **이력 축의 차이**다 ⇒ 로더 이력과 라이브를 **같은 판정식으로** 재서 차이의 정체를 먼저 규명 | — |

---

## E4. 승인 후 집행분 (사용자 승인 = 「FAIL 2건 등 내 의견이 필요하지 않은 부분 전부 진행」)

### E4-1. `D13 ㉠` — stale 날짜 인벤토리 CSV 은퇴

이동 = `30_output_share/02_gold 스키마 컬럼 인벤토리_20260904.csv` **395,270 B**
`sha 156b846240cf306d` → `_archive/…csv.O169-o169-retire-stale-inventory`.
🔴 `R1-7-7` 절차 = ㉠ 스냅샷 선행 ㉡ **스냅샷 바이트 동일 확인 후에만** 삭제 ㉢ `os.listdir` 재확인.
잔존 = `…_20260903.csv`(옛 이름 0건) · `…_20260916.csv`(옛 이름 0건).
⚠️ **SILVER 쌍둥이 `…_20260904.csv` 는 옮기지 않았다** — 옛 이름 **0건**이라 위반이 아니고
`R2-8-3`(확신 없으면 원본 보존)을 따랐다.

### E4-2. `D13 ㉢` — 게이트 버킷 분류 정정 (`SNAPSHOT` 축 신설)

🔴 **정정 사유 = 축2 의 계약이 그 파일들에 참이 아니었다.** 축2 라벨은 「재생성으로 해소」인데:

| 파일 | 옛 이름 | 실제 정체 | 재생성으로 사라지는가 |
|---|---:|---|---|
| `scripts/golden/doc_headings.json` | 7 | 6건이 골든 **자신의 `_updates` 감사 이력**(개명을 기록한 문장) | 🔴 아니다(append-only) |
| `scripts/outputs.json` | 77 | 62건이 `metrics/inv.entry_keys` ⇒ 원천은 **수기 문서** `03_top-down_gold/05_필드 인벤토리.md`(`test_generators.DOC_INV`) | 🔴 아니다(복사를 되풀이한다) |
| `scripts/doc_headings.json` · `index_rows.json` · `golden/*` | 나머지 | 정본 텍스트의 **측정 스냅샷** | 🔴 아니다 |

⇒ 🟢 처방 = `rename_stale_gate` 에 **`SNAPSHOT` 축 신설**(축3 취급 · `bucket()` 에서 `GEN` **앞**에 판정).
🔴 `GEN` 앞에 두는 이유 = `scripts/golden/rename_stale_baseline.json` 처럼 **두 축에 다 걸리는 경로**가 있고,
그때는 **처방이 통하는 쪽**을 택해야 한다.
🟢 **판정식(신설) = 「버킷은 생성 여부가 아니라 처방으로 가른다.」**

음성 테스트 = `test_rename_stale_gate` **축5 를 7건 → 14건으로 확장**(스냅샷 5건 + 역방향 오탐 2건).
🔴 그 확장이 **축11 의 기존 단정과 충돌**했다(`golden/outputs.json` 을 `GEN` 으로 단정) ⇒ 함께 정정.
🟢 **교훈 = 축을 늘릴 때 기존 축의 단정도 함께 재라**(`R3-9 ㉡` 「같은 것을 다르게 재는 지점」).

**집합 차이**: 축1 **0**(불변) · 축2 **299 → 0** · 축3 **444 → 542** · 병기 **270**.

### E4-3. `99_NEXT_SESSION` 재균형 — 🔴 축이 옮겨 갔다

`split_doc.py 99_NEXT_SESSION.md --rebalance --fill 0.7 --apply` (예행 선행 · 스냅샷 `O169-prerebalance`).
결과 = 조각 재기록 · **max 28,524 B · 최소 여유 12,436 B · 조각 상한 초과 0** · concat SHA256 일치 · 유실 0.

🔴🔴 **그런데 직후 `doc_type_gate` 가 「허브 44,521 B > 40KB」로 FAIL 했다.**
· 내 직접 실측은 **허브 38,620 B**(조각 24) ⇒ 두 값이 어긋났다.
· 🟢 판정 = **낡은 뷰**였다(`OPS-3`). 규약(`references/session-end.md` §2)이
  *"유지 연산 직후의 게이트 FAIL 을 곧바로 믿지 마라 — 2~3회 재실행 후 판정"* 이라 적어 두었고,
  🔴 **2회 재실행으로는 부족했다**(1·2회차 rc=1 · **3회차 rc=0 · 상한 초과 0**).
⇒ 🟢 **판정식(보강) = 「2~3회」의 하한을 믿지 마라 — rc 가 뒤집힐 때까지 시간을 두고 다시 재라.**

🟢 부수 확인 = 채움률별 예행 실측(`--fill` 0.80/0.90/0.95 → 조각 27/25/24 · max 32,125/35,980/37,709 B ·
상한 초과 **전건 0**) ⇒ 현재 상태(24조각)가 이미 안정 구간이다.
🟢 편집한 조각의 허브 **7종 재발행** = 원장 · 문서50 · 문서30 · 문서02 · 문서03 · 문서10 · 문서20
(전건 `--expect` 토큰 통과 · 「본문 바이트 동일 · 유실 0」).

### E4-4. `sv_code_label_gate` 선행 FAIL 3건 — 라이브 정정

**(a) 실측** — 🔴 첫 시도에서 **그릇을 틀렸다**: `CMMN_BRND_NM` 을 `GOLD.DIM_CAMPAIGN` 에서 재서
**17종**을 얻었으나, 게이트의 backing 은 `GOLD.FACT_MEMBER_EVENT.CMMN_BRND_NM_AT_EVENT` 였다.
🟢 SV DDL 문법(`<논리테이블>.<차원명> AS <별칭>.<물리컬럼>`)을 읽고 물리 컬럼을 특정한 뒤 재측정:

| 축 | 종전 COMMENT | 라이브 실측 | 판정 |
|---|---|---|---|
| `DAY_OF_WEEK`(backing `DIM_DATE.DAY_OF_WEEK`) | 요일 **영문 약칭 7개** + 「+ NULL」 | 코드 **`0`~`6`** · **NULL 0건**(16,437행) | 🔴 값도 거짓 · NULL 선언도 거짓 |
| `CMMN_BRND_NM` 종수 | 「실제값 **14종**」 | distinct **16** · NULL **1,061,449** | 🔴 종수 불일치 |
| `CMMN_BRND_NM` 열거 | **`일시`** 를 열거 | 라이브에 **없다** | 🔴 유령 열거(0행 오답 경로) |
| `CMMN_BRND_NM` 누락 | — | `대면모금`·`직원개발`·`콜개발` **3종** 미열거 | 🔴 열거 결손 |

요일 대응은 **창작하지 않고** 코드별 `DAYNAME` 교차로 확정했다 = `0`=일 · `1`=월 · … · `6`=토.
🔴 **부수 발견** = 코드 `0` 에 시드 행 `1900-01-01`(실제 **월요일**) **1건**이 섞여 있다
(`0` = 2,349행 · 나머지 각 2,348행) ⇒ **일요일 집계가 1행 과대**다. COMMENT 에 경고로 남겼다.

**(b) 집행** — 정본 `05_SV-Agent_ai/05_2_SV_DDL_MEMBER_EVENT.sql` 수정 후 라이브 재배포.
🟢 **파괴 위험이 없음을 먼저 확인했다** = 본문이 **`CREATE OR ALTER SEMANTIC VIEW`** 이므로
GRANT·소유권이 보존된다(`P125` 의 `CREATE OR REPLACE` 경로가 아니다).
배포문은 `extract_sv_deploy.py` 로 추출(21,056자 · GRANT 3건)하고 러너
`scripts/_o169_sv_redeploy.py` 로 실행(가드 = `CREATE OR REPLACE` 혼입 검사 + `^CREATE OR ALTER` 확인).
문장 7개 · 오류 0 · **실행 후 라이브 COMMENT 를 되읽어 도달을 단정**했다.
🟢 **GRANT 보존 실측** = `SHOW GRANTS` **7건 전건 잔존** · `created_on` **2026-09-15 불변** ·
OWNERSHIP = `GN_DW_ADMIN`.

**(c) 🔴🔴 1차 재배포는 실패했다 — O167 이 적어 둔 함정을 그대로 밟았다.**
1차 COMMENT 는 정정 사실을 남기려고 **옛 값 목록을 본문에 인용**했고,
게이트의 열거 추출기가 **그 옛 목록을 현재 열거로 집어** `DAY_OF_WEEK` 위반이 **그대로 남았다**
(3건 → 1건에서 멈췄다). 이것이 O167 의 *「부정문을 심으면 그 부정문이 탐지기에 걸린다」* 재발이다.
🟢 처방 = **COMMENT 에는 현재 코드값만** 두고 경위·수치·유령값 이름은 **DDL `--` 주석과 근거철**에 둔다
(`R2-6` 「COMMENT 에는 코드값만 · 실측 수치는 문서에」와 같은 축이다).
🟢 `CMMN_BRND_NM` 도 같은 잠재 함정(유령값 인용)을 갖고 있었으므로 **함께** 정리했다.
2차 재배포 후 게이트 = **위반 0건 · rc=0**.

### E4-5. 이 절의 집합 차이 (종료 실측)

| 축 | 착수 시 | 종료 시 |
|---|---|---|
| 개명 축1 | 68 | **0** |
| 개명 축2 | 301 | **0** |
| `doc_type_gate` | 🟡 경고 | **rc=0 · 상한 초과 0** |
| `sv_code_label_gate` | 🔴 위반 3(blocking) | **rc=0 · 위반 0** |
| `scripts/test_*.py` | 37/37 | **37/37**(축 신설·시정 3곳 포함) |

---
