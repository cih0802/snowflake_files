<!-- LLM-METADATA
doc_id: ISSUE_40_INTAKE_PENDING
doc_role: 입고 대기 — 외부 원천 의존 (우리끼리 해소 불가) + 데이터 기간요건
project: GN_DW (굿네이버스)
created: 2026-07-15
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# 40. 입고 대기 · 원천 의존 (업무단계: 입고 대기)

> **외부 원천 입고·현업 제공이 있어야만** 해소되는 하드블로커. 설계·실행으로 앞당길 수 없음.
> 🔷 **[2026-07-30 Phase 배정] 본 문서의 항목은 정의상 전부 Phase-2다.** Phase-1 = 현재 BRONZE에 있는 데이터로 만드는 범위이고, 이 문서는 "BRONZE에 없거나 값이 비어 있는 것"을 모으기 때문이다. Phase-1 착수 범위는 `00_INDEX_이슈원장.md` §2-C-2 Phase 배정표 참조.
> 전체 인덱스: `00_INDEX_이슈원장.md`

---

## 1. 외부 원천 하드블로커
| ID | 원천 | 필요 데이터 | 현황 | 조치 |
|---|---|---|---|---|
| 🔴 G-5 | GA4 | 전체기간 샤드(이벤트 40여종·개발전환 실증) | 1일 샤드(`events_20260501` 287,025행)만 | 전기간 샤드 입고 대기(외부 커넥터) |
| 🔴 E-6 | **CRM** | 사업목표(FTG-B, 연/추경목표) — 예산원장≠사업목표, 기존 CRM 개발목표 테이블(개발구분 축만)과도 별개 | CRM 신규 목표 테이블 입고 대기 | **[2026-07-20 정정·최신]** 원천=**CRM**(현업 수동입력 신규 목표 테이블) 확정 — 정본 인벤토리(마케팅 §1 "CRM에 부서별 목표만 존재")·현업 확언 근거. 입고요청서(`41_입고요청서_CRM_BIZ_TARGET.md` + BRONZE DDL) 발행 → 현업 회신 대기. [2026-07-16] `FACT_TARGET_BIZ`+`WIDE_TARGET_BIZ` dbt 스켈레톤 저작(build green·0행). ✅**단위 확정=건(#152~155, 지표사전 기준)** → 해소조건: 현업이 '건' 목표 원천 제공. ✅ SILVER 테이블·dbt 모델 = `CRM_BIZ_TARGET`(리네임 완료, TARGET_CNT/TARGET_TYPE 구조). |
| 🔴 E-1·E-4/O3 | ERP/AGENCY | 모금성비용·광고비·캠페인/매체 연결키 | 원장에 없음 | `AGENCY_COST` 보강 대기. 캠페인별=대행사·매체별=ERP/CRM. 국내/해외 구분 보류 |
| 🔴 Q10 | ERP | 세세목↔캠페인 연결키(캠페인 ROI) | 연결키 없음 | 부서 grain 진행 가능·캠페인 ROI는 현업 매핑 대기 |
| 🟡 C-8/DEC-7 | 현업 별도 | 인바운드콜·TS콜 월간 수치(단위 콜/건) | 결정→입고(자리만 존재, 값 대기) | 현업 별도파일 입고 요청 |
| 🟡 E-5·A-9 | ERP/AGENCY/GA4 | 적재 시작시점·보유 범위(요건 2025~) | 실적재 후 확정 | — |
| 🔴 **AD-5** *(신규 2026-07-29)* | **AGENCY(대행사)** | **VIDEO 광고 개발실적**(개발건수·개발회원수) | **원천에 항목 자체가 없음** — `BRONZE_AGENCY.VIDEO_AD_CMPGN_DTLS` 에 개발 컬럼 부재(`AD_CNT`·`CONV_CALL_CNT`·`INBOUND_CALL_CNT` 뿐). 비디오 리포트는 개발 대신 **전환콜**을 보고한다. 반면 `REBRDC_AD_CMPGN_DTLS` 에는 `DVLP_CNT`·`DVLP_MBER_CNT` 존재(커버리지 96.03%) | ⚠️ **미적재가 아니라 구조적 부재** — 적재 독려로 해소되지 않는다. 대행사에 **비디오 일별 리포트에 개발건수 항목 신설**을 요청해야 한다. 미해소 시 **방송 광고비의 29%(₩6,215,838,012) 구간은 개발 효율(개발단가) 측정 불가**. 재방송분은 `REBRDC_DEV_UNIT_PRICE`(157,969원)로 **이미 산출 중**(DEC-14-A) → 요청 전까지는 "재방송 한정" 표기가 정상 상태 |
| 🟡 **CMP-1** *(신설 예고 2026-08-10 · O53)* | **CRM (현업 신설 예정)** | **캠페인 상위코드 + 공통코드 신규 코드군** — 광고 캠페인을 상위 계층으로 묶는 코드축 | 🔴 **광고의 캠페인 축이 사실상 죽어 있다(실측)**: `GOLD.WIDE_AD_PERFORMANCE.CAMPAIGN_PARENT` = **0 / 243,545 전건 NULL** · `CAMPAIGN_NAME` **distinct 1**(전량 단일 센티넬). 반면 `SILVER.CRM_CODE` 는 **336 코드군 / 5,838행** 으로 이미 존재하므로 신설분은 **코드군 추가**로 흡수된다 | ⚠️ **신규 결손이 아니다** — 기지 **O3·Q10**(캠페인/매체 연결키 부재)의 **해소 경로**다. 사용자 고지(2026-08-10): *"캠페인 상위코드와 공통코드도 만들어질 예정 … 다음주에 신설되면 바로 반영할 수 있으면 좋겠다"*.<br>🟢 **즉시 반영 가능하게 설계를 맞춰 뒀다**(O53): 광고 SV base 를 **물리 테이블이 아니라 `GOLD.WIDE_AD_COMBINED` dbt 뷰**로 두어 ① 중복 저장 0 ② 원천 변경 시 **뷰 정의 1곳 + `ref()` 위상정렬**로 반영 ③ SV 는 GOLD 만 참조. 반영 경로 = `CRM_CODE` 코드군 추가 → `DIM_CAMPAIGN`(또는 상위 축 차원) → `WIDE_AD_COMBINED` → SV_AD dimension.<br>⚠️ 입고 시 **fan-out 검증 필수**(상위코드가 1:N 이면 광고비가 팽창한다 · §1-6 P3 패턴) |
| 🔴 **HOL-1** *(신규 2026-07-30)* | **외부 (공휴일 캘린더)** | **한국 공휴일 일자 목록** (신정·설·삼일절·어린이날·부처님오신날·현충일·광복절·추석·개천절·한글날·성탄절 + **대체공휴일**) | **전 스키마에 원천 없음.** `GOLD.DIM_DATE.IS_HOLIDAY`는 dbt 모델이 `FALSE` **하드코딩**(`DIM_DATE.sql`: `FALSE as IS_HOLIDAY, -- ⚠️ 휴일 원천 없음(추후 보정)`) → 16,437행 **전건 FALSE**. `%HOLIDAY%`·`%HDAY%`·`%RESTDE%`·`%WORKDAY%` 전 스키마 검색 결과 BRONZE/SILVER 매칭 0 | **ML 정본 요건이 이걸 직접 요구.** 기획실 개발예측 **A안 3종 전부** = *"연도말까지 **주말(휴일)** 제외한 나머지 개발가능일수 반영"*. 주말은 `DIM_DATE.DAY_OF_WEEK`(정상 채움)로 산출 가능하나 **공휴일은 불가**.<br>조치 후보: ① **한국천문연구원 특일정보 OpenAPI** 연간 적재 ② 현업이 사내 근무 캘린더 제공(대체공휴일·창립기념일 등 사내 휴무 반영 가능)<br>⚠️ WIDE 6종(`WIDE_MEMBER_EVENT`·`WIDE_SERVICE_EVENT`·`WIDE_GA_BEHAVIOR`·`WIDE_EVENT_PARTICIPATION`·`WIDE_AD_BROADCAST`/`_DIGITAL`/`_PERFORMANCE`)이 이 컬럼을 상속 → 현재 전부 FALSE. **소비 측이 "휴일 아님"으로 오독할 위험.** 상세 = 문서50 §BLOCKING-5 확장 |
| 🔴 **C-9-R** *(재제기 2026-07-30)* | **CRM (메일/알림톡 발송 시스템)** | **발송 오픈 여부·오픈율** (회원 단위) | **C-9에서 원천을 폐기 확정**했다(문서90 §2: *"`SND_MEMBER_LIST.RESPONSED_YN`·URL_OTHBC 폐기. ⚠️2026-09 메일테이블 재검토"*). 실측: `GOLD.FACT_SERVICE_EVENT.OPEN_MEMBERS` **전건 0**(38,470,780행) · BRONZE `TD_MS_EMAIL_LQY_SNDNG.URL_OTHBC_CNT_CTNT`/`URL_OTHBC_RT_CTNT` **497,777행 전건 NULL** · `SILVER.CRM_SEND_RESULT.TOT_CLICK_CNT`도 이슈 D(100% NULL) | 🔴 **ML 요건 정본과 정면 충돌 → 2026-09 재검토 대기가 부적절.** 정본에서 '오픈'은 부가정보가 아니라 **예측 Feature**로 명시:<br>· 증액회원 예측 → *"예측 변수(Feature): **발송서비스 오픈율**"*<br>· 충성회원 예측 → *"참여 특성: **알림톡 오픈율** / **이메일 오픈율**"*, *"핵심요인: **서비스 오픈율**"*<br>· 중단 고위험군 예측 → *"발송받은 알림톡·메일 **오픈여부**"*<br>→ **ML 예측 3종이 이 원천에 의존.** 현업 재질의 = 문서20 신규 §F-3. 우선순위 상향 필요 |
| 🔴 **PST-1** *(신규 2026-08-11 · O59 · DEC-35 §23-H)* | **CRM (우편 발송 시스템)** | **우편 발송 결과(반송) — 회원 단위** | 🔴 **우편 3계층 전부에 결과코드가 없다(실측)**: 상세 `TD_MS_PSTMTR_SNDNG_DTL` **14컬럼 전수 — 결과코드 0개** · 요청 마스터 `TM_MS_PSTMTR_SNDNG` 의 `PRCS_STAT_CD` 는 **요청 처리상태**이고 회원 단위 발송 결과가 아니다 · 집계 `TD_MS_PSTMTR_LQY_SNDNG` 은 `SNDNG_CNT`(발신건수)만 보유하고 **`SUCCES_CNT`·`FAILR_CNT` 컬럼 자체가 없다**(EMAIL·MSG_AT·SND 3채널은 보유).<br>⇒ 그래서 `SILVER.CRM_SEND_MEMBER` 의 우편 branch 가 결과코드를 **`NULL` 하드코딩**하고(모델 15행) `FSE.SEND_STATUS` 가 우편 행에서 전건 NULL 이다 | 🟢 **컬럼이 생긴 이유는 업무 요건이 아니라 `UNION ALL` arity 다** — 4채널을 한 테이블로 합칠 때 결과코드 슬롯을 1개 두었고 우편만 채울 원천이 없다. 즉 **결손이 아니라 구조적 부재**(P21).<br>🔴 **그러나 요건은 실재한다** — 정본 **#86 성공수(명)·#87 실패수(명)** 은 채널 무관 요구이고, 신#31 발송대비 수신율도 우편에서 충족 불가다.<br>🟢 **사전은 이미 있다** — `SILVER.CRM_CODE` **`MS001`** = `1`정상·`2`주소불명·`3`이사·`4`수취거절·`5`수취인부재·`6`반송함투입·`7`폐문부재·`8`수취인불명 (우편 반송사유 8종). **담을 BRONZE 컬럼만 없다.**<br>⚠️ **AD-5 와 동일 유형** — 미적재가 아니라 **원천 항목 부재**이므로 적재 독려로 해소되지 않는다. 우편 발송 시스템에 **반송 결과 컬럼 신설**(권장 = `MS001` 코드 그대로)을 요청해야 한다.<br>📌 미해소 시 정상 상태 = **NULL 유지 + 「구조적 부재」 COMMENT 명시**(DEC-35 §23-F 결정 2 · 라벨 창작 금지 DEC-17-B) |

> D(원천 값 전무 컬럼) 판정은 문서20 §C-D. 그 중 `CRM_BIZ_TARGET`(SILVER 테이블명)은 여기 E-6와 동일 원인.
> **HOL-1·C-9-R**(2026-07-30 신규)은 **ML 요건 정본**(`99_provided_definition/데이터플랫폼 ML 예측관련_취합_20260730.xlsx`) 대조에서 발견. 종전에는 ML 요건 원본을 대조한 적이 없어 드러나지 않았다.

## 2. 데이터 기간 요건 (원본: 30_BRONZE 컨트랙트 §6)
| 원천/데이터 | 반영 기간 |
|---|---|
| CRM 개발데이터 | 1991~2026 |
| CRM 활동(미납·중단) | 2024~2026 (지표 25년~) |
| CRM 회비데이터 | 2021~2026 |
| CRM 서비스(참여) | 2024~2026 |
| ERP | 2025~2026 |
| AGENCY | 2025~2026 (개발단가 최근 2개년) |
| GA4 | 확인 필요(G-5) |

## 3. 입고 완료 시 자동 해소되는 다운스트림
- **회원 마스터 전량입고** → dbt BLOCKING-1(참조무결성 warn→error 복귀, 15건)·의심데이터 B(고아 9,248명) 자동 해소 (문서50·문서20-B).
- **GA4 전기간 입고(G-5)** → GA4 트랙 본착수 + identity **재검증**. (현재 ID-활성은 2026-07-15 **1일 샤드 기반으로 배선완료**·매칭 1,274명 — `DIM_MEMBER_IDENTITY` 활성·`FACT/WIDE_GA_BEHAVIOR` 실조인. G-5 시 채움률 실증·재실행 필수. 문서30·50)
- **AGENCY 광고 개발건수 재제공(AD-3)** → 2026-06부터 원천이 개발건수 대신 단가를 제공하는 포맷으로 변경 → **개발단가(공7) 2026-05 이후 산출 불가**. 대행사에 개발건수 재제공 요청 시 시계열 연속성 회복(대안: 대행사 산정 단가 채택 — 정의 상이로 이전 기간과 비교 불가). 문서20 §E-AD-3 · 문서10 §9-D
- ~~**AGENCY 방송 개발실적 전량(AD-1)** → 현재 `DVLP_CNT` 커버리지 **5.2%**(1,982/37,886) → 방송 개발단가(공8) **SV 미노출**. 전체 편성분 입고 시 지표 활성화 가능. 문서20 §E-AD-1 · 문서10 §9-B~~
  → 🟢 **[정정 2026-07-30] AD-1은 오진 철회·해소됨(2026-07-29, 순서9-K).** 위 5.2%는 **범주 오류**(비디오를 분모에 포함). 올바른 모집단인 **재방송 단독 커버리지 = 96.03%**(1,982/2,064)이고 정합 왜곡 0.61%. 지표는 `REBRDC_DEV_UNIT_PRICE`(157,969원)로 **복원·노출**됨(공8 P2→**P1**). 정본 = 문서90 §1 AD-1 · 문서20 §E(취소선 처리) · 문서30 DEC-14-A. **본 문서만 미반영 상태였음** → 파생 신규 이슈는 아래 **AD-5**(VIDEO 개발실적 구조적 부재).

### paid 계정 게이트 (원천 무관 · 플랫폼 제약)
- **Agent NL 스모크·성능 측정** → 트라이얼 계정에서 `DATA_AGENT_RUN`(cortex_agent_query) **차단**(순서9-E 6-C). SV 데이터층 ground-truth(`SELECT ... FROM SEMANTIC_VIEW`)는 트라이얼에서도 검증 가능해 **정확성은 확인 완료**이나, **자연어 질의 응답 품질·응답시간·비용은 paid 계정에서 측정 필요**.
  - 대상: SV 6종(`SV_AD` 포함)·Agent 2종(`AGENT_OVERALL` analyst_ad 편입분 포함).
  - 절차 정본 = `05_SV-Agent_ai/12_paid_테스트_실행가이드.md` · 평가셋 = `07_평가셋_eval.md` · 검증문항 = `10_SI연결_검증.md §3`.
  - ⚠ 이관 전 선행 = `05_SV-Agent_ai/13_SV_AD_배포_추가작업.sql`(소유권 이전·AGENT_OVERALL 재배포) 완료 후 스냅샷 이관.

## 4. BRONZE 스테이지 업로드 기준값 (2026-08-18 O85 등재 · 사용자 제공)

> 🔴 **용도 = 신 계정 업로드 후 대조용 기준값이다.** 사용자 제공 원문 주석 =
> *"2026-08-18 실행 결과 (기준값 — C 업로드 후 이 값과 대조한다)"*.
> ⚠️ **측정 계정은 구 계정**이고 현 계정(**`UA93987`**)에는 **스테이지 0 · 테이블 0** 이다(O85 실재 확인).
> ⇒ 이 표는 **목표치**이지 현 상태가 아니다. 업로드 후 재측정해 **폴더·파일·바이트 3축을 전건 대조**한다.

| 스테이지 | 폴더 | 파일 | 바이트 |
|---|---|---|---|
| `BRONZE_AGENCY` | 4 | 5 | 4,129,568 |
| `BRONZE_BIGQUERY` | 911 | 6,517 | 63,725,277,056 |
| `BRONZE_CRM` | 45 | 281 | 3,252,000,576 |
| `BRONZE_ERP` | 1 | 1 | 55,888 |
| `ML` | 16 | 54 | 30,304,368 |
| **합계** | **977** | **6,858** | **67,011,767,456** (약 62.41 GiB) |

> 🟢 **합계 검산 = 자체 정합**(O85 재계산):
> · 폴더 4+911+45+1+16 = **977** ✅
> · 파일 5+6,517+281+1+54 = **6,858** ✅
> · 바이트 4,129,568+63,725,277,056+3,252,000,576+55,888+30,304,368 = **67,011,767,456** ✅
> · 67,011,767,456 / 1024³ = **62.41 GiB** ✅
>
> 🔴 **`BIGQUERY` 가 파일 95.0% · 바이트 95.1% 를 차지한다** — 업로드 시간·비용의 지배 요인이다.
> 🟠 **`ML` 스테이지는 BRONZE 4종과 별개 축이다**(예측 입력·산출). 대조 시 누락하지 말 것.
> 🔴 **현 계정에 스테이지가 0개다** ⇒ 업로드 전에 **스테이지 5종을 먼저 생성**해야 한다.
>   `02_GN_DW_building/07_ENVIRONMENT_RBAC_setup.sql` 은 스키마·역할까지만 만든다(O85 실측).
> 🔴 **파일 수 일치만으로 「업로드 완료」로 닫지 마라** — 바이트 축까지 대조한다(`R2-8-4`).

---
_Co-authored with CoCo_
