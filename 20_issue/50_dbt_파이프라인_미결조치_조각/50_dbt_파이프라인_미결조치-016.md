<!-- SPLIT-CHUNK 50_dbt_파이프라인_미결조치.md | 016/024 | 허브 = 50_dbt_파이프라인_미결조치.md | 원문 2141~2320행 -->
<!-- 🔴 이 파일은 원문 무변경 조각이다. 편집은 허브 계약을 따른다 (scripts/split_doc.py --verify 로 바이트 동일성이 검사된다). -->
<!-- BODY-BEGIN (아래는 원문 무변경 · 편집 금지) -->
## 🟢 [2026-08-06 O43] BLOCKING-5 전수 재측정 — 미주입 106/386(27.5%)을 컬럼 단위로 확정

> 🆕 🟠 **[2026-09-16 O167 라이브 전수 재측정 · 열림 유지] 아래 O43 표는 2026-08-06 판본이다.**
> 근거철 = `20_issue/_o167_evidence.md` **§E3**(컬럼 단위 **전량 목록 포함**).
> · 실측 = GOLD 기본테이블 **37** · DATA 컬럼 **598** · 주입 **503** · **미주입 95**
>   (전건 `0` **59** · 전건 `NULL` **28** · 0행 테이블 `FACT_TARGET_PROJECT` **8**) · 비중 **15.9%**.
> · 🔴 **106 → 95 를 「11건 해소」로 읽지 마라** — 분모가 **386 → 598** 로 다르다(그 사이 GOLD 가 확장됐다).
>   🟢 **비교 가능한 것은 비중**이고 같은 판정식으로 **27.5% → 15.9%** 다.
> · 🟢🟢 **죽은 포인터를 복원했다** — 종전 「컬럼 단위 전량 목록」 좌표
>   (`30_output_share/_archive/20260806/02_원천결손_Gap분석.md` §부록)는 **아카이브로 이관돼 공유 폴더에 없었다**.
>   이제 `_o167_evidence.md` §E3 에 **라이브 실측 95건 전건**이 테이블별로 있다(배정 누락 0).
> · 🔴 **판정식은 그대로다** — `COUNT()` 만으로는 **0 상수 주입을 「적재됨」으로 오판**한다.
>   반드시 `COUNT_IF(<컬럼> is not null and <컬럼> <> 0)` 로 센다.
> · 🟠 **왜 아직 열려 있나** = 버킷 **B(원천은 있으나 산출 로직 미구현)** 는 내부 착수 가능하지만 대부분
>   **규칙 확정 선행**(CONF-2·CONF-3)이고, **A/F** 는 외부 입고 대기다(성격 불변).

> 종전 BLOCKING-5 표(아래)는 FACT 5종만 대상이었고 `0/NULL` 을 한 칸에 묶었다.
> O43 에서 **GOLD 전 기본 테이블 전 컬럼**에 `COUNT`/`COUNT_IF(<>0)` 를 돌려 전수 확정했다.
> ⚠️ `COUNT()` 만으로는 **0 상수 주입을 「적재됨」으로 오판**한다 — 반드시 `COUNT_IF(<>0)` 로 판정할 것.

| 구분 | 건수 | 비중 |
|---|---:|---:|
| GOLD DATA 컬럼(감사컬럼 `DW_*` 제외) | **386** | 100% |
| 값 주입됨 | **280** | 72.5% |
| 전건 `0` | **62** | 16.1% |
| 전건 `NULL` | **36** | 9.3% |
| 0행 테이블(`FACT_TARGET_PROJECT`) | **8** | 2.1% |
| **미주입 합계** | **106** | **27.5%** |

**원인별 분류 (전량 배정 · 누락 0)**

| 버킷 | 건수 | 해소 주체 |
|---|---:|---|
| A 원천에 항목 자체가 없음 | 18 | 🔴 외부 입고(E-6·E-1/E-4·C-9-R·HOL-1·AD-5·C-8) |
| **B 원천은 있으나 산출 로직 미구현** | **51** | 🟡 **내부 구현 — 외부 의존 0** |
| C 코드체계 미확정 | 11 | 🟠 현업 확인(O28) |
| D 연결키 부재 | 14 | 🔴 연결키 회신(Q10·O8) · 🟢 **[2026-08-29 O112 병기]** `O8` 은 **후원사업 축만 `DEC-41` 로 닫혔다** — 이 14건의 차단은 **캠페인·결제 축**이므로 **여전히 열려 있다**(부분 종결을 종결로 읽지 마라) |
| E 산출규칙 미확정 | 4 | 🟠 현업 확정(CONF-4) |
| F 적재 범위·시점 | 8 | 🔴 입고 범위 확대(G-5) |

🆕 🟢🟢 **[2026-09-21 O174 등재] 버킷 `A-R` 신설분 등재 — 위 표(분모 386 · 2026-08-06 O43)는 그대로 둔다**

🔴 **분모가 다르다 — 위 표를 고치지 않는다**(`㉡` 원집합 ↔ 현 잔여를 섞지 마라).
위 표 = **2026-08-06 O43 시점 · 분모 386** · 아래 = **2026-09-17 O170 전수 재실측 · 분모 598**
(테이블 37 · `_o170_evidence.md` §E4·§E8-1이 정본이고 여기는 등재다).

| 축 | O43(위 표) | 🆕 O170 재실측 |
|---|---:|---:|
| GOLD DATA 컬럼 | 386 | **598** |
| 미주입 | 106 (27.5%) | **95 (15.9%)** — 전건0 **47** · 전건NULL **40** · 0행 **8** |
| 원인 배정 | A~F 6버킷 | 기존 배정 **45** + **B 45** + 🆕 **A-R 5** = **95** · 미배정 **0** |

🆕 **버킷 `A-R` 정의** = 원천에 **컬럼은 있고 값이 비었다**(회귀·미보고) ⇒ 처방 = **원상 복구 요청**.
🔴 버킷 `A`(항목 자체가 없음 ⇒ **신설 입고 요청**)와 **처방이 다르다** — `A` 로 뭉개면
「없는 것을 만들어 달라」고 요청하게 된다. 🟢 판정식 = **「부재」와 「회귀에 의한 부재」는 다른 사건이다.**

**`A-R` 5건 전수 · 🔴 두 갈래로 갈라서 묻는다**(2026-09-21 O174 라이브 재측정 · 계정 `RY53492`)

| 갈래 | GOLD 컬럼 | 원천 컬럼 | 원천 실측 | GOLD 실측 |
|---|---|---|---|---|
| **A-R-1 AGENCY**(회귀 실증) | `FACT_AD_BROADCAST.CONV_CALL_CNT` | `VIDEO_AD_CMPGN_DTLS.CONV_CALL_CNT` | non-NULL **0 / 37,674** | 0 / 39,823 |
| 〃 | `FACT_AD_DIGITAL.MEDIA_POTENTIAL_CUST_CNT` | `DGT_AD_CMPGN_DTLS.MEDIA_PTNT_CUST_CNT` | non-NULL **0 / 203,138** | 0 / 203,138 |
| 〃 | `FACT_AD_DIGITAL.CRM_DEV_CNT` | `DGT_AD_CMPGN_DTLS.CRM_DVLP_CNT` | non-NULL **0 / 203,138** | 0 / 203,138 |
| **A-R-2 ERP**(미발생 가능 · 회귀 아닐 수 있다) | `FACT_BUDGET_YEARLY.CHN_BUDGET_YEAR` | `BDGT_ACMSLT_LEDGER.CHN_BDGT_TOT_AMT` | non-NULL **258** · non-zero **0** | non-NULL 111 · non-zero **0** |
| 〃 | `FACT_BUDGET_YEARLY.ADJ_BUDGET_YEAR` | `BDGT_ACMSLT_LEDGER.ADJ_BDGT_TOT_AMT` | non-NULL **258** · non-zero **0** | non-NULL 111 · non-zero **0** |

· 🟢 **A-R-1 은 문서20 §N-14 로 문항 발행됨**(2026-09-21 O174) — 대조군 정상(`GA_CONV_MBER_CNT` 202,739 ·
  `CONV_VU_CNT` 203,063 · `GA_AD_COST` 203,133) ⇒ 테이블 전체가 아니라 **이 컬럼만** 비었다.
· 🔴 **A-R-2 를 A-R-1 과 한 문항으로 묻지 마라** — ERP 는 값이 **NULL 이 아니라 전건 0** 이고
  「추경·조정 미발생」이면 **정상 상태**다(회귀가 아니다). ⇒ 별건으로 남긴다.

🆕 🔴 **`CPC`·`CPA` 축 판정 = `A-R` 에 넣지 않는다**(2026-09-21 O174 판정 · 라이브 근거)

| 판정 축 | 실측 | 결론 |
|---|---|---|
| 분모 자격 | `GOLD.FACT_AD_DIGITAL.CPC_SRC`·`CPA_SRC` = **9,080 / 203,138 부분 채움** | 🟢 미주입 판정식(전건0 · 전건NULL · 0행)에 **해당하지 않는다** ⇒ 미주입 95 **분모 밖**이므로 버킷 배정 대상이 아니다 |
| 성격 | 신방식 구간(2026-06-01~08-20 · 9,080행) **전건 채움** ↔ 구방식 구간(2024-01-01~2026-05-31 · 194,058행) **전건 0** | 🔴 **회귀가 아니라 원천 포맷 전환**이다 ⇒ 성격은 버킷 **F(적재 범위·시점)** 에 가깝다 |
| 종전 신호 | 「`CPC` 채움 9,080 = `DATE` 실값 9,080 과 정확히 같다 ⇒ 같은 재적재가 둘을 함께 비웠다」 | 🔴 **기각** — 같은 건수는 **포맷 경계**의 결과다. 🟢 판정식 = **「건수 일치」는 인과의 증거가 아니다** |

⚠️ 같은 경계에 `DEV_UNIT_PRICE`·`CPM`(9,080) · `CTR`·`CVR`(7,038 · 신방식 안에서도 부분)이 함께 있다
⇒ 문서20 §N-14 ④-1·④-2 로 **소급 공급 가능 여부**를 물었다.

🔴 **B 가 최대(48.1%)이고 외부 의존이 0 이다.** 종전 문서는 이 구분 없이 「원천 결손」으로 묶어 전부
외부 대기로 보이게 했다. B 착수의 선결조건은 대부분 **규칙 확정 1건**이다:
- ~~활동회원 8종 → **CONF-3**(정본 #51 판정 조건 내부 모순) 현업 확인~~
  🔴🔴 **[2026-09-16 O169 실측 정정] 이 줄은 두 축이 동시에 stale 이다.** 근거철 = `_o169_evidence.md` §E1.
  · 🟢 **`CONF-3` 은 닫혔다** = **2026-09-07 O141 현업 회신 확정**(「재후원 우세 · 후원사업 유효 기준」 ·
    당월 말 `SPNSR_DSCNTC_YN='N'` 후원사업 1개 이상 보유 시 월말활동회원) ⇒ 정본 = `DEC-47 §33-B`
    (`30_설계_의사결정-015.md:157`) · 회신 절 = 문서20 §N-11(`20_현업확인_요청-007.md:240`).
    선행 2단계 = `O93-B`(「모순」 전제 자체가 오류 · 층위 구분) → `O95`(「방향 상반」 판정 철회).
    ⇒ 🔴 세 기재는 **상충이 아니라 한 축의 3단계**다(기술 해소 → 물음 폐기 → 업무 규칙 확정).
  · 🔴🔴 **「8종 미주입」도 stale 이다 — 6종은 이미 적재됐다**(라이브 실측 2026-09-16 · 41,508,842행 ·
    판정식 `COUNT_IF(<>0 AND NOT NULL)`): `ACTIVE_CNT` **39,854,326**(96.01%) ·
    `ACTIVE_MEMBERS` **39,854,335** · `MONTH_END_ACTIVE_CNT` **39,854,326** ·
    `PREV_MONTH_END_ACTIVE_CNT` **39,122,763**(94.25%) ·
    `YEAR_END_ACTIVE_CNT` **37,676,363**(90.77%) · `YEAR_START_ACTIVE_CNT` **36,847,772**(88.77%).
  · 🔴 **잔여는 누계 2종뿐이고 차단 근거가 `CONF-3` 이 아니다** = `ACTIVE_CUM_CNT`(#159) ·
    `ACTIVE_CUM_MEMBERS`(#158). 진짜 근거 = **버킷 C 「정본 정의 부재」 · 슬롯 유지 · 정의 창작 금지**
    (`30_설계_의사결정-002.md:78`·`:155`) ⇒ 내부 착수 불가 · **결정 문항 `D12` 로 승격**.
    🔴 선례 = 이 두 컬럼에 「누계」 정의를 붙이려던 시도는 `_o159_mstr_impact_measure.md:141` 에서
    이미 **🔴 창작**으로 판정됐다.
  · 🔴 **「전건 0」이 아니라 「전건 NULL」이다**(그릇 정정 · `J2`) — non-null **0**건 · `MAX()` **NULL**.
    종전 기재 *"6종 40,054,883행 전건 0(NULL 아님)"*(`01_세션이력-022.md:80` · `10_진단_원인분석-017.md:179`)
    은 **그 시점에 참**이었고, 그 뒤 모델이 `CAST(NULL AS …)` 로 바뀌었다
    (`FACT_MEMBER_MONTHLY.sql:318~319`) ⇒ 🟢 버킷 집계에서 이 2건은 **전건NULL** 쪽이다(전건0 쪽이 아니다).
  · 🟢 **판정식(신설) = 「묶음의 차단 근거는 묶음 단위로 상속되지 않는다」** — 묶음 표기는
    **최초 등재 시점의 최대공약수**일 뿐이다. ⇒ 진척은 총계가 아니라 **집합 차이**로 적는다
    (해소 6 · 잔여 2 · 신규 0). 🔴 음성 축 = **일부 적재를 근거로 묶음을 「해소」로 닫지 마라.**
- 미납 카운트 3종 → **CONF-2**(`(건)` = 건수인가 금액÷10,000인가)
- 발송 성공/실패 → 채널별 성공/실패 코드매핑 업무 확정
- 증액·감액·이탈 4종 / 서신·선물금 8종 / 미납중단 2종 / 회비 분해 4종 → ~~**선결조건 없음(즉시 착수 가능)**~~
  🔴🔴 **[2026-09-16 O168 실측 정정] 이 한 줄은 stale 이고 18종 중 2종만 참이었다.** 착수하려고 컬럼을 실제로 특정하니:
  · 🟢 **미납중단 2종**(`FACT_MEMBER_EVENT.UNPAID_STOP_CNT`·`_MEMBERS`) = **참** ⇒ `DEC-54` 로 배선했다(사유명 「미납」 술어 · 미납 계열 3종 389,010/1,061,431 = 36.65% · 회원 336,334 · 사유명 NULL 0). 🟢 **[2026-09-16 대사 완료]** 빌드 `PASS=524 ERROR=0` 후 실측 = **389,010 / 336,334 / 전체중단 1,061,431 · 부분집합 위반 0 · 건·명 쌍 불일치 0 · 하류 `WIDE_MEMBER_EVENT` 389,010** ⇒ `DEC-54` 🟢 종결. GOLD 미주입 **94/598(15.7%)**.
  · 🔴 **서신·선물금 8종** = **거짓**. 4종이 `FACT_MESSAGE_DISPATCH.D5_LETTER_PART_*`·`D5_GIFT_PART_*` 이고 **`DEC-33` 이 차단**했다(코호트 **모집단 정의** = 현업 확인 3건 · 문서20 §L-1). `DEC-33` 성격 변경일 **2026-08-07** = 이 버킷표(**2026-08-06 O43**) **하루 뒤**다.
  · 🔴 **증액·감액·이탈 4종** = **거짓**. 모델 자신이 `10_dbt_pipeline/models/gold/fact/FACT_MEMBER_MONTHLY.sql:26` 에서 `INCREASE_CNT`(#151)·`DECREASE_CNT`(#38)를 **`CONF-2` 혼용 금지**로 묶어 두었다.
  · 🟠 **회비 분해 4종** = **미확정**(같은 `CONF-2` 단위 축).
  🔴 **같은 절 안에서 자기모순이었다** — 아래 §BLOCKING-5 `:165` 는 *"입고 대기(②/외부)는 소수(… **D5 코호트** …)"* 라고 적어 D5 를 **반대로 분류**한다.
  🟢 **교훈** = 「선결조건 없음」은 상태가 아니라 **그 시점의 판정**이다. 묶음 이름으로 세지 말고 **그 묶음의 컬럼을 특정해 각 컬럼의 차단 근거를 다시 재라**(`O111 ㉠` 계열 · 정본 = 문서30 `DEC-54` §40-E).

**컬럼 단위 전량 목록** = `30_output_share/_archive/20260806/02_원천결손_Gap분석.md` §부록
  ⚠️ **[2026-08-13 O65 경로 정정]** 이 문서는 O59-T 판본 교체 때 아카이브로 이관됐고 **공유 폴더에 없다**(종전 경로는 죽은 포인터였다).
  **수기 문서라 재생성 대상이 아니며**(생성기 없음) 인용 시 **측정일 2026-08-06 을 병기**한다 —
  🔴 분모 구성이 현행과 다르다(그 문서 「105/411」 vs 아카이브 `00_README_이관안내.md` 실측 「108/492」) ⇒ 두 수치를 개선·악화로 읽지 말 것.
**재현 방법** = `python3 scripts/dump_schema.py && python3 scripts/census_columns.py`

---

## 🔴→🟡 BLOCKING-5 — GOLD 팩트 measure·차원FK 대규모 미적재 (SV/Agent 착수 시 실측 발견 2026-07-21 · A1/A3 부분해소 진행중)
> SV 설계(05_SV-Agent_ai 2단계) 중 **배포된 GOLD 실데이터**를 컬럼별 실측(`COUNT_IF`)한 결과, 행수는 있으나 **대부분의 카운트 measure·차원 FK가 전건 0/NULL**. 기존 Item D("원천 값 전무 5종")·D1(스캐폴드)·E(고아)의 범위를 **초과**하는 미적재. Cortex Analyst SV가 이 위에 서면 0/오답을 자신 있게 반환 → **배포 전 원인규명 필수**.
> 🟡 **[2026-07-21 진행] A1·A3 착수** — 입고된 SILVER 데이터로 채울 수 있는 것부터 구현(아래 §BLOCKING-5 진행분).

| FACT | ✅ 적재(활성 가능) | ❌ 전건 0/NULL (미적재) |
|---|---|---|
| FMM 37.8M→40.05M | PAID_FEE(36.09M)·BILLED_AMT·UNPAID_FLAG_BOM/EOM · ✅**DEV/STOP 건·명(A1: FME 롤업)** · ✅**HAS_BILLING(A1 출처플래그)** | UNPAID/ACTIVE/CUM/MONTH_END/YEAR_*·INCREASE 건·명·CAMPAIGN_UNPAID·STATUS_UNPAID·INBOUND/TS_CALL·REGULAR_FEE·SPONSOR/PAID_MONTHS·DEV_TYPE·밴드·JOIN_DATE·NEW_FLAG·NEW_EXISTING_FLAG·**CAMPAIGN/PAYMENT_SK** · ~~**SPONSORSHIP/REASON_SK**~~ → 🔴 **[2026-08-31 O125-B 실측 정정] 이 두 축은 「전건 0」이 아니다**: `SPONSORSHIP_SK` **35,785,665/40,054,883**(89.34% · O118 이 먼저 반박) · `REASON_SK` **3,164,724/40,054,883**(7.90% · **이번에 처음 적발**) ⇒ 🔴 미적재가 아니라 **부분 적재**이므로 한정 표기 없이 「전건 0」으로 인용하지 마라(이 절 말미 교훈의 자기위반이었다) |
| FME 4.6M | DEV_CNT/MEMBERS·JOIN_DATE(3.59M)·STOP_CNT/MEMBERS/STOP_DATE(1.04M) · ✅**CAMPAIGN_SK(DEV 한정, 2026-07-30 실측: DEV 3,594,825/3,594,843 · 16,318종)** · ✅**SPONSORSHIP_SK(DEV 한정 · 2026-08-06 O45 배선 · 2026-08-14 O73-C 실측 3,594,843/3,594,843)** | UNPAID_STOP·~~**ORG_SK**~~ → 🔴 **[2026-08-31 O125-B 실측 정정] `ORG_SK` 는 DEV 한정으로 채워져 있다** = **3,594,835/4,633,105**(O38 배선분) ⇒ 한정 표기 누락이었다. 🔴 STOP 분기는 여전히 0 이다 · **CAMPAIGN_SK(STOP 한정 — 원천 `CRM_MEMBER_DISCONTINUE`에 캠페인 컬럼 부재)**·**SPONSORSHIP_SK(STOP 한정 — 🔴 사유는 「원천 부재」가 아니다 · 아래 O73-C 주 참조)**·**REASON_SK·NEW_EXISTING_FLAG** |
| FSE 38.5M | SEND_MEMBERS(전행)·SEND_STATUS(35.75M) · ✅**SERVICE_SK(A3: 요청마스터 조인, 99.97%)** · ✅**SEND_TITLE(A3)** | SUCCESS/FAIL/OPEN/LETTER/GIFT·**D5_\*(증액·서신·선물·중단)**·**CAMPAIGN_SK**(원천 캠페인 컬럼 부재) |
| FEP 1.1M | MEMBER_DK·PARTICIPANT_CNT·EVENT_SK(76.8%) | TOTAL/RECRUIT_CNT·CAMPAIGN_SK·SPONSORSHIP_SK |
| FBD 24.5K | BUDGET_ITEM_SK(전행)·PLAN_BUDGET_MONTH(7,290)·EXEC_BUDGET_ERP(3,244) | PLAN_BUDGET_YEAR·FUNDRAISING_COST·AD_COST·CAMPAIGN_SK·ORG_SK |
| FDA 37.5K (FACT_MEMBER_DEV_ACHIEVEMENT) | MONTH_KEY·DEV_TYPE·GOAL_CNT(합 4.62M)·ACTUAL_CNT(합 2.29M)·ACHV_RATE · ✅**DEPT_ID/부서명(원천 확보)** | ORG_CORP(0/37,522)·ORG_DIVISION(0/37,522)·ORG_TEAM(0/37,522)·ORG_DEPARTMENT(0/37,522) (🔴 조직 계층 컬럼 전건 0/NULL · ⚠️ 원천 결손이 아니라 DIM_ORG 계층 도출 로직 미구현 · 부분 적재 한정 표기 필수) |

- **영향**: 캠페인별/조직별/서비스구분별/납입방식별/신규기존별 지표, 활동·개발·중단 카운트 비율, 목표대비(공1~3), 서비스 수신/참여/증액/코호트(신31~53) = **전부 계산 불가**. Phase-1 실활성 = 납부율·미납회원감소·납입/청구/예산(세세목)·개발/중단 총건·유지기간·발송수·참여자수뿐.
- **원인 후보(규명 필요)**: ① dbt gold.fact 모델이 해당 measure/FK 매핑 미구현(스캐폴드 컬럼 잔존) ② SILVER 원천 컬럼 공란(=Item D 확장) ③ FK 조인 미결선(전건 센티넬 0). → **GOLD/ETL 담당 확인**: 의도된 NULL인가 vs 적재 로직 누락인가.
  - 🟡 **[2026-07-21 규명 결과]**: 원인은 대부분 **①(스캐폴드 컬럼 잔존, 로직 미구현)** — SILVER 원천은 입고돼 있음(FME dev/stop·CRM_SEND_REQUEST 등). 입고 대기(②/외부)는 소수(INBOUND/TS_CALL=C-8·FTG_BIZ=E-6·D5 코호트·FSE CAMPAIGN_SK[원천 캠페인 컬럼 부재]).
- **조치**: (a) 좁은 Phase-1 SV는 실적재 컬럼만으로 배포(진행), (b) 본 항목 원인규명 후 measure/FK 적재 → SV metric 순차 활성(구조 DDL 불변). 
- **검증 재현**: `SELECT COUNT_IF(<col><>0) FROM GN_DW.GOLD.<fact>` (상세 매트릭스 = `05_SV-Agent_ai/04_SV_설계.md §0.6`).

### 🔴🔴 [신규 2026-08-14 O73-C] BLOCKING-5 표 정정 — `FME.SPONSORSHIP_SK(STOP)` 의 미적재 사유가 틀렸다

> **왜 지금 나왔나**: O73 이 Agent 의 「후원사업변경+감액 산출 불가」를 반박하려고 FME 를 실측했는데,
> 그 과정에서 위 표의 `SPONSORSHIP_SK` 기재가 **DEV/STOP 을 구별하지 않고 있었다**는 것이 드러났다.

**① 표 기재는 절반이 stale 이었다** — 라이브 실측(2026-08-14):
- FME 전체 **4,633,105**행 중 `SPONSORSHIP_SK<>0` = **3,594,843**(77.6%) = **DEV 전건**
- **STOP 1,038,262행은 전건 0** ⇒ 「전건 0/NULL」은 **STOP 에만 참**이다
- 배선 시점·주체 = `models/gold/fact/FACT_MEMBER_EVENT.sql` **179행** 주석 `[2026-08-06 O45] 후원사업 축 실배선`
- STOP 분기는 같은 파일 **232행** 이 `0 as CAMPAIGN_SK, 0 as SPONSORSHIP_SK` 로 하드코딩한다

**② 🔴 STOP 측 후원사업 원천은 존재한다 — 「원천 부재」는 캠페인 축의 사유이고 후원사업에는 적용되지 않는다**
- 위 표의 `CAMPAIGN_SK(STOP 한정)` 주석 *"원천 `CRM_MEMBER_DISCONTINUE`에 캠페인 컬럼 부재"* 는 **캠페인에만 맞다.**
  그 원천(`TM_MM_FDRM_MBER_SPNSR_DSCNTC`)에 후원사업 컬럼도 없는 것은 사실이나, **다른 원천에 있다.**
- 실측 경로 = `TM_MM_FDRM_MBER_SPNSR`(약정 헤더) × `TM_MM_FDRM_MBER_SPNSR_BSNS`(`SPNSR_DSCNTC_YN='Y'` · `SPNSR_DSCNTC_DE`)
- 도달률 = 중단 사건의 (회원, 중단일) 키 **970,486 / 973,207 = 99.72%** · 후원사업 **27종**

**③ 🔴 그러나 그대로 조인하면 팬아웃한다 — 이것이 진짜 미적재 사유다**
- matched pairs **1,517,797** / 키 **970,486** = **1.56배** ⇒ **한 중단일에 여러 사업이 동시 중단된다**
- ⚠️ O45 주석 180~181행의 *"사건 grain 에서는 그 사건의 후원사업이 하나로 확정되므로 (O8) 귀속 규칙이 필요 없다"* 는
  **DEV 에만 참**이다. STOP 에 그대로 적용하면 **O8 과 같은 유형의 팬아웃**이 조용히 생긴다.
- ⇒ 🔴 **정확한 미적재 사유 = 「원천 부재」가 아니라 「동시중단 다중사업 귀속 규칙 미확정」이다.**

**④ 조치 — 1차 분해 배선 완료 및 현업 질의 상정 (2026-09-14 O160)**
- **[O160 실측 재분해]** 팬아웃 1.56배 기재는 날짜 일치 없는 회원단위 조인(1.67배)의 과대표기였으며, 정상 중단일 조인은 1.1020배다. 차단 대상의 92.45%(959,872행)는 `DISTINCT_BIZ=1`로 룰 없이 고유 확정된다.
- **[1차 배선 완료]** `FACT_MEMBER_EVENT.sql` STOP 브랜치에 `stop_single_biz` CTE를 신설하여 **단일사업 중단 959,872행(92.45%) 1:1 배선 완료 (팬아웃 0 · Grain 불변)**.
- **[현업 질의 등재]** 잔여 다중사업 건(75,660행, 7.29%) 및 미매칭(2,730행, 0.26%)은 0 센티넬을 유지하고 문서20 **§N-13** 현업 질의 상정. FME 전체 행수(4,633,105) 및 STOP 행수(1,038,262) 100% 보존.
- 🟠 **B 계열에 편입** — 아래 `B2`(FMM SPONSORSHIP_SK)와 **같은 뿌리(O8 귀속 규칙)**이나 **다른 grain**이다. B2 는 월×회원 grain 이고 이 건은 사건 grain 이다 ⇒ 규칙을 하나로 합치지 말 것.

> 🔴 **이 절이 남긴 교훈** — 위 표는 「미적재」 열에 컬럼명만 적어 **DEV/STOP 처럼 부분 적재된 축을 표현할 수 없었다.**
> `CAMPAIGN_SK` 는 `(DEV 한정)`·`(STOP 한정)` 표기 관례가 이미 있었는데 `SPONSORSHIP_SK` 에는 적용되지 않았다.
> ⇒ **부분 적재 축은 반드시 한정 표기를 붙인다.** 붙이지 않으면 다음 세션이 「전건 0」으로 읽거나(O73 이 그랬다)
> 반대로 「해소됐다」고 뭉갠다.
