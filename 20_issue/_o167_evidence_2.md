<!-- LLM-METADATA
doc_id: O167_EVIDENCE_PART2
doc_role: O167 근거철 **속편** — §12-C 즉시가능 3건 집행(06 재생성 · ㉗ 축㉢ · 개명 축1)
project: GN_DW (굿네이버스)
created: 2026-09-16
created_by: O167
index: 20_issue/00_INDEX_이슈원장.md
END-METADATA -->

# `_o167_evidence_2.md` — O167 근거철 속편 (§12-C)

> 🔴 **본편 = `20_issue/_o167_evidence.md`**(§0~§13). 이 파일은 그 §12-C 를 **무변경 이관**한 것이다.
> 🔴 **왜 나눴나** = 본편이 **41,196 B** 로 조각 상한 **40KB** 를 넘어 `doc_type_gate` 축2 가 막았다.
>   근거철은 **정적 발행물**이라 허브+조각 구조를 만들 이유가 없다 ⇒ **파일 분리**를 택했다
>   (O167 이 스킬 정본에서 쓴 것과 같은 판정식).

## ▣ 12-C. 🟢 즉시 가능 3건 집행 (사용자 지시)

### C1. `06` 「노출됨 −1」 축 — 🟢 **종결**

🟢 재생성 = `python3 scripts/run_bronze_audit_host.py`(무인자 = 라이브 직접조회) **rc=0** ·
스냅샷 **3종 선행**(`md`·`csv`·`xlsx`) · `test_generators` **rc=0 · 골든 30지표 일치**.
🟢🟢 **신규 산출물 ↔ 직전 판본 SHA256 동일**(`47f95b80484e679e` · 161,440 B · 차이 **0줄**)
⇒ **재생성이 완전히 결정적**임을 실증했다.
🔴 과거 −1 **그 1건은 원리적으로 귀속 불가**다(이전 판본 사본이 없다 · 변함없음).
🟢 **그러나 이 항목이 남긴 축은 닫혔다** — 「다음 갱신 때 잡는다」의 전제였던 **스냅샷이 배선됐고**
재생성이 바이트 동일이므로 **미귀속 차이가 새로 생기지 않는다**.
🟢 **판정식** = 「귀속 불가한 과거」와 「재발 방지 장치」는 다른 축이다 — **후자를 닫으면 항목이 닫힌다.**

### C2. 착수표 `㉗` 축 `㉢`(단위 · 예측/실적 분리) — 🟢 **33/33 준수 · ㉗ 전축 종결**

도구 = `scripts/nl_routing_judge.py --units`(신설 모드 · 🔴 **관측 도구**이고 최종 판정은 사람이 한다).

| 축 | 관측 | 사람 판정 |
|---|---|---|
| 금액 문항 단위 표기 | 금액 문항 **19** · 단위 토큰 없음 **0** | 🟢 준수 |
| 예측 문항 예측 표기 | 예측 문항 **6** · 표기 없음 **0** | 🟢 준수 |
| 예측·실적 혼재 | 플래그 **3** | 🟢 **전건 준수** — 아래 원문 인용 |

🟢 **혼재 플래그 3건은 전부 「분리 명시」였다**(위반이 아니라 준수의 증거):
* `AGENT_EXECUTIVE_09` = *"금액·건수·회원수 같은 업무 실적치가 아닙니다"*
* `AGENT_EXECUTIVE_10` = *"서로 다른 시스템에서 적재되므로 한 표에 합산·비교하지 않습니다"* +
  *"이 예측치는 실적이 아니며 머신러닝은 현재 테스트 단계입니다"*
* `AGENT_MEMBER_12` = *"두 데이터를 한 표에 합산하지 않는 것이 원칙입니다"*

🟢 표본 실물 확인 = `AGENT_EXECUTIVE_05` 는 *"**단위는 만원**입니다"* + *"예측치이며 실적이 아닙니다"* +
*"머신러닝은 현재 테스트 단계"* 를 모두 명시했다.
⇒ 🟢 **`㉗` 는 축 ㉠(라우팅 33/33) · ㉡(SQL 30/33 · 미생성 3건은 원천질문 = 정답) · ㉢(33/33) 전건 종결**이다.

### C3. 개명 축1 — 🟢 **469 → 458 (−11 · 기준선 하향 발행)**

대상 = `05_SV-Agent_ai/04_SV_설계.md`(최다 44건). 🔴 **기계 치환을 쓰지 않았다** — 이 파일의
`DIM_MEMBER_CURRENT` 는 **개명이 아니라 객체 소멸**이므로 이름만 바꾸면 문안이 거짓이 된다.

🟢 **라이브 실측으로 확정한 사실 3개**:
1. `GOLD.DIM_MEMBER_CURRENT` = `INFORMATION_SCHEMA.TABLES` **0건**(부재)
2. `GOLD.DIM_MEMBER` **24컬럼에 `IS_CURRENT` 부재** ⇒ 종전 우회식(`WHERE IS_CURRENT = TRUE`)은 **성립 불가**
3. 라이브 SV 는 **`DIM_MEMBER` 를 직접** 논리테이블로 쓴다(`SV_MEMBER_MONTHLY`·`SV_SERVICE`·
   `SV_EVENT_PARTICIPATION` = `SEMANTIC_TABLES` 조회)

🟢 **시정 2묶음**:
* **§0.1 · §0.5 설계 규칙 6줄** — 「SCD2 회원 fan-out」 위험 자체가 **소멸**했음을 명시
  (`DIM_MEMBER` 가 1행/회원 · SCD2 이력은 `DIM_MEMBER_STATUS_HISTORY` 로 분리) ·
  §0.5 F행(성능 우회)은 **불요**로 정정.
  🟢 위험 서술은 남겼다 — 「현재행 조인이 과거를 왜곡한다」는 **객체와 무관하게 유효**하다.
* **§0.9-B 라이브 배포표 base 4건** — 그 절이 *"배포 상태의 정본"* 을 자칭하므로 stale 이 **실해**였다.
  실측 신명으로 정정 = `FACT_MESSAGE_DISPATCH` · `FACT_EVENT_ATTENDANCE` ·
  `FACT_MEMBER_DEV_ACHIEVEMENT` · `FACT_MEMBER_SPONSORSHIP_SPAN`.

🔴 **잔여 33건은 160행 이후이고 손대지 않았다** — 그 구간을 **전량 독해하지 않았기 때문**이다
(미독 편집 금지). 🟢 **경계를 숫자로 남긴다** = 이 파일 **1,254줄** 중 **1~159행은 위반 0**,
잔여는 §0.8 이후 설계 서사 구간이다. 🔴 다음 세션은 **160행부터 읽고** 이으면 된다.

### C4. 🔴 이 턴에 내가 낸 결함 2건

| # | 결함 | 실해 | 시정 |
|---|---|---|---|
| 10 | `--units` 초판 정규식의 `\b` 가 **백스페이스 문자(0x08)로 굳었다** — heredoc 을 경유하며 이스케이프가 한 겹 벗겨졌다 | *"…199원"* 이 명시된 응답을 **단위 미표기로 오탐**(금액 문항 **7건 전부**) ⇒ Agent 가 규칙을 어긴 것으로 **잘못 보고할 뻔했다** | 정규식 정정 → 미표기 **0** · 🟢 판정식 = **정규식을 셸 경유로 쓰지 말고**(`R1-7-9` 축) **쓴 뒤 실제 문자열로 한 건 시험하라** |
| 11 | 혼재 탐지가 *"예측치이며 실적이 아닙니다"* 를 **위반으로 오탐** | 그 문장은 **준수의 증거**다 ⇒ 8건이 거짓 플래그였다 | 부정 문맥을 먼저 제거하도록 정정(8 → 3) · 남은 3건은 **원문을 열어 사람이 준수로 판정** · 🟢 판정식 = **부정문을 담은 문장은 그 규칙의 준수 증거일 수 있다** |

🔴 **둘 다 「내가 만든 탐지기를 실물로 시험하지 않은 것」**이고, O167 이 이미 두 번 겪은
**`tool_use.input.sql` 오보**·**`SHOW` vs `DESCRIBE` 오탐**과 **같은 계열**이다(5·6차 재발).
🟢 ⇒ **판정식 강화 = 「탐지기를 만들면 「걸려야 하는 표본」과 「걸리면 안 되는 표본」을 각각 1건 넣고 돌려라.」**

---

_Co-authored with CoCo_
