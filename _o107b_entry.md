> #### 🔴 [2026-08-28 O107-B] O107 후속 — **스테이지 stale read 로 편집 1건 소실·복구** + 게이트 오탐 2축 관측
>
> 🔴 이 항목은 **§O107 항목을 롤오버한 뒤에 발견된 사실**이다(그 항목에는 없다).
>
> **1. 편집 소실 사고 (복구 완료 · 손실 0)**
> * 경위 = ① `99_NEXT_SESSION-001.md` 에 인수인계 절(§0-MMM)을 `edit` 로 추가 → 직후 실측
>   **352줄 / 33,172B** 확인 ② `--republish` 의 게이트2 가 **논리 문서 2,174줄**로 보고(편집 반영됨) ·
>   게이트6 이 상한 초과(352 > 300)로 FAIL ③ 사용자 승인 후 `--rebalance --fill 0.7` 실행 →
>   이때 `collect_bodies` 가 읽은 것은 **2,107줄**(편집 **없는** 옛 내용)이었고
>   그 내용으로 **14조각 전부를 재기록**해 §0-MMM 이 사라졌다.
> * 확정 근거 = `_archive/99_NEXT_SESSION.md.O83B-prerebalance`(재균형이 읽은 그 내용)에
>   `0-MMM`·`O107`·`승계됨` 토큰이 **전부 부재**. 2,174 ↔ 2,107 = **67줄** = 추가한 절의 줄 수와 일치.
> * 🔴🔴 **게이트가 전부 🟢 인 채로 편집이 사라졌다.** `--verify` 6종은 **자기일관성**만 본다
>   (조각 concat ↔ 방금 계산한 해시) ⇒ 재균형이 stale 을 읽으면 그 stale 이 **새 정본이 되고
>   해시도 그것으로 재발행**되므로 어느 축도 울리지 않는다. `R1-7-2`(해시 안정성)도 못 잡는다 —
>   그 조문은 **같은 시점 2회 읽기**를 보고, 이 사고는 **시점 간 불일치**다.
> * 🟢 **복구** = 작성자가 본문을 **다시 썼다**(스냅샷에도 없어 다른 경로가 없었다) ·
>   재작성 후 `-001` 은 **236줄 / 23,491B**(상한 이내)라 재균형 없이 `--republish` 만 했고
>   연산 **전·후 모두 토큰 grep** 으로 실재를 확인했다(3건 매칭).
> * 🔴 **처방(조문 후보)** = 유지 연산(`--rebalance`·`--rollover`·`retire_*`) **직전·직후에
>   「방금 넣은 토큰」을 grep 으로 단정**한다. 해시 안정성은 이 축을 대체하지 않는다.
>   🟢 **긴 절은 별도 파일(`_o1NN_entry.md`)에 먼저 쓰고 붙인다** — 이력 항목은 그래서 살았다.
>
> **2. 게이트 오탐 2축 관측(둘 다 blocking 아님 · 다음 세션 판단 대상)**
> * `doc_census` **이월 귀속 오탐** = 원장 §1 에 새 행을 넣었더니 stale **2건**이 떴는데,
>   원인은 **내 행이 언급한 문서명(`90` 계열)이 `carry` 로 다음 줄(O106 행)에 귀속**되어
>   그 줄의 「12조각」·「1」을 조각 수 기재로 오인한 것이었다. 🔴 **내가 적은 조각 수는 0건**이다.
>   ⇒ 문서명 표기를 바꿔 회피했고(stale 0 회복) **스캐너 규칙 자체는 그대로**다.
>   O106 이 이미 「긴 행 폴백」을 `SHORT_LINE=300` 으로 막았는데, **`carry` 축에는 그 가드가 없다.**
> * `decision_closure_gate` **선택표 오탐** = 허브 「조각 선택표」의 `- ID: …` 줄이
>   `DEC` 인용처로 잡힌다(⚪ 등급). 🔴 O107 은 **결정을 내리지 않았다** ⇒ 21건은 전부 이전 세션 것이고
>   그중 일부는 이 신설 표 때문에 늘어난 노이즈다. ⇒ 분모에서 허브를 제외할지 판단이 필요하다.
>
> **3. 확정위반 재집계** = O107 계열 총 **1건**(`R1-4-3` 선점 등재 시점). 🔴 소실 사고는
> 위반이 아니라 **환경 결함 + 규율 공백**이다(공백을 메우는 처방을 위 1 에 적었다).
