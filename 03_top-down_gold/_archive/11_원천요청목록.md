<!-- LLM-METADATA
doc_id: GOLD_SOURCE_REQUEST
doc_role: source_to_gold_gap_and_request (원천 대조 데이터 요청 목록)
project: GN_DW (굿네이버스)
derived_from: 05_필드인벤토리.md(GOLD 필요) + 지표사전 215 ∩ 원천 스키마(BRONZE_CRM 41테이블·08_GA4 events·인덱스)
relation: 10_BRONZE컨트랙트.md(상위 요청서)의 원천 스키마 근거 상세본. 중복 회신 항목은 그 문서로 위임.
status: CRM·GA4 = 스키마 수령(대조 완료) / ERP·AGENCY·GADS·ADMIN = 미수령(요청)
END-METADATA -->

# 원천 데이터 요청 목록 (원천↔GOLD 대조) — GN_DW

**전제**: GOLD는 `05_필드인벤토리.md`(24테이블) + 지표사전 215를 **전부 구축**한다. 그 컬럼을 채우는 데 필요한 데이터를 **원천에서 추가 확보**한다. 본 문서는 **원천 스키마를 실제로 보고** "이미 있는 것 / 정의·코드값만 더 필요한 것 / 아예 요청해야 하는 것"을 분류한 요청 목록이다.

> 회신 양식·적재 규칙·역할분담은 `10_BRONZE컨트랙트.md` 참조. 본 문서는 **항목별 원천 근거**에 집중한다.
> 표기: ✅충족(원천 존재·매핑됨) · 🟡정의/코드값 필요(데이터는 있으나 의미·라벨 확인) · 🔴요청(원천 미수령/미존재) · ⛔차단(없으면 해당 지표 불가).

---

## 1. 원천별 커버리지 요약

| 원천 | 수령 | GOLD 충족도 | 핵심 미해결 |
|---|---|---|---|
| **CRM** (41테이블/876컬럼) | ✅ | 회원·개발·중단·납입·청구·캠페인·조직·발송·참여 대부분 충족 | 상태변경이력(SCD2)·후원사업 마스터·미납이력 컬럼정의·코드값 라벨 |
| **GA4** (events_YYYYMMDD) | ✅ 스키마 | 세션·이벤트·유입·페이지·전환 충족(가공 전제) | ⛔`user_id`=회원번호 여부 / 이탈율·평균세션시간 **사전집계 미포함** / 클릭 CRM 중복 |
| **ERP** | 🔴 미수령 | FBD 비용 0% | 모금성비용 세세목·집행예산 |
| **AGENCY**(대행사) | 🔴 미수령 | FAD·DIM_AD_CREATIVE 0% | 광고비·노출·클릭·인입콜·편성비·소재 메타 |
| **GADS**(Google Ads) | 🔴 미수령 | FAD 광고 0% | 광고비·노출·클릭·전환(출처구분) |
| **ADMIN**(어드민 화면) | 🔴 미수령 | FSE 앱푸시·FEP 조회수 0% | 앱푸시 발송/성공·이벤트 조회수 |

---

## 2. CRM — 대부분 충족, 잔여 요청만

### 2-1. ✅ 충족 (원천 확인됨 — 추가 요청 없음)
| GOLD 대상 | 원천 테이블·컬럼 |
|---|---|
| DIM_MEMBER 성별·지역·상태·신규기존·가입경로 | `TM_MM_FDRM_MBER_INFO`(SEX CM013·AREA_CD CM018·MBER_STAT_CD MM010·RELATNSP_DIV_CD MM019·JOIN_PATH_CD MM014), 일시 `TM_MM_ONCE_MBER_INFO` |
| DIM_CAMPAIGN 국내해외#15·사업사례#16 | `TM_CM_CMPGN_MNG.CMPGN_TYPE1_BSN`(MM295)·`CMPGN_TYPE2_BSN`(MM296) |
| DIM_CAMPAIGN 브랜드·상위캠페인·홍보방법 | `TM_CM_BRND_MNG`·`UPPER_CMPGN_CD`·`PR_MTH_CD`(CM008) |
| DIM_ORG 부서계층 | `TM_CM_DEPT_INFO`(UPPER_DEPT_ID·실적부서) |
| DIM_PAYMENT 회비유형 | `TM_PM_MBRFEE_ACMSLT.MBRFEE_DIV_CD`(PM010), 결제 `TM_PM_SETLE_INFO` |
| DIM_REASON 코드 | `TC_CMMN_DTL_CD` — 취소감액 MM002·후원중단 MM005·중단경로 MM287 |
| FMM/FME 개발·취소감액 | `TM_MM_FDRM_MBER_DVLP_AMT`(SPNSR_AMT·DVLP_DIV_CD MM015·CANCL_RDCAMT_RSN_CD MM002) |
| FMM/FME 중단·재후원 | `TM_MM_FDRM_MBER_SPNSR_DSCNTC`(MM005·MM287)·`TM_MM_FDRM_MBER_RE_SPNSR` |
| FMM 회비(청구·납입) | `TM_PM_MBRFEE_ACMSLT`(RQEST_AMT·PAY_AMT·PAY_DE), 일시 `TM_PM_DNTN_DTLS` |
| FTG_D 회원개발목표 | `TM_CM_MBER_DVLP_GOAL`(STDYY·STDR_MT·MBER_DVLP_DIV_CD·DEPT_ID·GOAL_CNT) |
| FSE 발송/성공/실패 | `TD_MS_EMAIL_LQY_SNDNG`·`TD_MS_MSG_AT_LQY_SNDNG`(SNDNG/SUCCES/FAILR_CNT) + 상세 `*_SNDNG_DTLS`(회원단위) |
| FSE 클릭(서비스) | `TD_MS_MSG_AT_LQY_SNDNG.TOT_CLICK_CNT_CTNT` (③-2 CRM 클릭 존재) |
| FEP 참여/본인/수납/동반 | `TD_MS_CRMN_PRTCPNT`(PARTCPT_STAT_CD·SELF_PARTCPT_CD·RCPMNY_AMT·ACMPNY_PARTCPT_CO) |
| FEP 행사 당첨 | `TD_MS_EVENT_PRTCPNT_DTL.PRZWIN_CD`, 행사 `TM_MS_EVENT` |

### 2-2. 🟡 정의/코드값 확인 요청 (데이터는 있음)
| # | 요청 | 원천 | 영향 |
|---|---|---|---|
| C-1 | **코드값 라벨 정의서** — MM293·MM294·MM295·MM296 / `MK_CMPGN_CD` | `TM_CM_CMPGN_MNG`·`TM_CM_MKTNG_CMPGN_MNG` | 캠페인 속성 라벨 |
| C-2 | **캠페인유형#17 원천 확정** (후보 `CMPGN_CTGR_CD` MM294) | `TM_CM_CMPGN_MNG` | DIM_CAMPAIGN |
| C-3 | **'팀' 조직레벨 정의** — 부서계층 어느 레벨이 '팀'인가 | `TM_CM_DEPT_INFO` | DIM_ORG grain |
| C-4 | **물리 데이터타입 제공** (`INFORMATION_SCHEMA.COLUMNS`) | CRM 전체 | 타입 확정(7단계)·DDL |
| C-5 | 결연중단 `RELATNSP_DSCNTC_YN`=0/1(Y/N 아님) 처리 확인 | `SND_MEMBER_LIST` | 이탈/중단 식별 |

### 2-3. 🔴/⛔ 컬럼 정의 부재 — **요청 필수**
| # | 요청 | 원천 | 영향 |
|---|---|---|---|
| **C-6 ⛔** | `TH_MM_FDRM_MBER_STNG_DTLS`의 **COL2~COL6 실제 컬럼명·의미** (정기회원 상태변경내역) | CRM | **FMM 시점지표 #49·50·52·53(연도초/말·월말·전월말 활동회원) 산출 차단**. SCD2 회원상태 이력의 핵심 |
| C-7 ⛔ | `TM_CM_SPNSR_BSNS_INFO` **13컬럼 정의**(후원사업명 마스터) | CRM | DIM_SPONSORSHIP 명·약칭 |
| C-8 ⛔ | `TM_MM_FDRM_MBER_IRSD` **상세 컬럼 정의**(미납/증감 이력, OCCRRNC_DE 외) | CRM | FMM/FME 미납·증액 measure |
| C-9 | `TM_RM_RELATNSP_GFTMNEY_INFO.EHGT` 약어 해독 | CRM | FSE 선물금참여 |
| C-10 | 인바운드콜·TS콜 **월간 수치 원천** 확정(콜/건 단위) | CRM(월간)? | FMM 콜 measure(overview) |
| C-11 | 오픈(명) 정의 — `URL_OTHBC_CNT`(URL공개)=오픈으로 볼지 | `TD_MS_EMAIL_LQY_SNDNG` | FSE OPEN_MEMBERS |

---

## 3. GA4 — 스키마 수령, 가공 전제 + 3대 확인

### 3-1. ✅ 충족 (events 원본에서 가공 가능)
| GOLD 대상 | GA4 근거 |
|---|---|
| FGA 방문·세션·활성·이벤트·조회 | events + `is_active_user`, 세션키=`user_pseudo_id`+`ga_session_id` |
| FGA 유입(utm source/medium/term/content) | `session_traffic_source_last_click.manual_campaign.*` (③ GA4 UI 일치) |
| DIM_GA_SOURCE/EVENT | 위 + `event_name`/`event_params` |
| FGA 페이지경로·결연아동코드 | `event_params['page_location']` 파싱(아동코드 URL 추출) |
| FGA 전환수 | 전환 `event_name` 집계 |
| DIM_DEVICE | `device.category`(mobile/desktop/tablet)·`platform`(WEB/ANDROID/IOS) |

### 3-2. 🔴/⛔ GA4 요청·확인
| # | 요청 | GA4 근거 | 영향 |
|---|---|---|---|
| **G-1 ⛔** | **`user_id`에 CRM 회원번호가 심겨 있는지 확인** (없으면 `user_pseudo_id`+`transaction_id` 간접) | §2.3 | **DIM_MEMBER_IDENTITY 신원매칭 / #81·신32·33 클릭·전환 회원(명) 전부 차단** |
| **G-2 ⛔** | **이탈율(#108)·평균세션시간(#98)은 events 원본에 사전집계 컬럼 없음** → ⓐ raw에서 산출 규칙 합의 or ⓑ GA4 Data API 집계 별도 수령 중 택1 | §3 전체(해당 leaf 부재) | FGA 비가산 measure(O1 직접 확정) |
| G-3 | 서비스 **클릭이 CRM(`TOT_CLICK_CNT`)과 GA4 양쪽 존재** → 정본 1개 선택(중복 집계 방지) | §3.11/CRM | 신#32·33 분모 |
| G-4 | GA **개발 실적** 정본 = GA4 직접 vs CRM 채널코드(`TD_MS_EMAIL_LQY_SNDNG`) | ③-1 | #5·8 GA개발건 |
| G-5 | 스크롤깊이 단위(% vs 단계), engagement_time 보유 여부 | `event_params` | FGA SCROLL_DEPTH·참여시간 |
| G-6 | 72h 지연 — 확정데이터 기준일(D-3) 합의 | §1 latency | 적재 신선도 |

> 주의: **광고비·노출은 GA4 events 원본에 없음**(cost/impression 미포함). FAD 광고비·노출은 **AGENCY/GADS**에서 받아야 함(§4). GA4는 전환·세션·행동만.

---

## 4. ERP / AGENCY / GADS / ADMIN — 전량 미수령(요청)

### 4-1. ERP (FBD 비용) 🔴
| # | 요청 | GOLD 대상 |
|---|---|---|
| E-1 ⛔ | **모금성비용 세세목**(캠페인/매체 연결 가능한 최소단위) | FBD `FUNDRAISING_COST`, 신#10 매체별 개발단가 |
| E-2 | 집행예산(ERP 마감 확정) | FBD `EXEC_BUDGET_ERP` |
| E-3 | 편성예산(월/연/누계) | FBD `PLAN_BUDGET_*`, 신#9 개발단가 |
| E-4 | 캠페인별 비용 집계기준 **합의**(없으면 부서 grain까지만) | 신#11 ROI(O3) |

> 목표(연사업/추경/누계 FTG_B)는 ERP 아님 — CRM `TM_CM_MBER_DVLP_GOAL`로 확정(중복 요청 제외).

### 4-2. AGENCY(대행사 일별레포트) 🔴
| # | 요청 | GOLD 대상 |
|---|---|---|
| A-1 ⛔ | **광고비·노출·클릭·인입콜**(일×캠페인×소재 단위) | FAD `AD_COST·IMPRESSIONS·CLICKS·INBOUND_CALL` |
| A-2 | **편성비** | FAD/FBD, 신#9 캠페인 개발단가 |
| A-3 | 광고소재 메타(매체·플랫폼·플랫폼유형·기기·소재·CM위치·초수·RT유형·광고유형·타겟그룹) | DIM_AD_CREATIVE |
| A-4 | 인입콜 단위(콜/건), 송출일(BROADCAST_DATE)≠실적일 보존 | FAD degen |

### 4-3. GADS(Google Ads) 🔴
| # | 요청 | GOLD 대상 |
|---|---|---|
| GA-1 ⛔ | 광고비·노출·클릭·**전환수(명/건)** — **출처를 AGENCY와 구분**해 적재 | FAD `_SOURCE_SYSTEM=GADS`, 공#8·10 |

### 4-4. ADMIN(어드민 화면 수집) 🔴
| # | 요청 | GOLD 대상 |
|---|---|---|
| AD-1 | 앱푸시 **발송건수·성공건수** | FSE `APP_PUSH_SEND_CNT·APP_PUSH_SUCCESS_CNT`(channel=APP_PUSH) |
| AD-2 | 이벤트 **조회수** | FEP `VIEW_CNT` |

---

## 5. GOLD 컬럼 ↔ 요청 매핑 (FACT/DIM별 미충족 집약)

| GOLD 테이블 | 충족(원천) | 요청(미충족) |
|---|---|---|
| DIM_MEMBER | CRM ✅ | 상태이력 SCD2 = **C-6** |
| DIM_SPONSORSHIP | — | **C-7** |
| DIM_CAMPAIGN | CRM ✅(#15·16) | #17(C-2)·코드라벨(C-1) |
| DIM_AD_CREATIVE | — | **A-3** (AGENCY 전량) |
| DIM_MEMBER_IDENTITY | 회원번호 ✅ | GA 연계키 **G-1** |
| FMM/FME | 개발·중단·회비 ✅ | 시점지표(C-6)·미납이력(C-8)·콜(C-10) |
| FSE | 발송·성공·클릭 ✅ | 오픈정의(C-11)·앱푸시(AD-1) |
| FGA | 세션·전환 ✅ | 신원(G-1)·이탈율/세션시간(G-2) |
| FAD | — | 광고비·노출·클릭(A-1·GA-1) 전량 |
| FBD | — | 비용(E-1~3) 전량 |
| FEP | 참여 ✅ | 조회수(AD-2) |
| FTG_D | CRM ✅ | — |
| FTG_B | — | ERP 목표(미수령) |

---

## 6. 적재 시 필수 보존 (재확인 — 10_BRONZE컨트랙트 §6과 동일)
1. **상태 이력 보존**(덮어쓰기 금지) — SCD2·시점지표의 전제. 특히 C-6 상태변경내역.
2. **회원 연결키 3종**: 회원번호(CRM)·memnum(=회원번호 문자)·GA `user_id`(G-1). 서로 연결 가능 형태.
3. **금액 원본 보존**: `(건)`=SUM(금액)/10000 다수 → 반올림 전 원금액(P6).
4. **발생 시각/순번 보존**: 같은 날 중단·재후원 순서, 발송↔참여 5일 계산(FSE D5).
5. **출처 구분 적재**: 광고비·전환 = AGENCY vs GADS `_SOURCE_SYSTEM`.

---

## 7. 우선순위 (차단도 기준)

**⛔ 즉시(없으면 지표 자체 불가)**: C-6(시점지표)·C-7(후원사업)·C-8(미납이력)·G-1(GA 신원)·G-2(GA 이탈/세션시간)·E-1(모금성비용)·A-1(광고 실적)·GA-1(Google Ads).

**🟡 병행**: C-1~C-5 코드/타입/조직레벨, G-3~G-6 GA 정본·단위, A-2~A-4·E-2~E-4·AD-1·AD-2.

> 모든 미수령 원천은 GOLD에 **빈 컬럼이 이미 설계돼 있음**(P9) — 데이터만 들어오면 채워지고 모델 변경 불필요. 본 목록은 "무엇을 받아야 그 자리가 채워지는가"의 체크리스트다.
