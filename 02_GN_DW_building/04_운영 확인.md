---
project_id: GN_DW
doc_type: work_plan_chapter
chapter: "04_운영"
sections: [5, 6, 7, 8]
index: "00_INDEX.md"
depends_on: ["02_DB_BRONZE_SILVER.md", "03_GOLD_SERVING.md"]
provides: [tasks, tests, security, monitoring]
language: ko (설명) / en (구조 키)
---

# 04. 운영 (pipeline + tests + security + monitoring)

> 인덱스: `00_INDEX.md` · 핵심 원칙(P1~P7)은 인덱스 참조.
> 본 챕터는 구축 step 11~14를 다룬다. (ETL은 dbt 파이프라인 — 02 §4 연계)

---

## 5. ETL 오케스트레이션 (dbt pipeline)

> **라이브(2026-07-22): ETL은 dbt 프로젝트 `GN_DW.OPS.DW_PIPELINE`으로 운영.** 구설계의 Serverless Task DAG 4종(`TASK_VALIDATE_BRONZE`→`TASK_REFINEMENT_ROOT`→`TASK_LOAD_GOLD`→`TASK_FINALIZER`)·정제 프로시저·`ETL_LOG`는 **폐기**되었다(SHOW TASKS/PROCEDURES 실측 0).

```yaml
pipeline:
  engine: dbt
  project: GN_DW.OPS.DW_PIPELINE
  models: 65   # SILVER 32 → GOLD star 24 → WIDE VIEW 9
  flow: "BRONZE_<SOURCE> → SILVER 32 → GOLD star 24 → WIDE VIEW 9"
  execution: "dbt run / dbt build (dbt 프로젝트 실행). 워크스페이스 build green 확인."
  quality_gate: "dbt schema tests(not_null/unique/relationships). relationships=severity:warn(메달리온 BP), 핵심 PK=error"
run_principles:
  - "멱등성: dbt run이 table CREATE OR REPLACE / view 재생성 → 재실행 안전"
  - "센티넬 SK=0 라우팅으로 미매칭 차원키 흡수(파이프라인 중단 없이 관측)"
  - "스캐폴드 팩트(FACT_TARGET_BIZ)는 원천 미입고 시 0행 통과"
future_scheduling:
  - "정기 스케줄은 dbt 프로젝트를 감싸는 CREATE TASK(cron)로 래핑 예정 — 현재 수동/온디맨드 실행"
  - "Task 도입 시 Serverless(EXECUTE MANAGED TASK ON ACCOUNT, 01_환경_Role.md 2.2)"
  - "BRONZE 신선도 기반 트리거(Stream/WHEN)로 확장 가능"
deploy_ref: "10_dbt_pipeline/00_배포운영_통합_*.md · deploy_dbt_project.sql"
```

---

## 6. 테스트 (tests)

```yaml
permission_tests:
  - { case: "BRONZE SELECT", role: GN_DW_ENGINEER, expect: PASS }
  - { case: "BRONZE SELECT", role: GN_DW_ANALYST, expect: PASS }   # 🟢 [2026-08-18] FAIL→PASS 요건 변경(07 §D.4)
  - { case: "BRONZE SELECT", role: GN_DW_VIEWER, expect: FAIL }    # 🟢 [2026-08-18] 신설 — ANALYST 확대가 VIEWER로 새지 않음을 증명
  - { case: "BRONZE INSERT", role: GN_DW_LOADER, expect: PASS }
  - { case: "BRONZE INSERT", role: GN_DW_ANALYST, expect: FAIL }   # SELECT만 부여 — 원천 무결성 유지
  - { case: "SILVER SELECT", role: GN_DW_ANALYST, expect: PASS }
  - { case: "GOLD SELECT", role: GN_DW_VIEWER, expect: PASS }
  - { case: "GOLD CREATE VIEW", role: GN_DW_VIEWER, expect: FAIL }
  - { case: "Semantic View USAGE", role: GN_DW_ANALYST, expect: PASS }
  - { case: "Agent USAGE", role: GN_DW_VIEWER, expect: PASS }
  - { case: "Agent USAGE", role: GN_DW_LOADER, expect: FAIL }
  - { case: "COMPUTE POOL USAGE(Workspace Streamlit 저작)", role: GN_DW_ANALYST, expect: PASS }  # 🟢 [2026-08-18] PUBLIC 상속(07 §D.8)
  - { case: "CREATE STREAMLIT ON SCHEMA GN_DW.SERVING", role: GN_DW_ANALYST, expect: FAIL }      # 🟢 미부여가 정상 — 저작은 개인 Workspace(07 §D.8)
test_method:
  - "🔴 [2026-08-18 실측 교훈] 권한 테스트 시 **반드시 `USE SECONDARY ROLES NONE` 을 먼저 실행**한다.
     기본 세션은 secondary roles 가 활성(ALL)이라 사용자가 보유한 전 롤의 권한이 **합집합**으로 평가된다 —
     이 상태에서 VIEWER 로 BRONZE SELECT 를 시도하면 **차단돼야 하는데 성공**해서 테스트가 무효가 된다(실제 오탐 발생).
     검증: `CURRENT_SECONDARY_ROLES()` 가 빈 값이어야 유효한 테스트다."
executed_results:   # 🟢 [2026-08-18 라이브 실측 — 임시 테이블 BRONZE_CRM._RBAC_TEST 로 검증 후 DROP]
  - { case: "BRONZE SELECT", role: GN_DW_ANALYST, expect: PASS, actual: PASS, note: "FUTURE grant 가 신규 테이블에 즉시 적용됨을 동시 확인" }
  - { case: "BRONZE SELECT", role: GN_DW_VIEWER, expect: FAIL, actual: FAIL, note: "Schema does not exist or not authorized — 상속 차단 확인" }
  - { case: "BRONZE INSERT", role: GN_DW_ANALYST, expect: FAIL, actual: FAIL, note: "must have INSERT granted — SELECT 전용 확인" }
  - { case: "COMPUTE POOL 가시성", role: GN_DW_ANALYST, expect: PASS, actual: PASS, note: "SHOW COMPUTE POOLS 1건 — PUBLIC 상속" }
  - { case: "SERVING 접근", role: GN_DW_ANALYST, expect: FAIL, actual: FAIL, note: "08_After_Deploy_DBT.sql §E.1 미실행 상태라 정상. 08 실행 후 PASS 로 전환" }
  - { case: "Agent/Cortex 호출", role: GN_DW_ANALYST, expect: PASS, actual: BLOCKED, note: "🔴 아래 trial_account_limit 참조 — 권한 문제 아님" }
trial_account_limit:
  - "🔴 [2026-08-18 실측] 이 계정은 **트라이얼**이라 AI 함수가 전면 차단된다.
     `SNOWFLAKE.CORTEX.COUNT_TOKENS` → 'AI function COUNT_TOKENS is not available for trial accounts',
     `AI_COMPLETE` → 'AI function _COMPLETE_WITH_PROMPT_HISTORY_LLM is not available for trial accounts'."
  - "⇒ ANALYST 의 **'agent 사용' 요건은 현 계정에서 기능 검증이 불가**하다. 권한(CORTEX_USER via PUBLIC)은
     `SHOW GRANTS TO ROLE PUBLIC` 으로 확인했으나, Agent·Cortex Analyst 실동작은 **유료 계정 전환 후** 재검증해야 한다."
  - "참고: `IS_DATABASE_ROLE_IN_SESSION('SNOWFLAKE.CORTEX_USER')` 는 ACCOUNTADMIN 으로도 FALSE 를 반환한다 —
     PUBLIC 경유 상속을 반영하지 못하므로 **이 함수로 판정하지 말 것**(SHOW GRANTS 로 판정)."
e2e_pipeline_tests:
  - "1: BRONZE 원천 적재 확인 (LOADER, 외부)"
  - "2: dbt run(SILVER) → SILVER 32테이블 생성 확인"
  - "3: dbt run(GOLD) → star 24 + WIDE 9 생성 확인"
  - "4: dbt test → not_null/unique/relationships 통과(warn 관측)"
  - "5: SERVING SV/Agent 질의 → 결과 반환 확인"
data_integrity_tests:
  - { check: "BRONZE 적재 완전성", method: "CRM 43 + AGENCY 3 + ERP 1 + GA4 1 = 48테이블 존재 확인" }
  - { check: "SILVER 통합 건수", method: "SILVER 32테이블 생성 확인, 원천 대비 건수 정합" }
  - { check: "GOLD 적재", method: "star 24 + WIDE 9 존재. FACT_TARGET_BIZ=0행(E-6 대기) 허용" }
  - { check: "NULL key 없음", method: "GOLD DIM/FACT PK not_null(dbt test)" }
  - { check: "센티넬 라우팅", method: "미매칭 FK가 SK=0 Unknown으로 라우팅되는지 확인" }
  - { check: "Semantic View 동작", method: "Agent 질문 → SQL 생성 → 결과 반환 (fan-out 0)" }
```

---

## 7. 보안 세팅 (security)

### 7.1 네트워크 (network)

```yaml
network_rules:   # GN_DW.SECURITY 스키마
  - { id: NR_OFFICE_IP, purpose: 사무실 IP 대역 허용, type: "IPV4 (INGRESS)" }
  - { id: NR_VPN_IP, purpose: VPN 접속 IP 허용, type: "IPV4 (INGRESS)" }
  - { id: NR_SERVICE_IP, purpose: 서비스/ETL 서버 IP, type: "IPV4 (INGRESS)" }
network_policies:
  - { id: NP_GN_DW_ACCOUNT, applies_to: Account, desc: "사무실+VPN+서비스 IP만 허용" }
  - { id: NP_GN_DW_SERVICE, applies_to: GN_DW_SERVICE 유저, desc: "서비스 IP만 허용(더 엄격)" }
apply_order:
  - "Network Rule 생성 -> Network Policy 생성 -> 테스트(본인 IP 포함 확인)"
  - "Account 활성화(ALTER ACCOUNT SET NETWORK_POLICY)"
warning: "본인 IP를 ALLOWED에 미포함 시 즉시 잠김. 반드시 테스트 후 적용 (R1)"
```

### 7.2 마스킹 정책 (masking)

```yaml
masking_policies:   # GN_DW.SECURITY 스키마, SILVER 컬럼 매핑
  - { id: MASK_MEMBER_ID, target_column: 회원번호, rule: "ENGINEER/ADMIN -> 원본, 나머지 -> 앞4자리+****" }
masking_layer:
  - "SILVER 물리 테이블 컬럼에 직접 적용 (View REPLACE 시 정책 단절 방지)"
  - "SILVER 마스킹은 상단 GOLD View까지 자연 상속"
  - "🔴 [2026-08-18 설계 파괴 — 조치 필요] 종전 근거 「BRONZE는 ANALYST/VIEWER 직접 접근 불가 -> 불필요」가
     **더는 성립하지 않는다**. 07 §D.4 로 ANALYST 가 BRONZE_* SELECT 를 갖게 되면서,
     MASK_MEMBER_ID 규칙(ENGINEER/ADMIN 외 마스킹)이 **BRONZE 직접 조회로 우회**된다 —
     ANALYST 는 SILVER.회원번호는 마스킹된 값을, BRONZE_CRM 의 동일 원천 컬럼은 원본을 본다."
  - "⇒ 선택지 3: (a) BRONZE_CRM 회원번호에도 동일 마스킹 정책을 적용(마스킹 계층을 BRONZE 로 확장),
     (b) ANALYST 의 BRONZE_CRM SELECT 만 제외하고 ERP/AGENCY/BIGQUERY 3종만 허용(요건 축소 협의),
     (c) 마스킹 규칙에 ANALYST 를 원본 허용 롤로 추가(=마스킹 포기 결정을 명시화).
     🔴 마스킹 정책 배포 전까지는 미결 항목이다(라이브 SHOW MASKING POLICIES = 0건 — 아직 미배포)."
note: "현재 확인된 민감 컬럼=회원번호. 추가 PII 도입 시 동일 패턴 확장"
```

### 7.3 MFA (mfa)

```yaml
mfa_policy:
  - { target: "ACCOUNTADMIN 유저", policy: 필수 }
  - { target: "GN_DW_ADMIN 유저", policy: 필수 }
  - { target: GN_DW_ENGINEER, policy: 권장 }
  - { target: GN_DW_ANALYST, policy: "선택(조직 정책)" }
note: "유저별 웹UI/CLI 설정. DDL 강제 시 AUTHENTICATION POLICY 사용"
```

---

## 8. 모니터링 세팅 (monitoring)

> **배포 상태(2026-07-22 실측):** 아래 Resource Monitor·Alert·Cost View는 **설계안(미배포)**이다. 현재 Warehouse 3종에 Resource Monitor 미연결(SHOW WAREHOUSES: resource_monitor=null). 운영 승격 시 아래 사양대로 생성한다.

### 8.1 Resource Monitor (resource_monitors)

> 계정 레벨 객체 (특정 스키마 미소속).

```yaml
resource_monitors:
  - { id: RM_ETL, target: GN_DW_ETL_WH, monthly_credit_limit: 200, triggers: "75% 알림, 90% SUSPEND, 100% SUSPEND_IMMEDIATE" }
  - { id: RM_ANALYTICS, target: GN_DW_ANALYTICS_WH, monthly_credit_limit: 500, triggers: "75% 알림, 90% 알림, 100% SUSPEND" }
  - { id: RM_DEV, target: GN_DW_DEV_WH, monthly_credit_limit: 100, triggers: "80% 알림, 100% SUSPEND" }
  - { id: RM_ACCOUNT, target: 전체 계정, monthly_credit_limit: 1000, triggers: "80% 알림, 95% SUSPEND" }
```

### 8.2 Alert (alerts)

> Alert는 운영 승격 시 `GN_DW.OPS`에 배치(설계안). ⚠️ 구설계의 `ETL_LOG` 기반 조건은 dbt 전환으로 폐기 — dbt run/test 결과(로그·artifacts) 기반 실패 알림으로 대체한다.

```yaml
alerts:   # 설계안 (미배포)
  - { id: ALERT_DBT_FAILURE, condition: "dbt run/test 실패(로그 기준)", action: 이메일 알림 }
  - { id: ALERT_LONG_QUERY, condition: "쿼리 실행 > 30분", action: 이메일 알림 }
  - { id: ALERT_BRONZE_STALE, condition: "BRONZE 최종 업데이트 > 24시간", action: 이메일 알림 }
```

### 8.3 비용 추적 (cost_tracking)

```yaml
cost_tracking:
  sources:
    - SNOWFLAKE.ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY  # Warehouse별 크레딧 추이
    - SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY               # 쿼리별 비용
    - "Resource Monitor 알림 (임계 도달 즉시)"
  cost_report_views:   # 신규 객체, GN_DW.OPS 배치
    - id: V_MONTHLY_COST_REPORT
      desc: "Warehouse별 월간 집계"
      base: SNOWFLAKE.ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY
      columns: [WAREHOUSE_NAME, MONTH, TOTAL_CREDITS_USED, TOTAL_CREDITS_BILLED]
    - id: V_COST_BY_ROLE
      desc: "Role별 일간 집계"
      base: SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY
      columns: [ROLE_NAME, QUERY_DATE, QUERY_COUNT, TOTAL_ELAPSED_SEC, ESTIMATED_CREDITS]
```

---

> **이전 단계:** `03_GOLD_SERVING.md` · **구축 완료.** 전체 흐름은 `00_INDEX.md`의 execution_order 참조.
