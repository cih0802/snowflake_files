<!-- LLM-METADATA
doc_id: SILVER_GA4_WORKPLAN
doc_role: silver_work_plan (GA4-only subset / 트랙 B)
project: GN_DW (굿네이버스)
created: 2026-07-14
scope: GA4 단독 — BRONZE_GA4 이벤트 샤드 → SILVER GA4 5객체 + 신원브리지 GA측
status: 🟢 BRONZE 적재(1일 샤드 events_20260501 287,025행)·SILVER 미생성 — 트랙 B 착수 대기(전기간 샤드 입고 후)
master_plan: 04_silver_design/02_SILVER_작업계획_BRONZE-GOLD연결 20260714.md  # 매핑 인덱스(정본)
relation: master §1 매핑의 GA4 행 + 트랙 B 상세 실행본
inputs(bronze): 99_provided_definition/14_bronze_ga4_events_schema.md(구조) · GA4스키마 20260626.csv
ddl(초안): _archive/09_SILVER_DDL_20260702.sql 의 GA4 5테이블 DDL (트랙 B 착수 시 08로 이관)
refs: 07_GA4_SILVER_샤드통합 설계결정.md · 12_GA4_파이프라인_dbt로 작업전_주의사항.md · 13_GA4_파이프라인_SP로 작업전_주의사항.md · 10_dbt_pipeline/macros/ga4_union_shards.sql
END-METADATA -->

# SILVER 작업계획 — GA4 전용 (트랙 B)

> ⚠️ 본 문서는 **마스터 인덱스(`02_...20260714.md`)의 GA4 분리 실행본**이다.
> 공통 원칙·GOLD 커버리지 요약·교차소스(신원브리지)는 master 참조. 객체 명칭·정제 규칙 불일치 시 master 우선.

---

## 0. 범위

- **만들 것**: `GN_DW.SILVER` 의 **GA4 5개 객체** (BRONZE_GA4 이벤트 샤드 → FLATTEN 정제).
- **이로써 빌드되는 GOLD**: `DIM_GA_SOURCE` · `DIM_GA_EVENT` · `DIM_DEVICE`(GA분) · `FGA` + `DIM_MEMBER_IDENTITY`(GA측 신원 매핑).
- **현 상태**: BRONZE_GA4 = 1일 샤드 `events_20260501`(287,025행)만 적재. **전기간 샤드 입고 후 트랙 B 본착수**.
- **범위 외**: CRM/ERP/AGENCY 객체(각 전용 문서). 신원 브리지 최종 완성(S-7)은 master §6.

---

## 1. 생성할 GA4 5개 객체

| # | SILVER 객체 | BRONZE 원천 | 그레인 | 충족 GOLD |
|---|---|---|---|---|
| 1 | `GA4_EVENT` | `events_YYYYMMDD`(전 샤드 UNION) | 이벤트 1건(event_timestamp×user) | FGA |
| 2 | `GA4_TRAFFIC_SOURCE` | `session_traffic_source_last_click`(UI 일치) | utm 조합(source/medium/content/term) | DIM_GA_SOURCE |
| 3 | `GA4_EVENT_DIM` | `event_name`·`event_params` | 이벤트 정의(category/label/action) | DIM_GA_EVENT |
| 4 | `GA4_DEVICE` | `device`·`platform` | 기기(PC/M/APP) | DIM_DEVICE(GA분) |
| 5 | `GA4_IDENTITY` | `user_id`·`user_pseudo_id` + URL 파라미터 | 식별자 1건 | DIM_MEMBER_IDENTITY(GA측) → S-7 |

> DDL 초안 = `_archive/09_SILVER_DDL_20260702.sql`(GA4_TRAFFIC_SOURCE·GA4_EVENT_DIM·GA4_DEVICE·GA4_EVENT·GA4_IDENTITY). 트랙 B 착수 시 `08_SILVER_테이블DDL_20260714.sql`로 이관.

---

## 2. 빌드되는 GOLD

| GOLD | 충족 SILVER | 상태 | 비고 |
|---|---|---|---|
| `DIM_GA_SOURCE` | `GA4_TRAFFIC_SOURCE` | 🟡 | utm source/medium/content/term. `LATERAL FLATTEN` 후 추출 |
| `DIM_GA_EVENT` | `GA4_EVENT_DIM` | 🟡 | event_category/label/action. `LATERAL FLATTEN(event_params)` |
| `DIM_DEVICE` | `GA4_DEVICE` (또는 seed) | 🟡 | PC/M/APP. **APP 정의**(GA4 platform vs 앱SDK) 확인 필요(O2) — AGENCY 기기와 conform |
| `FGA` GA_BEHAVIOR | `GA4_EVENT` | 🟡 | grain=일×IDENTITY×EVENT×SOURCE×DEVICE×CAMP×페이지. #98 평균세션시간·#108 이탈율 = **비가산 적재**(O1). 결연아동코드 URL 파싱 |
| `DIM_MEMBER_IDENTITY`(GA측) | `GA4_IDENTITY` → `IDENTITY_MEMBER_XREF` | 🟡 | GA `user_id` ↔ CRM 회원번호 매핑(S-7, master §6) |
| `FAD`(전환분) | `GA4_EVENT`(전환) | 🟡 | AGENCY 광고성과와 결합(전환수 GA측) — AGENCY 문서(06) FAD 참조 |

---

## 3. 트랙 B 선결·실측 검토 (S-6 착수 전 게이트)

- **Q1 — GA↔CRM 식별자 매핑 (⚠️핵심)**: GA4 `user_id`에 CRM 회원번호 심김 여부.
  - **실측(결론9)**: `user_id` **채움률 4.22%**(전수 12,120/287,025 · 식별 1,290명 · 익명 95.8%). 형식은 일치(조인 성립)하나 **회원단위 GA 지표는 부분 커버리지** → 전수 단정 금지, SV 커버리지 경고 필수.
  - 미설정 구간은 `user_pseudo_id` + `transaction_id` 간접연계 검토.
- **전기간 샤드 입고**: 현재 1일(`events_20260501`)만. 전 기간 `events_YYYYMMDD` 입고 후 UNION 정제.
- **DIM_DEVICE APP 정의(O2)**: GA4 `platform`(WEB/IOS/ANDROID) vs 앱 SDK 기준 — AGENCY 기기 차원과 conform 규칙 확정.

---

## 4. GA4 정제 규칙 (공통 규칙 + GA4 특수)

- **공통 규칙**: master §3 (타입·NULL·코드→라벨·메타 4컬럼·금액 원단위) 준수.
- **GA4 특수(스키마 스펙 기준)**:
  1. 중첩 컬럼(`event_params`·`items`·`user_properties`)은 `LATERAL FLATTEN`, leaf는 `::` 캐스팅.
  2. 트래픽소스는 `session_traffic_source_last_click` 사용(GA4 UI 일치).
  3. float은 `double_value` 사용(int_value 아님).
  4. **비가산 지표**(평균세션시간·이탈율)는 SILVER에 원자료로 적재, 율 계산은 GOLD/SV(O1).
- **⚠️ dbt 매크로 주의**: GA4 구축은 `10_dbt_pipeline/macros/ga4_union_shards.sql`(**대소문자 안전 수정본**) 기준. 문서 07/12/13의 예시 매크로는 소문자 샤드 버그 참고용. `ga4_union_shards`의 `SELECT *` → **명시 컬럼 30개**로 고정(주의사항 문서 참조).

---

## 5. 완료 정의 & 다음

> **🥇 착수 순서 = 1차** (master §7). GA4를 먼저 완성해야 **S-7 신원 브리지 → GOLD** critical path가 열린다. GA4 완료 직후 S-7(`IDENTITY_MEMBER_XREF`) 착수.
> **실행 사이클(master 원칙 A)**: 전기간 샤드 입고 → `08 DDL append`(구 `_archive/09` 초안 이관) → `09 적재쿼리 append` → 실행 → DQ 검증. 
> **블로커 事前 triage(master 원칙 B)**: §3 게이트 중 **전기간 샤드 미입고**가 유일한 하드 블로커. Q1(user_id 4.22% 커버리지)은 *비블로커* — 커버리지 경고를 달고 진행(descope 아님). O2(DEVICE APP 정의)는 AGENCY(3차)와 conform 시점에 확정.

- **완료 조건**: 전기간 샤드 입고 → GA4 5객체 SILVER 신설(S-6, S-1~S-5 반복) → GOLD `DIM_GA_*`·`DIM_DEVICE`·`FGA` 빌드 + `IDENTITY_MEMBER_XREF`(S-7) 구성.
- **다음(LLM)**: (a) 전기간 GA4 샤드 입고 확인 → (b) `_archive/09` GA4 DDL 초안을 `08`로 이관·신설 → (c) FLATTEN 정제 적재 → (d) 신원 브리지(master §6, S-7) → (e) `03_top-down_gold/08_silver의존.md` GA4 lineage로 GOLD 적재.
- **참조**: `07_GA4_SILVER_샤드통합 설계결정.md`(샤드 UNION 설계) · `12`/`13`(dbt·SP 작업 전 주의사항) · `14_bronze_ga4_events_schema.md`(BRONZE 구조).
