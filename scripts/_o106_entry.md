> #### 🟢 [2026-08-28 O106] 세션 시작 독해량 구조 개편 — 전량 독해 폐지 · 브리핑 생성물화 · stale 17건 제거

> **착수 경위** — 사용자가 `99_NEXT_SESSION` 문서군이 왜 이렇게 많은지 물었고, 이어
> *"20_issue 폴더 문서들과 99_next_session 을 세션 시작시 읽게 만드는데 너무 양이 많다"* 로
> 개선 요청이 왔다. 사용자 결정 = **세션 시작 필수 독해 상한 「2~3파일 · 총 100KB 이내」**.
> 이후 *"llm 전문가 입장에서 비판적으로 검토하고 이상 없다면 전부 승인"* · *"전부 python
> 스크립트로 재현 가능하게"* · *"이 계정은 문서작업용이니 DB 실검증 없이"* · 마지막에
> *"다중파일작성 전부 승인"* 이 왔다. `R4-4-2 ㉡`(지시 동봉) 분기로 진행했다.

> 🔴🔴 **진단 — 문제는 「양이 많다」가 아니라 「규약이 이행 불가능하다」였다.**
> `init_ihcho` Step 2 가 요구한 5문서 전량 독해를 실측하니:
> 지침 **18,109자** + `00_INDEX` **114,934** + `50_dbt` **190,010** + `99_NEXT` **146,825**
> = 「필수」만 **469,878자**(전 문서군 **1,678,616자**).
> ⇒ 지키면 컨텍스트가 터지고, 터지지 않으려면 **읽지 않고 읽은 척**하게 된다.
> 이것이 `R1-3-7`(미반환 = 미독)이 잡으려던 실패의 **상류 원인**이고,
> `R4-4-1` 브리핑 위반이 **6세션 연속** 등재된 것과 **같은 구조**다 — `R4-4` 가 이미
> *"원인은 게으름이 아니라 조문 설계"* 라고 같은 진단을 내려 놓고 **독해 축에는 적용하지 않았다.**

> 🔴 **최대 발견 = 「경고」로는 stale 이 멈추지 않는다.**
> 손으로 적은 조각 수를 전수 대조하니 **3곳 17건** 불일치이고 **세 곳이 서로 다른 값**이었다:
> `00_INDEX §0 분할표` · `00_INDEX §0 유형등재표` · `init_ihcho`.
> 🔴 **O83-H 가 8행을, O90 이 4건을 이미 교정하고 각각 「인용하지 말고 실측하라」를 적었는데
> 그 경고를 적은 표가 다시 stale 이 됐다.** ⇒ 진단은 두 번 끝났고 **집행 장치만 없었다.**
> 처방 = 교정이 아니라 **숫자를 적을 자리를 제거**하고 게이트로 0을 강제한다.

> 🟢 **신설 도구 6종**(전부 dry-run 기본 · 재현 가능):
> · `scripts/doc_census.py` — 조각 수·바이트·여유·독해 예산 실측 **+ 문서 기재값 대조**(`--strict` blocking)
> · `scripts/fix_stale_counts.py` — 기재된 숫자를 **실측 포인터로 교체**(21곳 · 스냅샷 자동)
> · `scripts/session_brief.py` — 브리핑을 **정본에서 기계 추출**해 `00_BRIEF.md` 생성
> · `scripts/gen_measure_backlog.py` — 라이브 실측 필요 항목 추출 → `92_…md`
> · `scripts/split_narrative.py` — 조문 경위 서사 무변경 이관(**불변식 3종 blocking**)
> · `scripts/test_doc_census.py`(3축 12단정) · `scripts/test_session_brief.py`(5축 12단정)

> 🟢 **신설 문서 3종**(유형 등재표 선등재 → 게이트 3곳 편입 → 골든 발행 순서 준수):
> · `20_issue/00_BRIEF.md` — **자동 생성** 세션 진입점. 🔴 색인이고 정본이 아니다(제목+상태+**좌표**만).
> · `20_issue/91_사고사례집.md` — 지침 경위 **12블록** 무변경 이관부.
> · `20_issue/92_실측필요_후속작업.md` — **19항목**(배포/객체 실재 9 · dbt 실행 9 · 재측정 9 · 적재후 4 · 사람 UI 2).

> 🟢 **성과(실측)** — 세션 시작 필수 독해 **469,878자 → 24,500자 / 42,940B / 2파일**(**-94.8%**).
> 사용자 상한 「2~3파일 · 100KB」 충족. 조각 수 stale **17 → 0**.
> 지침 **33,495B → 30,994B** ⇒ `doc_type_gate` 축3 🟠 **해소**.
> 재균형·분할로 축3 경고 **4건 → 1건**(`01_세션이력` 꼬리만 잔존):
> `90_해소완료_로그` 미분할 38,730B → **조각 3개 · 최소 여유 23,629B**(2단계 = `--outdir` 후 `--rebalance --fill 0.5`) ·
> `00_INDEX` 여유 3,448 → **16,508B**(조각 8) · `30_설계` 4,356 → **16,494B**(조각 12).

> 🔴🔴 **자기시정 5건 — 전부 음성 테스트가 잡았고, 정상 입력으로는 하나도 발견되지 않았다.**
> · ㉠ `doc_census` 분모가 좁아 **17건 중 13건만** 잡음(같은 줄 문서명 1개만 봄) ⇒ 최장 stem + 2줄 이월.
> · ㉡ `fix_stale_counts` 가 *"꼬리가 차면 새 조각 **1개**로 끝난다(O(1))"* 를 오탐 ⇒ **문서 stem 가드**.
>   🔴 그대로 적용하면 `R1-6` 조문의 뜻이 바뀌었다(`R2-8-3` 위반 직전에 멈췄다).
> · ㉢ `session_brief` 착수표 추출이 **4열 이상 표 전부**를 먹어 허브 조각 목차 13행이 실림 ⇒ **헤더 서명 한정**.
> · ㉣ `scan_text` 가 `PATS` 두 정규식 중복으로 같은 항목 **2번** 반환 ⇒ 함수 내부 dedup.
> · ㉤ 긴 행(원장 §1 = 최대 **1,644자**)에서 「같은 줄 문서명 1개」 폴백이 **무관한 문서에 귀속** ⇒ `SHORT_LINE=300` 가드.
> 🟢 **일반화**: 게이트를 만들면 **음성 테스트를 같이 만든다**(`R3-2` · `init_ihcho` ⑥ 신설).

> 🔴🔴 **실사고 1건 — `R1-7-9` 를 O106 이 다시 어겼다.**
> 원장 행 등재를 `python3 -c "…"` 로 했고 그 안의 백틱 2개가 **셸 명령치환으로 해석**되어
> (`bash: R1-3-7: command not found` · `bash: R4-4-1: command not found`)
> `` `R1-3-7`·`R4-4-1` `` 구간이 삭제돼 `(· 6세션 연속…)` 이 됐다. **피해 1곳 · `edit` 툴로 복구 · 손실 0.**
> 🔴 지침이 *"백틱 구간이 명령치환으로 해석되어 23줄에서 전부 삭제"* 로 경고한 그 사고의 **2번째 발생**이다.
> ⇒ `init_ihcho` Step 0.7 축에 **「`python3 -c` 도 셸을 경유하므로 안전하지 않다」** 를 명문화했다.

> 🔴 **계획 결함 6건을 착수 전 검토가 잡았다**(전부 내 초안의 오류):
> · **D1** `retire_rows.py` 는 **표 본문 행의 장문 셀**만 다룬다(`big_cell_index`) ⇒ 「50_dbt 🟢 18절 은퇴」
>   계획은 **도구가 없어 불가능** ⇒ **철회**. 절 단위 은퇴 도구는 여전히 **미신설**이다.
> · **D2** 은퇴 목적지 `90` 이 꼬리 여유 **2,230B** 로 이미 포화 ⇒ 목적지를 신규 `91` 로 변경 + `90` 선행 분할.
> · **D3** 「3파일 설계」가 `99_NEXT-009` **착수 순서**(열린 작업 11건의 정본)를 버렸다 ⇒ 브리핑이 추출하도록 변경.
> · **D4** 「`99_NEXT` 12조각은 무효 지층」은 **과대 주장** — `-009`(착수표)·`-013`(§7 운영 환경 지뢰)은 현행 필수.
> · **D5** BRIEF 상한을 40KB 로 잡았으나 `MIN_FREE=0.20` ⇒ **32,768B** 여야 축3 경고가 안 난다.
> · **D6** stale 규모를 2건으로 과소 추정(실제 17건 · 도구가 내 손 대조 15건보다 2건 더 잡았다).
> 🟢 **일반화**: 「감소율 40~60%」 같은 추정을 **측정 전에 말하지 마라** — 실제 경위 분리는 **9.8%** 였고
> 진짜 이득은 비율이 아니라 **축3 경고 해소**였다.

> 🔴 **미독 명시**(`R1-3-7` 「대체 근거 금지」 이행) — `10_진단_원인분석` · `30_설계_의사결정` ·
> `02_상태상세_대시보드_갱신형` · `20_현업확인_요청` **전건 미독**이다.
> ⇒ 그 문서 소관 판정·수치를 **인용하지 않았다.** 「절 제목 89개 상태 무표기」는 `grep` 결과라 유효하지만
> 「닫힌 절이 많아 은퇴 여지가 크다」는 추정은 **근거가 없어 철회**했다.
> 🔴 착수표 **⑥** 이 *"문서10 전량 독해 미이행 — 6세션 연속"* 으로 등재돼 있고 **O106 이 7번째**다.

> **`read` 미반환**(`R1-3-7-a` 판정식 = 재호출 ≥ 미반환) = 미반환 **0건** · 재호출 **0건**.
> 독해 좌표(`R1-3-7-b`) = `00_guides/00_작업지침…`(전량 · 고유토큰 `R1-3-7-b`·`P231`·`MIN_FREE`) ·
> `scripts/retire_rows.py`(전량 · `big_cell_index`·`TOK_CODE`) · `scripts/doc_type_gate.py`(전량 · `EXTRA_DOCS`·`CHUNK_RX`) ·
> `20_issue/00_INDEX-001`(1~140 · `문서 유형 등재표`·`좌표 환산표`) · `99_NEXT_SESSION-009`(전량 · `착수 순서`·`⑫`·`⑭`) ·
> `99_NEXT_SESSION.md` 허브(34~75 · `0-KKK`·`0-JJJ`) · `.snowflake/…/init_ihcho/SKILL.md`(115~175 · `Step 2`·`Step 3`).

> **게이트 최종**(전건 재실행) = `doc_census` stale **0** · `doc_type_gate` 축1 **0**·축2 **0**·축3 경고 **1** ·
> `clause_order_gate` 조문 **88** 역전 0 중복 0 · `index_row_gate` 유실 0 중복 0 ·
> `doc_heading_gate` 제목 **1,063** 유실 0(골든 발행 1회 · 신설 26 승인) · `line_len` PASS ·
> `ws_stage_verify` 반영 **전건** · 음성 테스트 **24단정** 전건 통과.

> 🟠 **잔여(다음 세션)** = ① **절 단위 은퇴 도구 미신설**(D1) ② `01_세션이력` 꼬리 여유 부족(축3 잔존 1건) ③
> `10_진단`·`30_설계`·`02_상태상세`·`20_현업확인` 미독 해소(착수표 ⑥) ④ 계정 확인 — 문서 기재
> `NX55103`/`ZL50263` ↔ 이번 세션 컨텍스트 **`sz99178`** 로 **`P169` 재발 가능**(SQL 미실행 · `92 §0` 소관).

_Co-authored with CoCo_
