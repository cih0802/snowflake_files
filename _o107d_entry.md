> #### 🟢 [2026-08-28 O107-D] 폴더 이전 **선행 수정 3건** — 자기비판으로 적발한 결함을 닫았다
>
> **계기(사용자 지시)** = 「다른 허브 문서들로 확장해도 될지 방금 작업을 비판적으로 검토해」
> ⇒ 검토 결과 **확장 금지** 판정 + 선행 수정 3건 확정. 실제 7문서 이전은 **다음 세션**으로 이월.
>
> **🔴 결함1 — `to_outdir` 의 바이트 동일 검증이 자기참조적이었다 (가장 심각)**
> * 초판은 ㉠ digest 를 **형제 읽기**에서 뽑고 ㉡ 폴더를 되읽어 비교한 뒤 ㉢ **같은 실행에서**
>   형제를 지웠다 ⇒ 형제 읽기가 stale 이면 **양쪽이 함께 stale** 이라 검증이 통과하고
>   **fresh 내용을 담은 형제가 삭제**된다. `§O107-B` 사고의 **증폭판**이다
>   (그 사고는 재균형이라 원본이 남았지만, 이 도구는 **지운다**).
> * 🔴 99_NEXT 이전이 안전했던 것은 도구의 보장이 아니라 **우연**이었다 — 이전 직전에
>   `--republish` 를 해서 **허브 발행 SHA == 폴더 concat**(실측 `True`)인 독립 기준선이 있었다.
> * 🟢 처방 = **방어 3중**으로 재설계:
>   · **선행 해시 대조(blocking)** = 허브 **발행 SHA256** ↔ 형제 concat 일치해야 착수.
>     불일치면 「미재발행 편집」 또는 「stale 읽기」로 보고 중단하고 `--republish` 를 요구한다.
>   · **2단계 분리** = 1단계는 **복사만**(형제·마커 유지) · 2단계 `--drop-siblings` 에서
>     **그 시점에 다시 읽어** 허브·형제·폴더 **3자 일치**를 확인한 뒤 삭제.
>     🟢 **이것이 본질적 방어다** — 시점이 다른 두 번째 읽기가 생겨 **시점 간 불일치**를 잡는다.
>     🔴 `R1-7-2`(같은 시점 2회 읽기)로는 이 축이 잡히지 않는다(그래서 O107-B 가 났다).
>
> **🔴 결함2 — `to_outdir` 는 스냅샷을 만들지 않는 유일한 파괴 연산이었다**
> * 실측 = `to_outdir` 스냅샷 **False** / `os.remove` 1회 ↔ `build`·`rebalance`·`republish` 는
>   전부 **True**. 14파일을 지우면서 **되돌림 경로가 없었다**.
> * 🟢 처방 = 1단계가 논리 문서를 `_archive/<base>.O107-pretooutdir` 로 남긴다.
>
> **🔴 결함3 — 「기능적 참조 0건」은 99_NEXT 한정 사실이었다(범위 과대 일반화)**
> * `scripts/fix_stale_counts.py` 의 `TARGETS` 가 **00_INDEX 조각 경로 7개를 하드코딩**한
>   **기능 코드**였고, **이미 stale** 이었다 — 실제 조각은 **8개**인데 `-008` 이 목록에 없어
>   그 조각의 stale 수치는 **검사 밖**이었다.
>   🔴 이 도구가 막으려던 실패(「손으로 적은 수는 낡는다」)를 **도구 자신이 갖고 있었다**(`P106`).
> * 🟢 처방 = 조각 경로를 지우고 **허브만 선언** → `doc_census.chunk_paths` 가 마커로 확장한다
>   ⇒ 확인 결과 `-008` 이 **자동 편입**(7 → 8) · 형제·폴더 어느 배치에서도 추종한다.
>
> **🔴 결함4 — `decision_closure_gate` 분모 붕괴(증명 완료)**
> * 실측 = `30_설계_의사결정-*.md` 현재 **12건 → 폴더 이전 시 0건** ·
>   `20_issue/*.md` 1단계 glob 이 폴더 조각을 보는가 = **False**.
>   🔴 이 게이트는 **경고 전용**이라 분모가 0이 되어도 아무도 알아채지 못한다 — 가장 나쁜 조합.
> * 🟢 처방 = `_md_files()` 신설(직속 + **1단계 하위 폴더**) · 두 수집 지점을 그것으로 교체.
>   ⇒ 인용처 분모 **90 → 129건**(폴더 조각 **39건** 편입) · 결정 정의 분모 12건 유지.
>
> **🟢 검토에서 확인된 강점(확장 판단의 근거)**
> * **행 키 축은 안전하다** — 골든에 `조각 목차 ¦ 99_NEXT_SESSION-001.md` 키가 **7건 있는데도**
>   유실 0인 이유는 `index_row_gate` 가 `조각 목차`·`구 행번호 → 신 좌표 대응표`를
>   **의도적으로 제외**하기 때문이다(`GENERATED_SECTIONS` · O83-B). 오탐이 아니라 설계다.
> * `doc_line_length_gate` 는 **glob 0건을 자가검출**한다(분모 자기검사 14/14) ·
>   `id_collision_gate` 는 `*/*.md` 로 이미 폴더를 본다.
>
> **확장 순서(인용 건수 역순 · 실측 총 45건)**
> `03_이슈상세` **0** → `02_상태상세` 2 → `10_진단` 3 → `30_설계` 6 → `50_dbt` 7 →
> `20_현업확인` 8 → **`00_INDEX` 19(마지막)**. 🔴 00_INDEX 는 원장 자신이라 사고 시 파급이 최대다.
>
> **게이트** = census stale 0 · 유형 · 조문 **91** 역전 0 · 행 유실 0 · 제목 유실 0 ·
> `line_len` PASS · 음성 테스트 4종(8축 **76단정** · 9축 55 · 21 · 12).
> 🔴 **이 세션에서 실제 이전은 하지 않았다**(사용자 결정 = 새 세션에서 이어서).
