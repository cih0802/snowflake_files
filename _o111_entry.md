> #### 🟢 [2026-08-28 O111] O110 승계 미결 `▣EEEE ①~⑥` 전건 착수 + 도구 4종 결함 시정 + 음성 테스트 1종 신설
>
> **인수** = `99_NEXT §0-PPP ▣EEEE`(O110 이 닫지 못한 6건). **`R4-4-2 ㉡` 적용**(지시 동봉 ⇒ 브리핑 후 즉시 수행).
> 🔒 라벨 = `--next O` **정의 110 ↔ 참조 110 ⇒ `O111`** · 🟢 **착수 전 원장 §1 에 선점 등재**(`R1-4-3` · O110 에 이어 2세션 연속 이행).
> **사용자 결정 2건**(`ask_user_question`) = ㉠ 지침 여유 부족 → **경위 서사 추가 이관** · ㉡ 스킬 비대 → **사고 경위를 `91` 로 이관**.
>
> ##### ① 지침 여유 부족 해소 (`▣EEEE ①` · 사용자 결정 ㉠) — 🟢 닫힘
>
> 착수 실측 = `doc_type_gate` 축3 🟠 「max 여유 **7,974 B**」(상한 40,960 대비 19.5%) · 지침 32,986 B / 319줄.
> `split_narrative.py --apply` 로 서사 블록 **3개**를 `91_사고사례집.md` `§C13`~`§C15` 로 무변경 이관 ⇒
> 지침 **32,986 → 32,200 B**(−786 B) · 여유 **8,760 B** ⇒ `doc_type_gate` **여유 부족 1 → 0 · ✅ 통과**.
> 불변식 = 조문 ID **65종 소실 0** · 제목 **6 소실 0** · 토큰 **7종 부재 0** · 기존 앵커 **12 보존**.
> 재실행 = **0건**(멱등) · 포인터 **15개 ↔ `## C` 15개** 일치.
>
> 🔴🔴 **초판은 「서사 블록 0개 — 이미 분리돼 있다」를 냈다.** 원인 = `NARRATIVE` 정규식이
> `왜 필요한가`·`왜 이 규칙이 있는가` 만 알고 원본에 실재하는 변형 3종을 몰랐다:
> `왜 바꿨나`(48행) · `왜 예외가 필요한가`(68행) · `왜 이 규약이 있는가`(163행) — 전부 `grep` 실측으로 확인.
> ⇒ 🟢 **일반화: 0건 출력은 「없다」가 아니라 「내 판정식이 못 본다」일 수 있다.** 도구 주석에 명문화했다.
>
> ##### ② 🔴🔴 도구 최대 결함 — `split_narrative.py` 가 목적지를 통째로 덮어썼다
>
> 착수 직전 코드 실측 = `write_text(dst, dtext)` 로 **목적지를 무조건 새로 쓴다.**
> 1회차(O106)에는 목적지가 비어 있어 무해했지만 **2회차 실행이면**
> ㉠ 기존 `§C01`~`§C12` **본문 12블록이 경고 없이 사라지고**
> ㉡ 새 블록이 다시 `C01` 부터 번호를 받아 **지침에 남은 포인터 12줄이 다른 내용을 가리킨다.**
> 🔴 **기존 불변식 1~3 은 「원본」만 검사하므로 이 사고에 전부 🟢 를 낸다** — 원리적으로 못 잡는다.
> ⇒ 🟢 **불변식 4 신설**(blocking) = 기존 앵커·표 행·본문 줄 **전건 보존** + **번호 재사용 0** +
> 머리말 원문 보존(손으로 적은 문장·`created_by` 포함) + 표 머리·꼬리 이중화 금지 + **목적지 스냅샷**.
>
> 🔴 **그 불변식 4 초판조차 결함이 있었다** — 「신규」 판정을 `parse_existing` **반환값**으로 했다
> (`if ex_prefix is None and ex_max == 0`) ⇒ **파서가 기존 내용을 못 보는 바로 그 실패 모드에서
> 검사가 조용히 건너뛴다**(`R3-9 ㉢` 「분모를 등재하지 않으면 0 을 내며 침묵」과 같은 축).
> ⇒ 분모를 **디스크 원문**(`dst_pre`)으로 교정. 🔴 **이것을 잡은 것은 게이트가 아니라 내가 만든 음성 테스트 축6 이다.**
>
> 🟢 **`scripts/test_split_narrative.py` 신설 — 6축 27단정**(`R3-2`):
> 축1 신호어 변형 재현율 · 축2 조문 정의 자신은 미이관(오탐 0) · 축3 처방 마커(🟢/⚠️/**⇒**) 미이관 ·
> 축3b **개념 인용 가드**(`「왜 필요한가」` 는 서사가 아니다) · 축4 신규 목적지 = `C01` 부터 ·
> 축5 **기존 목적지 append 보존 9단정** · 축6 **음성 = 파서를 눈감게 만들면 blocking FAIL 이어야 한다**(rc 1 · 파일 불변).
>
> ##### ③ 스킬 사고 경위 이관 (`▣EEEE ②` · 사용자 결정 ㉡) — 🟠 목적 미달
>
> 착수 실측 = `init_ihcho/SKILL.md` **24,719자 / 44,073 B / 449줄**(인수인계 값과 일치).
> `--signal` 옵션을 신설해(원본별 신호어 · 전역 `NARRATIVE` 에 넣으면 **지침의 포인터 줄 「경위·실사고 근거 = …」**
> 자체가 다시 서사로 잡혀 무한 재이관된다) 블록 **3개**를 `§C16`~`§C18` 로 이관 ⇒ **24,208자 / 43,002 B**(−511자 · **−2.4%**).
>
> 🔴 **미달을 「처리」로 쓰지 않았다**(`▣UUU` 일반화 = 「회피·미달은 미시정으로 분류하라」).
> 🔴 **더 옮길 수 없는 이유(실측)** = 자동 추출이 잡은 5블록 중 **2블록은 옮기면 처방이 소실된다**:
> ㉠ 336행 `· 🆕 **조문의 「왜 필요한가」 경위 서사**는 `split_narrative.py` 로 … 이관한다` = **도구 사용 지시**인데
>    「왜 필요한가」를 **개념으로 인용**해서 서사로 오탐됐다 ⇒ **개념 인용 가드**(`「…」` 제거 후 판정) 신설.
> ㉡ 289~292행 블록은 **`⇒` 가 줄 중간**에 있어(`… 구간이 삭제됐다(…). ⇒ 🔴 본문에 백틱이 있으면 edit/write 툴을 쓴다.`)
>    줄 단위 절단으로 경위와 처방을 분리할 수 없다 ⇒ **의도적으로 제외**(`--signal` 을 좁혔다).
> 🟢 부수 = `⇒` 로 시작하는 줄을 `PRESCRIPTION` 에 추가해 `[O109 실사고]` 블록의 **4축 사후검증 처방 2줄**을 지켰다
> (종전 판정식은 그것까지 끌고 갔다 — 5줄 → 3줄로 축소됨을 dry-run 으로 확인).
> ⇒ 🔴 **결론 = ㉡ 는 소진됐다. 스킬 비대의 본체는 서사가 아니라 Step 0.7 조문 요약이고, 그 축약은 사용자 결정이다.**
>
> ##### ④ `index_row_gate` 중복 은폐 (`▣EEEE ③`) — 🟢 닫힘 · 🔴 인수인계 진단은 틀렸다
>
> 🔴 **인수인계는 「`GENERATED_SECTIONS` 제외 규칙에 가려졌다」고 적었으나 실측은 다르다.**
> 실측 = `00_INDEX` 리스트 **291** ↔ 집합 **281**(초과 10행) · 중복 키 **2종** · **자동 생성표 소관 0종**.
> 정체 = `🔷 Phase 배정 (2026-07-30 확정) ¦ P2`(×8) · `¦ P1`(×4) ⇒ **첫 셀이 유일키가 아닌 정상 표**다
> (골든도 같은 수를 담고 있어 「증가 0」이 정확한 판정이었다). ⇒ **blocking 으로 올리면 정상 문서가 막힌다**(`P130`).
>
> 🟢 **실제 결함은 판정이 아니라 문구였다** — 요약이 `중복 0` 이라 「중복이 없다」로 읽혔지만 뜻은 「**골든 대비 증가 0**」이었다.
> 처방 3건 = ㉠ 제외 규칙(`is_generated`)을 **「유실」 축에만** 적용(O83-B 근거 「값이 매번 바뀐다」는 **중복 축에 성립하지 않는다**)
> ㉡ **절대 중복을 숫자로 노출**(행·고유·중복행·키수·자동생성표 소관 + 표본) ㉢ 판정 문구를 「골든 대비 중복 증가 0」으로 정정.
> ⚠️ 주의 문구는 **판정 줄보다 먼저** 찍고 이모지로 시작하지 않게 했다 — `session_brief.gate_verdict` 는
> **마지막 상태 이모지 줄**을 판정으로 읽으므로 뒤에 🔴 줄을 두면 브리핑이 이 게이트를 **FAIL 로 표시**한다(O109 D5 와 같은 축).
>
> 🟢 **`scripts/test_index_row_gate.py` 신설 — 5축 12단정**. 🔴 **오탐·재현율 양축**을 함께 단정한다:
> 축1 정상 append 통과 · 축2 유실 FAIL · 축3 중복 FAIL · **축4 자동 생성표의 중복도 FAIL(신설) + 그 표의 값 교체는 통과(오탐 축) +
> 사람 원장 행 교체는 유실 FAIL(대조군)** · 축5 절대 중복 관측 + **관측 축이 판정 축을 삼키지 않는다**.
> ⚠️ O82-B 는 「4종으로 테스트했다」고 적었지만 **테스트 파일이 남지 않았다** — 이 파일이 그 수단이다.
>
> ##### ⑤ `doc_coord_gate` 경고 처분 (`▣EEEE ④`) — 🟢 닫힘 · 🔴 여기서도 전제가 게이트 오분류였다
>
> 🔴🔴 **인수인계의 「파일명 3종 · `_archive` 에도 없다 ⇒ 삭제인지 개칭인지 사람 판단」은 잘못된 전제였다.**
> 전역 `find` 실측 = `02_원천결손_Gap분석.md`·`00_README_이관안내.md` 는 **`30_output_share/_archive/20260716`·`20260806` 에 실재**한다.
> 원인 = `basename_index()` 가 `SKIP_DIRS`(= `_archive` 포함)를 **가지치기**해 등급이 `dead` 로 떨어졌다
> ⇒ **축1b 9건 중 8건이 오분류**였고, 그 결과 **하지 않아도 되는 삭제 판단 8건**이 다음 세션으로 넘어갔다.
> 🟢 처방 = `_archive` 를 **스캔 원본에서는 계속 제외**하되 **실재 판정 분모에는 포함**하고 등급 `archived` 로 분리
> ⇒ **축6 「은퇴본만 존재」(관측) 신설** + `test_doc_coord_gate.py` **축11 신설**(재현율 대조군 = 진짜 부재는 여전히 축1b).
>
> **처분 결과** = 축1b **9 → 1** · 축2 **7 → 3** · 축3 **6 → 3** · 축4 **4 → 1** · 축5 **7**(불변) · 축6 **12**(신설·관측).
> · 축1b 잔여 1 = `20_issue/00_통합이슈_레지스트리_20260715.md` — 인용처가 `supersedes:` 메타였고 **이 원장으로 흡수·삭제**된 것이므로
>   `supersedes_status:` 3줄을 신설해 **「무엇이 됐는지」**를 그 자리에 적었다(전역 실측 근거 병기).
> · 축3 시정 3건 = `10_진단-014:136` 은 **인용 문구가 그대로 실재**해 새 좌표로 갱신
>   (`20_issue/20_현업확인_요청_조각/20_현업확인_요청-002.md:122`) · `50_dbt-010:58`·`02_상태상세-003:23` 은
>   인용 문구가 현행 조각에 **없어** 「행 번호 없는 인용」으로 전환 + 사망 사실 병기.
> · 축4 시정 3건 = 약칭을 전체 경로로 교체. **잔여 1건은 표의 첫 셀**이라 바꾸면 행 키가 깨진다(`R1-7-4`)
>   ⇒ **같은 행 2번째 셀에 정확한 경로를 병기**하고 그 사실을 적었다.
> · 축3·축5 잔여(3+7)는 **전부 `01_세션이력`** ⇒ `R1-3-6`(과거 기록 불수정)으로 손대지 않았다.
> 🟢 부수 = `split_narrative` 가 쓰는 포인터를 **basename → 전체 경로**로 바꿨다(축5 모호성을 원리적으로 회피).
>
> ##### ⑥ O108·O109 승계분 (`▣EEEE ⑥`) — 🟡 부분
>
> 🟢 **`▣WWW ③`(문서 4종 전량 미독) = 폐지 승계 표기.** 이 항목은 **착수표 ⑥ 과 같은 것**이고 그 행은
> O109 가 재정의·O110 이 취소선 처리했는데 **`▣WWW ③` 은 그대로 남아 있었다** ⇒ 다음 세션이 여기를 읽으면
> **폐지된 처방(「`read` 60~100줄 분할」로 4문서 전량 독해)을 그대로 실행**한다. 취소선 + 「실행하지 마라」 + 대체 처방을 적었다.
> 🟢 **`▣WWW ⑥` 남은 판단 = 결정.** 허브 「조각 선택표」의 `- ID:` 줄은 `--republish` 가 매번 다시 쓰는 **자동 생성물**이라
> 「병기해 닫을」 자리가 **원리적으로 없다** ⇒ `decision_closure_gate` 인용처 분모에서 **제외**(`GENERATED_ID_LINE`).
> 🟢 **재현율 실측 = 미봉합 표행 21건 불변**(제외 후에도 동일) ⇒ 관측 노이즈만 줄었고 검출은 그대로다.
> 🔴 **미착수 = `▣WWW ④⑤⑦`** — 절 은퇴 실적용 · 승계 절 적층 정리 · 임시 스크립트 삭제. **셋 다 `R4-4-3` 승인 대상**이라
> 지시가 동봉됐어도 손대지 않았다(그 예외가 `R4-4-2` 보다 강하다).
>
> ##### ⑦ `test_generators` FAIL (`▣EEEE ⑤`) — 🟠 전제 정정 · 골든 미갱신
>
> 🔴 **「라이브 데이터가 필요하다」는 전제가 틀렸다.** 차이 3건의 출처는 **수기 문서**
> `03_top-down_gold/05_필드 인벤토리.md` 다(`DOC_INV`) — 라이브 스키마와 무관하다.
> 실측 = `inv.entries` 328→**331** · `inv.index_keys` 311→**314** · `inv.desc_cells` 53→**56** = **일관된 +3**
> (`inv.label_collisions` 14 · `inv.unreachable` 0 은 불변 ⇒ 엔트리 3건이 추가되고 각각 색인·설명 셀을 가진 형태).
> 🔴 **그래도 `--update-golden` 을 하지 않았다** — 골든이 **카운트만** 보관해 「어느 3건인가」를 귀속할 수 없고,
> 그 문서의 이전 판본이 `_archive` 에 **없다**(전역 실측) · 이력 grep 으로도 귀속 실패 ⇒ **규명 불가**.
> 🟢 **처방 확정(다음 세션)** = `measure()` 가 그 세 지표를 **카운트 대신 「엔트리 키 목록」**으로 저장하게 고친 뒤
> 차이를 규명하고 갱신한다. 🔴 **순서를 지켜라** — 먼저 갱신하면 규명 기회가 영구히 사라진다.
>
> ##### 🔴 자기결함 5건 (전건 자기신고 · 게이트 6종이 전부 🟢 인 상태에서 남는다)
>
> ㉠ **`▣EEEE ②` 실질 미달** — 사용자 결정 ㉡ 를 수행했으나 감소 **2.4%**. 목적(세션 시작 독해 경량화)에 도달하지 못했다.
> ㉡ **`decision_closure_gate` 를 고쳤는데 음성 테스트를 만들지 않았다**(`R3-2` 위반). 완화 = 재현율만 실측(21건 불변).
> ㉢ **도구를 고친 세션이 그 도구로 자기 작업을 통과시켰다**(순환 · `▣VVV ㉢` 재발). 완화 = 음성 테스트 양축뿐.
> ㉣ **`split_narrative.py` 에 중복 구현 2건이 겹쳐 있었다** — `parse_existing` 이 두 곳에 정의돼 뒤 정의가 앞을 가리고
>    표 머리가 이중 출력될 상태였다. `R1-7-2` 대로 **해시 3회 안정**(21,325 B · `759e4bac9459c323`) 확인 후 `edit` 로 제거하고
>    4축 검증(`py_compile` · `def` 8개 · 꼬리 · `__main__`)을 돌렸다.
> ㉤ 🔴🔴 **원장 §1 `O111` 행이 한 번 소실됐다.** 추가 직후 `grep -c` = 1 로 확인했는데(그리고 `--republish` PASS),
>    이후 재확인 시 **부재**였다(조각 줄 수도 감소). **조각 스냅샷이 없어**(`_archive` 에는 허브 스냅샷만) 복원은 **재작성**뿐이었다.
>    ⇒ O107 `▣OOO` 형 사고(「유지 연산이 낡은 내용을 읽어 편집이 사라진다」)의 재발이고 **게이트 6종이 전부 🟢 였다.**
>    🟢 그 자리에서 재작성 복원 → **`grep` 전후 2회** + `line_len` + `--republish` PASS 로 고정했다.
>    ⚠️ 복원 과정에서 행이 **3,061자 → 2,007자 → 1,911자**로 두 번 초과했다(표 행은 개행 불가 · `R1-5-1`)
>    ⇒ 상세를 이 이력으로 넘기고 행을 압축했다.
>
> ##### 🟢 게이트·검증 (종료 시)
>
> `doc_census` stale **0** · `doc_type_gate` **✅ 여유 부족 0**(종전 🟡) · `clause_order_gate` ✅ 조문 93 ·
> `index_row_gate` ✅ 유실 0 · 골든 대비 중복 증가 0 · `doc_heading_gate` ✅ 제목 유실 0(신설 6 = `§C13`~`§C18`) ·
> `doc_coord_gate` ✅ 축1a 0 · `decision_closure_gate` 🟠 21(전부 이전 세션).
> 음성 테스트 = `ls scripts/test_*.py` **11종** ⇒ **10종 통과 · `test_generators` 1건 FAIL**(위 ⑦).
> `read` **미반환 0건 · 재호출 0건**(`R1-3-7-a` 판정식 충족).
> 🔴 **`R4-4-2 ㉡` 로 진행한 세션이므로 확정위반 건수를 함께 적는다**(`R4-4-4`) = **확정위반 1**(㉡ `R3-2`) ·
> 자기결함 총 **5**(㉠~㉤) · 🔴 이 표를 「지침 준수」의 증거로 쓰지 마라(`R3-9`).
