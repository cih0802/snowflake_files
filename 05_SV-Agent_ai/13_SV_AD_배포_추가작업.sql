-- [DEPRECATED 2026-07-31] SV_AD 잔여 배포 작업 — 05·09 로 분해 완료된 포인터 스텁
-- Co-authored with CoCo
-- ============================================================================
-- 이 파일은 더 이상 실행 대상이 아니다. 내용이 전부 이관되었다.
--
--   원 §1  소유권 정상화(SV_AD·FACT_AD_COMBINED)
--          → **폐기**. cs94293 1회성 보정이었다. 05 를 `USE ROLE GN_DW_ADMIN` 으로 실행하면
--            애초에 발생하지 않는다. 잘못 생성한 경우의 복구 SQL 만 05 §8-11 주석에 남겼다.
--   원 §2  AGENT_OVERALL 비파괴 갱신 경로 + DESCRIBE AGENT
--          → **09_AGENT_spec_구현.sql** 헤더 경로선택표 · [4-B] · [5-1]
--   원 §3  USAGE grant 3건 + CoWork SI 재등록
--          → **09** [3] · [4]. 09 쪽이 멱등 EXECUTE IMMEDIATE 로 더 안전하다.
--   원 §4  최종 검증 — SV·helper grant(4-1~4-3)  → **05_SV_DDL.sql §8-11~8-13**
--                    — Agent grant·SI(4-4)      → **09 [5]**
--   원 §5  SV_AD 스모크 7건                      → **05_SV_DDL.sql §8-4~8-10**
--   원 §6  현업 확인 근거 쿼리 5건               → **03_SV_metric_배속.md §8.5**(§6-H·§6-I·§6-J)
--   원 §7  구조적 제약 교훈 5건                  → **04_SV_설계.md §6.9**
--
--   전문 이력(2026-07-28 cs94293 실측값 포함) = `_archive/13_SV_AD_배포_추가작업_원본_20260728.sql`
--
-- ============================================================================
-- ▶ 실행 순서 정정 (이 파일이 존재했던 이유이자, 틀렸던 부분)
-- ============================================================================
--   구 13 §0 은 "05 → 09 → 13", 구 09 헤더는 "05 → 13 → 09" 로 **서로 반대**를 적고 있었다.
--   판정: 13 에는 객체를 만드는 DDL 이 한 줄도 없었다(소유권 보정·GRANT·검증·스모크뿐).
--         09 가 필요로 하는 SV_AD·FACT_AD_COMBINED 는 05 가 만들고, 13 의 Agent 검증절은
--         09 실행을 전제한다 → 참인 순서는 **05 → 09** 이며 13 은 사후 검증 문서였다.
--
--   ▶ 신규 계정 재현 순서 (2026-07-31 mq60369 에서 실증)
--     1. 02_GN_DW_building/07_ENVIRONMENT_RBAC_setup.sql   -- WH 3·역할 6·SERVING·CoWork SI object
--     2. (dbt 파이프라인 BRONZE→GOLD)                       -- GOLD 는 FUTURE grant 로 신규 FACT 자동 커버
--     3. 05_SV_DDL.sql                                     -- SV 6종 + helper 뷰 + §7 GRANT + §8 검증
--     4. 09_AGENT_spec_구현.sql [1-ALT]→[3]→[4]→[5]         -- Agent 2개 + USAGE 6건 + SI 2건 + 검증
--        ※ [2] 소유권 이전은 [1-ALT] 를 GN_DW_ADMIN 으로 실행하면 불요.
--
--   ▶ 변경(업데이트) 시 — "언제" 가 아니라 "무엇을 바꾸는가" 로 갈린다
--     · SV/데이터만 변경        → 05 통째 재실행(§7 GRANT 포함) → 05 §8 스모크
--                                 ※ 05 는 최초·재배포 공용 단일 파일이다. update 스크립트는 없다.
--     · Agent 스펙 변경         → cortex_project/*.agent.yaml 정본 갱신 → 09 [4-B] → 08 §3 동기화
--     · 둘 다                   → 05 → 09 [4-B] (09 헤더 "변경 반영 순서")
--
-- ============================================================================
-- ▶ 이관 중 발견해 교정한 결함 (2026-07-31 실측)
-- ============================================================================
--   1) 구 05 스모크의 `FILTER fap.AD_SOURCE_TYPE = 'DIGITAL'` 은 **컴파일 실패**한다
--      ("syntax error ... unexpected 'fap'"). SEMANTIC_VIEW(...) 내부 FILTER 로 별칭 컬럼을
--      쓸 수 없다 → 차원을 DIMENSIONS 에 넣고 바깥 WHERE 로 거른다. 05 §8-6 이 교정본.
--   2) 구 13 §5-5 의 `ORDER BY TOTAL_AD_COST DESC` 는 Snowflake 기본이 NULLS FIRST 라
--      광고비 NULL 채널사가 "top 10" 상위를 점유했다 → `NULLS LAST` 필수. 05 §8-8.
--   3) 구 13 §5-7 과 04 §6.3 의 "dim 20" 은 오류 — 실제 dim 19. 개수는 문서에 박지 말고
--      05 §8-10 쿼리(INFORMATION_SCHEMA)로 확인한다.
--   4) 스모크 "기대값" 은 전부 cs94293 리터럴이어서 타 계정에서 즉시 오탐이 된다 →
--      05 §8 은 **불변식 판정**(sv_val==fact_val, dig+brc==fap==combined)으로 재작성했다.
--   5) ✅ SV COMMENT 의 절대값(행수·합계·커버리지%·건수·적재기간) 전량 제거 — Agent 가 COMMENT 를
--      원천 답변 근거로 인용하므로 박아둔 수치는 적재량이 바뀌는 순간 거짓이 된다.
--      05 헤더에 **COMMENT 작성 규약 4개항**을 신설했다(수치 금지 · `[원천]`은 테이블·컬럼 이름만 ·
--      코드값 열거 · 원천 변경 시 계보문서 동기화). 04 §6.9-(8) 에 원칙 기록.
-- ============================================================================
