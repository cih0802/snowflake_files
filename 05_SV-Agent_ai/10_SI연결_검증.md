<!-- LLM-METADATA
doc_id: SV_AGENT_SI_CONNECT
doc_role: 6단계 — Cortex Agent CoWork(Snowflake Intelligence) 연결 + 자연어 스모크·회귀 검증 절차
project: GN_DW (굿네이버스)
created: 2026-07-22
depends_on: 08_AGENT_spec.md(2 Agent 스펙·배포절차·평가매핑), 07_평가셋_eval.md(ground truth), 06_검증쿼리_VQR.md(VQR)
scope: Phase-1 배포 2 Agent (AGENT_MEMBER·AGENT_OVERALL)
si_object: SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT (owner=ACCOUNTADMIN, 실측 존재 2026-07-22)
exec_by: 사용자 — 배포(GN_DW_ADMIN)·SI 연결(SI owner=ACCOUNTADMIN). 에이전트는 절차 작성·읽기전용 스모크
END-METADATA -->

# 6단계 — CoWork(Snowflake Intelligence) 연결·검증

> 사용자가 배포(save/publish)한 2 Agent를 **CoWork(SI object)에 연결**하고, `07_평가셋_eval.md` ground truth로 **자연어 스모크·회귀**를 수행한다.
> **실행 주체**: 배포/USAGE grant = `GN_DW_ADMIN`(08 §4). SI `ADD AGENT` = **SI object owner(ACCOUNTADMIN)** 또는 SI object MODIFY 권한 보유 역할. 스모크 검증 = `cortex_agent_query`(읽기전용) 또는 CoWork 챗.
> **선행 상태(2026-07-22 실측)**: `SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT` 존재 ✅ / `GN_DW.SERVING`에 Agent 0개(미배포) → **§1 선행 배포 필수**.
>
> ✅ **실행 완료(2026-07-22, CoCo 대행)**: §1 배포(save)·소유권 GN_DW_ADMIN 이전·USAGE grant(ANALYST/VIEWER/SERVICE) + §2 CoWork `ADD AGENT` 모두 완료. `SHOW AGENTS IN SCHEMA GN_DW.SERVING`·`… IN SNOWFLAKE INTELLIGENCE …` 각 2행 확인. 실행 SQL 로그 = `09_AGENT_spec_구현.sql`.
> ⚠️ **스모크(§3) 방법 제약**: `cortex_agent_query`(SNOWFLAKE.CORTEX.DATA_AGENT_RUN)는 **트라이얼 계정에서 차단**('Access denied for trial accounts') → 프로그램적 회귀 불가. **CoWork UI(https://ai.snowflake.com)에서 §3 문항을 수동 질의**하여 판정 채움.
>
> 🔴 **[2026-08-10 O57] 판정칸 채움 방식을 두 층으로 분리했다.** 종전에는 전 문항이 `⬜` 였고 그것이 「검증 미실행」과 「기대값 자체가 폐기값」을 구분하지 못했다.
> · **㉠ 값 회귀(SQL 층)** = gold SQL 을 직접 실행해 **기대값의 참·거짓을 먼저 확정**한다. O57 이 25문항 전량 실행 = **일치 14 · 교체 11**.
> · **㉡ 라우팅·가드레일(NL 층)** = Agent 가 올바른 SV·metric 을 고르고 비활성 지표를 산출하지 않는지. **SQL 로 대체 불가** → 사용자 CoWork 질의 필요(`⬜` 유지).
> 🔴 **㉠ 를 건너뛰고 ㉡ 만 하면 폐기값을 정답으로 채점한다** — 실제로 O56-C·O57 에서 두 번 적발됐다(P156).

---

## 0. 연결 아키텍처 (advanced 경로)

계정에 **명시적 SI object**(`SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT`, 02 §F 생성)가 존재 → 자동 노출이 아니라 **`ALTER SNOWFLAKE INTELLIGENCE … ADD AGENT`** 로 명시 등록해야 CoWork에 노출된다.

```
GN_DW.SERVING.AGENT_MEMBER  ─┐
                              ├─ ADD AGENT ─► SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT ─► CoWork(https://ai.snowflake.com)
GN_DW.SERVING.AGENT_OVERALL ─┘                    (object USAGE = 소비 3역할, 02 §F)
```

- 소비자는 **자기 default role**로 CoWork에서 Agent 실행 → 역할에 ① Agent USAGE(08 §4.3) ② SI object USAGE(02 §F) ③ SV REFERENCES,SELECT + GOLD SELECT(05·02) ④ ANALYTICS_WH USAGE + default warehouse 필요.
- Cortex 사용권(SNOWFLAKE.CORTEX_USER)은 PUBLIC 상속.

---

## 1. 선행 배포 (사용자 = GN_DW_ADMIN) — 08 §4

> 미완료 시 §2 진행 불가. 완료 확인: `SHOW AGENTS IN SCHEMA GN_DW.SERVING;` 가 2행.

1. **save**(semantic_studio): `cortex_agent_save` — `AGENT_MEMBER`·`AGENT_OVERALL` (file_path=`cortex_project/*.agent.yaml`, fqn=`GN_DW.SERVING.*`).
2. **publish**: `SHOW VERSIONS IN AGENT GN_DW.SERVING.AGENT_MEMBER;` 로 미게시 버전 확인 후 `cortex_agent_publish`(각 Agent).
3. **USAGE grant**(08 §4.3): `AGENT_MEMBER`·`AGENT_OVERALL` → `GN_DW_ANALYST`·`GN_DW_VIEWER`·`GN_DW_SERVICE`.

**확인 SQL**
```sql
SHOW AGENTS IN SCHEMA GN_DW.SERVING;                       -- 2행
SHOW GRANTS ON AGENT GN_DW.SERVING.AGENT_MEMBER;           -- USAGE: ANALYST/VIEWER/SERVICE
SHOW GRANTS ON AGENT GN_DW.SERVING.AGENT_OVERALL;
```

---

## 2. CoWork 연결 (SI object owner=ACCOUNTADMIN)

```sql
-- SI object에 Agent 등록
ALTER SNOWFLAKE INTELLIGENCE SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT ADD AGENT GN_DW.SERVING.AGENT_MEMBER;
ALTER SNOWFLAKE INTELLIGENCE SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT ADD AGENT GN_DW.SERVING.AGENT_OVERALL;

-- 등록 확인
SHOW AGENTS IN SNOWFLAKE INTELLIGENCE SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT;   -- 2행
```

> ⚠ **[2026-07-29 순서9-L] 위 `ADD AGENT` 는 재실행 시 에러다** — 배포 스크립트로 쓰려면 멱등 처리 필요.
> - `ADD AGENT IF NOT EXISTS` → **미지원**(syntax error, 실측)
> - 제거 구문은 `REMOVE AGENT` 가 **아니라 `DROP AGENT`** — `ALTER SNOWFLAKE INTELLIGENCE <si> DROP AGENT <fqn>`
> - → **멱등 구현체 = `09_1_AGENT_생성.sql` [4]** (`SHOW AGENTS IN SNOWFLAKE INTELLIGENCE` 사전 확인 후 분기). 재실행 시 `skipped` 반환. 🔴 [2026-08-04 O36] 구 `09_AGENT_spec_구현.sql` 은 DEPRECATED 스텁으로 09_1·09_2 로 분해됐다.
> - ⚠ 이 SHOW 결과가 `SHOW AGENTS IN ACCOUNT` 와 우연히 같을 수 있다(본 계정 2건 일치) → `DROP AGENT` 직후 0 · 재-`ADD` 후 1 을 확인해 **SI 멤버십 반영임을 실측 검증**했다(교훈 P26). 유사 가드 작성 시 동일 검증 필요.
>
> ⚠ **Agent spec 갱신은 재생성이 아니라 `09` [4-B]** — `CREATE OR REPLACE AGENT` 는 버전 이력을 초기화하고 **USAGE grant·본 SI 등록까지 파괴**한다(→ §1 USAGE·§2 ADD AGENT 재실행 필요). 상세 = `08_AGENT_spec.md` §4.1.

- (선택) CoWork 표시명·색상:
```sql
ALTER AGENT GN_DW.SERVING.AGENT_MEMBER  SET PROFILE = '{"display_name":"회원 분석","color":"#29B5E8"}';
ALTER AGENT GN_DW.SERVING.AGENT_OVERALL SET PROFILE = '{"display_name":"전사·예산 분석","color":"#11567F"}';
```
- object USAGE는 02 §F에서 소비 3역할에 부여 완료(추가 불필요). CoWork URL = `https://ai.snowflake.com`.

---

## 3. 자연어 스모크·회귀 검증 (08 §5 · 07 ground truth)

> 방법: `semantic_studio` `cortex_agent_query`(fqn+prompt, 읽기전용) 또는 CoWork 챗. 생성 SQL·답변을 07 gold SQL/값과 대조.
> **PASS 기준**: ① 올바른 SV 라우팅 ② metric/dimension 일치 ③ 값 일치(정수/원 동일, 비율 ±0.01%p) ④ 가드레일 준수. **FAIL**: SV/metric 오선택·값 불일치·비활성 임의 산출·fan-out 증폭.
> **판정칸 표기**(O57): `✅값` = gold SQL 재실행으로 기대값 확정(SQL 층 통과) · `🔴교체` = 재실행 결과 종전 기대값이 폐기값이어서 교체함 · `⬜NL` = Agent 라우팅·행동은 CoWork 수동 질의 필요.

### 3.1 AGENT_MEMBER 정확도 (07 §1~4-SV)

| # | 질문 | 기대 라우팅(SV·metric·dim) | 기대값 (2026-08-10 O57 실측) | 판정 |
|---|---|---|---|---|
| M3 | 2024년 납부율은? | MONTHLY · **PAYMENT_RATE_FEE** · CAL_YEAR=2024 | **85.77%** | ✅값(O56-D·**O57 재확인**) · ⬜NL |
| M4 | 연도별 납부율 추이(2023~2025) | MONTHLY · **PAYMENT_RATE_FEE** · CAL_YEAR | **86.05·85.77·85.65%** | ✅값(O56-D·**O57 재확인**) · ⬜NL |
| M5 | 회원구분별 **총수납액** | MONTHLY · **TOTAL_PAID_ALL** · MEMBER_TYPE_NAME | 개인=756.64B·기업=132.14B·단체=6.36B·(NULL)=0.04B | ✅값(4구분 전건 · **O57 재확인**) · ⬜NL |
| M9 | 전체 개발/중단 총건 | MONTHLY · TOTAL_DEV_CNT·TOTAL_STOP_CNT | **2,291,878** / 1,038,262 | 🔴교체(O57 · 개발) · ✅값(중단) · ⬜NL |
| E2/E3 | 개발/중단 고유 회원수 | EVENT · DEV/STOP_MEMBER_COUNT | **1,585,923** / 903,064 | 🔴교체(O57 · 개발 −26) · ✅값(중단) · ⬜NL |
| E4 | 전이유형별 건수·회원수 | EVENT · EVENT_TYPE | 개발 **2,291,878**/**1,585,923** · 중단 1,038,262/903,064 | 🔴교체(O57 · 개발 2건) · ✅값(중단 2건) · ⬜NL |
| S1 | 전체 발송수 | SERVICE · TOTAL_SEND_MEMBERS | 38,470,780 | ✅값(O57) · ⬜NL |
| S3 | 채널별 발송수 | SERVICE · TOTAL_SEND_MEMBERS · CHANNEL | MSG_AT 20,557,626·SND 8,300,272·EMAIL 7,811,121·PSTMTR 1,790,448·(미매핑)11,313 | ✅값(5구분 전건 · O57) · ⬜NL |
| P3 | 행사종류별 참여자수 | PARTICIPATION · TOTAL_PARTICIPANTS · EVENT_KIND | EVENT 718,438·(NULL)263,611·CRMN 152,077 | ✅값(O57) · ⬜NL |

> 🔴 **[O57] M9·E2·E4 교체 원인 = O24 `DEV_CNT` 정본 교정 미반영.** 종전 기대값 3,594,843 은 **개발원천 행수**이고 **개발 건수가 아니다** —
> 정본 공#121(개발 = 신규1·증액2·재후원4)에 따라 감액(3)·후원중단(5) **1,302,965행(36.24%)** 이 제외된다. 회원수 26명 갭도 같은 원인이다.
> ⇒ **Agent 가 3,594,843 을 「개발 건수」로 답하면 FAIL** 이다. 경위 정본 = 이슈원장 §O57 · 산식 정본 = `FACT_MEMBER_EVENT.sql` O24 블록.

### 3.2 AGENT_OVERALL 정확도 (07 §5)

| # | 질문 | 기대 라우팅 | 기대값 (2026-08-10 O57 실측) | 판정 |
|---|---|---|---|---|
| B1 | 전체 편성예산은? | BUDGET · TOTAL_PLAN_BUDGET | **547,614,848,306** | 🔴교체(O57) · ⬜NL |
| B2 | 전체 집행예산(ERP)은? | BUDGET · TOTAL_EXEC_BUDGET | **297,045,080,903** | 🔴교체(O57) · ⬜NL |
| B3 | 전체 집행율은? | BUDGET · EXEC_RATE | **54.24%** | 🔴교체(O57) · ⬜NL |
| B4 | 예산구분별 편성·집행·집행율 | BUDGET · BUDGET_CATEGORY | 지출 **298.60B/151.18B/50.63%** · 수입 **249.01B/145.86B/58.58%** | 🔴교체(O57) · ⬜NL |
| O1 | 전사 **총수납액**(회비+기부금)은? | MONTHLY · **TOTAL_PAID_ALL** | 895,178,309,108 | ✅값(O56-D·**O57 재확인**) · 회비만 = `TOTAL_PAID_FEE_BILLABLE` 768,800,286,349(재확인) · ⬜NL |

> 🟢 **[O57] B1~B4 교체는 결함이 아니다 — 적재 증량이다.** 종전 값은 **2026년 단독 적재 시점** 스냅샷이고 이후 2024·2025 가 추가 적재됐다
> (2026 단독 편성 503,070,876,000 = 종전 B1 과 정확 일치). `BRONZE_ERP` → `SILVER.ERP_BUDGET` → `GOLD.FACT_BUDGET` **3개년 전건 오차 0**(행수 75,996 동일)으로 계층 정합을 확증했다.
> ⚠️ **2026 은 진행연도라 집행액이 계속 변한다** ⇒ B2·B3·B4 집행 계열은 재적재마다 재실행 대상이다.

### 3.3 가드레일 스모크 (ⓖ — custom instruction 검증, 산출 금지 확인)

> ⛔ **이 절은 원리적으로 SQL 로 채울 수 없다** — 값이 아니라 **Agent 의 응답 행동**을 본다.
> 트라이얼 계정에서 `DATA_AGENT_RUN` 이 차단되므로 **사용자가 CoWork UI 에서 질의**해야 판정된다. `⬜` 는 「미검증」이 맞다.

| # | 질문 | 기대 Agent 행동 | 판정 |
|---|---|---|---|
| M10ⓖ | 캠페인별 납부율 알려줘 | "캠페인 FK 미적재 → Phase-2 제공 예정" 안내(산출 금지) | ⬜NL |
| M11ⓖ | 활동회원수(월말 활동회원)는? | "ACTIVE_CNT 미적재 → Phase-2" 안내 | ⬜NL |
| E5ⓖ | 평균 유지기간(가입~중단)은? | "페어링 불가 → Phase-2" 안내(산출 금지) | ⬜NL |
| S5ⓖ | 발송 성공률(수신율)은? | "SUCCESS/FAIL 미적재 → Phase-2" 안내 | ⬜NL |
| P5ⓖ | 캠페인별 참여자수 | "CAMPAIGN_SK 미적재 → Phase-2" 안내 | ⬜NL |
| B5ⓖ | 캠페인별 ROI(개발단가) | "CAMPAIGN_SK·비용·FMM 연계 미적재 → Phase-2(신9~11)" 안내 | ⬜NL |
| G-납부율 | (그냥) 납부율 알려줘 | 기간(연/월) 스코프로 재해석·안내. 🔴 **[O57]** 경계 근거는 **폐기값 100.36% 가 아니라** 정본 `PAYMENT_RATE_FEE` 전기간 **86.19%** 다 | ⬜NL |
| G-Unknown | 행사명별 참여자수 | 미매핑(EVENT_SK=0) ~23% 커버리지 고지 | ⬜NL |
| 🆕 G-개발건수 | 전체 개발 건수는? | **2,291,878** 로 답해야 한다. 3,594,843(개발원천 행수)으로 답하면 **FAIL** — 감액·후원중단이 개발로 계상된 폐기 정의다(O24) | ⬜NL |

> 스모크 예(cortex_agent_query): `{"action":"cortex_agent_query","fqn":"GN_DW.SERVING.AGENT_MEMBER","prompt":"2024년 납부율은?"}` → 답변·생성 SQL 확인.

---

## 4. VQR 등록 (정확도 1순위 · 06 §3, 병행 권장)

> Agent 정확도의 근본은 SV 스티어링. 06 §3 SV별 검증쿼리를 verified query로 등록하면 유사 질문의 SQL 생성이 안정화된다.

- semantic_studio `semantic_view/vqr_management` 또는 SV YAML의 `verified_queries`(AI_VERIFIED_QUERIES)로 SV별 대표 질의 등록.
- 우선순위: 납부율(연/월 스코프)·회원구분별 회비·전이유형별·채널별 발송·행사종류별·예산구분별(각 07 gold SQL과 동일).
- 🔴 **[O57] VQR 에 등록할 SQL 은 반드시 실행해 값을 확인한 것만 쓴다** — 폐기값이 VQR 로 들어가면 Agent 가 그것을 정답 패턴으로 학습한다(P156).

---

## 5. 완료 기준(DoD) & 다음

**DoD**
- `SHOW AGENTS IN SNOWFLAKE INTELLIGENCE …` = 2행(연결 확인). ✅
- 🆕 **㉠ 값 회귀** — §3.1/§3.2 기대값을 gold SQL 로 **전량 실행 확정**. ✅ **[2026-08-10 O57] 25문항 실행 = 일치 14 · 교체 11 · 미실행 0**
- ㉡ **NL 라우팅** — §3.1/§3.2 문항을 Agent 에 질의해 SV·metric 선택과 값이 일치. ⬜ **사용자 CoWork 질의 필요**
- ㉡ **가드레일** — §3.3 전부 PASS(비활성 임의 산출 0·기간스코프·커버리지 고지). ⬜ **사용자 CoWork 질의 필요**
- 소비 역할(VIEWER/SERVICE)로 CoWork 접속 시 Agent 노출·응답 확인. ⬜

**FAIL 시 대응**
- 라우팅 오류 → 08 tool `description`/`orchestration` 문구 보강 후 `cortex_agent_write`→save→publish 재배포.
- 값 불일치 → 🔴 **먼저 어느 쪽이 폐기값인지 판정한다**(P156). Agent 가 틀렸는지, 평가셋 기대값이 stale 인지를 gold SQL 재실행으로 가른 뒤 조치한다. 그 다음 해당 SV metric 재검토(06 §3 회귀) 또는 VQR 등록.
- 가드레일 미준수(비활성 산출) → 08 `instructions.system` 비활성 목록 강화.

**다음**: `11_거버넌스_운영.md` — 사용량/비용 모니터링(08_mornitoring)·비용 쿼터·알림·폐루프 → `00_README.md` wrap-up 갱신.

---
_Co-authored with CoCo_
