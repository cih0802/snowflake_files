> #### 🟢 [2026-08-28 O107-C] 조각 폴더화 — 도구(`--to-outdir`) 신설 + **99_NEXT 시범 이전**(사용자 승인)
>
> **배경(사용자 제안)** = 「허브와 조각이 같은 폴더에 있어 사람이 한눈에 보기 어렵다.
> 허브가 있는 문서는 조각을 폴더에 넣는 방안은 어떤가 · 기존 설계와 충돌하나?」
>
> **판정 = 설계와 충돌하지 않는다.** 폴더 방식은 이미 `R1-6-10` 의 정식 방식이고
> 10개 허브 중 2개(`01_세션이력`·`90_해소완료_로그`)가 그렇게 쓰고 있었다
> ⇒ 🔴 **오히려 혼재가 규약과 어긋난 상태**였다. 다만 **이행에 실제 충돌 4건**이 있었고
> 그중 1건은 **세션 시작을 붕괴**시키는 것이었다(아래 ㉠).
>
> **착수 시 실측(형제 방식 8문서 · 조각 89개 = `20_issue/` 75 + 루트 14)**
> * ㉠ 🔴🔴 **`session_brief.family_lines('99_NEXT_SESSION.md')` 가 `where='sibling'` 기본값**을 썼다
>   ⇒ 폴더로 옮기면 브리핑이 **허브만 읽어 착수표·인수인계를 조용히 비운다**(= `R1-1` 착수 경로 붕괴).
>   🟢 처방 = `doc_census.chunk_paths` 가 **허브의 `SPLIT-OUTDIR` 마커를 우선**하게 고쳤다
>   (조각 위치의 정본은 마커다 · 게이트 3종은 이미 그렇게 한다) ⇒ 호출자 전부가 자동 추종한다.
> * ㉡ 🔴 **이전 연산 자체가 없었다** — `--outdir` 는 최초 `build` 전용.
>   허브 마커만 바꾸면 `--rebalance` 가 폴더에 쓰지만 **형제가 남아 본문이 두 곳에** 존재한다.
> * ㉢ 🟠 손으로 고칠 분모 = `doc_census.FAMILIES` · `doc_line_length_gate.CANON_GLOB`.
>   🔴 **`decision_closure_gate` 2곳은 아직 안 고쳤다** — `DECISION_GLOB = "30_설계_의사결정-*.md"` 는
>   **형제 파일명 직접 의존**이라 30_설계를 옮기면 **분모 0건**이 된다(`P106` 재현) ·
>   `ISSUE.glob("*.md")` 는 1단계만 본다 ⇒ **`20_issue/` 이전의 선행 조건**이다.
> * ㉣ 🟠 경로 인용 = 총 **415건** 중 **349건은 자동 생성물**(허브·`00_BRIEF`·`92`·golden)이라 재생성으로 해소.
>   99_NEXT 이전으로 깨진 **수동 인용 21건 / 12파일** 중 **기능적 참조 0건**(전부 서술·주석)
>   ⇒ 과거 시점 기록은 고치지 않는다(`R2-8-3`). 🔴 다만 **경로 인용은 행번호 인용과 다르다** —
>   행번호는 stale 이지만 경로는 **파일 부재**다(조문 `R1-6-21` 에 그 구분을 명문화했다).
>
> **신설 = `split_doc.py --to-outdir <이름>_조각 [--apply]`**(조문 `R1-6-21`)
> * 🔴 **순서가 안전이다** = ㉠ 폴더에 먼저 쓴다 → ㉡ 되읽어 **concat 바이트 동일** 확인(blocking) →
>   ㉢ **그때만** 형제 **개별 삭제**(`R1-7-7`) → ㉣ 허브 마커 갱신.
>   ⇒ 어느 단계에서 멈춰도 **본문이 최소 한 곳에 온전**하다.
> * 거부 조건 2 = 폴더명에 **`_조각` 접미 없음**(`R1-6-10` 실사고 집행) · **이미 폴더**(멱등).
> * 기본 **dry-run** · 음성 테스트 축8 신설(`test_split_doc_toc.py` **8축 69단정**).
>
> **집행 결과 (99_NEXT 만 · 사용자 선택 「시범 이전」)**
> * 14조각 → `99_NEXT_SESSION_조각/` · **concat SHA256 `38e0fbab99e87153` 동일** ·
>   형제 개별 삭제 14 · **잔존 0**(로컬 `os.listdir` + `cortex ws ls` **2축 확인** · `OPS-3`).
> * 🟢 **루트 `.md` 파일 24 → 10**(사용자가 지목한 문제의 직접 해소).
> * 게이트 = `--verify` 6종 PASS(토큰 유실 0) · `doc_census` stale 0 · 제목 유실 0 · 행 유실 0 ·
>   유형·용량 통과 · `doc_line_length_gate` **분모 자기검사 14/14**(폴더 패턴 14건 인식) ·
>   `session_brief` 재생성 후 **착수표 11 · 인수인계 5 유지**(좌표가 폴더 경로로 전환됨).
> * 🔴 **stale read 재발 방지** = 이전 직전 `grep -c "0-MMM"` = 3 확인 후 집행(`R1-7-2` 로는 부족하다 · §O107-B).
>
> **🟠 남긴 것** = `20_issue/` 7문서(75조각)는 **형제 유지**. 사용자 선택이 「시범」이었고,
> 🔴 이전하려면 **`decision_closure_gate` 2곳 수정이 선행**이다(위 ㉢).
> ⇒ 인수인계 `99_NEXT §0-MMM ▣QQQ` 에 등재했다.
