> #### 🟢 [2026-08-28 O105] 이슈1·2 실적재 검증 → Agent 2종 보강·배포 · 추천질문 축약 · WIDE 동결 전환 · SV 규칙7 시정
>
> **계정 `ZL50263`**(O102 정본과 동일 · `R2-8-4-a` 3요소 = 무엇을·어디서·언제).
> 🔒 라벨 = `id_collision_gate --next O` = **O105**(정의 104 ↔ 참조 104 **양축 일치**) · `R1-4-3` 이행
> (산출물 저작 **전에** 원장 §1 행을 먼저 썼다).
> 🔴 **장문 경위·근거 정본 = `02_상태상세_대시보드_갱신형.md §O105`** — 이 항목은 서술 요약이다.
>
> ##### ▣ A. 착수 지시와 처리 순서 (사용자 5단 지시)
>
> ① 「데이터 실적재 기준으로 이슈1(캠페인 7속성)·이슈2(후원/법인구분)가 gold·sv·agent 까지 반영됐는지 확인」
> ② 「agent 정의 yaml 을 읽고 수정」 ③ 「추천질문 최대 15개 제한 · 유형 분류해 10개 정도로 축약」
> ④ 「`--rollover` 승인」 ⑤ 「wide 수정」 ⑥ 「SV 에 수치 넣지 마라 했는데 또 넣었다 — 이슈문서 참고해 수정」
> ⑦ 「세션 검토 + 추측 채움분 실측 + 리소스 과다분은 다음 세션 프롬프트로」.
>
> ##### ▣ B. 🟢 실적재 검증 — SILVER·GOLD·SV 전건 반영 / Agent 만 미반영
>
> · **원천 계보가 요청 명세와 정확히 일치**한다 — `CRM_MEMBER_DEV.sql:99·105` =
>   `source('bronze_crm','TM_MM_FDRM_MBER_DVLP_AMT')` ⟵ `CMPGN_CD` ⟶ `ref('CRM_CAMPAIGN')`(= `TM_CM_CMPGN_MNG`).
> · 반영 = SILVER `CRM_MEMBER_DEV` **18컬럼**(ord 25~42) · GOLD `FACT_MEMBER_EVENT` `*_AT_EVENT` **18컬럼**
>   (ord 35~52) · `DIM_CAMPAIGN` ord 21~28 · `WIDE_MEMBER_EVENT` ord 53~62 · `SV_MEMBER_EVENT` **9차원**.
> · 승계 손실 **0** — SILVER 3,594,843행 ↔ GOLD 18컬럼 COUNT 전건 동일(3,594,825/3,594,843).
> · **이슈2 값 수준 검증** = DW 라벨 ↔ `TC_CMMN_DTL_CD` **전건 일치**(CM035 1정기후원/2일시후원 ·
>   CM019 A통합/I사단/S사복) · **미매핑(코드 有·라벨 無) 0/36,164 양축** · 창작 라벨 **0**.
>   결손은 **원천 승계뿐** — BRONZE `TM_CM_CMPGN_MNG` 36,163 중 두 컬럼 원천 NULL **1,459**
>   ⇒ `DIM_CAMPAIGN` 34,704 = 36,164 − 1,459 − 센티넬 1 **분모가 정확히 상쇄**된다.
> · 🔴 **Agent 계층만 미반영**이었다 — 스펙 3종(파일·라이브) 전건에서 `MKTG_UTM`·`CMMN_BRND`·`SPNSR_DIV`·
>   `CPR_DIV`·`공통브랜드`·`후원구분`·`법인구분`·`동결` 토큰 **0건**(`O98 ▣BB` 3순위가 그대로 열려 있었다).
>
> ##### ▣ C. 🔴🔴 최대 발견 — 「법인」이 두 축인데 Agent 가 한 축만 알고 「산출 불가」로 답하게 돼 있었다
>
> `AGENT_MARKETING` 은 *"**법인 단위는 컬럼이 전건 비어 있어**(값 미주입) 산출 불가"*,
> `AGENT_MEMBER` 는 *"상위 조직(본부/지부·팀·**법인**)별 분해는 여전히 산출 불가"* 로 **단정**하고 있었다.
> 그 문장은 **조직 계층의 법인**(`DIM_ORG.CORP` · 전건 NULL)을 가리키는데, 이제 **세부캠페인 법인구분**
> (`CPR_DIV_NM` · CM019)이 별도로 실재한다 ⇒ 현업이 「법인구분별 개발건」을 물으면
> **산출 가능한 축을 「불가」로 답하는 오답**이 난다. 🟢 두 스펙에 **두 축 구별**을 명시해 닫았다.
> 🟢 이것이 이 세션에서 가장 값어치 있는 발견이다 — 컬럼을 추가하는 작업은 **부정형 서술을 회수하는
> 작업과 한 쌍**이다(`P61` 축의 재확인 · Agent 는 「없다」고 적힌 문장을 그대로 답한다).
>
> ##### ▣ D. 🟢 Agent 보강·배포·기능검증 (배포는 사용자 실행 · `R4-1`·⑳ 준수)
>
> · 수정 = `AGENT_MEMBER`·`AGENT_MARKETING` `analyst_member_event` description(차원 4종 + 9속성
>   **동결값 성격** + **법인 두 축 구별** + DEV 전용/STOP NULL + UTM 미등재 경고) · MEMBER 는
>   `instructions.orchestration` 라우팅 문단 + `system` 활성목록 병기 추가.
> · 🔴 **수치를 일부러 넣지 않았다** — 스펙 자체 규약(*"원천 답변에 수치를 넣지 않는다"*)이자 규칙7 이며,
>   실제로 SV 지시문의 하드코딩 `약 36%` 가 stale 이 된 원인이다 ⇒ 「채움이 낮으니 **조회로 확인**하고
>   부분집합임을 밝혀라」로 썼다.
> · 배포 경로 = `05_SV-Agent_ai/09_2_AGENT_버전업.sql`(`ADD VERSION FROM`) — 착수표 **⑳** 대로
>   `cortex_agent_save`/`deploy` 는 쓰지 않았다(live 버전이 `ADD VERSION FROM` 을 거부한다).
> · 결과 = `AGENT_MEMBER` **VERSION$9** · `AGENT_MARKETING` **VERSION$5** 둘 다 `is_default=true` ·
>   신규 토큰 라이브 실재 ↔ 직전 `VERSION$7`·`VERSION$4` 는 **전건 0**(반영이 이 배포에서 처음 실렸다).
> · 🟢 **기능 검증** = 「세부캠페인 법인구분별 개발건수」 질의가 `analyst_member_event` 로 라우팅돼
>   **통합 195,796 · 사단 1,005 · 사복 1** 반환. 답변이 *「조직 계층의 법인과는 다른 세부캠페인
>   법인구분(CM019)」*·*「사건 시점 동결값」*·*「개발 전용이라 중단건으로 쓸 수 없다」* 를 **자발 고지**
>   ⇒ ▣C 의 오답 경로가 실제로 닫혔음이 실증됐다.
> · 🟢 부수 확인 = `cortex_agent_read(source=workspace)` 는 **정상 동작**한다. O98 이 「재현 가능 실패」로
>   남긴 것은 `source=snowflake` 경로였다 ⇒ 그 미결의 **범위가 좁혀졌다**(원인 규명은 여전히 미결).
> · 🟢 이력 `§O98`(27:133) 의 잔여 *"`AGENT_MARKETING` 소유자 = `ACCOUNTADMIN` ⇒ 소유권 이관"* 은
>   **stale 로 확인**됐다 — `SHOW AGENTS` 실측 3종 전부 owner = `GN_DW_ADMIN`.
>
> ##### ▣ E. 🟢 추천질문 축약 (사용자 지시 ③ · 상한 15 규약 신설)
>
> 실측 = `AGENT_MEMBER` **34** · `AGENT_MARKETING` **11** · `AGENT_OVERALL` **10** ⇒ MEMBER 만 초과.
> 🔴 **O84 가 30→10 으로 줄였는데 34 로 복귀**했다 — 문항은 축이 늘 때 함께 늘어 **한 방향으로만 증가**한다.
> 유형 분류(도구 귀속) = 회비 7 · 개발사건 8 · 코호트 5 · 예측 6 · 목표 3 · 원천 2 · 발송 1 · 행사 1 · 활동회원 1.
> 축약 원칙 = **도구 1개당 대표 1문항 + 원천 규칙 1문항**, 같은 도구 안의 축 나열은 1문항으로 병합
> (연령대+지역 · 후원구분+법인구분 · 부서+월 · 이탈률 3종) ⇒ **34 → 12**(11개 도구 전건 커버).
> 🔴 system/orchestration 에 이미 처방이 있는 특수 사례(「10대 미만이 왜 많은가」)는 **중복 노출 제거**.
> 🟢 규약을 설계 정본 `05_SV-Agent_ai/08_AGENT_spec.md` 머리말에 등재했다(상한 15 · 목표 10 내외).
> ⬜ **게이트 미비** — 이 상한을 검사하는 게이트는 없다(`R3-9` 축).
>
> ##### ▣ F. 🟢 WIDE 동결 전환 (사용자 지시 ⑤ · 발견 ㉠ 처분)
>
> 착수 시 적발 = 같은 캠페인 9속성이 **두 표면에서 다른 소스**였다 —
> `WIDE_MEMBER_EVENT` = `DIM_CAMPAIGN` **실시간 조인**(`c.*`) ↔ `SV_MEMBER_EVENT` = **동결값**(`fme.*_AT_EVENT`).
> `DEC-43` 이 채택한 축은 동결값이므로 **규약에서 벗어난 쪽은 WIDE** 였다.
> 🟢 **지금 바꾼 것이 옳다** — 전환 시점 두 소스 값이 **전건 동일**(불일치 0 / DEV 3,594,843 ·
> `CAMPAIGN_SK` PK 조인)이라 **값 변화가 0** 이다. 마스터가 정정된 뒤에 바꾸면 과거 리포트가 조용히 달라진다.
> · 구현 = 모델 11컬럼 소스 전환 + `_wide_schema.yml` 11 description 재작성(라이브 뷰 컬럼 COMMENT 정본).
> · 🔴 **컬럼명은 바꾸지 않았다**(`CAMPAIGN_*` 유지) — 소비측(Streamlit·산출물) 파괴 회피 · SV 가
>   친화명을 유지한 것과 같은 판단이다.
> · ⚠️ **NULL 의 뜻이 바뀐 유일한 지점** = 중단(STOP) 행. 종전엔 `CAMPAIGN_SK=0` 센티넬 조인으로
>   `'(미매핑)'` 이 나왔고 이제는 **NULL** 이다 ⇒ yml 문안에 명시했다(0·'미상' 대체 해석 금지 · `R2-7`).
> · 🟢 **신설 게이트** `scripts/wide_select_yml_gate.py` — SELECT 별칭 ↔ yml `columns[]` 를 **순서까지** 대조.
>   결과 **73 ↔ 73 순서 일치**. 🔴 초판 대조기는 한 줄 다중 컬럼을 놓쳐 44/73 으로 세고 「불일치」로
>   오판했다 ⇒ **분할 단위를 줄이 아니라 콤마로** 고쳤다(`P128` 축의 두 번째 발생 · 아래 ▣H 참조).
> · ⏸ **`dbt build` 는 실행하지 않았다**(`R4-1` 정지점) — 사용자 실행 대기.
>
> ##### ▣ G. 🟢 SV 규칙7 시정 + 🔴 게이트 분모 공백 2건 적발 (사용자 지시 ⑥)
>
> 규칙7 정본 = *"COMMENT 에는 코드값만 · 실측 수치(행수·합계·커버리지%·건수·금액·적재기간)는 문서10과
> 이슈원장에"*(`04_SV_설계.md §6.9-(8)` · `05_0` COMMENT 작성 규약 (1)).
> 🔴🔴 **공백 1 = `audit_ddl_rule7` 의 분모에 SV DDL(`05_*`)이 아예 없다** — `06_DDL`(GOLD) + SILVER DDL
> 뿐이다. 그래서 SV COMMENT 에 수치가 들어가도 **어떤 게이트도 잡지 않았다**(O59-N 이 SILVER 에서 겪은 것과
> 동형). 🔴 SV COMMENT 는 Analyst 가 프롬프트 context 로 **직접 읽는 표면**이라 파급이 GOLD DDL 보다 크다.
> 🟢 **신설 스캐너** `scripts/sv_rule7_scan.py` — 위반 패턴·의미 예외를 `audit_ddl_rule7`·`sv_unit_gate` 와
> **1벌로 공유**하고(두 게이트가 같은 규칙을 다르게 판정하는 `P182` 회피) **라이브 도달 여부로 분리**한다:
> `COMMENT`/지시문 리터럴 안 = 진짜 위반 ↔ `--` SQL 주석 = 문서 stale(경고).
> 🔴🔴 **공백 2 = 내가 만든 그 스캐너도 초판에 분모 공백이 있었다** — `COMMENT =` 만 보고
> **`AI_SQL_GENERATION` 지시문을 놓쳤다**(그 구문은 `=` 가 없다). 실제로 그 안에 있던 `약 36%` 를
> 「문서 주석」으로 **오분류**했다 ⇒ 정규식에 지시문 키워드를 편입해 시정했다(`P224`·`P106`).
> · 실측 = **라이브 도달 17건 · 문서 주석 47건**. 그중 **오탐 7건**을 문맥 실측으로 확정 —
>   `10,000`(금액÷10,000 = CONF-2 **정의 상수**) 1건 · `95%`(**신뢰구간 정의**) 6건 ⇒ 유지.
> · 🟢 **시정 = `SV_MEMBER_EVENT` 2곳**(컬럼 COMMENT L133 + `AI_SQL_GENERATION` L178)에서 `약 36%` 제거,
>   「조회로 확인하라 + 규모는 §O105 참조」로 교체 ⇒ 이 SV 라이브 도달 위반 **0**.
> · 🟠 **잔여 진짜 위반 10건** = `05_10_MEMBER_SPONSOR_BIZ` 8 · `05_1_MEMBER_MONTHLY` 1 · `05_8_DEV_ACHIEVEMENT` 1.
>   시정은 **SV 라이브 재배포**를 부르므로(`R4-4-3` 승인 대상) 다음 세션으로 넘긴다.
>
> ##### ▣ H. 🔴 자기적발 — 같은 결함(`P128` 분모 미확인)을 한 세션에 두 번 냈다
>
> ㉠ **WIDE↔FME 동결값 대조를 업무키로 조인**(`DATE_SK`+`MEMBER_DK`+`EVENT_TYPE`+`SPNSR_AMT`+`DVLP_DIV_CD`)해
>   **7,005,277행**(> DEV 3,594,843)을 만들고 불일치 **45만~62만**을 보고했다. 그 키는 FME 에서 유일하지
>   않다(**팩트 PK 미선언**이 SV COMMENT 에 이미 적혀 있었다) ⇒ `CAMPAIGN_SK` PK 조인으로 **0** 정정.
> ㉡ **SELECT↔yml 대조기**가 한 줄 다중 컬럼을 놓쳐 **44/73** 으로 세고 「순서 불일치」로 오판했다.
> 🔴 **두 건의 공통 뿌리 = 조인·추출 결과의 행수(분모)를 기대값과 먼저 대조하지 않은 것**이다.
> 🟢 **교훈** = 팬아웃·누락은 불일치를 **만들어내거나 지운다** — 대조 전에 **분모 일치를 먼저 확인**한다.
> 🟢 두 건 모두 **보고 전에 자기적발**해 정정했고(사용자에게 잘못된 판정이 최종 전달되지 않았다),
> ㉡ 는 그 자리에서 **재사용 가능한 게이트로 승격**했다.
>
> ##### ▣ I. 🔴🔴 동시 편집 사고(`C7`) — 게이트 3종이 각각 다른 이유로 침묵했다
>
> 본 세션이 원장을 전량 읽은 뒤(10:30 KST) 라벨을 선점 등재하려는 사이 **타 세션이 10:42 KST 같은 `O105`
> 라벨 행을 삽입하고 허브를 `--republish`** 했다(허브 SHA `41b1c4e8…` → `70130b6d…`). 본 세션은 10:45 KST
> `R1-7-2` 해시 안정성을 확인했으나 **그 시점에 상대 쓰기가 이미 완료**돼 2회 읽기가 동일했다 ⇒ 통과.
> 10:46 KST 삽입으로 **행 2개**가 됐다. `index_row_gate`(행 키가 달라 중복 아님) · `id_collision_gate`
> (둘 다 참조 형태라 정의 축 밖) · `R1-7-2`(**내 직전 읽기와 대조하지 않는다**) **3종 전부 🟢**.
> 🟢 **처방 후보 = `R1-7-2` 에 「직전 읽기 해시 보존·대조」를 추가**한다 — 현 조문은 **부분 반환(`C5`)** 은
> 잡고 **완료된 타 세션 쓰기(`C7`)** 는 구조적으로 못 잡는다 ⇒ **두 사고 유형에 필요한 게이트가 다르다.**
> ⇒ 사용자 승인으로 중복 2행을 1행 통합했다(스냅샷 `_archive/00_INDEX_이슈원장-001.md.O105-pre-merge`).
>
> ##### ▣ J. 게이트·검증 (전수)
>
> `split_doc --verify` 🟢(00·02 양 허브 · 유실 0 · 상한 초과 0) · `index_row_gate` 🟢(유실 0 · 중복 0) ·
> `id_collision_gate` 🟢 · `doc_heading_gate` 🟢(신설 5 · 유실 0 ⇒ **골든 발행** `R3-9 ㉢`) ·
> `clause_order_gate` 🟢(조문 88 · 역전 0) · `line_len` 🟢(전 파일 상한 초과 0) ·
> `doc_type_gate` 🟡(축2 초과 0 · 여유 부족 경고) · `sv_identifier_gate` 🟢 ·
> `wide_select_yml_gate` 🟢(신설 · 73↔73) · `sv_rule7_scan` 신설(경고 등급).
> 🟠 **`agent_tool_claim_gate` FAIL 1건은 선재 결함으로 실증**했다 — 편집을 메모리에서 되돌려 재측정해도
> 판정 동일(`-1 ↔ +1`)이고 지목 도구는 이 세션이 만지지 않은 `analyst_member_sponsor_biz` 다.
> 🔴 원인 = `stance()` 가 스펙을 **yaml 파싱 없이 원문 그대로** 읽고 문장을 자르므로 **YAML 줄바꿈이 문장
> 경계**가 된다 ⇒ **같은 산문이 줄바꿈 위치에 따라 반대로 채점**된다(첫 neg-only 문장에서 즉시 `return -1`
> 하는 순서 의존까지 겹친다). 로직 변경은 기존 21건 분류를 바꿔 회귀 대조가 필요해 **손대지 않았다**.
> · `R2-5` 스테이지 실체 확인 완료 · 편집 대상 전건 `R1-7-2` 해시 안정성 선확인 · 스냅샷 5종 `_archive/`.
>
> ##### ▣ K. 🟠 잔여 (다음 세션 · 정본 = `99_NEXT_SESSION §0-KKK`)
>
> ① **`dbt build --select WIDE_MEMBER_EVENT`**(사용자 실행) + 전환 후 회귀(값 불변·STOP NULL 전환 확인)
> ② SV 규칙7 잔여 **10건** 시정 + 라이브 재배포(승인) ③ `agent_tool_claim_gate` 로직 수정(승인·회귀)
> ④ `R1-7-2` 조문 보강(`C7` 축) ⑤ 추천질문 상한 게이트 신설 ⑥ `09_2` 버전 COMMENT 「추천질문 31」 stale(실제 12)
> ⑦ `cortex_agent_read(source=snowflake)` 실패 원인(O98 승계) ⑧ `MKTG_UTM` 고아코드 `192` 현업 회신(`N-8`)
> ⑨ `SV_MEMBER_COHORT.CAMPAIGN_NAME` 시점 혼합 현업 회신(`N-9`)
